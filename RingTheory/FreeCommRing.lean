/-
Copyright (c) 2019 Kenny Lau. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Kenny Lau, Johan Commelin
-/
module

public import Mathlib.Algebra.MvPolynomial.Equiv
public import Mathlib.Algebra.MvPolynomial.CommRing
public import Mathlib.Logic.Equiv.Functor
public import Mathlib.RingTheory.FreeRing

/-!
# Free commutative rings

The theory of the free commutative ring generated by a type `α`.
It is isomorphic to the polynomial ring over ℤ with variables
in `α`

## Main definitions

* `FreeCommRing α`   : the free commutative ring on a type α
* `lift (f : α → R)` : the ring hom `FreeCommRing α →+* R` induced by functoriality from `f`.
* `map (f : α → β)`  : the ring hom `FreeCommRing α →*+ FreeCommRing β` induced by
                           functoriality from f.

## Main results

`FreeCommRing` has functorial properties (it is an adjoint to the forgetful functor).
In this file we have:

* `of : α → FreeCommRing α`
* `lift (f : α → R) : FreeCommRing α →+* R`
* `map (f : α → β) : FreeCommRing α →+* FreeCommRing β`

* `freeCommRingEquivMvPolynomialInt : FreeCommRing α ≃+* MvPolynomial α ℤ` :
    `FreeCommRing α` is isomorphic to a polynomial ring.

## Implementation notes

`FreeCommRing α` is implemented not using `MvPolynomial` but
directly as the free abelian group on `Multiset α`, the type
of monomials in this free commutative ring.

## Tags

free commutative ring, free ring
-/

@[expose] public section

assert_not_exists Cardinal

noncomputable section

open Polynomial

universe u v

variable (α : Type u)

/--
If `α` is a type, then `FreeCommRing α` is the free commutative ring generated by `α`.
This is a commutative ring equipped with a function `FreeCommRing.of : α → FreeCommRing α` which has
the following universal property: if `R` is any commutative ring, and `f : α → R` is any function,
then this function is the composite of `FreeCommRing.of` and a unique ring homomorphism
`FreeCommRing.lift f : FreeCommRing α →+* R`.

A typical element of `FreeCommRing α` is a `ℤ`-linear combination of
formal products of elements of `α`.
For example if `x` and `y` are terms of type `α` then `3 * x * x * y - 2 * x * y + 1` is a
"typical" element of `FreeCommRing α`. In particular if `α` is empty
then `FreeCommRing α` is isomorphic to `ℤ`, and if `α` has one term `t`
then `FreeCommRing α` is isomorphic to the polynomial ring `ℤ[t]`.
One can think of `FreeRing α` as the free polynomial ring
with coefficients in the integers and variables indexed by `α`.
-/
/-
**FreeCommRing** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：FreeCommRing (α : Type u) : Type u
参数：α : Type u。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If `α` is a type, then `FreeCommRing α` is the free commutative ring generated b
y `α`.
This is a commutative ring equipped with a function `FreeCommRing.of : α → FreeC
ommRing α` which has
the following universal property: if `R` is any commutative ring, and `f : α → R
` is any function,
then this function is the composite of `FreeCommRing.of` and a unique ring homom
orphism
`FreeCommRing.lift f : FreeCommRing α →+* R`.

A typical element of `FreeCommRing α` is a `ℤ`-linear combination of
formal products of elements of `α`.
For example if `x` and `y` are terms of type `α` then `3 * x * x * y - 2 * x * y
 + 1` is a
"typical" element of `FreeCommRing α`. In particular if `α` is empty
then `FreeCommRing α` is isomorphic to `ℤ`, and if `α` has one term `t`
then `FreeCommRing α` is isomorphic to the polynomial ring `ℤ[t]`.
One can think of `FreeRing α` as the free polynomial ring
with coefficients in the integers and variables indexed by `α`.
-/
def FreeCommRing (α : Type u) : Type u :=
  FreeAbelianGroup <| Multiplicative <| Multiset α
deriving CommRing, Inhabited

namespace FreeCommRing

variable {α}

/-- The canonical map from `α` to the free commutative ring on `α`. -/
/-
**FreeCommRing.of** 是 Mathlib 中的一个定义，位于命名空间 `FreeCommRing`。
形式化陈述：of (x : α) : FreeCommRing α
参数：x : α。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The canonical map from `α` to the free commutative ring on `α`.
-/
def of (x : α) : FreeCommRing α :=
  FreeAbelianGroup.of <| Multiplicative.ofAdd ({x} : Multiset α)
/-
**FreeCommRing.of_injective** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：of_injective : Function.Injective (of : α -> FreeCommRing α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Function.Injective.comp`：∀ {α : Sort u_1} {β : Sort u_2} {γ : Sort u_3} 
{g : β → γ} {f : α → β},   Function.Injective g → Function.Injective f → Functio
n.Injective (…
· 使用定理 `FreeAbelianGroup.of_injective`：of_injective : Function.Injective (of : α
 -> FreeAbelianGroup α)
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `Multiset.coe_eq_coe`：coe_eq_coe {l₁ l₂ : List α} : (l₁ : Multiset α) = l
₂ ↔ l₁ ~ l₂
· 使用定理 `List.singleton_perm_singleton`：∀ {α : Type u_1} {a b : α}, [a].Perm [b] 
↔ a = b
-/
theorem of_injective : Function.Injective (of : α → FreeCommRing α) :=
  FreeAbelianGroup.of_injective.comp fun _ _ =>
    (Multiset.coe_eq_coe.trans List.singleton_perm_singleton).mp

@[simp]
/-
**FreeCommRing.of_ne_zero** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：of_ne_zero (x : α) : of x != 0
参数：x : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `FreeAbelianGroup.of_ne_zero`：of_ne_zero (x : α) : of x != 0
-/
theorem of_ne_zero (x : α) : of x ≠ 0 := FreeAbelianGroup.of_ne_zero _

@[simp]
/-
**FreeCommRing.zero_ne_of** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：zero_ne_of (x : α) : 0 != of x
参数：x : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `FreeAbelianGroup.zero_ne_of`：∀ {α : Type u} (x : α), 0 ≠ FreeAbelianGrou
p.of x
-/
theorem zero_ne_of (x : α) : 0 ≠ of x := FreeAbelianGroup.zero_ne_of _

@[simp]
/-
**FreeCommRing.of_ne_one** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：of_ne_one (x : α) : of x != 1
参数：x : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Function.Injective.ne`：∀ {α : Sort u_1} {β : Sort u_2} {f : α → β}, Func
tion.Injective f → ∀ {a₁ a₂ : α}, a₁ ≠ a₂ → f a₁ ≠ f a₂
· 使用定理 `FreeAbelianGroup.of_injective`：of_injective : Function.Injective (of : α
 -> FreeAbelianGroup α)
· 使用定理 `Multiset.singleton_ne_zero`：singleton_ne_zero (a : α) : ({a} : Multiset 
α) != 0
-/
theorem of_ne_one (x : α) : of x ≠ 1 :=
  FreeAbelianGroup.of_injective.ne <| Multiset.singleton_ne_zero _

@[simp]
/-
**FreeCommRing.one_ne_of** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：one_ne_of (x : α) : 1 != of x
参数：x : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Function.Injective.ne`：∀ {α : Sort u_1} {β : Sort u_2} {f : α → β}, Func
tion.Injective f → ∀ {a₁ a₂ : α}, a₁ ≠ a₂ → f a₁ ≠ f a₂
· 使用定理 `FreeAbelianGroup.of_injective`：of_injective : Function.Injective (of : α
 -> FreeAbelianGroup α)
· 使用定理 `Multiset.zero_ne_singleton`：∀ {α : Type u_1} (a : α), 0 ≠ {a}
-/
theorem one_ne_of (x : α) : 1 ≠ of x :=
  FreeAbelianGroup.of_injective.ne <| Multiset.zero_ne_singleton _

set_option backward.isDefEq.respectTransparency false in
/-
**FreeCommRing.of_cons** 是 Mathlib 中的一个引理，位于命名空间 `FreeCommRing`。
形式化陈述：of_cons (a : α) (m : Multiset α) : (FreeAbelianGroup.of (Multiplicative.of
Add (a ::ₘ m))) = @HMul.hMul _ (FreeCommRing α) (FreeCommRing α) _ (of a) (FreeA
belianGroup.of (Multiplicative.ofAdd m))
参数：a : α；m : Multiset α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Multiset.singleton_add`：singleton_add (a : α) (s : Multiset α) : {a} + s
 = a ::ₘ s
· 使用定理 `ofAdd_add`：ofAdd_add [Add α] (x y : α) : ofAdd (x + y) = ofAdd x * ofAdd
 y
· 使用定理 `FreeCommRing.of.eq_1`：∀ {α : Type u} (x : α), FreeCommRing.of x = FreeAb
elianGroup.of (Multiplicative.ofAdd {x})
· 使用定理 `FreeAbelianGroup.of_mul_of`：of_mul_of (x y : α) : of x * of y = of (x * 
y)
-/
lemma of_cons (a : α) (m : Multiset α) : (FreeAbelianGroup.of (Multiplicative.ofAdd (a ::ₘ m))) =
    @HMul.hMul _ (FreeCommRing α) (FreeCommRing α) _ (of a)
    (FreeAbelianGroup.of (Multiplicative.ofAdd m)) := by
  dsimp [FreeCommRing]
  rw [← Multiset.singleton_add, ofAdd_add,
    of, FreeAbelianGroup.of_mul_of]

@[elab_as_elim, induction_eliminator]
/-
**FreeCommRing.induction_on** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：∀ {α : Type u} {motive : FreeCommRing α → Prop} (z : FreeCommRing α),   mo
tive (-1) →     (∀ (b : α), motive (FreeCommRing.of b)) →       (∀ (x y : FreeCo
mmRing α), motive x → motive y → motive (x + y)) →         (∀ (x y : FreeCommRin
g α), motive x → motive y → motive (x * y)) → motive z
参数：z : FreeCommRing α；-1；∀ (b : α), motive (FreeCommRing.of b)；∀ (x y : FreeComm
Ring α), motive x → motive y → motive (x + y)；∀ (x y : FreeCommRing α), motive x
 → motive y → motive (x * y)。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `neg_one_mul`：neg_one_mul (a : α) : -1 * a = -a
· 使用定理 `neg_neg`：∀ {G : Type u_1} [inst : InvolutiveNeg G] (a : G), - -a = a
· 使用定理 `FreeAbelianGroup.induction_on`：∀ {α : Type u} {motive : FreeAbelianGroup
 α → Prop} (z : FreeAbelianGroup α),   motive 0 →     (∀ (x : α), motive (FreeAb
elianGroup.of x)) →…
· 使用定理 `neg_add_cancel`：∀ {G : Type u_1} [inst : AddGroup G] (a : G), -a + a = 0
· 使用定理 `Multiset.induction_on`：∀ {α : Type u_1} {p : Multiset α → Prop} (s : Mul
tiset α), p 0 → (∀ (a : α) (s : Multiset α), p s → p (a ::ₘ s)) → p s
-/
protected theorem induction_on {motive : FreeCommRing α → Prop} (z : FreeCommRing α)
    (neg_one : motive (-1)) (of : ∀ b, motive (of b))
    (add : ∀ x y, motive x → motive y → motive (x + y))
    (mul : ∀ x y, motive x → motive y → motive (x * y)) : motive z :=
  have neg : ∀ x, motive x → motive (-x) := fun x ih => neg_one_mul x ▸ mul _ _ neg_one ih
  have one : motive 1 := neg_neg (1 : FreeCommRing α) ▸ neg _ neg_one
  FreeAbelianGroup.induction_on z (neg_add_cancel (1 : FreeCommRing α) ▸ add _ _ neg_one one)
    (fun m => Multiset.induction_on m one fun a _ ih => mul (FreeCommRing.of a) _ (of a) ih)
    (fun _ ih => neg _ ih) add

section lift

variable {R : Type v} [CommRing R] (f : α → R)

set_option backward.isDefEq.respectTransparency false in
set_option backward.privateInPublic true in
/-- A helper to implement `lift`. This is essentially `FreeCommMonoid.lift`, but this does not
currently exist. -/
/-
**FreeCommRing.liftToMultiset** 是 Mathlib 中的一个定义，位于命名空间 `FreeCommRing`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A helper to implement `lift`. This is essentially `FreeCommMonoid.lift`, but thi
s does not
currently exist.
-/
private def liftToMultiset : (α → R) ≃ (Multiplicative (Multiset α) →* R) where
  toFun f :=
    { toFun := fun s => (s.toAdd.map f).prod
      map_mul' := fun x y =>
        calc
          _ = Multiset.prod (Multiset.map f x + Multiset.map f y) := by
            rw [← Multiset.map_add]
            rfl
          _ = _ := Multiset.prod_add _ _
      map_one' := rfl }
  invFun F x := F (Multiplicative.ofAdd ({x} : Multiset α))
  left_inv f := funext fun x => show (Multiset.map f {x}).prod = _ by simp
  right_inv F := MonoidHom.ext fun x =>
    let F' := F.toAdditiveRight
    let x' := x.toAdd
    show (Multiset.map (fun a => F' {a}) x').sum = F' x' by
      rw [← Function.comp_def (fun x => F' x) (fun x => {x}), ← Multiset.map_map,
        ← AddMonoidHom.map_multiset_sum]
      exact DFunLike.congr_arg F (Multiset.sum_map_singleton x')

set_option backward.privateInPublic true in
set_option backward.privateInPublic.warn false in
/-- Lift a map `α → R` to an additive group homomorphism `FreeCommRing α → R`. -/
/-
**FreeCommRing.lift** 是 Mathlib 中的一个定义，位于命名空间 `FreeCommRing`。
形式化陈述：lift : (α -> R) ≃ (FreeCommRing α ->+* R)
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.trans`：Equiv.trans {s t u : Computation α} : s ~ t -> t ~ u -> s ~
 u

--- 原说明 ---
Lift a map `α → R` to an additive group homomorphism `FreeCommRing α → R`.
-/
def lift : (α → R) ≃ (FreeCommRing α →+* R) :=
  Equiv.trans liftToMultiset FreeAbelianGroup.liftMonoid

@[simp]
/-
**FreeCommRing.lift_of** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：lift_of (x : α) : lift f (of x) = f x
参数：x : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `FreeAbelianGroup.lift_apply_of`：lift_apply_of (x : α) : lift f (of x) = 
f x
· 使用定理 `mul_one`：mul_one : forall a : M, a * 1 = a
-/
theorem lift_of (x : α) : lift f (of x) = f x :=
  (FreeAbelianGroup.lift_apply_of _ _).trans <| mul_one _

@[simp]
/-
**FreeCommRing.lift_comp_of** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：lift_comp_of (f : FreeCommRing α ->+* R) : lift (f ∘ of) = f
参数：f : FreeCommRing α ->+* R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `RingHom.ext`：ext ⦃f g : α ->+* β⦄ : (forall x, f x = g x) -> f = g
· 使用定理 `FreeCommRing.induction_on`：∀ {α : Type u} {motive : FreeCommRing α → Pro
p} (z : FreeCommRing α),   motive (-1) →     (∀ (b : α), motive (FreeCommRing.of
 b)) →       (∀…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `map_neg`：∀ {G : Type u_7} {H : Type u_8} {F : Type u_9} [inst : FunLike 
F G H] [inst_1 : AddGroup G]   [inst_2 : SubtractionMonoid H] [AddMonoidHomCl…
· 使用定理 `RingHomClass.toAddMonoidHomClass`：∀ {F : Type u_5} {α : outParam (Type u
_6)} {β : outParam (Type u_7)} {inst : NonAssocSemiring α}   {inst_1 : NonAssocS
emiring β} {inst_2 : F…
· 使用定理 `map_one`：map_one [OneHomClass F M N] (f : F) : f 1 = 1
· 使用定理 `MonoidHomClass.toOneHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `RingHom.map_neg`：∀ {α : Type u_2} {β : Type u_3} [inst : NonAssocRing α]
 [inst_1 : NonAssocRing β] (f : α →+* β) (x : α), f (-x) = -f x
· 使用定理 `RingHom.map_one`：∀ {α : Type u_2} {β : Type u_3} {x : NonAssocSemiring α
} {x_1 : NonAssocSemiring β} (f : α →+* β), f 1 = 1
· 使用定理 `FreeCommRing.lift_of`：lift_of (x : α) : lift f (of x) = f x
· 使用定理 `map_add`：∀ {M : Type u_4} {N : Type u_5} {F : Type u_9} [inst : Add M] [
inst_1 : Add N] [inst_2 : FunLike F M N]   [AddHomClass F M N] (f : F) (x y :…
· 使用定理 `AddMonoidHomClass.toAddHomClass`：∀ {F : Type u_10} {M : outParam (Type u
_11)} {N : outParam (Type u_12)} {inst : AddZero M} {inst_1 : AddZero N}   {inst
_2 : FunLike F M N} […
· 使用定理 `RingHom.map_add`：∀ {α : Type u_2} {β : Type u_3} {x : NonAssocSemiring α
} {x_1 : NonAssocSemiring β} (f : α →+* β) (a b : α),   f (a + b) = f a + f b
· 使用定理 `map_mul`：map_mul [MulHomClass F M N] (f : F) (x y : M) : f (x * y) = f x
 * f y
· 使用定理 `NonUnitalRingHomClass.toMulHomClass`：∀ {F : Type u_5} {α : outParam (Typ
e u_6)} {β : outParam (Type u_7)} {inst : NonUnitalNonAssocSemiring α}   {inst_1
 : NonUnitalNonAssocSemir…
· 使用定理 `RingHomClass.toNonUnitalRingHomClass`：∀ {F : Type u_1} {α : Type u_2} {β
 : Type u_3} [inst : FunLike F α β] {x : NonAssocSemiring α}   {x_1 : NonAssocSe
miring β} [RingHomClass F …
· 使用定理 `RingHom.map_mul`：∀ {α : Type u_2} {β : Type u_3} {x : NonAssocSemiring α
} {x_1 : NonAssocSemiring β} (f : α →+* β) (a b : α),   f (a * b) = f a * f b
-/
theorem lift_comp_of (f : FreeCommRing α →+* R) : lift (f ∘ of) = f :=
  RingHom.ext fun x =>
    FreeCommRing.induction_on x (by rw [map_neg, map_one, f.map_neg, f.map_one])
      (lift_of _) (fun x y ihx ihy => by rw [map_add, f.map_add, ihx, ihy])
      fun x y ihx ihy => by rw [map_mul, f.map_mul, ihx, ihy]

@[ext 1100]
/-
**FreeCommRing.hom_ext** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：hom_ext ⦃f g : FreeCommRing α ->+* R⦄ (h : forall x, f (of x) = g (of x)) 
: f = g
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.injective`：∀ {α : Sort u} {β : Sort v} (e : α ≃ β), Function.Injec
tive ⇑e
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
-/
theorem hom_ext ⦃f g : FreeCommRing α →+* R⦄ (h : ∀ x, f (of x) = g (of x)) : f = g :=
  lift.symm.injective (funext h)

end lift

variable {β : Type v} (f : α → β)

/-- A map `f : α → β` produces a ring homomorphism `FreeCommRing α →+* FreeCommRing β`. -/
/-
**FreeCommRing.map** 是 Mathlib 中的一个定义，位于命名空间 `FreeCommRing`。
形式化陈述：map : FreeCommRing α ->+* FreeCommRing β
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A map `f : α → β` produces a ring homomorphism `FreeCommRing α →+* FreeCommRing 
β`.
-/
def map : FreeCommRing α →+* FreeCommRing β :=
  lift <| of ∘ f

@[simp]
/-
**FreeCommRing.map_of** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：map_of (x : α) : map f (of x) = of (f x)
参数：x : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `FreeCommRing.lift_of`：lift_of (x : α) : lift f (of x) = f x
-/
theorem map_of (x : α) : map f (of x) = of (f x) :=
  lift_of _ _

/-- `is_supported x s` means that all monomials showing up in `x` have variables in `s`. -/
/-
**FreeCommRing.IsSupported** 是 Mathlib 中的一个定义，位于命名空间 `FreeCommRing`。
形式化陈述：IsSupported (x : FreeCommRing α) (s : Set α) : Prop
参数：x : FreeCommRing α；s : Set α。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`is_supported x s` means that all monomials showing up in `x` have variables in 
`s`.
-/
def IsSupported (x : FreeCommRing α) (s : Set α) : Prop :=
  x ∈ Subring.closure (of '' s)

section IsSupported

variable {x y : FreeCommRing α} {s t : Set α}

/-
**FreeCommRing.isSupported_upwards** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：isSupported_upwards (hs : IsSupported x s) (hst : s subseteq t) : IsSuppor
ted x t
参数：hs : IsSupported x s；hst : s subseteq t。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subring.closure_mono`：closure_mono ⦃s t : Set R⦄ (h : s subseteq t) : cl
osure s <= closure t
· 使用引理 `Set.monotone_image`：monotone_image : Monotone (image f)
-/
theorem isSupported_upwards (hs : IsSupported x s) (hst : s ⊆ t) : IsSupported x t :=
  Subring.closure_mono (Set.monotone_image hst) hs
/-
**FreeCommRing.isSupported_add** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：isSupported_add (hxs : IsSupported x s) (hys : IsSupported y s) : IsSuppor
ted (x + y) s
参数：hxs : IsSupported x s；hys : IsSupported y s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subring.add_mem`：∀ {R : Type u} [inst : NonAssocRing R] (s : Subring R) 
{x y : R}, x ∈ s → y ∈ s → x + y ∈ s
-/
theorem isSupported_add (hxs : IsSupported x s) (hys : IsSupported y s) : IsSupported (x + y) s :=
  Subring.add_mem _ hxs hys
/-
**FreeCommRing.isSupported_neg** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：isSupported_neg (hxs : IsSupported x s) : IsSupported (-x) s
参数：hxs : IsSupported x s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subring.neg_mem`：∀ {R : Type u} [inst : NonAssocRing R] (s : Subring R) 
{x : R}, x ∈ s → -x ∈ s
-/
theorem isSupported_neg (hxs : IsSupported x s) : IsSupported (-x) s :=
  Subring.neg_mem _ hxs
/-
**FreeCommRing.isSupported_sub** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：isSupported_sub (hxs : IsSupported x s) (hys : IsSupported y s) : IsSuppor
ted (x - y) s
参数：hxs : IsSupported x s；hys : IsSupported y s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subring.sub_mem`：∀ {R : Type u} [inst : NonAssocRing R] (s : Subring R) 
{x y : R}, x ∈ s → y ∈ s → x - y ∈ s
-/
theorem isSupported_sub (hxs : IsSupported x s) (hys : IsSupported y s) : IsSupported (x - y) s :=
  Subring.sub_mem _ hxs hys
/-
**FreeCommRing.isSupported_mul** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：isSupported_mul (hxs : IsSupported x s) (hys : IsSupported y s) : IsSuppor
ted (x * y) s
参数：hxs : IsSupported x s；hys : IsSupported y s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subring.mul_mem`：∀ {R : Type u} [inst : NonAssocRing R] (s : Subring R) 
{x y : R}, x ∈ s → y ∈ s → x * y ∈ s
-/
theorem isSupported_mul (hxs : IsSupported x s) (hys : IsSupported y s) : IsSupported (x * y) s :=
  Subring.mul_mem _ hxs hys
/-
**FreeCommRing.isSupported_zero** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：isSupported_zero : IsSupported 0 s
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subring.zero_mem`：∀ {R : Type u} [inst : NonAssocRing R] (s : Subring R)
, 0 ∈ s
-/
theorem isSupported_zero : IsSupported 0 s :=
  Subring.zero_mem _
/-
**FreeCommRing.isSupported_one** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：isSupported_one : IsSupported 1 s
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subring.one_mem`：∀ {R : Type u} [inst : NonAssocRing R] (s : Subring R),
 1 ∈ s
-/
theorem isSupported_one : IsSupported 1 s :=
  Subring.one_mem _
/-
**FreeCommRing.isSupported_int** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：isSupported_int {i : Int} {s : Set α} : IsSupported (↑i) s
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Int.induction_on`：∀ {motive : ℤ → Prop} (i : ℤ),   motive 0 → (∀ (i : ℕ)
, motive ↑i → motive (↑i + 1)) → (∀ (i : ℕ), motive (-↑i) → motive (-↑i - 1)) → 
motive…
· 使用定理 `FreeCommRing.isSupported_zero`：isSupported_zero : IsSupported 0 s
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Int.cast_add`：∀ {R : Type u} [inst : AddGroupWithOne R] (m n : ℤ), ↑(m +
 n) = ↑m + ↑n
· 使用定理 `Int.cast_one`：cast_one : ((1 : Int) : R) = 1
· 使用定理 `FreeCommRing.isSupported_add`：isSupported_add (hxs : IsSupported x s) (h
ys : IsSupported y s) : IsSupported (x + y) s
· 使用定理 `FreeCommRing.isSupported_one`：isSupported_one : IsSupported 1 s
· 使用定理 `Int.cast_sub`：cast_sub (m n) : ((m - n : Int) : R) = m - n
· 使用定理 `FreeCommRing.isSupported_sub`：isSupported_sub (hxs : IsSupported x s) (h
ys : IsSupported y s) : IsSupported (x - y) s
-/
theorem isSupported_int {i : ℤ} {s : Set α} : IsSupported (↑i) s :=
  Int.induction_on i isSupported_zero
    (fun i hi => by rw [Int.cast_add, Int.cast_one]; exact isSupported_add hi isSupported_one)
    fun i hi => by rw [Int.cast_sub, Int.cast_one]; exact isSupported_sub hi isSupported_one

end IsSupported

/-- The restriction map from `FreeCommRing α` to `FreeCommRing s` where `s : Set α`, defined
  by sending all variables not in `s` to zero. -/
/-
**FreeCommRing.restriction** 是 Mathlib 中的一个定义，位于命名空间 `FreeCommRing`。
形式化陈述：restriction (s : Set α) [DecidablePred (· in s)] : FreeCommRing α ->+* Fre
eCommRing s
参数：s : Set α；· in s。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The restriction map from `FreeCommRing α` to `FreeCommRing s` where `s : Set α`,
 defined
  by sending all variables not in `s` to zero.
-/
def restriction (s : Set α) [DecidablePred (· ∈ s)] : FreeCommRing α →+* FreeCommRing s :=
  lift (fun a => if H : a ∈ s then of ⟨a, H⟩ else 0)

section Restriction

variable (s : Set α) [DecidablePred (· ∈ s)] (x y : FreeCommRing α)

@[simp]
/-
**FreeCommRing.restriction_of** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：restriction_of (p) : restriction s (of p) = if H : p in s then of ⟨p, H⟩ e
lse 0
参数：p。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `FreeCommRing.lift_of`：lift_of (x : α) : lift f (of x) = f x
-/
theorem restriction_of (p) : restriction s (of p) = if H : p ∈ s then of ⟨p, H⟩ else 0 :=
  lift_of _ _

end Restriction

/-
**FreeCommRing.isSupported_of** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：isSupported_of {p} {s : Set α} : IsSupported (of p) s ↔ p in s
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subring.InClosure.recOn`：∀ {R : Type u_1} [inst : Ring R] {s : Set R} {C
 : R → Prop} {x : R},   x ∈ Subring.closure s →     C 1 → C (-1) → (∀ z ∈ s, ∀ (
n : R), C n →…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `map_one`：map_one [OneHomClass F M N] (f : F) : f 1 = 1
· 使用定理 `MonoidHomClass.toOneHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Nat.cast_one`：cast_one : ((1 : Nat) : R) = 1
· 使用定理 `Int.cast_one`：cast_one : ((1 : Int) : R) = 1
· 使用定理 `of_decide_eq_true`：∀ {p : Prop} [inst : Decidable p], decide p = true → 
p
· 使用定理 `map_neg`：∀ {G : Type u_7} {H : Type u_8} {F : Type u_9} [inst : FunLike 
F G H] [inst_1 : AddGroup G]   [inst_2 : SubtractionMonoid H] [AddMonoidHomCl…
· 使用定理 `RingHomClass.toAddMonoidHomClass`：∀ {F : Type u_5} {α : outParam (Type u
_6)} {β : outParam (Type u_7)} {inst : NonAssocSemiring α}   {inst_1 : NonAssocS
emiring β} {inst_2 : F…
· 使用定理 `Int.cast_neg`：∀ {R : Type u} [inst : AddGroupWithOne R] (n : ℤ), ↑(-n) =
 -↑n
· 使用定理 `map_mul`：map_mul [MulHomClass F M N] (f : F) (x y : M) : f (x * y) = f x
 * f y
· 使用定理 `NonUnitalRingHomClass.toMulHomClass`：∀ {F : Type u_5} {α : outParam (Typ
e u_6)} {β : outParam (Type u_7)} {inst : NonUnitalNonAssocSemiring α}   {inst_1
 : NonUnitalNonAssocSemir…
· 使用定理 `RingHomClass.toNonUnitalRingHomClass`：∀ {F : Type u_1} {α : Type u_2} {β
 : Type u_3} [inst : FunLike F α β] {x : NonAssocSemiring α}   {x_1 : NonAssocSe
miring β} [RingHomClass F …
· 使用定理 `FreeCommRing.lift_of`：lift_of (x : α) : lift f (of x) = f x
· 使用定理 `if_pos`：∀ {c : Prop} {h : Decidable c}, c → ∀ {α : Sort u} {t e : α}, (i
f c then t else e) = t
· 使用定理 `MulZeroClass.zero_mul`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, 0 * a = 0
· 使用定理 `Nat.cast_zero`：cast_zero : ((0 : Nat) : R) = 0
· 使用定理 `Int.cast_zero`：cast_zero : ((0 : Int) : R) = 0
· 使用定理 `map_add`：∀ {M : Type u_4} {N : Type u_5} {F : Type u_9} [inst : Add M] [
inst_1 : Add N] [inst_2 : FunLike F M N]   [AddHomClass F M N] (f : F) (x y :…
· 使用定理 `AddMonoidHomClass.toAddHomClass`：∀ {F : Type u_10} {M : outParam (Type u
_11)} {N : outParam (Type u_12)} {inst : AddZero M} {inst_1 : AddZero N}   {inst
_2 : FunLike F M N} […
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `Ne.symm`：∀ {α : Sort u} {a b : α}, a ≠ b → b ≠ a
· 使用定理 `Int.zero_ne_one`：0 ≠ 1
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `if_neg`：∀ {c : Prop} {h : Decidable c}, ¬c → ∀ {α : Sort u} {t e : α}, (
if c then t else e) = e
（共 36 条，此处仅展示前 30 条）
-/
theorem isSupported_of {p} {s : Set α} : IsSupported (of p) s ↔ p ∈ s :=
  suffices IsSupported (of p) s → p ∈ s from ⟨this, fun hps => Subring.subset_closure ⟨p, hps, rfl⟩⟩
  fun hps : IsSupported (of p) s => by
  have := Classical.decPred (· ∈ s)
  have : ∀ x, IsSupported x s →
        ∃ n : ℤ, lift (fun a => if a ∈ s then (0 : ℤ[X]) else Polynomial.X) x = n := by
    intro x hx
    refine Subring.InClosure.recOn hx ?_ ?_ ?_ ?_
    · use 1
      rw [map_one]
      norm_cast
    · use -1
      rw [map_neg, map_one, Int.cast_neg, Int.cast_one]
    · rintro _ ⟨z, hzs, rfl⟩ _ _
      use 0
      rw [map_mul, lift_of, if_pos hzs, zero_mul]
      norm_cast
    · rintro x y ⟨q, hq⟩ ⟨r, hr⟩
      refine ⟨q + r, ?_⟩
      rw [map_add, hq, hr]
      norm_cast
  specialize this (of p) hps
  rw [lift_of] at this
  split_ifs at this with h
  · exact h
  exfalso
  apply Ne.symm Int.zero_ne_one
  rcases this with ⟨w, H⟩
  rw [← Polynomial.C_eq_intCast] at H
  have : Polynomial.X.coeff 1 = (Polynomial.C ↑w).coeff 1 := by rw [H]; rfl
  rwa [Polynomial.coeff_C, if_neg (one_ne_zero : 1 ≠ 0), Polynomial.coeff_X, if_pos rfl] at this
/-
**FreeCommRing.map_subtype_val_restriction** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRi
ng`。
形式化陈述：map_subtype_val_restriction {x} (s : Set α) [DecidablePred (· in s)] (hxs 
: IsSupported x s) : map (↑) (restriction s x) = x
参数：s : Set α；· in s；hxs : IsSupported x s。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subring.InClosure.recOn`：∀ {R : Type u_1} [inst : Ring R] {s : Set R} {C
 : R → Prop} {x : R},   x ∈ Subring.closure s →     C 1 → C (-1) → (∀ z ∈ s, ∀ (
n : R), C n →…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `map_one`：map_one [OneHomClass F M N] (f : F) : f 1 = 1
· 使用定理 `MonoidHomClass.toOneHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `map_neg`：∀ {G : Type u_7} {H : Type u_8} {F : Type u_9} [inst : FunLike 
F G H] [inst_1 : AddGroup G]   [inst_2 : SubtractionMonoid H] [AddMonoidHomCl…
· 使用定理 `RingHomClass.toAddMonoidHomClass`：∀ {F : Type u_5} {α : outParam (Type u
_6)} {β : outParam (Type u_7)} {inst : NonAssocSemiring α}   {inst_1 : NonAssocS
emiring β} {inst_2 : F…
· 使用定理 `map_mul`：map_mul [MulHomClass F M N] (f : F) (x y : M) : f (x * y) = f x
 * f y
· 使用定理 `NonUnitalRingHomClass.toMulHomClass`：∀ {F : Type u_5} {α : outParam (Typ
e u_6)} {β : outParam (Type u_7)} {inst : NonUnitalNonAssocSemiring α}   {inst_1
 : NonUnitalNonAssocSemir…
· 使用定理 `RingHomClass.toNonUnitalRingHomClass`：∀ {F : Type u_1} {α : Type u_2} {β
 : Type u_3} [inst : FunLike F α β] {x : NonAssocSemiring α}   {x_1 : NonAssocSe
miring β} [RingHomClass F …
· 使用定理 `FreeCommRing.restriction_of`：restriction_of (p) : restriction s (of p) =
 if H : p in s then of ⟨p, H⟩ else 0
· 使用定理 `dif_pos`：∀ {c : Prop} {h : Decidable c} (hc : c) {α : Sort u} {t : c → α
} {e : ¬c → α}, dite c t e = t hc
· 使用定理 `FreeCommRing.map_of`：map_of (x : α) : map f (of x) = of (f x)
· 使用定理 `map_add`：∀ {M : Type u_4} {N : Type u_5} {F : Type u_9} [inst : Add M] [
inst_1 : Add N] [inst_2 : FunLike F M N]   [AddHomClass F M N] (f : F) (x y :…
· 使用定理 `AddMonoidHomClass.toAddHomClass`：∀ {F : Type u_10} {M : outParam (Type u
_11)} {N : outParam (Type u_12)} {inst : AddZero M} {inst_1 : AddZero N}   {inst
_2 : FunLike F M N} […
-/
theorem map_subtype_val_restriction {x} (s : Set α) [DecidablePred (· ∈ s)]
    (hxs : IsSupported x s) : map (↑) (restriction s x) = x := by
  refine Subring.InClosure.recOn hxs ?_ ?_ ?_ ?_
  · rw [map_one]
    rfl
  · rw [map_neg, map_one]
    rfl
  · rintro _ ⟨p, hps, rfl⟩ n ih
    rw [map_mul, restriction_of, dif_pos hps, map_mul, map_of, ih]
  · intro x y ihx ihy
    rw [map_add, map_add, ihx, ihy]
/-
**FreeCommRing.exists_finite_support** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：exists_finite_support (x : FreeCommRing α) : exists s : Set α, Set.Finite 
s ∧ IsSupported x s
参数：x : FreeCommRing α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `FreeCommRing.induction_on`：∀ {α : Type u} {motive : FreeCommRing α → Pro
p} (z : FreeCommRing α),   motive (-1) →     (∀ (b : α), motive (FreeCommRing.of
 b)) →       (∀…
· 使用定理 `Set.finite_empty`：finite_empty : (∅ : Set α).Finite
· 使用定理 `FreeCommRing.isSupported_neg`：isSupported_neg (hxs : IsSupported x s) : 
IsSupported (-x) s
· 使用定理 `FreeCommRing.isSupported_one`：isSupported_one : IsSupported 1 s
· 使用定理 `Set.finite_singleton`：finite_singleton (a : α) : ({a} : Set α).Finite
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `FreeCommRing.isSupported_of`：isSupported_of {p} {s : Set α} : IsSupporte
d (of p) s ↔ p in s
· 使用定理 `Set.mem_singleton`：mem_singleton (a : α) : a in ({a} : Set α)
· 使用定理 `Set.Finite.union`：∀ {α : Type u} {s t : Set α}, s.Finite → t.Finite → (s
 ∪ t).Finite
· 使用定理 `FreeCommRing.isSupported_add`：isSupported_add (hxs : IsSupported x s) (h
ys : IsSupported y s) : IsSupported (x + y) s
· 使用定理 `FreeCommRing.isSupported_upwards`：isSupported_upwards (hs : IsSupported 
x s) (hst : s subseteq t) : IsSupported x t
· 使用定理 `Set.subset_union_left`：subset_union_left {s t : Set α} : s subseteq s un
ion t
· 使用定理 `Set.subset_union_right`：subset_union_right {s t : Set α} : t subseteq s 
union t
· 使用定理 `FreeCommRing.isSupported_mul`：isSupported_mul (hxs : IsSupported x s) (h
ys : IsSupported y s) : IsSupported (x * y) s
-/
theorem exists_finite_support (x : FreeCommRing α) : ∃ s : Set α, Set.Finite s ∧ IsSupported x s :=
  FreeCommRing.induction_on x ⟨∅, Set.finite_empty, isSupported_neg isSupported_one⟩
    (fun p => ⟨{p}, Set.finite_singleton p, isSupported_of.2 <| Set.mem_singleton _⟩)
    (fun _ _ ⟨s, hfs, hxs⟩ ⟨t, hft, hxt⟩ =>
      ⟨s ∪ t, hfs.union hft,
        isSupported_add (isSupported_upwards hxs Set.subset_union_left)
          (isSupported_upwards hxt Set.subset_union_right)⟩)
    fun _ _ ⟨s, hfs, hxs⟩ ⟨t, hft, hxt⟩ =>
    ⟨s ∪ t, hfs.union hft,
      isSupported_mul (isSupported_upwards hxs Set.subset_union_left)
        (isSupported_upwards hxt Set.subset_union_right)⟩
/-
**FreeCommRing.exists_finset_support** 是 Mathlib 中的一个定理，位于命名空间 `FreeCommRing`。
形式化陈述：exists_finset_support (x : FreeCommRing α) : exists s : Finset α, IsSuppor
ted x ↑s
参数：x : FreeCommRing α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `FreeCommRing.exists_finite_support`：exists_finite_support (x : FreeCommR
ing α) : exists s : Set α, Set.Finite s ∧ IsSupported x s
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Set.Finite.coe_toFinset`：∀ {α : Type u} {s : Set α} (hs : s.Finite), ↑hs
.toFinset = s
-/
theorem exists_finset_support (x : FreeCommRing α) : ∃ s : Finset α, IsSupported x ↑s :=
  let ⟨s, hfs, hxs⟩ := exists_finite_support x
  ⟨hfs.toFinset, by rwa [Set.Finite.coe_toFinset]⟩

end FreeCommRing

namespace FreeRing

open Function

/-- The canonical ring homomorphism from the free ring generated by `α` to the free commutative ring
generated by `α`. -/
/-
**FreeRing.toFreeCommRing** 是 Mathlib 中的一个定义，位于命名空间 `FreeRing`。
形式化陈述：toFreeCommRing {α} : FreeRing α ->+* FreeCommRing α
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The canonical ring homomorphism from the free ring generated by `α` to the free 
commutative ring
generated by `α`.
-/
def toFreeCommRing {α} : FreeRing α →+* FreeCommRing α :=
  FreeRing.lift FreeCommRing.of

/-- The coercion defined by the canonical ring homomorphism from the free ring generated by `α` to
the free commutative ring generated by `α`. -/
/-
**FreeRing.castFreeCommRing** 是 Mathlib 中的一个定义，位于命名空间 `FreeRing`。
形式化陈述：{α : Type u_1} → FreeRing α → FreeCommRing α
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The coercion defined by the canonical ring homomorphism from the free ring gener
ated by `α` to
the free commutative ring generated by `α`.
-/
@[coe] def castFreeCommRing {α} : FreeRing α → FreeCommRing α := toFreeCommRing
/-
**FreeRing.FreeCommRing.instCoe** 是 Mathlib 中的一个定义，位于命名空间 `FreeRing.FreeCommRing
`。
形式化陈述：(α : Type u) → Coe (FreeRing α) (FreeCommRing α)
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The coercion defined by the canonical ring homomorphism from the free ring gener
ated by `α` to
the free commutative ring generated by `α`.
-/
instance FreeCommRing.instCoe : Coe (FreeRing α) (FreeCommRing α) :=
  ⟨castFreeCommRing⟩

/-- The natural map `FreeRing α → FreeCommRing α`, as a `RingHom`. -/
/-
**FreeRing.coeRingHom** 是 Mathlib 中的一个定义，位于命名空间 `FreeRing`。
形式化陈述：coeRingHom : FreeRing α ->+* FreeCommRing α
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The natural map `FreeRing α → FreeCommRing α`, as a `RingHom`.
-/
def coeRingHom : FreeRing α →+* FreeCommRing α :=
  toFreeCommRing

@[simp, norm_cast]
/-
**FreeRing.coe_zero** 是 Mathlib 中的一个定理，位于命名空间 `FreeRing`。
形式化陈述：∀ (α : Type u), ↑0 = 0
参数：α : Type u。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
protected theorem coe_zero : ↑(0 : FreeRing α) = (0 : FreeCommRing α) := rfl

@[simp, norm_cast]
/-
**FreeRing.coe_one** 是 Mathlib 中的一个定理，位于命名空间 `FreeRing`。
形式化陈述：∀ (α : Type u), ↑1 = 1
参数：α : Type u。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
protected theorem coe_one : ↑(1 : FreeRing α) = (1 : FreeCommRing α) := rfl

variable {α}

@[simp]
/-
**FreeRing.coe_of** 是 Mathlib 中的一个定理，位于命名空间 `FreeRing`。
形式化陈述：∀ {α : Type u} (a : α), ↑(FreeRing.of a) = FreeCommRing.of a
参数：a : α；FreeRing.of a。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `FreeRing.lift_of`：lift_of (x : α) : lift f (of x) = f x
-/
protected theorem coe_of (a : α) : ↑(FreeRing.of a) = FreeCommRing.of a :=
  FreeRing.lift_of _ _

@[simp, norm_cast]
/-
**FreeRing.coe_neg** 是 Mathlib 中的一个定理，位于命名空间 `FreeRing`。
形式化陈述：∀ {α : Type u} (x : FreeRing α), ↑(-x) = -↑x
参数：x : FreeRing α；-x。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `FreeRing.castFreeCommRing.eq_1`：∀ {α : Type u_1}, FreeRing.castFreeCommR
ing = ⇑FreeRing.toFreeCommRing
· 使用定理 `map_neg`：∀ {G : Type u_7} {H : Type u_8} {F : Type u_9} [inst : FunLike 
F G H] [inst_1 : AddGroup G]   [inst_2 : SubtractionMonoid H] [AddMonoidHomCl…
· 使用定理 `RingHomClass.toAddMonoidHomClass`：∀ {F : Type u_5} {α : outParam (Type u
_6)} {β : outParam (Type u_7)} {inst : NonAssocSemiring α}   {inst_1 : NonAssocS
emiring β} {inst_2 : F…
-/
protected theorem coe_neg (x : FreeRing α) : ↑(-x) = -(x : FreeCommRing α) := by
  rw [castFreeCommRing, map_neg]

@[simp, norm_cast]
/-
**FreeRing.coe_add** 是 Mathlib 中的一个定理，位于命名空间 `FreeRing`。
形式化陈述：∀ {α : Type u} (x y : FreeRing α), ↑(x + y) = ↑x + ↑y
参数：x y : FreeRing α；x + y。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `RingHom.map_add`：∀ {α : Type u_2} {β : Type u_3} {x : NonAssocSemiring α
} {x_1 : NonAssocSemiring β} (f : α →+* β) (a b : α),   f (a + b) = f a + f b
-/
protected theorem coe_add (x y : FreeRing α) : ↑(x + y) = (x : FreeCommRing α) + y :=
  (FreeRing.lift _).map_add _ _

@[simp, norm_cast]
/-
**FreeRing.coe_sub** 是 Mathlib 中的一个定理，位于命名空间 `FreeRing`。
形式化陈述：∀ {α : Type u} (x y : FreeRing α), ↑(x - y) = ↑x - ↑y
参数：x y : FreeRing α；x - y。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `FreeRing.castFreeCommRing.eq_1`：∀ {α : Type u_1}, FreeRing.castFreeCommR
ing = ⇑FreeRing.toFreeCommRing
· 使用定理 `map_sub`：∀ {G : Type u_7} {H : Type u_8} {F : Type u_9} [inst : FunLike 
F G H] [inst_1 : AddGroup G]   [inst_2 : SubtractionMonoid H] [AddMonoidHomCl…
· 使用定理 `RingHomClass.toAddMonoidHomClass`：∀ {F : Type u_5} {α : outParam (Type u
_6)} {β : outParam (Type u_7)} {inst : NonAssocSemiring α}   {inst_1 : NonAssocS
emiring β} {inst_2 : F…
-/
protected theorem coe_sub (x y : FreeRing α) : ↑(x - y) = (x : FreeCommRing α) - y := by
  rw [castFreeCommRing, map_sub]

@[simp, norm_cast]
/-
**FreeRing.coe_mul** 是 Mathlib 中的一个定理，位于命名空间 `FreeRing`。
形式化陈述：∀ {α : Type u} (x y : FreeRing α), ↑(x * y) = ↑x * ↑y
参数：x y : FreeRing α；x * y。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `RingHom.map_mul`：∀ {α : Type u_2} {β : Type u_3} {x : NonAssocSemiring α
} {x_1 : NonAssocSemiring β} (f : α →+* β) (a b : α),   f (a * b) = f a * f b
-/
protected theorem coe_mul (x y : FreeRing α) : ↑(x * y) = (x : FreeCommRing α) * y :=
  (FreeRing.lift _).map_mul _ _

variable (α)
/-
**FreeRing.coe_surjective** 是 Mathlib 中的一个定理，位于命名空间 `FreeRing`。
形式化陈述：∀ (α : Type u), Function.Surjective FreeRing.castFreeCommRing
参数：α : Type u。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `FreeCommRing.induction_on`：∀ {α : Type u} {motive : FreeCommRing α → Pro
p} (z : FreeCommRing α),   motive (-1) →     (∀ (b : α), motive (FreeCommRing.of
 b)) →       (∀…
· 使用定理 `RingHom.map_add`：∀ {α : Type u_2} {β : Type u_3} {x : NonAssocSemiring α
} {x_1 : NonAssocSemiring β} (f : α →+* β) (a b : α),   f (a + b) = f a + f b
· 使用定理 `RingHom.map_mul`：∀ {α : Type u_2} {β : Type u_3} {x : NonAssocSemiring α
} {x_1 : NonAssocSemiring β} (f : α →+* β) (a b : α),   f (a * b) = f a * f b
-/
protected theorem coe_surjective : Surjective ((↑) : FreeRing α → FreeCommRing α) := fun x => by
  induction x with
  | neg_one => use -1; rfl
  | of b => exact ⟨FreeRing.of b, rfl⟩
  | add _ _ hx hy =>
    rcases hx with ⟨x, rfl⟩; rcases hy with ⟨y, rfl⟩
    exact ⟨x + y, (FreeRing.lift _).map_add _ _⟩
  | mul _ _ hx hy =>
    rcases hx with ⟨x, rfl⟩; rcases hy with ⟨y, rfl⟩
    exact ⟨x * y, (FreeRing.lift _).map_mul _ _⟩

set_option backward.isDefEq.respectTransparency false in
/-
**FreeRing.coe_eq** 是 Mathlib 中的一个定理，位于命名空间 `FreeRing`。
形式化陈述：coe_eq : ((↑) : FreeRing α -> FreeCommRing α) = @Functor.map FreeAbelianGr
oup _ _ _ fun l : List α => (l : Multiset α)
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `AddMonoidHom.instAddMonoidHomClass`：∀ {M : Type u_4} {N : Type u_5} [ins
t : AddZero M] [inst_1 : AddZero N], AddMonoidHomClass (M →+ N) M N
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `AddMonoidHom.coe_coe`：∀ {M : Type u_4} {N : Type u_5} {F : Type u_9} [in
st : AddZero M] [inst_1 : AddZero N] [inst_2 : FunLike F M N]   [inst_3 : AddMon
oidHomClas…
· 使用定理 `FreeAbelianGroup.lift_unique`：lift_unique (g : FreeAbelianGroup α ->+ β)
 (hg : forall x, g (of x) = f x) {x} : g x = lift f x
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `FreeAbelianGroup.lift_apply_of`：lift_apply_of (x : α) : lift f (of x) = 
f x
· 使用定理 `MonoidHom.map_mul`：∀ {M : Type u_4} {N : Type u_5} [inst : MulOne M] [in
st_1 : MulOne N] (f : M →* N) (a b : M), f (a * b) = f a * f b
· 使用定理 `FreeMonoid.lift_eval_of`：lift_eval_of (f : α -> M) (x : α) : lift f (of 
x) = f x
-/
theorem coe_eq : ((↑) : FreeRing α → FreeCommRing α) =
    @Functor.map FreeAbelianGroup _ _ _ fun l : List α => (l : Multiset α) := by
  funext x
  dsimp [castFreeCommRing, toFreeCommRing, FreeRing.lift, FreeRing, FreeAbelianGroup.liftMonoid_coe,
    Functor.map]
  rw [← AddMonoidHom.coe_coe]
  apply FreeAbelianGroup.lift_unique; intro L
  simp only [AddMonoidHom.coe_coe, comp_apply, FreeAbelianGroup.lift_apply_of]
  exact
    FreeMonoid.recOn L rfl fun hd tl ih => by
      rw [(FreeMonoid.lift _).map_mul, FreeMonoid.lift_eval_of, ih]
      conv_lhs => reduce
      rfl

/-- If α has size at most 1 then the natural map from the free ring on `α` to the
free commutative ring on `α` is an isomorphism of rings. -/
/-
**FreeRing.subsingletonEquivFreeCommRing** 是 Mathlib 中的一个定义，位于命名空间 `FreeRing`。
形式化陈述：subsingletonEquivFreeCommRing [Subsingleton α] : FreeRing α ≃+* FreeCommRi
ng α
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If α has size at most 1 then the natural map from the free ring on `α` to the
free commutative ring on `α` is an isomorphism of rings.
-/
def subsingletonEquivFreeCommRing [Subsingleton α] : FreeRing α ≃+* FreeCommRing α :=
  RingEquiv.ofBijective (coeRingHom _) (by
    have : (coeRingHom _ : FreeRing α → FreeCommRing α) =
        Functor.mapEquiv FreeAbelianGroup (Multiset.subsingletonEquiv α) :=
      coe_eq α
    rw [this]
    apply Equiv.bijective)
/-
**FreeRing.instCommRing** 是 Mathlib 中的一个实例，位于命名空间 `FreeRing`。
形式化陈述：instCommRing [Subsingleton α] : CommRing (FreeRing α)
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance instCommRing [Subsingleton α] : CommRing (FreeRing α) :=
  { (inferInstance : Ring (FreeRing α)) with
    mul_comm := fun x y => by
      rw [← (subsingletonEquivFreeCommRing α).symm_apply_apply (y * x),
        (subsingletonEquivFreeCommRing α).map_mul, mul_comm,
        ← (subsingletonEquivFreeCommRing α).map_mul,
        (subsingletonEquivFreeCommRing α).symm_apply_apply] }

end FreeRing

/-- The free commutative ring on `α` is isomorphic to the polynomial ring over ℤ with
variables in `α` -/
/-
**freeCommRingEquivMvPolynomialInt** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：freeCommRingEquivMvPolynomialInt : FreeCommRing α ≃+* MvPolynomial α Int
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The free commutative ring on `α` is isomorphic to the polynomial ring over ℤ wit
h
variables in `α`
-/
def freeCommRingEquivMvPolynomialInt : FreeCommRing α ≃+* MvPolynomial α ℤ :=
  RingEquiv.ofRingHom (FreeCommRing.lift <| (fun a => MvPolynomial.X a : α → MvPolynomial α ℤ))
    (MvPolynomial.eval₂Hom (Int.castRingHom (FreeCommRing α)) FreeCommRing.of)
    (by ext <;> simp) (by ext; simp)

/-- The free commutative ring on the empty type is isomorphic to `ℤ`. -/
/-
**freeCommRingPEmptyEquivInt** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：freeCommRingPEmptyEquivInt : FreeCommRing PEmpty.{u + 1} ≃+* Int
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The free commutative ring on the empty type is isomorphic to `ℤ`.
-/
def freeCommRingPEmptyEquivInt : FreeCommRing PEmpty.{u + 1} ≃+* ℤ :=
  RingEquiv.trans (freeCommRingEquivMvPolynomialInt _) (MvPolynomial.isEmptyRingEquiv _ PEmpty)

@[deprecated (since := "2026-02-08")]
noncomputable alias freeCommRingPemptyEquivInt := freeCommRingPEmptyEquivInt

/-- The free commutative ring on a type with one term is isomorphic to `ℤ[X]`. -/
/-
**freeCommRingPUnitEquivPolynomialInt** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：freeCommRingPUnitEquivPolynomialInt : FreeCommRing PUnit.{u + 1} ≃+* Int[X
]
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The free commutative ring on a type with one term is isomorphic to `ℤ[X]`.
-/
def freeCommRingPUnitEquivPolynomialInt : FreeCommRing PUnit.{u + 1} ≃+* ℤ[X] :=
  (freeCommRingEquivMvPolynomialInt _).trans (MvPolynomial.uniqueAlgEquiv ℤ PUnit).toRingEquiv

@[deprecated (since := "2026-02-08")]
noncomputable alias freeCommRingPunitEquivPolynomialInt := freeCommRingPUnitEquivPolynomialInt

open FreeRing

/-- The free ring on the empty type is isomorphic to `ℤ`. -/
/-
**freeRingPEmptyEquivInt** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：freeRingPEmptyEquivInt : FreeRing PEmpty.{u + 1} ≃+* Int
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `instSubsingletonPEmpty`：Subsingleton PEmpty.{u_1}

--- 原说明 ---
The free ring on the empty type is isomorphic to `ℤ`.
-/
def freeRingPEmptyEquivInt : FreeRing PEmpty.{u + 1} ≃+* ℤ :=
  RingEquiv.trans (subsingletonEquivFreeCommRing _) freeCommRingPEmptyEquivInt

@[deprecated (since := "2026-02-08")]
noncomputable alias freeRingPemptyEquivInt := freeRingPEmptyEquivInt

/-- The free ring on a type with one term is isomorphic to `ℤ[X]`. -/
/-
**freeRingPUnitEquivPolynomialInt** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：freeRingPUnitEquivPolynomialInt : FreeRing PUnit.{u + 1} ≃+* Int[X]
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `instSubsingletonPUnit`：Subsingleton PUnit.{u_1}

--- 原说明 ---
The free ring on a type with one term is isomorphic to `ℤ[X]`.
-/
def freeRingPUnitEquivPolynomialInt : FreeRing PUnit.{u + 1} ≃+* ℤ[X] :=
  RingEquiv.trans (subsingletonEquivFreeCommRing _) freeCommRingPUnitEquivPolynomialInt

@[deprecated (since := "2026-02-08")]
noncomputable alias freeRingPunitEquivPolynomialInt := freeRingPUnitEquivPolynomialInt
