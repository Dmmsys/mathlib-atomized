/-
Copyright (c) 2017 Johannes Hölzl. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Johannes Hölzl, Mario Carneiro
-/
module

public import Mathlib.MeasureTheory.MeasurableSpace.Constructions
public import Mathlib.Order.Filter.AtTopBot.CompleteLattice
public import Mathlib.Order.Filter.AtTopBot.CountablyGenerated
public import Mathlib.Order.Filter.SmallSets
public import Mathlib.Order.LiminfLimsup
public import Mathlib.Tactic.FinCases

/-!
# Measurably generated filters

We say that a filter `f` is measurably generated if every set `s ∈ f` includes a measurable
set `t ∈ f`. This property is useful, e.g., to extract a measurable witness of `Filter.Eventually`.
-/

public section

open Set Filter

universe uι

variable {α β γ δ : Type*} {ι : Sort uι}

namespace MeasurableSpace

/-- The sigma-algebra generated by a single set `s` is `{∅, s, sᶜ, univ}`. -/
/-
**MeasurableSpace.generateFrom_singleton** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSp
ace`。
形式化陈述：∀ {α : Type u_1} (s : Set α), MeasurableSpace.generateFrom {s} = Measurabl
eSpace.comap (fun x => x ∈ s) ⊤
参数：s : Set α；fun x => x ∈ s。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
· 使用定理 `trivial`：True
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Set.preimage_singleton_true`：∀ {α : Type u_1} (p : α → Prop), p ⁻¹' {Tru
e} = {a | p a}
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `MeasurableSet.mem`：∀ {α : Type u_1} {s : Set α} [inst : MeasurableSpace 
α], MeasurableSet s → Measurable fun x => x ∈ s
· 使用定理 `Set.mem_singleton`：mem_singleton (a : α) : a in ({a} : Set α)

--- 原说明 ---
The sigma-algebra generated by a single set `s` is `{∅, s, sᶜ, univ}`.
-/
@[simp] theorem generateFrom_singleton (s : Set α) :
    generateFrom {s} = MeasurableSpace.comap (· ∈ s) ⊤ := by
  let : MeasurableSpace α := generateFrom {s}
  refine le_antisymm (generateFrom_le fun t ht => ⟨{True}, trivial, by simp [ht.symm]⟩) ?_
  rintro _ ⟨u, -, rfl⟩
  exact (show MeasurableSet s from GenerateMeasurable.basic _ <| mem_singleton s).mem trivial
/-
**MeasurableSpace.generateFrom_singleton_le** 是 Mathlib 中的一个引理，位于命名空间 `Measurabl
eSpace`。
形式化陈述：generateFrom_singleton_le {m : MeasurableSpace α} {s : Set α} (hs : Measur
ableSet s) : MeasurableSpace.generateFrom {s} <= m
参数：hs : MeasurableSet s。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Set.mem_singleton_iff`：mem_singleton_iff {a b : α} : a in ({b} : Set α) 
↔ a = b
-/
lemma generateFrom_singleton_le {m : MeasurableSpace α} {s : Set α} (hs : MeasurableSet s) :
    MeasurableSpace.generateFrom {s} ≤ m :=
  generateFrom_le (fun _ ht ↦ mem_singleton_iff.1 ht ▸ hs)
/-
**MeasurableSpace.comap_indicator_const_le_generateFrom_singleton** 是 Mathlib 中的
一个引理，位于命名空间 `MeasurableSpace`。
形式化陈述：comap_indicator_const_le_generateFrom_singleton {M : Type*} [Zero M] [Meas
urableSpace M] (s : Set α) (c : M) : MeasurableSpace.comap (s.indicator (fun _ =
> c)) inferInstance <= MeasurableSpace.generateFrom {s}
参数：s : Set α；c : M。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Measurable.comap_le`：∀ {α : Type u_1} {β : Type u_2} {m₁ : MeasurableSpa
ce α} {m₂ : MeasurableSpace β} {f : α → β},   Measurable f → MeasurableSpace.com
ap f m₂ ≤…
· 使用定理 `Measurable.indicator`：Measurable.indicator [Zero β] (hf : Measurable f) 
(hs : MeasurableSet s) : Measurable (s.indicator f)
· 使用定理 `measurable_const`：measurable_const {_ : MeasurableSpace α} {_ : Measurab
leSpace β} {a : α} : Measurable fun _ : β => a
· 使用定理 `MeasurableSpace.measurableSet_generateFrom`：measurableSet_generateFrom {
s : Set (Set α)} {t : Set α} (ht : t in s) : MeasurableSet[generateFrom s] t
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma comap_indicator_const_le_generateFrom_singleton {M : Type*} [Zero M] [MeasurableSpace M]
    (s : Set α) (c : M) :
    MeasurableSpace.comap (s.indicator (fun _ ↦ c)) inferInstance ≤
      MeasurableSpace.generateFrom {s} :=
  (measurable_const.indicator (measurableSet_generateFrom (by simp))).comap_le

end MeasurableSpace

namespace MeasureTheory

/-
**MeasureTheory.measurableSet_generateFrom_singleton_iff** 是 Mathlib 中的一个定理，位于命名
空间 `MeasureTheory`。
形式化陈述：measurableSet_generateFrom_singleton_iff {s t : Set α} : MeasurableSet[Mea
surableSpace.generateFrom {s}] t ↔ t = ∅ ∨ t = s ∨ t = sᶜ ∨ t = univ
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSpace.generateFrom_singleton`：∀ {α : Type u_1} (s : Set α), Me
asurableSpace.generateFrom {s} = MeasurableSpace.comap (fun x => x ∈ s) ⊤
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `true_and`：∀ (p : Prop), (True ∧ p) = p
· 使用定理 `Fintype.complete`：∀ {α : Type u_4} [self : Fintype α] (x : α), x ∈ Finty
pe.elems
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `noConfusion_of_Nat`：∀ {α : Sort u} (f : α → ℕ) {a b : α}, a = b → Bool.r
ec False True ((f a).beq (f b))
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Set.preimage_singleton_true`：∀ {α : Type u_1} (p : α → Prop), p ⁻¹' {Tru
e} = {a | p a}
· 使用定理 `Set.preimage_singleton_false`：∀ {α : Type u_1} (p : α → Prop), p ⁻¹' {Fa
lse} = {a | ¬p a}
-/
theorem measurableSet_generateFrom_singleton_iff {s t : Set α} :
    MeasurableSet[MeasurableSpace.generateFrom {s}] t ↔ t = ∅ ∨ t = s ∨ t = sᶜ ∨ t = univ := by
  simp_rw +instances [MeasurableSpace.generateFrom_singleton]
  unfold MeasurableSet MeasurableSpace.MeasurableSet' MeasurableSpace.comap
  simp_rw [MeasurableSpace.measurableSet_top, true_and]
  constructor
  · rintro ⟨x, rfl⟩
    by_cases hT : True ∈ x
    · by_cases hF : False ∈ x
      · suffices x = univ by grind
        grind [univ_eq_true_false]
      · grind
    · by_cases hF : False ∈ x
      · grind
      · suffices x ⊆ ∅ by grind
        intro p hp
        fin_cases p <;> contradiction
  · rintro (rfl | rfl | rfl | rfl)
    on_goal 1 => use ∅
    on_goal 2 => use {True}
    on_goal 3 => use {False}
    on_goal 4 => use Set.univ
    all_goals
      simp [compl_def]

end MeasureTheory

namespace Filter

variable [MeasurableSpace α]

/-- A filter `f` is measurably generated if each `s ∈ f` includes a measurable `t ∈ f`. -/
/-
**Filter.IsMeasurablyGenerated** 是 Mathlib 中的一个归纳类型，位于命名空间 `Filter`。
形式化陈述：{α : Type u_1} → [MeasurableSpace α] → Filter α → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A filter `f` is measurably generated if each `s ∈ f` includes a measurable `t ∈ 
f`.
-/
class IsMeasurablyGenerated (f : Filter α) : Prop where
  exists_measurable_subset : ∀ ⦃s⦄, s ∈ f → ∃ t ∈ f, MeasurableSet t ∧ t ⊆ s
/-
**Filter.isMeasurablyGenerated_bot** 是 Mathlib 中的一个实例，位于命名空间 `Filter`。
形式化陈述：isMeasurablyGenerated_bot : IsMeasurablyGenerated (⊥ : Filter α)
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Filter.mem_bot`：mem_bot {s : Set α} : s in (⊥ : Filter α)
· 使用定理 `MeasurableSet.empty`：MeasurableSet.empty [MeasurableSpace α] : Measurabl
eSet (∅ : Set α)
· 使用定理 `Set.empty_subset`：empty_subset (s : Set α) : ∅ subseteq s
-/
instance isMeasurablyGenerated_bot : IsMeasurablyGenerated (⊥ : Filter α) :=
  ⟨fun _ _ => ⟨∅, mem_bot, MeasurableSet.empty, empty_subset _⟩⟩
/-
**Filter.isMeasurablyGenerated_top** 是 Mathlib 中的一个实例，位于命名空间 `Filter`。
形式化陈述：isMeasurablyGenerated_top : IsMeasurablyGenerated (⊤ : Filter α)
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Filter.univ_mem`：univ_mem : univ in f
· 使用定理 `MeasurableSet.univ`：∀ {α : Type u_1} {m : MeasurableSpace α}, Measurable
Set Set.univ
-/
instance isMeasurablyGenerated_top : IsMeasurablyGenerated (⊤ : Filter α) :=
  ⟨fun _s hs => ⟨univ, univ_mem, MeasurableSet.univ, fun x _ => hs x⟩⟩
/-
**Filter.Eventually.exists_measurable_mem** 是 Mathlib 中的一个定理，位于命名空间 `Filter.Even
tually`。
形式化陈述：∀ {α : Type u_1} [inst : MeasurableSpace α] {f : Filter α} [f.IsMeasurably
Generated] {p : α → Prop},   (∀ᶠ (x : α) in f, p x) → ∃ s ∈ f, MeasurableSet s ∧
 ∀ x ∈ s, p x
参数：∀ᶠ (x : α) in f, p x。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Filter.IsMeasurablyGenerated.exists_measurable_subset`：∀ {α : Type u_1} 
{inst : MeasurableSpace α} {f : Filter α} [self : f.IsMeasurablyGenerated] ⦃s : 
Set α⦄,   s ∈ f → ∃ t ∈ f, MeasurableSet t …
-/
theorem Eventually.exists_measurable_mem {f : Filter α} [IsMeasurablyGenerated f] {p : α → Prop}
    (h : ∀ᶠ x in f, p x) : ∃ s ∈ f, MeasurableSet s ∧ ∀ x ∈ s, p x :=
  IsMeasurablyGenerated.exists_measurable_subset h
/-
**Filter.Eventually.exists_measurable_mem_of_smallSets** 是 Mathlib 中的一个定理，位于命名空间
 `Filter.Eventually`。
形式化陈述：∀ {α : Type u_1} [inst : MeasurableSpace α] {f : Filter α} [f.IsMeasurably
Generated] {p : Set α → Prop},   (∀ᶠ (s : Set α) in f.smallSets, p s) → ∃ s ∈ f,
 MeasurableSet s ∧ p s
参数：∀ᶠ (s : Set α) in f.smallSets, p s。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Filter.eventually_smallSets`：eventually_smallSets {p : Set α -> Prop} : 
(forallᶠ s in l.smallSets, p s) ↔ exists s in l, forall t, t subseteq s -> p t
· 使用定理 `Filter.IsMeasurablyGenerated.exists_measurable_subset`：∀ {α : Type u_1} 
{inst : MeasurableSpace α} {f : Filter α} [self : f.IsMeasurablyGenerated] ⦃s : 
Set α⦄,   s ∈ f → ∃ t ∈ f, MeasurableSet t …
-/
theorem Eventually.exists_measurable_mem_of_smallSets {f : Filter α} [IsMeasurablyGenerated f]
    {p : Set α → Prop} (h : ∀ᶠ s in f.smallSets, p s) : ∃ s ∈ f, MeasurableSet s ∧ p s :=
  let ⟨_s, hsf, hs⟩ := eventually_smallSets.1 h
  let ⟨t, htf, htm, hts⟩ := IsMeasurablyGenerated.exists_measurable_subset hsf
  ⟨t, htf, htm, hs t hts⟩
/-
**Filter.inf_isMeasurablyGenerated** 是 Mathlib 中的一个实例，位于命名空间 `Filter`。
形式化陈述：inf_isMeasurablyGenerated (f g : Filter α) [IsMeasurablyGenerated f] [IsMe
asurablyGenerated g] : IsMeasurablyGenerated (f ⊓ g)
参数：f g : Filter α。
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Filter.IsMeasurablyGenerated.exists_measurable_subset`：∀ {α : Type u_1} 
{inst : MeasurableSpace α} {f : Filter α} [self : f.IsMeasurablyGenerated] ⦃s : 
Set α⦄,   s ∈ f → ∃ t ∈ f, MeasurableSet t …
· 使用定理 `Filter.inter_mem_inf`：inter_mem_inf {α : Type u} {f g : Filter α} {s t :
 Set α} (hs : s in f) (ht : t in g) : s inter t in f ⊓ g
· 使用定理 `MeasurableSet.inter`：∀ {α : Type u_1} {m : MeasurableSpace α} {s₁ s₂ : S
et α}, MeasurableSet s₁ → MeasurableSet s₂ → MeasurableSet (s₁ ∩ s₂)
· 使用定理 `Set.inter_subset_inter`：inter_subset_inter {s₁ s₂ t₁ t₂ : Set α} (h₁ : s
₁ subseteq t₁) (h₂ : s₂ subseteq t₂) : s₁ inter s₂ subseteq t₁ inter t₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
-/
instance inf_isMeasurablyGenerated (f g : Filter α) [IsMeasurablyGenerated f]
    [IsMeasurablyGenerated g] : IsMeasurablyGenerated (f ⊓ g) := by
  constructor
  rintro t ⟨sf, hsf, sg, hsg, rfl⟩
  rcases IsMeasurablyGenerated.exists_measurable_subset hsf with ⟨s'f, hs'f, hmf, hs'sf⟩
  rcases IsMeasurablyGenerated.exists_measurable_subset hsg with ⟨s'g, hs'g, hmg, hs'sg⟩
  refine ⟨s'f ∩ s'g, inter_mem_inf hs'f hs'g, hmf.inter hmg, ?_⟩
  exact inter_subset_inter hs'sf hs'sg
/-
**Filter.principal_isMeasurablyGenerated_iff** 是 Mathlib 中的一个定理，位于命名空间 `Filter`。
形式化陈述：principal_isMeasurablyGenerated_iff {s : Set α} : IsMeasurablyGenerated (𝓟
 s) ↔ MeasurableSet s
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Filter.mem_principal_self`：mem_principal_self (s : Set α) : s in 𝓟 s
· 使用定理 `LE.le.antisymm`：∀ {α : Type u_1} [inst : PartialOrder α] {a b : α}, a ≤ 
b → b ≤ a → a = b
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
-/
theorem principal_isMeasurablyGenerated_iff {s : Set α} :
    IsMeasurablyGenerated (𝓟 s) ↔ MeasurableSet s := by
  refine ⟨?_, fun hs => ⟨fun t ht => ⟨s, mem_principal_self s, hs, ht⟩⟩⟩
  rintro ⟨hs⟩
  rcases hs (mem_principal_self s) with ⟨t, ht, htm, hts⟩
  have : t = s := hts.antisymm ht
  rwa [← this]

alias ⟨_, _root_.MeasurableSet.principal_isMeasurablyGenerated⟩ :=
  principal_isMeasurablyGenerated_iff
/-
**Filter.iInf_isMeasurablyGenerated** 是 Mathlib 中的一个实例，位于命名空间 `Filter`。
形式化陈述：iInf_isMeasurablyGenerated {f : ι -> Filter α} [forall i, IsMeasurablyGene
rated (f i)] : IsMeasurablyGenerated (⨅ i, f i)
参数：f i。
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Filter.mem_iInf`：mem_iInf {ι} {s : ι -> Filter α} {U : Set α} : (U in ⨅ 
i, s i) ↔ exists I : Set ι, I.Finite ∧ exists V : I -> Set α, (forall (i : I), V
 i in…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Function.Surjective.iInf_comp`：∀ {α : Type u_1} {ι : Sort u_4} {ι' : Sor
t u_5} [inst : InfSet α] {f : ι → ι'},   Function.Surjective f → ∀ (g : ι' → α),
 ⨅ x, g (f x) = ⨅ y…
· 使用定理 `Equiv.surjective`：∀ {α : Sort u} {β : Sort v} (e : α ≃ β), Function.Surj
ective ⇑e
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `Encodable.countable`：∀ {α : Type u_1} [Encodable α], Countable α
· 使用定理 `Set.Finite.countable`：∀ {α : Type u} {s : Set α}, s.Finite → s.Countable
· 使用定理 `MeasurableSet.iInter`：MeasurableSet.iInter [Countable ι] {f : ι -> Set α
} (h : forall b, MeasurableSet (f b)) : MeasurableSet (⋂ b, f b)
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b
· 使用定理 `Set.iInter_mono`：iInter_mono {s t : ι -> Set α} (h : forall i, s i subse
teq t i) : ⋂ i, s i subseteq ⋂ i, t i
· 使用定理 `Filter.IsMeasurablyGenerated.exists_measurable_subset`：∀ {α : Type u_1} 
{inst : MeasurableSpace α} {f : Filter α} [self : f.IsMeasurablyGenerated] ⦃s : 
Set α⦄,   s ∈ f → ∃ t ∈ f, MeasurableSet t …
· 使用定理 `Classical.choose_spec`：∀ {α : Sort u} {p : α → Prop} (h : ∃ x, p x), p (
Classical.choose h)
-/
instance iInf_isMeasurablyGenerated {f : ι → Filter α} [∀ i, IsMeasurablyGenerated (f i)] :
    IsMeasurablyGenerated (⨅ i, f i) := by
  refine ⟨fun s hs => ?_⟩
  rw [← Equiv.plift.surjective.iInf_comp, mem_iInf] at hs
  rcases hs with ⟨t, ht, ⟨V, hVf, rfl⟩⟩
  choose U hUf hU using fun i => IsMeasurablyGenerated.exists_measurable_subset (hVf i)
  refine ⟨⋂ i : t, U i, ?_, ?_, ?_⟩
  · rw [← Equiv.plift.surjective.iInf_comp, mem_iInf]
    exact ⟨t, ht, U, hUf, rfl⟩
  · have := ht.countable.toEncodable.countable
    exact MeasurableSet.iInter fun i => (hU i).1
  · exact iInter_mono fun i => (hU i).2

end Filter

/-- The set of points for which a sequence of measurable functions converges to a given value
is measurable. -/
@[measurability]
/-
**measurableSet_tendsto** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：measurableSet_tendsto {_ : MeasurableSpace β} [MeasurableSpace γ] [Countab
le δ] {l : Filter δ} [l.IsCountablyGenerated] (l' : Filter γ) [l'.IsCountablyGen
erated] [hl' : l'.IsMeasurablyGenerated] {f : δ -> β -> γ} (hf : forall i, Measu
rable (f i)) : MeasurableSet { x | Tendsto (fun n => f n x) l l' }
参数：l' : Filter γ；hf : forall i, Measurable (f i)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Filter.exists_antitone_basis`：exists_antitone_basis (f : Filter α) [f.Is
CountablyGenerated] : exists x : Nat -> Set α, f.HasAntitoneBasis x
· 使用定理 `Filter.HasBasis.exists_antitone_subbasis`：∀ {α : Type u_1} {ι' : Sort u_
5} {f : Filter α} [h : f.IsCountablyGenerated] {p : ι' → Prop} {s : ι' → Set α},
   f.HasBasis p s → ∃ x, (∀ (i…
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Filter.hasBasis_self`：hasBasis_self {l : Filter α} {P : Set α -> Prop} :
 HasBasis l (fun s => s in l ∧ P s) id ↔ forall t in l, exists r in l, P r ∧ r s
ubseteq t
· 使用定理 `Filter.IsMeasurablyGenerated.exists_measurable_subset`：∀ {α : Type u_1} 
{inst : MeasurableSpace α} {f : Filter α} [self : f.IsMeasurablyGenerated] ⦃s : 
Set α⦄,   s ∈ f → ∃ t ∈ f, MeasurableSet t …
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Filter.HasBasis.tendsto_iff`：∀ {α : Type u_1} {β : Type u_2} {ι : Sort u
_4} {ι' : Sort u_5} {la : Filter α} {pa : ι → Prop} {sa : ι → Set α}   {lb : Fil
ter β} {pb : ι' →…
· 使用定理 `Filter.HasAntitoneBasis.toHasBasis`：∀ {α : Type u_1} {ι'' : Type u_6} [i
nst : Preorder ι''] {l : Filter α} {s : ι'' → Set α},   l.HasAntitoneBasis s → l
.HasBasis (fun x => True…
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `true_and`：∀ (p : Prop), (True ∧ p) = p
· 使用定理 `Set.ofPred_forall`：ofPred_forall (p : ι -> β -> Prop) : { x | forall i, 
p i x } = ⋂ i, { x | p i x }
· 使用定理 `Set.ofPred_exists`：ofPred_exists (p : ι -> β -> Prop) : { x | exists i, 
p i x } = ⋃ i, { x | p i x }
· 使用定理 `MeasurableSet.iInter`：MeasurableSet.iInter [Countable ι] {f : ι -> Set α
} (h : forall b, MeasurableSet (f b)) : MeasurableSet (⋂ b, f b)
· 使用定理 `instCountableNat`：Countable ℕ
· 使用定理 `MeasurableSet.iUnion`：∀ {α : Type u_1} {ι : Sort u_6} {m : MeasurableSpa
ce α} [Countable ι] ⦃f : ι → Set α⦄,   (∀ (b : ι), MeasurableSet (f b)) → Measur
ableSet (⋃…
· 使用定理 `MeasurableSet.biInter`：MeasurableSet.biInter {f : β -> Set α} {s : Set β
} (hs : s.Countable) (h : forall b in s, MeasurableSet (f b)) : MeasurableSet (⋂
 b in s, f …
· 使用定理 `Set.to_countable`：to_countable (s : Set α) [Countable s] : s.Countable
· 使用定理 `SetCoe.countable`：∀ {α : Type u} [Countable α] (s : Set α), Countable ↑s
· 使用定理 `MeasurableSet.preimage`：∀ {α : Type u_1} {β : Type u_2} {f : α → β} {m :
 MeasurableSpace α} {mβ : MeasurableSpace β} {t : Set β},   MeasurableSet t → Me
asurable f →…
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b

--- 原说明 ---
The set of points for which a sequence of measurable functions converges to a gi
ven value
is measurable.
-/
lemma measurableSet_tendsto {_ : MeasurableSpace β} [MeasurableSpace γ]
    [Countable δ] {l : Filter δ} [l.IsCountablyGenerated]
    (l' : Filter γ) [l'.IsCountablyGenerated] [hl' : l'.IsMeasurablyGenerated]
    {f : δ → β → γ} (hf : ∀ i, Measurable (f i)) :
    MeasurableSet { x | Tendsto (fun n ↦ f n x) l l' } := by
  rcases l.exists_antitone_basis with ⟨u, hu⟩
  rcases (Filter.hasBasis_self.mpr hl'.exists_measurable_subset).exists_antitone_subbasis with
    ⟨v, v_meas, hv⟩
  simp only [hu.tendsto_iff hv.toHasBasis, true_imp_iff, true_and, ofPred_forall, ofPred_exists]
  exact .iInter fun n ↦ .iUnion fun _ ↦ .biInter (to_countable _) fun i _ ↦
    (v_meas n).2.preimage (hf i)

namespace MeasurableSet

variable [MeasurableSpace α]

/-
**MeasurableSet.iUnion_of_monotone_of_frequently** 是 Mathlib 中的一个定理，位于命名空间 `Meas
urableSet`。
形式化陈述：∀ {α : Type u_1} [inst : MeasurableSpace α] {ι : Type u_5} [inst_1 : Preor
der ι] [Filter.atTop.IsCountablyGenerated]   {s : ι → Set α}, Monotone s → (∃ᶠ (
i : ι) in Filter.atTop, MeasurableSet (s i)) → MeasurableSet (⋃ i, s i)
参数：∃ᶠ (i : ι) in Filter.atTop, MeasurableSet (s i)；⋃ i, s i。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Filter.exists_seq_forall_of_frequently`：exists_seq_forall_of_frequently 
{ι : Type*} {l : Filter ι} {p : ι -> Prop} [l.IsCountablyGenerated] (h : existsᶠ
 n in l, p n) : exists ns : …
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Monotone.iUnion_comp_tendsto_atTop`：∀ {α : Type u_3} {β : Type u_4} {γ :
 Type u_5} [inst : Preorder β] {l : Filter α} [l.NeBot] {s : β → Set γ},   Monot
one s → ∀ {f : α → β}, F…
· 使用定理 `SemilatticeSup.instIsDirectedOrder`：∀ {α : Type u_1} [inst : Semilattice
Sup α], IsDirectedOrder α
· 使用定理 `instNonemptyOfInhabited`：∀ {α : Sort u} [Inhabited α], Nonempty α
· 使用定理 `MeasurableSet.iUnion`：∀ {α : Type u_1} {ι : Sort u_6} {m : MeasurableSpa
ce α} [Countable ι] ⦃f : ι → Set α⦄,   (∀ (b : ι), MeasurableSet (f b)) → Measur
ableSet (⋃…
· 使用定理 `instCountableNat`：Countable ℕ
-/
protected theorem iUnion_of_monotone_of_frequently
    {ι : Type*} [Preorder ι] [(atTop : Filter ι).IsCountablyGenerated] {s : ι → Set α}
    (hsm : Monotone s) (hs : ∃ᶠ i in atTop, MeasurableSet (s i)) : MeasurableSet (⋃ i, s i) := by
  rcases exists_seq_forall_of_frequently hs with ⟨x, hx, hxm⟩
  rw [← hsm.iUnion_comp_tendsto_atTop hx]
  exact .iUnion hxm
/-
**MeasurableSet.iInter_of_antitone_of_frequently** 是 Mathlib 中的一个定理，位于命名空间 `Meas
urableSet`。
形式化陈述：∀ {α : Type u_1} [inst : MeasurableSpace α] {ι : Type u_5} [inst_1 : Preor
der ι] [Filter.atTop.IsCountablyGenerated]   {s : ι → Set α}, Antitone s → (∃ᶠ (
i : ι) in Filter.atTop, MeasurableSet (s i)) → MeasurableSet (⋂ i, s i)
参数：∃ᶠ (i : ι) in Filter.atTop, MeasurableSet (s i)；⋂ i, s i。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MeasurableSet.compl_iff`：MeasurableSet.compl_iff : MeasurableSet sᶜ ↔ Me
asurableSet s
· 使用定理 `Set.compl_iInter`：compl_iInter (s : ι -> Set β) : (⋂ i, s i)ᶜ = ⋃ i, (s 
i)ᶜ
· 使用定理 `MeasurableSet.iUnion_of_monotone_of_frequently`：∀ {α : Type u_1} [inst :
 MeasurableSpace α] {ι : Type u_5} [inst_1 : Preorder ι] [Filter.atTop.IsCountab
lyGenerated]   {s : ι → Set α}, Mono…
· 使用定理 `Antitone.comp`：∀ {α : Type u} {β : Type v} {γ : Type w} [inst : Preorder
 α] [inst_1 : Preorder β] [inst_2 : Preorder γ] {g : β → γ}   {f : α → β}, Antit
one…
· 使用定理 `compl_anti`：compl_anti : Antitone (compl : α -> α)
· 使用定理 `Filter.Frequently.mono`：∀ {α : Type u} {p q : α → Prop} {f : Filter α}, 
(∃ᶠ (x : α) in f, p x) → (∀ (x : α), p x → q x) → ∃ᶠ (x : α) in f, q x
· 使用定理 `MeasurableSet.compl`：∀ {α : Type u_1} {s : Set α} {m : MeasurableSpace α
}, MeasurableSet s → MeasurableSet sᶜ
-/
protected theorem iInter_of_antitone_of_frequently
    {ι : Type*} [Preorder ι] [(atTop : Filter ι).IsCountablyGenerated] {s : ι → Set α}
    (hsm : Antitone s) (hs : ∃ᶠ i in atTop, MeasurableSet (s i)) : MeasurableSet (⋂ i, s i) := by
  rw [← compl_iff, compl_iInter]
  exact .iUnion_of_monotone_of_frequently (compl_anti.comp hsm) <| hs.mono fun _ ↦ .compl
/-
**MeasurableSet.iUnion_of_monotone** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：∀ {α : Type u_1} [inst : MeasurableSpace α] {ι : Type u_5} [inst_1 : Preor
der ι] [IsDirectedOrder ι]   [Filter.atTop.IsCountablyGenerated] {s : ι → Set α}
,   Monotone s → (∀ (i : ι), MeasurableSet (s i)) → MeasurableSet (⋃ i, s i)
参数：∀ (i : ι), MeasurableSet (s i)；⋃ i, s i。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `isEmpty_or_nonempty`：isEmpty_or_nonempty : IsEmpty α ∨ Nonempty α
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Set.iUnion_of_empty`：iUnion_of_empty [IsEmpty ι] (s : ι -> Set α) : ⋃ i,
 s i = ∅
· 使用定理 `MeasurableSet.iUnion_of_monotone_of_frequently`：∀ {α : Type u_1} [inst :
 MeasurableSpace α] {ι : Type u_5} [inst_1 : Preorder ι] [Filter.atTop.IsCountab
lyGenerated]   {s : ι → Set α}, Mono…
· 使用定理 `Filter.Frequently.of_forall`：∀ {α : Type u} {f : Filter α} [f.NeBot] {p 
: α → Prop}, (∀ (x : α), p x) → ∃ᶠ (x : α) in f, p x
-/
protected theorem iUnion_of_monotone {ι : Type*} [Preorder ι] [IsDirectedOrder ι]
    [(atTop : Filter ι).IsCountablyGenerated] {s : ι → Set α}
    (hsm : Monotone s) (hs : ∀ i, MeasurableSet (s i)) : MeasurableSet (⋃ i, s i) := by
  cases isEmpty_or_nonempty ι with
  | inl _ => simp
  | inr _ => exact .iUnion_of_monotone_of_frequently hsm <| .of_forall hs
/-
**MeasurableSet.iInter_of_antitone** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：∀ {α : Type u_1} [inst : MeasurableSpace α] {ι : Type u_5} [inst_1 : Preor
der ι] [IsDirectedOrder ι]   [Filter.atTop.IsCountablyGenerated] {s : ι → Set α}
,   Antitone s → (∀ (i : ι), MeasurableSet (s i)) → MeasurableSet (⋂ i, s i)
参数：∀ (i : ι), MeasurableSet (s i)；⋂ i, s i。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MeasurableSet.compl_iff`：MeasurableSet.compl_iff : MeasurableSet sᶜ ↔ Me
asurableSet s
· 使用定理 `Set.compl_iInter`：compl_iInter (s : ι -> Set β) : (⋂ i, s i)ᶜ = ⋃ i, (s 
i)ᶜ
· 使用定理 `MeasurableSet.iUnion_of_monotone`：∀ {α : Type u_1} [inst : MeasurableSpa
ce α] {ι : Type u_5} [inst_1 : Preorder ι] [IsDirectedOrder ι]   [Filter.atTop.I
sCountablyGenerated] {…
· 使用定理 `Antitone.comp`：∀ {α : Type u} {β : Type v} {γ : Type w} [inst : Preorder
 α] [inst_1 : Preorder β] [inst_2 : Preorder γ] {g : β → γ}   {f : α → β}, Antit
one…
· 使用定理 `compl_anti`：compl_anti : Antitone (compl : α -> α)
· 使用定理 `MeasurableSet.compl`：∀ {α : Type u_1} {s : Set α} {m : MeasurableSpace α
}, MeasurableSet s → MeasurableSet sᶜ
-/
protected theorem iInter_of_antitone {ι : Type*} [Preorder ι] [IsDirectedOrder ι]
    [(atTop : Filter ι).IsCountablyGenerated] {s : ι → Set α}
    (hsm : Antitone s) (hs : ∀ i, MeasurableSet (s i)) : MeasurableSet (⋂ i, s i) := by
  rw [← compl_iff, compl_iInter]
  exact .iUnion_of_monotone (compl_anti.comp hsm) fun i ↦ (hs i).compl

/-!
### Typeclasses on `Subtype MeasurableSet`
-/

/-
**MeasurableSet.Subtype.instMembership** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSet.
Subtype`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → Membership α (Subtype Measur
ableSet)
参数：Subtype MeasurableSet。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
### Typeclasses on `Subtype MeasurableSet`
-/
instance Subtype.instMembership : Membership α (Subtype (MeasurableSet : Set α → Prop)) :=
  ⟨fun s a => a ∈ (s : Set α)⟩

@[simp]
/-
**MeasurableSet.mem_coe** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：mem_coe (a : α) (s : Subtype (MeasurableSet : Set α -> Prop)) : a in (s : 
Set α) ↔ a in s
参数：a : α；s : Subtype (MeasurableSet : Set α -> Prop)。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem mem_coe (a : α) (s : Subtype (MeasurableSet : Set α → Prop)) : a ∈ (s : Set α) ↔ a ∈ s :=
  Iff.rfl
/-
**MeasurableSet.Subtype.instEmptyCollection** 是 Mathlib 中的一个定义，位于命名空间 `Measurabl
eSet.Subtype`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → EmptyCollection (Subtype Mea
surableSet)
参数：Subtype MeasurableSet。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSet.empty`：MeasurableSet.empty [MeasurableSpace α] : Measurabl
eSet (∅ : Set α)
-/
instance Subtype.instEmptyCollection : EmptyCollection (Subtype (MeasurableSet : Set α → Prop)) :=
  ⟨⟨∅, MeasurableSet.empty⟩⟩

@[simp]
/-
**MeasurableSet.coe_empty** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：coe_empty : ↑(∅ : Subtype (MeasurableSet : Set α -> Prop)) = (∅ : Set α)
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem coe_empty : ↑(∅ : Subtype (MeasurableSet : Set α → Prop)) = (∅ : Set α) :=
  rfl
/-
**MeasurableSet.Subtype.instInsert** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSet.Subt
ype`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → [MeasurableSingletonClass α]
 → Insert α (Subtype MeasurableSet)
参数：Subtype MeasurableSet。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance Subtype.instInsert [MeasurableSingletonClass α] :
    Insert α (Subtype (MeasurableSet : Set α → Prop)) :=
  ⟨fun a s => ⟨insert a (s : Set α), s.prop.insert a⟩⟩

@[simp]
/-
**MeasurableSet.coe_insert** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：coe_insert [MeasurableSingletonClass α] (a : α) (s : Subtype (MeasurableSe
t : Set α -> Prop)) : ↑(Insert.insert a s) = (Insert.insert a s : Set α)
参数：a : α；s : Subtype (MeasurableSet : Set α -> Prop)。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem coe_insert [MeasurableSingletonClass α] (a : α)
    (s : Subtype (MeasurableSet : Set α → Prop)) :
    ↑(Insert.insert a s) = (Insert.insert a s : Set α) :=
  rfl
/-
**MeasurableSet.Subtype.instSingleton** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSet.S
ubtype`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → [MeasurableSingletonClass α]
 → Singleton α (Subtype MeasurableSet)
参数：Subtype MeasurableSet。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSet.singleton`：MeasurableSet.singleton [MeasurableSpace α] [Me
asurableSingletonClass α] (a : α) : MeasurableSet {a}
-/
instance Subtype.instSingleton [MeasurableSingletonClass α] :
    Singleton α (Subtype (MeasurableSet : Set α → Prop)) :=
  ⟨fun a => ⟨{a}, .singleton _⟩⟩
/-
**MeasurableSet.coe_singleton** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：∀ {α : Type u_1} [inst : MeasurableSpace α] [inst_1 : MeasurableSingletonC
lass α] (a : α), ↑{a} = {a}
参数：a : α。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
@[simp] theorem coe_singleton [MeasurableSingletonClass α] (a : α) :
    ↑({a} : Subtype (MeasurableSet : Set α → Prop)) = ({a} : Set α) :=
  rfl
/-
**MeasurableSet.Subtype.instLawfulSingleton** 是 Mathlib 中的一个定理，位于命名空间 `Measurabl
eSet.Subtype`。
形式化陈述：∀ {α : Type u_1} [inst : MeasurableSpace α] [inst_1 : MeasurableSingletonC
lass α],   LawfulSingleton α (Subtype MeasurableSet)
参数：Subtype MeasurableSet。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subtype.ext`：∀ {α : Sort u} {p : α → Prop} {a1 a2 : { x // p x }}, ↑a1 =
 ↑a2 → a1 = a2
· 使用定理 `LawfulSingleton.insert_empty_eq`：∀ {α : Type u} {β : Type v} {inst : Emp
tyCollection β} {inst_1 : Insert α β} {inst_2 : Singleton α β}   [self : LawfulS
ingleton α β] (x : α)…
· 使用定理 `Set.instLawfulSingleton`：∀ {α : Type u_1}, LawfulSingleton α (Set α)
-/
instance Subtype.instLawfulSingleton [MeasurableSingletonClass α] :
    LawfulSingleton α (Subtype (MeasurableSet : Set α → Prop)) :=
  ⟨fun _ => Subtype.ext <| insert_empty_eq _⟩
/-
**MeasurableSet.Subtype.instCompl** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSet.Subty
pe`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → Compl (Subtype MeasurableSet
)
参数：Subtype MeasurableSet。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance Subtype.instCompl : Compl (Subtype (MeasurableSet : Set α → Prop)) :=
  ⟨fun x => ⟨xᶜ, x.prop.compl⟩⟩

@[simp]
/-
**MeasurableSet.coe_compl** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：coe_compl (s : Subtype (MeasurableSet : Set α -> Prop)) : ↑sᶜ = (sᶜ : Set 
α)
参数：s : Subtype (MeasurableSet : Set α -> Prop)。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem coe_compl (s : Subtype (MeasurableSet : Set α → Prop)) : ↑sᶜ = (sᶜ : Set α) :=
  rfl
/-
**MeasurableSet.Subtype.instUnion** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSet.Subty
pe`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → Union (Subtype MeasurableSet
)
参数：Subtype MeasurableSet。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance Subtype.instUnion : Union (Subtype (MeasurableSet : Set α → Prop)) :=
  ⟨fun x y => ⟨(x : Set α) ∪ y, x.prop.union y.prop⟩⟩

@[simp]
/-
**MeasurableSet.coe_union** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：coe_union (s t : Subtype (MeasurableSet : Set α -> Prop)) : ↑(s union t) =
 (s union t : Set α)
参数：s t : Subtype (MeasurableSet : Set α -> Prop)。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem coe_union (s t : Subtype (MeasurableSet : Set α → Prop)) : ↑(s ∪ t) = (s ∪ t : Set α) :=
  rfl
/-
**MeasurableSet.Subtype.instSup** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSet.Subtype
`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → Max (Subtype MeasurableSet)
参数：Subtype MeasurableSet。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance Subtype.instSup : Max (Subtype (MeasurableSet : Set α → Prop)) :=
  ⟨fun x y => x ∪ y⟩

@[simp]
/-
**MeasurableSet.sup_eq_union** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：∀ {α : Type u_1} [inst : MeasurableSpace α] (s t : { s // MeasurableSet s 
}), s ⊔ t = s ∪ t
参数：s t : { s // MeasurableSet s }。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
protected theorem sup_eq_union (s t : {s : Set α // MeasurableSet s}) : s ⊔ t = s ∪ t := rfl
/-
**MeasurableSet.Subtype.instInter** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSet.Subty
pe`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → Inter (Subtype MeasurableSet
)
参数：Subtype MeasurableSet。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance Subtype.instInter : Inter (Subtype (MeasurableSet : Set α → Prop)) :=
  ⟨fun x y => ⟨x ∩ y, x.prop.inter y.prop⟩⟩

@[simp]
/-
**MeasurableSet.coe_inter** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：coe_inter (s t : Subtype (MeasurableSet : Set α -> Prop)) : ↑(s inter t) =
 (s inter t : Set α)
参数：s t : Subtype (MeasurableSet : Set α -> Prop)。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem coe_inter (s t : Subtype (MeasurableSet : Set α → Prop)) : ↑(s ∩ t) = (s ∩ t : Set α) :=
  rfl
/-
**MeasurableSet.Subtype.instInf** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSet.Subtype
`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → Min (Subtype MeasurableSet)
参数：Subtype MeasurableSet。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance Subtype.instInf : Min (Subtype (MeasurableSet : Set α → Prop)) :=
  ⟨fun x y => x ∩ y⟩

@[simp]
/-
**MeasurableSet.inf_eq_inter** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：∀ {α : Type u_1} [inst : MeasurableSpace α] (s t : { s // MeasurableSet s 
}), s ⊓ t = s ∩ t
参数：s t : { s // MeasurableSet s }。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
protected theorem inf_eq_inter (s t : {s : Set α // MeasurableSet s}) : s ⊓ t = s ∩ t := rfl
/-
**MeasurableSet.Subtype.instSDiff** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSet.Subty
pe`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → SDiff (Subtype MeasurableSet
)
参数：Subtype MeasurableSet。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance Subtype.instSDiff : SDiff (Subtype (MeasurableSet : Set α → Prop)) :=
  ⟨fun x y => ⟨x \ y, x.prop.diff y.prop⟩⟩

-- TODO: Why does it complain that `x ⇨ y` is noncomputable?
/-
**MeasurableSet.Subtype.instHImp** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSet.Subtyp
e`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → HImp (Subtype MeasurableSet)
参数：Subtype MeasurableSet。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
noncomputable instance Subtype.instHImp : HImp (Subtype (MeasurableSet : Set α → Prop)) where
  himp x y := ⟨x ⇨ y, x.prop.himp y.prop⟩

@[simp]
/-
**MeasurableSet.coe_sdiff** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：coe_sdiff (s t : Subtype (MeasurableSet : Set α -> Prop)) : ↑(s \ t) = (s 
: Set α) \ t
参数：s t : Subtype (MeasurableSet : Set α -> Prop)。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem coe_sdiff (s t : Subtype (MeasurableSet : Set α → Prop)) : ↑(s \ t) = (s : Set α) \ t :=
  rfl

@[simp]
/-
**MeasurableSet.coe_himp** 是 Mathlib 中的一个引理，位于命名空间 `MeasurableSet`。
形式化陈述：coe_himp (s t : Subtype (MeasurableSet : Set α -> Prop)) : ↑(s ⇨ t) = (s ⇨
 t : Set α)
参数：s t : Subtype (MeasurableSet : Set α -> Prop)。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma coe_himp (s t : Subtype (MeasurableSet : Set α → Prop)) : ↑(s ⇨ t) = (s ⇨ t : Set α) := rfl
/-
**MeasurableSet.Subtype.instBot** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSet.Subtype
`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → Bot (Subtype MeasurableSet)
参数：Subtype MeasurableSet。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance Subtype.instBot : Bot (Subtype (MeasurableSet : Set α → Prop)) := ⟨∅⟩

@[simp]
/-
**MeasurableSet.coe_bot** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：coe_bot : ↑(⊥ : Subtype (MeasurableSet : Set α -> Prop)) = (⊥ : Set α)
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem coe_bot : ↑(⊥ : Subtype (MeasurableSet : Set α → Prop)) = (⊥ : Set α) :=
  rfl

@[simp]
/-
**MeasurableSet.subtype_bot_eq** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：subtype_bot_eq : (⟨∅, .empty⟩ : Subtype (MeasurableSet : Set α -> Prop)) =
 ⊥
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSet.empty`：MeasurableSet.empty [MeasurableSpace α] : Measurabl
eSet (∅ : Set α)
-/
theorem subtype_bot_eq : (⟨∅, .empty⟩ : Subtype (MeasurableSet : Set α → Prop)) = ⊥ :=
  rfl
/-
**MeasurableSet.Subtype.instTop** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSet.Subtype
`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → Top (Subtype MeasurableSet)
参数：Subtype MeasurableSet。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSet.univ`：∀ {α : Type u_1} {m : MeasurableSpace α}, Measurable
Set Set.univ
-/
instance Subtype.instTop : Top (Subtype (MeasurableSet : Set α → Prop)) :=
  ⟨⟨Set.univ, MeasurableSet.univ⟩⟩

@[simp]
/-
**MeasurableSet.coe_top** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：coe_top : ↑(⊤ : Subtype (MeasurableSet : Set α -> Prop)) = (⊤ : Set α)
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem coe_top : ↑(⊤ : Subtype (MeasurableSet : Set α → Prop)) = (⊤ : Set α) :=
  rfl
/-
**MeasurableSet.Subtype.instBooleanAlgebra** 是 Mathlib 中的一个定义，位于命名空间 `Measurable
Set.Subtype`。
形式化陈述：{α : Type u_1} → [inst : MeasurableSpace α] → BooleanAlgebra (Subtype Meas
urableSet)
参数：Subtype MeasurableSet。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSet.coe_union`：coe_union (s t : Subtype (MeasurableSet : Set α
 -> Prop)) : ↑(s union t) = (s union t : Set α)
· 使用定理 `MeasurableSet.coe_inter`：coe_inter (s t : Subtype (MeasurableSet : Set α
 -> Prop)) : ↑(s inter t) = (s inter t : Set α)
· 使用定理 `MeasurableSet.coe_top`：coe_top : ↑(⊤ : Subtype (MeasurableSet : Set α ->
 Prop)) = (⊤ : Set α)
· 使用定理 `MeasurableSet.coe_bot`：coe_bot : ↑(⊥ : Subtype (MeasurableSet : Set α ->
 Prop)) = (⊥ : Set α)
· 使用定理 `MeasurableSet.coe_compl`：coe_compl (s : Subtype (MeasurableSet : Set α -
> Prop)) : ↑sᶜ = (sᶜ : Set α)
· 使用定理 `MeasurableSet.coe_sdiff`：coe_sdiff (s t : Subtype (MeasurableSet : Set α
 -> Prop)) : ↑(s \ t) = (s : Set α) \ t
· 使用引理 `MeasurableSet.coe_himp`：coe_himp (s t : Subtype (MeasurableSet : Set α -
> Prop)) : ↑(s ⇨ t) = (s ⇨ t : Set α)
-/
noncomputable instance Subtype.instBooleanAlgebra :
    BooleanAlgebra (Subtype (MeasurableSet : Set α → Prop)) :=
  Subtype.coe_injective.booleanAlgebra _ .rfl .rfl coe_union coe_inter coe_top coe_bot coe_compl
    coe_sdiff coe_himp

@[measurability]
/-
**MeasurableSet.measurableSet_blimsup** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：measurableSet_blimsup {s : Nat -> Set α} {p : Nat -> Prop} (h : forall n, 
p n -> MeasurableSet (s n)) : MeasurableSet blimsup s atTop p
参数：h : forall n, p n -> MeasurableSet (s n)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Filter.blimsup_eq_iInf_biSup_of_nat`：blimsup_eq_iInf_biSup_of_nat {p : N
at -> Prop} {u : Nat -> α} : blimsup u atTop p = ⨅ i, ⨆ (j) (_ : p j ∧ i <= j), 
u j
· 使用定理 `MeasurableSet.iInter`：MeasurableSet.iInter [Countable ι] {f : ι -> Set α
} (h : forall b, MeasurableSet (f b)) : MeasurableSet (⋂ b, f b)
· 使用定理 `instCountableNat`：Countable ℕ
· 使用定理 `MeasurableSet.iUnion`：∀ {α : Type u_1} {ι : Sort u_6} {m : MeasurableSpa
ce α} [Countable ι] ⦃f : ι → Set α⦄,   (∀ (b : ι), MeasurableSet (f b)) → Measur
ableSet (⋃…
· 使用定理 `Prop.countable`：∀ (p : Prop), Countable p
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
-/
theorem measurableSet_blimsup {s : ℕ → Set α} {p : ℕ → Prop} (h : ∀ n, p n → MeasurableSet (s n)) :
    MeasurableSet <| blimsup s atTop p := by
  simp only [blimsup_eq_iInf_biSup_of_nat, iSup_eq_iUnion, iInf_eq_iInter]
  exact .iInter fun _ => .iUnion fun m => .iUnion fun hm => h m hm.1

@[measurability]
/-
**MeasurableSet.measurableSet_bliminf** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：measurableSet_bliminf {s : Nat -> Set α} {p : Nat -> Prop} (h : forall n, 
p n -> MeasurableSet (s n)) : MeasurableSet Filter.bliminf s Filter.atTop p
参数：h : forall n, p n -> MeasurableSet (s n)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Filter.bliminf_eq_iSup_biInf_of_nat`：bliminf_eq_iSup_biInf_of_nat {p : N
at -> Prop} {u : Nat -> α} : bliminf u atTop p = ⨆ i, ⨅ (j) (_ : p j ∧ i <= j), 
u j
· 使用定理 `MeasurableSet.iUnion`：∀ {α : Type u_1} {ι : Sort u_6} {m : MeasurableSpa
ce α} [Countable ι] ⦃f : ι → Set α⦄,   (∀ (b : ι), MeasurableSet (f b)) → Measur
ableSet (⋃…
· 使用定理 `instCountableNat`：Countable ℕ
· 使用定理 `MeasurableSet.iInter`：MeasurableSet.iInter [Countable ι] {f : ι -> Set α
} (h : forall b, MeasurableSet (f b)) : MeasurableSet (⋂ b, f b)
· 使用定理 `Prop.countable`：∀ (p : Prop), Countable p
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
-/
theorem measurableSet_bliminf {s : ℕ → Set α} {p : ℕ → Prop} (h : ∀ n, p n → MeasurableSet (s n)) :
    MeasurableSet <| Filter.bliminf s Filter.atTop p := by
  simp only [Filter.bliminf_eq_iSup_biInf_of_nat, iInf_eq_iInter, iSup_eq_iUnion]
  exact .iUnion fun n => .iInter fun m => .iInter fun hm => h m hm.1

@[measurability]
/-
**MeasurableSet.measurableSet_limsup** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：measurableSet_limsup {s : Nat -> Set α} (hs : forall n, MeasurableSet <| s
 n) : MeasurableSet Filter.limsup s Filter.atTop
参数：hs : forall n, MeasurableSet <| s n。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSet.measurableSet_blimsup`：measurableSet_blimsup {s : Nat -> S
et α} {p : Nat -> Prop} (h : forall n, p n -> MeasurableSet (s n)) : MeasurableS
et blimsup s atTop p
-/
theorem measurableSet_limsup {s : ℕ → Set α} (hs : ∀ n, MeasurableSet <| s n) :
    MeasurableSet <| Filter.limsup s Filter.atTop := by
  simpa only [← blimsup_true] using measurableSet_blimsup fun n _ => hs n

@[measurability]
/-
**MeasurableSet.measurableSet_liminf** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSet`。
形式化陈述：measurableSet_liminf {s : Nat -> Set α} (hs : forall n, MeasurableSet <| s
 n) : MeasurableSet Filter.liminf s Filter.atTop
参数：hs : forall n, MeasurableSet <| s n。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSet.measurableSet_bliminf`：measurableSet_bliminf {s : Nat -> S
et α} {p : Nat -> Prop} (h : forall n, p n -> MeasurableSet (s n)) : MeasurableS
et Filter.bliminf s Filte…
-/
theorem measurableSet_liminf {s : ℕ → Set α} (hs : ∀ n, MeasurableSet <| s n) :
    MeasurableSet <| Filter.liminf s Filter.atTop := by
  simpa only [← bliminf_true] using measurableSet_bliminf fun n _ => hs n

end MeasurableSet

