/-
Copyright (c) 2025 Benoît Guillemet. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Adam Topaz, Benoît Guillemet
-/
module

public import Mathlib.CategoryTheory.Sites.Hypercover.Zero
public import Mathlib.CategoryTheory.Sites.SheafOfTypes

/-!

# Grothendieck topology generated by a precoverage

For any category `C`, we define the Grothendieck topology generated by a precoverage `J` on `C`.
It is the smallest Grothendieck topology containing all the sieves generated by the covering
presieves of `J`.

The main definitions and theorems are:
- `Precoverage.toGrothendieck`: The Grothendieck topology generated by the sieves generated by the
  covering presieves of `J`.
- `Precoverage.toGrothendieck_eq_sInf`: The Grothendieck topology generated by `J` is the infimum
  of the Grothendieck topologies containing all the sieves generated by the covering presieves
  of `J`.
- `Presieve.isSheaf_toGrothendieck_iff`: Given `J : Precoverage C` with associated Grothendieck
  topology `K`, a `Type*`-valued presheaf on `C` is a sheaf for `K` if and only if it is a sheaf
  for all pullbacks of the covering presieves of `J`.
-/

@[expose] public section

namespace CategoryTheory

variable {C : Type _} [Category C]

namespace Precoverage

variable {J : Precoverage C}

/--
An auxiliary definition used to define the Grothendieck topology associated to a precoverage.
See `Precoverage.toGrothendieck`.
-/
/-
**CategoryTheory.Precoverage.Saturate** 是 Mathlib 中的一个归纳类型，位于命名空间 `CategoryTheor
y.Precoverage`。
形式化陈述：{C : Type u_1} →   [inst : CategoryTheory.Category.{u_2, u_1} C] → Categor
yTheory.Precoverage C → (X : C) → CategoryTheory.Sieve X → Prop
参数：X : C。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
An auxiliary definition used to define the Grothendieck topology associated to a
 precoverage.
See `Precoverage.toGrothendieck`.
-/
inductive Saturate (J : Precoverage C) : (X : C) → Sieve X → Prop where
  | of (X : C) (S : Presieve X) (hS : S ∈ J X) : J.Saturate X (Sieve.generate S)
  | top (X : C) : J.Saturate X ⊤
  | pullback (X : C) (S : Sieve X) : J.Saturate X S → ∀ (Y : C) (f : Y ⟶ X),
      J.Saturate Y (S.pullback f)
  | transitive (X : C) (S R : Sieve X) : J.Saturate X S →
      (∀ ⦃Y : C⦄ ⦃f : Y ⟶ X⦄, S f → J.Saturate Y (R.pullback f)) → J.Saturate X R

/--
The Grothendieck topology associated to a precoverage `J`.
It is defined *inductively* as follows:
1. If `S` is a covering presieve for `J`, then the sieve generated by `S` is a covering
  sieve for the associated Grothendieck topology.
2. The top sieves are in the associated Grothendieck topology.
3. Add all sieves required by the pullback stability condition of a Grothendieck topology.
4. Add all sieves required by the *local character* axiom of a Grothendieck topology.
-/
/-
**CategoryTheory.Precoverage.toGrothendieck** 是 Mathlib 中的一个定义，位于命名空间 `CategoryT
heory.Precoverage`。
形式化陈述：toGrothendieck (J : Precoverage C) : GrothendieckTopology C where sieves X
参数：J : Precoverage C。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The Grothendieck topology associated to a precoverage `J`.
It is defined *inductively* as follows:
1. If `S` is a covering presieve for `J`, then the sieve generated by `S` is a c
overing
  sieve for the associated Grothendieck topology.
2. The top sieves are in the associated Grothendieck topology.
3. Add all sieves required by the pullback stability condition of a Grothendieck
 topology.
4. Add all sieves required by the *local character* axiom of a Grothendieck topo
logy.
-/
def toGrothendieck (J : Precoverage C) : GrothendieckTopology C where
  sieves X := Set.ofPred (J.Saturate X)
  top_mem' := .top
  pullback_stable' _ _ _ _ hS := .pullback _ _ hS _ _
  transitive' _ _ hS _ hR := .transitive _ _ _ hS hR
/-
**CategoryTheory.Precoverage.mem_toGrothendieck_iff** 是 Mathlib 中的一个引理，位于命名空间 `C
ategoryTheory.Precoverage`。
形式化陈述：mem_toGrothendieck_iff {X : C} {S : Sieve X} : S in J.toGrothendieck X ↔ J
.Saturate X S
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma mem_toGrothendieck_iff {X : C} {S : Sieve X} :
    S ∈ J.toGrothendieck X ↔ J.Saturate X S := .rfl

@[grind .]
/-
**CategoryTheory.Precoverage.generate_mem_toGrothendieck** 是 Mathlib 中的一个引理，位于命名
空间 `CategoryTheory.Precoverage`。
形式化陈述：generate_mem_toGrothendieck {X : C} {R : Presieve X} (hR : R in J X) : Sie
ve.generate R in J.toGrothendieck X
参数：hR : R in J X。
该定理/引理描述了相关对象所满足的性质。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma generate_mem_toGrothendieck {X : C} {R : Presieve X} (hR : R ∈ J X) :
    Sieve.generate R ∈ J.toGrothendieck X :=
  .of _ _ hR

/--
An alternative characterization of the Grothendieck topology associated to a precoverage `J`:
it is the infimum of all Grothendieck topologies containing `Sieve.generate S` for all presieves
`S` in `J`.
-/
/-
**CategoryTheory.Precoverage.toGrothendieck_eq_sInf** 是 Mathlib 中的一个引理，位于命名空间 `C
ategoryTheory.Precoverage`。
形式化陈述：toGrothendieck_eq_sInf (J : Precoverage C) : J.toGrothendieck = sInf { K |
 forall ⦃X : C⦄ (S : Presieve X), S in J X -> Sieve.generate S in K X }
参数：J : Precoverage C。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `le_sInf_iff`：∀ {α : Type u_1} [inst : CompleteSemilatticeInf α] {s : Set
 α} {a : α}, a ≤ sInf s ↔ ∀ b ∈ s, a ≤ b
· 使用定理 `CategoryTheory.GrothendieckTopology.top_mem`：top_mem (X : C) : ⊤ in J X
· 使用定理 `CategoryTheory.GrothendieckTopology.pullback_stable`：pullback_stable (f 
: Y ⟶ X) (hS : S in J X) : S.pullback f in J Y
· 使用定理 `CategoryTheory.GrothendieckTopology.transitive`：transitive (hS : S in J 
X) (R : Sieve X) (h : forall ⦃Y⦄ ⦃f : Y ⟶ X⦄, S f -> R.pullback f in J Y) : R in
 J X
· 使用定理 `sInf_le`：∀ {α : Type u_1} [inst : CompleteSemilatticeInf α] {s : Set α} 
{a : α}, a ∈ s → sInf s ≤ a

--- 原说明 ---
An alternative characterization of the Grothendieck topology associated to a pre
coverage `J`:
it is the infimum of all Grothendieck topologies containing `Sieve.generate S` f
or all presieves
`S` in `J`.
-/
lemma toGrothendieck_eq_sInf (J : Precoverage C) :
    J.toGrothendieck =
      sInf { K | ∀ ⦃X : C⦄ (S : Presieve X), S ∈ J X → Sieve.generate S ∈ K X } := by
  refine le_antisymm ?_ ?_
  · rw [le_sInf_iff]
    intro K hK _ _ hS
    induction hS with
    | of _ _ hS => exact hK _ hS
    | top => exact K.top_mem _
    | pullback _ _ _ _ _ ih => exact K.pullback_stable _ ih
    | transitive _ _ _ _ _ ih1 ih2 => exact K.transitive ih1 _ ih2
  · exact sInf_le (fun _ _ hS => .of _ _ hS)

/--
The main theorem of this file: given a precoverage `J` on `C`, a `Type*`-valued presheaf on `C` is
a sheaf for the associated Grothendieck topology if and only if it is a sheaf for all pullback
sieves of presieves in `J`.
-/
/-
**CategoryTheory.Precoverage.isSheaf_toGrothendieck_iff** 是 Mathlib 中的一个定理，位于命名空
间 `CategoryTheory.Precoverage`。
形式化陈述：isSheaf_toGrothendieck_iff (P : Cᵒᵖ ⥤ Type*) : Presieve.IsSheaf J.toGrothe
ndieck P ↔ (forall {X Y : C} {f : Y ⟶ X} (R : Presieve X), R in J X -> Presieve.
IsSheafFor P ((Sieve.generate R).pullback f).arrows)
参数：P : Cᵒᵖ ⥤ Type*。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Presieve.IsSheaf.isSheafFor`：∀ {C : Type u} [inst : Categ
oryTheory.Category.{v, u} C] {X : C} {J : CategoryTheory.GrothendieckTopology C}
   {P : CategoryTheory.Functor C…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `CategoryTheory.Sieve.generate_sieve`：generate_sieve (S : Sieve X) : gene
rate S = S
· 使用定理 `CategoryTheory.GrothendieckTopology.pullback_stable`：pullback_stable (f 
: Y ⟶ X) (hS : S in J X) : S.pullback f in J Y
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `CategoryTheory.Sieve.pullback_top`：pullback_top {f : Y ⟶ X} : (⊤ : Sieve
 X).pullback f = ⊤
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `CategoryTheory.Presieve.isSheafFor_top`：isSheafFor_top (P : Cᵒᵖ ⥤ Type w
) : IsSheafFor P (⊤ : Presieve X)
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `CategoryTheory.Sieve.pullback_comp`：pullback_comp {f : Y ⟶ X} {g : Z ⟶ Y
} (S : Sieve X) : S.pullback (g ≫ f) = (S.pullback f).pullback g
· 使用定理 `CategoryTheory.Presieve.IsSeparatedFor.ext`：∀ {C : Type u₁} [inst : Cate
goryTheory.Category.{v₁, u₁} C] {P : CategoryTheory.Functor Cᵒᵖ (Type w)} {X : C
}   {R : CategoryTheory.Presieve…
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `CategoryTheory.Functor.map_comp`：∀ {C : Type u₁} [inst : CategoryTheory.
Category.{v₁, u₁} C] {D : Type u₂} [inst_1 : CategoryTheory.Category.{v₂, u₂} D]
   (self : CategoryTh…
· 使用定理 `CategoryTheory.op_comp`：op_comp {X Y Z : C} {f : X ⟶ Y} {g : Y ⟶ Z} : (f
 ≫ g).op = g.op ≫ f.op
· 使用定理 `CategoryTheory.Sieve.pullback_apply`：∀ {C : Type u₁} [inst : CategoryThe
ory.Category.{v₁, u₁} C] {X Y : C} (h : Y ⟶ X) (S : CategoryTheory.Sieve X) (x :
 C)   (sl : x ⟶ Y), (Cate…
· 使用定理 `CategoryTheory.Category.assoc`：∀ {obj : Type u} [self : CategoryTheory.C
ategory.{v, u} obj] {W X Y Z : obj} (f : W ⟶ X) (g : X ⟶ Y) (h : Y ⟶ Z),   Categ
oryTheory.CategoryS…
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrFun`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, f = g →
 ∀ (a : α), f a = g a
· 使用定理 `CategoryTheory.Sieve.pullback_id`：pullback_id : S.pullback (𝟙 _) = S
· 使用定理 `Mathlib.Tactic.Reassoc.eq_whisker'`：eq_whisker' {C : Type*} [Category* C
] {X Y : C} {f g : X ⟶ Y} (w : f = g) {Z : C} (h : Y ⟶ Z) : f ≫ h = g ≫ h
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b
· 使用定理 `CategoryTheory.comp_apply`：∀ {C : Type u} [inst : CategoryTheory.Categor
y.{v, u} C] {FC : C → C → Type u_1} {CC : C → Type w}   [inst_1 : (X Y : C) → Fu
nLike (FC X Y) …
· 使用定理 `Classical.choose_spec`：∀ {α : Sort u} {p : α → Prop} (h : ∃ x, p x), p (
Classical.choose h)

--- 原说明 ---
The main theorem of this file: given a precoverage `J` on `C`, a `Type*`-valued 
presheaf on `C` is
a sheaf for the associated Grothendieck topology if and only if it is a sheaf fo
r all pullback
sieves of presieves in `J`.
-/
theorem isSheaf_toGrothendieck_iff (P : Cᵒᵖ ⥤ Type*) :
    Presieve.IsSheaf J.toGrothendieck P ↔
      (∀ {X Y : C} {f : Y ⟶ X} (R : Presieve X), R ∈ J X →
        Presieve.IsSheafFor P ((Sieve.generate R).pullback f).arrows) := by
  constructor
  · intro H _ _ _ _ hR
    apply H.isSheafFor
    rw [Sieve.generate_sieve]
    exact J.toGrothendieck.pullback_stable _ (Saturate.of _ _ hR)
  · intro H X S hS
    -- This is the key point of the proof:
    -- We must generalize the induction in the correct way.
    suffices ∀ ⦃Y : C⦄ (f : Y ⟶ X), Presieve.IsSheafFor P (S.pullback f).arrows by
      simpa using this (f := 𝟙 _)
    induction hS with
    | of X S hS =>
      exact fun _ _ => H S hS
    | top =>
      simp [Presieve.isSheafFor_top P]
    | pullback X S hS _ f ih =>
      intro Y f
      rw [← S.pullback_comp]
      exact ih (f ≫ _)
    | transitive X R S hS h H1 H2 =>
      intro Y f
      simp only [← Presieve.isSeparatedFor_and_exists_isAmalgamation_iff_isSheafFor] at *
      choose H1 H1' using H1
      choose H2 H2' using H2
      refine ⟨?_, fun x hx => ?_⟩
      · intro x t₁ t₂ h₁ h₂
        refine (H1 f).ext (fun Z g hg => ?_)
        refine (H2 hg (𝟙 _)).ext (fun ZZ gg hgg => ?_)
        simp only [Sieve.pullback_id, Sieve.pullback_apply] at hgg
        simp only [← comp_apply]
        rw [← P.map_comp, ← op_comp, h₁, h₂]
        simpa only [Sieve.pullback_apply, Category.assoc] using hgg
      let y : ∀ ⦃Z : C⦄ (g : Z ⟶ Y),
        ((S.pullback (g ≫ f)).pullback (𝟙 _)).arrows.FamilyOfElements P :=
        fun Z g ZZ gg hgg => x (gg ≫ g) (by simpa using hgg)
      have hy : ∀ ⦃Z : C⦄ (g : Z ⟶ Y), (y g).Compatible := by
        intro Z g Y₁ Y₂ ZZ g₁ g₂ f₁ f₂ h₁ h₂ h
        rw [hx]
        rw [reassoc_of% h]
      choose z hz using fun ⦃Z : C⦄ ⦃g : Z ⟶ Y⦄ (hg : R.pullback f g) =>
        H2' hg (𝟙 _) (y g) (hy g)
      let q : (R.pullback f).arrows.FamilyOfElements P := fun Z g hg => z hg
      have hq : q.Compatible := by
        intro Y₁ Y₂ Z g₁ g₂ f₁ f₂ h₁ h₂ h
        apply (H2 h₁ g₁).ext
        intro ZZ gg hgg
        simp only [← comp_apply]
        rw [← P.map_comp, ← P.map_comp, ← op_comp, ← op_comp, hz, hz]
        · dsimp [y]; congr 1; simp only [Category.assoc, h]
        · simpa [reassoc_of% h] using hgg
        · simpa using hgg
      obtain ⟨t, ht⟩ := H1' f q hq
      refine ⟨t, fun Z g hg => ?_⟩
      refine (H1 (g ≫ f)).ext (fun ZZ gg hgg => ?_)
      rw [← comp_apply _ (P.map gg.op), ← P.map_comp, ← op_comp, ht]
      on_goal 2 => simpa using hgg
      refine (H2 hgg (𝟙 _)).ext (fun ZZZ ggg hggg => ?_)
      rw [← comp_apply _ (P.map ggg.op), ← P.map_comp, ← op_comp, hz]
      on_goal 2 => simpa using hggg
      refine (H2 hgg ggg).ext (fun ZZZZ gggg _ => ?_)
      rw [← comp_apply _ (P.map gggg.op), ← P.map_comp, ← op_comp]
      apply hx
      simp
/-
**CategoryTheory.Precoverage.mem_toGrothendieck_iff_of_isStableUnderComposition*
* 是 Mathlib 中的一个引理，位于命名空间 `CategoryTheory.Precoverage`。
形式化陈述：mem_toGrothendieck_iff_of_isStableUnderComposition [IsStableUnderCompositi
on J] [IsStableUnderBaseChange J] [J.HasPullbacks] [HasIsos J] {X : C} {S : Siev
e X} : S in J.toGrothendieck X ↔ exists R in J X, R <= S
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Sieve.le_generate`：le_generate (R : Presieve X) : R <= ge
nerate R
· 使用定理 `CategoryTheory.Precoverage.mem_coverings_of_isIso`：∀ {C : Type u} {inst 
: CategoryTheory.Category.{v, u} C} {J : CategoryTheory.Precoverage C} [self : J
.HasIsos] {S T : C}   (f : S ⟶ T) [Cate…
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `CategoryTheory.Precoverage.hasPullbacks_of_mem`：∀ {C : Type u} {inst : C
ategoryTheory.Category.{v, u} C} {J : CategoryTheory.Precoverage C} [self : J.Ha
sPullbacks]   {X Y : C} {R : Categor…
· 使用引理 `CategoryTheory.Precoverage.pullbackArrows_mem`：pullbackArrows_mem {J : P
recoverage C} [IsStableUnderBaseChange J] {X Y : C} (f : X ⟶ Y) {R : Presieve Y}
 (hR : R in J Y) [R.HasPullbacks f]…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `CategoryTheory.Sieve.generate_le_iff`：generate_le_iff (R : Presieve X) (
S : Sieve X) : generate R <= S ↔ R <= S
· 使用定理 `CategoryTheory.Sieve.pullbackArrows_comm`：pullbackArrows_comm {X Y : C} 
(f : Y ⟶ X) (R : Presieve X) [R.HasPullbacks f] : Sieve.generate (R.pullbackArro
ws f) = (Sieve.generate R).pul…
· 使用定理 `CategoryTheory.Sieve.pullback_monotone`：pullback_monotone (f : Y ⟶ X) : 
Monotone (Sieve.pullback f)
· 使用引理 `CategoryTheory.Precoverage.mem_iff_exists_zeroHypercover`：mem_iff_exists
_zeroHypercover {X : C} {R : Presieve X} : R in J X ↔ exists (𝒰 : ZeroHypercover
.{max u v} J X), R = Presieve.ofArrows 𝒰.X 𝒰.f
· 使用定理 `CategoryTheory.Precoverage.ZeroHypercover.mem₀`：∀ {C : Type u} [inst : C
ategoryTheory.Category.{v, u} C] {J : CategoryTheory.Precoverage C} {S : C}   (s
elf : J.ZeroHypercover S), self.pres…
· 使用引理 `CategoryTheory.Presieve.ofArrows_le_iff`：ofArrows_le_iff {X : C} {ι : Ty
pe*} {Y : ι -> C} {f : forall i, Y i ⟶ X} {R : Presieve X} : Presieve.ofArrows Y
 f <= R ↔ forall i, R (f i)
· 使用定理 `Classical.choose_spec`：∀ {α : Sort u} {p : α → Prop} (h : ∃ x, p x), p (
Classical.choose h)
· 使用定理 `CategoryTheory.GrothendieckTopology.superset_covering`：superset_covering
 (Hss : S <= R) (sjx : S in J X) : R in J X
· 使用引理 `CategoryTheory.Precoverage.generate_mem_toGrothendieck`：generate_mem_toG
rothendieck {X : C} {R : Presieve X} (hR : R in J X) : Sieve.generate R in J.toG
rothendieck X
-/
lemma mem_toGrothendieck_iff_of_isStableUnderComposition [IsStableUnderComposition J]
    [IsStableUnderBaseChange J] [J.HasPullbacks] [HasIsos J] {X : C} {S : Sieve X} :
    S ∈ J.toGrothendieck X ↔ ∃ R ∈ J X, R ≤ S := by
  refine ⟨fun hS ↦ ?_, fun ⟨R, hR, hle⟩ ↦ ?_⟩
  · induction hS with
    | of X R hR =>
      use R, hR
      exact Sieve.le_generate R
    | top X =>
      exact ⟨Presieve.singleton (𝟙 X), mem_coverings_of_isIso (𝟙 X), by simp⟩
    | pullback X S hS Y f h =>
      obtain ⟨R, hR, hle⟩ := h
      have : R.HasPullbacks f := J.hasPullbacks_of_mem f hR
      refine ⟨R.pullbackArrows f, pullbackArrows_mem f hR, ?_⟩
      rw [← Sieve.generate_le_iff, Sieve.pullbackArrows_comm]
      apply Sieve.pullback_monotone
      rwa [Sieve.generate_le_iff]
    | transitive X S T hS hT hleS hleT =>
      obtain ⟨R, hR, hle⟩ := hleS
      rw [mem_iff_exists_zeroHypercover] at hR
      obtain ⟨E, rfl⟩ := hR
      replace hleT (i : E.I₀) : ∃ (F : J.ZeroHypercover (E.X i)),
          F.presieve₀ ≤ (Sieve.pullback (E.f i) T).arrows := by
        obtain ⟨R', hR', hle'⟩ := hleT (hle _ _ ⟨i⟩)
        rw [mem_iff_exists_zeroHypercover] at hR'
        obtain ⟨F, rfl⟩ := hR'
        use F
      choose F hle' using hleT
      refine ⟨(E.bind F).presieve₀, (E.bind F).mem₀, ?_⟩
      rw [Presieve.ofArrows_le_iff]
      intro i
      exact hle' _ _ _ ⟨i.snd⟩
  · rw [← Sieve.generate_le_iff] at hle
    apply GrothendieckTopology.superset_covering _ hle
    exact generate_mem_toGrothendieck hR
/-
**CategoryTheory.Precoverage.toGrothendieck_toPretopology_eq_toGrothendieck** 是 
Mathlib 中的一个引理，位于命名空间 `CategoryTheory.Precoverage`。
形式化陈述：toGrothendieck_toPretopology_eq_toGrothendieck [IsStableUnderComposition J
] [IsStableUnderBaseChange J] [Limits.HasPullbacks C] [HasIsos J] : J.toPretopol
ogy.toGrothendieck = J.toGrothendieck
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.GrothendieckTopology.ext`：ext {J₁ J₂ : GrothendieckTopolo
gy C} (h : (J₁ : forall X : C, Set (Sieve X)) = J₂) : J₁ = J₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `Iff.symm`：∀ {a b : Prop}, (a ↔ b) → (b ↔ a)
· 使用引理 `CategoryTheory.Precoverage.mem_toGrothendieck_iff_of_isStableUnderCompos
ition`：mem_toGrothendieck_iff_of_isStableUnderComposition [IsStableUnderComposit
ion J] [IsStableUnderBaseChange J] [J.HasPullbacks] [HasIsos J] {X …
· 使用定理 `CategoryTheory.Precoverage.instHasPullbacksOfHasPullbacks`：∀ {C : Type u
} [inst : CategoryTheory.Category.{v, u} C] (J : CategoryTheory.Precoverage C)  
 [CategoryTheory.Limits.HasPullbacks C], J.HasP…
-/
lemma toGrothendieck_toPretopology_eq_toGrothendieck [IsStableUnderComposition J]
    [IsStableUnderBaseChange J] [Limits.HasPullbacks C] [HasIsos J] :
    J.toPretopology.toGrothendieck = J.toGrothendieck := by
  ext
  exact J.mem_toGrothendieck_iff_of_isStableUnderComposition.symm

end Precoverage

@[grind .]
/-
**CategoryTheory.Presieve.IsSheaf.isSheafFor_of_mem_precoverage** 是 Mathlib 中的一个
定理，位于命名空间 `CategoryTheory.Presieve.IsSheaf`。
形式化陈述：∀ {C : Type u_3} [inst : CategoryTheory.Category.{u_2, u_3} C] {J : Catego
ryTheory.Precoverage C}   {P : CategoryTheory.Functor Cᵒᵖ (Type u_1)},   Categor
yTheory.Presieve.IsSheaf J.toGrothendieck P →     ∀ {S : C} {R : CategoryTheory.
Presieve S}, R ∈ J.coverings S → CategoryTheory.Presieve.IsSheafFor P R
参数：Type u_1。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `CategoryTheory.Sieve.generate_sieve`：generate_sieve (S : Sieve X) : gene
rate S = S
· 使用定理 `CategoryTheory.Sieve.pullback_id`：pullback_id : S.pullback (𝟙 _) = S
· 使用定理 `CategoryTheory.Precoverage.isSheaf_toGrothendieck_iff`：isSheaf_toGrothen
dieck_iff (P : Cᵒᵖ ⥤ Type*) : Presieve.IsSheaf J.toGrothendieck P ↔ (forall {X Y
 : C} {f : Y ⟶ X} (R : Presieve X), R in J …
-/
lemma Presieve.IsSheaf.isSheafFor_of_mem_precoverage {J : Precoverage C} {P : Cᵒᵖ ⥤ Type*}
    (h : Presieve.IsSheaf J.toGrothendieck P) {S : C} {R : Presieve S}
    (hR : R ∈ J S) : R.IsSheafFor P := by
  rw [J.isSheaf_toGrothendieck_iff] at h
  simpa [Presieve.isSheafFor_iff_generate] using h (f := 𝟙 S) R hR
/-
**CategoryTheory.PreZeroHypercover.isSheafFor_iff_of_iso** 是 Mathlib 中的一个定理，位于命名
空间 `CategoryTheory.PreZeroHypercover`。
形式化陈述：∀ {C : Type u_3} [inst : CategoryTheory.Category.{u_2, u_3} C] {F : Catego
ryTheory.Functor Cᵒᵖ (Type u_1)} {S : C}   {𝒰 𝒱 : CategoryTheory.PreZeroHypercov
er S} (e : 𝒰 ≅ 𝒱),   CategoryTheory.Presieve.IsSheafFor F 𝒰.presieve₀ ↔ Category
Theory.Presieve.IsSheafFor F 𝒱.presieve₀
参数：Type u_1；e : 𝒰 ≅ 𝒱。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `CategoryTheory.Presieve.isSheafFor_iff_generate`：isSheafFor_iff_generate
 (R : Presieve X) : IsSheafFor P R ↔ IsSheafFor P (generate R : Presieve X)
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `CategoryTheory.Sieve.ofArrows.eq_1`：∀ {C : Type u₁} [inst : CategoryTheo
ry.Category.{v₁, u₁} C] {I : Type u_1} {X : C} (Y : I → C) (f : (i : I) → Y i ⟶ 
X),   CategoryTheory.Sie…
· 使用定理 `CategoryTheory.PreZeroHypercover.sieve₀.eq_1`：∀ {C : Type u} [inst : Cat
egoryTheory.Category.{v, u} C] {S : C} (E : CategoryTheory.PreZeroHypercover S),
   E.sieve₀ = CategoryTheory.Sieve…
· 使用引理 `CategoryTheory.PreZeroHypercover.sieve₀_eq_of_iso`：sieve₀_eq_of_iso {E F
 : PreZeroHypercover S} (e : E ≅ F) : E.sieve₀ = F.sieve₀
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma PreZeroHypercover.isSheafFor_iff_of_iso {F : Cᵒᵖ ⥤ Type*} {S : C}
    {𝒰 𝒱 : PreZeroHypercover S} (e : 𝒰 ≅ 𝒱) :
    𝒰.presieve₀.IsSheafFor F ↔ 𝒱.presieve₀.IsSheafFor F := by
  rw [Presieve.isSheafFor_iff_generate, ← Sieve.ofArrows, ← PreZeroHypercover.sieve₀,
    PreZeroHypercover.sieve₀_eq_of_iso e, ← Presieve.isSheafFor_iff_generate]
/-
**CategoryTheory.Presieve.isSheafFor_ofArrows_comp_iff** 是 Mathlib 中的一个定理，位于命名空间
 `CategoryTheory.Presieve`。
形式化陈述：∀ {C : Type u_4} [inst : CategoryTheory.Category.{u_3, u_4} C] {F : Catego
ryTheory.Functor Cᵒᵖ (Type u_1)} {X : C}   {ι : Type u_2} {Y Z : ι → C} (g : (i 
: ι) → Z i ⟶ X) (e : (i : ι) → Y i ≅ Z i),   CategoryTheory.Presieve.IsSheafFor 
F       (CategoryTheory.Presieve.ofArrows Y fun i => CategoryTheory.CategoryStru
ct.comp (e i).hom (g i)) ↔     CategoryTheory.Presieve.IsSheafFor F (CategoryThe
ory.Presieve.ofArrows Z g)
参数：Type u_1；g : (i : ι) → Z i ⟶ X；e : (i : ι) → Y i ≅ Z i；CategoryTheory.Presiev
e.ofArrows Y fun i => CategoryTheory.CategoryStruct.comp (e i).hom (g i)；Categor
yTheory.Presieve.ofArrows Z g。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.refl`：Equiv.refl (s : Computation α) : s ~ s
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `CategoryTheory.Iso.inv_hom_id_assoc`：∀ {C : Type u} [inst : CategoryTheo
ry.Category.{v, u} C] {X Y : C} (self : X ≅ Y) {Z : C} (h : Y ⟶ Z),   CategoryTh
eory.CategoryStruct.comp …
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `CategoryTheory.PreZeroHypercover.isSheafFor_iff_of_iso`：∀ {C : Type u_3}
 [inst : CategoryTheory.Category.{u_2, u_3} C] {F : CategoryTheory.Functor Cᵒᵖ (
Type u_1)} {S : C}   {𝒰 𝒱 : CategoryTheory.P…
-/
lemma Presieve.isSheafFor_ofArrows_comp_iff {F : Cᵒᵖ ⥤ Type*} {X : C} {ι : Type*} {Y Z : ι → C}
    (g : ∀ i, Z i ⟶ X) (e : ∀ i, Y i ≅ Z i) :
    IsSheafFor F (ofArrows _ (fun i ↦ (e i).hom ≫ g i)) ↔ IsSheafFor F (ofArrows _ g) := by
  let 𝒰 : PreZeroHypercover X := ⟨_, _, g⟩
  let 𝒱 : PreZeroHypercover X := ⟨_, _, fun i ↦ (e i).hom ≫ g i⟩
  let e : 𝒰 ≅ 𝒱 := PreZeroHypercover.isoMk (.refl _) (fun i ↦ (e i).symm)
  exact PreZeroHypercover.isSheafFor_iff_of_iso e.symm
/-
**CategoryTheory.Presieve.isSheafFor_singleton_iff_of_iso** 是 Mathlib 中的一个定理，位于命
名空间 `CategoryTheory.Presieve`。
形式化陈述：∀ {C : Type u_3} [inst : CategoryTheory.Category.{u_2, u_3} C] {F : Catego
ryTheory.Functor Cᵒᵖ (Type u_1)} {S X Y : C}   (f : X ⟶ S) (g : Y ⟶ S) (e : X ≅ 
Y),   CategoryTheory.CategoryStruct.comp e.hom g = f →     (CategoryTheory.Presi
eve.IsSheafFor F (CategoryTheory.Presieve.singleton f) ↔       CategoryTheory.Pr
esieve.IsSheafFor F (CategoryTheory.Presieve.singleton g))
参数：Type u_1；f : X ⟶ S；g : Y ⟶ S；e : X ≅ Y；CategoryTheory.Presieve.IsSheafFor F (
CategoryTheory.Presieve.singleton f) ↔       CategoryTheory.Presieve.IsSheafFor 
F (CategoryTheory.Presieve.singleton g)。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `CategoryTheory.Presieve.ofArrows_pUnit`：ofArrows_pUnit : (ofArrows _ fun
 _ : PUnit.{w + 1} => f) = singleton f
· 使用定理 `CategoryTheory.Presieve.isSheafFor_ofArrows_comp_iff`：∀ {C : Type u_4} [
inst : CategoryTheory.Category.{u_3, u_4} C] {F : CategoryTheory.Functor Cᵒᵖ (Ty
pe u_1)} {X : C}   {ι : Type u_2} {Y Z : ι…
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma Presieve.isSheafFor_singleton_iff_of_iso {F : Cᵒᵖ ⥤ Type*} {S X Y : C} (f : X ⟶ S)
    (g : Y ⟶ S) (e : X ≅ Y) (he : e.hom ≫ g = f) :
    (singleton f).IsSheafFor F ↔ (singleton g).IsSheafFor F := by
  subst he
  rw [← Presieve.ofArrows_pUnit.{0}, ← Presieve.ofArrows_pUnit,
    Presieve.isSheafFor_ofArrows_comp_iff]

open Limits

variable {D : Type*} [Category* D]

set_option backward.defeqAttrib.useBackward true in
set_option backward.isDefEq.respectTransparency false in
/-
**CategoryTheory.Presieve.IsSheafFor.comp_iff_of_preservesPairwisePullbacks** 是 
Mathlib 中的一个定理，位于命名空间 `CategoryTheory.Presieve.IsSheafFor`。
形式化陈述：∀ {C : Type u_4} [inst : CategoryTheory.Category.{u_3, u_4} C] {D : Type u
_1}   [inst_1 : CategoryTheory.Category.{v_1, u_1} D] (F : CategoryTheory.Functo
r C D)   (P : CategoryTheory.Functor Dᵒᵖ (Type u_2)) {X : C} (R : CategoryTheory
.Presieve X) [R.HasPairwisePullbacks]   [F.PreservesPairwisePullbacks R],   Cate
goryTheory.Presieve.IsSheafFor (F.op.comp P) R ↔     CategoryTheory.Presieve.IsS
heafFor P (CategoryTheory.Presieve.map F R)
参数：F : CategoryTheory.Functor C D；P : CategoryTheory.Functor Dᵒᵖ (Type u_2)；R : 
CategoryTheory.Presieve X；F.op.comp P；CategoryTheory.Presieve.map F R。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Presieve.HasPairwisePullbacks.map_of_preservesPairwisePul
lbacks`：∀ {C : Type u_1} {D : Type u_2} [inst : CategoryTheory.Category.{v_1, u_
1} C]   [inst_1 : CategoryTheory.Category.{v_2, u_2} D] (F : Categor…
· 使用引理 `CategoryTheory.Presieve.exists_eq_ofArrows`：exists_eq_ofArrows (R : Pres
ieve X) : exists (ι : Type (max u₁ v₁)) (Y : ι -> C) (f : forall i, Y i ⟶ X), R 
= .ofArrows Y f
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `CategoryTheory.Presieve.map_ofArrows`：map_ofArrows {X : C} {ι : Type*} {
Y : ι -> C} (f : forall i, Y i ⟶ X) : (ofArrows Y f).map F = ofArrows _ (fun i =
> F.map (f i))
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `iff_of_eq`：∀ {a b : Prop}, a = b → (a ↔ b)
· 使用定理 `CategoryTheory.Presieve.instHasPullbackOfHasPairwisePullbacksOfArrows`：∀
 {C : Type u₁} [inst : CategoryTheory.Category.{v₁, u₁} C] {α : Type v₂} {X : α 
→ C} {B : C} (π : (a : α) → X a ⟶ B)   [(CategoryTheory.Pre…
· 使用定理 `pi_congr`：∀ {α : Sort u} {β β' : α → Sort v}, (∀ (a : α), β a = β' a) → 
((a : α) → β a) = ((a : α) → β' a)
· 使用定理 `forall₂_congr`：∀ {α : Sort u_1} {β : α → Sort u_2} {p q : (a : α) → β a 
→ Prop},   (∀ (a : α) (b : β a), p a b ↔ q a b) → ((∀ (a : α) (b : β a), p a b) 
↔ ∀…
· 使用定理 `CategoryTheory.Functor.preservesLimit_cospan_of_mem_presieve`：∀ {C : Typ
e u_1} {D : Type u_2} {inst : CategoryTheory.Category.{v_1, u_1} C}   {inst_1 : 
CategoryTheory.Category.{v_2, u_2} D} {F : Categor…
· 使用定理 `CategoryTheory.Limits.hasPullback_of_preservesPullback`：hasPullback_of_p
reservesPullback [HasPullback f g] : HasPullback (G.map f) (G.map g)
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `CategoryTheory.Limits.pullbackComparison_comp_fst`：pullbackComparison_co
mp_fst (f : X ⟶ Z) (g : Y ⟶ Z) [HasPullback f g] [HasPullback (G.map f) (G.map g
)] : pullbackComparison G f g ≫ pullbac…
· 使用定理 `CategoryTheory.op_comp`：op_comp {X Y Z : C} {f : X ⟶ Y} {g : Y ⟶ Z} : (f
 ≫ g).op = g.op ≫ f.op
· 使用定理 `CategoryTheory.Functor.map_comp`：∀ {C : Type u₁} [inst : CategoryTheory.
Category.{v₁, u₁} C] {D : Type u₂} [inst_1 : CategoryTheory.Category.{v₂, u₂} D]
   (self : CategoryTh…
· 使用定理 `CategoryTheory.Limits.pullbackComparison_comp_snd`：pullbackComparison_co
mp_snd (f : X ⟶ Z) (g : Y ⟶ Z) [HasPullback f g] [HasPullback (G.map f) (G.map g
)] : pullbackComparison G f g ≫ pullbac…
· 使用定理 `CategoryTheory.isIso_iff_bijective`：isIso_iff_bijective {X Y : Type u} (
f : X ⟶ Y) : IsIso f ↔ Function.Bijective f
· 使用定理 `CategoryTheory.Limits.instIsIsoPullbackComparison`：∀ {C : Type u₁} [inst
 : CategoryTheory.Category.{v₁, u₁} C] {D : Type u₂} [inst_1 : CategoryTheory.Ca
tegory.{v₂, u₂} D]   (G : CategoryTheor…
· 使用定理 `Function.Injective.eq_iff`：∀ {α : Sort u_1} {β : Sort u_2} {f : α → β}, 
Function.Injective f → ∀ {a b : α}, f a = f b ↔ a = b
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma Presieve.IsSheafFor.comp_iff_of_preservesPairwisePullbacks (F : C ⥤ D) (P : Dᵒᵖ ⥤ Type*)
    {X : C} (R : Presieve X) [R.HasPairwisePullbacks]
    [F.PreservesPairwisePullbacks R] :
    Presieve.IsSheafFor (F.op ⋙ P) R ↔ Presieve.IsSheafFor P (R.map F) := by
  have : (R.map F).HasPairwisePullbacks := .map_of_preservesPairwisePullbacks _ _
  obtain ⟨ι, Y, f, rfl⟩ := R.exists_eq_ofArrows
  rw [map_ofArrows] at this ⊢
  simp_rw [Presieve.isSheafFor_arrows_iff_pullbacks]
  dsimp [Arrows.PullbackCompatible]
  congr! with x
  rw [forall₂_congr]
  intro i j
  have : PreservesLimit (cospan (f i) (f j)) F :=
    F.preservesLimit_cospan_of_mem_presieve (ofArrows _ f) ⟨i⟩ ⟨j⟩
  have : HasPullback (F.map (f i)) (F.map (f j)) := hasPullback_of_preservesPullback _ _ _
  rw [← pullbackComparison_comp_fst, op_comp, Functor.map_comp,
    ← pullbackComparison_comp_snd, op_comp, Functor.map_comp]
  have : Function.Bijective (P.map (pullbackComparison F (f i) (f j)).op) := by
    rw [← isIso_iff_bijective]
    infer_instance
  exact this.1.eq_iff

namespace GrothendieckTopology

/-- The induced coverage by a Grothendieck topology as a precoverage. -/
/-
**CategoryTheory.GrothendieckTopology.toPrecoverage** 是 Mathlib 中的一个定义，位于命名空间 `C
ategoryTheory.GrothendieckTopology`。
形式化陈述：toPrecoverage (J : GrothendieckTopology C) : Precoverage C where coverings
 S
参数：J : GrothendieckTopology C。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The induced coverage by a Grothendieck topology as a precoverage.
-/
def toPrecoverage (J : GrothendieckTopology C) : Precoverage C where
  coverings S := { R | Sieve.generate R ∈ J S }
/-
**CategoryTheory.GrothendieckTopology.mem_toPrecoverage_iff** 是 Mathlib 中的一个引理，位
于命名空间 `CategoryTheory.GrothendieckTopology`。
形式化陈述：mem_toPrecoverage_iff (J : GrothendieckTopology C) {S : C} (R : Presieve S
) : R in toPrecoverage J S ↔ Sieve.generate R in J S
参数：J : GrothendieckTopology C；R : Presieve S。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma mem_toPrecoverage_iff (J : GrothendieckTopology C) {S : C} (R : Presieve S) :
    R ∈ toPrecoverage J S ↔ Sieve.generate R ∈ J S := .rfl
/-
**CategoryTheory.GrothendieckTopology.arrows_mem_toPrecoverage_iff** 是 Mathlib 中
的一个引理，位于命名空间 `CategoryTheory.GrothendieckTopology`。
形式化陈述：arrows_mem_toPrecoverage_iff (J : GrothendieckTopology C) {S : C} (R : Sie
ve S) : R.arrows in toPrecoverage J S ↔ R in J S
参数：J : GrothendieckTopology C；R : Sieve S。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `CategoryTheory.GrothendieckTopology.mem_toPrecoverage_iff`：mem_toPrecove
rage_iff (J : GrothendieckTopology C) {S : C} (R : Presieve S) : R in toPrecover
age J S ↔ Sieve.generate R in J S
· 使用定理 `CategoryTheory.Sieve.generate_sieve`：generate_sieve (S : Sieve X) : gene
rate S = S
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma arrows_mem_toPrecoverage_iff (J : GrothendieckTopology C) {S : C} (R : Sieve S) :
    R.arrows ∈ toPrecoverage J S ↔ R ∈ J S := by
  rw [mem_toPrecoverage_iff, Sieve.generate_sieve]
/-
**CategoryTheory.GrothendieckTopology.** 是 Mathlib 中的一个实例，位于命名空间 `CategoryTheory
.GrothendieckTopology`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (J : GrothendieckTopology C) : (toPrecoverage J).HasIsos where
  mem_coverings_of_isIso f hf := by simp [mem_toPrecoverage_iff]
/-
**CategoryTheory.GrothendieckTopology.** 是 Mathlib 中的一个实例，位于命名空间 `CategoryTheory
.GrothendieckTopology`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (J : GrothendieckTopology C) : (toPrecoverage J).IsStableUnderComposition where
  comp_mem_coverings {ι} S X f hf σ Y g hg := by
    rw [mem_toPrecoverage_iff, ← Presieve.bindOfArrows_ofArrows]
    exact J.bindOfArrows hf hg
/-
**CategoryTheory.GrothendieckTopology.** 是 Mathlib 中的一个实例，位于命名空间 `CategoryTheory
.GrothendieckTopology`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (J : GrothendieckTopology C) : (toPrecoverage J).IsStableUnderBaseChange where
  mem_coverings_of_isPullback {ι} S X f hf Y g P p₁ p₂ h := by
    rw [mem_toPrecoverage_iff, ← Sieve.ofArrows, Sieve.ofArrows_eq_pullback_of_isPullback _ h]
    exact J.pullback_stable _ hf

end GrothendieckTopology

namespace Precoverage

variable {K : Precoverage C} {J : GrothendieckTopology C}

/-- `toGrothendieck` and `toPrecoverage` form a Galois connection. -/
/-
**CategoryTheory.Precoverage.toGrothendieck_le_iff_le_toPrecoverage** 是 Mathlib 
中的一个引理，位于命名空间 `CategoryTheory.Precoverage`。
形式化陈述：toGrothendieck_le_iff_le_toPrecoverage : K.toGrothendieck <= J ↔ K <= J.to
Precoverage
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `CategoryTheory.GrothendieckTopology.mem_toPrecoverage_iff`：mem_toPrecove
rage_iff (J : GrothendieckTopology C) {S : C} (R : Presieve S) : R in toPrecover
age J S ↔ Sieve.generate R in J S
· 使用引理 `CategoryTheory.Precoverage.generate_mem_toGrothendieck`：generate_mem_toG
rothendieck {X : C} {R : Presieve X} (hR : R in J X) : Sieve.generate R in J.toG
rothendieck X

--- 原说明 ---
`toGrothendieck` and `toPrecoverage` form a Galois connection.
-/
lemma toGrothendieck_le_iff_le_toPrecoverage : K.toGrothendieck ≤ J ↔ K ≤ J.toPrecoverage := by
  refine ⟨fun h X R hR ↦ ?_, fun h ↦ ?_⟩
  · rw [GrothendieckTopology.mem_toPrecoverage_iff]
    exact h _ (generate_mem_toGrothendieck hR)
  · intro X R hR
    induction hR with
    | of X S hS => exact h _ hS
    | top X => grind
    | pullback X S _ Y f _ => grind
    | transitive X S R _ _ _ _ => grind

variable (C) in
/-
**CategoryTheory.Precoverage.galoisConnection_toGrothendieck_toPrecoverage** 是 M
athlib 中的一个引理，位于命名空间 `CategoryTheory.Precoverage`。
形式化陈述：galoisConnection_toGrothendieck_toPrecoverage : GaloisConnection (toGrothe
ndieck (C
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `CategoryTheory.Precoverage.toGrothendieck_le_iff_le_toPrecoverage`：toGro
thendieck_le_iff_le_toPrecoverage : K.toGrothendieck <= J ↔ K <= J.toPrecoverage
-/
lemma galoisConnection_toGrothendieck_toPrecoverage :
    GaloisConnection (toGrothendieck (C := C)) GrothendieckTopology.toPrecoverage :=
  fun _ _ ↦ toGrothendieck_le_iff_le_toPrecoverage

end Precoverage

@[simp, grind =]
/-
**CategoryTheory.Precoverage.toGrothendieck_bot** 是 Mathlib 中的一个定理，位于命名空间 `Categ
oryTheory.Precoverage`。
形式化陈述：∀ {C : Type u_2} [inst : CategoryTheory.Category.{u_3, u_2} C], ⊥.toGrothe
ndieck = ⊥
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `GaloisConnection.l_bot`：∀ {α : Type u} {β : Type v} [inst : PartialOrder
 α] [inst_1 : Preorder β] [inst_2 : OrderBot α] [inst_3 : OrderBot β]   {u : α →
 β} {l : β →…
· 使用引理 `CategoryTheory.Precoverage.galoisConnection_toGrothendieck_toPrecoverage
`：galoisConnection_toGrothendieck_toPrecoverage : GaloisConnection (toGrothendie
ck (C
-/
lemma Precoverage.toGrothendieck_bot : toGrothendieck (⊥ : Precoverage C) = ⊥ :=
  (galoisConnection_toGrothendieck_toPrecoverage C).l_bot

@[simp, grind =]
/-
**CategoryTheory.GrothendieckTopology.toPrecoverage_top** 是 Mathlib 中的一个定理，位于命名空
间 `CategoryTheory.GrothendieckTopology`。
形式化陈述：∀ {C : Type u_2} [inst : CategoryTheory.Category.{u_3, u_2} C], ⊤.toPrecov
erage = ⊤
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `GaloisConnection.u_top`：u_top [OrderTop β] {l : α -> β} {u : β -> α} (gc
 : GaloisConnection l u) : u ⊤ = ⊤
· 使用引理 `CategoryTheory.Precoverage.galoisConnection_toGrothendieck_toPrecoverage
`：galoisConnection_toGrothendieck_toPrecoverage : GaloisConnection (toGrothendie
ck (C
-/
lemma GrothendieckTopology.toPrecoverage_top : toPrecoverage (⊤ : GrothendieckTopology C) = ⊤ :=
  (Precoverage.galoisConnection_toGrothendieck_toPrecoverage C).u_top
/-
**CategoryTheory.Precoverage.toGrothendieck_monotone** 是 Mathlib 中的一个定理，位于命名空间 `
CategoryTheory.Precoverage`。
形式化陈述：∀ {C : Type u_2} [inst : CategoryTheory.Category.{u_3, u_2} C], Monotone C
ategoryTheory.Precoverage.toGrothendieck
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `GaloisConnection.monotone_l`：∀ {α : Type u} {β : Type v} [inst : Preorde
r α] [inst_1 : Preorder β] {u : α → β} {l : β → α},   GaloisConnection l u → Mon
otone l
· 使用引理 `CategoryTheory.Precoverage.galoisConnection_toGrothendieck_toPrecoverage
`：galoisConnection_toGrothendieck_toPrecoverage : GaloisConnection (toGrothendie
ck (C
-/
lemma Precoverage.toGrothendieck_monotone : Monotone (toGrothendieck (C := C)) :=
  (galoisConnection_toGrothendieck_toPrecoverage C).monotone_l

@[gcongr]
/-
**CategoryTheory.Precoverage.toGrothendieck_mono** 是 Mathlib 中的一个定理，位于命名空间 `Cate
goryTheory.Precoverage`。
形式化陈述：∀ {C : Type u_3} [inst : CategoryTheory.Category.{u_2, u_3} C] {J K : Cate
goryTheory.Precoverage C},   J ≤ K → J.toGrothendieck ≤ K.toGrothendieck
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Precoverage.toGrothendieck_monotone`：∀ {C : Type u_2} [in
st : CategoryTheory.Category.{u_3, u_2} C], Monotone CategoryTheory.Precoverage.
toGrothendieck
-/
lemma Precoverage.toGrothendieck_mono {J K : Precoverage C} (h : J ≤ K) :
    J.toGrothendieck ≤ K.toGrothendieck :=
  toGrothendieck_monotone h

@[mono, gcongr]
/-
**CategoryTheory.GrothendieckTopology.toPrecoverage_monotone** 是 Mathlib 中的一个定理，
位于命名空间 `CategoryTheory.GrothendieckTopology`。
形式化陈述：∀ {C : Type u_2} [inst : CategoryTheory.Category.{u_3, u_2} C],   Monotone
 CategoryTheory.GrothendieckTopology.toPrecoverage
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `GaloisConnection.monotone_u`：monotone_u : Monotone u
· 使用引理 `CategoryTheory.Precoverage.galoisConnection_toGrothendieck_toPrecoverage
`：galoisConnection_toGrothendieck_toPrecoverage : GaloisConnection (toGrothendie
ck (C
-/
lemma GrothendieckTopology.toPrecoverage_monotone : Monotone (toPrecoverage (C := C)) :=
  (Precoverage.galoisConnection_toGrothendieck_toPrecoverage C).monotone_u
/-
**CategoryTheory.Precoverage.le_toPrecoverage_toGrothendieck** 是 Mathlib 中的一个定理，
位于命名空间 `CategoryTheory.Precoverage`。
形式化陈述：∀ {C : Type u_3} [inst : CategoryTheory.Category.{u_2, u_3} C] (J : Catego
ryTheory.Precoverage C),   J ≤ J.toGrothendieck.toPrecoverage
参数：J : CategoryTheory.Precoverage C。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `GaloisConnection.le_u_l`：le_u_l (a) : a <= u (l a)
· 使用引理 `CategoryTheory.Precoverage.galoisConnection_toGrothendieck_toPrecoverage
`：galoisConnection_toGrothendieck_toPrecoverage : GaloisConnection (toGrothendie
ck (C
-/
lemma Precoverage.le_toPrecoverage_toGrothendieck (J : Precoverage C) :
    J ≤ J.toGrothendieck.toPrecoverage :=
  (galoisConnection_toGrothendieck_toPrecoverage C).le_u_l _
/-
**CategoryTheory.GrothendieckTopology.toGrothendieck_toPrecoverage_le** 是 Mathli
b 中的一个定理，位于命名空间 `CategoryTheory.GrothendieckTopology`。
形式化陈述：∀ {C : Type u_3} [inst : CategoryTheory.Category.{u_2, u_3} C] (J : Catego
ryTheory.GrothendieckTopology C),   J.toPrecoverage.toGrothendieck ≤ J
参数：J : CategoryTheory.GrothendieckTopology C。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `GaloisConnection.l_u_le`：∀ {α : Type u} {β : Type v} [inst : Preorder α]
 [inst_1 : Preorder β] {u : α → β} {l : β → α},   GaloisConnection l u → ∀ (a : 
α), l (u a) ≤…
· 使用引理 `CategoryTheory.Precoverage.galoisConnection_toGrothendieck_toPrecoverage
`：galoisConnection_toGrothendieck_toPrecoverage : GaloisConnection (toGrothendie
ck (C
-/
lemma GrothendieckTopology.toGrothendieck_toPrecoverage_le (J : GrothendieckTopology C) :
    J.toPrecoverage.toGrothendieck ≤ J :=
  (Precoverage.galoisConnection_toGrothendieck_toPrecoverage C).l_u_le _

end CategoryTheory

