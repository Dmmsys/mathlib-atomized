/-
Copyright (c) 2018 Patrick Massot. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Patrick Massot, Johannes Hölzl
-/
module

public import Mathlib.Topology.UniformSpace.AbstractCompletion

/-!
# Hausdorff completions of uniform spaces

The goal is to construct a left-adjoint to the inclusion of complete Hausdorff uniform spaces
into all uniform spaces. Any uniform space `α` gets a completion `Completion α` and a morphism
(i.e. uniformly continuous map) `(↑) : α → Completion α` which solves the universal
mapping problem of factorizing morphisms from `α` to any complete Hausdorff uniform space `β`.
It means any uniformly continuous `f : α → β` gives rise to a unique morphism
`Completion.extension f : Completion α → β` such that `f = Completion.extension f ∘ (↑)`.
Actually `Completion.extension f` is defined for all maps from `α` to `β` but it has the desired
properties only if `f` is uniformly continuous.

Beware that `(↑)` is not injective if `α` is not Hausdorff. But its image is always
dense. The adjoint functor acting on morphisms is then constructed by the usual abstract nonsense.
For every uniform spaces `α` and `β`, it turns `f : α → β` into a morphism
  `Completion.map f : Completion α → Completion β`
such that
  `(↑) ∘ f = (Completion.map f) ∘ (↑)`
provided `f` is uniformly continuous. This construction is compatible with composition.

In this file we introduce the following concepts:

* `CauchyFilter α` the uniform completion of the uniform space `α` (using Cauchy filters).
  These are not minimal filters.

* `Completion α := Quotient (separationSetoid (CauchyFilter α))` the Hausdorff completion.

## References

This formalization is mostly based on
  N. Bourbaki: General Topology
  I. M. James: Topologies and Uniformities
From a slightly different perspective in order to reuse material in `Topology.UniformSpace.Basic`.
-/

@[expose] public section

noncomputable section

open Filter Set
open scoped SetRel Uniformity Topology

universe u v w

/-- Space of Cauchy filters

This is essentially the completion of a uniform space. The embeddings are the neighbourhood filters.
This space is not minimal, the separated uniform space (i.e. quotiented on the intersection of all
entourages) is necessary for this.
-/
/-
**CauchyFilter** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：CauchyFilter (α : Type u) [UniformSpace α] : Type u
参数：α : Type u。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Space of Cauchy filters

This is essentially the completion of a uniform space. The embeddings are the ne
ighbourhood filters.
This space is not minimal, the separated uniform space (i.e. quotiented on the i
ntersection of all
entourages) is necessary for this.
-/
def CauchyFilter (α : Type u) [UniformSpace α] : Type u :=
  { f : Filter α // Cauchy f }

namespace CauchyFilter

section

variable {α : Type u} [UniformSpace α]
variable {β : Type v} {γ : Type w}
variable [UniformSpace β] [UniformSpace γ]

/-
**CauchyFilter.** 是 Mathlib 中的一个实例，位于命名空间 `CauchyFilter`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (f : CauchyFilter α) : NeBot f.1 := f.2.1

/-- The pairs of Cauchy filters generated by a set. -/
/-
**CauchyFilter.gen** 是 Mathlib 中的一个定义，位于命名空间 `CauchyFilter`。
形式化陈述：gen (s : SetRel α α) : SetRel (CauchyFilter α) (CauchyFilter α)
参数：s : SetRel α α。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The pairs of Cauchy filters generated by a set.
-/
def gen (s : SetRel α α) : SetRel (CauchyFilter α) (CauchyFilter α) :=
  { p | s ∈ p.1.val ×ˢ p.2.val }
/-
**CauchyFilter.monotone_gen** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter`。
形式化陈述：monotone_gen : Monotone (gen : SetRel α α -> _)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.monotone_ofPred`：monotone_ofPred [Preorder α] {p : α -> β -> Prop} (
hp : forall b, Monotone fun a => p a b) : Monotone fun a => { b | p a b }
· 使用定理 `Filter.monotone_mem`：monotone_mem {f : Filter α} : Monotone fun s => s i
n f
-/
theorem monotone_gen : Monotone (gen : SetRel α α → _) :=
  monotone_ofPred fun p => @Filter.monotone_mem _ (p.1.val ×ˢ p.2.val)

-- Porting note: this was a calc proof, but I could not make it work
/-
**CauchyFilter.symm_gen** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
private theorem symm_gen : map Prod.swap ((𝓤 α).lift' gen) ≤ (𝓤 α).lift' gen := by
  let f := fun s : SetRel α α =>
        { p : CauchyFilter α × CauchyFilter α | s ∈ (p.2.val ×ˢ p.1.val : Filter (α × α)) }
  have h₁ : map Prod.swap ((𝓤 α).lift' gen) = (𝓤 α).lift' f := by
    delta gen
    simp [f, map_lift'_eq, monotone_ofPred, Filter.monotone_mem, Function.comp_def,
      image_swap_eq_preimage_swap]
  have h₂ : (𝓤 α).lift' f ≤ (𝓤 α).lift' gen :=
    uniformity_lift_le_swap
      (monotone_principal.comp
        (monotone_ofPred fun p => @Filter.monotone_mem _ (p.2.val ×ˢ p.1.val)))
      (by
        have h := fun p : CauchyFilter α × CauchyFilter α => @Filter.prod_comm _ _ p.2.val p.1.val
        simp only [Function.comp, h, mem_map, f]
        exact le_rfl)
  exact h₁.trans_le h₂
/-
**CauchyFilter.subset_gen_relComp** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
private theorem subset_gen_relComp {s t : SetRel α α} : gen s ○ gen t ⊆ gen (s ○ t) :=
  fun ⟨f, g⟩ ⟨h, h₁, h₂⟩ =>
  let ⟨t₁, (ht₁ : t₁ ∈ f.val), t₂, (ht₂ : t₂ ∈ h.val), (h₁ : t₁ ×ˢ t₂ ⊆ s)⟩ := mem_prod_iff.mp h₁
  let ⟨t₃, (ht₃ : t₃ ∈ h.val), t₄, (ht₄ : t₄ ∈ g.val), (h₂ : t₃ ×ˢ t₄ ⊆ t)⟩ := mem_prod_iff.mp h₂
  have : t₂ ∩ t₃ ∈ h.val := inter_mem ht₂ ht₃
  let ⟨x, xt₂, xt₃⟩ := h.property.left.nonempty_of_mem this
  (f.val ×ˢ g.val).sets_of_superset (prod_mem_prod ht₁ ht₄)
    fun ⟨a, b⟩ ⟨(ha : a ∈ t₁), (hb : b ∈ t₄)⟩ =>
    ⟨x, h₁ (show (a, x) ∈ t₁ ×ˢ t₂ from ⟨ha, xt₂⟩), h₂ (show (x, b) ∈ t₃ ×ˢ t₄ from ⟨xt₃, hb⟩)⟩
/-
**CauchyFilter.comp_gen** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
private theorem comp_gen : ((𝓤 α).lift' gen).lift' (fun s ↦ s ○ s) ≤ (𝓤 α).lift' gen :=
  calc
        ((𝓤 α).lift' gen).lift' (fun s ↦ s ○ s)
    _ = (𝓤 α).lift' fun s ↦ gen s ○ gen s := by
      rw [lift'_lift'_assoc]
      · exact monotone_gen
      · exact monotone_id.relComp monotone_id
    _ ≤ (𝓤 α).lift' fun s ↦ gen <| s ○ s := lift'_mono' fun _ _hs => subset_gen_relComp
    _ = ((𝓤 α).lift' fun s : SetRel α α => s ○ s).lift' gen := by
      rw [lift'_lift'_assoc]
      · exact monotone_id.relComp monotone_id
      · exact monotone_gen
    _ ≤ (𝓤 α).lift' gen := lift'_mono comp_le_uniformity le_rfl
/-
**CauchyFilter.** 是 Mathlib 中的一个实例，位于命名空间 `CauchyFilter`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : UniformSpace (CauchyFilter α) :=
  UniformSpace.ofCore
    { uniformity := (𝓤 α).lift' gen
      refl := principal_le_lift'.2 fun _s hs ⟨a, b⟩ =>
        fun (a_eq_b : a = b) => a_eq_b ▸ a.property.right hs
      symm := by exact symm_gen
      comp := by exact comp_gen }
/-
**CauchyFilter.mem_uniformity** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter`。
形式化陈述：mem_uniformity {s : Set (CauchyFilter α × CauchyFilter α)} : s in 𝓤 (Cauch
yFilter α) ↔ exists t in 𝓤 α, gen t subseteq s
参数：CauchyFilter α × CauchyFilter α。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Filter.mem_lift'_sets`：∀ {α : Type u_1} {β : Type u_2} {f : Filter α} {h
 : Set α → Set β},   Monotone h → ∀ {s : Set β}, s ∈ f.lift' h ↔ ∃ t ∈ f, h t ⊆ 
s
· 使用定理 `CauchyFilter.monotone_gen`：monotone_gen : Monotone (gen : SetRel α α -> 
_)
-/
theorem mem_uniformity {s : Set (CauchyFilter α × CauchyFilter α)} :
    s ∈ 𝓤 (CauchyFilter α) ↔ ∃ t ∈ 𝓤 α, gen t ⊆ s :=
  mem_lift'_sets monotone_gen
/-
**CauchyFilter.basis_uniformity** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter`。
形式化陈述：basis_uniformity {ι : Sort*} {p : ι -> Prop} {s : ι -> SetRel α α} (h : (𝓤
 α).HasBasis p s) : (𝓤 (CauchyFilter α)).HasBasis p (gen ∘ s)
参数：h : (𝓤 α).HasBasis p s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Filter.HasBasis.lift'`：Filter.HasBasis.lift'_interior {l : Filter X} {p 
: ι -> Prop} {s : ι -> Set X} (h : l.HasBasis p s) : (l.lift' interior).HasBasis
 p fun i =>…
· 使用定理 `CauchyFilter.monotone_gen`：monotone_gen : Monotone (gen : SetRel α α -> 
_)
-/
theorem basis_uniformity {ι : Sort*} {p : ι → Prop} {s : ι → SetRel α α} (h : (𝓤 α).HasBasis p s) :
    (𝓤 (CauchyFilter α)).HasBasis p (gen ∘ s) :=
  h.lift' monotone_gen
/-
**CauchyFilter.mem_uniformity'** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter`。
形式化陈述：mem_uniformity' {s : Set (CauchyFilter α × CauchyFilter α)} : s in 𝓤 (Cauc
hyFilter α) ↔ exists t in 𝓤 α, forall f g : CauchyFilter α, t in f.1 ×ˢ g.1 -> (
f, g) in s
参数：CauchyFilter α × CauchyFilter α。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `CauchyFilter.mem_uniformity`：mem_uniformity {s : Set (CauchyFilter α × C
auchyFilter α)} : s in 𝓤 (CauchyFilter α) ↔ exists t in 𝓤 α, gen t subseteq s
· 使用定理 `exists_congr`：∀ {α : Sort u_1} {p q : α → Prop}, (∀ (a : α), p a ↔ q a) 
→ ((∃ a, p a) ↔ ∃ a, q a)
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `and_congr_right_iff`：∀ {a b c : Prop}, (a ∧ b ↔ a ∧ c) ↔ a → (b ↔ c)
-/
theorem mem_uniformity' {s : Set (CauchyFilter α × CauchyFilter α)} :
    s ∈ 𝓤 (CauchyFilter α) ↔ ∃ t ∈ 𝓤 α, ∀ f g : CauchyFilter α, t ∈ f.1 ×ˢ g.1 → (f, g) ∈ s := by
  refine mem_uniformity.trans (exists_congr (fun t => and_congr_right_iff.mpr (fun _h => ?_)))
  exact ⟨fun h _f _g ht => h ht, fun h _p hp => h _ _ hp⟩

/-- Embedding of `α` into its completion `CauchyFilter α` -/
/-
**CauchyFilter.pureCauchy** 是 Mathlib 中的一个定义，位于命名空间 `CauchyFilter`。
形式化陈述：pureCauchy (a : α) : CauchyFilter α
参数：a : α。
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `cauchy_pure`：cauchy_pure {a : α} : Cauchy (pure a)

--- 原说明 ---
Embedding of `α` into its completion `CauchyFilter α`
-/
def pureCauchy (a : α) : CauchyFilter α :=
  ⟨pure a, cauchy_pure⟩
/-
**CauchyFilter.isUniformInducing_pureCauchy** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFil
ter`。
形式化陈述：isUniformInducing_pureCauchy : IsUniformInducing (pureCauchy : α -> Cauchy
Filter α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `cauchy_pure`：cauchy_pure {a : α} : Cauchy (pure a)
· 使用定理 `Filter.prod_pure`：prod_pure {b : β} : f ×ˢ pure b = map (fun a => (a, b)
) f
· 使用定理 `Prod.mk.eta`：∀ {α : Type u_1} {β : Type u_2} {p : α × β}, (p.1, p.2) = p
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `Filter.lift'`：lift'_top (h : Set α -> Set β) : (⊤ : Filter α).lift' h = 
𝓟 (h univ)
· 使用定理 `Filter.comap_lift'_eq`：∀ {α : Type u_1} {β : Type u_2} {γ : Type u_3} {f
 : Filter α} {h : Set α → Set β} {m : γ → β},   Filter.comap m (f.lift' h) = f.l
ift' (Set.p…
· 使用定理 `Filter.lift'_id`：∀ {α : Type u_1} {f : Filter α}, f.lift' id = f
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem isUniformInducing_pureCauchy : IsUniformInducing (pureCauchy : α → CauchyFilter α) :=
  ⟨have : (preimage fun x : α × α => (pureCauchy x.fst, pureCauchy x.snd)) ∘ gen = id :=
      funext fun s =>
        Set.ext fun ⟨a₁, a₂⟩ => by simp [preimage, gen, pureCauchy]
    calc
      comap (fun x : α × α => (pureCauchy x.fst, pureCauchy x.snd)) ((𝓤 α).lift' gen) =
          (𝓤 α).lift' ((preimage fun x : α × α => (pureCauchy x.fst, pureCauchy x.snd)) ∘ gen) :=
        comap_lift'_eq
      _ = 𝓤 α := by simp [this]
      ⟩
/-
**CauchyFilter.isUniformEmbedding_pureCauchy** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFi
lter`。
形式化陈述：isUniformEmbedding_pureCauchy : IsUniformEmbedding (pureCauchy : α -> Cauc
hyFilter α) where __
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CauchyFilter.isUniformInducing_pureCauchy`：isUniformInducing_pureCauchy 
: IsUniformInducing (pureCauchy : α -> CauchyFilter α)
· 使用定理 `Filter.pure_injective`：pure_injective : Injective (pure : α -> Filter α)
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Subtype.ext_iff`：∀ {α : Sort u} {p : α → Prop} {a1 a2 : { x // p x }}, a
1 = a2 ↔ ↑a1 = ↑a2
-/
theorem isUniformEmbedding_pureCauchy : IsUniformEmbedding (pureCauchy : α → CauchyFilter α) where
  __ := isUniformInducing_pureCauchy
  injective _a₁ _a₂ h := pure_injective <| Subtype.ext_iff.1 h
/-
**CauchyFilter.denseRange_pureCauchy** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter`。
形式化陈述：denseRange_pureCauchy : DenseRange (pureCauchy : α -> CauchyFilter α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Filter.lift'`：lift'_top (h : Set α -> Set β) : (⊤ : Filter α).lift' h = 
𝓟 (h univ)
· 使用定理 `Filter.mem_lift'_sets`：∀ {α : Type u_1} {β : Type u_2} {f : Filter α} {h
 : Set α → Set β},   Monotone h → ∀ {s : Set β}, s ∈ f.lift' h ↔ ∃ t ∈ f, h t ⊆ 
s
· 使用定理 `CauchyFilter.monotone_gen`：monotone_gen : Monotone (gen : SetRel α α -> 
_)
· 使用定理 `comp_mem_uniformity_sets`：comp_mem_uniformity_sets {s : SetRel α α} (hs 
: s in 𝓤 α) : exists t in 𝓤 α, t ○ t subseteq s
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b
· 使用定理 `Subtype.property`：∀ {α : Sort u} {p : α → Prop} (self : Subtype p), p ↑s
elf
· 使用定理 `Filter.mem_prod_same_iff`：∀ {α : Type u_1} {la : Filter α} {s : Set (α ×
 α)}, s ∈ la ×ˢ la ↔ ∃ t ∈ la, t ×ˢ t ⊆ s
· 使用定理 `Filter.NeBot.nonempty_of_mem`：∀ {α : Type u} {f : Filter α}, f.NeBot → ∀
 {s : Set α}, s ∈ f → s.Nonempty
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Filter.mem_prod_iff`：mem_prod_iff {s : Set (α × β)} {f : Filter α} {g : 
Filter β} : s in f ×ˢ g ↔ exists t₁ in f, exists t₂ in g, t₁ ×ˢ t₂ subseteq s
· 使用定理 `Set.mk_mem_prod`：mk_mem_prod (ha : a in s) (hb : b in t) : (a, b) in s ×
ˢ t
· 使用引理 `SetRel.prodMk_mem_comp`：prodMk_mem_comp (hab : a ~[R] b) (hbc : b ~[S] c
) : a ~[R ○ S] c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `closure_eq_cluster_pts`：closure_eq_cluster_pts : closure s = { a | Clust
erPt a (𝓟 s) }
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `nhds_eq_uniformity`：nhds_eq_uniformity {x : α} : 𝓝 x = (𝓤 α).lift' (ball
 x)
· 使用定理 `Filter.lift'_inf_principal_eq`：∀ {α : Type u_1} {β : Type u_2} {f : Filt
er α} {h : Set α → Set β} {s : Set β},   f.lift' h ⊓ Filter.principal s = f.lift
' fun t => h t ∩ s
· 使用定理 `Set.inter_comm`：inter_comm (a b : Set α) : a inter b = b inter a
· 使用定理 `Filter.lift'_neBot_iff`：∀ {α : Type u_1} {β : Type u_2} {f : Filter α} {
h : Set α → Set β},   Monotone h → ((f.lift' h).NeBot ↔ ∀ s ∈ f, (h s).Nonempty)
· 使用定理 `Monotone.inter`：Monotone.inter [Preorder β] {f g : β -> Set α} (hf : Mon
otone f) (hg : Monotone g) : Monotone fun x => f x inter g x
· 使用定理 `monotone_const`：monotone_const [Preorder α] [Preorder β] {c : β} : Monot
one fun _ : α => c
· 使用定理 `Set.monotone_preimage`：monotone_preimage {f : α -> β} : Monotone (preima
ge f)
· 使用定理 `Set.mem_range_self`：∀ {α : Type u} {ι : Sort u_1} {f : ι → α} (i : ι), f
 i ∈ Set.range f
-/
theorem denseRange_pureCauchy : DenseRange (pureCauchy : α → CauchyFilter α) := fun f => by
  have h_ex : ∀ s ∈ 𝓤 (CauchyFilter α), ∃ y : α, (f, pureCauchy y) ∈ s := fun s hs =>
    let ⟨t'', ht''₁, (ht''₂ : gen t'' ⊆ s)⟩ := (mem_lift'_sets monotone_gen).mp hs
    let ⟨t', ht'₁, ht'₂⟩ := comp_mem_uniformity_sets ht''₁
    have : t' ∈ f.val ×ˢ f.val := f.property.right ht'₁
    let ⟨t, ht, (h : t ×ˢ t ⊆ t')⟩ := mem_prod_same_iff.mp this
    let ⟨x, (hx : x ∈ t)⟩ := f.property.left.nonempty_of_mem ht
    have : t'' ∈ f.val ×ˢ pure x :=
      mem_prod_iff.mpr
        ⟨t, ht, { y : α | (x, y) ∈ t' }, h <| mk_mem_prod hx hx,
          fun ⟨a, b⟩ ⟨(h₁ : a ∈ t), (h₂ : (x, b) ∈ t')⟩ =>
          ht'₂ <| SetRel.prodMk_mem_comp (@h (a, x) ⟨h₁, hx⟩) h₂⟩
    ⟨x, ht''₂ <| by dsimp [gen]; exact this⟩
  simp only [closure_eq_cluster_pts, ClusterPt, nhds_eq_uniformity, lift'_inf_principal_eq,
    Set.inter_comm _ (range pureCauchy), mem_ofPred_eq]
  refine (lift'_neBot_iff ?_).mpr (fun s hs => ?_)
  · exact monotone_const.inter monotone_preimage
  · let ⟨y, hy⟩ := h_ex s hs
    have : pureCauchy y ∈ range pureCauchy ∩ { y : CauchyFilter α | (f, y) ∈ s } :=
      ⟨mem_range_self y, hy⟩
    exact ⟨_, this⟩
/-
**CauchyFilter.isDenseInducing_pureCauchy** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilte
r`。
形式化陈述：isDenseInducing_pureCauchy : IsDenseInducing (pureCauchy : α -> CauchyFilt
er α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `IsUniformInducing.isDenseInducing`：IsUniformInducing.isDenseInducing (h 
: IsUniformInducing f) (hd : DenseRange f) : IsDenseInducing f where toIsInducin
g
· 使用定理 `CauchyFilter.isUniformInducing_pureCauchy`：isUniformInducing_pureCauchy 
: IsUniformInducing (pureCauchy : α -> CauchyFilter α)
· 使用定理 `CauchyFilter.denseRange_pureCauchy`：denseRange_pureCauchy : DenseRange (
pureCauchy : α -> CauchyFilter α)
-/
theorem isDenseInducing_pureCauchy : IsDenseInducing (pureCauchy : α → CauchyFilter α) :=
  isUniformInducing_pureCauchy.isDenseInducing denseRange_pureCauchy
/-
**CauchyFilter.isDenseEmbedding_pureCauchy** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilt
er`。
形式化陈述：isDenseEmbedding_pureCauchy : IsDenseEmbedding (pureCauchy : α -> CauchyFi
lter α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsUniformEmbedding.isDenseEmbedding`：IsUniformEmbedding.isDenseEmbedding
 {f : α -> β} (h : IsUniformEmbedding f) (hd : DenseRange f) : IsDenseEmbedding 
f
· 使用定理 `CauchyFilter.isUniformEmbedding_pureCauchy`：isUniformEmbedding_pureCauch
y : IsUniformEmbedding (pureCauchy : α -> CauchyFilter α) where __
· 使用定理 `CauchyFilter.denseRange_pureCauchy`：denseRange_pureCauchy : DenseRange (
pureCauchy : α -> CauchyFilter α)
-/
theorem isDenseEmbedding_pureCauchy : IsDenseEmbedding (pureCauchy : α → CauchyFilter α) :=
  isUniformEmbedding_pureCauchy.isDenseEmbedding denseRange_pureCauchy
/-
**CauchyFilter.nonempty_cauchyFilter_iff** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter
`。
形式化陈述：nonempty_cauchyFilter_iff : Nonempty (CauchyFilter α) ↔ Nonempty α
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Set.eq_univ_iff_forall`：eq_univ_iff_forall {s : Set α} : s = univ ↔ fora
ll x, x in s
· 使用定理 `IsDenseInducing.closure_range`：closure_range (di : IsDenseInducing i) : 
closure (range i) = univ
· 使用引理 `IsDenseEmbedding.isDenseInducing`：isDenseInducing (de : IsDenseEmbedding
 e) : IsDenseInducing e
· 使用定理 `CauchyFilter.isDenseEmbedding_pureCauchy`：isDenseEmbedding_pureCauchy : 
IsDenseEmbedding (pureCauchy : α -> CauchyFilter α)
· 使用定理 `mem_closure_iff`：mem_closure_iff : x in closure s ↔ forall o, IsOpen o -
> x in o -> (o inter s).Nonempty
· 使用定理 `isOpen_univ`：∀ {X : Type u} [inst : TopologicalSpace X], IsOpen Set.univ
· 使用定理 `trivial`：True
-/
theorem nonempty_cauchyFilter_iff : Nonempty (CauchyFilter α) ↔ Nonempty α := by
  constructor <;> rintro ⟨c⟩
  · have := eq_univ_iff_forall.1 isDenseEmbedding_pureCauchy.isDenseInducing.closure_range c
    obtain ⟨_, ⟨_, a, _⟩⟩ := mem_closure_iff.1 this _ isOpen_univ trivial
    exact ⟨a⟩
  · exact ⟨pureCauchy c⟩

section

/-
**CauchyFilter.** 是 Mathlib 中的一个实例，位于命名空间 `CauchyFilter`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : CompleteSpace (CauchyFilter α) :=
  completeSpace_extension isUniformInducing_pureCauchy denseRange_pureCauchy fun f hf =>
    let f' : CauchyFilter α := ⟨f, hf⟩
    have : map pureCauchy f ≤ (𝓤 <| CauchyFilter α).lift' (preimage (Prod.mk f')) :=
      le_lift'.2 fun _ hs =>
        let ⟨t, ht₁, ht₂⟩ := (mem_lift'_sets monotone_gen).mp hs
        let ⟨t', ht', (h : t' ×ˢ t' ⊆ t)⟩ := mem_prod_same_iff.mp (hf.right ht₁)
        have : t' ⊆ { y : α | (f', pureCauchy y) ∈ gen t } := fun x hx =>
          (f ×ˢ pure x).sets_of_superset (prod_mem_prod ht' hx) h
        f.sets_of_superset ht' <| Subset.trans this (preimage_mono ht₂)
    ⟨f', by simpa [nhds_eq_uniformity]⟩

end

/-
**CauchyFilter.** 是 Mathlib 中的一个实例，位于命名空间 `CauchyFilter`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [Inhabited α] : Inhabited (CauchyFilter α) :=
  ⟨pureCauchy default⟩
/-
**CauchyFilter.** 是 Mathlib 中的一个实例，位于命名空间 `CauchyFilter`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [h : Nonempty α] : Nonempty (CauchyFilter α) :=
  h.recOn fun a => Nonempty.intro <| CauchyFilter.pureCauchy a

section Extend

open scoped Classical in
/-- Extend a uniformly continuous function `α → β` to a function `CauchyFilter α → β`.
Outputs junk when `f` is not uniformly continuous. -/
/-
**CauchyFilter.extend** 是 Mathlib 中的一个定义，位于命名空间 `CauchyFilter`。
形式化陈述：extend (f : α -> β) : CauchyFilter α -> β
参数：f : α -> β。
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `CauchyFilter.isDenseInducing_pureCauchy`：isDenseInducing_pureCauchy : Is
DenseInducing (pureCauchy : α -> CauchyFilter α)

--- 原说明 ---
Extend a uniformly continuous function `α → β` to a function `CauchyFilter α → β
`.
Outputs junk when `f` is not uniformly continuous.
-/
def extend (f : α → β) : CauchyFilter α → β :=
  if UniformContinuous f then isDenseInducing_pureCauchy.extend f
  else fun x => f (nonempty_cauchyFilter_iff.1 ⟨x⟩).some

section T0Space

variable [T0Space β]

/-
**CauchyFilter.extend_pureCauchy** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter`。
形式化陈述：extend_pureCauchy {f : α -> β} (hf : UniformContinuous f) (a : α) : extend
 f (pureCauchy a) = f a
参数：hf : UniformContinuous f；a : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CauchyFilter.isDenseInducing_pureCauchy`：isDenseInducing_pureCauchy : Is
DenseInducing (pureCauchy : α -> CauchyFilter α)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `CauchyFilter.extend.eq_1`：∀ {α : Type u} [inst : UniformSpace α] {β : Ty
pe v} [inst_1 : UniformSpace β] (f : α → β),   CauchyFilter.extend f = if Unifor
mContinuous f …
· 使用定理 `if_pos`：∀ {c : Prop} {h : Decidable c}, c → ∀ {α : Sort u} {t e : α}, (i
f c then t else e) = t
· 使用定理 `uniformly_extend_of_ind`：uniformly_extend_of_ind (b : β) : ψ (e b) = f b
· 使用定理 `CauchyFilter.isUniformInducing_pureCauchy`：isUniformInducing_pureCauchy 
: IsUniformInducing (pureCauchy : α -> CauchyFilter α)
· 使用定理 `CauchyFilter.denseRange_pureCauchy`：denseRange_pureCauchy : DenseRange (
pureCauchy : α -> CauchyFilter α)
-/
theorem extend_pureCauchy {f : α → β} (hf : UniformContinuous f) (a : α) :
    extend f (pureCauchy a) = f a := by
  rw [extend, if_pos hf]
  exact uniformly_extend_of_ind isUniformInducing_pureCauchy denseRange_pureCauchy hf _

end T0Space

variable [CompleteSpace β]

@[fun_prop]
/-
**CauchyFilter.uniformContinuous_extend** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter`
。
形式化陈述：uniformContinuous_extend {f : α -> β} : UniformContinuous (extend f)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CauchyFilter.isDenseInducing_pureCauchy`：isDenseInducing_pureCauchy : Is
DenseInducing (pureCauchy : α -> CauchyFilter α)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `CauchyFilter.extend.eq_1`：∀ {α : Type u} [inst : UniformSpace α] {β : Ty
pe v} [inst_1 : UniformSpace β] (f : α → β),   CauchyFilter.extend f = if Unifor
mContinuous f …
· 使用定理 `if_pos`：∀ {c : Prop} {h : Decidable c}, c → ∀ {α : Sort u} {t e : α}, (i
f c then t else e) = t
· 使用定理 `uniformContinuous_uniformly_extend`：uniformContinuous_uniformly_extend [
CompleteSpace γ] : UniformContinuous ψ
· 使用定理 `CauchyFilter.isUniformInducing_pureCauchy`：isUniformInducing_pureCauchy 
: IsUniformInducing (pureCauchy : α -> CauchyFilter α)
· 使用定理 `CauchyFilter.denseRange_pureCauchy`：denseRange_pureCauchy : DenseRange (
pureCauchy : α -> CauchyFilter α)
· 使用定理 `if_neg`：∀ {c : Prop} {h : Decidable c}, ¬c → ∀ {α : Sort u} {t e : α}, (
if c then t else e) = e
· 使用定理 `uniformContinuous_of_const`：uniformContinuous_of_const {c : α -> β} (h :
 forall a b, c a = c b) : UniformContinuous c
-/
theorem uniformContinuous_extend {f : α → β} : UniformContinuous (extend f) := by
  by_cases hf : UniformContinuous f
  · rw [extend, if_pos hf]
    exact uniformContinuous_uniformly_extend isUniformInducing_pureCauchy denseRange_pureCauchy hf
  · rw [extend, if_neg hf]
    exact uniformContinuous_of_const fun a _b => by congr

end Extend

/-
**CauchyFilter.inseparable_iff** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter`。
形式化陈述：inseparable_iff {f g : CauchyFilter α} : Inseparable f g ↔ f.1 ×ˢ g.1 <= 𝓤
 α
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Filter.HasBasis.inseparable_iff_uniformity`：Filter.HasBasis.inseparable_
iff_uniformity {ι : Sort*} {p : ι -> Prop} {s : ι -> Set (α × α)} (h : (𝓤 α).Has
Basis p s) {x y : α} : Inseparab…
· 使用定理 `CauchyFilter.basis_uniformity`：basis_uniformity {ι : Sort*} {p : ι -> Pr
op} {s : ι -> SetRel α α} (h : (𝓤 α).HasBasis p s) : (𝓤 (CauchyFilter α)).HasBas
is p (gen ∘ s)
· 使用定理 `Filter.basis_sets`：basis_sets (l : Filter α) : l.HasBasis (fun s : Set α
 => s in l) id
-/
theorem inseparable_iff {f g : CauchyFilter α} : Inseparable f g ↔ f.1 ×ˢ g.1 ≤ 𝓤 α :=
  (basis_uniformity (basis_sets _)).inseparable_iff_uniformity
/-
**CauchyFilter.inseparable_iff_of_le_nhds** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilte
r`。
形式化陈述：inseparable_iff_of_le_nhds {f g : CauchyFilter α} {a b : α} (ha : f.1 <= 𝓝
 a) (hb : g.1 <= 𝓝 b) : Inseparable a b ↔ Inseparable f g
参数：ha : f.1 <= 𝓝 a；hb : g.1 <= 𝓝 b。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `CauchyFilter.inseparable_iff`：inseparable_iff {f g : CauchyFilter α} : I
nseparable f g ↔ f.1 ×ˢ g.1 <= 𝓤 α
· 使用定理 `Filter.Tendsto.inseparable_iff_uniformity`：Filter.Tendsto.inseparable_if
f_uniformity {β} {l : Filter β} [NeBot l] {f g : β -> α} {a b : α} (ha : Tendsto
 f l (𝓝 a)) (hb : Tendsto g l (…
· 使用定理 `Filter.prod.instNeBot`：∀ {α : Type u_1} {β : Type u_2} {f : Filter α} {g
 : Filter β} [hf : f.NeBot] [hg : g.NeBot], (f ×ˢ g).NeBot
· 使用定理 `CauchyFilter.instNeBotValFilterCauchy`：∀ {α : Type u} [inst : UniformSpa
ce α] (f : CauchyFilter α), (↑f).NeBot
· 使用定理 `Filter.Tendsto.comp`：∀ {α : Type u_1} {β : Type u_2} {γ : Type u_3} {f :
 α → β} {g : β → γ} {x : Filter α} {y : Filter β} {z : Filter γ},   Filter.Tends
to g y z …
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Filter.tendsto_id'`：tendsto_id' {x y : Filter α} : Tendsto id x y ↔ x <=
 y
· 使用定理 `Filter.tendsto_fst`：tendsto_fst : Tendsto Prod.fst (f ×ˢ g) f
· 使用定理 `Filter.tendsto_snd`：tendsto_snd : Tendsto Prod.snd (f ×ˢ g) g
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Prod.mk.eta`：∀ {α : Type u_1} {β : Type u_2} {p : α × β}, (p.1, p.2) = p
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
theorem inseparable_iff_of_le_nhds {f g : CauchyFilter α} {a b : α}
    (ha : f.1 ≤ 𝓝 a) (hb : g.1 ≤ 𝓝 b) : Inseparable a b ↔ Inseparable f g := by
  rw [← tendsto_id'] at ha hb
  rw [inseparable_iff, (ha.comp tendsto_fst).inseparable_iff_uniformity (hb.comp tendsto_snd)]
  simp only [Function.comp_apply, id_eq, Prod.mk.eta, ← Function.id_def, tendsto_id']
/-
**CauchyFilter.inseparable_lim_iff** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter`。
形式化陈述：inseparable_lim_iff [CompleteSpace α] {f g : CauchyFilter α} : haveI
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CauchyFilter.inseparable_iff_of_le_nhds`：inseparable_iff_of_le_nhds {f g
 : CauchyFilter α} {a b : α} (ha : f.1 <= 𝓝 a) (hb : g.1 <= 𝓝 b) : Inseparable a
 b ↔ Inseparable f g
· 使用定理 `Filter.NeBot.nonempty`：∀ {α : Type u} (f : Filter α) [hf : f.NeBot], Non
empty α
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `Subtype.property`：∀ {α : Sort u} {p : α → Prop} (self : Subtype p), p ↑s
elf
· 使用定理 `Cauchy.le_nhds_lim`：Cauchy.le_nhds_lim [CompleteSpace α] {f : Filter α} 
(hf : Cauchy f) : haveI
-/
theorem inseparable_lim_iff [CompleteSpace α] {f g : CauchyFilter α} :
    haveI := f.2.1.nonempty; Inseparable (lim f.1) (lim g.1) ↔ Inseparable f g :=
  inseparable_iff_of_le_nhds f.2.le_nhds_lim g.2.le_nhds_lim

end

/-
**CauchyFilter.cauchyFilter_eq** 是 Mathlib 中的一个定理，位于命名空间 `CauchyFilter`。
形式化陈述：cauchyFilter_eq {α : Type*} [UniformSpace α] [CompleteSpace α] [T0Space α]
 {f g : CauchyFilter α} : haveI
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Filter.NeBot.nonempty`：∀ {α : Type u} (f : Filter α) [hf : f.NeBot], Non
empty α
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `Subtype.property`：∀ {α : Sort u} {p : α → Prop} (self : Subtype p), p ↑s
elf
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `inseparable_iff_eq`：inseparable_iff_eq [T0Space X] {x y : X} : Inseparab
le x y ↔ x = y
· 使用定理 `CauchyFilter.inseparable_lim_iff`：inseparable_lim_iff [CompleteSpace α] 
{f g : CauchyFilter α} : haveI
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem cauchyFilter_eq {α : Type*} [UniformSpace α] [CompleteSpace α] [T0Space α]
    {f g : CauchyFilter α} :
    haveI := f.2.1.nonempty; lim f.1 = lim g.1 ↔ Inseparable f g := by
  rw [← inseparable_iff_eq, inseparable_lim_iff]

section

/-
**CauchyFilter.separated_pureCauchy_injective** 是 Mathlib 中的一个定理，位于命名空间 `CauchyF
ilter`。
形式化陈述：separated_pureCauchy_injective {α : Type*} [UniformSpace α] [T0Space α] : 
Function.Injective fun a : α => SeparationQuotient.mk (pureCauchy a)
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Inseparable.eq`：Inseparable.eq [T0Space X] {x y : X} (h : Inseparable x 
y) : x = y
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `CauchyFilter.inseparable_iff_of_le_nhds`：inseparable_iff_of_le_nhds {f g
 : CauchyFilter α} {a b : α} (ha : f.1 <= 𝓝 a) (hb : g.1 <= 𝓝 b) : Inseparable a
 b ↔ Inseparable f g
· 使用定理 `pure_le_nhds`：pure_le_nhds : pure <= (𝓝 : X -> Filter X)
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `SeparationQuotient.mk_eq_mk`：mk_eq_mk : mk x = mk y ↔ (x ~ᵢ y)
-/
theorem separated_pureCauchy_injective {α : Type*} [UniformSpace α] [T0Space α] :
    Function.Injective fun a : α => SeparationQuotient.mk (pureCauchy a) := fun a b h ↦
  Inseparable.eq <| (inseparable_iff_of_le_nhds (pure_le_nhds a) (pure_le_nhds b)).2 <|
    SeparationQuotient.mk_eq_mk.1 h

end

end CauchyFilter

open CauchyFilter Set

namespace UniformSpace

variable (α : Type*) [UniformSpace α]
variable {β : Type*} [UniformSpace β]
variable {γ : Type*} [UniformSpace γ]

/-- Hausdorff completion of `α` -/
/-
**UniformSpace.Completion** 是 Mathlib 中的一个定义，位于命名空间 `UniformSpace`。
形式化陈述：Completion
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Hausdorff completion of `α`
-/
def Completion := SeparationQuotient (CauchyFilter α)

namespace Completion

/-
**UniformSpace.Completion.inhabited** 是 Mathlib 中的一个实例，位于命名空间 `UniformSpace.Comp
letion`。
形式化陈述：inhabited [Inhabited α] : Inhabited (Completion α)
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance inhabited [Inhabited α] : Inhabited (Completion α) :=
  inferInstanceAs <| Inhabited (Quotient _)
/-
**UniformSpace.Completion.uniformSpace** 是 Mathlib 中的一个实例，位于命名空间 `UniformSpace.C
ompletion`。
形式化陈述：uniformSpace : UniformSpace (Completion α)
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance uniformSpace : UniformSpace (Completion α) :=
  fast_instance% SeparationQuotient.instUniformSpace
/-
**UniformSpace.Completion.completeSpace** 是 Mathlib 中的一个实例，位于命名空间 `UniformSpace.
Completion`。
形式化陈述：completeSpace : CompleteSpace (Completion α)
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `CauchyFilter.instCompleteSpace`：∀ {α : Type u} [inst : UniformSpace α], 
CompleteSpace (CauchyFilter α)
-/
instance completeSpace : CompleteSpace (Completion α) :=
  SeparationQuotient.instCompleteSpace
/-
**UniformSpace.Completion.t0Space** 是 Mathlib 中的一个实例，位于命名空间 `UniformSpace.Comple
tion`。
形式化陈述：t0Space : T0Space (Completion α)
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance t0Space : T0Space (Completion α) := SeparationQuotient.instT0Space

variable {α} in
/-- The map from a uniform space to its completion. -/
/-
**UniformSpace.Completion.coe'** 是 Mathlib 中的一个定义，位于命名空间 `UniformSpace.Completio
n`。
形式化陈述：{α : Type u_1} → [inst : UniformSpace α] → α → UniformSpace.Completion α
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The map from a uniform space to its completion.
-/
@[coe] def coe' : α → Completion α := SeparationQuotient.mk ∘ pureCauchy

/-- Automatic coercion from `α` to its completion. Not always injective. -/
/-
**UniformSpace.Completion.** 是 Mathlib 中的一个实例，位于命名空间 `UniformSpace.Completion`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Automatic coercion from `α` to its completion. Not always injective.
-/
instance : Coe α (Completion α) :=
  ⟨coe'⟩

-- note [use has_coe_t]
/-
**UniformSpace.Completion.coe_eq** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.Complet
ion`。
形式化陈述：∀ (α : Type u_1) [inst : UniformSpace α], UniformSpace.Completion.coe' = S
eparationQuotient.mk ∘ CauchyFilter.pureCauchy
参数：α : Type u_1。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
protected theorem coe_eq : ((↑) : α → Completion α) = SeparationQuotient.mk ∘ pureCauchy := rfl
/-
**UniformSpace.Completion.isUniformInducing_coe** 是 Mathlib 中的一个定理，位于命名空间 `Unifo
rmSpace.Completion`。
形式化陈述：isUniformInducing_coe : IsUniformInducing ((↑) : α -> Completion α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsUniformInducing.comp`：IsUniformInducing.comp {g : β -> γ} (hg : IsUnif
ormInducing g) {f : α -> β} (hf : IsUniformInducing f) : IsUniformInducing (g ∘ 
f)
· 使用引理 `SeparationQuotient.isUniformInducing_mk`：SeparationQuotient.isUniformInd
ucing_mk : IsUniformInducing (mk : α -> SeparationQuotient α)
· 使用定理 `CauchyFilter.isUniformInducing_pureCauchy`：isUniformInducing_pureCauchy 
: IsUniformInducing (pureCauchy : α -> CauchyFilter α)
-/
theorem isUniformInducing_coe : IsUniformInducing ((↑) : α → Completion α) :=
  SeparationQuotient.isUniformInducing_mk.comp isUniformInducing_pureCauchy
/-
**UniformSpace.Completion.comap_coe_eq_uniformity** 是 Mathlib 中的一个定理，位于命名空间 `Uni
formSpace.Completion`。
形式化陈述：comap_coe_eq_uniformity : ((𝓤 _).comap fun p : α × α => ((p.1 : Completion
 α), (p.2 : Completion α))) = 𝓤 α
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsUniformInducing.comap_uniformity`：∀ {α : Type ua} {β : Type ub} [inst 
: UniformSpace α] [inst_1 : UniformSpace β] {f : α → β},   IsUniformInducing f →
 Filter.comap (fun x => …
· 使用定理 `UniformSpace.Completion.isUniformInducing_coe`：isUniformInducing_coe : I
sUniformInducing ((↑) : α -> Completion α)
-/
theorem comap_coe_eq_uniformity :
    ((𝓤 _).comap fun p : α × α => ((p.1 : Completion α), (p.2 : Completion α))) = 𝓤 α :=
  (isUniformInducing_coe _).1

variable {α} in
/-
**UniformSpace.Completion.denseRange_coe** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace
.Completion`。
形式化陈述：denseRange_coe : DenseRange ((↑) : α -> Completion α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `DenseRange.comp`：DenseRange.comp {g : Y -> Z} {f : α -> Y} (hg : DenseRa
nge g) (hf : DenseRange f) (cg : Continuous g) : DenseRange (g ∘ f)
· 使用定理 `Function.Surjective.denseRange`：Function.Surjective.denseRange (hf : Fun
ction.Surjective f) : DenseRange f
· 使用定理 `SeparationQuotient.surjective_mk`：surjective_mk : Surjective (mk : X -> 
SeparationQuotient X)
· 使用定理 `CauchyFilter.denseRange_pureCauchy`：denseRange_pureCauchy : DenseRange (
pureCauchy : α -> CauchyFilter α)
· 使用定理 `SeparationQuotient.continuous_mk`：continuous_mk : Continuous (mk : X -> 
SeparationQuotient X)
-/
theorem denseRange_coe : DenseRange ((↑) : α → Completion α) :=
  SeparationQuotient.surjective_mk.denseRange.comp denseRange_pureCauchy
    SeparationQuotient.continuous_mk

/-- The Hausdorff completion as an abstract completion. -/
/-
**UniformSpace.Completion.cPkg** 是 Mathlib 中的一个定义，位于命名空间 `UniformSpace.Completio
n`。
形式化陈述：cPkg {α : Type*} [UniformSpace α] : AbstractCompletion α where space
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `UniformSpace.Completion.isUniformInducing_coe`：isUniformInducing_coe : I
sUniformInducing ((↑) : α -> Completion α)
· 使用定理 `UniformSpace.Completion.denseRange_coe`：denseRange_coe : DenseRange ((↑)
 : α -> Completion α)

--- 原说明 ---
The Hausdorff completion as an abstract completion.
-/
def cPkg {α : Type*} [UniformSpace α] : AbstractCompletion α where
  space := Completion α
  coe := (↑)
  uniformStruct := by infer_instance
  complete := by infer_instance
  separation := by infer_instance
  isUniformInducing := Completion.isUniformInducing_coe α
  dense := Completion.denseRange_coe
/-
**UniformSpace.Completion.AbstractCompletion.inhabited** 是 Mathlib 中的一个定义，位于命名空间
 `UniformSpace.Completion.AbstractCompletion`。
形式化陈述：(α : Type u_1) → [inst : UniformSpace α] → Inhabited (AbstractCompletion.{
u_1, u_1} α)
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance AbstractCompletion.inhabited : Inhabited (AbstractCompletion α) :=
  ⟨cPkg⟩

attribute [local instance]
  AbstractCompletion.uniformStruct AbstractCompletion.complete AbstractCompletion.separation
/-
**UniformSpace.Completion.nonempty_completion_iff** 是 Mathlib 中的一个定理，位于命名空间 `Uni
formSpace.Completion`。
形式化陈述：nonempty_completion_iff : Nonempty (Completion α) ↔ Nonempty α
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.symm`：∀ {a b : Prop}, (a ↔ b) → (b ↔ a)
· 使用定理 `DenseRange.nonempty_iff`：∀ {X : Type u_1} [inst : TopologicalSpace X] {α
 : Type u_4} {f : α → X}, DenseRange f → (Nonempty α ↔ Nonempty X)
· 使用定理 `AbstractCompletion.dense`：∀ {α : Type u} [inst : UniformSpace α] (self :
 AbstractCompletion.{v, u} α), DenseRange self.coe
-/
theorem nonempty_completion_iff : Nonempty (Completion α) ↔ Nonempty α :=
  cPkg.dense.nonempty_iff.symm

@[fun_prop]
/-
**UniformSpace.Completion.uniformContinuous_coe** 是 Mathlib 中的一个定理，位于命名空间 `Unifo
rmSpace.Completion`。
形式化陈述：uniformContinuous_coe : UniformContinuous ((↑) : α -> Completion α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.uniformContinuous_coe`：uniformContinuous_coe : Unifor
mContinuous ι
-/
theorem uniformContinuous_coe : UniformContinuous ((↑) : α → Completion α) :=
  cPkg.uniformContinuous_coe
/-
**UniformSpace.Completion.continuous_coe** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace
.Completion`。
形式化陈述：continuous_coe : Continuous ((↑) : α -> Completion α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.continuous_coe`：continuous_coe : Continuous ι
-/
theorem continuous_coe : Continuous ((↑) : α → Completion α) :=
  cPkg.continuous_coe
/-
**UniformSpace.Completion.isUniformEmbedding_coe** 是 Mathlib 中的一个定理，位于命名空间 `Unif
ormSpace.Completion`。
形式化陈述：isUniformEmbedding_coe [T0Space α] : IsUniformEmbedding ((↑) : α -> Comple
tion α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `UniformSpace.Completion.comap_coe_eq_uniformity`：comap_coe_eq_uniformity
 : ((𝓤 _).comap fun p : α × α => ((p.1 : Completion α), (p.2 : Completion α))) =
 𝓤 α
· 使用定理 `CauchyFilter.separated_pureCauchy_injective`：separated_pureCauchy_inject
ive {α : Type*} [UniformSpace α] [T0Space α] : Function.Injective fun a : α => S
eparationQuotient.mk (pureCauchy …
-/
theorem isUniformEmbedding_coe [T0Space α] : IsUniformEmbedding ((↑) : α → Completion α) :=
  { comap_uniformity := comap_coe_eq_uniformity α
    injective := separated_pureCauchy_injective }
/-
**UniformSpace.Completion.coe_injective** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.
Completion`。
形式化陈述：coe_injective [T0Space α] : Function.Injective ((↑) : α -> Completion α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsUniformEmbedding.injective`：∀ {α : Type ua} {β : Type ub} [inst : Unif
ormSpace α] [inst_1 : UniformSpace β] {f : α → β},   IsUniformEmbedding f → Func
tion.Injective f
· 使用定理 `UniformSpace.Completion.isUniformEmbedding_coe`：isUniformEmbedding_coe [
T0Space α] : IsUniformEmbedding ((↑) : α -> Completion α)
-/
theorem coe_injective [T0Space α] : Function.Injective ((↑) : α → Completion α) :=
  IsUniformEmbedding.injective (isUniformEmbedding_coe _)

variable {α}

@[simp]
/-
**UniformSpace.Completion.coe_inj** 是 Mathlib 中的一个引理，位于命名空间 `UniformSpace.Comple
tion`。
形式化陈述：coe_inj [T0Space α] {a b : α} : (a : Completion α) = b ↔ a = b
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Function.Injective.eq_iff`：∀ {α : Sort u_1} {β : Sort u_2} {f : α → β}, 
Function.Injective f → ∀ {a b : α}, f a = f b ↔ a = b
· 使用定理 `UniformSpace.Completion.coe_injective`：coe_injective [T0Space α] : Funct
ion.Injective ((↑) : α -> Completion α)
-/
lemma coe_inj [T0Space α] {a b : α} : (a : Completion α) = b ↔ a = b :=
  (coe_injective _).eq_iff
/-
**UniformSpace.Completion.isDenseInducing_coe** 是 Mathlib 中的一个定理，位于命名空间 `Uniform
Space.Completion`。
形式化陈述：isDenseInducing_coe : IsDenseInducing ((↑) : α -> Completion α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsUniformInducing.isInducing`：IsUniformInducing.isInducing {f : α -> β} 
(h : IsUniformInducing f) : IsInducing f
· 使用定理 `UniformSpace.Completion.isUniformInducing_coe`：isUniformInducing_coe : I
sUniformInducing ((↑) : α -> Completion α)
· 使用定理 `UniformSpace.Completion.denseRange_coe`：denseRange_coe : DenseRange ((↑)
 : α -> Completion α)
-/
theorem isDenseInducing_coe : IsDenseInducing ((↑) : α → Completion α) :=
  { (isUniformInducing_coe α).isInducing with dense := denseRange_coe }

/-- The uniform bijection between a complete space and its uniform completion. -/
/-
**UniformSpace.Completion.UniformCompletion.completeEquivSelf** 是 Mathlib 中的一个定义
，位于命名空间 `UniformSpace.Completion.UniformCompletion`。
形式化陈述：{α : Type u_1} → [inst : UniformSpace α] → [CompleteSpace α] → [T0Space α]
 → UniformSpace.Completion α ≃ᵤ α
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The uniform bijection between a complete space and its uniform completion.
-/
def UniformCompletion.completeEquivSelf [CompleteSpace α] [T0Space α] : Completion α ≃ᵤ α :=
  AbstractCompletion.compareEquiv Completion.cPkg AbstractCompletion.ofComplete

open TopologicalSpace
/-
**UniformSpace.Completion.separableSpace_completion** 是 Mathlib 中的一个实例，位于命名空间 `U
niformSpace.Completion`。
形式化陈述：separableSpace_completion [SeparableSpace α] : SeparableSpace (Completion 
α)
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `IsDenseInducing.separableSpace`：∀ {α : Type u_1} {β : Type u_2} [inst : 
TopologicalSpace α] [inst_1 : TopologicalSpace β] {i : α → β}   [TopologicalSpac
e.SeparableSpace α],…
· 使用定理 `UniformSpace.Completion.isDenseInducing_coe`：isDenseInducing_coe : IsDen
seInducing ((↑) : α -> Completion α)
-/
instance separableSpace_completion [SeparableSpace α] : SeparableSpace (Completion α) :=
  Completion.isDenseInducing_coe.separableSpace
/-
**UniformSpace.Completion.isDenseEmbedding_coe** 是 Mathlib 中的一个定理，位于命名空间 `Unifor
mSpace.Completion`。
形式化陈述：isDenseEmbedding_coe [T0Space α] : IsDenseEmbedding ((↑) : α -> Completion
 α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `UniformSpace.Completion.isDenseInducing_coe`：isDenseInducing_coe : IsDen
seInducing ((↑) : α -> Completion α)
· 使用定理 `CauchyFilter.separated_pureCauchy_injective`：separated_pureCauchy_inject
ive {α : Type*} [UniformSpace α] [T0Space α] : Function.Injective fun a : α => S
eparationQuotient.mk (pureCauchy …
-/
theorem isDenseEmbedding_coe [T0Space α] : IsDenseEmbedding ((↑) : α → Completion α) :=
  { isDenseInducing_coe with injective := separated_pureCauchy_injective }
/-
**UniformSpace.Completion.denseRange_coe** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace
.Completion`。
形式化陈述：denseRange_coe : DenseRange ((↑) : α -> Completion α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `DenseRange.comp`：DenseRange.comp {g : Y -> Z} {f : α -> Y} (hg : DenseRa
nge g) (hf : DenseRange f) (cg : Continuous g) : DenseRange (g ∘ f)
· 使用定理 `Function.Surjective.denseRange`：Function.Surjective.denseRange (hf : Fun
ction.Surjective f) : DenseRange f
· 使用定理 `SeparationQuotient.surjective_mk`：surjective_mk : Surjective (mk : X -> 
SeparationQuotient X)
· 使用定理 `CauchyFilter.denseRange_pureCauchy`：denseRange_pureCauchy : DenseRange (
pureCauchy : α -> CauchyFilter α)
· 使用定理 `SeparationQuotient.continuous_mk`：continuous_mk : Continuous (mk : X -> 
SeparationQuotient X)
-/
theorem denseRange_coe₂ :
    DenseRange fun x : α × β => ((x.1 : Completion α), (x.2 : Completion β)) :=
  denseRange_coe.prodMap denseRange_coe
/-
**UniformSpace.Completion.denseRange_coe** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace
.Completion`。
形式化陈述：denseRange_coe : DenseRange ((↑) : α -> Completion α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `DenseRange.comp`：DenseRange.comp {g : Y -> Z} {f : α -> Y} (hg : DenseRa
nge g) (hf : DenseRange f) (cg : Continuous g) : DenseRange (g ∘ f)
· 使用定理 `Function.Surjective.denseRange`：Function.Surjective.denseRange (hf : Fun
ction.Surjective f) : DenseRange f
· 使用定理 `SeparationQuotient.surjective_mk`：surjective_mk : Surjective (mk : X -> 
SeparationQuotient X)
· 使用定理 `CauchyFilter.denseRange_pureCauchy`：denseRange_pureCauchy : DenseRange (
pureCauchy : α -> CauchyFilter α)
· 使用定理 `SeparationQuotient.continuous_mk`：continuous_mk : Continuous (mk : X -> 
SeparationQuotient X)
-/
theorem denseRange_coe₃ :
    DenseRange fun x : α × β × γ =>
      ((x.1 : Completion α), ((x.2.1 : Completion β), (x.2.2 : Completion γ))) :=
  denseRange_coe.prodMap denseRange_coe₂

@[elab_as_elim]
/-
**UniformSpace.Completion.induction_on** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.C
ompletion`。
形式化陈述：induction_on {p : Completion α -> Prop} (a : Completion α) (hp : IsClosed 
{ a | p a }) (ih : forall a : α, p a) : p a
参数：a : Completion α；hp : IsClosed { a | p a }；ih : forall a : α, p a。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `isClosed_property`：isClosed_property [TopologicalSpace β] {e : α -> β} {
p : β -> Prop} (he : DenseRange e) (hp : IsClosed { x | p x }) (h : forall a, p 
(e a)) …
· 使用定理 `UniformSpace.Completion.denseRange_coe`：denseRange_coe : DenseRange ((↑)
 : α -> Completion α)
-/
theorem induction_on {p : Completion α → Prop} (a : Completion α) (hp : IsClosed { a | p a })
    (ih : ∀ a : α, p a) : p a :=
  isClosed_property denseRange_coe hp ih a

@[elab_as_elim]
/-
**UniformSpace.Completion.induction_on** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.C
ompletion`。
形式化陈述：induction_on {p : Completion α -> Prop} (a : Completion α) (hp : IsClosed 
{ a | p a }) (ih : forall a : α, p a) : p a
参数：a : Completion α；hp : IsClosed { a | p a }；ih : forall a : α, p a。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `isClosed_property`：isClosed_property [TopologicalSpace β] {e : α -> β} {
p : β -> Prop} (he : DenseRange e) (hp : IsClosed { x | p x }) (h : forall a, p 
(e a)) …
· 使用定理 `UniformSpace.Completion.denseRange_coe`：denseRange_coe : DenseRange ((↑)
 : α -> Completion α)
-/
theorem induction_on₂ {p : Completion α → Completion β → Prop} (a : Completion α) (b : Completion β)
    (hp : IsClosed { x : Completion α × Completion β | p x.1 x.2 })
    (ih : ∀ (a : α) (b : β), p a b) : p a b :=
  have : ∀ x : Completion α × Completion β, p x.1 x.2 :=
    isClosed_property denseRange_coe₂ hp fun ⟨a, b⟩ => ih a b
  this (a, b)

@[elab_as_elim]
/-
**UniformSpace.Completion.induction_on** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.C
ompletion`。
形式化陈述：induction_on {p : Completion α -> Prop} (a : Completion α) (hp : IsClosed 
{ a | p a }) (ih : forall a : α, p a) : p a
参数：a : Completion α；hp : IsClosed { a | p a }；ih : forall a : α, p a。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `isClosed_property`：isClosed_property [TopologicalSpace β] {e : α -> β} {
p : β -> Prop} (he : DenseRange e) (hp : IsClosed { x | p x }) (h : forall a, p 
(e a)) …
· 使用定理 `UniformSpace.Completion.denseRange_coe`：denseRange_coe : DenseRange ((↑)
 : α -> Completion α)
-/
theorem induction_on₃ {p : Completion α → Completion β → Completion γ → Prop} (a : Completion α)
    (b : Completion β) (c : Completion γ)
    (hp : IsClosed { x : Completion α × Completion β × Completion γ | p x.1 x.2.1 x.2.2 })
    (ih : ∀ (a : α) (b : β) (c : γ), p a b c) : p a b c :=
  have : ∀ x : Completion α × Completion β × Completion γ, p x.1 x.2.1 x.2.2 :=
    isClosed_property denseRange_coe₃ hp fun ⟨a, b, c⟩ => ih a b c
  this (a, b, c)
/-
**UniformSpace.Completion.ext** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.Completion
`。
形式化陈述：ext {Y : Type*} [TopologicalSpace Y] [T2Space Y] {f g : Completion α -> Y}
 (hf : Continuous f) (hg : Continuous g) (h : forall a : α, f a = g a) : f = g
参数：hf : Continuous f；hg : Continuous g；h : forall a : α, f a = g a。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.funext`：∀ {α : Type uα} [inst : UniformSpace α] (pkg 
: AbstractCompletion.{vα, uα} α) {β : Type uβ}   [inst_1 : TopologicalSpace β] [
T2Space β] {f g…
-/
theorem ext {Y : Type*} [TopologicalSpace Y] [T2Space Y] {f g : Completion α → Y}
    (hf : Continuous f) (hg : Continuous g) (h : ∀ a : α, f a = g a) : f = g :=
  cPkg.funext hf hg h
/-
**UniformSpace.Completion.ext'** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.Completio
n`。
形式化陈述：ext' {Y : Type*} [TopologicalSpace Y] [T2Space Y] {f g : Completion α -> Y
} (hf : Continuous f) (hg : Continuous g) (h : forall a : α, f a = g a) (a : Com
pletion α) : f a = g a
参数：hf : Continuous f；hg : Continuous g；h : forall a : α, f a = g a；a : Completio
n α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congr_fun`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, f = g 
→ ∀ (a : α), f a = g a
· 使用定理 `UniformSpace.Completion.ext`：ext {Y : Type*} [TopologicalSpace Y] [T2Spa
ce Y] {f g : Completion α -> Y} (hf : Continuous f) (hg : Continuous g) (h : for
all a : α, f a = …
-/
theorem ext' {Y : Type*} [TopologicalSpace Y] [T2Space Y] {f g : Completion α → Y}
    (hf : Continuous f) (hg : Continuous g) (h : ∀ a : α, f a = g a) (a : Completion α) :
    f a = g a :=
  congr_fun (ext hf hg h) a

section Extension

variable {f : α → β}

/-- "Extension" to the completion. It is defined for any map `f` but
returns an arbitrary constant value if `f` is not uniformly continuous -/
/-
**UniformSpace.Completion.extension** 是 Mathlib 中的一个定义，位于命名空间 `UniformSpace.Comp
letion`。
形式化陈述：{α : Type u_1} → [inst : UniformSpace α] → {β : Type u_2} → [UniformSpace 
β] → (α → β) → UniformSpace.Completion α → β
参数：α → β。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
"Extension" to the completion. It is defined for any map `f` but
returns an arbitrary constant value if `f` is not uniformly continuous
-/
protected def extension (f : α → β) : Completion α → β :=
  cPkg.extend f

section CompleteSpace

variable [CompleteSpace β]

@[fun_prop]
/-
**UniformSpace.Completion.uniformContinuous_extension** 是 Mathlib 中的一个定理，位于命名空间 
`UniformSpace.Completion`。
形式化陈述：uniformContinuous_extension : UniformContinuous (Completion.extension f)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.uniformContinuous_extend`：uniformContinuous_extend : 
UniformContinuous (pkg.extend f)
-/
theorem uniformContinuous_extension : UniformContinuous (Completion.extension f) :=
  cPkg.uniformContinuous_extend

@[continuity, fun_prop]
/-
**UniformSpace.Completion.continuous_extension** 是 Mathlib 中的一个定理，位于命名空间 `Unifor
mSpace.Completion`。
形式化陈述：continuous_extension : Continuous (Completion.extension f)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.continuous_extend`：continuous_extend : Continuous (pk
g.extend f)
-/
theorem continuous_extension : Continuous (Completion.extension f) :=
  cPkg.continuous_extend

end CompleteSpace

/-
**UniformSpace.Completion.extension_coe** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.
Completion`。
形式化陈述：extension_coe [T0Space β] (hf : UniformContinuous f) (a : α) : (Completion
.extension f) a = f a
参数：hf : UniformContinuous f；a : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.extend_coe`：extend_coe [T2Space β] (hf : UniformConti
nuous f) (a : α) : (pkg.extend f) (ι a) = f a
· 使用定理 `T25Space.t2Space`：∀ {X : Type u_1} [inst : TopologicalSpace X] [T25Space
 X], T2Space X
· 使用定理 `T3Space.t25Space`：∀ {X : Type u_1} [inst : TopologicalSpace X] [T3Space 
X], T25Space X
· 使用定理 `instT3Space`：∀ {X : Type u_1} [inst : TopologicalSpace X] [T0Space X] [R
egularSpace X], T3Space X
· 使用定理 `UniformSpace.to_regularSpace`：∀ {α : Type u} [inst : UniformSpace α], Re
gularSpace α
-/
theorem extension_coe [T0Space β] (hf : UniformContinuous f) (a : α) :
    (Completion.extension f) a = f a :=
  cPkg.extend_coe hf a
/-
**UniformSpace.Completion.inseparable_extension_coe** 是 Mathlib 中的一个定理，位于命名空间 `U
niformSpace.Completion`。
形式化陈述：inseparable_extension_coe (hf : UniformContinuous f) (x : α) : Inseparable
 (Completion.extension f x) (f x)
参数：hf : UniformContinuous f；x : α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.inseparable_extend_coe`：inseparable_extend_coe (hf : 
UniformContinuous f) (x : α) : Inseparable (pkg.extend f (ι x)) (f x)
-/
theorem inseparable_extension_coe (hf : UniformContinuous f) (x : α) :
    Inseparable (Completion.extension f x) (f x) :=
  cPkg.inseparable_extend_coe hf x
/-
**UniformSpace.Completion.isUniformInducing_extension** 是 Mathlib 中的一个引理，位于命名空间 
`UniformSpace.Completion`。
形式化陈述：isUniformInducing_extension [CompleteSpace β] (h : IsUniformInducing f) : 
IsUniformInducing (Completion.extension f)
参数：h : IsUniformInducing f。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `AbstractCompletion.isUniformInducing_extend`：isUniformInducing_extend (h
 : IsUniformInducing f) : IsUniformInducing (pkg.extend f)
-/
lemma isUniformInducing_extension [CompleteSpace β] (h : IsUniformInducing f) :
    IsUniformInducing (Completion.extension f) :=
  cPkg.isUniformInducing_extend h

variable [T0Space β] [CompleteSpace β]
/-
**UniformSpace.Completion.extension_unique** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpa
ce.Completion`。
形式化陈述：extension_unique (hf : UniformContinuous f) {g : Completion α -> β} (hg : 
UniformContinuous g) (h : forall a : α, f a = g (a : Completion α)) : Completion
.extension f = g
参数：hf : UniformContinuous f；hg : UniformContinuous g；h : forall a : α, f a = g (
a : Completion α)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.extend_unique`：extend_unique (hf : UniformContinuous 
f) {g : hatα -> β} (hg : UniformContinuous g) (h : forall a : α, f a = g (ι a)) 
: pkg.extend f = g
-/
theorem extension_unique (hf : UniformContinuous f) {g : Completion α → β}
    (hg : UniformContinuous g) (h : ∀ a : α, f a = g (a : Completion α)) :
    Completion.extension f = g :=
  cPkg.extend_unique hf hg h

@[simp]
/-
**UniformSpace.Completion.extension_comp_coe** 是 Mathlib 中的一个定理，位于命名空间 `UniformS
pace.Completion`。
形式化陈述：extension_comp_coe {f : Completion α -> β} (hf : UniformContinuous f) : Co
mpletion.extension (f ∘ (↑)) = f
参数：hf : UniformContinuous f。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.extend_comp_coe`：extend_comp_coe {f : hatα -> β} (hf 
: UniformContinuous f) : pkg.extend (f ∘ ι) = f
-/
theorem extension_comp_coe {f : Completion α → β} (hf : UniformContinuous f) :
    Completion.extension (f ∘ (↑)) = f :=
  cPkg.extend_comp_coe hf

end Extension

section Map

variable {f : α → β}

/-- Completion functor acting on morphisms -/
/-
**UniformSpace.Completion.map** 是 Mathlib 中的一个定义，位于命名空间 `UniformSpace.Completion
`。
形式化陈述：{α : Type u_1} →   [inst : UniformSpace α] →     {β : Type u_2} → [inst_1 
: UniformSpace β] → (α → β) → UniformSpace.Completion α → UniformSpace.Completio
n β
参数：α → β。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Completion functor acting on morphisms
-/
protected def map (f : α → β) : Completion α → Completion β :=
  cPkg.map cPkg f

@[fun_prop]
/-
**UniformSpace.Completion.uniformContinuous_map** 是 Mathlib 中的一个定理，位于命名空间 `Unifo
rmSpace.Completion`。
形式化陈述：uniformContinuous_map : UniformContinuous (Completion.map f)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.uniformContinuous_map`：uniformContinuous_map : Unifor
mContinuous (map f)
-/
theorem uniformContinuous_map : UniformContinuous (Completion.map f) :=
  cPkg.uniformContinuous_map cPkg f

@[continuity, fun_prop]
/-
**UniformSpace.Completion.continuous_map** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace
.Completion`。
形式化陈述：continuous_map : Continuous (Completion.map f)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.continuous_map`：continuous_map : Continuous (map f)
-/
theorem continuous_map : Continuous (Completion.map f) :=
  cPkg.continuous_map cPkg f
/-
**UniformSpace.Completion.map_coe** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.Comple
tion`。
形式化陈述：map_coe (hf : UniformContinuous f) (a : α) : (Completion.map f) a = f a
参数：hf : UniformContinuous f；a : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.map_coe`：map_coe (hf : UniformContinuous f) (a : α) :
 map f (ι a) = ι' (f a)
-/
theorem map_coe (hf : UniformContinuous f) (a : α) : (Completion.map f) a = f a :=
  cPkg.map_coe cPkg hf a
/-
**UniformSpace.Completion.map_unique** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.Com
pletion`。
形式化陈述：map_unique {f : α -> β} {g : Completion α -> Completion β} (hg : UniformCo
ntinuous g) (h : forall a : α, ↑(f a) = g a) : Completion.map f = g
参数：hg : UniformContinuous g；h : forall a : α, ↑(f a) = g a。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.map_unique`：map_unique {f : α -> β} {g : hatα -> hatβ
} (hg : UniformContinuous g) (h : forall a, ι' (f a) = g (ι a)) : map f = g
-/
theorem map_unique {f : α → β} {g : Completion α → Completion β} (hg : UniformContinuous g)
    (h : ∀ a : α, ↑(f a) = g a) : Completion.map f = g :=
  cPkg.map_unique cPkg hg h

@[simp]
/-
**UniformSpace.Completion.map_id** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.Complet
ion`。
形式化陈述：map_id : Completion.map (@id α) = id
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.map_id`：map_id : pkg.map pkg id = id
-/
theorem map_id : Completion.map (@id α) = id :=
  cPkg.map_id
/-
**UniformSpace.Completion.extension_map** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.
Completion`。
形式化陈述：extension_map [CompleteSpace γ] [T0Space γ] {f : β -> γ} {g : α -> β} (hf 
: UniformContinuous f) (hg : UniformContinuous g) : Completion.extension f ∘ Com
pletion.map g = Completion.extension (f ∘ g)
参数：hf : UniformContinuous f；hg : UniformContinuous g。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `UniformSpace.Completion.ext`：ext {Y : Type*} [TopologicalSpace Y] [T2Spa
ce Y] {f g : Completion α -> Y} (hf : Continuous f) (hg : Continuous g) (h : for
all a : α, f a = …
· 使用定理 `T25Space.t2Space`：∀ {X : Type u_1} [inst : TopologicalSpace X] [T25Space
 X], T2Space X
· 使用定理 `T3Space.t25Space`：∀ {X : Type u_1} [inst : TopologicalSpace X] [T3Space 
X], T25Space X
· 使用定理 `instT3Space`：∀ {X : Type u_1} [inst : TopologicalSpace X] [T0Space X] [R
egularSpace X], T3Space X
· 使用定理 `UniformSpace.to_regularSpace`：∀ {α : Type u} [inst : UniformSpace α], Re
gularSpace α
· 使用定理 `Continuous.comp`：Continuous.comp {g : Y -> Z} (hg : Continuous g) (hf : 
Continuous f) : Continuous (g ∘ f)
· 使用定理 `UniformSpace.Completion.continuous_extension`：continuous_extension : Con
tinuous (Completion.extension f)
· 使用定理 `UniformSpace.Completion.continuous_map`：continuous_map : Continuous (Com
pletion.map f)
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `UniformSpace.Completion.map_coe`：map_coe (hf : UniformContinuous f) (a :
 α) : (Completion.map f) a = f a
· 使用定理 `UniformSpace.Completion.extension_coe`：extension_coe [T0Space β] (hf : U
niformContinuous f) (a : α) : (Completion.extension f) a = f a
· 使用定理 `UniformContinuous.comp`：∀ {α : Type ua} {β : Type ub} {γ : Type uc} [ins
t : UniformSpace α] [inst_1 : UniformSpace β] [inst_2 : UniformSpace γ]   {g : β
 → γ} {f : α…
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
-/
theorem extension_map [CompleteSpace γ] [T0Space γ] {f : β → γ} {g : α → β}
    (hf : UniformContinuous f) (hg : UniformContinuous g) :
    Completion.extension f ∘ Completion.map g = Completion.extension (f ∘ g) :=
  Completion.ext (continuous_extension.comp continuous_map) continuous_extension <| by
    simp [hf, hg, hf.comp hg, map_coe, extension_coe]

set_option backward.isDefEq.respectTransparency false in
/-
**UniformSpace.Completion.map_comp** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.Compl
etion`。
形式化陈述：map_comp {g : β -> γ} {f : α -> β} (hg : UniformContinuous g) (hf : Unifor
mContinuous f) : Completion.map g ∘ Completion.map f = Completion.map (g ∘ f)
参数：hg : UniformContinuous g；hf : UniformContinuous f。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `UniformSpace.Completion.extension_map`：extension_map [CompleteSpace γ] [
T0Space γ] {f : β -> γ} {g : α -> β} (hf : UniformContinuous f) (hg : UniformCon
tinuous g) : Completion.ext…
· 使用定理 `UniformContinuous.comp`：∀ {α : Type ua} {β : Type ub} {γ : Type uc} [ins
t : UniformSpace α] [inst_1 : UniformSpace β] [inst_2 : UniformSpace γ]   {g : β
 → γ} {f : α…
· 使用定理 `UniformSpace.Completion.uniformContinuous_coe`：uniformContinuous_coe : U
niformContinuous ((↑) : α -> Completion α)
-/
theorem map_comp {g : β → γ} {f : α → β} (hg : UniformContinuous g) (hf : UniformContinuous f) :
    Completion.map g ∘ Completion.map f = Completion.map (g ∘ f) :=
  extension_map ((uniformContinuous_coe _).comp hg) hf

/-- The uniform isomorphism between two completions of isomorphic uniform spaces. -/
/-
**UniformSpace.Completion.mapEquiv** 是 Mathlib 中的一个定义，位于命名空间 `UniformSpace.Compl
etion`。
形式化陈述：mapEquiv (e : α ≃ᵤ β) : Completion α ≃ᵤ Completion β
参数：e : α ≃ᵤ β。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The uniform isomorphism between two completions of isomorphic uniform spaces.
-/
def mapEquiv (e : α ≃ᵤ β) : Completion α ≃ᵤ Completion β := cPkg.mapEquiv cPkg e

@[simp]
/-
**UniformSpace.Completion.mapEquiv_symm** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.
Completion`。
形式化陈述：mapEquiv_symm (e : α ≃ᵤ β) : (mapEquiv e).symm = mapEquiv e.symm
参数：e : α ≃ᵤ β。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.mapEquiv_symm`：mapEquiv_symm (e : α ≃ᵤ β) : (pkg.mapE
quiv pkg' e).symm = pkg'.mapEquiv pkg e.symm
-/
theorem mapEquiv_symm (e : α ≃ᵤ β) : (mapEquiv e).symm = mapEquiv e.symm :=
  cPkg.mapEquiv_symm cPkg e

@[simp]
/-
**UniformSpace.Completion.mapEquiv_coe** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.C
ompletion`。
形式化陈述：mapEquiv_coe (e : α ≃ᵤ β) (a : α) : mapEquiv e a = (e a)
参数：e : α ≃ᵤ β；a : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.mapEquiv_coe`：mapEquiv_coe (e : α ≃ᵤ β) (a : α) : pkg
.mapEquiv pkg' e (ι a) = ι' (e a)
-/
theorem mapEquiv_coe (e : α ≃ᵤ β) (a : α) : mapEquiv e a = (e a) := cPkg.mapEquiv_coe cPkg e a

end Map

/- In this section we construct isomorphisms between the completion of a uniform space and the
completion of its separation quotient -/
section SeparationQuotientCompletion

open SeparationQuotient in
/-- The isomorphism between the completion of a uniform space and the completion of its separation
quotient. -/
/-
**UniformSpace.Completion.completionSeparationQuotientEquiv** 是 Mathlib 中的一个定义，位
于命名空间 `UniformSpace.Completion`。
形式化陈述：completionSeparationQuotientEquiv (α : Type u) [UniformSpace α] : Completi
on (SeparationQuotient α) ≃ Completion α
参数：α : Type u。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The isomorphism between the completion of a uniform space and the completion of 
its separation
quotient.
-/
def completionSeparationQuotientEquiv (α : Type u) [UniformSpace α] :
    Completion (SeparationQuotient α) ≃ Completion α := by
  refine ⟨Completion.extension (lift' ((↑) : α → Completion α)),
    Completion.map SeparationQuotient.mk, fun a ↦ ?_, fun a ↦ ?_⟩
  · refine induction_on a (isClosed_eq (continuous_map.comp continuous_extension) continuous_id) ?_
    refine SeparationQuotient.surjective_mk.forall.2 fun a ↦ ?_
    rw [extension_coe (uniformContinuous_lift' _), lift'_mk (uniformContinuous_coe α),
      map_coe uniformContinuous_mk]
  · refine induction_on a
      (isClosed_eq (continuous_extension.comp continuous_map) continuous_id) fun a ↦ ?_
    rw [map_coe uniformContinuous_mk, extension_coe (uniformContinuous_lift' _),
      lift'_mk (uniformContinuous_coe _)]

@[fun_prop]
/-
**UniformSpace.Completion.uniformContinuous_completionSeparationQuotientEquiv** 
是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.Completion`。
形式化陈述：uniformContinuous_completionSeparationQuotientEquiv : UniformContinuous (c
ompletionSeparationQuotientEquiv α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `UniformSpace.Completion.uniformContinuous_extension`：uniformContinuous_e
xtension : UniformContinuous (Completion.extension f)
-/
theorem uniformContinuous_completionSeparationQuotientEquiv :
    UniformContinuous (completionSeparationQuotientEquiv α) :=
  uniformContinuous_extension

@[fun_prop]
/-
**UniformSpace.Completion.uniformContinuous_completionSeparationQuotientEquiv_sy
mm** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace.Completion`。
形式化陈述：uniformContinuous_completionSeparationQuotientEquiv_symm : UniformContinuo
us (completionSeparationQuotientEquiv α).symm
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `UniformSpace.Completion.uniformContinuous_map`：uniformContinuous_map : U
niformContinuous (Completion.map f)
-/
theorem uniformContinuous_completionSeparationQuotientEquiv_symm :
    UniformContinuous (completionSeparationQuotientEquiv α).symm :=
  uniformContinuous_map

end SeparationQuotientCompletion

section Extension₂

variable (f : α → β → γ)

open Function

/-- Extend a two variable map to the Hausdorff completions. -/
/-
**UniformSpace.Completion.extension** 是 Mathlib 中的一个定义，位于命名空间 `UniformSpace.Comp
letion`。
形式化陈述：{α : Type u_1} → [inst : UniformSpace α] → {β : Type u_2} → [UniformSpace 
β] → (α → β) → UniformSpace.Completion α → β
参数：α → β。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Extend a two variable map to the Hausdorff completions.
-/
protected def extension₂ (f : α → β → γ) : Completion α → Completion β → γ :=
  cPkg.extend₂ cPkg f

section T0Space

variable [T0Space γ] {f}

/-
**UniformSpace.Completion.extension** 是 Mathlib 中的一个定义，位于命名空间 `UniformSpace.Comp
letion`。
形式化陈述：{α : Type u_1} → [inst : UniformSpace α] → {β : Type u_2} → [UniformSpace 
β] → (α → β) → UniformSpace.Completion α → β
参数：α → β。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem extension₂_coe_coe (hf : UniformContinuous₂ f) (a : α) (b : β) :
    Completion.extension₂ f a b = f a b :=
  cPkg.extension₂_coe_coe cPkg hf a b

end T0Space

variable [CompleteSpace γ]

@[fun_prop]
/-
**UniformSpace.Completion.uniformContinuous_extension** 是 Mathlib 中的一个定理，位于命名空间 
`UniformSpace.Completion`。
形式化陈述：uniformContinuous_extension : UniformContinuous (Completion.extension f)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.uniformContinuous_extend`：uniformContinuous_extend : 
UniformContinuous (pkg.extend f)
-/
theorem uniformContinuous_extension₂ : UniformContinuous₂ (Completion.extension₂ f) :=
  cPkg.uniformContinuous_extension₂ cPkg f

end Extension₂

section Map₂

open Function

/-- Lift a two variable map to the Hausdorff completions. -/
/-
**UniformSpace.Completion.map** 是 Mathlib 中的一个定义，位于命名空间 `UniformSpace.Completion
`。
形式化陈述：{α : Type u_1} →   [inst : UniformSpace α] →     {β : Type u_2} → [inst_1 
: UniformSpace β] → (α → β) → UniformSpace.Completion α → UniformSpace.Completio
n β
参数：α → β。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Lift a two variable map to the Hausdorff completions.
-/
protected def map₂ (f : α → β → γ) : Completion α → Completion β → Completion γ :=
  cPkg.map₂ cPkg cPkg f

@[fun_prop]
/-
**UniformSpace.Completion.uniformContinuous_map** 是 Mathlib 中的一个定理，位于命名空间 `Unifo
rmSpace.Completion`。
形式化陈述：uniformContinuous_map : UniformContinuous (Completion.map f)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.uniformContinuous_map`：uniformContinuous_map : Unifor
mContinuous (map f)
-/
theorem uniformContinuous_map₂ (f : α → β → γ) : UniformContinuous₂ (Completion.map₂ f) :=
  cPkg.uniformContinuous_map₂ cPkg cPkg f
/-
**UniformSpace.Completion.continuous_map** 是 Mathlib 中的一个定理，位于命名空间 `UniformSpace
.Completion`。
形式化陈述：continuous_map : Continuous (Completion.map f)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AbstractCompletion.continuous_map`：continuous_map : Continuous (map f)
-/
theorem continuous_map₂ {δ} [TopologicalSpace δ] {f : α → β → γ} {a : δ → Completion α}
    {b : δ → Completion β} (ha : Continuous a) (hb : Continuous b) :
    Continuous fun d : δ => Completion.map₂ f (a d) (b d) :=
  cPkg.continuous_map₂ cPkg cPkg ha hb
/-
**UniformSpace.Completion.map** 是 Mathlib 中的一个定义，位于命名空间 `UniformSpace.Completion
`。
形式化陈述：{α : Type u_1} →   [inst : UniformSpace α] →     {β : Type u_2} → [inst_1 
: UniformSpace β] → (α → β) → UniformSpace.Completion α → UniformSpace.Completio
n β
参数：α → β。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem map₂_coe_coe (a : α) (b : β) (f : α → β → γ) (hf : UniformContinuous₂ f) :
    Completion.map₂ f (a : Completion α) (b : Completion β) = f a b :=
  cPkg.map₂_coe_coe cPkg cPkg a b f hf

end Map₂

end Completion

end UniformSpace

