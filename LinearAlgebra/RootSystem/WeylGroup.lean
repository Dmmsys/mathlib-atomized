/-
Copyright (c) 2024 Scott Carnahan. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Scott Carnahan
-/
module

public import Mathlib.LinearAlgebra.RootSystem.Hom
public import Mathlib.RepresentationTheory.Basic
public import Mathlib.Tactic.CrossRefAttribute

/-!
# The Weyl group of a root pairing

This file defines the Weyl group of a root pairing as the subgroup of automorphisms generated by
reflection automorphisms. This deviates from the existing literature, which typically defines the
Weyl group as the subgroup of linear transformations of the weight space generated by linear
reflections. However, the automorphism group of a root pairing comes with a permutation
representation on the set indexing roots and faithful linear representations on the weight space and
coweight space. Thus, our formalism gives us an isomorphism to the traditional Weyl group together
with the natural dual representation generated by coreflections and the permutation representation
on roots.

## Main definitions
* `RootPairing.weylGroup` : The group of automorphisms generated by reflections.
* `RootPairing.weylGroupToPerm` : The permutation representation of the Weyl group on roots.

## Results
* `RootPairing.range_weylGroup_weightHom` : The image of the weight space representation is equal
  to the subgroup generated by linear reflections.
* `RootPairing.range_weylGroup_coweightHom` : The image of the coweight space representation is
  equal to the subgroup generated by linear coreflections.
* `RootPairing.range_weylGroupToPerm` : The image of the permutation representation is equal to the
  subgroup generated by reflection permutations.

## TODO
* faithfulness of `weylGroupToPerm` when multiplication by 2 is injective on the weight space.
-/

@[expose] public section

open Set Function

variable {ι R M N : Type*}

noncomputable section

namespace RootPairing

variable [CommRing R] [AddCommGroup M] [Module R M] [AddCommGroup N] [Module R N]
  (P : RootPairing ι R M N) (i : ι)

/-- The `Weyl group` of a root pairing is the group of automorphisms of the root pairing generated
by reflections. -/
@[wikidata Q768074]
/--
Definition of `weylGroup` / `weylGroup` 的定义

English:
definition weylGroup
  signature: : Subgroup (Aut P)
  body: Subgroup.closure (range (Equiv.reflection P))

中文:
定义 weylGroup
  签名: : 子群 (Aut P)
  定义体: Subgroup.closure (range (Equiv.reflection P))

Depends on / 依赖: Equiv.reflection, Subgroup, Subgroup.closure, closure, reflection
-/
def weylGroup : Subgroup (Aut P) :=
  Subgroup.closure (range (Equiv.reflection P))

/--
lemma `reflection_mem_weylGroup` / 引理 `reflection_mem_weylGroup`

English:
lemma reflection_mem_weylGroup
  statement: Equiv.reflection P i in P.weylGroup
  proof: Subgroup.subset_closure mem_range_self i

中文:
引理 reflection_mem_weylGroup
  结论: 等价.reflection P i in P.weylGroup
  证明: Subgroup.subset_closure mem_range_self i

Depends on / 依赖: Subgroup, Subgroup.subset_closure, mem_range_self, subset_closure
-/
lemma reflection_mem_weylGroup : Equiv.reflection P i in P.weylGroup :=
Subgroup.subset_closure mem_range_self i

/--
Definition of `weylGroup.ofIdx` / `weylGroup.ofIdx` 的定义

English:
definition weylGroup.ofIdx
  signature: (i : ι)
  body: ⟨_, P.reflection_mem_weylGroup i⟩

中文:
定义 weylGroup.ofIdx
  签名: (i : ι)
  定义体: ⟨_, P.reflection_mem_weylGroup i⟩

Depends on / 依赖: P.reflection_mem_weylGroup, reflection_mem_weylGroup
-/
def weylGroup.ofIdx (i : ι) : P.weylGroup := ⟨_, P.reflection_mem_weylGroup i⟩

/--
lemma `weylGroup.ofIdx_smul` / 引理 `weylGroup.ofIdx_smul`

English:
lemma weylGroup.ofIdx_smul
  given: (i : ι) (m : M)
  proof: rfl

中文:
引理 weylGroup.ofIdx_smul
  条件: (i : ι) (m : M)
  证明: rfl
-/
@[simp] lemma weylGroup.ofIdx_smul (i : ι) (m : M) :
    weylGroup.ofIdx P i • m = Equiv.reflection P i • m :=
  rfl

/--
lemma `weylGroup_toSubmonoid` / 引理 `weylGroup_toSubmonoid`

English:
lemma weylGroup_toSubmonoid
  proof: by
  suffices range (Equiv.reflection P) = (range (Equiv.reflection P))⁻¹ by
    rw [weylGroup]; rw [Subgroup.closure_toSubmonoid]; rw [← this]; rw [union_self]
  ext; simp [← inv_eq_iff_eq_inv]

@[elab_as_elim]

中文:
引理 weylGroup_toSubmonoid
  证明: by
  suffices range (Equiv.reflection P) = (range (Equiv.reflection P))⁻¹ by
    rw [weylGroup]; rw [Subgroup.closure_toSubmonoid]; rw [← this]; rw [union_self]
  ext; simp [← inv_eq_iff_eq_inv]

@[elab_as_elim]

Depends on / 依赖: Equiv.reflection, Subgroup, Subgroup.closure_toSubmonoid, closure_toSubmonoid, inv_eq_iff_eq_inv, reflection, union_self, weylGroup
-/
lemma weylGroup_toSubmonoid :
    P.weylGroup.toSubmonoid = Submonoid.closure (range (Equiv.reflection P)) := by
  suffices range (Equiv.reflection P) = (range (Equiv.reflection P))⁻¹ by
    rw [weylGroup]; rw [Subgroup.closure_toSubmonoid]; rw [← this]; rw [union_self]
  ext; simp [← inv_eq_iff_eq_inv]

@[elab_as_elim]
/--
lemma `weylGroup.induction` / 引理 `weylGroup.induction`

English:
lemma weylGroup.induction
  statement: {pred : (g : Aut P) -> g in P.weylGroup -> Prop}
  proof: by
  let pred' : (g : Aut P) -> g in Submonoid.closure (range (Equiv.reflection P)) -> Prop :=
fun g hg => pred g by change g in P.weylGroup.toSubmonoid; rwa [weylGroup_toSubmonoid]
  have hx' : x in Submonoid.closure (range (Equiv.reflection P)) := by rwa [← weylGroup_toSubmonoid]
  suffices pred' x hx' from this
  apply Submonoid.closure_induction
  · rintro - ⟨i, rfl⟩
    exact mem i
  · exact one
  · intro x y hx hy hx' hy'
    rw [← weylGroup_toSubmonoid] at hx hy
    exact mul x y hx hy hx' hy'

@[elab_as_elim]

中文:
引理 weylGroup.induction
  结论: {pred : (g : Aut P) -> g in P.weylGroup -> 命题}
  证明: by
  let pred' : (g : Aut P) -> g in Submonoid.closure (range (Equiv.reflection P)) -> Prop :=
fun g hg => pred g by change g in P.weylGroup.toSubmonoid; rwa [weylGroup_toSubmonoid]
  have hx' : x in Submonoid.closure (range (Equiv.reflection P)) := by rwa [← weylGroup_toSubmonoid]
  suffices pred' x hx' from this
  apply Submonoid.closure_induction
  · rintro - ⟨i, rfl⟩
    exact mem i
  · exact one
  · intro x y hx hy hx' hy'
    rw [← weylGroup_toSubmonoid] at hx hy
    exact mul x y hx hy hx' hy'

@[elab_as_elim]

Depends on / 依赖: Equiv.reflection, P.weylGroup.toSubmonoid, Submonoid, Submonoid.closure, Submonoid.closure_induction, closure, closure_induction, reflection, toSubmonoid, weylGroup, weylGroup_toSubmonoid
-/
lemma weylGroup.induction {pred : (g : Aut P) -> g in P.weylGroup -> Prop}
    (mem : forall i, pred (Equiv.reflection P i) (P.reflection_mem_weylGroup i))
    (one : pred 1 (one_mem _))
    (mul : forall x y hx hy, pred x hx -> pred y hy -> pred (x * y) (mul_mem hx hy))
    {x} (hx : x in P.weylGroup) :
    pred x hx := by
  let pred' : (g : Aut P) -> g in Submonoid.closure (range (Equiv.reflection P)) -> Prop :=
fun g hg => pred g by change g in P.weylGroup.toSubmonoid; rwa [weylGroup_toSubmonoid]
  have hx' : x in Submonoid.closure (range (Equiv.reflection P)) := by rwa [← weylGroup_toSubmonoid]
  suffices pred' x hx' from this
  apply Submonoid.closure_induction
  · rintro - ⟨i, rfl⟩
    exact mem i
  · exact one
  · intro x y hx hy hx' hy'
    rw [← weylGroup_toSubmonoid] at hx hy
    exact mul x y hx hy hx' hy'

@[elab_as_elim]
/--
lemma `weylGroup.induction'` / 引理 `weylGroup.induction'`

English:
lemma weylGroup.induction'
  statement: [Nonempty ι] {pred : (g : Aut P) -> g in P.weylGroup -> Prop}
  proof: by
  refine weylGroup.induction P mem ?_ mul hx
  obtain ⟨i⟩ : Nonempty ι := inferInstance
  have : (Equiv.reflection P i) ^ 2 = 1 := by
    rw [sq]; rw [mul_eq_one_iff_inv_eq]; rw [Equiv.reflection_inv P i]
  simpa [sq, ← this] using mul _ _ _ _ (mem i) (mem i)

中文:
引理 weylGroup.induction'
  结论: [非空 ι] {pred : (g : Aut P) -> g in P.weylGroup -> 命题}
  证明: by
  refine weylGroup.induction P mem ?_ mul hx
  obtain ⟨i⟩ : Nonempty ι := inferInstance
  have : (Equiv.reflection P i) ^ 2 = 1 := by
    rw [sq]; rw [mul_eq_one_iff_inv_eq]; rw [Equiv.reflection_inv P i]
  simpa [sq, ← this] using mul _ _ _ _ (mem i) (mem i)

Depends on / 依赖: Equiv.reflection, Equiv.reflection_inv, Nonempty, mul_eq_one_iff_inv_eq, reflection, reflection_inv, weylGroup, weylGroup.induction
-/
lemma weylGroup.induction' [Nonempty ι] {pred : (g : Aut P) -> g in P.weylGroup -> Prop}
    (mem : forall i, pred (Equiv.reflection P i) (P.reflection_mem_weylGroup i))
    (mul : forall x y hx hy, pred x hx -> pred y hy -> pred (x * y) (mul_mem hx hy))
    {x} (hx : x in P.weylGroup) :
    pred x hx := by
  refine weylGroup.induction P mem ?_ mul hx
  obtain ⟨i⟩ : Nonempty ι := inferInstance
  have : (Equiv.reflection P i) ^ 2 = 1 := by
    rw [sq]; rw [mul_eq_one_iff_inv_eq]; rw [Equiv.reflection_inv P i]
  simpa [sq, ← this] using mul _ _ _ _ (mem i) (mem i)

set_option backward.isDefEq.respectTransparency false in
/--
lemma `range_weylGroup_weightHom` / 引理 `range_weylGroup_weightHom`

English:
lemma range_weylGroup_weightHom
  proof: by
  refine (Subgroup.closure_eq_of_le _ ?_ ?_).symm
  · rintro - ⟨i, rfl⟩
    simp only [MonoidHom.domRestrict_range, Subgroup.coe_map, Equiv.weightHom_apply, mem_image,
      SetLike.mem_coe]
    use Equiv.reflection P i
    exact ⟨reflection_mem_weylGroup P i, Equiv.reflection_weightEquiv P i⟩
  · rintro fg ⟨⟨w, hw⟩, rfl⟩
    induction hw using Subgroup.closure_induction'' with
    | one =>
      change ((Equiv.weightHom P).domRestrict P.weylGroup) 1 in _
      simp only [map_one, one_mem]
    | mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [MonoidHom.domRestrict_apply, Equiv.weightHom_apply, Equiv.reflection_weightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | inv_mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [Equiv.reflection_inv, MonoidHom.domRestrict_apply, Equiv.weightHom_apply,
        Equiv.reflection_weightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | mul w₁ w₂ hw₁ hw₂ h₁ h₂ =>
      simpa only [← Submonoid.mk_mul_mk _ w₁ w₂ hw₁ hw₂, map_mul] using! Subgroup.mul_mem _ h₁ h₂

中文:
引理 range_weylGroup_weightHom
  证明: by
  refine (Subgroup.closure_eq_of_le _ ?_ ?_).symm
  · rintro - ⟨i, rfl⟩
    simp only [MonoidHom.domRestrict_range, Subgroup.coe_map, Equiv.weightHom_apply, mem_image,
      SetLike.mem_coe]
    use Equiv.reflection P i
    exact ⟨reflection_mem_weylGroup P i, Equiv.reflection_weightEquiv P i⟩
  · rintro fg ⟨⟨w, hw⟩, rfl⟩
    induction hw using Subgroup.closure_induction'' with
    | one =>
      change ((Equiv.weightHom P).domRestrict P.weylGroup) 1 in _
      simp only [map_one, one_mem]
    | mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [MonoidHom.domRestrict_apply, Equiv.weightHom_apply, Equiv.reflection_weightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | inv_mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [Equiv.reflection_inv, MonoidHom.domRestrict_apply, Equiv.weightHom_apply,
        Equiv.reflection_weightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | mul w₁ w₂ hw₁ hw₂ h₁ h₂ =>
      simpa only [← Submonoid.mk_mul_mk _ w₁ w₂ hw₁ hw₂, map_mul] using! Subgroup.mul_mem _ h₁ h₂

Depends on / 依赖: Equiv.reflection, Equiv.reflection_weightEquiv, Equiv.weightHom, Equiv.weightHom_apply, MonoidHom, MonoidHom.domRestrict_range, P.weylGroup, SetLike, SetLike.mem_coe, Subgroup, Subgroup.closure_eq_of_le, Subgroup.closure_induction, Subgroup.coe_map, closure_eq_of_le, closure_induction, coe_map, domRestrict, domRestrict_range, map_one, mem_coe
-/
lemma range_weylGroup_weightHom :
    MonoidHom.range ((Equiv.weightHom P).domRestrict P.weylGroup) =
      Subgroup.closure (range P.reflection) := by
  refine (Subgroup.closure_eq_of_le _ ?_ ?_).symm
  · rintro - ⟨i, rfl⟩
    simp only [MonoidHom.domRestrict_range, Subgroup.coe_map, Equiv.weightHom_apply, mem_image,
      SetLike.mem_coe]
    use Equiv.reflection P i
    exact ⟨reflection_mem_weylGroup P i, Equiv.reflection_weightEquiv P i⟩
  · rintro fg ⟨⟨w, hw⟩, rfl⟩
    induction hw using Subgroup.closure_induction'' with
    | one =>
      change ((Equiv.weightHom P).domRestrict P.weylGroup) 1 in _
      simp only [map_one, one_mem]
    | mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [MonoidHom.domRestrict_apply, Equiv.weightHom_apply, Equiv.reflection_weightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | inv_mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [Equiv.reflection_inv, MonoidHom.domRestrict_apply, Equiv.weightHom_apply,
        Equiv.reflection_weightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | mul w₁ w₂ hw₁ hw₂ h₁ h₂ =>
      simpa only [← Submonoid.mk_mul_mk _ w₁ w₂ hw₁ hw₂, map_mul] using! Subgroup.mul_mem _ h₁ h₂

set_option backward.isDefEq.respectTransparency false in
/--
lemma `range_weylGroup_coweightHom` / 引理 `range_weylGroup_coweightHom`

English:
lemma range_weylGroup_coweightHom
  proof: by
  refine (Subgroup.closure_eq_of_le _ ?_ ?_).symm
  · rintro - ⟨i, rfl⟩
    simp only [MonoidHom.domRestrict_range, Subgroup.coe_map, mem_image,
      SetLike.mem_coe]
    use Equiv.reflection P i
    refine ⟨reflection_mem_weylGroup P i, by simp⟩
  · rintro fg ⟨⟨w, hw⟩, rfl⟩
    induction hw using Subgroup.closure_induction'' with
    | one =>
      change ((Equiv.coweightHom P).domRestrict P.weylGroup) 1 in _
      simp only [map_one, one_mem]
    | mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [MonoidHom.domRestrict_apply, Equiv.coweightHom_apply,
        Equiv.reflection_coweightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | inv_mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [Equiv.reflection_inv, MonoidHom.domRestrict_apply, Equiv.coweightHom_apply,
        Equiv.reflection_coweightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | mul w₁ w₂ hw₁ hw₂ h₁ h₂ =>
      simpa only [← Submonoid.mk_mul_mk _ w₁ w₂ hw₁ hw₂, map_mul] using! Subgroup.mul_mem _ h₁ h₂

中文:
引理 range_weylGroup_coweightHom
  证明: by
  refine (Subgroup.closure_eq_of_le _ ?_ ?_).symm
  · rintro - ⟨i, rfl⟩
    simp only [MonoidHom.domRestrict_range, Subgroup.coe_map, mem_image,
      SetLike.mem_coe]
    use Equiv.reflection P i
    refine ⟨reflection_mem_weylGroup P i, by simp⟩
  · rintro fg ⟨⟨w, hw⟩, rfl⟩
    induction hw using Subgroup.closure_induction'' with
    | one =>
      change ((Equiv.coweightHom P).domRestrict P.weylGroup) 1 in _
      simp only [map_one, one_mem]
    | mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [MonoidHom.domRestrict_apply, Equiv.coweightHom_apply,
        Equiv.reflection_coweightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | inv_mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [Equiv.reflection_inv, MonoidHom.domRestrict_apply, Equiv.coweightHom_apply,
        Equiv.reflection_coweightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | mul w₁ w₂ hw₁ hw₂ h₁ h₂ =>
      simpa only [← Submonoid.mk_mul_mk _ w₁ w₂ hw₁ hw₂, map_mul] using! Subgroup.mul_mem _ h₁ h₂

Depends on / 依赖: Equiv.coweig, Equiv.coweightHom, Equiv.reflection, MonoidHom, MonoidHom.domRestrict_apply, MonoidHom.domRestrict_range, P.weylGroup, SetLike, SetLike.mem_coe, Subgroup, Subgroup.closure_eq_of_le, Subgroup.closure_induction, Subgroup.coe_map, closure_eq_of_le, closure_induction, coe_map, coweig, coweightHom, domRestrict, domRestrict_apply
-/
lemma range_weylGroup_coweightHom :
    MonoidHom.range ((Equiv.coweightHom P).domRestrict P.weylGroup) =
      Subgroup.closure (range (MulOpposite.op ∘ P.coreflection)) := by
  refine (Subgroup.closure_eq_of_le _ ?_ ?_).symm
  · rintro - ⟨i, rfl⟩
    simp only [MonoidHom.domRestrict_range, Subgroup.coe_map, mem_image,
      SetLike.mem_coe]
    use Equiv.reflection P i
    refine ⟨reflection_mem_weylGroup P i, by simp⟩
  · rintro fg ⟨⟨w, hw⟩, rfl⟩
    induction hw using Subgroup.closure_induction'' with
    | one =>
      change ((Equiv.coweightHom P).domRestrict P.weylGroup) 1 in _
      simp only [map_one, one_mem]
    | mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [MonoidHom.domRestrict_apply, Equiv.coweightHom_apply,
        Equiv.reflection_coweightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | inv_mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [Equiv.reflection_inv, MonoidHom.domRestrict_apply, Equiv.coweightHom_apply,
        Equiv.reflection_coweightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | mul w₁ w₂ hw₁ hw₂ h₁ h₂ =>
      simpa only [← Submonoid.mk_mul_mk _ w₁ w₂ hw₁ hw₂, map_mul] using! Subgroup.mul_mem _ h₁ h₂

/--
Definition of `weylGroupToPerm` / `weylGroupToPerm` 的定义

English:
abbreviation weylGroupToPerm
  body: (Equiv.indexHom P).domRestrict P.weylGroup

中文:
缩写 weylGroupToPerm
  定义体: (Equiv.indexHom P).domRestrict P.weylGroup

Depends on / 依赖: Equiv.indexHom, P.weylGroup, domRestrict, indexHom, weylGroup
-/
abbrev weylGroupToPerm := (Equiv.indexHom P).domRestrict P.weylGroup

/--
lemma `range_weylGroupToPerm` / 引理 `range_weylGroupToPerm`

English:
lemma range_weylGroupToPerm
  proof: by
  refine (Subgroup.closure_eq_of_le _ ?_ ?_).symm
  · rintro - ⟨i, rfl⟩
    simp only [MonoidHom.domRestrict_range, Subgroup.coe_map, mem_image, SetLike.mem_coe]
    use Equiv.reflection P i
    refine ⟨reflection_mem_weylGroup P i, by simp⟩
  · rintro fg ⟨⟨w, hw⟩, rfl⟩
    induction hw using Subgroup.closure_induction'' with
    | one =>
      change ((Equiv.indexHom P).domRestrict P.weylGroup) 1 in _
      simp only [map_one, one_mem]
    | mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [MonoidHom.domRestrict_apply, Equiv.indexHom_apply, Equiv.reflection_indexEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | inv_mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [Equiv.reflection_inv, MonoidHom.domRestrict_apply, Equiv.indexHom_apply,
        Equiv.reflection_indexEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | mul w₁ w₂ hw₁ hw₂ h₁ h₂ =>
      simpa only [← Submonoid.mk_mul_mk _ w₁ w₂ hw₁ hw₂, map_mul] using! Subgroup.mul_mem _ h₁ h₂

中文:
引理 range_weylGroupToPerm
  证明: by
  refine (Subgroup.closure_eq_of_le _ ?_ ?_).symm
  · rintro - ⟨i, rfl⟩
    simp only [MonoidHom.domRestrict_range, Subgroup.coe_map, mem_image, SetLike.mem_coe]
    use Equiv.reflection P i
    refine ⟨reflection_mem_weylGroup P i, by simp⟩
  · rintro fg ⟨⟨w, hw⟩, rfl⟩
    induction hw using Subgroup.closure_induction'' with
    | one =>
      change ((Equiv.indexHom P).domRestrict P.weylGroup) 1 in _
      simp only [map_one, one_mem]
    | mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [MonoidHom.domRestrict_apply, Equiv.indexHom_apply, Equiv.reflection_indexEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | inv_mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [Equiv.reflection_inv, MonoidHom.domRestrict_apply, Equiv.indexHom_apply,
        Equiv.reflection_indexEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | mul w₁ w₂ hw₁ hw₂ h₁ h₂ =>
      simpa only [← Submonoid.mk_mul_mk _ w₁ w₂ hw₁ hw₂, map_mul] using! Subgroup.mul_mem _ h₁ h₂

Depends on / 依赖: Equiv.indexHom, Equiv.indexHom_, Equiv.reflection, MonoidHom, MonoidHom.domRestrict_apply, MonoidHom.domRestrict_range, P.weylGroup, SetLike, SetLike.mem_coe, Subgroup, Subgroup.closure_eq_of_le, Subgroup.closure_induction, Subgroup.coe_map, closure_eq_of_le, closure_induction, coe_map, domRestrict, domRestrict_apply, domRestrict_range, indexHom
-/
lemma range_weylGroupToPerm :
    P.weylGroupToPerm.range = Subgroup.closure (range P.reflectionPerm) := by
  refine (Subgroup.closure_eq_of_le _ ?_ ?_).symm
  · rintro - ⟨i, rfl⟩
    simp only [MonoidHom.domRestrict_range, Subgroup.coe_map, mem_image, SetLike.mem_coe]
    use Equiv.reflection P i
    refine ⟨reflection_mem_weylGroup P i, by simp⟩
  · rintro fg ⟨⟨w, hw⟩, rfl⟩
    induction hw using Subgroup.closure_induction'' with
    | one =>
      change ((Equiv.indexHom P).domRestrict P.weylGroup) 1 in _
      simp only [map_one, one_mem]
    | mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [MonoidHom.domRestrict_apply, Equiv.indexHom_apply, Equiv.reflection_indexEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | inv_mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [Equiv.reflection_inv, MonoidHom.domRestrict_apply, Equiv.indexHom_apply,
        Equiv.reflection_indexEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | mul w₁ w₂ hw₁ hw₂ h₁ h₂ =>
      simpa only [← Submonoid.mk_mul_mk _ w₁ w₂ hw₁ hw₂, map_mul] using! Subgroup.mul_mem _ h₁ h₂

/--
Definition of `weylGroupRootRep` / `weylGroupRootRep` 的定义

English:
definition weylGroupRootRep
  signature: : Representation R P.weylGroup M
  body: Representation.ofDistribMulAction R P.weylGroup M

中文:
定义 weylGroupRootRep
  签名: : Representation R P.weylGroup M
  定义体: Representation.ofDistribMulAction R P.weylGroup M

Depends on / 依赖: P.weylGroup, Representation, Representation.ofDistribMulAction, ofDistribMulAction, weylGroup
-/
def weylGroupRootRep : Representation R P.weylGroup M :=
  Representation.ofDistribMulAction R P.weylGroup M

/--
Definition of `weylGroupCorootRep` / `weylGroupCorootRep` 的定义

English:
definition weylGroupCorootRep
  signature: : Representation R P.weylGroup.op N
  body: Representation.ofDistribMulAction R P.weylGroup.op N

中文:
定义 weylGroupCorootRep
  签名: : Representation R P.weylGroup.op N
  定义体: Representation.ofDistribMulAction R P.weylGroup.op N

Depends on / 依赖: P.weylGroup.op, Representation, Representation.ofDistribMulAction, ofDistribMulAction, weylGroup
-/
def weylGroupCorootRep : Representation R P.weylGroup.op N :=
  Representation.ofDistribMulAction R P.weylGroup.op N

/--
lemma `weylGroup_apply_root` / 引理 `weylGroup_apply_root`

English:
lemma weylGroup_apply_root
  given: (g : P.weylGroup) (i : ι)
  proof: Hom.root_weightMap_apply _ _ _ _

@[simp]

中文:
引理 weylGroup_apply_root
  条件: (g : P.weylGroup) (i : ι)
  证明: Hom.root_weightMap_apply _ _ _ _

@[simp]

Depends on / 依赖: Hom.root_weightMap_apply, root_weightMap_apply
-/
lemma weylGroup_apply_root (g : P.weylGroup) (i : ι) :
    g • P.root i = P.root (P.weylGroupToPerm g i) :=
  Hom.root_weightMap_apply _ _ _ _

@[simp]
/--
lemma `InvariantForm.apply_weylGroup_smul` / 引理 `InvariantForm.apply_weylGroup_smul`

English:
lemma InvariantForm.apply_weylGroup_smul
  given: {B : P.InvariantForm} (g : P.weylGroup) (x y : M)
  proof: by
  revert x y
  obtain ⟨g, hg⟩ := g
  induction hg using weylGroup.induction with
  | mem i => simp
  | one => simp
  | mul g₁ g₂ hg₁ hg₂ hg₁' hg₂' =>
    intro x y
    rw [← Submonoid.mk_mul_mk _ _ _ hg₁ hg₂]; rw [mul_smul]; rw [mul_smul]; rw [hg₁']; rw [hg₂']

中文:
引理 不变形式.apply_weylGroup_smul
  条件: {B : P.不变形式} (g : P.weylGroup) (x y : M)
  证明: by
  revert x y
  obtain ⟨g, hg⟩ := g
  induction hg using weylGroup.induction with
  | mem i => simp
  | one => simp
  | mul g₁ g₂ hg₁ hg₂ hg₁' hg₂' =>
    intro x y
    rw [← Submonoid.mk_mul_mk _ _ _ hg₁ hg₂]; rw [mul_smul]; rw [mul_smul]; rw [hg₁']; rw [hg₂']

Depends on / 依赖: Submonoid, Submonoid.mk_mul_mk, mk_mul_mk, mul_smul, revert, weylGroup, weylGroup.induction
-/
lemma InvariantForm.apply_weylGroup_smul {B : P.InvariantForm} (g : P.weylGroup) (x y : M) :
    B.form (g • x) (g • y) = B.form x y := by
  revert x y
  obtain ⟨g, hg⟩ := g
  induction hg using weylGroup.induction with
  | mem i => simp
  | one => simp
  | mul g₁ g₂ hg₁ hg₂ hg₁' hg₂' =>
    intro x y
    rw [← Submonoid.mk_mul_mk _ _ _ hg₁ hg₂]; rw [mul_smul]; rw [mul_smul]; rw [hg₁']; rw [hg₂']

end RootPairing
