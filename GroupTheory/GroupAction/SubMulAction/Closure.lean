/-
Copyright (c) 2025 Dexin Zhang. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Dexin Zhang
-/
module

public import Mathlib.Data.Set.Finite.Basic
public import Mathlib.Data.Set.Lattice
public import Mathlib.GroupTheory.GroupAction.SubMulAction

/-!
# Closure and finiteness of `SubMulAction` and `SubAddAction`
-/

@[expose] public section

namespace SubMulAction

/-- The `SubMulAction` generated by a set `s`. -/
@[to_additive /-- The `SubAddAction` generated by a set `s`. -/]
/-
**SubMulAction.closure** 是 Mathlib 中的一个定义，位于命名空间 `SubMulAction`。
形式化陈述：closure (R : Type*) {M : Type*} [SMul R M] (s : Set M) : SubMulAction R M
参数：R : Type*；s : Set M。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The `SubMulAction` generated by a set `s`.
-/
def closure (R : Type*) {M : Type*} [SMul R M] (s : Set M) : SubMulAction R M :=
  sInf { p | s ⊆ (p : Set M) }

variable {R M : Type*} [SMul R M] {s t : Set M} {x : M}

@[to_additive]
/-
**SubMulAction.mem_closure** 是 Mathlib 中的一个定理，位于命名空间 `SubMulAction`。
形式化陈述：mem_closure : x in closure R s ↔ forall p : SubMulAction R M, s subseteq p
 -> x in p
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.mem_iInter₂`：mem_iInter₂ {x : γ} {s : forall i, κ i -> Set γ} : (x i
n ⋂ (i) (j), s i j) ↔ forall i j, x in s i j
-/
theorem mem_closure : x ∈ closure R s ↔ ∀ p : SubMulAction R M, s ⊆ p → x ∈ p :=
  Set.mem_iInter₂

@[to_additive]
/-
**SubMulAction.subset_closure** 是 Mathlib 中的一个定理，位于命名空间 `SubMulAction`。
形式化陈述：subset_closure : s subseteq closure R s
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `SubMulAction.mem_closure`：mem_closure : x in closure R s ↔ forall p : Su
bMulAction R M, s subseteq p -> x in p
-/
theorem subset_closure : s ⊆ closure R s := fun _ h => mem_closure.2 fun _ hp => hp h

@[to_additive]
/-
**SubMulAction.mem_closure_of_mem** 是 Mathlib 中的一个定理，位于命名空间 `SubMulAction`。
形式化陈述：mem_closure_of_mem (hx : x in s) : x in closure R s
参数：hx : x in s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `SubMulAction.subset_closure`：subset_closure : s subseteq closure R s
-/
theorem mem_closure_of_mem (hx : x ∈ s) : x ∈ closure R s := subset_closure hx

@[to_additive]
/-
**SubMulAction.closure_le** 是 Mathlib 中的一个定理，位于命名空间 `SubMulAction`。
形式化陈述：closure_le {p} : closure R s <= p ↔ s subseteq p
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用定理 `SubMulAction.subset_closure`：subset_closure : s subseteq closure R s
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `SubMulAction.mem_closure`：mem_closure : x in closure R s ↔ forall p : Su
bMulAction R M, s subseteq p -> x in p
-/
theorem closure_le {p} : closure R s ≤ p ↔ s ⊆ p :=
  ⟨subset_closure.trans, fun ss _ h => mem_closure.1 h _ ss⟩

@[to_additive (attr := gcongr)]
/-
**SubMulAction.closure_mono** 是 Mathlib 中的一个定理，位于命名空间 `SubMulAction`。
形式化陈述：closure_mono (h : s subseteq t) : closure R s <= closure R t
参数：h : s subseteq t。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `SubMulAction.closure_le`：closure_le {p} : closure R s <= p ↔ s subseteq 
p
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用定理 `SubMulAction.subset_closure`：subset_closure : s subseteq closure R s
-/
theorem closure_mono (h : s ⊆ t) : closure R s ≤ closure R t :=
  closure_le.2 <| h.trans subset_closure

/-- A `SubMulAction` is finitely generated if it is the closure of a finite set. -/
@[to_additive /-- A `SubAddAction` is finitely generated if it is the closure of a finite set. -/]
/-
**SubMulAction.FG** 是 Mathlib 中的一个定义，位于命名空间 `SubMulAction`。
形式化陈述：FG (p : SubMulAction R M)
参数：p : SubMulAction R M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A `SubMulAction` is finitely generated if it is the closure of a finite set.
-/
def FG (p : SubMulAction R M) :=
  ∃ (s : Set M), s.Finite ∧ p = closure R s

@[to_additive]
/-
**SubMulAction.fg_iff** 是 Mathlib 中的一个定理，位于命名空间 `SubMulAction`。
形式化陈述：fg_iff {p : SubMulAction R M} : p.FG ↔ exists (s : Finset M), p = closure 
R s
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.exists_finite_iff_finset`：exists_finite_iff_finset {p : Set α -> Pro
p} : (exists s : Set α, s.Finite ∧ p s) ↔ exists s : Finset α, p ↑s
-/
theorem fg_iff {p : SubMulAction R M} : p.FG ↔ ∃ (s : Finset M), p = closure R s :=
  Set.exists_finite_iff_finset

end SubMulAction

