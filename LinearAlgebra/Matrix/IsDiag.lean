/-
Copyright (c) 2021 Lu-Ming Zhang. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Lu-Ming Zhang
-/
module

public import Mathlib.LinearAlgebra.Matrix.Kronecker
public import Mathlib.LinearAlgebra.Matrix.Orthogonal
public import Mathlib.LinearAlgebra.Matrix.Symmetric

/-!
# Diagonal matrices

This file contains the definition and basic results about diagonal matrices.

## Main results

- `Matrix.IsDiag`: a proposition that states a given square matrix `A` is diagonal.

## Tags

diag, diagonal, matrix
-/

@[expose] public section


namespace Matrix

variable {α β R n m : Type*}

open Function

open Matrix Kronecker

/-- `A.IsDiag` means square matrix `A` is a diagonal matrix. -/
/-
**Matrix.IsDiag** 是 Mathlib 中的一个定义，位于命名空间 `Matrix`。
形式化陈述：IsDiag [Zero α] (A : Matrix n n α) : Prop
参数：A : Matrix n n α。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`A.IsDiag` means square matrix `A` is a diagonal matrix.
-/
def IsDiag [Zero α] (A : Matrix n n α) : Prop :=
  Pairwise fun i j => A i j = 0

@[simp]
/-
**Matrix.isDiag_diagonal** 是 Mathlib 中的一个定理，位于命名空间 `Matrix`。
形式化陈述：isDiag_diagonal [Zero α] [DecidableEq n] (d : n -> α) : (diagonal d).IsDia
g
参数：d : n -> α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Matrix.diagonal_apply_ne`：diagonal_apply_ne [Zero α] (d : n -> α) {i j :
 n} (h : i != j) : (diagonal d) i j = 0
-/
theorem isDiag_diagonal [Zero α] [DecidableEq n] (d : n → α) : (diagonal d).IsDiag := fun _ _ =>
  Matrix.diagonal_apply_ne _

/-- Diagonal matrices are generated by the `Matrix.diagonal` of their `Matrix.diag`. -/
/-
**Matrix.IsDiag.diagonal_diag** 是 Mathlib 中的一个定理，位于命名空间 `Matrix.IsDiag`。
形式化陈述：∀ {α : Type u_1} {n : Type u_4} [inst : Zero α] [inst_1 : DecidableEq n] {
A : Matrix n n α},   A.IsDiag → Matrix.diagonal A.diag = A
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Matrix.ext`：ext : (forall i j, M i j = N i j) -> M = N
· 使用定理 `Decidable.eq_or_ne`：Decidable.eq_or_ne {α : Sort*} (x y : α) [Decidable 
(x = y)] : x = y ∨ x != y
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Matrix.diagonal_apply_eq`：diagonal_apply_eq [Zero α] (d : n -> α) (i : n
) : (diagonal d) i i = d i
· 使用定理 `Matrix.diag.eq_1`：∀ {n : Type u_3} {α : Type v} (A : Matrix n n α) (i : 
n), A.diag i = A i i
· 使用定理 `Matrix.diagonal_apply_ne`：diagonal_apply_ne [Zero α] (d : n -> α) {i j :
 n} (h : i != j) : (diagonal d) i j = 0

--- 原说明 ---
Diagonal matrices are generated by the `Matrix.diagonal` of their `Matrix.diag`.
-/
theorem IsDiag.diagonal_diag [Zero α] [DecidableEq n] {A : Matrix n n α} (h : A.IsDiag) :
    diagonal (diag A) = A :=
  ext fun i j => by
    obtain rfl | hij := Decidable.eq_or_ne i j
    · rw [diagonal_apply_eq, diag]
    · rw [diagonal_apply_ne _ hij, h hij]

/-- `Matrix.IsDiag.diagonal_diag` as an iff. -/
/-
**Matrix.isDiag_iff_diagonal_diag** 是 Mathlib 中的一个定理，位于命名空间 `Matrix`。
形式化陈述：isDiag_iff_diagonal_diag [Zero α] [DecidableEq n] (A : Matrix n n α) : A.I
sDiag ↔ diagonal (diag A) = A
参数：A : Matrix n n α。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Matrix.IsDiag.diagonal_diag`：∀ {α : Type u_1} {n : Type u_4} [inst : Zer
o α] [inst_1 : DecidableEq n] {A : Matrix n n α},   A.IsDiag → Matrix.diagonal A
.diag = A
· 使用定理 `Matrix.isDiag_diagonal`：isDiag_diagonal [Zero α] [DecidableEq n] (d : n 
-> α) : (diagonal d).IsDiag

--- 原说明 ---
`Matrix.IsDiag.diagonal_diag` as an iff.
-/
theorem isDiag_iff_diagonal_diag [Zero α] [DecidableEq n] (A : Matrix n n α) :
    A.IsDiag ↔ diagonal (diag A) = A :=
  ⟨IsDiag.diagonal_diag, fun hd => hd ▸ isDiag_diagonal (diag A)⟩

/-- Every matrix indexed by a subsingleton is diagonal. -/
/-
**Matrix.isDiag_of_subsingleton** 是 Mathlib 中的一个定理，位于命名空间 `Matrix`。
形式化陈述：isDiag_of_subsingleton [Zero α] [Subsingleton n] (A : Matrix n n α) : A.Is
Diag
参数：A : Matrix n n α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subsingleton.elim`：∀ {α : Sort u} [h : Subsingleton α] (a b : α), a = b

--- 原说明 ---
Every matrix indexed by a subsingleton is diagonal.
-/
theorem isDiag_of_subsingleton [Zero α] [Subsingleton n] (A : Matrix n n α) : A.IsDiag :=
  fun i j h => (h <| Subsingleton.elim i j).elim

/-- Every zero matrix is diagonal. -/
@[simp]
/-
**Matrix.isDiag_zero** 是 Mathlib 中的一个定理，位于命名空间 `Matrix`。
形式化陈述：isDiag_zero [Zero α] : (0 : Matrix n n α).IsDiag
该定理/引理描述了相关对象所满足的性质。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Every zero matrix is diagonal.
-/
theorem isDiag_zero [Zero α] : (0 : Matrix n n α).IsDiag := fun _ _ _ => rfl

/-- Every identity matrix is diagonal. -/
@[simp]
/-
**Matrix.isDiag_one** 是 Mathlib 中的一个定理，位于命名空间 `Matrix`。
形式化陈述：isDiag_one [DecidableEq n] [Zero α] [One α] : (1 : Matrix n n α).IsDiag
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Matrix.one_apply_ne`：one_apply_ne {i j} : i != j -> (1 : Matrix n n α) i
 j = 0

--- 原说明 ---
Every identity matrix is diagonal.
-/
theorem isDiag_one [DecidableEq n] [Zero α] [One α] : (1 : Matrix n n α).IsDiag := fun _ _ =>
  one_apply_ne
/-
**Matrix.IsDiag.map** 是 Mathlib 中的一个定理，位于命名空间 `Matrix.IsDiag`。
形式化陈述：∀ {α : Type u_1} {β : Type u_2} {n : Type u_4} [inst : Zero α] [inst_1 : Z
ero β] {A : Matrix n n α},   A.IsDiag → ∀ {f : α → β}, f 0 = 0 → (A.map f).IsDia
g
参数：A.map f。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem IsDiag.map [Zero α] [Zero β] {A : Matrix n n α} (ha : A.IsDiag) {f : α → β} (hf : f 0 = 0) :
    (A.map f).IsDiag := by
  intro i j h
  simp [ha h, hf]
/-
**Matrix.IsDiag.neg** 是 Mathlib 中的一个定理，位于命名空间 `Matrix.IsDiag`。
形式化陈述：∀ {α : Type u_1} {n : Type u_4} [inst : SubtractionMonoid α] {A : Matrix n
 n α}, A.IsDiag → (-A).IsDiag
参数：-A。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `neg_zero`：neg_zero {R} [CommRing R] : -(0 : R) = 0
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem IsDiag.neg [SubtractionMonoid α] {A : Matrix n n α} (ha : A.IsDiag) : (-A).IsDiag := by
  intro i j h
  simp [ha h]

@[simp]
/-
**Matrix.isDiag_neg_iff** 是 Mathlib 中的一个定理，位于命名空间 `Matrix`。
形式化陈述：isDiag_neg_iff [SubtractionMonoid α] {A : Matrix n n α} : (-A).IsDiag ↔ A.
IsDiag
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `neg_eq_zero`：∀ {α : Type u_1} [inst : SubtractionMonoid α] {a : α}, -a =
 0 ↔ a = 0
· 使用定理 `Matrix.IsDiag.neg`：∀ {α : Type u_1} {n : Type u_4} [inst : SubtractionMo
noid α] {A : Matrix n n α}, A.IsDiag → (-A).IsDiag
-/
theorem isDiag_neg_iff [SubtractionMonoid α] {A : Matrix n n α} : (-A).IsDiag ↔ A.IsDiag :=
  ⟨fun ha _ _ h => neg_eq_zero.1 (ha h), IsDiag.neg⟩
/-
**Matrix.IsDiag.add** 是 Mathlib 中的一个定理，位于命名空间 `Matrix.IsDiag`。
形式化陈述：∀ {α : Type u_1} {n : Type u_4} [inst : AddZeroClass α] {A B : Matrix n n 
α}, A.IsDiag → B.IsDiag → (A + B).IsDiag
参数：A + B。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `add_zero`：∀ {M : Type u} [inst : AddZeroClass M] (a : M), a + 0 = a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem IsDiag.add [AddZeroClass α] {A B : Matrix n n α} (ha : A.IsDiag) (hb : B.IsDiag) :
    (A + B).IsDiag := by
  intro i j h
  simp [ha h, hb h]
/-
**Matrix.IsDiag.sub** 是 Mathlib 中的一个定理，位于命名空间 `Matrix.IsDiag`。
形式化陈述：∀ {α : Type u_1} {n : Type u_4} [inst : SubtractionMonoid α] {A B : Matrix
 n n α}, A.IsDiag → B.IsDiag → (A - B).IsDiag
参数：A - B。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `sub_zero`：∀ {G : Type u_3} [inst : SubNegZeroMonoid G] (a : G), a - 0 = 
a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem IsDiag.sub [SubtractionMonoid α] {A B : Matrix n n α} (ha : A.IsDiag) (hb : B.IsDiag) :
    (A - B).IsDiag := by
  intro i j h
  simp [ha h, hb h]
/-
**Matrix.IsDiag.smul** 是 Mathlib 中的一个定理，位于命名空间 `Matrix.IsDiag`。
形式化陈述：∀ {α : Type u_1} {R : Type u_3} {n : Type u_4} [inst : Zero α] [inst_1 : S
MulZeroClass R α] (k : R) {A : Matrix n n α},   A.IsDiag → (k • A).IsDiag
参数：k : R；k • A。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `smul_zero`：smul_zero (a : M) : a • (0 : A) = 0
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem IsDiag.smul [Zero α] [SMulZeroClass R α] (k : R) {A : Matrix n n α}
    (ha : A.IsDiag) : (k • A).IsDiag := by
  intro i j h
  simp [ha h]

@[simp]
/-
**Matrix.isDiag_smul_one** 是 Mathlib 中的一个定理，位于命名空间 `Matrix`。
形式化陈述：isDiag_smul_one (n) [MulZeroOneClass α] [DecidableEq n] (k : α) : (k • (1 
: Matrix n n α)).IsDiag
参数：n；k : α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Matrix.IsDiag.smul`：∀ {α : Type u_1} {R : Type u_3} {n : Type u_4} [inst
 : Zero α] [inst_1 : SMulZeroClass R α] (k : R) {A : Matrix n n α},   A.IsDiag →
 (k • A)…
· 使用定理 `Matrix.isDiag_one`：isDiag_one [DecidableEq n] [Zero α] [One α] : (1 : Ma
trix n n α).IsDiag
-/
theorem isDiag_smul_one (n) [MulZeroOneClass α] [DecidableEq n] (k : α) :
    (k • (1 : Matrix n n α)).IsDiag :=
  isDiag_one.smul k
/-
**Matrix.IsDiag.transpose** 是 Mathlib 中的一个定理，位于命名空间 `Matrix.IsDiag`。
形式化陈述：∀ {α : Type u_1} {n : Type u_4} [inst : Zero α] {A : Matrix n n α}, A.IsDi
ag → A.transpose.IsDiag
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ne.symm`：∀ {α : Sort u} {a b : α}, a ≠ b → b ≠ a
-/
theorem IsDiag.transpose [Zero α] {A : Matrix n n α} (ha : A.IsDiag) : Aᵀ.IsDiag := fun _ _ h =>
  ha h.symm

@[simp]
/-
**Matrix.isDiag_transpose_iff** 是 Mathlib 中的一个定理，位于命名空间 `Matrix`。
形式化陈述：isDiag_transpose_iff [Zero α] {A : Matrix n n α} : Aᵀ.IsDiag ↔ A.IsDiag
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Matrix.IsDiag.transpose`：∀ {α : Type u_1} {n : Type u_4} [inst : Zero α]
 {A : Matrix n n α}, A.IsDiag → A.transpose.IsDiag
-/
theorem isDiag_transpose_iff [Zero α] {A : Matrix n n α} : Aᵀ.IsDiag ↔ A.IsDiag :=
  ⟨IsDiag.transpose, IsDiag.transpose⟩
/-
**Matrix.IsDiag.conjTranspose** 是 Mathlib 中的一个定理，位于命名空间 `Matrix.IsDiag`。
形式化陈述：∀ {α : Type u_1} {n : Type u_4} [inst : NonUnitalNonAssocSemiring α] [inst
_1 : StarRing α] {A : Matrix n n α},   A.IsDiag → A.conjTranspose.IsDiag
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Matrix.IsDiag.map`：∀ {α : Type u_1} {β : Type u_2} {n : Type u_4} [inst 
: Zero α] [inst_1 : Zero β] {A : Matrix n n α},   A.IsDiag → ∀ {f : α → β}, f 0 
= 0 → (…
· 使用定理 `Matrix.IsDiag.transpose`：∀ {α : Type u_1} {n : Type u_4} [inst : Zero α]
 {A : Matrix n n α}, A.IsDiag → A.transpose.IsDiag
· 使用定理 `star_zero`：star_zero [AddMonoid R] [StarAddMonoid R] : star (0 : R) = 0
-/
theorem IsDiag.conjTranspose [NonUnitalNonAssocSemiring α] [StarRing α] {A : Matrix n n α}
    (ha : A.IsDiag) : Aᴴ.IsDiag :=
  ha.transpose.map (star_zero _)

@[simp]
/-
**Matrix.isDiag_conjTranspose_iff** 是 Mathlib 中的一个定理，位于命名空间 `Matrix`。
形式化陈述：isDiag_conjTranspose_iff [NonUnitalNonAssocSemiring α] [StarRing α] {A : M
atrix n n α} : Aᴴ.IsDiag ↔ A.IsDiag
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Matrix.conjTranspose_conjTranspose`：conjTranspose_conjTranspose [Involut
iveStar α] (M : Matrix m n α) : Mᴴᴴ = M
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Matrix.IsDiag.conjTranspose`：∀ {α : Type u_1} {n : Type u_4} [inst : Non
UnitalNonAssocSemiring α] [inst_1 : StarRing α] {A : Matrix n n α},   A.IsDiag →
 A.conjTranspose.…
-/
theorem isDiag_conjTranspose_iff [NonUnitalNonAssocSemiring α] [StarRing α] {A : Matrix n n α} :
    Aᴴ.IsDiag ↔ A.IsDiag :=
  ⟨fun ha => by
    convert! ha.conjTranspose
    simp, IsDiag.conjTranspose⟩
/-
**Matrix.IsDiag.submatrix** 是 Mathlib 中的一个定理，位于命名空间 `Matrix.IsDiag`。
形式化陈述：∀ {α : Type u_1} {n : Type u_4} {m : Type u_5} [inst : Zero α] {A : Matrix
 n n α},   A.IsDiag → ∀ {f : m → n}, Function.Injective f → (A.submatrix f f).Is
Diag
参数：A.submatrix f f。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Function.Injective.ne`：∀ {α : Sort u_1} {β : Sort u_2} {f : α → β}, Func
tion.Injective f → ∀ {a₁ a₂ : α}, a₁ ≠ a₂ → f a₁ ≠ f a₂
-/
theorem IsDiag.submatrix [Zero α] {A : Matrix n n α} (ha : A.IsDiag) {f : m → n}
    (hf : Injective f) : (A.submatrix f f).IsDiag := fun _ _ h => ha (hf.ne h)

/-- `(A ⊗ B).IsDiag` if both `A` and `B` are diagonal. -/
/-
**Matrix.IsDiag.kronecker** 是 Mathlib 中的一个定理，位于命名空间 `Matrix.IsDiag`。
形式化陈述：∀ {α : Type u_1} {n : Type u_4} {m : Type u_5} [inst : MulZeroClass α] {A 
: Matrix m m α} {B : Matrix n n α},   A.IsDiag → B.IsDiag → (Matrix.kroneckerMap
 (fun x1 x2 => x1 * x2) A B).IsDiag
参数：Matrix.kroneckerMap (fun x1 x2 => x1 * x2) A B。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `MulZeroClass.zero_mul`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, 0 * a = 0
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `MulZeroClass.mul_zero`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, a * 0 = 0

--- 原说明 ---
`(A ⊗ B).IsDiag` if both `A` and `B` are diagonal.
-/
theorem IsDiag.kronecker [MulZeroClass α] {A : Matrix m m α} {B : Matrix n n α} (hA : A.IsDiag)
    (hB : B.IsDiag) : (A ⊗ₖ B).IsDiag := by
  rintro ⟨a, b⟩ ⟨c, d⟩ h
  simp only [Prod.mk_inj, Ne, not_and_or] at h
  rcases h with hac | hbd
  · simp [hA hac]
  · simp [hB hbd]
/-
**Matrix.IsDiag.isSymm** 是 Mathlib 中的一个定理，位于命名空间 `Matrix.IsDiag`。
形式化陈述：∀ {α : Type u_1} {n : Type u_4} [inst : Zero α] {A : Matrix n n α}, A.IsDi
ag → A.IsSymm
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Matrix.ext`：ext : (forall i j, M i j = N i j) -> M = N
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Matrix.transpose_apply`：transpose_apply (M : Matrix m n α) (i j) : trans
pose M i j = M j i
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `Ne.symm`：∀ {α : Sort u} {a b : α}, a ≠ b → b ≠ a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem IsDiag.isSymm [Zero α] {A : Matrix n n α} (h : A.IsDiag) : A.IsSymm := by
  ext i j
  by_cases g : i = j; · rw [g, transpose_apply]
  simp [h g, h (Ne.symm g)]

/-- The block matrix `A.fromBlocks 0 0 D` is diagonal if `A` and `D` are diagonal. -/
/-
**Matrix.IsDiag.fromBlocks** 是 Mathlib 中的一个定理，位于命名空间 `Matrix.IsDiag`。
形式化陈述：∀ {α : Type u_1} {n : Type u_4} {m : Type u_5} [inst : Zero α] {A : Matrix
 m m α} {D : Matrix n n α},   A.IsDiag → D.IsDiag → (Matrix.fromBlocks A 0 0 D).
IsDiag
参数：Matrix.fromBlocks A 0 0 D。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `ne_of_apply_ne`：∀ {α : Sort u_1} {β : Sort u_2} (f : α → β) {x y : α}, f
 x ≠ f y → x ≠ y

--- 原说明 ---
The block matrix `A.fromBlocks 0 0 D` is diagonal if `A` and `D` are diagonal.
-/
theorem IsDiag.fromBlocks [Zero α] {A : Matrix m m α} {D : Matrix n n α} (ha : A.IsDiag)
    (hd : D.IsDiag) : (A.fromBlocks 0 0 D).IsDiag := by
  rintro (i | i) (j | j) hij
  · exact ha (ne_of_apply_ne _ hij)
  · rfl
  · rfl
  · exact hd (ne_of_apply_ne _ hij)

/-- This is the `iff` version of `Matrix.IsDiag.fromBlocks`. -/
/-
**Matrix.isDiag_fromBlocks_iff** 是 Mathlib 中的一个定理，位于命名空间 `Matrix`。
形式化陈述：isDiag_fromBlocks_iff [Zero α] {A : Matrix m m α} {B : Matrix m n α} {C : 
Matrix n m α} {D : Matrix n n α} : (A.fromBlocks B C D).IsDiag ↔ A.IsDiag ∧ B = 
0 ∧ C = 0 ∧ D.IsDiag
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Function.Injective.ne`：∀ {α : Sort u_1} {β : Sort u_2} {f : α → β}, Func
tion.Injective f → ∀ {a₁ a₂ : α}, a₁ ≠ a₂ → f a₁ ≠ f a₂
· 使用定理 `Sum.inl_injective`：inl_injective : Function.Injective (inl : α -> α oplu
s β)
· 使用定理 `Matrix.ext`：ext : (forall i j, M i j = N i j) -> M = N
· 使用定理 `Sum.inl_ne_inr`：∀ {α : Type u_1} {a : α} {β : Type u_2} {b : β}, Sum.inl
 a ≠ Sum.inr b
· 使用定理 `Sum.inr_ne_inl`：∀ {β : Type u_1} {b : β} {α : Type u_2} {a : α}, Sum.inr
 b ≠ Sum.inl a
· 使用定理 `Sum.inr_injective`：inr_injective : Function.Injective (inr : β -> α oplu
s β)
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Matrix.IsDiag.fromBlocks`：∀ {α : Type u_1} {n : Type u_4} {m : Type u_5}
 [inst : Zero α] {A : Matrix m m α} {D : Matrix n n α},   A.IsDiag → D.IsDiag → 
(Matrix.fromBl…

--- 原说明 ---
This is the `iff` version of `Matrix.IsDiag.fromBlocks`.
-/
theorem isDiag_fromBlocks_iff [Zero α] {A : Matrix m m α} {B : Matrix m n α} {C : Matrix n m α}
    {D : Matrix n n α} : (A.fromBlocks B C D).IsDiag ↔ A.IsDiag ∧ B = 0 ∧ C = 0 ∧ D.IsDiag := by
  constructor
  · intro h
    refine ⟨fun i j hij => ?_, ext fun i j => ?_, ext fun i j => ?_, fun i j hij => ?_⟩
    · exact h (Sum.inl_injective.ne hij)
    · exact h Sum.inl_ne_inr
    · exact h Sum.inr_ne_inl
    · exact h (Sum.inr_injective.ne hij)
  · rintro ⟨ha, hb, hc, hd⟩
    convert! IsDiag.fromBlocks ha hd

/-- A symmetric block matrix `A.fromBlocks B C D` is diagonal
if `A` and `D` are diagonal and `B` is `0`. -/
/-
**Matrix.IsDiag.fromBlocks_of_isSymm** 是 Mathlib 中的一个定理，位于命名空间 `Matrix.IsDiag`。
形式化陈述：∀ {α : Type u_1} {n : Type u_4} {m : Type u_5} [inst : Zero α] {A : Matrix
 m m α} {C : Matrix n m α} {D : Matrix n n α},   (Matrix.fromBlocks A 0 C D).IsS
ymm → A.IsDiag → D.IsDiag → (Matrix.fromBlocks A 0 C D).IsDiag
参数：Matrix.fromBlocks A 0 C D；Matrix.fromBlocks A 0 C D。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Matrix.isSymm_fromBlocks_iff`：isSymm_fromBlocks_iff {A : Matrix m m α} {
B : Matrix m n α} {C : Matrix n m α} {D : Matrix n n α} : (A.fromBlocks B C D).I
sSymm ↔ A.IsSymm ∧…
· 使用定理 `Matrix.IsDiag.fromBlocks`：∀ {α : Type u_1} {n : Type u_4} {m : Type u_5}
 [inst : Zero α] {A : Matrix m m α} {D : Matrix n n α},   A.IsDiag → D.IsDiag → 
(Matrix.fromBl…

--- 原说明 ---
A symmetric block matrix `A.fromBlocks B C D` is diagonal
if `A` and `D` are diagonal and `B` is `0`.
-/
theorem IsDiag.fromBlocks_of_isSymm [Zero α] {A : Matrix m m α} {C : Matrix n m α}
    {D : Matrix n n α} (h : (A.fromBlocks 0 C D).IsSymm) (ha : A.IsDiag) (hd : D.IsDiag) :
    (A.fromBlocks 0 C D).IsDiag := by
  rw [← (isSymm_fromBlocks_iff.1 h).2.1]
  exact ha.fromBlocks hd
/-
**Matrix.mul_transpose_self_isDiag_iff_hasOrthogonalRows** 是 Mathlib 中的一个定理，位于命名
空间 `Matrix`。
形式化陈述：mul_transpose_self_isDiag_iff_hasOrthogonalRows [Fintype n] [Mul α] [AddCo
mmMonoid α] {A : Matrix m n α} : (A * Aᵀ).IsDiag ↔ A.HasOrthogonalRows
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem mul_transpose_self_isDiag_iff_hasOrthogonalRows [Fintype n] [Mul α] [AddCommMonoid α]
    {A : Matrix m n α} : (A * Aᵀ).IsDiag ↔ A.HasOrthogonalRows :=
  Iff.rfl
/-
**Matrix.transpose_mul_self_isDiag_iff_hasOrthogonalCols** 是 Mathlib 中的一个定理，位于命名
空间 `Matrix`。
形式化陈述：transpose_mul_self_isDiag_iff_hasOrthogonalCols [Fintype m] [Mul α] [AddCo
mmMonoid α] {A : Matrix m n α} : (Aᵀ * A).IsDiag ↔ A.HasOrthogonalCols
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem transpose_mul_self_isDiag_iff_hasOrthogonalCols [Fintype m] [Mul α] [AddCommMonoid α]
    {A : Matrix m n α} : (Aᵀ * A).IsDiag ↔ A.HasOrthogonalCols :=
  Iff.rfl

end Matrix

