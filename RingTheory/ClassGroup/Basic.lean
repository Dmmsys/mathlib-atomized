/-
Copyright (c) 2021 Anne Baanen. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Anne Baanen
-/
module

public import Mathlib.RingTheory.DedekindDomain.Ideal.Basic
public import Mathlib.Tactic.CrossRefAttribute

/-!
# The ideal class group

This file defines the ideal class group `ClassGroup R` of fractional ideals of `R`
inside its field of fractions.

## Main definitions

- `toPrincipalIdeal` sends an invertible `x : K` to an invertible fractional ideal
- `ClassGroup` is the quotient of invertible fractional ideals modulo `toPrincipalIdeal.range`
- `ClassGroup.mk0` sends a nonzero integral ideal in a Dedekind domain to its class

## Main results
- `ClassGroup.mk0_eq_mk0_iff` shows the equivalence with the "classical" definition,
  where `I ~ J` iff `x I = y J` for `x y ≠ (0 : R)`
- `ClassGroup.mk0_eq_quotientMk` identifies `ClassGroup.mk0` with the canonical quotient
  projection on `(FractionalIdeal R⁰ (FractionRing R))ˣ`

## Implementation details

The definition of `ClassGroup R` involves `FractionRing R`. However, the API should be completely
identical no matter the choice of field of fractions for `R`.
-/

@[expose] public noncomputable section


variable {R K : Type*} [CommRing R] [Field K] [Algebra R K] [IsFractionRing R K]

open scoped nonZeroDivisors

open IsLocalization IsFractionRing FractionalIdeal Units

section

variable (R K)

/-- `toPrincipalIdeal R K x` sends `x ≠ 0 : K` to the fractional `R`-ideal generated by `x` -/
irreducible_def toPrincipalIdeal : Kˣ →* (FractionalIdeal R⁰ K)ˣ :=
  { toFun := fun x =>
      ⟨spanSingleton _ x, spanSingleton _ x⁻¹, by
        simp only [spanSingleton_one, Units.mul_inv', spanSingleton_mul_spanSingleton], by
        simp only [spanSingleton_one, Units.inv_mul', spanSingleton_mul_spanSingleton]⟩
    map_mul' := fun x y =>
      ext (by simp only [Units.val_mul, spanSingleton_mul_spanSingleton])
    map_one' := ext (by simp only [spanSingleton_one, Units.val_one]) }

variable {R K}

@[simp]
/-
**coe_toPrincipalIdeal** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：coe_toPrincipalIdeal (x : Kˣ) : (toPrincipalIdeal R K x : FractionalIdeal 
R⁰ K) = spanSingleton _ (x : K)
参数：x : Kˣ。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `toPrincipalIdeal_def`：∀ (R : Type u_3) (K : Type u_4) [inst : CommRing R
] [inst_1 : Field K] [inst_2 : Algebra R K]   [inst_3 : IsFractionRing R K],   t
oPrincipal…
-/
theorem coe_toPrincipalIdeal (x : Kˣ) :
    (toPrincipalIdeal R K x : FractionalIdeal R⁰ K) = spanSingleton _ (x : K) := by
  simp only [toPrincipalIdeal]; rfl

@[simp]
/-
**toPrincipalIdeal_eq_iff** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：toPrincipalIdeal_eq_iff {I : (FractionalIdeal R⁰ K)ˣ} {x : Kˣ} : toPrincip
alIdeal R K x = I ↔ spanSingleton R⁰ (x : K) = I
参数：FractionalIdeal R⁰ K。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `toPrincipalIdeal_def`：∀ (R : Type u_3) (K : Type u_4) [inst : CommRing R
] [inst_1 : Field K] [inst_2 : Algebra R K]   [inst_3 : IsFractionRing R K],   t
oPrincipal…
· 使用定理 `Units.ext_iff`：∀ {α : Type u} [inst : Monoid α] {u v : αˣ}, u = v ↔ ↑u =
 ↑v
-/
theorem toPrincipalIdeal_eq_iff {I : (FractionalIdeal R⁰ K)ˣ} {x : Kˣ} :
    toPrincipalIdeal R K x = I ↔ spanSingleton R⁰ (x : K) = I := by
  simp only [toPrincipalIdeal]; exact Units.ext_iff
/-
**mem_principal_ideals_iff** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：mem_principal_ideals_iff {I : (FractionalIdeal R⁰ K)ˣ} : I in (toPrincipal
Ideal R K).range ↔ exists x : K, spanSingleton R⁰ x = I
参数：FractionalIdeal R⁰ K。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `FractionalIdeal.spanSingleton_zero`：spanSingleton_zero : spanSingleton S
 (0 : P) = 0
· 使用定理 `eq_false`：∀ {p : Prop}, ¬p → p = False
· 使用定理 `Ne.symm`：∀ {α : Sort u} {a b : α}, a ≠ b → b ≠ a
· 使用定理 `Units.ne_zero`：ne_zero [Nontrivial M₀] (u : M₀ˣ) : (u : M₀) != 0
· 使用定理 `FractionalIdeal.instNontrivialNonZeroDivisors`：∀ {R₁ : Type u_3} [inst :
 CommRing R₁] {K : Type u_4} [inst_1 : Field K] [inst_2 : Algebra R₁ K],   Nontr
ivial (FractionalIdeal (nonZeroDivi…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
-/
theorem mem_principal_ideals_iff {I : (FractionalIdeal R⁰ K)ˣ} :
    I ∈ (toPrincipalIdeal R K).range ↔ ∃ x : K, spanSingleton R⁰ x = I := by
  simp only [MonoidHom.mem_range, toPrincipalIdeal_eq_iff]
  constructor <;> rintro ⟨x, hx⟩
  · exact ⟨x, hx⟩
  · refine ⟨Units.mk0 x ?_, hx⟩
    rintro rfl
    simp [I.ne_zero.symm] at hx
/-
**PrincipalIdeals.normal** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：PrincipalIdeals.normal : (toPrincipalIdeal R K).range.Normal
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Subgroup.normal_of_isMulCommutative`：∀ {G : Type u_1} [inst : Group G] [
IsMulCommutative G] (H : Subgroup G), H.Normal
-/
instance PrincipalIdeals.normal : (toPrincipalIdeal R K).range.Normal :=
  Subgroup.normal_of_isMulCommutative _

end

variable (R)
variable [IsDomain R]

/-- The ideal class group of `R` is the group of invertible fractional ideals
modulo the principal ideals. -/
@[wikidata Q912083]
/-
**ClassGroup** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：ClassGroup
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The ideal class group of `R` is the group of invertible fractional ideals
modulo the principal ideals.
-/
def ClassGroup :=
  (FractionalIdeal R⁰ (FractionRing R))ˣ ⧸ (toPrincipalIdeal R (FractionRing R)).range
deriving CommGroup, Inhabited

/-- The class group of `R` is isomorphic to the group of invertible `R`-submodules in `Frac(R)`
modulo the principal submodules (invertible submodules are automatically fractional ideals). -/
/-
**ClassGroup.mulEquivUnitsSubmoduleQuotRange** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：ClassGroup.mulEquivUnitsSubmoduleQuotRange : ClassGroup R ≃* (Submodule R 
(FractionRing R))ˣ ⧸ (Units.map (Submodule.spanSingleton R).toMonoidHom).range
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The class group of `R` is isomorphic to the group of invertible `R`-submodules i
n `Frac(R)`
modulo the principal submodules (invertible submodules are automatically fractio
nal ideals).
-/
def ClassGroup.mulEquivUnitsSubmoduleQuotRange :
    ClassGroup R ≃*
    (Submodule R (FractionRing R))ˣ ⧸ (Units.map (Submodule.spanSingleton R).toMonoidHom).range :=
  QuotientGroup.congr _ _ unitsMulEquivSubmodule <| by
    simp_rw [MonoidHom.range_eq_map, Subgroup.map_map]; congr; ext; simp [unitsMulEquivSubmodule]

variable {R} (K)

/-- Send a nonzero fractional ideal to the corresponding class in the class group. -/
/-
**ClassGroup.mk** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：ClassGroup.mk : (FractionalIdeal R⁰ K)ˣ ->* ClassGroup R
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Send a nonzero fractional ideal to the corresponding class in the class group.
-/
def ClassGroup.mk : (FractionalIdeal R⁰ K)ˣ →* ClassGroup R :=
  (QuotientGroup.mk' (toPrincipalIdeal R (FractionRing R)).range).comp
    (Units.map (FractionalIdeal.canonicalEquiv R⁰ K (FractionRing R)))

variable {K}
/-
**ClassGroup.mk_def** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：ClassGroup.mk_def (I : (FractionalIdeal R⁰ K)ˣ) : ClassGroup.mk K I = (Quo
tientGroup.mk' (toPrincipalIdeal R (FractionRing R)).range) (Units.map (Fraction
alIdeal.canonicalEquiv R⁰ K (FractionRing R)) I)
参数：I : (FractionalIdeal R⁰ K)ˣ。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma ClassGroup.mk_def (I : (FractionalIdeal R⁰ K)ˣ) :
    ClassGroup.mk K I =
      (QuotientGroup.mk' (toPrincipalIdeal R (FractionRing R)).range)
        (Units.map (FractionalIdeal.canonicalEquiv R⁰ K (FractionRing R)) I) := rfl

-- Can't be `@[simp]` because it can't figure out the quotient relation.
/-
**ClassGroup.Quot_mk_eq_mk** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.Quot_mk_eq_mk (I : (FractionalIdeal R⁰ (FractionRing R))ˣ) : Qu
ot.mk _ I = ClassGroup.mk (FractionRing R) I
参数：I : (FractionalIdeal R⁰ (FractionRing R))ˣ。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `RingEquivClass.toRingHomClass`：∀ {F : Type u_1} {R : Type u_4} {S : Type
 u_5} [inst : EquivLike F R S] [inst_1 : NonAssocSemiring R]   [inst_2 : NonAsso
cSemiring S] [h : R…
· 使用定理 `RingEquiv.instRingEquivClass`：∀ {R : Type u_4} {S : Type u_5} [inst : Mu
l R] [inst_1 : Mul S] [inst_2 : Add R] [inst_3 : Add S],   RingEquivClass (R ≃+*
 S) R S
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `ClassGroup.mk_def`：ClassGroup.mk_def (I : (FractionalIdeal R⁰ K)ˣ) : Cla
ssGroup.mk K I = (QuotientGroup.mk' (toPrincipalIdeal R (FractionRing R)).range)
 (Units…
· 使用定理 `FractionalIdeal.canonicalEquiv_self`：canonicalEquiv_self : canonicalEqui
v S P P = RingEquiv.refl _
· 使用定理 `RingEquiv.coe_monoidHom_refl`：coe_monoidHom_refl : (RingEquiv.refl R : R
 ->* R) = MonoidHom.id R
· 使用定理 `Units.map_id`：map_id : map (MonoidHom.id M) = MonoidHom.id Mˣ
· 使用定理 `MonoidHom.id_apply`：∀ (M : Type u_10) [inst : MulOne M] (x : M), (Monoid
Hom.id M) x = x
· 使用定理 `QuotientGroup.mk'_apply`：∀ {G : Type u_1} [inst : Group G] (N : Subgroup
 G) [nN : N.Normal] (x : G), (QuotientGroup.mk' N) x = ↑x
-/
theorem ClassGroup.Quot_mk_eq_mk (I : (FractionalIdeal R⁰ (FractionRing R))ˣ) :
    Quot.mk _ I = ClassGroup.mk (FractionRing R) I := by
  rw [ClassGroup.mk_def, canonicalEquiv_self, RingEquiv.coe_monoidHom_refl, Units.map_id,
    MonoidHom.id_apply, QuotientGroup.mk'_apply]
  rfl

set_option backward.isDefEq.respectTransparency false in
/-
**ClassGroup.mk_eq_mk** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.mk_eq_mk {I J : (FractionalIdeal R⁰ <| FractionRing R)ˣ} : Clas
sGroup.mk (FractionRing R) I = ClassGroup.mk (FractionRing R) J ↔ exists x : (Fr
actionRing R)ˣ, I * toPrincipalIdeal R (FractionRing R) x = J
参数：FractionalIdeal R⁰ <| FractionRing R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `RingEquivClass.toRingHomClass`：∀ {F : Type u_1} {R : Type u_4} {S : Type
 u_5} [inst : EquivLike F R S] [inst_1 : NonAssocSemiring R]   [inst_2 : NonAsso
cSemiring S] [h : R…
· 使用定理 `RingEquiv.instRingEquivClass`：∀ {R : Type u_4} {S : Type u_5} [inst : Mu
l R] [inst_1 : Mul S] [inst_2 : Add R] [inst_3 : Add S],   RingEquivClass (R ≃+*
 S) R S
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `ClassGroup.mk_def`：ClassGroup.mk_def (I : (FractionalIdeal R⁰ K)ˣ) : Cla
ssGroup.mk K I = (QuotientGroup.mk' (toPrincipalIdeal R (FractionRing R)).range)
 (Units…
· 使用定理 `QuotientGroup.mk'_eq_mk'`：∀ {G : Type u_1} [inst : Group G] (N : Subgrou
p G) [nN : N.Normal] {x y : G},   (QuotientGroup.mk' N) x = (QuotientGroup.mk' N
) y ↔ ∃ z ∈ N,…
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `MonoidHomClass.toMonoidHom.congr_simp`：∀ {M : Type u_4} {N : Type u_5} {
F : Type u_9} [inst : MulOne M] [inst_1 : MulOne N] [inst_2 : FunLike F M N]   [
inst_3 : MonoidHomClass F M…
· 使用定理 `FractionalIdeal.canonicalEquiv_self`：canonicalEquiv_self : canonicalEqui
v S P P = RingEquiv.refl _
· 使用定理 `Units.map_id`：map_id : map (MonoidHom.id M) = MonoidHom.id Mˣ
· 使用定理 `MonoidHom.id_apply`：∀ (M : Type u_10) [inst : MulOne M] (x : M), (Monoid
Hom.id M) x = x
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
theorem ClassGroup.mk_eq_mk {I J : (FractionalIdeal R⁰ <| FractionRing R)ˣ} :
    ClassGroup.mk (FractionRing R) I = ClassGroup.mk (FractionRing R) J ↔
      ∃ x : (FractionRing R)ˣ, I * toPrincipalIdeal R (FractionRing R) x = J := by
  rw [mk_def, mk_def, QuotientGroup.mk'_eq_mk']
  simp [RingEquiv.coe_monoidHom_refl, MonoidHom.mem_range, -toPrincipalIdeal_eq_iff]
/-
**ClassGroup.mk_eq_mk_of_coe_ideal** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.mk_eq_mk_of_coe_ideal {I J : (FractionalIdeal R⁰ <| FractionRin
g R)ˣ} {I' J' : Ideal R} (hI : (I : FractionalIdeal R⁰ <| FractionRing R) = I') 
(hJ : (J : FractionalIdeal R⁰ <| FractionRing R) = J') : ClassGroup.mk (Fraction
Ring R) I = ClassGroup.mk (FractionRing R) J ↔ exists x y : R, x != 0 ∧ y != 0 ∧
 Ideal.span {x} * I' = Ideal.span {y} * J'
参数：FractionalIdeal R⁰ <| FractionRing R；hI : (I : FractionalIdeal R⁰ <| Fraction
Ring R) = I'；hJ : (J : FractionalIdeal R⁰ <| FractionRing R) = J'。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `ClassGroup.mk_eq_mk`：ClassGroup.mk_eq_mk {I J : (FractionalIdeal R⁰ <| F
ractionRing R)ˣ} : ClassGroup.mk (FractionRing R) I = ClassGroup.mk (FractionRin
g R) J ↔ …
· 使用定理 `IsLocalization.sec_fst_ne_zero`：sec_fst_ne_zero {x : S} (hx : x != 0) : 
(sec M x).fst != 0
· 使用定理 `Units.ne_zero`：ne_zero [Nontrivial M₀] (u : M₀ˣ) : (u : M₀) != 0
· 使用定理 `FractionRing.instNontrivial`：∀ (R : Type u_1) [inst : CommRing R] [Nontr
ivial R], Nontrivial (FractionRing R)
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `IsLocalization.sec_snd_ne_zero`：sec_snd_ne_zero [Nontrivial R] (hM : M <
= nonZeroDivisors R) (x : S) : ((sec M x).snd : R) != 0
· 使用引理 `le_rfl`：le_rfl : a <= a
· 使用定理 `FractionalIdeal.spanSingleton_mul_coeIdeal_eq_coeIdeal`：spanSingleton_mu
l_coeIdeal_eq_coeIdeal {I J : Ideal R₁} {z : K} : spanSingleton R₁⁰ z * (I : Fra
ctionalIdeal R₁⁰ K) = J ↔ Ideal.span {((IsLo…
· 使用定理 `mul_comm`：mul_comm : forall a b : G, a * b = b * a
· 使用定理 `coe_toPrincipalIdeal`：coe_toPrincipalIdeal (x : Kˣ) : (toPrincipalIdeal 
R K x : FractionalIdeal R⁰ K) = spanSingleton _ (x : K)
· 使用定理 `Units.val_mul`：val_mul : (↑(a * b) : α) = a * b
· 使用引理 `IsLocalization.mk'`：IsLocalization.mk'_algebraMap_eq_mk' [IsLocalization
 (Algebra.algebraMapSubmonoid A S) Aₛ] {x : A} {s : S} : IsLocalization.mk' Aₛ x
 ⟨_, Alg…
· 使用定理 `mem_nonZeroDivisors_of_ne_zero`：mem_nonZeroDivisors_of_ne_zero (hx : x !
= 0) : x in M₀⁰
· 使用定理 `IsDomain.to_noZeroDivisors`：∀ (α : Type u_3) [inst : Semiring α] [IsDoma
in α], NoZeroDivisors α
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Units.val_inj`：val_inj {a b : αˣ} : (a : α) = b ↔ a = b
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `FractionalIdeal.mk'_mul_coeIdeal_eq_coeIdeal`：∀ {R₁ : Type u_3} [inst : 
CommRing R₁] (K : Type u_4) [inst_1 : Field K] [inst_2 : Algebra R₁ K]   [inst_3
 : IsFractionRing R₁ K] {I J : Ide…
-/
theorem ClassGroup.mk_eq_mk_of_coe_ideal {I J : (FractionalIdeal R⁰ <| FractionRing R)ˣ}
    {I' J' : Ideal R} (hI : (I : FractionalIdeal R⁰ <| FractionRing R) = I')
    (hJ : (J : FractionalIdeal R⁰ <| FractionRing R) = J') :
    ClassGroup.mk (FractionRing R) I = ClassGroup.mk (FractionRing R) J ↔
      ∃ x y : R, x ≠ 0 ∧ y ≠ 0 ∧ Ideal.span {x} * I' = Ideal.span {y} * J' := by
  rw [ClassGroup.mk_eq_mk]
  constructor
  · rintro ⟨x, rfl⟩
    rw [Units.val_mul, hI, coe_toPrincipalIdeal, mul_comm,
      spanSingleton_mul_coeIdeal_eq_coeIdeal] at hJ
    exact ⟨_, _, sec_fst_ne_zero x.ne_zero,
      sec_snd_ne_zero (R := R) le_rfl (x : FractionRing R), hJ⟩
  · rintro ⟨x, y, hx, hy, h⟩
    have : IsUnit (mk' (FractionRing R) x ⟨y, mem_nonZeroDivisors_of_ne_zero hy⟩) := by
      simpa only [isUnit_iff_ne_zero, ne_eq, mk'_eq_zero_iff_eq_zero] using hx
    refine ⟨this.unit, ?_⟩
    rw [mul_comm, ← Units.val_inj, Units.val_mul, coe_toPrincipalIdeal]
    convert!
      (mk'_mul_coeIdeal_eq_coeIdeal (FractionRing R) <| mem_nonZeroDivisors_of_ne_zero hy).2 h
/-
**ClassGroup.mk_eq_one_of_coe_ideal** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.mk_eq_one_of_coe_ideal {I : (FractionalIdeal R⁰ <| FractionRing
 R)ˣ} {I' : Ideal R} (hI : (I : FractionalIdeal R⁰ <| FractionRing R) = I') : Cl
assGroup.mk (FractionRing R) I = 1 ↔ exists x : R, x != 0 ∧ I' = Ideal.span {x}
参数：FractionalIdeal R⁰ <| FractionRing R；hI : (I : FractionalIdeal R⁰ <| Fraction
Ring R) = I'。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `map_one`：map_one [OneHomClass F M N] (f : F) : f 1 = 1
· 使用定理 `MonoidHomClass.toOneHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `ClassGroup.mk_eq_mk_of_coe_ideal`：ClassGroup.mk_eq_mk_of_coe_ideal {I J 
: (FractionalIdeal R⁰ <| FractionRing R)ˣ} {I' J' : Ideal R} (hI : (I : Fraction
alIdeal R⁰ <| Fraction…
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Ideal.mem_span_singleton_mul`：mem_span_singleton_mul {x y : R} {I : Idea
l R} : x in span {y} * I ↔ exists z in I, y * z = x
· 使用定理 `Ideal.span_singleton_le_iff_mem`：span_singleton_le_iff_mem {x : α} : spa
n {x} <= I ↔ x in I
· 使用定理 `Eq.ge`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a = b → b ≤ a
· 使用定理 `Ideal.mul_top`：mul_top [I.IsTwoSided] : I * ⊤ = I
· 使用定理 `Ideal.instIsTwoSided_1`：∀ {α : Type u_1} [inst : CommRing α] (I : Ideal 
α), I.IsTwoSided
· 使用定理 `Ideal.span_singleton_mul_right_inj`：span_singleton_mul_right_inj [IsDoma
in R] {x : R} (hx : x != 0) : span {x} * I = span {x} * J ↔ I = J
· 使用定理 `Ideal.span_singleton_mul_span_singleton`：span_singleton_mul_span_singlet
on (r s : R) [(span {r}).IsTwoSided] : span {r} * span {s} = (span {r * s} : Ide
al R)
· 使用定理 `one_ne_zero`：∀ {α : Type u_2} [inst : Zero α] [inst_1 : One α] [NeZero 1
], 1 ≠ 0
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `Ideal.span_singleton_one`：span_singleton_one : span ({1} : Set α) = ⊤
· 使用定理 `Ideal.top_mul`：top_mul : ⊤ * I = I
-/
theorem ClassGroup.mk_eq_one_of_coe_ideal {I : (FractionalIdeal R⁰ <| FractionRing R)ˣ}
    {I' : Ideal R} (hI : (I : FractionalIdeal R⁰ <| FractionRing R) = I') :
    ClassGroup.mk (FractionRing R) I = 1 ↔ ∃ x : R, x ≠ 0 ∧ I' = Ideal.span {x} := by
  rw [← map_one (ClassGroup.mk (R := R) (K := FractionRing R)),
    ClassGroup.mk_eq_mk_of_coe_ideal hI]
  any_goals rfl
  constructor
  · rintro ⟨x, y, hx, hy, h⟩
    rw [Ideal.mul_top] at h
    rcases Ideal.mem_span_singleton_mul.mp ((Ideal.span_singleton_le_iff_mem _).mp h.ge) with
      ⟨i, _hi, rfl⟩
    rw [← Ideal.span_singleton_mul_span_singleton, Ideal.span_singleton_mul_right_inj hx] at h
    exact ⟨i, by grobner, h⟩
  · rintro ⟨x, hx, rfl⟩
    exact ⟨1, x, one_ne_zero, hx, by rw [Ideal.span_singleton_one, Ideal.top_mul, Ideal.mul_top]⟩

variable (K)

/-- Induction principle for the class group: to show something holds for all `x : ClassGroup R`,
we can choose a fraction field `K` and show it holds for the equivalence class of each
`I : FractionalIdeal R⁰ K`. -/
@[elab_as_elim]
/-
**ClassGroup.induction** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.induction {P : ClassGroup R -> Prop} (h : forall I : (Fractiona
lIdeal R⁰ K)ˣ, P (ClassGroup.mk K I)) (x : ClassGroup R) : P x
参数：h : forall I : (FractionalIdeal R⁰ K)ˣ, P (ClassGroup.mk K I)；x : ClassGroup 
R。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `QuotientGroup.induction_on`：induction_on {C : α ⧸ s -> Prop} (x : α ⧸ s)
 (H : forall z, C (QuotientGroup.mk z)) : C x
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `RingEquivClass.toMulEquivClass`：∀ {F : Type u_7} {R : Type u_8} {S : Typ
e u_9} {inst : Mul R} {inst_1 : Add R} {inst_2 : Mul S} {inst_3 : Add S}   {inst
_4 : EquivLike F R S…
· 使用定理 `RingEquiv.instRingEquivClass`：∀ {R : Type u_4} {S : Type u_5} [inst : Mu
l R] [inst_1 : Mul S] [inst_2 : Add R] [inst_3 : Add S],   RingEquivClass (R ≃+*
 S) R S
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `FractionalIdeal.canonicalEquiv_canonicalEquiv`：canonicalEquiv_canonicalE
quiv (P'' : Type*) [CommRing P''] [Algebra R P''] [IsLocalization S P''] (I : Fr
actionalIdeal S P) : canonicalEquiv…
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `FractionalIdeal.canonicalEquiv_self`：canonicalEquiv_self : canonicalEqui
v S P P = RingEquiv.refl _
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `congr_arg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ 
→ f a₁ = f a₂

--- 原说明 ---
Induction principle for the class group: to show something holds for all `x : Cl
assGroup R`,
we can choose a fraction field `K` and show it holds for the equivalence class o
f each
`I : FractionalIdeal R⁰ K`.
-/
theorem ClassGroup.induction {P : ClassGroup R → Prop}
    (h : ∀ I : (FractionalIdeal R⁰ K)ˣ, P (ClassGroup.mk K I)) (x : ClassGroup R) : P x :=
  QuotientGroup.induction_on x fun I => by
    have : I = (Units.mapEquiv (canonicalEquiv R⁰ K (FractionRing R)).toMulEquiv)
      (Units.mapEquiv (canonicalEquiv R⁰ (FractionRing R) K).toMulEquiv I) := by
      simp [← Units.val_inj]
    rw [congr_arg (QuotientGroup.mk (s := (toPrincipalIdeal R (FractionRing R)).range)) this]
    exact h _

/-- The definition of the class group does not depend on the choice of field of fractions. -/
/-
**ClassGroup.equiv** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：ClassGroup.equiv : ClassGroup R ≃* (FractionalIdeal R⁰ K)ˣ ⧸ (toPrincipalI
deal R K).range
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The definition of the class group does not depend on the choice of field of frac
tions.
-/
def ClassGroup.equiv :
    ClassGroup R ≃* (FractionalIdeal R⁰ K)ˣ ⧸ (toPrincipalIdeal R K).range := by
  haveI : Subgroup.map
    (Units.mapEquiv (canonicalEquiv R⁰ (FractionRing R) K).toMulEquiv).toMonoidHom
    (toPrincipalIdeal R (FractionRing R)).range = (toPrincipalIdeal R K).range := by
    ext I
    simp only [Subgroup.mem_map, mem_principal_ideals_iff]
    constructor
    · rintro ⟨I, ⟨x, hx⟩, rfl⟩
      refine ⟨FractionRing.algEquiv R K x, ?_⟩
      simp only [RingEquiv.toMulEquiv_eq_coe, MulEquiv.coe_toMonoidHom, coe_mapEquiv, ← hx,
        RingEquiv.coe_toMulEquiv, canonicalEquiv_spanSingleton]
      rfl
    · rintro ⟨x, hx⟩
      refine ⟨Units.mapEquiv (canonicalEquiv R⁰ K (FractionRing R)).toMulEquiv I,
        ⟨(FractionRing.algEquiv R K).symm x, ?_⟩, Units.ext ?_⟩
      · simp only [RingEquiv.toMulEquiv_eq_coe, coe_mapEquiv, ← hx, RingEquiv.coe_toMulEquiv,
          canonicalEquiv_spanSingleton]
        rfl
      · simp only [RingEquiv.toMulEquiv_eq_coe, MulEquiv.coe_toMonoidHom, coe_mapEquiv,
          RingEquiv.coe_toMulEquiv, canonicalEquiv_canonicalEquiv, canonicalEquiv_self,
          RingEquiv.refl_apply]
  exact QuotientGroup.congr (toPrincipalIdeal R (FractionRing R)).range (toPrincipalIdeal R K).range
    (Units.mapEquiv (FractionalIdeal.canonicalEquiv R⁰ (FractionRing R) K).toMulEquiv) this

set_option backward.isDefEq.respectTransparency false in
@[simp]
/-
**ClassGroup.equiv_mk** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.equiv_mk (K' : Type*) [Field K'] [Algebra R K'] [IsFractionRing
 R K'] (I : (FractionalIdeal R⁰ K)ˣ) : ClassGroup.equiv K' (ClassGroup.mk K I) =
 QuotientGroup.mk' _ (Units.mapEquiv (↑(FractionalIdeal.canonicalEquiv R⁰ K K'))
 I)
参数：K' : Type*；I : (FractionalIdeal R⁰ K)ˣ。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `RingEquivClass.toMulEquivClass`：∀ {F : Type u_7} {R : Type u_8} {S : Typ
e u_9} {inst : Mul R} {inst_1 : Add R} {inst_2 : Mul S} {inst_3 : Add S}   {inst
_4 : EquivLike F R S…
· 使用定理 `RingEquiv.instRingEquivClass`：∀ {R : Type u_4} {S : Type u_5} [inst : Mu
l R] [inst_1 : Mul S] [inst_2 : Add R] [inst_3 : Add S],   RingEquivClass (R ≃+*
 S) R S
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `ClassGroup.equiv.eq_1`：∀ {R : Type u_1} (K : Type u_2) [inst : CommRing 
R] [inst_1 : Field K] [inst_2 : Algebra R K]   [inst_3 : IsFractionRing R K] [in
st_4 : IsDo…
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `RingEquivClass.toRingHomClass`：∀ {F : Type u_1} {R : Type u_4} {S : Type
 u_5} [inst : EquivLike F R S] [inst_1 : NonAssocSemiring R]   [inst_2 : NonAsso
cSemiring S] [h : R…
· 使用引理 `ClassGroup.mk_def`：ClassGroup.mk_def (I : (FractionalIdeal R⁰ K)ˣ) : Cla
ssGroup.mk K I = (QuotientGroup.mk' (toPrincipalIdeal R (FractionRing R)).range)
 (Units…
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `heq_of_eq`：∀ {α : Sort u_1} {a a' : α}, a = a' → a ≍ a'
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Units.val_inj`：val_inj {a b : αˣ} : (a : α) = b ↔ a = b
· 使用定理 `Units.coe_mapEquiv`：coe_mapEquiv (h : M ≃* N) (x : Mˣ) : (mapEquiv h x :
 N) = h x
· 使用定理 `Units.coe_map`：coe_map (f : M ->* N) (x : Mˣ) : ↑(map f x) = f x
· 使用定理 `FractionalIdeal.canonicalEquiv_canonicalEquiv`：canonicalEquiv_canonicalE
quiv (P'' : Type*) [CommRing P''] [Algebra R P''] [IsLocalization S P''] (I : Fr
actionalIdeal S P) : canonicalEquiv…
-/
theorem ClassGroup.equiv_mk (K' : Type*) [Field K'] [Algebra R K'] [IsFractionRing R K']
    (I : (FractionalIdeal R⁰ K)ˣ) :
    ClassGroup.equiv K' (ClassGroup.mk K I) =
      QuotientGroup.mk' _ (Units.mapEquiv (↑(FractionalIdeal.canonicalEquiv R⁰ K K')) I) := by
  -- `simp` can't apply `ClassGroup.mk_def` and `rw` can't unfold `ClassGroup`.
  rw [ClassGroup.equiv, ClassGroup.mk_def]
  simp only [ClassGroup, QuotientGroup.congr_mk']
  congr
  rw [← Units.val_inj, Units.coe_mapEquiv, Units.coe_mapEquiv, Units.coe_map]
  exact FractionalIdeal.canonicalEquiv_canonicalEquiv _ _ _ _ _

@[simp]
/-
**ClassGroup.mk_canonicalEquiv** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.mk_canonicalEquiv (K' : Type*) [Field K'] [Algebra R K'] [IsFra
ctionRing R K'] (I : (FractionalIdeal R⁰ K)ˣ) : ClassGroup.mk K' (Units.map (↑(c
anonicalEquiv R⁰ K K')) I : (FractionalIdeal R⁰ K')ˣ) = ClassGroup.mk K I
参数：K' : Type*；I : (FractionalIdeal R⁰ K)ˣ。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `RingEquivClass.toRingHomClass`：∀ {F : Type u_1} {R : Type u_4} {S : Type
 u_5} [inst : EquivLike F R S] [inst_1 : NonAssocSemiring R]   [inst_2 : NonAsso
cSemiring S] [h : R…
· 使用定理 `RingEquiv.instRingEquivClass`：∀ {R : Type u_4} {S : Type u_5} [inst : Mu
l R] [inst_1 : Mul S] [inst_2 : Add R] [inst_3 : Add S],   RingEquivClass (R ≃+*
 S) R S
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `ClassGroup.mk_def`：ClassGroup.mk_def (I : (FractionalIdeal R⁰ K)ˣ) : Cla
ssGroup.mk K I = (QuotientGroup.mk' (toPrincipalIdeal R (FractionRing R)).range)
 (Units…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MonoidHom.comp_apply`：MonoidHom.comp_apply [MulOne M] [MulOne N] [MulOne
 P] (g : N ->* P) (f : M ->* N) (x : M) : g.comp f x = g (f x)
· 使用定理 `Units.map_comp`：map_comp (f : M ->* N) (g : N ->* P) : map (g.comp f) = 
(map g).comp (map f)
· 使用定理 `RingEquiv.coe_monoidHom_trans`：coe_monoidHom_trans [NonAssocSemiring S']
 (e₁ : R ≃+* S) (e₂ : S ≃+* S') : (e₁.trans e₂ : R ->* S') = (e₂ : S ->* S').com
p ↑e₁
· 使用定理 `FractionalIdeal.canonicalEquiv_trans_canonicalEquiv`：canonicalEquiv_tran
s_canonicalEquiv (P'' : Type*) [CommRing P''] [Algebra R P''] [IsLocalization S 
P''] : (canonicalEquiv S P P').trans (can…
-/
theorem ClassGroup.mk_canonicalEquiv (K' : Type*) [Field K'] [Algebra R K'] [IsFractionRing R K']
    (I : (FractionalIdeal R⁰ K)ˣ) :
    ClassGroup.mk K' (Units.map (↑(canonicalEquiv R⁰ K K')) I : (FractionalIdeal R⁰ K')ˣ) =
      ClassGroup.mk K I := by
  rw [ClassGroup.mk_def, ClassGroup.mk_def, ← MonoidHom.comp_apply (Units.map _),
      ← Units.map_comp, ← RingEquiv.coe_monoidHom_trans,
      FractionalIdeal.canonicalEquiv_trans_canonicalEquiv]

set_option linter.overlappingInstances false

/-- Send a nonzero integral ideal to an invertible fractional ideal. -/
/-
**FractionalIdeal.mk0** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：FractionalIdeal.mk0 [IsDedekindDomain R] : (Ideal R)⁰ ->* (FractionalIdeal
 R⁰ K)ˣ where toFun I
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Send a nonzero integral ideal to an invertible fractional ideal.
-/
def FractionalIdeal.mk0 [IsDedekindDomain R] :
    (Ideal R)⁰ →* (FractionalIdeal R⁰ K)ˣ where
  toFun I := Units.mk0 I (coeIdeal_ne_zero.mpr <| mem_nonZeroDivisors_iff_ne_zero.mp I.2)
  map_one' := by simp
  map_mul' x y := by simp

@[simp]
/-
**FractionalIdeal.coe_mk0** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：FractionalIdeal.coe_mk0 [IsDedekindDomain R] (I : (Ideal R)⁰) : (Fractiona
lIdeal.mk0 K I : FractionalIdeal R⁰ K) = I
参数：I : (Ideal R)⁰。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem FractionalIdeal.coe_mk0 [IsDedekindDomain R] (I : (Ideal R)⁰) :
    (FractionalIdeal.mk0 K I : FractionalIdeal R⁰ K) = I := rfl
/-
**FractionalIdeal.canonicalEquiv_mk0** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：FractionalIdeal.canonicalEquiv_mk0 [IsDedekindDomain R] (K' : Type*) [Fiel
d K'] [Algebra R K'] [IsFractionRing R K'] (I : (Ideal R)⁰) : FractionalIdeal.ca
nonicalEquiv R⁰ K K' (FractionalIdeal.mk0 K I) = FractionalIdeal.mk0 K' I
参数：K' : Type*；I : (Ideal R)⁰。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `FractionalIdeal.canonicalEquiv_coeIdeal`：canonicalEquiv_coeIdeal (I : Id
eal R) : canonicalEquiv S P P' I = I
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem FractionalIdeal.canonicalEquiv_mk0 [IsDedekindDomain R] (K' : Type*) [Field K']
    [Algebra R K'] [IsFractionRing R K'] (I : (Ideal R)⁰) :
    FractionalIdeal.canonicalEquiv R⁰ K K' (FractionalIdeal.mk0 K I) =
    FractionalIdeal.mk0 K' I := by
  simp only [FractionalIdeal.coe_mk0, FractionalIdeal.canonicalEquiv_coeIdeal]

@[simp]
/-
**FractionalIdeal.map_canonicalEquiv_mk0** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：FractionalIdeal.map_canonicalEquiv_mk0 [IsDedekindDomain R] (K' : Type*) [
Field K'] [Algebra R K'] [IsFractionRing R K'] (I : (Ideal R)⁰) : Units.map (↑(F
ractionalIdeal.canonicalEquiv R⁰ K K')) (FractionalIdeal.mk0 K I) = FractionalId
eal.mk0 K' I
参数：K' : Type*；I : (Ideal R)⁰。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Units.ext`：ext {u v : αˣ} (huv : u.val = v.val) : u = v
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `RingEquivClass.toRingHomClass`：∀ {F : Type u_1} {R : Type u_4} {S : Type
 u_5} [inst : EquivLike F R S] [inst_1 : NonAssocSemiring R]   [inst_2 : NonAsso
cSemiring S] [h : R…
· 使用定理 `RingEquiv.instRingEquivClass`：∀ {R : Type u_4} {S : Type u_5} [inst : Mu
l R] [inst_1 : Mul S] [inst_2 : Add R] [inst_3 : Add S],   RingEquivClass (R ≃+*
 S) R S
· 使用定理 `FractionalIdeal.canonicalEquiv_mk0`：FractionalIdeal.canonicalEquiv_mk0 [
IsDedekindDomain R] (K' : Type*) [Field K'] [Algebra R K'] [IsFractionRing R K']
 (I : (Ideal R)⁰) : Frac…
-/
theorem FractionalIdeal.map_canonicalEquiv_mk0 [IsDedekindDomain R] (K' : Type*) [Field K']
    [Algebra R K'] [IsFractionRing R K'] (I : (Ideal R)⁰) :
    Units.map (↑(FractionalIdeal.canonicalEquiv R⁰ K K')) (FractionalIdeal.mk0 K I) =
      FractionalIdeal.mk0 K' I :=
  Units.ext (FractionalIdeal.canonicalEquiv_mk0 K K' I)

/-- Send a nonzero ideal to the corresponding class in the class group. -/
/-
**ClassGroup.mk0** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：ClassGroup.mk0 [IsDedekindDomain R] : (Ideal R)⁰ ->* ClassGroup R
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Send a nonzero ideal to the corresponding class in the class group.
-/
def ClassGroup.mk0 [IsDedekindDomain R] : (Ideal R)⁰ →* ClassGroup R :=
  (ClassGroup.mk (FractionRing R)).comp (FractionalIdeal.mk0 (FractionRing R))

@[simp]
/-
**ClassGroup.mk_mk0** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.mk_mk0 [IsDedekindDomain R] (I : (Ideal R)⁰) : ClassGroup.mk K 
(FractionalIdeal.mk0 K I) = ClassGroup.mk0 I
参数：I : (Ideal R)⁰。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `ClassGroup.mk0.eq_1`：∀ {R : Type u_1} [inst : CommRing R] [inst_1 : IsDo
main R] [inst_2 : IsDedekindDomain R],   ClassGroup.mk0 = (ClassGroup.mk (Fracti
onRing R)…
· 使用定理 `MonoidHom.comp_apply`：MonoidHom.comp_apply [MulOne M] [MulOne N] [MulOne
 P] (g : N ->* P) (f : M ->* N) (x : M) : g.comp f x = g (f x)
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `RingEquivClass.toRingHomClass`：∀ {F : Type u_1} {R : Type u_4} {S : Type
 u_5} [inst : EquivLike F R S] [inst_1 : NonAssocSemiring R]   [inst_2 : NonAsso
cSemiring S] [h : R…
· 使用定理 `RingEquiv.instRingEquivClass`：∀ {R : Type u_4} {S : Type u_5} [inst : Mu
l R] [inst_1 : Mul S] [inst_2 : Add R] [inst_3 : Add S],   RingEquivClass (R ≃+*
 S) R S
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `ClassGroup.mk_canonicalEquiv`：ClassGroup.mk_canonicalEquiv (K' : Type*) 
[Field K'] [Algebra R K'] [IsFractionRing R K'] (I : (FractionalIdeal R⁰ K)ˣ) : 
ClassGroup.mk K' (…
· 使用定理 `FractionalIdeal.map_canonicalEquiv_mk0`：FractionalIdeal.map_canonicalEqu
iv_mk0 [IsDedekindDomain R] (K' : Type*) [Field K'] [Algebra R K'] [IsFractionRi
ng R K'] (I : (Ideal R)⁰) : …
-/
theorem ClassGroup.mk_mk0 [IsDedekindDomain R] (I : (Ideal R)⁰) :
    ClassGroup.mk K (FractionalIdeal.mk0 K I) = ClassGroup.mk0 I := by
  rw [ClassGroup.mk0, MonoidHom.comp_apply, ← ClassGroup.mk_canonicalEquiv K (FractionRing R),
    FractionalIdeal.map_canonicalEquiv_mk0]

/-- `ClassGroup.mk0` factors through the canonical quotient projection on
`(FractionalIdeal R⁰ (FractionRing R))ˣ`. -/
/-
**ClassGroup.mk0_eq_quotientMk** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.mk0_eq_quotientMk [IsDedekindDomain R] (I : (Ideal R)⁰) : (Clas
sGroup.mk0 I : ClassGroup R) = QuotientGroup.mk (FractionalIdeal.mk0 (FractionRi
ng R) I)
参数：I : (Ideal R)⁰。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `ClassGroup.mk_mk0`：ClassGroup.mk_mk0 [IsDedekindDomain R] (I : (Ideal R)
⁰) : ClassGroup.mk K (FractionalIdeal.mk0 K I) = ClassGroup.mk0 I
· 使用定理 `ClassGroup.Quot_mk_eq_mk`：ClassGroup.Quot_mk_eq_mk (I : (FractionalIdeal
 R⁰ (FractionRing R))ˣ) : Quot.mk _ I = ClassGroup.mk (FractionRing R) I

--- 原说明 ---
`ClassGroup.mk0` factors through the canonical quotient projection on
`(FractionalIdeal R⁰ (FractionRing R))ˣ`.
-/
theorem ClassGroup.mk0_eq_quotientMk [IsDedekindDomain R] (I : (Ideal R)⁰) :
    (ClassGroup.mk0 I : ClassGroup R) =
      QuotientGroup.mk (FractionalIdeal.mk0 (FractionRing R) I) :=
  (ClassGroup.mk_mk0 (K := FractionRing R) I).symm.trans (ClassGroup.Quot_mk_eq_mk _).symm

@[simp]
/-
**ClassGroup.equiv_mk0** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.equiv_mk0 [IsDedekindDomain R] (I : (Ideal R)⁰) : ClassGroup.eq
uiv K (ClassGroup.mk0 I) = QuotientGroup.mk' (toPrincipalIdeal R K).range (Fract
ionalIdeal.mk0 K I)
参数：I : (Ideal R)⁰。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `ClassGroup.mk0.eq_1`：∀ {R : Type u_1} [inst : CommRing R] [inst_1 : IsDo
main R] [inst_2 : IsDedekindDomain R],   ClassGroup.mk0 = (ClassGroup.mk (Fracti
onRing R)…
· 使用定理 `MonoidHom.comp_apply`：MonoidHom.comp_apply [MulOne M] [MulOne N] [MulOne
 P] (g : N ->* P) (f : M ->* N) (x : M) : g.comp f x = g (f x)
· 使用定理 `RingEquivClass.toMulEquivClass`：∀ {F : Type u_7} {R : Type u_8} {S : Typ
e u_9} {inst : Mul R} {inst_1 : Add R} {inst_2 : Mul S} {inst_3 : Add S}   {inst
_4 : EquivLike F R S…
· 使用定理 `RingEquiv.instRingEquivClass`：∀ {R : Type u_4} {S : Type u_5} [inst : Mu
l R] [inst_1 : Mul S] [inst_2 : Add R] [inst_3 : Add S],   RingEquivClass (R ≃+*
 S) R S
· 使用定理 `ClassGroup.equiv_mk`：ClassGroup.equiv_mk (K' : Type*) [Field K'] [Algebr
a R K'] [IsFractionRing R K'] (I : (FractionalIdeal R⁰ K)ˣ) : ClassGroup.equiv K
' (ClassG…
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `heq_of_eq`：∀ {α : Sort u_1} {a a' : α}, a = a' → a ≍ a'
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `FractionalIdeal.canonicalEquiv_coeIdeal`：canonicalEquiv_coeIdeal (I : Id
eal R) : canonicalEquiv S P P' I = I
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem ClassGroup.equiv_mk0 [IsDedekindDomain R] (I : (Ideal R)⁰) :
    ClassGroup.equiv K (ClassGroup.mk0 I) =
      QuotientGroup.mk' (toPrincipalIdeal R K).range (FractionalIdeal.mk0 K I) := by
  rw [ClassGroup.mk0, MonoidHom.comp_apply, ClassGroup.equiv_mk]
  congr 1
  simp [← Units.val_inj]
/-
**ClassGroup.mk0_eq_mk0_iff_exists_fraction_ring** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.mk0_eq_mk0_iff_exists_fraction_ring [IsDedekindDomain R] {I J :
 (Ideal R)⁰} : ClassGroup.mk0 I = ClassGroup.mk0 J ↔ exists (x : _) (_ : x != (0
 : K)), spanSingleton R⁰ x * I = J
参数：Ideal R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `Iff.symm`：∀ {a b : Prop}, (a ↔ b) → (b ↔ a)
· 使用定理 `Function.Injective.eq_iff`：∀ {α : Sort u_1} {β : Sort u_2} {f : α → β}, 
Function.Injective f → ∀ {a b : α}, f a = f b ↔ a = b
· 使用定理 `MulEquiv.injective`：∀ {M : Type u_4} {N : Type u_5} [inst : Mul M] [inst
_1 : Mul N] (e : M ≃* N), Function.Injective ⇑e
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `ClassGroup.equiv_mk0`：ClassGroup.equiv_mk0 [IsDedekindDomain R] (I : (Id
eal R)⁰) : ClassGroup.equiv K (ClassGroup.mk0 I) = QuotientGroup.mk' (toPrincipa
lIdeal R K…
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `FractionalIdeal.spanSingleton_zero`：spanSingleton_zero : spanSingleton S
 (0 : P) = 0
· 使用定理 `eq_false`：∀ {p : Prop}, ¬p → p = False
· 使用定理 `Ne.symm`：∀ {α : Sort u} {a b : α}, a ≠ b → b ≠ a
· 使用定理 `Units.ne_zero`：ne_zero [Nontrivial M₀] (u : M₀ˣ) : (u : M₀) != 0
· 使用定理 `FractionalIdeal.instNontrivialNonZeroDivisors`：∀ {R₁ : Type u_3} [inst :
 CommRing R₁] {K : Type u_4} [inst_1 : Field K] [inst_2 : Algebra R₁ K],   Nontr
ivial (FractionalIdeal (nonZeroDivi…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `mul_comm`：mul_comm : forall a b : G, a * b = b * a
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `FractionalIdeal.spanSingleton_ne_zero_iff`：spanSingleton_ne_zero_iff {y 
: P} : spanSingleton S y != 0 ↔ y != 0
-/
theorem ClassGroup.mk0_eq_mk0_iff_exists_fraction_ring [IsDedekindDomain R] {I J : (Ideal R)⁰} :
    ClassGroup.mk0 I =
      ClassGroup.mk0 J ↔ ∃ (x : _) (_ : x ≠ (0 : K)), spanSingleton R⁰ x * I = J := by
  refine (ClassGroup.equiv K).injective.eq_iff.symm.trans ?_
  simp only [ClassGroup.equiv_mk0, QuotientGroup.mk'_eq_mk', mem_principal_ideals_iff,
    Units.ext_iff, Units.val_mul, FractionalIdeal.coe_mk0, exists_prop]
  constructor
  · rintro ⟨X, ⟨x, hX⟩, hx⟩
    refine ⟨x, ?_, ?_⟩
    · rintro rfl; simp [X.ne_zero.symm] at hX
    simpa only [hX, mul_comm] using! hx
  · rintro ⟨x, hx, eq_J⟩
    refine ⟨Units.mk0 _ (spanSingleton_ne_zero_iff.mpr hx), ⟨x, rfl⟩, ?_⟩
    simpa only [mul_comm] using! eq_J

variable {K}
/-
**ClassGroup.mk0_eq_mk0_iff** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.mk0_eq_mk0_iff [IsDedekindDomain R] {I J : (Ideal R)⁰} : ClassG
roup.mk0 I = ClassGroup.mk0 J ↔ exists (x y : R) (_hx : x != 0) (_hy : y != 0), 
Ideal.span {x} * (I : Ideal R) = Ideal.span {y} * J
参数：Ideal R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `ClassGroup.mk0_eq_mk0_iff_exists_fraction_ring`：ClassGroup.mk0_eq_mk0_if
f_exists_fraction_ring [IsDedekindDomain R] {I J : (Ideal R)⁰} : ClassGroup.mk0 
I = ClassGroup.mk0 J ↔ exists (x : _…
· 使用引理 `IsLocalization.mk'`：IsLocalization.mk'_algebraMap_eq_mk' [IsLocalization
 (Algebra.algebraMapSubmonoid A S) Aₛ] {x : A} {s : S} : IsLocalization.mk' Aₛ x
 ⟨_, Alg…
· 使用定理 `IsLocalization.exists_mk'_eq`：∀ {R : Type u_1} [inst : CommSemiring R] (
M : Submonoid R) {S : Type u_2} [inst_1 : CommSemiring S]   [inst_2 : Algebra R 
S] [inst_3 : IsLoc…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `IsFractionRing.mk'_eq_div`：∀ {A : Type u_4} [inst : CommRing A] {K : Typ
e u_5} [inst_1 : Field K] [inst_2 : Algebra A K]   [inst_3 : IsFractionRing A K]
 {r : A} (s : ↥…
· 使用定理 `map_zero`：∀ {M : Type u_4} {N : Type u_5} {F : Type u_9} [inst : Zero M]
 [inst_1 : Zero N] [inst_2 : FunLike F M N]   [ZeroHomClass F M N] (f : F), f …
· 使用定理 `MonoidWithZeroHomClass.toZeroHomClass`：∀ {F : Type u_7} {α : outParam (T
ype u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : MulZe
roOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `IsDomain.to_noZeroDivisors`：∀ (α : Type u_3) [inst : Semiring α] [IsDoma
in α], NoZeroDivisors α
· 使用定理 `zero_div`：zero_div (a : G₀) : 0 / a = 0
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `mem_nonZeroDivisors_iff_ne_zero`：∀ {M₀ : Type u_2} [inst : MonoidWithZer
o M₀] {x : M₀} [NoZeroDivisors M₀] [Nontrivial M₀],   x ∈ nonZeroDivisors M₀ ↔ x
 ≠ 0
· 使用定理 `FractionalIdeal.mk'_mul_coeIdeal_eq_coeIdeal`：∀ {R₁ : Type u_3} [inst : 
CommRing R₁] (K : Type u_4) [inst_1 : Field K] [inst_2 : Algebra R₁ K]   [inst_3
 : IsFractionRing R₁ K] {I J : Ide…
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用引理 `Mathlib.Tactic.Contrapose.contrapose₄`：contrapose₄ {p q : Prop} : (q -> 
p) -> (¬ p -> ¬ q)
· 使用定理 `Function.Injective.eq_iff`：∀ {α : Sort u_1} {β : Sort u_2} {f : α → β}, 
Function.Injective f → ∀ {a b : α}, f a = f b ↔ a = b
· 使用定理 `IsFractionRing.injective`：∀ (R : Type u_1) [inst : CommRing R] (K : Type
 u_5) [inst_1 : CommRing K] [inst_2 : Algebra R K] [IsFractionRing R K],   Funct
ion.Injective …
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `RingHom.map_zero`：∀ {α : Type u_2} {β : Type u_3} {x : NonAssocSemiring 
α} {x_1 : NonAssocSemiring β} (f : α →+* β), f 0 = 0
· 使用定理 `MulZeroClass.zero_mul`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, 0 * a = 0
· 使用定理 `IsLocalization.mk'_eq_iff_eq_mul`：∀ {R : Type u_1} [inst : CommSemiring 
R] {M : Submonoid R} {S : Type u_2} [inst_1 : CommSemiring S]   [inst_2 : Algebr
a R S] [inst_3 : IsLoc…
-/
theorem ClassGroup.mk0_eq_mk0_iff [IsDedekindDomain R] {I J : (Ideal R)⁰} :
    ClassGroup.mk0 I = ClassGroup.mk0 J ↔
      ∃ (x y : R) (_hx : x ≠ 0) (_hy : y ≠ 0), Ideal.span {x} * (I : Ideal R) =
      Ideal.span {y} * J := by
  refine (ClassGroup.mk0_eq_mk0_iff_exists_fraction_ring (FractionRing R)).trans ⟨?_, ?_⟩
  · rintro ⟨z, hz, h⟩
    obtain ⟨x, ⟨y, hy⟩, rfl⟩ := IsLocalization.exists_mk'_eq R⁰ z
    refine ⟨x, y, ?_, mem_nonZeroDivisors_iff_ne_zero.mp hy, ?_⟩
    · rintro hx; apply hz
      rw [hx, IsFractionRing.mk'_eq_div, map_zero, zero_div]
    · exact (FractionalIdeal.mk'_mul_coeIdeal_eq_coeIdeal _ hy).mp h
  · rintro ⟨x, y, hx, hy, h⟩
    have hy' : y ∈ R⁰ := mem_nonZeroDivisors_iff_ne_zero.mpr hy
    refine ⟨IsLocalization.mk' _ x ⟨y, hy'⟩, ?_, ?_⟩
    · contrapose hx
      rwa [mk'_eq_iff_eq_mul, zero_mul, ← (algebraMap R (FractionRing R)).map_zero,
        (IsFractionRing.injective R (FractionRing R)).eq_iff] at hx
    · exact (FractionalIdeal.mk'_mul_coeIdeal_eq_coeIdeal _ hy').mpr h

/-- A fractional ideal is a unit iff its integral numerator `num` is a unit. -/
/-
**FractionalIdeal.isUnit_num** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：FractionalIdeal.isUnit_num {I : FractionalIdeal R⁰ (FractionRing R)} : IsU
nit (I.num : FractionalIdeal R⁰ (FractionRing R)) ↔ IsUnit I
参数：FractionRing R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsFractionRing.to_map_ne_zero_of_mem_nonZeroDivisors`：∀ {R : Type u_1} [
inst : CommRing R] {K : Type u_5} [inst_1 : CommRing K] [inst_2 : Algebra R K] [
IsFractionRing R K]   [Nontrivial R] {x : …
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `Subtype.prop`：prop (x : Subtype p) : p x
· 使用定理 `IsDomain.to_noZeroDivisors`：∀ (α : Type u_3) [inst : Semiring α] [IsDoma
in α], NoZeroDivisors α
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `coe_toPrincipalIdeal`：coe_toPrincipalIdeal (x : Kˣ) : (toPrincipalIdeal 
R K x : FractionalIdeal R⁰ K) = spanSingleton _ (x : K)
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `FractionalIdeal.den_mul_self_eq_num'`：den_mul_self_eq_num' (I : Fraction
alIdeal S P) : spanSingleton S (algebraMap R P I.den) * I = I.num
· 使用定理 `Units.isUnit_units_mul`：Units.isUnit_units_mul {M : Type*} [Monoid M] (u
 : Mˣ) (a : M) : IsUnit (↑u * a) ↔ IsUnit a
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a

--- 原说明 ---
A fractional ideal is a unit iff its integral numerator `num` is a unit.
-/
theorem FractionalIdeal.isUnit_num {I : FractionalIdeal R⁰ (FractionRing R)} :
    IsUnit (I.num : FractionalIdeal R⁰ (FractionRing R)) ↔ IsUnit I := by
  have hden0 : algebraMap R (FractionRing R) I.den ≠ 0 :=
    IsFractionRing.to_map_ne_zero_of_mem_nonZeroDivisors I.den.prop
  let u : (FractionRing R)ˣ := Units.mk0 (algebraMap R (FractionRing R) I.den) hden0
  have hdenUnit : IsUnit (spanSingleton R⁰ (algebraMap R (FractionRing R) I.den)) :=
    ⟨toPrincipalIdeal R (FractionRing R) u, by simp [u]⟩
  obtain ⟨c, hc⟩ := hdenUnit
  rw [← den_mul_self_eq_num', ← hc, Units.isUnit_units_mul]

/-- Maps a nonzero fractional ideal to an integral representative in the class group. -/
/-
**ClassGroup.integralRep** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：ClassGroup.integralRep (I : FractionalIdeal R⁰ (FractionRing R)) : Ideal R
参数：I : FractionalIdeal R⁰ (FractionRing R)。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Maps a nonzero fractional ideal to an integral representative in the class group
.
-/
def ClassGroup.integralRep (I : FractionalIdeal R⁰ (FractionRing R)) :
    Ideal R := I.num
/-
**ClassGroup.integralRep_mem_nonZeroDivisors** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.integralRep_mem_nonZeroDivisors {I : FractionalIdeal R⁰ (Fracti
onRing R)} (hI : I != 0) : I.num in (Ideal R)⁰
参数：FractionRing R；hI : I != 0。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mem_nonZeroDivisors_iff_ne_zero`：∀ {M₀ : Type u_2} [inst : MonoidWithZer
o M₀] {x : M₀} [NoZeroDivisors M₀] [Nontrivial M₀],   x ∈ nonZeroDivisors M₀ ↔ x
 ≠ 0
· 使用定理 `Submodule.instNoZeroDivisors`：∀ {R : Type u} [inst : Semiring R] {A : Ty
pe v} [inst_1 : Semiring A] [inst_2 : _root_.Module R A]   [inst_3 : IsScalarTow
er R A A] [NoZeroD…
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `IsDomain.to_noZeroDivisors`：∀ (α : Type u_3) [inst : Semiring α] [IsDoma
in α], NoZeroDivisors α
· 使用定理 `Ideal.instNontrivial`：∀ {α : Type u} [inst : Semiring α] [Nontrivial α],
 Nontrivial (Ideal α)
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `ne_eq`：∀ {α : Sort u_1} (a b : α), (a ≠ b) = ¬a = b
· 使用定理 `FractionalIdeal.num_eq_zero_iff`：num_eq_zero_iff [IsDomain R] {I : Fract
ionalIdeal R⁰ K} : I.num = 0 ↔ I = 0 where mp h
-/
theorem ClassGroup.integralRep_mem_nonZeroDivisors
    {I : FractionalIdeal R⁰ (FractionRing R)} (hI : I ≠ 0) :
    I.num ∈ (Ideal R)⁰ := by
  rwa [mem_nonZeroDivisors_iff_ne_zero, ne_eq, FractionalIdeal.num_eq_zero_iff]
/-
**ClassGroup.mk0_integralRep** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.mk0_integralRep [IsDedekindDomain R] (I : (FractionalIdeal R⁰ (
FractionRing R))ˣ) : ClassGroup.mk0 ⟨ClassGroup.integralRep I, ClassGroup.integr
alRep_mem_nonZeroDivisors I.ne_zero⟩ = ClassGroup.mk (FractionRing R) I
参数：I : (FractionalIdeal R⁰ (FractionRing R))ˣ。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `ClassGroup.integralRep_mem_nonZeroDivisors`：ClassGroup.integralRep_mem_n
onZeroDivisors {I : FractionalIdeal R⁰ (FractionRing R)} (hI : I != 0) : I.num i
n (Ideal R)⁰
· 使用定理 `Units.ne_zero`：ne_zero [Nontrivial M₀] (u : M₀ˣ) : (u : M₀) != 0
· 使用定理 `FractionalIdeal.instNontrivialNonZeroDivisors`：∀ {R₁ : Type u_3} [inst :
 CommRing R₁] {K : Type u_4} [inst_1 : Field K] [inst_2 : Algebra R₁ K],   Nontr
ivial (FractionalIdeal (nonZeroDivi…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `ClassGroup.mk_mk0`：ClassGroup.mk_mk0 [IsDedekindDomain R] (I : (Ideal R)
⁰) : ClassGroup.mk K (FractionalIdeal.mk0 K I) = ClassGroup.mk0 I
· 使用定理 `eq_comm`：∀ {α : Sort u_1} {a b : α}, a = b ↔ b = a
· 使用定理 `ClassGroup.mk_eq_mk`：ClassGroup.mk_eq_mk {I J : (FractionalIdeal R⁰ <| F
ractionRing R)ˣ} : ClassGroup.mk (FractionRing R) I = ClassGroup.mk (FractionRin
g R) J ↔ …
· 使用定理 `IsFractionRing.to_map_ne_zero_of_mem_nonZeroDivisors`：∀ {R : Type u_1} [
inst : CommRing R] {K : Type u_5} [inst_1 : CommRing K] [inst_2 : Algebra R K] [
IsFractionRing R K]   [Nontrivial R] {x : …
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `SetLike.coe_mem`：coe_mem (x : p) : (x : B) in p
· 使用定理 `IsDomain.to_noZeroDivisors`：∀ (α : Type u_3) [inst : Semiring α] [IsDoma
in α], NoZeroDivisors α
· 使用定理 `Units.ext`：ext {u v : αˣ} (huv : u.val = v.val) : u = v
· 使用定理 `mul_comm`：mul_comm : forall a b : G, a * b = b * a
· 使用定理 `Units.val_mul`：val_mul : (↑(a * b) : α) = a * b
· 使用定理 `coe_toPrincipalIdeal`：coe_toPrincipalIdeal (x : Kˣ) : (toPrincipalIdeal 
R K x : FractionalIdeal R⁰ K) = spanSingleton _ (x : K)
· 使用定理 `Units.val_mk0`：val_mk0 {a : G₀} (h : a != 0) : (mk0 a h : G₀) = a
· 使用定理 `FractionalIdeal.den_mul_self_eq_num'`：den_mul_self_eq_num' (I : Fraction
alIdeal S P) : spanSingleton S (algebraMap R P I.den) * I = I.num
-/
theorem ClassGroup.mk0_integralRep [IsDedekindDomain R]
    (I : (FractionalIdeal R⁰ (FractionRing R))ˣ) :
    ClassGroup.mk0 ⟨ClassGroup.integralRep I, ClassGroup.integralRep_mem_nonZeroDivisors I.ne_zero⟩
      = ClassGroup.mk (FractionRing R) I := by
  rw [← ClassGroup.mk_mk0 (FractionRing R), eq_comm, ClassGroup.mk_eq_mk]
  have fd_ne_zero : (algebraMap R (FractionRing R)) I.1.den ≠ 0 := by
    exact IsFractionRing.to_map_ne_zero_of_mem_nonZeroDivisors (SetLike.coe_mem _)
  refine ⟨Units.mk0 (algebraMap R _ I.1.den) fd_ne_zero, ?_⟩
  apply Units.ext
  rw [mul_comm, val_mul, coe_toPrincipalIdeal, val_mk0]
  exact FractionalIdeal.den_mul_self_eq_num' R⁰ (FractionRing R) I
/-
**ClassGroup.mk0_surjective** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.mk0_surjective [IsDedekindDomain R] : Function.Surjective (Clas
sGroup.mk0 : (Ideal R)⁰ -> ClassGroup R)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `ClassGroup.integralRep_mem_nonZeroDivisors`：ClassGroup.integralRep_mem_n
onZeroDivisors {I : FractionalIdeal R⁰ (FractionRing R)} (hI : I != 0) : I.num i
n (Ideal R)⁰
· 使用定理 `Units.ne_zero`：ne_zero [Nontrivial M₀] (u : M₀ˣ) : (u : M₀) != 0
· 使用定理 `FractionalIdeal.instNontrivialNonZeroDivisors`：∀ {R₁ : Type u_3} [inst :
 CommRing R₁] {K : Type u_4} [inst_1 : Field K] [inst_2 : Algebra R₁ K],   Nontr
ivial (FractionalIdeal (nonZeroDivi…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `ClassGroup.mk0_integralRep`：ClassGroup.mk0_integralRep [IsDedekindDomain
 R] (I : (FractionalIdeal R⁰ (FractionRing R))ˣ) : ClassGroup.mk0 ⟨ClassGroup.in
tegralRep I, Cla…
· 使用定理 `ClassGroup.Quot_mk_eq_mk`：ClassGroup.Quot_mk_eq_mk (I : (FractionalIdeal
 R⁰ (FractionRing R))ˣ) : Quot.mk _ I = ClassGroup.mk (FractionRing R) I
-/
theorem ClassGroup.mk0_surjective [IsDedekindDomain R] :
    Function.Surjective (ClassGroup.mk0 : (Ideal R)⁰ → ClassGroup R) := by
  rintro ⟨I⟩
  refine ⟨⟨ClassGroup.integralRep I.1, ClassGroup.integralRep_mem_nonZeroDivisors I.ne_zero⟩, ?_⟩
  rw [ClassGroup.mk0_integralRep, ClassGroup.Quot_mk_eq_mk]
/-
**ClassGroup.mk_eq_one_iff** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.mk_eq_one_iff {I : (FractionalIdeal R⁰ K)ˣ} : ClassGroup.mk K I
 = 1 ↔ (I : Submodule R K).IsPrincipal
参数：FractionalIdeal R⁰ K。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Function.Injective.eq_iff`：∀ {α : Sort u_1} {β : Sort u_2} {f : α → β}, 
Function.Injective f → ∀ {a b : α}, f a = f b ↔ a = b
· 使用定理 `MulEquiv.injective`：∀ {M : Type u_4} {N : Type u_5} [inst : Mul M] [inst
_1 : Mul N] (e : M ≃* N), Function.Injective ⇑e
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `RingEquivClass.toMulEquivClass`：∀ {F : Type u_7} {R : Type u_8} {S : Typ
e u_9} {inst : Mul R} {inst_1 : Add R} {inst_2 : Mul S} {inst_3 : Add S}   {inst
_4 : EquivLike F R S…
· 使用定理 `RingEquiv.instRingEquivClass`：∀ {R : Type u_4} {S : Type u_5} [inst : Mu
l R] [inst_1 : Mul S] [inst_2 : Add R] [inst_3 : Add S],   RingEquivClass (R ≃+*
 S) R S
· 使用定理 `ClassGroup.equiv_mk`：ClassGroup.equiv_mk (K' : Type*) [Field K'] [Algebr
a R K'] [IsFractionRing R K'] (I : (FractionalIdeal R⁰ K)ˣ) : ClassGroup.equiv K
' (ClassG…
· 使用定理 `MulEquivClass.toMulEquiv.congr_simp`：∀ {F : Type u_1} {α : Type u_2} {β 
: Type u_3} [inst : EquivLike F α β] [inst_1 : Mul α] [inst_2 : Mul β]   [inst_3
 : MulEquivClass F α β] (…
· 使用定理 `FractionalIdeal.canonicalEquiv_self`：canonicalEquiv_self : canonicalEqui
v S P P = RingEquiv.refl _
· 使用定理 `map_one`：map_one [OneHomClass F M N] (f : F) : f 1 = 1
· 使用定理 `MonoidHomClass.toOneHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `MulEquivClass.instMonoidHomClass`：∀ (F : Type u_1) {M : Type u_4} {N : T
ype u_5} [inst : EquivLike F M N] [inst_1 : MulOneClass M]   [inst_2 : MulOneCla
ss N] [MulEquivClass F…
· 使用定理 `MulEquiv.instMulEquivClass`：∀ {M : Type u_4} {N : Type u_5} [inst : Mul 
M] [inst_1 : Mul N], MulEquivClass (M ≃* N) M N
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `coe_toPrincipalIdeal`：coe_toPrincipalIdeal (x : Kˣ) : (toPrincipalIdeal 
R K x : FractionalIdeal R⁰ K) = spanSingleton _ (x : K)
· 使用定理 `FractionalIdeal.coe_spanSingleton`：coe_spanSingleton (x : P) : (spanSing
leton S x : Submodule R P) = span R {x}
· 使用定理 `Submodule.IsPrincipal.principal`：∀ {R : Type u_1} {M : Type u_4} {inst :
 Semiring R} {inst_1 : AddCommMonoid M} {inst_2 : _root_.Module R M}   (S : Subm
odule R M) [self : S.…
· 使用定理 `Subtype.coe_injective`：coe_injective : Injective (fun (a : Subtype p) =>
 (a : α))
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Units.ne_zero`：ne_zero [Nontrivial M₀] (u : M₀ˣ) : (u : M₀) != 0
· 使用定理 `FractionalIdeal.instNontrivialNonZeroDivisors`：∀ {R₁ : Type u_3} [inst :
 CommRing R₁] {K : Type u_4} [inst_1 : Field K] [inst_2 : Algebra R₁ K],   Nontr
ivial (FractionalIdeal (nonZeroDivi…
· 使用定理 `FractionalIdeal.spanSingleton.congr_simp`：∀ {R : Type u_5} [inst : CommR
ing R] (S : Submonoid R) {P : Type u_6} [inst_1 : CommRing P] [inst_2 : Algebra 
R P]   [inst_3 : IsLocalizatio…
· 使用定理 `FractionalIdeal.spanSingleton_zero`：spanSingleton_zero : spanSingleton S
 (0 : P) = 0
-/
theorem ClassGroup.mk_eq_one_iff {I : (FractionalIdeal R⁰ K)ˣ} :
    ClassGroup.mk K I = 1 ↔ (I : Submodule R K).IsPrincipal := by
  rw [← (ClassGroup.equiv K).injective.eq_iff]
  simp only [equiv_mk, canonicalEquiv_self, RingEquiv.coe_mulEquiv_refl, QuotientGroup.mk'_apply,
    map_one, QuotientGroup.eq_one_iff, MonoidHom.mem_range, Units.ext_iff,
    coe_toPrincipalIdeal, coe_mapEquiv, MulEquiv.refl_apply]
  refine ⟨fun ⟨x, hx⟩ => ⟨⟨x, by rw [← hx, coe_spanSingleton]⟩⟩, ?_⟩
  intro hI
  obtain ⟨x, hx⟩ := @Submodule.IsPrincipal.principal _ _ _ _ _ _ hI
  have hx' : (I : FractionalIdeal R⁰ K) = spanSingleton R⁰ x := by
    apply Subtype.coe_injective
    simp only [val_eq_coe, hx, coe_spanSingleton]
  refine ⟨Units.mk0 x ?_, ?_⟩
  · intro x_eq; apply Units.ne_zero I; simp [hx', x_eq]
  · simp [hx']

/-- A fractional ideal is principal if a power coprime to the class number is principal. -/
/-
**FractionalIdeal.isPrincipal.of_isPrincipal_pow_of_coprime** 是 Mathlib 中的一个定理，位
于命名空间 ``。
形式化陈述：FractionalIdeal.isPrincipal.of_isPrincipal_pow_of_coprime [IsDedekindDomai
n R] [Fintype (ClassGroup R)] {n : Nat} (hn : n.Coprime (Fintype.card (ClassGrou
p R))) (I : FractionalIdeal R⁰ K) (hI : ((I ^ n : FractionalIdeal R⁰ K) : Submod
ule R K).IsPrincipal) : (I : Submodule R K).IsPrincipal
参数：ClassGroup R；hn : n.Coprime (Fintype.card (ClassGroup R))；I : FractionalIdeal
 R⁰ K；hI : ((I ^ n : FractionalIdeal R⁰ K) : Submodule R K).IsPrincipal。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `GroupWithZero.eq_zero_or_unit`：∀ {G₀ : Type u_3} [inst : GroupWithZero G
₀] (a : G₀), a = 0 ∨ ∃ u, a = ↑u
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `FractionalIdeal.coe_zero`：coe_zero : ↑(0 : FractionalIdeal S P) = (⊥ : S
ubmodule R P)
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `ClassGroup.mk_eq_one_iff`：ClassGroup.mk_eq_one_iff {I : (FractionalIdeal
 R⁰ K)ˣ} : ClassGroup.mk K I = 1 ↔ (I : Submodule R K).IsPrincipal
· 使用定理 `orderOf_eq_one_iff`：orderOf_eq_one_iff : orderOf x = 1 ↔ x = 1
· 使用定理 `Nat.dvd_one`：∀ {n : ℕ}, n ∣ 1 ↔ n = 1
· 使用定理 `Nat.dvd_gcd_iff`：∀ {k : ℕ} {m n : ℕ}, k ∣ m.gcd n ↔ k ∣ m ∧ k ∣ n
· 使用定理 `orderOf_dvd_iff_pow_eq_one`：orderOf_dvd_iff_pow_eq_one {n : Nat} : order
Of x ∣ n ↔ x ^ n = 1
· 使用定理 `map_pow`：∀ {G : Type u_7} {H : Type u_8} {F : Type u_9} [inst : FunLike 
F G H] [inst_1 : Monoid G] [inst_2 : Monoid H]   [MonoidHomClass F G H] (f : …
· 使用定理 `orderOf_dvd_card`：orderOf_dvd_card : orderOf x ∣ Fintype.card G

--- 原说明 ---
A fractional ideal is principal if a power coprime to the class number is princi
pal.
-/
theorem FractionalIdeal.isPrincipal.of_isPrincipal_pow_of_coprime [IsDedekindDomain R]
    [Fintype (ClassGroup R)] {n : ℕ} (hn : n.Coprime (Fintype.card (ClassGroup R)))
    (I : FractionalIdeal R⁰ K) (hI : ((I ^ n : FractionalIdeal R⁰ K) : Submodule R K).IsPrincipal) :
    (I : Submodule R K).IsPrincipal := by
  obtain (rfl | ⟨u, rfl⟩) := GroupWithZero.eq_zero_or_unit I
  · simp [bot_isPrincipal]
  rw [← ClassGroup.mk_eq_one_iff, ← orderOf_eq_one_iff, ← Nat.dvd_one, ← hn,
    Nat.dvd_gcd_iff]
  refine ⟨?_, orderOf_dvd_card⟩
  rw [orderOf_dvd_iff_pow_eq_one, ← map_pow, ClassGroup.mk_eq_one_iff]
  exact_mod_cast hI

/-- If the class group is trivial, any unit fractional ideal is principal. -/
/-
**ClassGroup.isPrincipal_coeSubmodule_of_isUnit** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.isPrincipal_coeSubmodule_of_isUnit [Subsingleton (ClassGroup R)
] (I : FractionalIdeal R⁰ K) (hI : IsUnit I) : (I : Submodule R K).IsPrincipal
参数：ClassGroup R；I : FractionalIdeal R⁰ K；hI : IsUnit I。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `ClassGroup.mk_eq_one_iff`：ClassGroup.mk_eq_one_iff {I : (FractionalIdeal
 R⁰ K)ˣ} : ClassGroup.mk K I = 1 ↔ (I : Submodule R K).IsPrincipal
· 使用定理 `Subsingleton.elim`：∀ {α : Sort u} [h : Subsingleton α] (a b : α), a = b

--- 原说明 ---
If the class group is trivial, any unit fractional ideal is principal.
-/
theorem ClassGroup.isPrincipal_coeSubmodule_of_isUnit [Subsingleton (ClassGroup R)]
    (I : FractionalIdeal R⁰ K) (hI : IsUnit I) :
    (I : Submodule R K).IsPrincipal := by
  rcases hI with ⟨I, rfl⟩
  rw [← ClassGroup.mk_eq_one_iff, Subsingleton.elim (ClassGroup.mk K I) 1]

/-- If the class group is trivial, any integral ideal that is a unit as a fractional ideal
is principal. -/
/-
**ClassGroup.isPrincipal_of_isUnit_coeIdeal** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.isPrincipal_of_isUnit_coeIdeal [Subsingleton (ClassGroup R)] (I
 : Ideal R) (hI : IsUnit (I : FractionalIdeal R⁰ K)) : I.IsPrincipal
参数：ClassGroup R；I : Ideal R；hI : IsUnit (I : FractionalIdeal R⁰ K)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `ClassGroup.isPrincipal_coeSubmodule_of_isUnit`：ClassGroup.isPrincipal_co
eSubmodule_of_isUnit [Subsingleton (ClassGroup R)] (I : FractionalIdeal R⁰ K) (h
I : IsUnit I) : (I : Submodule R K)…
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `IsFractionRing.coeSubmodule_isPrincipal`：coeSubmodule_isPrincipal {I : I
deal R} : (coeSubmodule K I).IsPrincipal ↔ I.IsPrincipal

--- 原说明 ---
If the class group is trivial, any integral ideal that is a unit as a fractional
 ideal
is principal.
-/
theorem ClassGroup.isPrincipal_of_isUnit_coeIdeal [Subsingleton (ClassGroup R)]
    (I : Ideal R) (hI : IsUnit (I : FractionalIdeal R⁰ K)) :
    I.IsPrincipal := by
  have hsub :
      ((I : FractionalIdeal R⁰ K) : Submodule R K).IsPrincipal :=
    ClassGroup.isPrincipal_coeSubmodule_of_isUnit (I := (I : FractionalIdeal R⁰ K)) hI
  exact (IsFractionRing.coeSubmodule_isPrincipal (R := R) (K := K) (I := I)).1
    (by simpa using hsub)
/-
**ClassGroup.mk0_eq_one_iff** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.mk0_eq_one_iff [IsDedekindDomain R] {I : Ideal R} (hI : I in (I
deal R)⁰) : ClassGroup.mk0 ⟨I, hI⟩ = 1 ↔ I.IsPrincipal
参数：hI : I in (Ideal R)⁰。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `ClassGroup.mk_eq_one_iff`：ClassGroup.mk_eq_one_iff {I : (FractionalIdeal
 R⁰ K)ˣ} : ClassGroup.mk K I = 1 ↔ (I : Submodule R K).IsPrincipal
· 使用定理 `IsFractionRing.coeSubmodule_isPrincipal`：coeSubmodule_isPrincipal {I : I
deal R} : (coeSubmodule K I).IsPrincipal ↔ I.IsPrincipal
-/
theorem ClassGroup.mk0_eq_one_iff [IsDedekindDomain R] {I : Ideal R} (hI : I ∈ (Ideal R)⁰) :
    ClassGroup.mk0 ⟨I, hI⟩ = 1 ↔ I.IsPrincipal :=
  ClassGroup.mk_eq_one_iff.trans (coeSubmodule_isPrincipal R _)

/-- An ideal is principal if a power coprime to the class number is principal. -/
/-
**Ideal.IsPrincipal.of_isPrincipal_pow_of_coprime** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：Ideal.IsPrincipal.of_isPrincipal_pow_of_coprime [IsDedekindDomain R] [Fint
ype (ClassGroup R)] {n : Nat} (hn : n.Coprime (Fintype.card (ClassGroup R))) {I 
: Ideal R} (hI : (I ^ n).IsPrincipal) : I.IsPrincipal
参数：ClassGroup R；hn : n.Coprime (Fintype.card (ClassGroup R))；hI : (I ^ n).IsPrin
cipal。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mem_nonZeroDivisors_of_ne_zero`：mem_nonZeroDivisors_of_ne_zero (hx : x !
= 0) : x in M₀⁰
· 使用定理 `Submodule.instNoZeroDivisors`：∀ {R : Type u} [inst : Semiring R] {A : Ty
pe v} [inst_1 : Semiring A] [inst_2 : _root_.Module R A]   [inst_3 : IsScalarTow
er R A A] [NoZeroD…
· 使用定理 `IsDomain.to_noZeroDivisors`：∀ (α : Type u_3) [inst : Semiring α] [IsDoma
in α], NoZeroDivisors α
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `ClassGroup.mk0_eq_one_iff`：ClassGroup.mk0_eq_one_iff [IsDedekindDomain R
] {I : Ideal R} (hI : I in (Ideal R)⁰) : ClassGroup.mk0 ⟨I, hI⟩ = 1 ↔ I.IsPrinci
pal
· 使用定理 `orderOf_eq_one_iff`：orderOf_eq_one_iff : orderOf x = 1 ↔ x = 1
· 使用定理 `Nat.dvd_one`：∀ {n : ℕ}, n ∣ 1 ↔ n = 1
· 使用定理 `Nat.dvd_gcd_iff`：∀ {k : ℕ} {m n : ℕ}, k ∣ m.gcd n ↔ k ∣ m ∧ k ∣ n
· 使用定理 `orderOf_dvd_iff_pow_eq_one`：orderOf_dvd_iff_pow_eq_one {n : Nat} : order
Of x ∣ n ↔ x ^ n = 1
· 使用定理 `map_pow`：∀ {G : Type u_7} {H : Type u_8} {F : Type u_9} [inst : FunLike 
F G H] [inst_1 : Monoid G] [inst_2 : Monoid H]   [MonoidHomClass F G H] (f : …
· 使用定理 `pow_mem`：∀ {M : Type u_3} {A : Type u_4} [inst : Monoid M] [inst_1 : Set
Like A M] [SubmonoidClass A M] {S : A} {x : M},   x ∈ S → ∀ (n : ℕ), x ^ n ∈ …
· 使用定理 `Submonoid.instSubmonoidClass`：∀ {M : Type u_1} [inst : MulOneClass M], S
ubmonoidClass (Submonoid M) M
· 使用定理 `SubmonoidClass.mk_pow`：mk_pow {M} [Monoid M] {A : Type*} [SetLike A M] [
SubmonoidClass A M] {S : A} (x : M) (hx : x in S) (n : Nat) : (⟨x, hx⟩ : S) ^ n 
= ⟨x ^ n, p…
· 使用定理 `orderOf_dvd_card`：orderOf_dvd_card : orderOf x ∣ Fintype.card G

--- 原说明 ---
An ideal is principal if a power coprime to the class number is principal.
-/
theorem Ideal.IsPrincipal.of_isPrincipal_pow_of_coprime [IsDedekindDomain R]
    [Fintype (ClassGroup R)] {n : ℕ} (hn : n.Coprime (Fintype.card (ClassGroup R)))
    {I : Ideal R} (hI : (I ^ n).IsPrincipal) : I.IsPrincipal := by
  by_cases hI0 : I = 0
  · simp [hI0, bot_isPrincipal]
  rw [← ClassGroup.mk0_eq_one_iff (pow_mem (mem_nonZeroDivisors_of_ne_zero hI0) n)] at hI
  rw [← ClassGroup.mk0_eq_one_iff (mem_nonZeroDivisors_of_ne_zero hI0), ← orderOf_eq_one_iff,
    ← Nat.dvd_one, ← hn, Nat.dvd_gcd_iff]
  refine ⟨?_, orderOf_dvd_card⟩
  rwa [orderOf_dvd_iff_pow_eq_one, ← map_pow, SubmonoidClass.mk_pow]
/-
**ClassGroup.mk0_eq_mk0_inv_iff** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ClassGroup.mk0_eq_mk0_inv_iff [IsDedekindDomain R] {I J : (Ideal R)⁰} : Cl
assGroup.mk0 I = (ClassGroup.mk0 J)⁻¹ ↔ exists x != (0 : R), I * J = Ideal.span 
{x}
参数：Ideal R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `eq_inv_iff_mul_eq_one`：eq_inv_iff_mul_eq_one : a = b⁻¹ ↔ a * b = 1
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `map_mul`：map_mul [MulHomClass F M N] (f : F) (x y : M) : f (x * y) = f x
 * f y
· 使用定理 `MonoidHomClass.toMulHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `Subtype.property`：∀ {α : Sort u} {p : α → Prop} (self : Subtype p), p ↑s
elf
· 使用定理 `ClassGroup.mk0_eq_one_iff`：ClassGroup.mk0_eq_one_iff [IsDedekindDomain R
] {I : Ideal R} (hI : I in (Ideal R)⁰) : ClassGroup.mk0 ⟨I, hI⟩ = 1 ↔ I.IsPrinci
pal
· 使用定理 `Submodule.isPrincipal_iff`：∀ {R : Type u_1} {M : Type u_4} [inst : Semir
ing R] [inst_1 : AddCommMonoid M] [inst_2 : _root_.Module R M]   (S : Submodule 
R M), S.IsPrinc…
· 使用定理 `Submonoid.coe_mul`：coe_mul (x y : S) : (↑(x * y) : M) = ↑x * ↑y
· 使用定理 `nonZeroDivisors.coe_ne_zero`：nonZeroDivisors.coe_ne_zero (x : M₀⁰) : (x 
: M₀) != 0
· 使用定理 `Ideal.instNontrivial`：∀ {α : Type u} [inst : Semiring α] [Nontrivial α],
 Nontrivial (Ideal α)
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b
· 使用定理 `Subtype.prop`：prop (x : Subtype p) : p x
· 使用定理 `Submodule.span_zero_singleton`：span_zero_singleton : R ∙ (0 : M) = ⊥
-/
theorem ClassGroup.mk0_eq_mk0_inv_iff [IsDedekindDomain R] {I J : (Ideal R)⁰} :
    ClassGroup.mk0 I = (ClassGroup.mk0 J)⁻¹ ↔
      ∃ x ≠ (0 : R), I * J = Ideal.span {x} := by
  rw [eq_inv_iff_mul_eq_one, ← map_mul, ClassGroup.mk0_eq_one_iff,
    Submodule.isPrincipal_iff, Submonoid.coe_mul]
  refine ⟨fun ⟨a, ha⟩ ↦ ⟨a, ?_, ha⟩, fun ⟨a, _, ha⟩ ↦ ⟨a, ha⟩⟩
  by_contra!
  rw [this, Submodule.span_zero_singleton] at ha
  exact nonZeroDivisors.coe_ne_zero _ <| J.prop.2 _ ha

/-- The class group of principal ideal domain is finite (in fact a singleton).

See `ClassGroup.fintypeOfAdmissibleOfFinite` for a finiteness proof that works for rings of integers
of global fields.
-/
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The class group of principal ideal domain is finite (in fact a singleton).

See `ClassGroup.fintypeOfAdmissibleOfFinite` for a finiteness proof that works f
or rings of integers
of global fields.
-/
instance [IsPrincipalIdealRing R] : Fintype (ClassGroup R) where
  elems := {1}
  complete := by
    refine ClassGroup.induction (R := R) (FractionRing R) (fun I => ?_)
    rw [Finset.mem_singleton]
    exact ClassGroup.mk_eq_one_iff.mpr (I : FractionalIdeal R⁰ (FractionRing R)).isPrincipal

/-- The class number of a principal ideal domain is `1`. -/
/-
**card_classGroup_eq_one** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：card_classGroup_eq_one [IsPrincipalIdealRing R] : Fintype.card (ClassGroup
 R) = 1
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Fintype.card_eq_one_iff`：card_eq_one_iff : card α = 1 ↔ exists x : α, fo
rall y, y = x
· 使用定理 `ClassGroup.induction`：ClassGroup.induction {P : ClassGroup R -> Prop} (h
 : forall I : (FractionalIdeal R⁰ K)ˣ, P (ClassGroup.mk K I)) (x : ClassGroup R)
 : P x
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `ClassGroup.mk_eq_one_iff`：ClassGroup.mk_eq_one_iff {I : (FractionalIdeal
 R⁰ K)ˣ} : ClassGroup.mk K I = 1 ↔ (I : Submodule R K).IsPrincipal

--- 原说明 ---
The class number of a principal ideal domain is `1`.
-/
theorem card_classGroup_eq_one [IsPrincipalIdealRing R] : Fintype.card (ClassGroup R) = 1 := by
  rw [Fintype.card_eq_one_iff]
  use 1
  refine ClassGroup.induction (R := R) (FractionRing R) (fun I => ?_)
  exact ClassGroup.mk_eq_one_iff.mpr (I : FractionalIdeal R⁰ (FractionRing R)).isPrincipal

/-- The class number is `1` iff the ring of integers is a principal ideal domain. -/
/-
**card_classGroup_eq_one_iff** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：card_classGroup_eq_one_iff [IsDedekindDomain R] [Fintype (ClassGroup R)] :
 Fintype.card (ClassGroup R) = 1 ↔ IsPrincipalIdealRing R
参数：ClassGroup R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Fintype.card_eq_one_iff`：card_eq_one_iff : card α = 1 ↔ exists x : α, fo
rall y, y = x
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `mem_nonZeroDivisors_iff_ne_zero`：∀ {M₀ : Type u_2} [inst : MonoidWithZer
o M₀] {x : M₀} [NoZeroDivisors M₀] [Nontrivial M₀],   x ∈ nonZeroDivisors M₀ ↔ x
 ≠ 0
· 使用定理 `Submodule.instNoZeroDivisors`：∀ {R : Type u} [inst : Semiring R] {A : Ty
pe v} [inst_1 : Semiring A] [inst_2 : _root_.Module R A]   [inst_3 : IsScalarTow
er R A A] [NoZeroD…
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `IsDomain.to_noZeroDivisors`：∀ (α : Type u_3) [inst : Semiring α] [IsDoma
in α], NoZeroDivisors α
· 使用定理 `Ideal.instNontrivial`：∀ {α : Type u} [inst : Semiring α] [Nontrivial α],
 Nontrivial (Ideal α)
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `ClassGroup.mk0_eq_one_iff`：ClassGroup.mk0_eq_one_iff [IsDedekindDomain R
] {I : Ideal R} (hI : I in (Ideal R)⁰) : ClassGroup.mk0 ⟨I, hI⟩ = 1 ↔ I.IsPrinci
pal
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `Fintype.card_congr'`：card_congr' {α β} [Fintype α] [Fintype β] (h : α = 
β) : card α = card β
· 使用定理 `card_classGroup_eq_one`：card_classGroup_eq_one [IsPrincipalIdealRing R] 
: Fintype.card (ClassGroup R) = 1

--- 原说明 ---
The class number is `1` iff the ring of integers is a principal ideal domain.
-/
theorem card_classGroup_eq_one_iff [IsDedekindDomain R] [Fintype (ClassGroup R)] :
    Fintype.card (ClassGroup R) = 1 ↔ IsPrincipalIdealRing R := by
  constructor; swap; · intros; convert! card_classGroup_eq_one (R := R)
  rw [Fintype.card_eq_one_iff]
  rintro ⟨I, hI⟩
  have eq_one : ∀ J : ClassGroup R, J = 1 := fun J => (hI J).trans (hI 1).symm
  refine ⟨fun I => ?_⟩
  by_cases hI : I = ⊥
  · rw [hI]; exact bot_isPrincipal
  · exact (ClassGroup.mk0_eq_one_iff (mem_nonZeroDivisors_iff_ne_zero.mpr hI)).mp (eq_one _)

section MulEquiv

/-
**FractionalIdeal.map_ringEquivOfRingEquiv_toPrincipalIdeal** 是 Mathlib 中的一个定理，位
于命名空间 ``。
形式化陈述：FractionalIdeal.map_ringEquivOfRingEquiv_toPrincipalIdeal {S L : Type*} [C
ommRing S] [IsDomain S] [Field L] [Algebra S L] [IsFractionRing S L] (f : R ≃+* 
S) : Subgroup.map ((Units.mapEquiv (ringEquivOfRingEquiv K L f))).toMonoidHom (t
oPrincipalIdeal R K).range = (toPrincipalIdeal S L).range
参数：f : R ≃+* S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subgroup.ext`：ext {H K : Subgroup G} (h : forall x, x in H ↔ x in K) : H
 = K
· 使用定理 `RingEquivClass.toMulEquivClass`：∀ {F : Type u_7} {R : Type u_8} {S : Typ
e u_9} {inst : Mul R} {inst_1 : Add R} {inst_2 : Mul S} {inst_3 : Add S}   {inst
_4 : EquivLike F R S…
· 使用定理 `RingEquiv.instRingEquivClass`：∀ {R : Type u_4} {S : Type u_5} [inst : Mu
l R] [inst_1 : Mul S] [inst_2 : Add R] [inst_3 : Add S],   RingEquivClass (R ≃+*
 S) R S
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `MulEquivClass.instMonoidHomClass`：∀ (F : Type u_1) {M : Type u_4} {N : T
ype u_5} [inst : EquivLike F M N] [inst_1 : MulOneClass M]   [inst_2 : MulOneCla
ss N] [MulEquivClass F…
· 使用定理 `MulEquiv.instMulEquivClass`：∀ {M : Type u_4} {N : Type u_5} [inst : Mul 
M] [inst_1 : Mul N], MulEquivClass (M ≃* N) M N
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `FractionalIdeal.ringEquivOfRingEquiv_spanSingleton`：ringEquivOfRingEquiv
_spanSingleton (x : K) : FractionalIdeal.ringEquivOfRingEquiv K L f (spanSinglet
on R⁰ x) = spanSingleton S⁰ (IsFractionR…
· 使用引理 `FractionalIdeal.ringEquivOfRingEquiv_symm_eq`：ringEquivOfRingEquiv_symm_
eq : (FractionalIdeal.ringEquivOfRingEquiv K L f).symm = FractionalIdeal.ringEqu
ivOfRingEquiv L K f.symm
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `RingEquiv.apply_symm_apply`：apply_symm_apply (e : R ≃+* S) : forall x, e
 (e.symm x) = x
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem FractionalIdeal.map_ringEquivOfRingEquiv_toPrincipalIdeal {S L : Type*} [CommRing S]
    [IsDomain S] [Field L] [Algebra S L] [IsFractionRing S L] (f : R ≃+* S) :
  Subgroup.map ((Units.mapEquiv (ringEquivOfRingEquiv K L f))).toMonoidHom
    (toPrincipalIdeal R K).range = (toPrincipalIdeal S L).range := by
  ext I
  simp only [MulEquiv.toMonoidHom_eq_coe, Subgroup.mem_map, MonoidHom.mem_range,
    toPrincipalIdeal_eq_iff, MonoidHom.coe_coe]
  refine ⟨fun ⟨u, ⟨v, huv⟩, hu⟩ ↦ ?_, fun ⟨u, hu⟩ ↦ ?_⟩
  · use Units.map (IsFractionRing.ringEquivOfRingEquiv f (K := K)
      (L := L)).toRingHom v
    rw [← hu]
    simp only [RingEquiv.toRingHom_eq_coe, Units.coe_map, MonoidHom.coe_coe, RingHom.coe_coe,
      Units.coe_mapEquiv, ← huv, RingEquiv.coe_toMulEquiv]
    rw [FractionalIdeal.ringEquivOfRingEquiv_spanSingleton]
  · use Units.mapEquiv (FractionalIdeal.ringEquivOfRingEquiv _ _ f).symm.toMulEquiv I
    refine ⟨?_, by simp [← Units.val_inj]⟩
    · use Units.map (IsFractionRing.ringEquivOfRingEquiv f (K := K)
        (L := L)).symm.toRingHom u
      simp only [IsFractionRing.ringEquivOfRingEquiv_symm, RingEquiv.toRingHom_eq_coe,
        Units.coe_map, MonoidHom.coe_coe, RingHom.coe_coe, RingEquiv.toMulEquiv_eq_coe,
        RingEquiv.coe_toMulEquiv_symm, Units.coe_mapEquiv]
      rw [← FractionalIdeal.ringEquivOfRingEquiv_spanSingleton,
        ← FractionalIdeal.ringEquivOfRingEquiv_symm_eq, hu]
      rfl

#adaptation_note
/-- `respectTransparency.types true` changes the auto-generated lemmas' signature -/
set_option backward.isDefEq.respectTransparency.types false in
/-- A ring isomorphism `R ≃+* R'` induces an isomorphism on their class groups. -/
@[simps!]
/-
**ClassGroup.mulEquiv** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：ClassGroup.mulEquiv {R' : Type*} [CommRing R'] [IsDomain R'] (g : R ≃+* R'
) : ClassGroup R ≃* ClassGroup R'
参数：g : R ≃+* R'。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A ring isomorphism `R ≃+* R'` induces an isomorphism on their class groups.
-/
noncomputable def ClassGroup.mulEquiv {R' : Type*} [CommRing R'] [IsDomain R'] (g : R ≃+* R') :
    ClassGroup R ≃* ClassGroup R' :=
  (ClassGroup.equiv (R := R) (FractionRing R)).trans
    ((QuotientGroup.congr (toPrincipalIdeal R (FractionRing R)).range
        (toPrincipalIdeal R' (FractionRing R')).range
        (Units.mapEquiv (FractionalIdeal.ringEquivOfRingEquiv (FractionRing R) (FractionRing R') g))
        (FractionalIdeal.map_ringEquivOfRingEquiv_toPrincipalIdeal g)).trans
      (ClassGroup.equiv (FractionRing R')).symm)

end MulEquiv

