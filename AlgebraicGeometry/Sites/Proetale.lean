/-
Copyright (c) 2026 Jiedong Jiang, Christian Merten. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Jiedong Jiang, Christian Merten
-/
module

public import Mathlib.AlgebraicGeometry.Morphisms.UniversallyOpen
public import Mathlib.AlgebraicGeometry.Morphisms.WeaklyEtale
public import Mathlib.AlgebraicGeometry.Sites.Etale
public import Mathlib.AlgebraicGeometry.Sites.QuasiCompact
public import Mathlib.AlgebraicGeometry.Sites.Fpqc
public import Mathlib.CategoryTheory.Comma.Over.StrictInitial
public import Mathlib.CategoryTheory.MorphismProperty.CommaSites
public import Mathlib.CategoryTheory.Sites.Subcanonical
public import Mathlib.CategoryTheory.Sites.SubcanonicalOver

/-!

# The pro-étale site

In this file we define the big and small pro-étale site. The big pro-étale site is
the category of schemes equipped with the pro-étale topology, which is the topology
generated by fpqc covers of weakly étale morphisms.

We prefer to work with weakly étale morphisms instead of pro-étale morphisms, since the property
of being pro-étale is not well-behaved: it is not local on the target.

We also define the small pro-étale site of a scheme `S` to be the category of schemes weakly-étale
over `S` and equip it with the induced topology.

## References

- [Bhatt, Bhargav and Scholze, Peter, The pro-étale topology for schemes][proetale2015]

-/

@[expose] public section

universe v u

open CategoryTheory MorphismProperty Limits

namespace AlgebraicGeometry.Scheme

set_option backward.isDefEq.respectTransparency.types false in
/--
Big pro-étale site: the pro-étale precoverage on the category of schemes given
by fpqc covers of weakly étale morphisms.
[Definition 4.1.1][proetale2015]
-/
/-
**AlgebraicGeometry.Scheme.proetalePrecoverage** 是 Mathlib 中的一个定义，位于命名空间 `Algebr
aicGeometry.Scheme`。
形式化陈述：proetalePrecoverage : Precoverage Scheme.{u}
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Big pro-étale site: the pro-étale precoverage on the category of schemes given
by fpqc covers of weakly étale morphisms.
[Definition 4.1.1][proetale2015]
-/
def proetalePrecoverage : Precoverage Scheme.{u} :=
  propQCPrecoverage @WeaklyEtale
  deriving Precoverage.IsStableUnderBaseChange, Precoverage.IsStableUnderComposition,
    Precoverage.HasIsos
/-
**AlgebraicGeometry.Scheme.proetalePrecoverage_le_precoverage_weaklyEtale** 是 Ma
thlib 中的一个引理，位于命名空间 `AlgebraicGeometry.Scheme`。
形式化陈述：proetalePrecoverage_le_precoverage_weaklyEtale : proetalePrecoverage <= Mo
rphismProperty.precoverage @WeaklyEtale
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_trans`：le_trans : a <= b -> b <= c -> a <= c
· 使用定理 `inf_le_right`：∀ {α : Type u} [inst : SemilatticeInf α] {a b : α}, a ⊓ b 
≤ b
-/
lemma proetalePrecoverage_le_precoverage_weaklyEtale :
    proetalePrecoverage ≤ MorphismProperty.precoverage @WeaklyEtale :=
  le_trans inf_le_right inf_le_right
/-
**AlgebraicGeometry.Scheme.proetalePrecoverage_le_fpqcPrecoverage** 是 Mathlib 中的
一个引理，位于命名空间 `AlgebraicGeometry.Scheme`。
形式化陈述：proetalePrecoverage_le_fpqcPrecoverage : proetalePrecoverage <= fpqcPrecov
erage
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `AlgebraicGeometry.Scheme.propQCPrecoverage_monotone`：propQCPrecoverage_m
onotone : Monotone propQCPrecoverage
· 使用定理 `AlgebraicGeometry.WeaklyEtale.flat`：∀ {X Y : AlgebraicGeometry.Scheme} {
f : X ⟶ Y} [self : AlgebraicGeometry.WeaklyEtale f], AlgebraicGeometry.Flat f
-/
lemma proetalePrecoverage_le_fpqcPrecoverage : proetalePrecoverage ≤ fpqcPrecoverage :=
  propQCPrecoverage_monotone fun _ _ _ _ ↦ inferInstance

/-- Big pro-étale site: the pro-étale topology on the category of schemes is
generated by the pro-étale precoverage. -/
/-
**AlgebraicGeometry.Scheme.proetaleTopology** 是 Mathlib 中的一个缩写定义，位于命名空间 `Algebra
icGeometry.Scheme`。
形式化陈述：proetaleTopology : GrothendieckTopology Scheme.{u}
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Big pro-étale site: the pro-étale topology on the category of schemes is
generated by the pro-étale precoverage.
-/
abbrev proetaleTopology : GrothendieckTopology Scheme.{u} :=
  proetalePrecoverage.toGrothendieck
/-
**AlgebraicGeometry.Scheme.proetaleTopology_eq_propQCTopology** 是 Mathlib 中的一个引理
，位于命名空间 `AlgebraicGeometry.Scheme`。
形式化陈述：proetaleTopology_eq_propQCTopology : proetaleTopology = propQCTopology @We
aklyEtale
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma proetaleTopology_eq_propQCTopology : proetaleTopology = propQCTopology @WeaklyEtale := rfl

set_option backward.isDefEq.respectTransparency.types false in
/-
**AlgebraicGeometry.Scheme.etalePrecoverage_le_proetalePrecoverage** 是 Mathlib 中
的一个引理，位于命名空间 `AlgebraicGeometry.Scheme`。
形式化陈述：etalePrecoverage_le_proetalePrecoverage : etalePrecoverage <= proetalePrec
overage
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `AlgebraicGeometry.Scheme.proetalePrecoverage.eq_1`：AlgebraicGeometry.Sch
eme.proetalePrecoverage = AlgebraicGeometry.Scheme.propQCPrecoverage @AlgebraicG
eometry.WeaklyEtale
· 使用定理 `AlgebraicGeometry.Scheme.propQCPrecoverage.eq_1`：∀ (P : CategoryTheory.M
orphismProperty AlgebraicGeometry.Scheme),   AlgebraicGeometry.Scheme.propQCPrec
overage P =     AlgebraicGeometry.Sch…
· 使用定理 `AlgebraicGeometry.Scheme.etalePrecoverage.eq_1`：AlgebraicGeometry.Scheme
.etalePrecoverage = AlgebraicGeometry.Scheme.precoverage @AlgebraicGeometry.Etal
e
· 使用定理 `le_inf_iff`：∀ {α : Type u} [inst : SemilatticeInf α] {c a b : α}, c ≤ a 
⊓ b ↔ c ≤ a ∧ c ≤ b
· 使用引理 `AlgebraicGeometry.Scheme.precoverage_le_qcPrecoverage_of_isOpenMap`：prec
overage_le_qcPrecoverage_of_isOpenMap {P : MorphismProperty Scheme.{u}} (hP : P 
<= fun _ _ f => IsOpenMap f.base) : precoverage P <= qcP…
· 使用定理 `AlgebraicGeometry.Scheme.Hom.isOpenMap`：∀ {X Y : AlgebraicGeometry.Schem
e} (f : X ⟶ Y) [AlgebraicGeometry.UniversallyOpen f], IsOpenMap ⇑f
· 使用定理 `AlgebraicGeometry.UniversallyOpen.of_flat`：∀ {X Y : AlgebraicGeometry.Sc
heme} (f : X ⟶ Y) [AlgebraicGeometry.Flat f]   [AlgebraicGeometry.LocallyOfFinit
ePresentation f], AlgebraicGeom…
· 使用定理 `AlgebraicGeometry.WeaklyEtale.flat`：∀ {X Y : AlgebraicGeometry.Scheme} {
f : X ⟶ Y} [self : AlgebraicGeometry.WeaklyEtale f], AlgebraicGeometry.Flat f
· 使用定理 `AlgebraicGeometry.WeaklyEtale.instOfEtale`：∀ {X Y : AlgebraicGeometry.Sc
heme} (f : X ⟶ Y) [AlgebraicGeometry.Etale f], AlgebraicGeometry.WeaklyEtale f
· 使用定理 `AlgebraicGeometry.instLocallyOfFinitePresentationOfSmooth`：∀ {X Y : Alge
braicGeometry.Scheme} (f : X ⟶ Y) [hf : AlgebraicGeometry.Smooth f],   Algebraic
Geometry.LocallyOfFinitePresentation f
· 使用定理 `AlgebraicGeometry.Etale.instSmooth`：∀ {X Y : AlgebraicGeometry.Scheme} (
f : X ⟶ Y) [AlgebraicGeometry.Etale f], AlgebraicGeometry.Smooth f
· 使用引理 `AlgebraicGeometry.Scheme.precoverage_mono`：precoverage_mono {P Q : Morph
ismProperty Scheme.{u}} (h : P <= Q) : precoverage P <= precoverage Q
-/
lemma etalePrecoverage_le_proetalePrecoverage : etalePrecoverage ≤ proetalePrecoverage := by
  rw [proetalePrecoverage, propQCPrecoverage, etalePrecoverage, le_inf_iff]
  refine ⟨precoverage_le_qcPrecoverage_of_isOpenMap fun X Y f hf ↦ f.isOpenMap, ?_⟩
  apply precoverage_mono
  intro X Y f hf
  infer_instance
/-
**AlgebraicGeometry.Scheme.etaleTopology_le_proetaleTopology** 是 Mathlib 中的一个引理，
位于命名空间 `AlgebraicGeometry.Scheme`。
形式化陈述：etaleTopology_le_proetaleTopology : etaleTopology <= proetaleTopology
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Precoverage.toGrothendieck_mono`：∀ {C : Type u_3} [inst :
 CategoryTheory.Category.{u_2, u_3} C] {J K : CategoryTheory.Precoverage C},   J
 ≤ K → J.toGrothendieck ≤ K.toGrothe…
· 使用引理 `AlgebraicGeometry.Scheme.etalePrecoverage_le_proetalePrecoverage`：etaleP
recoverage_le_proetalePrecoverage : etalePrecoverage <= proetalePrecoverage
-/
lemma etaleTopology_le_proetaleTopology : etaleTopology ≤ proetaleTopology :=
  Precoverage.toGrothendieck_mono etalePrecoverage_le_proetalePrecoverage
/-
**AlgebraicGeometry.Scheme.proetaleTopology_le_fpqcTopology** 是 Mathlib 中的一个引理，位
于命名空间 `AlgebraicGeometry.Scheme`。
形式化陈述：proetaleTopology_le_fpqcTopology : proetaleTopology <= fpqcTopology
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Precoverage.toGrothendieck_mono`：∀ {C : Type u_3} [inst :
 CategoryTheory.Category.{u_2, u_3} C] {J K : CategoryTheory.Precoverage C},   J
 ≤ K → J.toGrothendieck ≤ K.toGrothe…
· 使用引理 `AlgebraicGeometry.Scheme.proetalePrecoverage_le_fpqcPrecoverage`：proetal
ePrecoverage_le_fpqcPrecoverage : proetalePrecoverage <= fpqcPrecoverage
-/
lemma proetaleTopology_le_fpqcTopology : proetaleTopology ≤ fpqcTopology :=
  Precoverage.toGrothendieck_mono proetalePrecoverage_le_fpqcPrecoverage
/-
**AlgebraicGeometry.Scheme.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.Scheme`
。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : proetaleTopology.Subcanonical :=
  .of_le proetaleTopology_le_fpqcTopology
/-
**AlgebraicGeometry.Scheme.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.Scheme`
。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance {S : Scheme.{u}} (𝒰 : S.Cover (precoverage @WeaklyEtale)) (i : 𝒰.I₀) :
    WeaklyEtale (𝒰.f i) :=
  𝒰.map_prop i

set_option backward.isDefEq.respectTransparency.types false in
/--
The (small) pro-étale site of a scheme `S`: Its objects are the schemes weakly étale over `S`.
We prefer to work with weakly étale morphisms instead of pro-étale morphisms, since the property
of being pro-étale is not well-behaved: it is not local on the target.
[Definition 4.1.1][proetale2015]
-/
@[stacks 098K]
/-
**AlgebraicGeometry.Scheme.ProEt** 是 Mathlib 中的一个定义，位于命名空间 `AlgebraicGeometry.Sc
heme`。
形式化陈述：AlgebraicGeometry.Scheme → Type (u + 1)
参数：u + 1。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The (small) pro-étale site of a scheme `S`: Its objects are the schemes weakly é
tale over `S`.
We prefer to work with weakly étale morphisms instead of pro-étale morphisms, si
nce the property
of being pro-étale is not well-behaved: it is not local on the target.
[Definition 4.1.1][proetale2015]
-/
protected def ProEt (S : Scheme.{u}) : Type _ :=
  MorphismProperty.Over @WeaklyEtale ⊤ S
  deriving Category, HasFiniteLimits

namespace ProEt

variable (S : Scheme.{u})

/-
**AlgebraicGeometry.Scheme.ProEt.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.S
cheme.ProEt`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (U : S.ProEt) : WeaklyEtale U.hom := U.prop

variable {S} in
/-- Construct an object of the pro-étale site of `S` by giving a weakly étale morphism
`f : X ⟶ S`. -/
@[simps!]
/-
**AlgebraicGeometry.Scheme.ProEt.mk** 是 Mathlib 中的一个定义，位于命名空间 `AlgebraicGeometry
.Scheme.ProEt`。
形式化陈述：{S X : AlgebraicGeometry.Scheme} → (f : X ⟶ S) → [AlgebraicGeometry.Weakly
Etale f] → S.ProEt
参数：f : X ⟶ S。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Construct an object of the pro-étale site of `S` by giving a weakly étale morphi
sm
`f : X ⟶ S`.
-/
protected def mk {X : Scheme.{u}} (f : X ⟶ S) [WeaklyEtale f] : S.ProEt :=
  MorphismProperty.Over.mk _ f ‹_›

set_option backward.isDefEq.respectTransparency.types false in
/-- The forgetful functor the pro-étale site of `S` to schemes over `S`. -/
@[simps!]
/-
**AlgebraicGeometry.Scheme.ProEt.forget** 是 Mathlib 中的一个定义，位于命名空间 `AlgebraicGeom
etry.Scheme.ProEt`。
形式化陈述：(S : AlgebraicGeometry.Scheme) → CategoryTheory.Functor S.ProEt (CategoryT
heory.Over S)
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.MorphismProperty.IsMultiplicative.instTop`：∀ {C : Type u}
 [inst : CategoryTheory.Category.{v, u} C], ⊤.IsMultiplicative

--- 原说明 ---
The forgetful functor the pro-étale site of `S` to schemes over `S`.
-/
protected def forget : S.ProEt ⥤ Over S :=
  MorphismProperty.Over.forget @WeaklyEtale ⊤ S

set_option backward.isDefEq.respectTransparency.types false in
/-- The forgetful functor from the pro-étale site of `S` to schemes over `S` is fully faithful. -/
/-
**AlgebraicGeometry.Scheme.ProEt.forgetFullyFaithful** 是 Mathlib 中的一个定义，位于命名空间 `
AlgebraicGeometry.Scheme.ProEt`。
形式化陈述：forgetFullyFaithful : (ProEt.forget S).FullyFaithful
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The forgetful functor from the pro-étale site of `S` to schemes over `S` is full
y faithful.
-/
def forgetFullyFaithful : (ProEt.forget S).FullyFaithful :=
  MorphismProperty.Comma.forgetFullyFaithful _ _ _

set_option backward.isDefEq.respectTransparency.types false in
/-
**AlgebraicGeometry.Scheme.ProEt.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.S
cheme.ProEt`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : (ProEt.forget S).Full :=
  inferInstanceAs <| (MorphismProperty.Over.forget _ _ _).Full

set_option backward.isDefEq.respectTransparency.types false in
/-
**AlgebraicGeometry.Scheme.ProEt.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.S
cheme.ProEt`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : (ProEt.forget S).Faithful :=
  inferInstanceAs <| (MorphismProperty.Over.forget _ _ _).Faithful

set_option backward.isDefEq.respectTransparency.types false in
/-
**AlgebraicGeometry.Scheme.ProEt.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.S
cheme.ProEt`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : PreservesFiniteLimits (ProEt.forget S) :=
  inferInstanceAs <| PreservesFiniteLimits (MorphismProperty.Over.forget _ _ _)

set_option backward.isDefEq.respectTransparency.types false in
/-
**AlgebraicGeometry.Scheme.ProEt.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.S
cheme.ProEt`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : RepresentablyFlat (ProEt.forget S) :=
  flat_of_preservesFiniteLimits _

set_option backward.isDefEq.respectTransparency.types false in
/-
**AlgebraicGeometry.Scheme.ProEt.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.S
cheme.ProEt`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : (ProEt.forget S).LocallyCoverDense (proetaleTopology.over S) := by
  apply MorphismProperty.locallyCoverDense_forget_of_le
  exact proetalePrecoverage_le_precoverage_weaklyEtale

set_option backward.isDefEq.respectTransparency.types false in
/-- The pro-étale precoverage on the small pro-étale site. -/
/-
**AlgebraicGeometry.Scheme.ProEt.precoverage** 是 Mathlib 中的一个定义，位于命名空间 `Algebrai
cGeometry.Scheme.ProEt`。
形式化陈述：precoverage : Precoverage S.ProEt
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The pro-étale precoverage on the small pro-étale site.
-/
def precoverage : Precoverage S.ProEt :=
  proetalePrecoverage.comap (ProEt.forget S ⋙ Over.forget S)

set_option backward.isDefEq.respectTransparency.types false in
/-- The pro-étale topology on the small pro-étale site. -/
/-
**AlgebraicGeometry.Scheme.ProEt.topology** 是 Mathlib 中的一个缩写定义，位于命名空间 `Algebraic
Geometry.Scheme.ProEt`。
形式化陈述：topology : GrothendieckTopology S.ProEt
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The pro-étale topology on the small pro-étale site.
-/
abbrev topology : GrothendieckTopology S.ProEt :=
  (precoverage S).toGrothendieck

set_option backward.isDefEq.respectTransparency.types false in
/-
**AlgebraicGeometry.Scheme.ProEt.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.S
cheme.ProEt`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : (ProEt.forget S).IsContinuous (topology S) (proetaleTopology.over S) := by
  rw [Functor.isContinuous_iff_coverPreserving]
  exact coverPreserving_comap_forget _ proetalePrecoverage_le_precoverage_weaklyEtale

set_option backward.isDefEq.respectTransparency.types false in
/-
**AlgebraicGeometry.Scheme.ProEt.topology_eq_inducedTopology** 是 Mathlib 中的一个引理，
位于命名空间 `AlgebraicGeometry.Scheme.ProEt`。
形式化陈述：topology_eq_inducedTopology : topology S = (ProEt.forget S).inducedTopolog
y (proetaleTopology.over S)
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `CategoryTheory.MorphismProperty.toGrothendieck_comap_forget_eq_inducedTo
pology`：toGrothendieck_comap_forget_eq_inducedTopology (H : K <= P.precoverage) 
: (K.comap (MorphismProperty.Over.forget P ⊤ _ ⋙ CategoryTheory.Over…
· 使用定理 `CategoryTheory.MorphismProperty.IsMultiplicative.toIsStableUnderComposit
ion`：∀ {C : Type u} {inst : CategoryTheory.Category.{v, u} C} {W : CategoryTheor
y.MorphismProperty C}   [self : W.IsMultiplicative], W.IsStableUn…
· 使用定理 `AlgebraicGeometry.WeaklyEtale.instIsMultiplicativeScheme`：CategoryTheory
.MorphismProperty.IsMultiplicative @AlgebraicGeometry.WeaklyEtale
· 使用定理 `AlgebraicGeometry.Scheme.instHasIsosProetalePrecoverage`：AlgebraicGeomet
ry.Scheme.proetalePrecoverage.HasIsos
· 使用定理 `AlgebraicGeometry.Scheme.instIsStableUnderBaseChangeProetalePrecoverage`
：AlgebraicGeometry.Scheme.proetalePrecoverage.IsStableUnderBaseChange
· 使用定理 `AlgebraicGeometry.Scheme.instIsStableUnderCompositionProetalePrecoverage
`：AlgebraicGeometry.Scheme.proetalePrecoverage.IsStableUnderComposition
· 使用定理 `CategoryTheory.Precoverage.instHasPullbacksOfHasPullbacks`：∀ {C : Type u
} [inst : CategoryTheory.Category.{v, u} C] (J : CategoryTheory.Precoverage C)  
 [CategoryTheory.Limits.HasPullbacks C], J.HasP…
· 使用定理 `AlgebraicGeometry.Scheme.Pullback.instHasPullbacks`：CategoryTheory.Limit
s.HasPullbacks AlgebraicGeometry.Scheme
· 使用定理 `CategoryTheory.Limits.hasFiniteWidePullbacks_of_hasFiniteLimits`：∀ (C : 
Type u) [inst : CategoryTheory.Category.{v, u} C] [CategoryTheory.Limits.HasFini
teLimits C],   CategoryTheory.Limits.HasFiniteWidePul…
· 使用定理 `AlgebraicGeometry.instHasFiniteLimitsScheme`：CategoryTheory.Limits.HasFi
niteLimits AlgebraicGeometry.Scheme
· 使用定理 `AlgebraicGeometry.WeaklyEtale.instHasOfPostcompPropertyScheme`：CategoryT
heory.MorphismProperty.HasOfPostcompProperty @AlgebraicGeometry.WeaklyEtale @Alg
ebraicGeometry.WeaklyEtale
· 使用定理 `AlgebraicGeometry.WeaklyEtale.instIsStableUnderBaseChangeScheme`：Categor
yTheory.MorphismProperty.IsStableUnderBaseChange @AlgebraicGeometry.WeaklyEtale
· 使用定理 `CategoryTheory.MorphismProperty.IsMultiplicative.toContainsIdentities`：∀
 {C : Type u} {inst : CategoryTheory.Category.{v, u} C} {W : CategoryTheory.Morp
hismProperty C}   [self : W.IsMultiplicative], W.ContainsId…
· 使用引理 `AlgebraicGeometry.Scheme.proetalePrecoverage_le_precoverage_weaklyEtale`
：proetalePrecoverage_le_precoverage_weaklyEtale : proetalePrecoverage <= Morphis
mProperty.precoverage @WeaklyEtale
-/
lemma topology_eq_inducedTopology :
    topology S = (ProEt.forget S).inducedTopology (proetaleTopology.over S) :=
  MorphismProperty.toGrothendieck_comap_forget_eq_inducedTopology
    _ proetalePrecoverage_le_precoverage_weaklyEtale

set_option backward.isDefEq.respectTransparency.types false in
/-
**AlgebraicGeometry.Scheme.ProEt.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.S
cheme.ProEt`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : (ProEt.forget S ⋙ Over.forget S).IsContinuous (ProEt.topology S) proetaleTopology :=
  Functor.isContinuous_comp _ _ _ (proetaleTopology.over S) _

set_option backward.isDefEq.respectTransparency.types false in
/-
**AlgebraicGeometry.Scheme.ProEt.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.S
cheme.ProEt`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : (topology S).Subcanonical :=
  GrothendieckTopology.subcanonical_of_full_of_faithful (ProEt.forget S) _ (proetaleTopology.over S)

set_option backward.isDefEq.respectTransparency.types false in
/-- If `S` is the empty scheme, the pro-étale site over `S` is a point. -/
/-
**AlgebraicGeometry.Scheme.ProEt.equivOfIsEmpty** 是 Mathlib 中的一个定义，位于命名空间 `Algeb
raicGeometry.Scheme.ProEt`。
形式化陈述：equivOfIsEmpty [IsEmpty S] : S.ProEt ≌ Discrete PUnit
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.MorphismProperty.IsMultiplicative.toContainsIdentities`：∀
 {C : Type u} {inst : CategoryTheory.Category.{v, u} C} {W : CategoryTheory.Morp
hismProperty C}   [self : W.IsMultiplicative], W.ContainsId…
· 使用定理 `AlgebraicGeometry.WeaklyEtale.instIsMultiplicativeScheme`：CategoryTheory
.MorphismProperty.IsMultiplicative @AlgebraicGeometry.WeaklyEtale
· 使用定理 `CategoryTheory.MorphismProperty.IsMultiplicative.instTop`：∀ {C : Type u}
 [inst : CategoryTheory.Category.{v, u} C], ⊤.IsMultiplicative
· 使用定理 `CategoryTheory.MorphismProperty.instRespectsIsoTop`：∀ {C : Type u} [inst
 : CategoryTheory.Category.{v, u} C], ⊤.RespectsIso
· 使用定理 `AlgebraicGeometry.instHasStrictInitialObjectsScheme`：CategoryTheory.Limi
ts.HasStrictInitialObjects AlgebraicGeometry.Scheme

--- 原说明 ---
If `S` is the empty scheme, the pro-étale site over `S` is a point.
-/
noncomputable def equivOfIsEmpty [IsEmpty S] : S.ProEt ≌ Discrete PUnit :=
  MorphismProperty.overEquivOfIsInitial _ _ _ isInitialOfIsEmpty

set_option backward.defeqAttrib.useBackward true in
variable {S} in
set_option backward.isDefEq.respectTransparency false in
/-
**AlgebraicGeometry.Scheme.ProEt.bot_mem_topology** 是 Mathlib 中的一个引理，位于命名空间 `Alg
ebraicGeometry.Scheme.ProEt`。
形式化陈述：bot_mem_topology (X : S.ProEt) [IsEmpty X.left] : ⊥ in topology S X
参数：X : S.ProEt。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `AlgebraicGeometry.Scheme.ProEt.topology.eq_1`：∀ (S : AlgebraicGeometry.S
cheme),   AlgebraicGeometry.Scheme.ProEt.topology S = (AlgebraicGeometry.Scheme.
ProEt.precoverage S).toGrothendiec…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `CategoryTheory.Sieve.generate_bot`：generate_bot : generate (⊥ : Presieve
 X) = ⊥
· 使用引理 `CategoryTheory.Precoverage.generate_mem_toGrothendieck`：generate_mem_toG
rothendieck {X : C} {R : Presieve X} (hR : R in J X) : Sieve.generate R in J.toG
rothendieck X
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用引理 `CategoryTheory.Presieve.map_bot`：map_bot : map F (⊥ : Presieve X) = ⊥
-/
lemma bot_mem_topology (X : S.ProEt) [IsEmpty X.left] : ⊥ ∈ topology S X := by
  rw [topology, ← Sieve.generate_bot]
  refine Precoverage.generate_mem_toGrothendieck ?_
  simp [precoverage, proetalePrecoverage, bot_mem_propQCPrecoverage]

set_option backward.isDefEq.respectTransparency.types false in
/-
**AlgebraicGeometry.Scheme.ProEt.topology_eq_top_of_isEmpty** 是 Mathlib 中的一个引理，位
于命名空间 `AlgebraicGeometry.Scheme.ProEt`。
形式化陈述：topology_eq_top_of_isEmpty [IsEmpty S] : topology S = ⊤
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `CategoryTheory.GrothendieckTopology.eq_top_iff`：eq_top_iff (J : Grothend
ieckTopology C) : J = ⊤ ↔ forall X, ⊥ in J X
· 使用定理 `Function.isEmpty`：∀ {α : Sort u} {β : Sort v} [IsEmpty β] (f : α → β), I
sEmpty α
· 使用引理 `AlgebraicGeometry.Scheme.ProEt.bot_mem_topology`：bot_mem_topology (X : S
.ProEt) [IsEmpty X.left] : ⊥ in topology S X
-/
lemma topology_eq_top_of_isEmpty [IsEmpty S] : topology S = ⊤ := by
  rw [GrothendieckTopology.eq_top_iff]
  intro X
  have : IsEmpty X.left := X.hom.base.1.1.isEmpty (β := S)
  exact bot_mem_topology _

end ProEt

end AlgebraicGeometry.Scheme

