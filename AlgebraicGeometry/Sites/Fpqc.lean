/-
Copyright (c) 2026 Christian Merten. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Christian Merten
-/
module

public import Mathlib.AlgebraicGeometry.EffectiveEpi
public import Mathlib.AlgebraicGeometry.Morphisms.UniversallyOpen
public import Mathlib.AlgebraicGeometry.Sites.SheafQuasiCompact
public import Mathlib.CategoryTheory.Sites.EffectiveEpimorphic

/-!
# Fpqc topology

In this file we define the fpqc topology and show it is subcanonical. It is the quasi-compact
topology for flat morphisms.

## Main declarations

- `fppfPrecoverage`: The precoverage given by jointly-surjective families of flat
  morphisms, locally of finite presentation.
- `fpqcPrecoverage`: The precoverage given by quasi-compact, jointly-surjective families of
  flat morphisms.
- The fpqc topology is subcanonical. This is available by `inferInstance`.

-/

@[expose] public section

universe u

open CategoryTheory

namespace AlgebraicGeometry.Scheme

set_option backward.isDefEq.respectTransparency.types false in
/-- The fppf precoverage on the category of schemes. The covering families are jointly-surjective
families of flat morphisms, locally of finite presentation. -/
/-
**AlgebraicGeometry.Scheme.fppfPrecoverage** 是 Mathlib 中的一个定义，位于命名空间 `AlgebraicG
eometry.Scheme`。
形式化陈述：fppfPrecoverage : Precoverage Scheme.{u}
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The fppf precoverage on the category of schemes. The covering families are joint
ly-surjective
families of flat morphisms, locally of finite presentation.
-/
def fppfPrecoverage : Precoverage Scheme.{u} :=
  precoverage (@Flat ⊓ @LocallyOfFinitePresentation)
  deriving Precoverage.IsStableUnderBaseChange, Precoverage.IsStableUnderComposition
/-
**AlgebraicGeometry.Scheme.zariskiPrecoverage_le_fppfPrecoverage** 是 Mathlib 中的一
个引理，位于命名空间 `AlgebraicGeometry.Scheme`。
形式化陈述：zariskiPrecoverage_le_fppfPrecoverage : zariskiPrecoverage <= fppfPrecover
age
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `AlgebraicGeometry.Scheme.precoverage_mono`：precoverage_mono {P Q : Morph
ismProperty Scheme.{u}} (h : P <= Q) : precoverage P <= precoverage Q
· 使用定理 `AlgebraicGeometry.Flat.instOfIsOpenImmersion`：∀ {X Y : AlgebraicGeometry
.Scheme} (f : X ⟶ Y) [AlgebraicGeometry.IsOpenImmersion f], AlgebraicGeometry.Fl
at f
· 使用定理 `AlgebraicGeometry.locallyOfFinitePresentation_of_isOpenImmersion`：∀ {X Y
 : AlgebraicGeometry.Scheme} (f : X ⟶ Y) [AlgebraicGeometry.IsOpenImmersion f], 
  AlgebraicGeometry.LocallyOfFinitePresentation f
-/
lemma zariskiPrecoverage_le_fppfPrecoverage :
    zariskiPrecoverage ≤ fppfPrecoverage :=
  precoverage_mono fun _ _ _ _ ↦ ⟨inferInstance, inferInstance⟩
/-
**AlgebraicGeometry.Scheme.fppfPrecoverage_eq_inf** 是 Mathlib 中的一个引理，位于命名空间 `Alg
ebraicGeometry.Scheme`。
形式化陈述：fppfPrecoverage_eq_inf : fppfPrecoverage = precoverage @Flat ⊓ precoverage
 @LocallyOfFinitePresentation
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma fppfPrecoverage_eq_inf :
    fppfPrecoverage = precoverage @Flat ⊓ precoverage @LocallyOfFinitePresentation := by
  grind [fppfPrecoverage, precoverage, precoverage, MorphismProperty.precoverage_inf]

/-- The fppf topology on the category of schemes is topology generated by the fppf precoverage. -/
/-
**AlgebraicGeometry.Scheme.fppfTopology** 是 Mathlib 中的一个缩写定义，位于命名空间 `AlgebraicGe
ometry.Scheme`。
形式化陈述：fppfTopology : GrothendieckTopology Scheme.{u}
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The fppf topology on the category of schemes is topology generated by the fppf p
recoverage.
-/
abbrev fppfTopology : GrothendieckTopology Scheme.{u} :=
  fppfPrecoverage.toGrothendieck

set_option backward.isDefEq.respectTransparency.types false in
/-- The fpqc precoverage on the category of schemes is the quasi-compact precoverage
on flat morphisms. The covering families are jointly-surjective, quasi-compact families of
flat morphisms. -/
/-
**AlgebraicGeometry.Scheme.fpqcPrecoverage** 是 Mathlib 中的一个定义，位于命名空间 `AlgebraicG
eometry.Scheme`。
形式化陈述：fpqcPrecoverage : Precoverage Scheme.{u}
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The fpqc precoverage on the category of schemes is the quasi-compact precoverage
on flat morphisms. The covering families are jointly-surjective, quasi-compact f
amilies of
flat morphisms.
-/
def fpqcPrecoverage : Precoverage Scheme.{u} :=
  propQCPrecoverage @Flat
  deriving Precoverage.HasIsos, Precoverage.IsStableUnderBaseChange,
    Precoverage.IsStableUnderComposition

set_option backward.isDefEq.respectTransparency.types false in
/-
**AlgebraicGeometry.Scheme.fppfPrecoverage_le_fpqcPrecoverage** 是 Mathlib 中的一个引理
，位于命名空间 `AlgebraicGeometry.Scheme`。
形式化陈述：fppfPrecoverage_le_fpqcPrecoverage : fppfPrecoverage <= fpqcPrecoverage
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `AlgebraicGeometry.Scheme.fpqcPrecoverage.eq_1`：AlgebraicGeometry.Scheme.
fpqcPrecoverage = AlgebraicGeometry.Scheme.propQCPrecoverage @AlgebraicGeometry.
Flat
· 使用定理 `AlgebraicGeometry.Scheme.propQCPrecoverage.eq_1`：∀ (P : CategoryTheory.M
orphismProperty AlgebraicGeometry.Scheme),   AlgebraicGeometry.Scheme.propQCPrec
overage P =     AlgebraicGeometry.Sch…
· 使用定理 `le_inf_iff`：∀ {α : Type u} [inst : SemilatticeInf α] {c a b : α}, c ≤ a 
⊓ b ↔ c ≤ a ∧ c ≤ b
· 使用引理 `AlgebraicGeometry.Scheme.precoverage_le_qcPrecoverage_of_isOpenMap`：prec
overage_le_qcPrecoverage_of_isOpenMap {P : MorphismProperty Scheme.{u}} (hP : P 
<= fun _ _ f => IsOpenMap f.base) : precoverage P <= qcP…
· 使用定理 `AlgebraicGeometry.Scheme.Hom.isOpenMap`：∀ {X Y : AlgebraicGeometry.Schem
e} (f : X ⟶ Y) [AlgebraicGeometry.UniversallyOpen f], IsOpenMap ⇑f
· 使用定理 `AlgebraicGeometry.UniversallyOpen.of_flat`：∀ {X Y : AlgebraicGeometry.Sc
heme} (f : X ⟶ Y) [AlgebraicGeometry.Flat f]   [AlgebraicGeometry.LocallyOfFinit
ePresentation f], AlgebraicGeom…
· 使用引理 `AlgebraicGeometry.Scheme.precoverage_mono`：precoverage_mono {P Q : Morph
ismProperty Scheme.{u}} (h : P <= Q) : precoverage P <= precoverage Q
-/
lemma fppfPrecoverage_le_fpqcPrecoverage : fppfPrecoverage ≤ fpqcPrecoverage := by
  rw [fpqcPrecoverage, propQCPrecoverage, le_inf_iff]
  refine ⟨?_, precoverage_mono fun X Y f ⟨hf, _⟩ ↦ inferInstance⟩
  exact precoverage_le_qcPrecoverage_of_isOpenMap fun X Y f ⟨_, _⟩ ↦ f.isOpenMap
/-
**AlgebraicGeometry.Scheme.zariskiPrecoverage_le_fpqcPrecoverage** 是 Mathlib 中的一
个引理，位于命名空间 `AlgebraicGeometry.Scheme`。
形式化陈述：zariskiPrecoverage_le_fpqcPrecoverage : zariskiPrecoverage <= fpqcPrecover
age
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_trans`：le_trans : a <= b -> b <= c -> a <= c
· 使用引理 `AlgebraicGeometry.Scheme.zariskiPrecoverage_le_fppfPrecoverage`：zariskiP
recoverage_le_fppfPrecoverage : zariskiPrecoverage <= fppfPrecoverage
· 使用引理 `AlgebraicGeometry.Scheme.fppfPrecoverage_le_fpqcPrecoverage`：fppfPrecove
rage_le_fpqcPrecoverage : fppfPrecoverage <= fpqcPrecoverage
-/
lemma zariskiPrecoverage_le_fpqcPrecoverage : zariskiPrecoverage ≤ fpqcPrecoverage :=
  le_trans zariskiPrecoverage_le_fppfPrecoverage fppfPrecoverage_le_fpqcPrecoverage

/-- The fpqc topology on the category of schemes is topology generated by the fpqc precoverage. -/
/-
**AlgebraicGeometry.Scheme.fpqcTopology** 是 Mathlib 中的一个缩写定义，位于命名空间 `AlgebraicGe
ometry.Scheme`。
形式化陈述：fpqcTopology : GrothendieckTopology Scheme.{u}
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The fpqc topology on the category of schemes is topology generated by the fpqc p
recoverage.
-/
abbrev fpqcTopology : GrothendieckTopology Scheme.{u} :=
  fpqcPrecoverage.toGrothendieck
/-
**AlgebraicGeometry.Scheme.fpqcTopology_eq_propQCTopology** 是 Mathlib 中的一个引理，位于命
名空间 `AlgebraicGeometry.Scheme`。
形式化陈述：fpqcTopology_eq_propQCTopology : fpqcTopology = Scheme.propQCTopology @Fla
t
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma fpqcTopology_eq_propQCTopology : fpqcTopology = Scheme.propQCTopology @Flat := rfl
/-
**AlgebraicGeometry.Scheme.zariskiTopology_le_fpqcTopology** 是 Mathlib 中的一个引理，位于
命名空间 `AlgebraicGeometry.Scheme`。
形式化陈述：zariskiTopology_le_fpqcTopology : zariskiTopology <= fpqcTopology
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Precoverage.toGrothendieck_mono`：∀ {C : Type u_3} [inst :
 CategoryTheory.Category.{u_2, u_3} C] {J K : CategoryTheory.Precoverage C},   J
 ≤ K → J.toGrothendieck ≤ K.toGrothe…
· 使用引理 `AlgebraicGeometry.Scheme.zariskiPrecoverage_le_fpqcPrecoverage`：zariskiP
recoverage_le_fpqcPrecoverage : zariskiPrecoverage <= fpqcPrecoverage
-/
lemma zariskiTopology_le_fpqcTopology : zariskiTopology ≤ fpqcTopology :=
  Precoverage.toGrothendieck_mono zariskiPrecoverage_le_fpqcPrecoverage
/-
**AlgebraicGeometry.Scheme.fppfTopology_le_fpqcTopology** 是 Mathlib 中的一个引理，位于命名空
间 `AlgebraicGeometry.Scheme`。
形式化陈述：fppfTopology_le_fpqcTopology : fppfTopology <= fpqcTopology
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Precoverage.toGrothendieck_mono`：∀ {C : Type u_3} [inst :
 CategoryTheory.Category.{u_2, u_3} C] {J K : CategoryTheory.Precoverage C},   J
 ≤ K → J.toGrothendieck ≤ K.toGrothe…
· 使用引理 `AlgebraicGeometry.Scheme.fppfPrecoverage_le_fpqcPrecoverage`：fppfPrecove
rage_le_fpqcPrecoverage : fppfPrecoverage <= fpqcPrecoverage
-/
lemma fppfTopology_le_fpqcTopology : fppfTopology ≤ fpqcTopology :=
  Precoverage.toGrothendieck_mono fppfPrecoverage_le_fpqcPrecoverage

set_option backward.isDefEq.respectTransparency.types false in
/-
**AlgebraicGeometry.Scheme.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.Scheme`
。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : fpqcTopology.Subcanonical := by
  refine GrothendieckTopology.Subcanonical.of_isSheaf_yoneda_obj _ fun X ↦ ?_
  rw [fpqcTopology_eq_propQCTopology, isSheaf_type_propQCTopology_iff]
  refine ⟨?_, ?_⟩
  · exact GrothendieckTopology.Subcanonical.isSheaf_of_isRepresentable _
  · intro R S f hf₁ hf₂
    have : IsRegularEpi (Spec.map f) :=
      isRegularEpi_of_flat_of_surjective_of_isAffine _
    exact .singleton_of_isRepresentable_of_effectiveEpi _ _
/-
**AlgebraicGeometry.Scheme.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.Scheme`
。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : fppfTopology.Subcanonical :=
  .of_le fppfTopology_le_fpqcTopology

@[simp]
/-
**AlgebraicGeometry.Scheme.Hom.singleton_mem_fppfPrecoverage** 是 Mathlib 中的一个定理，
位于命名空间 `AlgebraicGeometry.Scheme.Hom`。
形式化陈述：∀ {X Y : AlgebraicGeometry.Scheme} (f : X ⟶ Y) [AlgebraicGeometry.Flat f] 
[AlgebraicGeometry.Surjective f]   [AlgebraicGeometry.LocallyOfFinitePresentatio
n f],   CategoryTheory.Presieve.singleton f ∈ AlgebraicGeometry.Scheme.fppfPreco
verage.coverings Y
参数：f : X ⟶ Y。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `CategoryTheory.Presieve.ofArrows_pUnit`：ofArrows_pUnit : (ofArrows _ fun
 _ : PUnit.{w + 1} => f) = singleton f
· 使用定理 `CategoryTheory.Precoverage.ZeroHypercover.mem₀`：∀ {C : Type u} [inst : C
ategoryTheory.Category.{v, u} C] {J : CategoryTheory.Precoverage C} {S : C}   (s
elf : J.ZeroHypercover S), self.pres…
-/
lemma Hom.singleton_mem_fppfPrecoverage {X Y : Scheme.{u}} (f : X ⟶ Y) [Flat f] [Surjective f]
    [LocallyOfFinitePresentation f] :
    Presieve.singleton f ∈ fppfPrecoverage Y := by
  rw [← Presieve.ofArrows_pUnit.{0}]
  exact (f.cover (P := @Flat ⊓ @LocallyOfFinitePresentation) ⟨‹_›, ‹_›⟩).mem₀

@[simp]
/-
**AlgebraicGeometry.Scheme.Hom.singleton_mem_fpqcPrecoverage** 是 Mathlib 中的一个定理，
位于命名空间 `AlgebraicGeometry.Scheme.Hom`。
形式化陈述：∀ {X Y : AlgebraicGeometry.Scheme} (f : X ⟶ Y) [AlgebraicGeometry.Flat f] 
[AlgebraicGeometry.Surjective f]   [AlgebraicGeometry.QuasiCompact f],   Categor
yTheory.Presieve.singleton f ∈ AlgebraicGeometry.Scheme.fpqcPrecoverage.covering
s Y
参数：f : X ⟶ Y。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AlgebraicGeometry.Scheme.Hom.singleton_mem_propQCPrecoverage`：∀ {P : Cat
egoryTheory.MorphismProperty AlgebraicGeometry.Scheme} {X Y : AlgebraicGeometry.
Scheme} {f : X ⟶ Y},   P f →     ∀ [AlgebraicGeome…
-/
lemma Hom.singleton_mem_fpqcPrecoverage {X Y : Scheme.{u}} (f : X ⟶ Y) [Flat f] [Surjective f]
    [QuasiCompact f] :
    Presieve.singleton f ∈ fpqcPrecoverage Y :=
  Hom.singleton_mem_propQCPrecoverage ‹_›

/-- Any surjective, quasi-compact and flat morphism is an effective epimorphism. -/
/-
**AlgebraicGeometry.Scheme.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.Scheme`
。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Any surjective, quasi-compact and flat morphism is an effective epimorphism.
-/
instance {X Y : Scheme} (f : X ⟶ Y) [QuasiCompact f] [Surjective f] [Flat f] : EffectiveEpi f := by
  rw [← Sieve.effectiveEpimorphic_singleton,
    Presieve.EffectiveEpimorphic.iff_forall_isSheafFor_yoneda]
  intro Z
  exact (GrothendieckTopology.Subcanonical.isSheaf_of_isRepresentable _).isSheafFor _
    (Precoverage.generate_mem_toGrothendieck f.singleton_mem_fpqcPrecoverage)

/-- Any surjective, flat morphism locally of finite presentation is an effective epimorphism.
In particular, étale surjections satisfy this. -/
/-
**AlgebraicGeometry.Scheme.** 是 Mathlib 中的一个实例，位于命名空间 `AlgebraicGeometry.Scheme`
。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Any surjective, flat morphism locally of finite presentation is an effective epi
morphism.
In particular, étale surjections satisfy this.
-/
instance {X Y : Scheme} (f : X ⟶ Y) [LocallyOfFinitePresentation f] [Surjective f] [Flat f] :
    EffectiveEpi f := by
  rw [← Sieve.effectiveEpimorphic_singleton,
    Presieve.EffectiveEpimorphic.iff_forall_isSheafFor_yoneda]
  intro Z
  exact (GrothendieckTopology.Subcanonical.isSheaf_of_isRepresentable _).isSheafFor _
    (Precoverage.generate_mem_toGrothendieck f.singleton_mem_fppfPrecoverage)

end AlgebraicGeometry.Scheme

