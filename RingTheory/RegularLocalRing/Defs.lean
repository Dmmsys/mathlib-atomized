/-
Copyright (c) 2025 Nailin Guan. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Nailin Guan
-/
module

public import Mathlib.Algebra.Module.SpanRankOperations
public import Mathlib.RingTheory.DedekindDomain.Dvr
public import Mathlib.RingTheory.Ideal.KrullsHeightTheorem
public import Mathlib.RingTheory.KrullDimension.Field
public import Mathlib.RingTheory.KrullDimension.PID

/-!
# Regular local rings

For a Noetherian local ring `R`, we define `IsRegularLocalRing` as
`(maximalIdeal R).spanFinrank = ringKrullDim R`. This definition is equivalent to
the dimension of the cotangent space over the residue field being equal to `ringKrullDim R`,
(see `IsRegularLocalRing.iff_finrank_cotangentSpace`).

For the next section, we define regular rings as Noetherian rings whose localization at every prime
are regular local rings.
(Note that a regular local ring is a regular ring, but this is not immediate under this definition).

# Main Definition and Results

* `IsRegularLocalRing` : A Noetherian local ring is regular if
  `(maximalIdeal R).spanFinrank = ringKrullDim R`,
  i.e. its maximal ideal can be generated by `dim R` elements.

* `IsRegularLocalRing.iff_finrank_cotangentSpace` : the equivalence of `IsRegularLocalRing` and
  `Module.finrank (ResidueField R) (CotangentSpace R) = ringKrullDim R`

* `IsRegularRing` : A noetherian ring is regular if its localization at any prime
  `IsRegularLocalRing`.

## TODO
Show that regular local rings are regular under this definition.
This follows from localizations of regular local rings being regular (@Thmoas-Guan).

-/

public section

open IsLocalRing

/-- A Noetherian local ring is said to be regular if its maximal ideal
can be generated by `dim R` elements. -/
@[stacks 00KU]
/-
**IsRegularLocalRing** 是 Mathlib 中的一个归纳类型，位于命名空间 ``。
形式化陈述：(R : Type u_1) → [CommRing R] → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A Noetherian local ring is said to be regular if its maximal ideal
can be generated by `dim R` elements.
-/
class IsRegularLocalRing (R : Type*) [CommRing R] : Prop extends
    IsLocalRing R, IsNoetherianRing R where
  spanFinrank_maximalIdeal : (maximalIdeal R).spanFinrank = ringKrullDim R

variable (R : Type*) [CommRing R]
/-
**isRegularLocalRing_iff** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：isRegularLocalRing_iff [IsLocalRing R] [IsNoetherianRing R] : IsRegularLoc
alRing R ↔ (maximalIdeal R).spanFinrank = ringKrullDim R
该定理/引理刻画了左右两侧的等价关系。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma isRegularLocalRing_iff [IsLocalRing R] [IsNoetherianRing R] :
    IsRegularLocalRing R ↔ (maximalIdeal R).spanFinrank = ringKrullDim R :=
  ⟨fun ⟨h⟩ ↦ h, fun h ↦ ⟨h⟩⟩

namespace IsRegularLocalRing

/-
**IsRegularLocalRing.of_spanFinrank_maximalIdeal_le** 是 Mathlib 中的一个引理，位于命名空间 `I
sRegularLocalRing`。
形式化陈述：of_spanFinrank_maximalIdeal_le [IsLocalRing R] [IsNoetherianRing R] (le : 
(maximalIdeal R).spanFinrank <= ringKrullDim R) : IsRegularLocalRing R
参数：le : (maximalIdeal R).spanFinrank <= ringKrullDim R。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用引理 `isRegularLocalRing_iff`：isRegularLocalRing_iff [IsLocalRing R] [IsNoethe
rianRing R] : IsRegularLocalRing R ↔ (maximalIdeal R).spanFinrank = ringKrullDim
 R
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用引理 `ringKrullDim_le_spanFinrank_maximalIdeal`：ringKrullDim_le_spanFinrank_ma
ximalIdeal [IsLocalRing R] : ringKrullDim R <= (IsLocalRing.maximalIdeal R).span
Finrank
-/
lemma of_spanFinrank_maximalIdeal_le [IsLocalRing R] [IsNoetherianRing R]
    (le : (maximalIdeal R).spanFinrank ≤ ringKrullDim R) : IsRegularLocalRing R :=
  (isRegularLocalRing_iff _).mpr (le_antisymm le (ringKrullDim_le_spanFinrank_maximalIdeal R))

variable {R} in
/-
**IsRegularLocalRing.of_ringEquiv** 是 Mathlib 中的一个引理，位于命名空间 `IsRegularLocalRing`
。
形式化陈述：of_ringEquiv [IsRegularLocalRing R] {R' : Type*} [CommRing R'] (e : R ≃+* 
R') : IsRegularLocalRing R'
参数：e : R ≃+* R'。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `RingEquiv.isLocalRing`：∀ {A : Type u_4} {B : Type u_5} [inst : CommSemir
ing A] [IsLocalRing A] [inst_2 : Semiring B] (e : A ≃+* B),   IsLocalRing B
· 使用定理 `IsRegularLocalRing.toIsLocalRing`：∀ {R : Type u_1} {inst : CommRing R} [
self : IsRegularLocalRing R], IsLocalRing R
· 使用定理 `isNoetherianRing_of_ringEquiv`：isNoetherianRing_of_ringEquiv (R) [Semiri
ng R] {S} [Semiring S] (f : R ≃+* S) [IsNoetherianRing R] : IsNoetherianRing S
· 使用定理 `IsRegularLocalRing.toIsNoetherian`：∀ {R : Type u_1} {inst : CommRing R} 
[self : IsRegularLocalRing R], IsNoetherian R R
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `isRegularLocalRing_iff`：isRegularLocalRing_iff [IsLocalRing R] [IsNoethe
rianRing R] : IsRegularLocalRing R ↔ (maximalIdeal R).spanFinrank = ringKrullDim
 R
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `ringKrullDim_eq_of_ringEquiv`：ringKrullDim_eq_of_ringEquiv (e : R ≃+* S)
 : ringKrullDim R = ringKrullDim S
· 使用引理 `IsLocalRing.map_ringEquiv_maximalIdeal`：map_ringEquiv_maximalIdeal [Comm
Ring R] [CommRing S] [IsLocalRing R] [IsLocalRing S] (e : R ≃+* S) : (maximalIde
al R).map e = maximalIdeal S
· 使用引理 `Ideal.spanFinrank_map_eq_of_ringEquiv`：Ideal.spanFinrank_map_eq_of_ringE
quiv (f : R ≃+* T) (I : Ideal R) : (I.map f).spanFinrank = I.spanFinrank
-/
lemma of_ringEquiv [IsRegularLocalRing R] {R' : Type*} [CommRing R']
    (e : R ≃+* R') : IsRegularLocalRing R' := by
  have := e.isLocalRing
  have := isNoetherianRing_of_ringEquiv R e
  rwa [isRegularLocalRing_iff, ← ringKrullDim_eq_of_ringEquiv e, ← map_ringEquiv_maximalIdeal e,
    Ideal.spanFinrank_map_eq_of_ringEquiv, ← isRegularLocalRing_iff]
/-
**IsRegularLocalRing.iff_finrank_cotangentSpace** 是 Mathlib 中的一个引理，位于命名空间 `IsReg
ularLocalRing`。
形式化陈述：iff_finrank_cotangentSpace [IsLocalRing R] [IsNoetherianRing R] : IsRegula
rLocalRing R ↔ Module.finrank (ResidueField R) (CotangentSpace R) = ringKrullDim
 R
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `isRegularLocalRing_iff`：isRegularLocalRing_iff [IsLocalRing R] [IsNoethe
rianRing R] : IsRegularLocalRing R ↔ (maximalIdeal R).spanFinrank = ringKrullDim
 R
· 使用引理 `IsLocalRing.spanFinrank_maximalIdeal_eq_finrank_cotangentSpace`：spanFinr
ank_maximalIdeal_eq_finrank_cotangentSpace [IsNoetherianRing R] : (maximalIdeal 
R).spanFinrank = Module.finrank (ResidueField R) (Co…
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma iff_finrank_cotangentSpace [IsLocalRing R] [IsNoetherianRing R] :
    IsRegularLocalRing R ↔ Module.finrank (ResidueField R) (CotangentSpace R) = ringKrullDim R := by
  rw [isRegularLocalRing_iff, spanFinrank_maximalIdeal_eq_finrank_cotangentSpace]
/-
**IsRegularLocalRing.** 是 Mathlib 中的一个实例，位于命名空间 `IsRegularLocalRing`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [IsLocalRing R] [IsDomain R] [IsPrincipalIdealRing R] : IsRegularLocalRing R := by
  by_cases isf : IsField R
  · let := isf.toField
    simp [isRegularLocalRing_iff, maximalIdeal_eq_bot]
  · apply of_spanFinrank_maximalIdeal_le
    rcases IsPrincipalIdealRing.principal (maximalIdeal R) with ⟨x, hx⟩
    simpa only [(IsPrincipalIdealRing.ringKrullDim_eq_one R) isf,
      Nat.cast_le_one, ← Set.ncard_singleton x, hx] using
        Submodule.spanFinrank_span_le_ncard_of_finite (Set.finite_singleton x)

end IsRegularLocalRing

/-- A noetherian ring is regular if its localization at any prime `IsRegularLocalRing`. -/
/-
**IsRegularRing** 是 Mathlib 中的一个归纳类型，位于命名空间 ``。
形式化陈述：(R : Type u_2) → [CommRing R] → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A noetherian ring is regular if its localization at any prime `IsRegularLocalRin
g`.
-/
class IsRegularRing (R : Type*) [CommRing R] : Prop extends IsNoetherianRing R where
  isRegularLocalRing_localization (p : Ideal R) [p.IsPrime] :
    IsRegularLocalRing (Localization.AtPrime p)

attribute [instance low] IsRegularRing.isRegularLocalRing_localization

section IsRegularRing

variable {R} in
/-
**isRegularRing_iff** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：isRegularRing_iff [IsNoetherianRing R] : IsRegularRing R ↔ forall (p : Ide
al R) [p.IsPrime], IsRegularLocalRing (Localization.AtPrime p)
该定理/引理刻画了左右两侧的等价关系。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma isRegularRing_iff [IsNoetherianRing R] : IsRegularRing R ↔
    ∀ (p : Ideal R) [p.IsPrime], IsRegularLocalRing (Localization.AtPrime p) :=
  ⟨fun ⟨h⟩ ↦ h, fun h ↦ ⟨h⟩⟩

variable {R} in
/-
**IsRegularRing.of_ringEquiv** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：IsRegularRing.of_ringEquiv {R' : Type*} [CommRing R'] (e : R ≃+* R') [IsRe
gularRing R] : IsRegularRing R'
参数：e : R ≃+* R'。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `isNoetherianRing_of_ringEquiv`：isNoetherianRing_of_ringEquiv (R) [Semiri
ng R] {S} [Semiring S] (f : R ≃+* S) [IsNoetherianRing R] : IsNoetherianRing S
· 使用定理 `IsRegularRing.toIsNoetherian`：∀ {R : Type u_2} {inst : CommRing R} [self
 : IsRegularRing R], IsNoetherian R R
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `isRegularRing_iff`：isRegularRing_iff [IsNoetherianRing R] : IsRegularRin
g R ↔ forall (p : Ideal R) [p.IsPrime], IsRegularLocalRing (Localization.AtPrime
 p)
· 使用引理 `IsRegularLocalRing.of_ringEquiv`：of_ringEquiv [IsRegularLocalRing R] {R'
 : Type*} [CommRing R'] (e : R ≃+* R') : IsRegularLocalRing R'
· 使用定理 `RingEquivClass.toRingHomClass`：∀ {F : Type u_1} {R : Type u_4} {S : Type
 u_5} [inst : EquivLike F R S] [inst_1 : NonAssocSemiring R]   [inst_2 : NonAsso
cSemiring S] [h : R…
· 使用定理 `RingEquiv.instRingEquivClass`：∀ {R : Type u_4} {S : Type u_5} [inst : Mu
l R] [inst_1 : Mul S] [inst_2 : Add R] [inst_3 : Add S],   RingEquivClass (R ≃+*
 S) R S
· 使用定理 `Ideal.IsPrime.comap`：∀ {R : Type u} {S : Type v} {F : Type u_1} [inst : 
Semiring R] [inst_1 : Semiring S] [inst_2 : FunLike F R S] (f : F)   {K : Ideal 
S} [inst_…
· 使用定理 `IsRegularRing.isRegularLocalRing_localization`：∀ {R : Type u_2} {inst : 
CommRing R} [self : IsRegularRing R] (p : Ideal R) [inst_1 : p.IsPrime],   IsReg
ularLocalRing (Localization.AtPrime…
· 使用定理 `RingEquiv.map_primeCompl_comap_eq`：∀ {R : Type u} {S : Type v} [inst : S
emiring R] [inst_1 : Semiring S] (e : R ≃+* S) (p : Ideal S) [inst_2 : p.IsPrime
],   Submonoid.map e (I…
-/
lemma IsRegularRing.of_ringEquiv {R' : Type*} [CommRing R'] (e : R ≃+* R') [IsRegularRing R] :
    IsRegularRing R' := by
  have := isNoetherianRing_of_ringEquiv R e
  rw [isRegularRing_iff]
  intro p hp
  exact IsRegularLocalRing.of_ringEquiv <| IsLocalization.ringEquivOfRingEquiv
    (Localization.AtPrime (p.comap e)) (Localization.AtPrime p) e (e.map_primeCompl_comap_eq p)
/-
**IsRegularLocalRing.of_isRegularRing_of_isLocalRing** 是 Mathlib 中的一个引理，位于命名空间 `
`。
形式化陈述：IsRegularLocalRing.of_isRegularRing_of_isLocalRing [IsLocalRing R] [IsRegu
larRing R] : IsRegularLocalRing R
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.IsMaximal.isPrime'`：∀ {α : Type u} [inst : CommSemiring α] (I : Id
eal α) [_H : I.IsMaximal], I.IsPrime
· 使用定理 `IsLocalRing.maximalIdeal.isMaximal`：∀ (R : Type u_1) [inst : CommSemirin
g R] [inst_1 : IsLocalRing R], (IsLocalRing.maximalIdeal R).IsMaximal
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `IsRegularLocalRing.of_ringEquiv`：of_ringEquiv [IsRegularLocalRing R] {R'
 : Type*} [CommRing R'] (e : R ≃+* R') : IsRegularLocalRing R'
· 使用定理 `IsRegularRing.isRegularLocalRing_localization`：∀ {R : Type u_2} {inst : 
CommRing R} [self : IsRegularRing R] (p : Ideal R) [inst_1 : p.IsPrime],   IsReg
ularLocalRing (Localization.AtPrime…
-/
lemma IsRegularLocalRing.of_isRegularRing_of_isLocalRing [IsLocalRing R] [IsRegularRing R] :
    IsRegularLocalRing R := by
  let e : R ≃ₐ[R] (Localization.AtPrime (maximalIdeal R)) :=
    IsLocalization.atUnits R (maximalIdeal R).primeCompl (fun x ↦ by simpa using! fun a ↦ a)
  exact IsRegularLocalRing.of_ringEquiv e.toRingEquiv.symm
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := low) [IsDedekindDomain R] : IsRegularRing R := by
  rw [isRegularRing_iff]
  intro p hp
  by_cases eqbot : p = ⊥
  · let : Field (Localization.AtPrime p) := IsField.toField <| by
      simp [isField_iff_maximalIdeal_eq, ← Localization.AtPrime.map_eq_maximalIdeal, eqbot]
    infer_instance
  · have := IsLocalization.AtPrime.isDiscreteValuationRing_of_dedekind_domain
      R eqbot (Localization.AtPrime p)
    infer_instance

end IsRegularRing

