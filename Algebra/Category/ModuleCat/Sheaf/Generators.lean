/-
Copyright (c) 2024 Joël Riou. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Joël Riou
-/
module

public import Mathlib.Algebra.Category.ModuleCat.Sheaf.Free
public import Mathlib.Algebra.Category.ModuleCat.Sheaf.PushforwardContinuous
public import Mathlib.CategoryTheory.Sites.CoversTop.Basic

/-!
# Generating sections of sheaves of modules

In this file, given a sheaf of modules `M` over a sheaf of rings `R`, we introduce
the structure `M.GeneratingSections` which consists of a family of (global)
sections `s : I → M.sections` which generate `M`.

We also introduce the structure `M.LocalGeneratorsData` which contains the data
of a covering `X i` of the terminal object and the data of a
`(M.over (X i)).GeneratingSections` for all `i`. This is used in order to
define sheaves of modules of finite type.

## References

* https://stacks.math.columbia.edu/tag/01B4

-/

@[expose] public section

universe w u v' u' u₁ v₁

open CategoryTheory Limits

variable {C : Type u'} [Category.{v'} C] {J : GrothendieckTopology C} {R : Sheaf J RingCat.{u}}
  [HasWeakSheafify J AddCommGrpCat.{u}] [J.WEqualsLocallyBijective AddCommGrpCat.{u}]

namespace SheafOfModules

variable (M N P : SheafOfModules.{u} R)

/-- The type of sections which generate a sheaf of modules. -/
/-
**SheafOfModules.GeneratingSections** 是 Mathlib 中的一个结构，位于命名空间 `SheafOfModules`。
形式化陈述：GeneratingSections where /-- the index type for the sections -/ I : Type u
 /-- a family of sections which generate the sheaf of modules -/ s : I -> M.sect
ions epi : Epi (M.freeHomEquiv.symm s)
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The type of sections which generate a sheaf of modules.
-/
structure GeneratingSections where
  /-- the index type for the sections -/
  I : Type u
  /-- a family of sections which generate the sheaf of modules -/
  s : I → M.sections
  epi : Epi (M.freeHomEquiv.symm s) := by infer_instance

namespace GeneratingSections

attribute [instance] epi

variable {M N P}

/-- The epimorphism `free σ.I ⟶ M` given by `σ : M.GeneratingSections`. -/
/-
**SheafOfModules.GeneratingSections.** 是 Mathlib 中的一个缩写定义，位于命名空间 `SheafOfModules
.GeneratingSections`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The epimorphism `free σ.I ⟶ M` given by `σ : M.GeneratingSections`.
-/
noncomputable abbrev π (σ : M.GeneratingSections) : free σ.I ⟶ M := M.freeHomEquiv.symm σ.s

/-- If `M ⟶ N` is an epimorphism and that `M` is generated by some sections,
then `N` is generated by the images of these sections. -/
@[simps]
/-
**SheafOfModules.GeneratingSections.ofEpi** 是 Mathlib 中的一个定义，位于命名空间 `SheafOfModu
les.GeneratingSections`。
形式化陈述：ofEpi (σ : M.GeneratingSections) (p : M ⟶ N) [Epi p] : N.GeneratingSection
s where I
参数：σ : M.GeneratingSections；p : M ⟶ N。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If `M ⟶ N` is an epimorphism and that `M` is generated by some sections,
then `N` is generated by the images of these sections.
-/
def ofEpi (σ : M.GeneratingSections) (p : M ⟶ N) [Epi p] :
    N.GeneratingSections where
  I := σ.I
  s i := sectionsMap p (σ.s i)
  epi := by
    rw [← freeHomEquiv_symm_comp]
    apply epi_comp
/-
**SheafOfModules.GeneratingSections.opEpi_id** 是 Mathlib 中的一个引理，位于命名空间 `SheafOfM
odules.GeneratingSections`。
形式化陈述：opEpi_id (σ : M.GeneratingSections) : σ.ofEpi (𝟙 M) = σ
参数：σ : M.GeneratingSections。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.instEpiId`：∀ {C : Type u} [inst : CategoryTheory.Category
.{v, u} C] (X : C),   CategoryTheory.Epi (CategoryTheory.CategoryStruct.id X)
-/
lemma opEpi_id (σ : M.GeneratingSections) :
    σ.ofEpi (𝟙 M) = σ := rfl
/-
**SheafOfModules.GeneratingSections.opEpi_comp** 是 Mathlib 中的一个引理，位于命名空间 `SheafO
fModules.GeneratingSections`。
形式化陈述：opEpi_comp (σ : M.GeneratingSections) (p : M ⟶ N) (q : N ⟶ P) [Epi p] [Epi
 q] : σ.ofEpi (p ≫ q) = (σ.ofEpi p).ofEpi q
参数：σ : M.GeneratingSections；p : M ⟶ N；q : N ⟶ P。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma opEpi_comp (σ : M.GeneratingSections) (p : M ⟶ N) (q : N ⟶ P) [Epi p] [Epi q] :
    σ.ofEpi (p ≫ q) = (σ.ofEpi p).ofEpi q := rfl

@[simp]
/-
**SheafOfModules.GeneratingSections.ofEpi_** 是 Mathlib 中的一个引理，位于命名空间 `SheafOfMod
ules.GeneratingSections`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma ofEpi_π (σ : M.GeneratingSections) (p : M ⟶ N) [Epi p] :
    (σ.ofEpi p).π = σ.π ≫ p := by
  simp [ofEpi, freeHomEquiv_symm_comp]

/-- Two isomorphic sheaves of modules have equivalent families of generating sections. -/
/-
**SheafOfModules.GeneratingSections.equivOfIso** 是 Mathlib 中的一个定义，位于命名空间 `SheafO
fModules.GeneratingSections`。
形式化陈述：equivOfIso (e : M ≅ N) : M.GeneratingSections ≃ N.GeneratingSections where
 toFun σ
参数：e : M ≅ N。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Two isomorphic sheaves of modules have equivalent families of generating section
s.
-/
def equivOfIso (e : M ≅ N) :
    M.GeneratingSections ≃ N.GeneratingSections where
  toFun σ := σ.ofEpi e.hom
  invFun σ := σ.ofEpi e.inv
  left_inv σ := by
    dsimp
    simp only [← opEpi_comp, e.hom_inv_id, opEpi_id]
  right_inv σ := by
    dsimp
    simp only [← opEpi_comp, e.inv_hom_id, opEpi_id]

/-- `σ : M.GeneratingSections` is finite type if the index type `σ.I` is finite. -/
/-
**SheafOfModules.GeneratingSections.IsFiniteType** 是 Mathlib 中的一个类，位于命名空间 `Sheaf
OfModules.GeneratingSections`。
形式化陈述：IsFiniteType (σ : M.GeneratingSections) : Prop where finite : Finite σ.I
参数：σ : M.GeneratingSections。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`σ : M.GeneratingSections` is finite type if the index type `σ.I` is finite.
-/
class IsFiniteType (σ : M.GeneratingSections) : Prop where
  finite : Finite σ.I := by infer_instance

attribute [instance] IsFiniteType.finite
/-
**SheafOfModules.GeneratingSections.** 是 Mathlib 中的一个实例，位于命名空间 `SheafOfModules.G
eneratingSections`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (σ : M.GeneratingSections) (p : M ⟶ N) [Epi p] [σ.IsFiniteType] :
    (σ.ofEpi p).IsFiniteType where
  finite := inferInstanceAs (Finite σ.I)

end GeneratingSections

section

variable [∀ (X : C), HasWeakSheafify (J.over X) AddCommGrpCat.{u}]
  [∀ (X : C), (J.over X).WEqualsLocallyBijective AddCommGrpCat.{u}]

/-- The data of generating sections of the restriction of a sheaf of modules
over a covering of the terminal object. -/
/-
**SheafOfModules.LocalGeneratorsData** 是 Mathlib 中的一个归纳类型，位于命名空间 `SheafOfModules
`。
形式化陈述：{C : Type u'} →   [inst : CategoryTheory.Category.{v', u'} C] →     {J : C
ategoryTheory.GrothendieckTopology C} →       {R : CategoryTheory.Sheaf J RingCa
t} →         SheafOfModules R →           [∀ (X : C), CategoryTheory.HasWeakShea
fify (J.over X) AddCommGrpCat] →             [∀ (X : C), (J.over X).WEqualsLocal
lyBijective AddCommGrpCat] → Type (max (max (max (u + 1) u') v') (w + 1))
参数：X : C；J.over X；X : C；J.over X；max (max (max (u + 1) u') v') (w + 1)。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The data of generating sections of the restriction of a sheaf of modules
over a covering of the terminal object.
-/
structure LocalGeneratorsData where
  /-- the index type of the covering -/
  I : Type w
  /-- a family of objects which cover the terminal object -/
  X : I → C
  coversTop : J.CoversTop X
  /-- the data of sections of `M` over `X i` which generate `M.over (X i)` -/
  generators (i : I) : (M.over (X i)).GeneratingSections

/-- Shrink the indexing type of `LocalGeneratorsData` into the universe of the site. -/
noncomputable
/-
**SheafOfModules.LocalGeneratorsData.shrink** 是 Mathlib 中的一个定义，位于命名空间 `SheafOfMo
dules.LocalGeneratorsData`。
形式化陈述：{C : Type u'} →   [inst : CategoryTheory.Category.{v', u'} C] →     {J : C
ategoryTheory.GrothendieckTopology C} →       {R : CategoryTheory.Sheaf J RingCa
t} →         [inst_1 : ∀ (X : C), CategoryTheory.HasWeakSheafify (J.over X) AddC
ommGrpCat] →           [inst_2 : ∀ (X : C), (J.over X).WEqualsLocallyBijective A
ddCommGrpCat] →             {M : SheafOfModules R} → M.LocalGeneratorsData → M.L
ocalGeneratorsData
参数：X : C；J.over X；X : C；J.over X。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
def LocalGeneratorsData.shrink {M : SheafOfModules.{u} R} (q : M.LocalGeneratorsData) :
    LocalGeneratorsData.{u'} M where
  I := Set.range q.X
  X i := q.X i.2.choose
  coversTop X := by
    refine J.superset_covering (fun Y hY H ↦ ?_) (q.coversTop X)
    obtain ⟨i, ⟨hi⟩⟩ := (Sieve.mem_ofObjects_iff ..).mp H
    exact ⟨⟨_, i, rfl⟩, ⟨hi ≫ eqToHom (by grind)⟩⟩
  generators i := q.generators i.2.choose

variable {M} in
/-- The data of local generators of a sheaf of modules is finite type if
each family of generators is finite type. -/
/-
**SheafOfModules.LocalGeneratorsData.IsFiniteType** 是 Mathlib 中的一个归纳类型，位于命名空间 `S
heafOfModules.LocalGeneratorsData`。
形式化陈述：{C : Type u'} →   [inst : CategoryTheory.Category.{v', u'} C] →     {J : C
ategoryTheory.GrothendieckTopology C} →       {R : CategoryTheory.Sheaf J RingCa
t} →         {M : SheafOfModules R} →           [inst_1 : ∀ (X : C), CategoryThe
ory.HasWeakSheafify (J.over X) AddCommGrpCat] →             [inst_2 : ∀ (X : C),
 (J.over X).WEqualsLocallyBijective AddCommGrpCat] → M.LocalGeneratorsData → Pro
p
参数：X : C；J.over X；X : C；J.over X。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The data of local generators of a sheaf of modules is finite type if
each family of generators is finite type.
-/
class LocalGeneratorsData.IsFiniteType (p : M.LocalGeneratorsData) : Prop where
  isFiniteType (i : p.I) : (p.generators i).IsFiniteType := by infer_instance

/-- A sheaf of modules is of finite type if locally, it is generated by finitely
many sections. -/
/-
**SheafOfModules.IsFiniteType** 是 Mathlib 中的一个归纳类型，位于命名空间 `SheafOfModules`。
形式化陈述：{C : Type u'} →   [inst : CategoryTheory.Category.{v', u'} C] →     {J : C
ategoryTheory.GrothendieckTopology C} →       {R : CategoryTheory.Sheaf J RingCa
t} →         [∀ (X : C), CategoryTheory.HasWeakSheafify (J.over X) AddCommGrpCat
] →           [∀ (X : C), (J.over X).WEqualsLocallyBijective AddCommGrpCat] → Sh
eafOfModules R → Prop
参数：X : C；J.over X；X : C；J.over X。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A sheaf of modules is of finite type if locally, it is generated by finitely
many sections.
-/
class IsFiniteType (M : SheafOfModules.{u} R) : Prop where
  exists_localGeneratorsData (M) :
    ∃ (σ : (LocalGeneratorsData.{u'} M)), σ.IsFiniteType

end

noncomputable section

variable {C' : Type u₁} [Category.{v₁} C'] {J' : GrothendieckTopology C'} {S : Sheaf J' RingCat.{u}}
  [HasSheafify J' AddCommGrpCat] [J'.WEqualsLocallyBijective AddCommGrpCat]

variable {M : SheafOfModules.{u} R} (G : M.GeneratingSections)
  (F : SheafOfModules.{u} R ⥤ SheafOfModules.{u} S) [PreservesColimitsOfSize.{u, u} F]
  (η : unit S ≅ F.obj (unit R))

-- `preservesColimitsOfSize_shrink` is not a global instance because it loops indefinitely.
-- But here it is fine as an instance since the universe `u` is inferrable from the type of `F`.
local instance : PreservesColimitsOfSize.{0, 0} F := preservesColimitsOfSize_shrink _

/-- Let `F` be a functor from sheaf of `R`-module to sheaf of `S`-module, if `F` preserves
colimits and `F.obj (unit R) ≅ unit S`, given generating sections `G : M.GeneratingSections`,
then we obtain a morphism `free G.I ⟶ F.obj M`. -/
/-
**SheafOfModules.GeneratingSections.mapFreeHom** 是 Mathlib 中的一个定义，位于命名空间 `SheafO
fModules.GeneratingSections`。
形式化陈述：{C : Type u'} →   [inst : CategoryTheory.Category.{v', u'} C] →     {J : C
ategoryTheory.GrothendieckTopology C} →       {R : CategoryTheory.Sheaf J RingCa
t} →         [inst_1 : CategoryTheory.HasWeakSheafify J AddCommGrpCat] →        
   [inst_2 : J.WEqualsLocallyBijective AddCommGrpCat] →             {C' : Type u
₁} →               [inst_3 : CategoryTheory.Category.{v₁, u₁} C'] →             
    {J' : CategoryTheory.GrothendieckTopology C'} →                   {S : Categ
oryTheory.Sheaf J' RingCat} →                     [inst_4 : CategoryTheory.HasSh
eafify J' AddCommGrpCat] →                       [inst_5 : J'.WEqualsLocallyBije
ctive AddCommGrpCat] →                         {M : SheafOfModules R} →         
                  (G : M.GeneratingSections) →                             (F : 
CategoryTheory.Functor (SheafOfModules R) (SheafOfModules S)) →                 
              [CategoryTheory.Limits.PreservesColimitsOfSize.{u, u, max u u', ma
x u u₁,                                       max (max (u + 1) u') v', max (max 
(u + 1) u₁) v₁}                                     F] →                        
         (SheafOfModules.unit S ≅ F.obj (SheafOfModules.unit R)) →              
                     (SheafOfModules.free G.I ⟶ F.obj M)
参数：G : M.GeneratingSections；F : CategoryTheory.Functor (SheafOfModules R) (Sheaf
OfModules S)；max (u + 1) u'；max (u + 1) u₁；SheafOfModules.unit S ≅ F.obj (SheafO
fModules.unit R)；SheafOfModules.free G.I ⟶ F.obj M。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.instHasWeakSheafifyOfHasSheafify`：∀ {C : Type u₁} [inst :
 CategoryTheory.Category.{v₁, u₁} C] (J : CategoryTheory.GrothendieckTopology C)
 (A : Type u₂)   [inst_1 : CategoryTh…

--- 原说明 ---
Let `F` be a functor from sheaf of `R`-module to sheaf of `S`-module, if `F` pre
serves
colimits and `F.obj (unit R) ≅ unit S`, given generating sections `G : M.Generat
ingSections`,
then we obtain a morphism `free G.I ⟶ F.obj M`.
-/
def GeneratingSections.mapFreeHom : free G.I ⟶ F.obj M :=
  (mapFreeIso F G.I η).hom ≫ F.map G.π

/-- Let `F` be a functor from sheaf of `R`-module to sheaf of `S`-module, if `F` preserves
colimits and `F.obj (unit R) ≅ unit S`, given generating sections `G : M.GeneratingSections`,
then we obtain generating sections of `F.obj M`. -/
@[simps]
/-
**SheafOfModules.GeneratingSections.map** 是 Mathlib 中的一个定义，位于命名空间 `SheafOfModule
s.GeneratingSections`。
形式化陈述：{C : Type u'} →   [inst : CategoryTheory.Category.{v', u'} C] →     {J : C
ategoryTheory.GrothendieckTopology C} →       {R : CategoryTheory.Sheaf J RingCa
t} →         [inst_1 : CategoryTheory.HasWeakSheafify J AddCommGrpCat] →        
   [inst_2 : J.WEqualsLocallyBijective AddCommGrpCat] →             {C' : Type u
₁} →               [inst_3 : CategoryTheory.Category.{v₁, u₁} C'] →             
    {J' : CategoryTheory.GrothendieckTopology C'} →                   {S : Categ
oryTheory.Sheaf J' RingCat} →                     [inst_4 : CategoryTheory.HasSh
eafify J' AddCommGrpCat] →                       [inst_5 : J'.WEqualsLocallyBije
ctive AddCommGrpCat] →                         {M : SheafOfModules R} →         
                  M.GeneratingSections →                             (F : Catego
ryTheory.Functor (SheafOfModules R) (SheafOfModules S)) →                       
        [CategoryTheory.Limits.PreservesColimitsOfSize.{u, u, max u u', max u u₁
,                                       max (max (u + 1) u') v', max (max (u + 1
) u₁) v₁}                                     F] →                              
   (SheafOfModules.unit S ≅ F.obj (SheafOfModules.unit R)) → (F.obj M).Generatin
gSections
参数：F : CategoryTheory.Functor (SheafOfModules R) (SheafOfModules S)；max (u + 1) 
u'；max (u + 1) u₁；SheafOfModules.unit S ≅ F.obj (SheafOfModules.unit R)；F.obj M。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.instHasWeakSheafifyOfHasSheafify`：∀ {C : Type u₁} [inst :
 CategoryTheory.Category.{v₁, u₁} C] (J : CategoryTheory.GrothendieckTopology C)
 (A : Type u₂)   [inst_1 : CategoryTh…

--- 原说明 ---
Let `F` be a functor from sheaf of `R`-module to sheaf of `S`-module, if `F` pre
serves
colimits and `F.obj (unit R) ≅ unit S`, given generating sections `G : M.Generat
ingSections`,
then we obtain generating sections of `F.obj M`.
-/
def GeneratingSections.map : (F.obj M).GeneratingSections where
  I := G.I
  s := freeHomEquiv (F.obj M) (G.mapFreeHom F η)
  epi := by
    simp only [mapFreeHom, Equiv.symm_apply_apply, epi_comp_iff_of_epi]
    infer_instance
/-
**SheafOfModules.** 是 Mathlib 中的一个实例，位于命名空间 `SheafOfModules`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [G.IsFiniteType] : (G.map F η).IsFiniteType where
  finite := inferInstanceAs (Finite G.I)
/-
**SheafOfModules.GeneratingSections.map_** 是 Mathlib 中的一个引理，位于命名空间 `SheafOfModul
es`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma GeneratingSections.map_π_eq : (G.map F η).π = (mapFreeIso F G.I η).hom ≫ F.map G.π :=
  (F.obj M).freeHomEquiv.symm_apply_eq.mpr rfl

set_option backward.isDefEq.respectTransparency false in
/-
**SheafOfModules.** 是 Mathlib 中的一个实例，位于命名空间 `SheafOfModules`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [IsIso G.π] : IsIso (G.map F η).π := by
  rw [GeneratingSections.map_π_eq]
  infer_instance

variable [∀ X, HasSheafify (J.over X) AddCommGrpCat.{u}] [HasBinaryProducts C]
  [∀ X, (J.over X).WEqualsLocallyBijective AddCommGrpCat.{u}]

/-- Given `G : M.GeneratingSections`, we naturally obtain `M.LocalGeneratorsData` using the
trivial cover of `C`. -/
@[simps]
/-
**SheafOfModules.GeneratingSections.localGeneratorsData** 是 Mathlib 中的一个定义，位于命名空
间 `SheafOfModules.GeneratingSections`。
形式化陈述：{C : Type u'} →   [inst : CategoryTheory.Category.{v', u'} C] →     {J : C
ategoryTheory.GrothendieckTopology C} →       {R : CategoryTheory.Sheaf J RingCa
t} →         [inst_1 : CategoryTheory.HasWeakSheafify J AddCommGrpCat] →        
   [inst_2 : J.WEqualsLocallyBijective AddCommGrpCat] →             [inst_3 : ∀ 
(X : C), CategoryTheory.HasSheafify (J.over X) AddCommGrpCat] →               [C
ategoryTheory.Limits.HasBinaryProducts C] →                 [inst_5 : ∀ (X : C),
 (J.over X).WEqualsLocallyBijective AddCommGrpCat] →                   {M : Shea
fOfModules R} → M.GeneratingSections → M.LocalGeneratorsData
参数：X : C；J.over X；X : C；J.over X。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.GrothendieckTopology.instIsContinuousOverForgetOver`：∀ {C
 : Type u} [inst : CategoryTheory.Category.{v, u} C] (J : CategoryTheory.Grothen
dieckTopology C) (X : C),   (CategoryTheory.Over.forget …

--- 原说明 ---
Given `G : M.GeneratingSections`, we naturally obtain `M.LocalGeneratorsData` us
ing the
trivial cover of `C`.
-/
def GeneratingSections.localGeneratorsData {M : SheafOfModules.{u} R} (G : M.GeneratingSections) :
    M.LocalGeneratorsData where
  I := C
  X := id
  coversTop _ := GrothendieckTopology.covering_of_eq_top J <| by simp
  generators x := G.map (pushforward (𝟙 (R.over x))) (Iso.refl _)

end

end SheafOfModules

