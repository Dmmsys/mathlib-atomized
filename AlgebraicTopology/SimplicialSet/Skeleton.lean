/-
Copyright (c) 2025 Joël Riou. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Joël Riou
-/
module

public import Mathlib.AlgebraicTopology.RelativeCellComplex.Basic
public import Mathlib.AlgebraicTopology.SimplicialSet.Boundary
public import Mathlib.AlgebraicTopology.SimplicialSet.SubcomplexEvaluation
public import Mathlib.CategoryTheory.MorphismProperty.FunctorCategory
public import Mathlib.CategoryTheory.Types.Monomorphisms

/-!
# The skeleton of a simplicial set

In this file, we define the skeleton `X.skeleton : ℕ →o X.Subcomplex`
of a simplicial set `X`. For any `n : ℕ`, `X.skeleton n` is the
subcomplex of `X` that is generated by (non-degenerate) simplices
of dimension `< n`.

If `i : X ⟶ Y` is a monomorphism, we define
`skeletonOfMono i : ℕ →o Y.Subcomplex` so that
`skeletonOfMono i n = Subcomplex.range i ⊔ Y.skeleton n`.
We show that this filtration is part of a relative cell complex structure for `i`,
with basic cells given by boundary inclusions `∂Δ[d] ⟶ Δ[d]` for all
nondegenerate `d`-simplices of `Y` which do not belong to the range of `i`.

## TODO
* show that `(SSet.sk n).obj X` is isomorphic to `X.skeleton (n + 1)`

-/

@[expose] public section

universe u

open CategoryTheory Simplicial Limits Opposite HomotopicalAlgebra

namespace SSet

variable (X : SSet.{u})

/-- `X.skeleton n` is the subcomplex of `X` generated by (non-degenerate)
simplices of dimension `< n`. -/
/-
**SSet.skeleton** 是 Mathlib 中的一个定义，位于命名空间 `SSet`。
形式化陈述：skeleton : Nat ->o X.Subcomplex where toFun n
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`X.skeleton n` is the subcomplex of `X` generated by (non-degenerate)
simplices of dimension `< n`.
-/
def skeleton : ℕ →o X.Subcomplex where
  toFun n := ⨆ (i : Fin n) (x : X.nonDegenerate i), Subcomplex.ofSimplex x.1
  monotone' i j h := by
    simp only [iSup_le_iff]
    intro k x
    exact le_trans (by exact le_trans (by rfl) (le_iSup _ x))
      (le_iSup _ ⟨k, by lia⟩)
/-
**SSet.mem_skeleton** 是 Mathlib 中的一个引理，位于命名空间 `SSet`。
形式化陈述：mem_skeleton {i : Nat} (x : X _⦋i⦌) {n : Nat} (hi : i < n
参数：x : X _⦋i⦌。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `SSet.exists_nonDegenerate`：exists_nonDegenerate (x : X _⦋n⦌) : exists (m
 : Nat) (f : ⦋n⦌ ⟶ ⦋m⦌) (_ : Epi f) (y : X.nonDegenerate m), x = X.map f.op y
· 使用引理 `le_trans`：le_trans : a <= b -> b <= c -> a <= c
· 使用引理 `lt_of_le_of_lt`：lt_of_le_of_lt (hab : a <= b) (hbc : b < c) : a < c
· 使用定理 `SimplexCategory.len_le_of_epi`：len_le_of_epi {x y : SimplexCategory} (f 
: x ⟶ y) [Epi f] : y.len <= x.len
· 使用定理 `le_refl`：∀ {α : Type u_1} [inst : Preorder α] (a : α), a ≤ a
· 使用定理 `le_iSup`：le_iSup (f : ι -> α) (i : ι) : f i <= iSup f
· 使用定理 `CategoryTheory.Subfunctor.map`：∀ {C : Type u} [inst : CategoryTheory.Cat
egory.{v, u} C] {F : CategoryTheory.Functor C (Type w)}   (self : CategoryTheory
.Subfunctor F) {U V…
· 使用引理 `SSet.Subcomplex.mem_ofSimplex_obj`：mem_ofSimplex_obj {n : Nat} (x : X _⦋
n⦌) : x in (ofSimplex x).obj _
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
-/
lemma mem_skeleton {i : ℕ} (x : X _⦋i⦌) {n : ℕ} (hi : i < n := by lia) :
    x ∈ (X.skeleton n).obj _ := by
  obtain ⟨j, f, _, y, rfl⟩ := X.exists_nonDegenerate x
  suffices Subcomplex.ofSimplex y.1 ≤ X.skeleton n from
    (X.skeleton n).map _ (this _ (Subcomplex.mem_ofSimplex_obj _))
  exact le_trans (by exact le_trans (by rfl) (le_iSup _ y))
    (le_iSup _ ⟨j, lt_of_le_of_lt
      (SimplexCategory.len_le_of_epi f) hi⟩)
/-
**SSet.skeleton_obj_eq_top** 是 Mathlib 中的一个引理，位于命名空间 `SSet`。
形式化陈述：skeleton_obj_eq_top {d n : Nat} (h : d < n) : (X.skeleton n).obj (op ⦋d⦌) 
= ⊤
参数：h : d < n。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `top_le_iff`：top_le_iff : ⊤ <= a ↔ a = ⊤
· 使用引理 `SSet.mem_skeleton`：mem_skeleton {i : Nat} (x : X _⦋i⦌) {n : Nat} (hi : i
 < n
-/
lemma skeleton_obj_eq_top {d n : ℕ} (h : d < n) :
    (X.skeleton n).obj (op ⦋d⦌) = ⊤ := by
  rw [← top_le_iff]
  intro x _
  exact mem_skeleton _ _ h
/-
**SSet.ofSimplex_le_skeleton** 是 Mathlib 中的一个引理，位于命名空间 `SSet`。
形式化陈述：ofSimplex_le_skeleton {i : Nat} (x : X _⦋i⦌) {n : Nat} (hi : i < n) : Subc
omplex.ofSimplex x <= X.skeleton n
参数：x : X _⦋i⦌；hi : i < n。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `SSet.mem_skeleton`：mem_skeleton {i : Nat} (x : X _⦋i⦌) {n : Nat} (hi : i
 < n
-/
lemma ofSimplex_le_skeleton {i : ℕ} (x : X _⦋i⦌) {n : ℕ} (hi : i < n) :
    Subcomplex.ofSimplex x ≤ X.skeleton n := by
  simpa using X.mem_skeleton x hi

set_option backward.isDefEq.respectTransparency.types false in
/-
**SSet.mem_skeleton_obj_iff_of_nonDegenerate** 是 Mathlib 中的一个引理，位于命名空间 `SSet`。
形式化陈述：mem_skeleton_obj_iff_of_nonDegenerate {d : Nat} (x : X.nonDegenerate d) (n
 : Nat) : x.1 in (X.skeleton n).obj _ ↔ d < n
参数：x : X.nonDegenerate d；n : Nat。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `CategoryTheory.Subfunctor.iSup_obj`：iSup_obj {ι : Sort*} (S : ι -> Subfu
nctor F) (U : C) : (⨆ i, S i).obj U = ⋃ i, (S i).obj U
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Set.iUnion_coe_set`：iUnion_coe_set {α β : Type*} (s : Set α) (f : s -> S
et β) : ⋃ i, f i = ⋃ i in s, f ⟨i, ‹i in s›⟩
· 使用定理 `Set.iUnion_congr_Prop`：iUnion_congr_Prop {p q : Prop} {f₁ : p -> Set α} 
{f₂ : q -> Set α} (pq : p ↔ q) (f : forall x, f₁ (pq.mpr x) = f₂ x) : iUnion f₁ 
= iUnion f₂
· 使用定理 `Iff.of_eq`：∀ {a b : Prop}, a = b → (a ↔ b)
· 使用引理 `SSet.mono_of_nonDegenerate`：mono_of_nonDegenerate (x : X.nonDegenerate n
) {m : SimplexCategory} (f : ⦋n⦌ ⟶ m) (y : X.obj (op m)) (hy : X.map f.op y = x)
 : Mono f
· 使用定理 `SimplexCategory.len_le_of_mono`：len_le_of_mono {x y : SimplexCategory} (
f : x ⟶ y) [Mono f] : x.len <= y.len
· 使用引理 `SSet.mem_skeleton`：mem_skeleton {i : Nat} (x : X _⦋i⦌) {n : Nat} (hi : i
 < n
-/
lemma mem_skeleton_obj_iff_of_nonDegenerate
    {d : ℕ} (x : X.nonDegenerate d) (n : ℕ) :
    x.1 ∈ (X.skeleton n).obj _ ↔ d < n := by
  refine ⟨fun h ↦ ?_, fun _ ↦ X.mem_skeleton _⟩
  obtain ⟨x, hx⟩ := x
  simp only [skeleton, OrderHom.coe_mk, Subfunctor.iSup_obj,
    Set.iUnion_coe_set, Set.mem_iUnion, exists_prop] at h
  obtain ⟨⟨i, hi⟩, y, hy, ⟨f⟩, rfl⟩ := h
  have := X.mono_of_nonDegenerate ⟨_, hx⟩ f y rfl
  have : d ≤ i := SimplexCategory.len_le_of_mono f
  lia

set_option backward.isDefEq.respectTransparency.types false in
@[simp]
/-
**SSet.skeleton_zero** 是 Mathlib 中的一个引理，位于命名空间 `SSet`。
形式化陈述：skeleton_zero : X.skeleton 0 = ⊥
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
-/
lemma skeleton_zero : X.skeleton 0 = ⊥ := by
  simp [skeleton]
/-
**SSet.iSup_skeleton** 是 Mathlib 中的一个引理，位于命名空间 `SSet`。
形式化陈述：iSup_skeleton : ⨆ (n : Nat), X.skeleton n = ⊤
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `SSet.Subcomplex.le_iff_contains_nonDegenerate`：le_iff_contains_nonDegene
rate (B : X.Subcomplex) : A <= B ↔ forall (n : Nat) (x : X.nonDegenerate n), x.v
al in A.obj _ -> x.val in B.obj _
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用引理 `CategoryTheory.Subfunctor.iSup_obj`：iSup_obj {ι : Sort*} (S : ι -> Subfu
nctor F) (U : C) : (⨆ i, S i).obj U = ⋃ i, (S i).obj U
· 使用引理 `SSet.mem_skeleton`：mem_skeleton {i : Nat} (x : X _⦋i⦌) {n : Nat} (hi : i
 < n
-/
lemma iSup_skeleton :
    ⨆ (n : ℕ), X.skeleton n = ⊤ :=
  le_antisymm (by simp) (by
    rw [Subcomplex.le_iff_contains_nonDegenerate]
    intro n x _
    simp only [Subfunctor.iSup_obj, Set.mem_iUnion]
    exact ⟨n + 1, mem_skeleton _ _ (by lia)⟩)

set_option backward.isDefEq.respectTransparency.types false in
/-
**SSet.skeleton_succ** 是 Mathlib 中的一个引理，位于命名空间 `SSet`。
形式化陈述：skeleton_succ (n : Nat) : X.skeleton (n + 1) = X.skeleton n ⊔ ⨆ (x : X.non
Degenerate n), Subcomplex.ofSimplex x.1
参数：n : Nat。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `LE.le.lt_or_eq`：∀ {α : Type u_1} [inst : PartialOrder α] {a b : α}, a ≤ 
b → a < b ∨ a = b
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Nat.lt_succ_iff`：∀ {m n : ℕ}, m < n.succ ↔ m ≤ n
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用引理 `SSet.ofSimplex_le_skeleton`：ofSimplex_le_skeleton {i : Nat} (x : X _⦋i⦌)
 {n : Nat} (hi : i < n) : Subcomplex.ofSimplex x <= X.skeleton n
· 使用定理 `le_sup_left`：le_sup_left : a <= a ⊔ b
· 使用引理 `le_trans`：le_trans : a <= b -> b <= c -> a <= c
· 使用定理 `le_refl`：∀ {α : Type u_1} [inst : Preorder α] (a : α), a ≤ a
· 使用定理 `le_iSup`：le_iSup (f : ι -> α) (i : ι) : f i <= iSup f
· 使用定理 `le_sup_right`：le_sup_right : b <= a ⊔ b
· 使用定理 `OrderHom.monotone`：∀ {α : Type u_2} {β : Type u_3} [inst : Preorder α] [
inst_1 : Preorder β] (f : α →o β), Monotone ⇑f
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `IsOrderedAddMonoid.toAddLeftMono`：∀ {α : Type u_1} [inst : AddCommMonoid
 α] [inst_1 : Preorder α] [IsOrderedAddMonoid α], AddLeftMono α
· 使用定理 `IsLeftCancelAdd.addLeftReflectLE_of_addLeftReflectLT`：∀ (N : Type u_2) [
inst : Add N] [IsLeftCancelAdd N] [inst_2 : PartialOrder N] [AddLeftReflectLT N]
, AddLeftReflectLE N
· 使用定理 `instIsLeftCancelAddOfAddLeftReflectLE`：∀ {α : Type u_1} [inst : Add α] [
inst_1 : PartialOrder α] [AddLeftReflectLE α], IsLeftCancelAdd α
· 使用定理 `IsOrderedCancelAddMonoid.toAddLeftReflectLE`：∀ {α : Type u_2} [inst : Ad
dCommMonoid α] [inst_1 : Preorder α] [IsOrderedCancelAddMonoid α], AddLeftReflec
tLE α
· 使用定理 `LinearOrderedCommMonoidWithZero.toIsBotZeroClass`：∀ {α : Type u_3} [self
 : LinearOrderedCommMonoidWithZero α], IsBotZeroClass α
· 使用定理 `IsLeftCancelAdd.addLeftStrictMono_of_addLeftMono`：∀ (N : Type u_2) [inst
 : Add N] [IsLeftCancelAdd N] [inst_2 : PartialOrder N] [AddLeftMono N], AddLeft
StrictMono N
· 使用定理 `Nat.instNeZeroSucc`：∀ {n : ℕ}, NeZero (n + 1)
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma skeleton_succ (n : ℕ) :
    X.skeleton (n + 1) =
      X.skeleton n ⊔ ⨆ (x : X.nonDegenerate n), Subcomplex.ofSimplex x.1 := by
  apply le_antisymm
  · conv_lhs => dsimp [skeleton]
    simp only [iSup_le_iff]
    rintro ⟨d, hd⟩ x
    rw [Nat.lt_succ_iff] at hd
    obtain hd | rfl := hd.lt_or_eq
    · exact (X.ofSimplex_le_skeleton _ hd).trans le_sup_left
    · exact le_trans (le_trans (by rfl) (le_iSup _ x)) le_sup_right
  · simp only [sup_le_iff, iSup_le_iff]
    constructor
    · exact X.skeleton.monotone (by simp)
    · intro x
      apply X.ofSimplex_le_skeleton _ (by simp)

variable {X} {Y : SSet.{u}} (i : X ⟶ Y)

/-- The skeleton of a monomorphism `i : X ⟶ Y` of simplicial sets.
It sends `n : ℕ` to `Subcomplex.range i ⊔ Y.skeleton n`.
See `SSet.relativeCellComplexOfMono` for the main result regarding
this definition.
(Even though `[Mono i]` is technically not required in this definition,
the intended use of this definition and the corresponding API is
when `[Mono i]` holds.) -/
/-
**SSet.skeletonOfMono** 是 Mathlib 中的一个定义，位于命名空间 `SSet`。
形式化陈述：skeletonOfMono : Nat ->o Y.Subcomplex where toFun n
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The skeleton of a monomorphism `i : X ⟶ Y` of simplicial sets.
It sends `n : ℕ` to `Subcomplex.range i ⊔ Y.skeleton n`.
See `SSet.relativeCellComplexOfMono` for the main result regarding
this definition.
(Even though `[Mono i]` is technically not required in this definition,
the intended use of this definition and the corresponding API is
when `[Mono i]` holds.)
-/
def skeletonOfMono : ℕ →o Y.Subcomplex where
  toFun n := Subcomplex.range i ⊔ Y.skeleton n
  monotone' n m h := by
    dsimp
    simp only [sup_le_iff, le_sup_left, true_and]
    exact le_trans (Y.skeleton.monotone h) le_sup_right

section

/-
**SSet.skeleton_le_skeletonOfMono** 是 Mathlib 中的一个引理，位于命名空间 `SSet`。
形式化陈述：skeleton_le_skeletonOfMono (n : Nat) : Y.skeleton n <= skeletonOfMono i n
参数：n : Nat。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `le_sup_right`：le_sup_right : b <= a ⊔ b
-/
lemma skeleton_le_skeletonOfMono (n : ℕ) :
    Y.skeleton n ≤ skeletonOfMono i n := le_sup_right

set_option backward.isDefEq.respectTransparency.types false in
@[simp]
/-
**SSet.skeletonOfMono_zero** 是 Mathlib 中的一个引理，位于命名空间 `SSet`。
形式化陈述：skeletonOfMono_zero : skeletonOfMono i 0 = Subcomplex.range i
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `SSet.skeleton_zero`：skeleton_zero : X.skeleton 0 = ⊥
· 使用定理 `sup_of_le_left`：∀ {α : Type u} [inst : SemilatticeSup α] {a b : α}, b ≤ 
a → a ⊔ b = a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma skeletonOfMono_zero :
    skeletonOfMono i 0 = Subcomplex.range i := by
  simp [skeletonOfMono]
/-
**SSet.iSup_skeletonOfMono** 是 Mathlib 中的一个引理，位于命名空间 `SSet`。
形式化陈述：iSup_skeletonOfMono : ⨆ (n : Nat), skeletonOfMono i n = ⊤
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `SSet.iSup_skeleton`：iSup_skeleton : ⨆ (n : Nat), X.skeleton n = ⊤
· 使用定理 `iSup_le_iff`：iSup_le_iff : iSup f <= a ↔ forall i, f i <= a
· 使用引理 `le_trans`：le_trans : a <= b -> b <= c -> a <= c
· 使用引理 `SSet.skeleton_le_skeletonOfMono`：skeleton_le_skeletonOfMono (n : Nat) : 
Y.skeleton n <= skeletonOfMono i n
· 使用定理 `le_iSup`：le_iSup (f : ι -> α) (i : ι) : f i <= iSup f
-/
lemma iSup_skeletonOfMono :
    ⨆ (n : ℕ), skeletonOfMono i n = ⊤ := by
  apply le_antisymm (by simp)
  rw [← Y.iSup_skeleton, iSup_le_iff]
  intro n
  exact le_trans (skeleton_le_skeletonOfMono i n) (le_iSup _ n)

set_option backward.isDefEq.respectTransparency.types false in
/-
**SSet.mem_skeletonOfMono_obj_iff_of_nonDegenerate** 是 Mathlib 中的一个引理，位于命名空间 `SS
et`。
形式化陈述：mem_skeletonOfMono_obj_iff_of_nonDegenerate {d : Nat} (x : Y.nonDegenerate
 d) (n : Nat) : x.1 in (skeletonOfMono i n).obj _ ↔ x.1 in Set.range (i.app _) ∨
 d < n
参数：x : Y.nonDegenerate d；n : Nat。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `CategoryTheory.Subfunctor.range_obj`：∀ {C : Type u} [inst : CategoryTheo
ry.Category.{v, u} C] {F F' : CategoryTheory.Functor C (Type w)} (p : F' ⟶ F)   
(U : C), (CategoryTheory.…
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
lemma mem_skeletonOfMono_obj_iff_of_nonDegenerate
    {d : ℕ} (x : Y.nonDegenerate d) (n : ℕ) :
    x.1 ∈ (skeletonOfMono i n).obj _ ↔
      x.1 ∈ Set.range (i.app _) ∨ d < n := by
  simp [skeletonOfMono, mem_skeleton_obj_iff_of_nonDegenerate]
/-
**SSet.skeletonOfMono_obj_eq_top** 是 Mathlib 中的一个引理，位于命名空间 `SSet`。
形式化陈述：skeletonOfMono_obj_eq_top {d n : Nat} (h : d < n) : (skeletonOfMono i n).o
bj (op ⦋d⦌) = ⊤
参数：h : d < n。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `top_le_iff`：top_le_iff : ⊤ <= a ↔ a = ⊤
· 使用引理 `SSet.skeleton_obj_eq_top`：skeleton_obj_eq_top {d n : Nat} (h : d < n) : 
(X.skeleton n).obj (op ⦋d⦌) = ⊤
· 使用定理 `le_sup_right`：le_sup_right : b <= a ⊔ b
-/
lemma skeletonOfMono_obj_eq_top {d n : ℕ} (h : d < n) :
    (skeletonOfMono i n).obj (op ⦋d⦌) = ⊤ := by
  rw [← top_le_iff, ← Y.skeleton_obj_eq_top h]
  exact le_sup_right

set_option backward.isDefEq.respectTransparency.types false in
/-
**SSet.skeletonOfMono_succ** 是 Mathlib 中的一个引理，位于命名空间 `SSet`。
形式化陈述：skeletonOfMono_succ (n : Nat) : skeletonOfMono i (n + 1) = skeletonOfMono 
i n ⊔ ⨆ (x : Y.nonDegenerate n) (_ : x.1 ∉ (Subcomplex.range i).obj _), Subcompl
ex.ofSimplex x.1
参数：n : Nat。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `SSet.skeleton_succ`：skeleton_succ (n : Nat) : X.skeleton (n + 1) = X.ske
leton n ⊔ ⨆ (x : X.nonDegenerate n), Subcomplex.ofSimplex x.1
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用定理 `le_sup_left`：le_sup_left : a <= a ⊔ b
· 使用定理 `le_sup_right`：le_sup_right : b <= a ⊔ b
· 使用引理 `le_trans`：le_trans : a <= b -> b <= c -> a <= c
· 使用定理 `CategoryTheory.Subfunctor.range_obj`：∀ {C : Type u} [inst : CategoryTheo
ry.Category.{v, u} C] {F F' : CategoryTheory.Functor C (Type w)} (p : F' ⟶ F)   
(U : C), (CategoryTheory.…
· 使用定理 `le_refl`：∀ {α : Type u_1} [inst : Preorder α] (a : α), a ≤ a
· 使用定理 `le_iSup`：le_iSup (f : ι -> α) (i : ι) : f i <= iSup f
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `OrderHom.monotone`：∀ {α : Type u_2} {β : Type u_3} [inst : Preorder α] [
inst_1 : Preorder β] (f : α →o β), Monotone ⇑f
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `IsOrderedAddMonoid.toAddLeftMono`：∀ {α : Type u_1} [inst : AddCommMonoid
 α] [inst_1 : Preorder α] [IsOrderedAddMonoid α], AddLeftMono α
· 使用定理 `IsLeftCancelAdd.addLeftReflectLE_of_addLeftReflectLT`：∀ (N : Type u_2) [
inst : Add N] [IsLeftCancelAdd N] [inst_2 : PartialOrder N] [AddLeftReflectLT N]
, AddLeftReflectLE N
· 使用定理 `instIsLeftCancelAddOfAddLeftReflectLE`：∀ {α : Type u_1} [inst : Add α] [
inst_1 : PartialOrder α] [AddLeftReflectLE α], IsLeftCancelAdd α
· 使用定理 `IsOrderedCancelAddMonoid.toAddLeftReflectLE`：∀ {α : Type u_2} [inst : Ad
dCommMonoid α] [inst_1 : Preorder α] [IsOrderedCancelAddMonoid α], AddLeftReflec
tLE α
· 使用定理 `LinearOrderedCommMonoidWithZero.toIsBotZeroClass`：∀ {α : Type u_3} [self
 : LinearOrderedCommMonoidWithZero α], IsBotZeroClass α
· 使用引理 `SSet.ofSimplex_le_skeleton`：ofSimplex_le_skeleton {i : Nat} (x : X _⦋i⦌)
 {n : Nat} (hi : i < n) : Subcomplex.ofSimplex x <= X.skeleton n
· 使用定理 `IsLeftCancelAdd.addLeftStrictMono_of_addLeftMono`：∀ (N : Type u_2) [inst
 : Add N] [IsLeftCancelAdd N] [inst_2 : PartialOrder N] [AddLeftMono N], AddLeft
StrictMono N
· 使用定理 `Nat.instNeZeroSucc`：∀ {n : ℕ}, NeZero (n + 1)
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma skeletonOfMono_succ (n : ℕ) :
    skeletonOfMono i (n + 1) =
      skeletonOfMono i n ⊔ ⨆ (x : Y.nonDegenerate n)
        (_ : x.1 ∉ (Subcomplex.range i).obj _), Subcomplex.ofSimplex x.1 := by
  dsimp only [skeletonOfMono, OrderHom.coe_mk]
  apply le_antisymm
  · simp only [sup_le_iff, iSup_le_iff, skeleton_succ]
    refine ⟨le_sup_left.trans le_sup_left,
      le_sup_right.trans le_sup_left, fun x ↦ ?_⟩
    by_cases hx : x.1 ∈ (Subcomplex.range i).obj _
    · exact le_trans (le_trans (by simpa) le_sup_left) le_sup_left
    · exact (le_trans (by exact le_trans (by rfl) (le_iSup _ hx))
        (le_iSup _ x)).trans le_sup_right
  · simp only [sup_le_iff, iSup_le_iff]
    exact ⟨⟨le_sup_left, (Y.skeleton.monotone (by simp)).trans le_sup_right⟩,
      fun _ _ ↦ (Y.ofSimplex_le_skeleton _ (by simp)).trans le_sup_right⟩

end

namespace relativeCellComplexOfMono

/-!
The main next technical result is that if `i : X ⟶ Y` is a monomorphism
of simplicial sets and `d : ℕ`, there is a pushout square:
```
                                t i d
∐ fun (c : Cell i d) ↦ ∂Δ[d] ----------> skeletonOfMono i d
               |                                   |
         l i d |                                   | r i d
               v                b i d              v
∐ fun (c : Cell i d) ↦ Δ[d]  ----------> skeletonOfMono i (d + 1)
```

The index type `Cell i d` identifies to the type of nondegenerate `d`-simplices
of `Y` not in the range of `i`.

-/

/-- If `i : X ⟶ Y` is a monomorphism of simplicial sets and `d : ℕ`, this is
the type of nondegenerate `d`-simplices of `Y` which do not belong to
the range of `i`. -/
@[ext]
/-
**SSet.relativeCellComplexOfMono.Cell** 是 Mathlib 中的一个归纳类型，位于命名空间 `SSet.relative
CellComplexOfMono`。
形式化陈述：{X Y : _root_.SSet} → (X ⟶ Y) → ℕ → Type u
参数：X ⟶ Y。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If `i : X ⟶ Y` is a monomorphism of simplicial sets and `d : ℕ`, this is
the type of nondegenerate `d`-simplices of `Y` which do not belong to
the range of `i`.
-/
structure Cell (d : ℕ) where
  /-- a `d`-simplex in the target of the monomorphism -/
  simplex : Y _⦋d⦌
  nonDegenerate : simplex ∈ Y.nonDegenerate d
  notMem : simplex ∉ Set.range (i.app _)

variable (d : ℕ)

/-- If `i : X ⟶ Y` is a monomorphism of simplicial sets and `d : ℕ`, this is
a coproduct of copies of `Δ[d]` indexed by the nondegenerate `d`-simplices of `Y`
not in the range of `i`. -/
/-
**SSet.relativeCellComplexOfMono.sigmaStdSimplex** 是 Mathlib 中的一个缩写定义，位于命名空间 `SS
et.relativeCellComplexOfMono`。
形式化陈述：sigmaStdSimplex : SSet.{u}
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If `i : X ⟶ Y` is a monomorphism of simplicial sets and `d : ℕ`, this is
a coproduct of copies of `Δ[d]` indexed by the nondegenerate `d`-simplices of `Y
`
not in the range of `i`.
-/
noncomputable abbrev sigmaStdSimplex : SSet.{u} :=
  ∐ fun (_ : Cell i d) ↦ Δ[d]

/-- If `i : X ⟶ Y` is a monomorphism of simplicial sets and `d : ℕ`, this is
a coproduct of copies of `∂Δ[d]` indexed by the nondegenerate `d`-simplices of `Y`
not in the range of `i`. -/
/-
**SSet.relativeCellComplexOfMono.sigmaBoundary** 是 Mathlib 中的一个缩写定义，位于命名空间 `SSet
.relativeCellComplexOfMono`。
形式化陈述：sigmaBoundary : SSet.{u}
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If `i : X ⟶ Y` is a monomorphism of simplicial sets and `d : ℕ`, this is
a coproduct of copies of `∂Δ[d]` indexed by the nondegenerate `d`-simplices of `
Y`
not in the range of `i`.
-/
noncomputable abbrev sigmaBoundary : SSet.{u} :=
  ∐ fun (_ : Cell i d) ↦ ∂Δ[d]

namespace Cell

variable {i d} (c : Cell i d)

/-- Given a monomorphism `i : X ⟶ Y` of simplicial sets and a nondegenerate `d`-simplex
of `Y` not in the range of `i`, this is the inclusion of the corresponding summand
of the coproduct `sigmaStdSimplex i d`. -/
/-
**SSet.relativeCellComplexOfMono.Cell.** 是 Mathlib 中的一个缩写定义，位于命名空间 `SSet.relativ
eCellComplexOfMono.Cell`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Given a monomorphism `i : X ⟶ Y` of simplicial sets and a nondegenerate `d`-simp
lex
of `Y` not in the range of `i`, this is the inclusion of the corresponding summa
nd
of the coproduct `sigmaStdSimplex i d`.
-/
noncomputable abbrev ιSigmaStdSimplex : Δ[d] ⟶ sigmaStdSimplex i d :=
  Sigma.ι (fun (_ : Cell i d) ↦ Δ[d]) c

/-- Given a monomorphism `i : X ⟶ Y` of simplicial sets and a nondegenerate `d`-simplex
of `Y` not in the range of `i`, this is the inclusion of the corresponding summand
of the coproduct `sigmaBoundary i d`. -/
/-
**SSet.relativeCellComplexOfMono.Cell.** 是 Mathlib 中的一个缩写定义，位于命名空间 `SSet.relativ
eCellComplexOfMono.Cell`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Given a monomorphism `i : X ⟶ Y` of simplicial sets and a nondegenerate `d`-simp
lex
of `Y` not in the range of `i`, this is the inclusion of the corresponding summa
nd
of the coproduct `sigmaBoundary i d`.
-/
noncomputable abbrev ιSigmaBoundary : (∂Δ[d] : SSet) ⟶ sigmaBoundary i d :=
  Sigma.ι (fun (_ : Cell i d) ↦ (∂Δ[d] : SSet)) c

/-- Given a monomorphism `i : X ⟶ Y` of simplicial sets and a nondegenerate `d`-simplex
of `Y` not in the range of `i`, this is the corresponding morphism `Δ[d] ⟶ Y`. -/
/-
**SSet.relativeCellComplexOfMono.Cell.map** 是 Mathlib 中的一个缩写定义，位于命名空间 `SSet.rela
tiveCellComplexOfMono.Cell`。
形式化陈述：map : Δ[d] ⟶ Y
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s

--- 原说明 ---
Given a monomorphism `i : X ⟶ Y` of simplicial sets and a nondegenerate `d`-simp
lex
of `Y` not in the range of `i`, this is the corresponding morphism `Δ[d] ⟶ Y`.
-/
abbrev map : Δ[d] ⟶ Y := yonedaEquiv.symm c.simplex

set_option backward.isDefEq.respectTransparency.types false in
/-
**SSet.relativeCellComplexOfMono.Cell.mem_skeletonOfMono_obj_iff** 是 Mathlib 中的一
个引理，位于命名空间 `SSet.relativeCellComplexOfMono.Cell`。
形式化陈述：mem_skeletonOfMono_obj_iff {d' : Nat} : c.simplex in (skeletonOfMono i d')
.obj _ ↔ c.simplex in Set.range (i.app _) ∨ d < d'
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `CategoryTheory.Subfunctor.range_obj`：∀ {C : Type u} [inst : CategoryTheo
ry.Category.{v, u} C] {F F' : CategoryTheory.Functor C (Type w)} (p : F' ⟶ F)   
(U : C), (CategoryTheory.…
· 使用定理 `SSet.relativeCellComplexOfMono.Cell.nonDegenerate`：∀ {X Y : _root_.SSet}
 {i : X ⟶ Y} {d : ℕ} (self : SSet.relativeCellComplexOfMono.Cell i d),   self.si
mplex ∈ Y.nonDegenerate d
· 使用引理 `SSet.mem_skeleton_obj_iff_of_nonDegenerate`：mem_skeleton_obj_iff_of_nonD
egenerate {d : Nat} (x : X.nonDegenerate d) (n : Nat) : x.1 in (X.skeleton n).ob
j _ ↔ d < n
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
lemma mem_skeletonOfMono_obj_iff {d' : ℕ} :
    c.simplex ∈ (skeletonOfMono i d').obj _ ↔
    c.simplex ∈ Set.range (i.app _) ∨ d < d' := by
  simp [skeletonOfMono, Y.mem_skeleton_obj_iff_of_nonDegenerate ⟨_, c.nonDegenerate⟩ d']
/-
**SSet.relativeCellComplexOfMono.Cell.range_map_le** 是 Mathlib 中的一个引理，位于命名空间 `SS
et.relativeCellComplexOfMono.Cell`。
形式化陈述：range_map_le : Subcomplex.range c.map <= skeletonOfMono i (d + 1)
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用引理 `SSet.Subcomplex.range_eq_ofSimplex`：range_eq_ofSimplex {n : Nat} (f : Δ[
n] ⟶ X) : range f = ofSimplex (yonedaEquiv f)
· 使用定理 `Equiv.apply_symm_apply`：∀ {α : Sort u} {β : Sort v} (e : α ≃ β) (x : β),
 e (e.symm x) = x
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `IsLeftCancelAdd.addLeftStrictMono_of_addLeftMono`：∀ (N : Type u_2) [inst
 : Add N] [IsLeftCancelAdd N] [inst_2 : PartialOrder N] [AddLeftMono N], AddLeft
StrictMono N
· 使用定理 `instIsLeftCancelAddOfAddLeftReflectLE`：∀ {α : Type u_1} [inst : Add α] [
inst_1 : PartialOrder α] [AddLeftReflectLE α], IsLeftCancelAdd α
· 使用定理 `IsOrderedCancelAddMonoid.toAddLeftReflectLE`：∀ {α : Type u_2} [inst : Ad
dCommMonoid α] [inst_1 : Preorder α] [IsOrderedCancelAddMonoid α], AddLeftReflec
tLE α
· 使用定理 `IsOrderedAddMonoid.toAddLeftMono`：∀ {α : Type u_1} [inst : AddCommMonoid
 α] [inst_1 : Preorder α] [IsOrderedAddMonoid α], AddLeftMono α
· 使用定理 `LinearOrderedCommMonoidWithZero.toIsBotZeroClass`：∀ {α : Type u_3} [self
 : LinearOrderedCommMonoidWithZero α], IsBotZeroClass α
· 使用定理 `Nat.instNeZeroSucc`：∀ {n : ℕ}, NeZero (n + 1)
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `or_true`：∀ (p : Prop), (p ∨ True) = True
-/
lemma range_map_le : Subcomplex.range c.map ≤ skeletonOfMono i (d + 1) := by
  simp [map, Subcomplex.range_eq_ofSimplex, mem_skeletonOfMono_obj_iff]

set_option backward.defeqAttrib.useBackward true in
@[simp]
/-
**SSet.relativeCellComplexOfMono.Cell.preimage_map** 是 Mathlib 中的一个引理，位于命名空间 `SS
et.relativeCellComplexOfMono.Cell`。
形式化陈述：preimage_map : (skeletonOfMono i d).preimage c.map = ∂Δ[d]
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `SSet.stdSimplex.eq_boundary_iff`：eq_boundary_iff : A = boundary n ↔ boun
dary n <= A ∧ A != ⊤
· 使用引理 `SSet.Subcomplex.le_iff_contains_nonDegenerate`：le_iff_contains_nonDegene
rate (B : X.Subcomplex) : A <= B ↔ forall (n : Nat) (x : X.nonDegenerate n), x.v
al in A.obj _ -> x.val in B.obj _
· 使用引理 `SSet.dim_lt_of_nonDegenerate`：dim_lt_of_nonDegenerate {n : Nat} (x : X.n
onDegenerate n) (d : Nat) [X.HasDimensionLT d] : n < d
· 使用定理 `SSet.instHasDimensionLTToSSetBoundary`：∀ {n : ℕ}, (SSet.boundary n).toSS
et.HasDimensionLT n
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用引理 `SSet.skeletonOfMono_obj_eq_top`：skeletonOfMono_obj_eq_top {d n : Nat} (h
 : d < n) : (skeletonOfMono i n).obj (op ⦋d⦌) = ⊤
· 使用定理 `SSet.relativeCellComplexOfMono.Cell.notMem`：∀ {X Y : _root_.SSet} {i : X
 ⟶ Y} {d : ℕ} (self : SSet.relativeCellComplexOfMono.Cell i d),   self.simplex ∉
 Set.range ⇑(CategoryTheory.Conc…
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用定理 `Eq.le`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a = b → a ≤ b
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `CategoryTheory.types_congr_hom`：types_congr_hom {X Y : Type u} {f g : X 
⟶ Y} (h : f = g) (x : X) : f x = g x
· 使用定理 `Equiv.apply_symm_apply`：∀ {α : Sort u} {β : Sort v} (e : α ≃ β) (x : β),
 e (e.symm x) = x
· 使用定理 `CategoryTheory.Functor.map_id`：∀ {C : Type u₁} [inst : CategoryTheory.Ca
tegory.{v₁, u₁} C] {D : Type u₂} [inst_1 : CategoryTheory.Category.{v₂, u₂} D]  
 (self : CategoryTh…
· 使用定理 `CategoryTheory.id_apply`：∀ {C : Type u} [inst : CategoryTheory.Category.
{v, u} C] {FC : C → C → Type u_1} {CC : C → Type w}   [inst_1 : (X Y : C) → FunL
ike (FC X Y) …
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `or_false`：∀ (p : Prop), (p ∨ False) = p
-/
lemma preimage_map : (skeletonOfMono i d).preimage c.map = ∂Δ[d] := by
  rw [stdSimplex.eq_boundary_iff]
  refine ⟨?_, fun h ↦ c.notMem ?_⟩
  · rw [Subcomplex.le_iff_contains_nonDegenerate]
    rintro n ⟨y, hy₁⟩ hy₂
    have : n < d := dim_lt_of_nonDegenerate (X := ∂Δ[d])
      ⟨⟨y, hy₂⟩, by simpa [Subcomplex.mem_nonDegenerate_iff]⟩ d
    simp [skeletonOfMono_obj_eq_top i this]
  · have := h.symm.le _ (show stdSimplex.objEquiv.symm (𝟙 ⦋d⦌) ∈ _ by simp)
    simpa [mem_skeletonOfMono_obj_iff]

end Cell

variable {i d} in
/-
**SSet.relativeCellComplexOfMono.** 是 Mathlib 中的一个引理，位于命名空间 `SSet.relativeCellCo
mplexOfMono`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma ιSigmaStdSimplex_jointly_surjective
    {n : ℕ} (a : (sigmaStdSimplex i d) _⦋n⦌) :
    ∃ (c : Cell i d) (x : Δ[d] _⦋n⦌), c.ιSigmaStdSimplex.app _ x = a :=
  Cofan.inj_jointly_surjective_of_isColimit
    (isColimitCofanMkObjOfIsColimit ((CategoryTheory.evaluation _ _).obj _) _ _
      (coproductIsCoproduct _)) a

/-- the left morphism of the pushout square `isPushout i d`: this is
the coproduct of copies of the boundary inclusion `∂Δ[d] ⟶ Δ[d]` indexed
by the nondegenerate `d`-simplices of `Y` not in the range of `i`. -/
/-
**SSet.relativeCellComplexOfMono.l** 是 Mathlib 中的一个缩写定义，位于命名空间 `SSet.relativeCel
lComplexOfMono`。
形式化陈述：l : sigmaBoundary i d ⟶ sigmaStdSimplex i d
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
the left morphism of the pushout square `isPushout i d`: this is
the coproduct of copies of the boundary inclusion `∂Δ[d] ⟶ Δ[d]` indexed
by the nondegenerate `d`-simplices of `Y` not in the range of `i`.
-/
noncomputable abbrev l : sigmaBoundary i d ⟶ sigmaStdSimplex i d :=
  Limits.Sigma.map (fun _ ↦ ∂Δ[d].ι)

/-- the top morphism of the pushout square `isPushout i d`. -/
/-
**SSet.relativeCellComplexOfMono.t** 是 Mathlib 中的一个缩写定义，位于命名空间 `SSet.relativeCel
lComplexOfMono`。
形式化陈述：t : sigmaBoundary i d ⟶ skeletonOfMono i d
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
the top morphism of the pushout square `isPushout i d`.
-/
noncomputable abbrev t : sigmaBoundary i d ⟶ skeletonOfMono i d :=
  Sigma.desc (fun c ↦ Subcomplex.lift (∂Δ[d].ι ≫ c.map)
    (by simp [Subcomplex.range_comp, Subcomplex.image_le_iff]))

/-- the bottom morphism of the pushout square `isPushout i d`. -/
/-
**SSet.relativeCellComplexOfMono.b** 是 Mathlib 中的一个缩写定义，位于命名空间 `SSet.relativeCel
lComplexOfMono`。
形式化陈述：b : sigmaStdSimplex i d ⟶ skeletonOfMono i (d + 1)
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用引理 `SSet.relativeCellComplexOfMono.Cell.range_map_le`：range_map_le : Subcomp
lex.range c.map <= skeletonOfMono i (d + 1)

--- 原说明 ---
the bottom morphism of the pushout square `isPushout i d`.
-/
noncomputable abbrev b : sigmaStdSimplex i d ⟶ skeletonOfMono i (d + 1) :=
  Sigma.desc (fun c ↦ Subcomplex.lift c.map c.range_map_le)

/-- the right morphism of the pushout square `isPushout i d`. -/
/-
**SSet.relativeCellComplexOfMono.r** 是 Mathlib 中的一个缩写定义，位于命名空间 `SSet.relativeCel
lComplexOfMono`。
形式化陈述：r : (skeletonOfMono i d : SSet) ⟶ skeletonOfMono i (d + 1)
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
the right morphism of the pushout square `isPushout i d`.
-/
abbrev r : (skeletonOfMono i d : SSet) ⟶ skeletonOfMono i (d + 1) :=
  Subcomplex.homOfLE ((skeletonOfMono i).monotone (by simp))

@[reassoc]
/-
**SSet.relativeCellComplexOfMono.w** 是 Mathlib 中的一个引理，位于命名空间 `SSet.relativeCellC
omplexOfMono`。
形式化陈述：w : t i d ≫ r i d = l i d ≫ b i d
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Limits.Sigma.hom_ext`：∀ {β : Type w} {C : Type u} [inst :
 CategoryTheory.Category.{v, u} C] {f : β → C}   [inst_1 : CategoryTheory.Limits
.HasCoproduct f] {X : C} …
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用引理 `SSet.relativeCellComplexOfMono.Cell.range_map_le`：range_map_le : Subcomp
lex.range c.map <= skeletonOfMono i (d + 1)
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `CategoryTheory.Limits.colimit.ι_desc_assoc`：∀ {J : Type u₁} [inst : Cate
goryTheory.Category.{v₁, u₁} J] {C : Type u} [inst_1 : CategoryTheory.Category.{
v, u} C]   {F : CategoryTheory.F…
· 使用定理 `CategoryTheory.Limits.Sigma.ι_map_assoc`：∀ {β : Type w} {C : Type u} [in
st : CategoryTheory.Category.{v, u} C] {f g : β → C}   [inst_1 : CategoryTheory.
Limits.HasCoproduct f] [inst_…
· 使用定理 `CategoryTheory.Limits.colimit.ι_desc`：∀ {J : Type u₁} [inst : CategoryTh
eory.Category.{v₁, u₁} J] {C : Type u} [inst_1 : CategoryTheory.Category.{v, u} 
C]   {F : CategoryTheory.F…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `CategoryTheory.cancel_mono`：∀ {C : Type u} [inst : CategoryTheory.Catego
ry.{v, u} C] {X Y Z : C} (f : Y ⟶ X) [CategoryTheory.Mono f] {g h : Z ⟶ Y},   Ca
tegoryTheory.Cat…
· 使用定理 `SSet.Subcomplex.instMonoι`：∀ {X : _root_.SSet} (A : X.Subcomplex), Categ
oryTheory.Mono A.ι
· 使用定理 `CategoryTheory.Category.assoc`：∀ {obj : Type u} [self : CategoryTheory.C
ategory.{v, u} obj] {W X Y Z : obj} (f : W ⟶ X) (g : X ⟶ Y) (h : Y ⟶ Z),   Categ
oryTheory.CategoryS…
· 使用定理 `CategoryTheory.Subfunctor.homOfLe_ι`：homOfLe_ι {G G' : Subfunctor F} (h 
: G <= G') : Subfunctor.homOfLe h ≫ G'.ι = G.ι
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma w : t i d ≫ r i d = l i d ≫ b i d := by
  ext c : 1
  simp [← cancel_mono (Subcomplex.ι _)]

namespace Cell

variable {i d}

@[reassoc]
/-
**SSet.relativeCellComplexOfMono.Cell.** 是 Mathlib 中的一个引理，位于命名空间 `SSet.relativeC
ellComplexOfMono.Cell`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma ι_t_ι_eq_ι_l_b_ι (c : Cell i d) :
    c.ιSigmaBoundary ≫ t i d ≫ Subcomplex.ι _ = ∂Δ[d].ι ≫
      c.ιSigmaStdSimplex ≫ b i d ≫ Subcomplex.ι _ := by
  simp

@[reassoc]
/-
**SSet.relativeCellComplexOfMono.Cell.** 是 Mathlib 中的一个引理，位于命名空间 `SSet.relativeC
ellComplexOfMono.Cell`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma ι_l (c : Cell i d) : c.ιSigmaBoundary ≫ l i d = ∂Δ[d].ι ≫ c.ιSigmaStdSimplex := by
  simp

#adaptation_note
/--
Now that `Cofan.mk` and `Discrete.functor` are implicit-reducible and
`backward.isDefEq.implicitBump` is enabled, the simp lemma `colimit.ι_desc_assoc` is applicable.
Previously, we had to use `by simp [Sigma.ι_desc_assoc]`, now `by simp` suffices.
The `simp` annotation on this lemma was removed because it would be redundant now, triggering the
`simpNF` linter.
-/
@[reassoc]
/-
**SSet.relativeCellComplexOfMono.Cell.** 是 Mathlib 中的一个引理，位于命名空间 `SSet.relativeC
ellComplexOfMono.Cell`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Now that `Cofan.mk` and `Discrete.functor` are implicit-reducible and
`backward.isDefEq.implicitBump` is enabled, the simp lemma `colimit.ι_desc_assoc
` is applicable.
Previously, we had to use `by simp [Sigma.ι_desc_assoc]`, now `by simp` suffices
.
The `simp` annotation on this lemma was removed because it would be redundant no
w, triggering the
`simpNF` linter.
-/
lemma ι_b_ι (c : Cell i d) : c.ιSigmaStdSimplex ≫ b i d ≫ Subcomplex.ι _ = c.map := by
  simp

set_option backward.isDefEq.respectTransparency.types false in
set_option backward.defeqAttrib.useBackward true in
/-
**SSet.relativeCellComplexOfMono.Cell.b_app_** 是 Mathlib 中的一个引理，位于命名空间 `SSet.rel
ativeCellComplexOfMono.Cell`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma b_app_ι_app_objEquiv_symm_val (c : Cell i d) {n : SimplexCategory} (f : n ⟶ ⦋d⦌) :
    dsimp% ((b i d).app _ (c.ιSigmaStdSimplex.app _ (stdSimplex.objEquiv.symm f))).val =
      Y.map f.op c.simplex := by
  simp only [← yonedaEquiv_symm_app_objEquiv_symm, ← ι_b_ι]
  dsimp +instances

end Cell

set_option backward.isDefEq.respectTransparency.types false in
set_option backward.defeqAttrib.useBackward true in
/-
**SSet.relativeCellComplexOfMono.isPullback** 是 Mathlib 中的一个引理，位于命名空间 `SSet.rela
tiveCellComplexOfMono`。
形式化陈述：isPullback : IsPullback (t i d) (l i d) (r i d) (b i d) where w
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `SSet.relativeCellComplexOfMono.w`：w : t i d ≫ r i d = l i d ≫ b i d
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `CategoryTheory.Limits.Types.isPullback_iff`：∀ {X₁ X₂ X₃ X₄ : Type u} (t 
: X₁ ⟶ X₂) (r : X₂ ⟶ X₄) (l : X₁ ⟶ X₃) (b : X₃ ⟶ X₄),   CategoryTheory.IsPullbac
k t l r b ↔     CategoryTheory.C…
· 使用定理 `CategoryTheory.NatTrans.congr_app`：congr_app {α β : F ⟶ G} (h : α = β) (
X : C) : α.app X = β.app X
· 使用定理 `CategoryTheory.injective_of_mono`：injective_of_mono {X Y : Type u} (f : 
X ⟶ Y) [hf : Mono f] : Function.Injective f
· 使用定理 `CategoryTheory.instMonoAppOfFunctor`：∀ {K : Type u} [inst : CategoryTheo
ry.Category.{v, u} K] {C : Type u'} [inst_1 : CategoryTheory.Category.{v', u'} C
]   {F G : CategoryTheory…
· 使用定理 `Finite.of_fintype`：∀ (α : Type u_4) [Fintype α], Finite α
· 使用定理 `CategoryTheory.Limits.hasFiniteWidePullbacks_of_hasFiniteLimits`：∀ (C : 
Type u) [inst : CategoryTheory.Category.{v, u} C] [CategoryTheory.Limits.HasFini
teLimits C],   CategoryTheory.Limits.HasFiniteWidePul…
· 使用定理 `CategoryTheory.Limits.hasFiniteLimits_of_hasLimits`：∀ (C : Type u) [inst
 : CategoryTheory.Category.{v, u} C] [CategoryTheory.Limits.HasLimits C],   Cate
goryTheory.Limits.HasFiniteLimits C
· 使用定理 `CategoryTheory.Limits.Types.hasLimitsOfSize`：∀ [UnivLE.{v, u}], Category
Theory.Limits.HasLimitsOfSize.{w, v, u, u + 1} (Type u)
· 使用定理 `CategoryTheory.MorphismProperty.instMonoMapOfIsStableUnderCoproductsOfSh
apeMonomorphisms`：∀ {C : Type u} [inst : CategoryTheory.Category.{v, u} C] (J : 
Type u_1)   [(CategoryTheory.MorphismProperty.monomorphisms C).IsStableUnderCo…
· 使用定理 `CategoryTheory.MorphismProperty.instIsStableUnderCoproductsOfShapeFuncto
rMonomorphismsOfHasCoproductsOfShapeOfHasPullbacks`：∀ {C : Type u} [inst : Categ
oryTheory.Category.{v, u} C] (J : Type u'') [inst_1 : CategoryTheory.Category.{v
'', u''} J]   (K' : Type u') [(C…
· 使用定理 `CategoryTheory.Types.instIsStableUnderCoproductsOfShapeMonomorphismsType
`：∀ (T : Type u'), (CategoryTheory.MorphismProperty.monomorphisms (Type u)).IsSt
ableUnderCoproductsOfShape T
· 使用定理 `CategoryTheory.Limits.Types.hasColimitsOfShape`：∀ {J : Type v} [inst : C
ategoryTheory.Category.{w, v} J] [Small.{u, v} J],   CategoryTheory.Limits.HasCo
limitsOfShape J (Type u)
· 使用定理 `CategoryTheory.instSmallDiscrete`：∀ (C : Type u) [Small.{w, u} C], Small
.{w, u} (CategoryTheory.Discrete C)
· 使用定理 `UnivLE.small`：∀ [self : UnivLE.{u, v}] (α : Type u), Small.{v, u} α
· 使用定理 `SSet.Subcomplex.instMonoι`：∀ {X : _root_.SSet} (A : X.Subcomplex), Categ
oryTheory.Mono A.ι
· 使用引理 `SSet.relativeCellComplexOfMono.ιSigmaStdSimplex_jointly_surjective`：ιSig
maStdSimplex_jointly_surjective {n : Nat} (a : (sigmaStdSimplex i d) _⦋n⦌) : exi
sts (c : Cell i d) (x : Δ[d] _⦋n⦌), c.ιSigmaStdSimplex.a…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `SSet.relativeCellComplexOfMono.Cell.preimage_map`：preimage_map : (skelet
onOfMono i d).preimage c.map = ∂Δ[d]
· 使用定理 `SSet.Subcomplex.preimage_obj`：∀ {X Y : _root_.SSet} (A : X.Subcomplex) (
p : Y ⟶ X) (n : SimplexCategoryᵒᵖ),   (A.preimage p).obj n = ⇑(CategoryTheory.Co
ncreteCategory.hom…
· 使用定理 `Set.mem_preimage`：mem_preimage {f : α -> β} {s : Set β} {a : α} : a in f
 ⁻¹' s ↔ f a in s
· 使用引理 `SSet.relativeCellComplexOfMono.Cell.ι_b_ι`：ι_b_ι (c : Cell i d) : c.ιSig
maStdSimplex ≫ b i d ≫ Subcomplex.ι _ = c.map
· 使用定理 `Subtype.ext_iff`：∀ {α : Sort u} {p : α → Prop} {a1 a2 : { x // p x }}, a
1 = a2 ↔ ↑a1 = ↑a2
· 使用引理 `SSet.relativeCellComplexOfMono.Cell.ι_t_ι_eq_ι_l_b_ι`：ι_t_ι_eq_ι_l_b_ι (
c : Cell i d) : c.ιSigmaBoundary ≫ t i d ≫ Subcomplex.ι _ = ∂Δ[d].ι ≫ c.ιSigmaSt
dSimplex ≫ b i d ≫ Subcomplex.ι _
· 使用引理 `SSet.relativeCellComplexOfMono.Cell.ι_l`：ι_l (c : Cell i d) : c.ιSigmaBo
undary ≫ l i d = ∂Δ[d].ι ≫ c.ιSigmaStdSimplex
-/
lemma isPullback : IsPullback (t i d) (l i d) (r i d) (b i d) where
  w := w i d
  isLimit' := ⟨evaluationJointlyReflectsLimits _ (fun ⟨⟨n⟩⟩ ↦ by
    refine (isLimitMapConePullbackConeEquiv _ _).2
      (IsPullback.isLimit ?_)
    dsimp
    rw [Types.isPullback_iff]
    refine ⟨NatTrans.congr_app (w i d) _,
      fun x₁ x₂ ⟨_, h⟩ ↦ injective_of_mono ((l i d).app (op ⦋n⦌)) h,
      fun ⟨x, hx⟩ y h ↦ ?_⟩
    rw [Subtype.ext_iff] at h
    dsimp at h
    subst h
    obtain ⟨c, y, rfl⟩ := ιSigmaStdSimplex_jointly_surjective y
    refine ⟨c.ιSigmaBoundary.app _ ⟨y, ?_⟩, ?_, ?_⟩
    · rw [dsimp% congr($(c.ι_b_ι).app (op ⦋n⦌) y)] at hx
      rwa [← c.preimage_map, Subcomplex.preimage_obj, Set.mem_preimage]
    · rw [Subtype.ext_iff]
      exact congr($(c.ι_t_ι_eq_ι_l_b_ι).app _ ⟨y, _⟩)
    · exact congr($(c.ι_l).app _ ⟨y, _⟩))⟩

set_option backward.defeqAttrib.useBackward true in
/-
**SSet.relativeCellComplexOfMono.sup_range_r_range_b** 是 Mathlib 中的一个引理，位于命名空间 `
SSet.relativeCellComplexOfMono`。
形式化陈述：sup_range_r_range_b : Subcomplex.range (r i d) ⊔ Subcomplex.range (b i d) 
= ⊤
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `top_le_iff`：top_le_iff : ⊤ <= a ↔ a = ⊤
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrFun`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, f = g →
 ∀ (a : α), f a = g a
· 使用引理 `SSet.skeletonOfMono_succ`：skeletonOfMono_succ (n : Nat) : skeletonOfMono
 i (n + 1) = skeletonOfMono i n ⊔ ⨆ (x : Y.nonDegenerate n) (_ : x.1 ∉ (Subcompl
ex.range i).ob…
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `iSup_congr_Prop`：iSup_congr_Prop {p q : Prop} {f₁ : p -> α} {f₂ : q -> α
} (pq : p ↔ q) (f : forall x, f₁ (pq.mpr x) = f₂ x) : iSup f₁ = iSup f₂
· 使用定理 `Iff.of_eq`：∀ {a b : Prop}, a = b → (a ↔ b)
· 使用引理 `CategoryTheory.Subfunctor.iSup_obj`：iSup_obj {ι : Sort*} (S : ι -> Subfu
nctor F) (U : C) : (⨆ i, S i).obj U = ⋃ i, (S i).obj U
· 使用定理 `Set.iUnion_coe_set`：iUnion_coe_set {α β : Type*} (s : Set α) (f : s -> S
et β) : ⋃ i, f i = ⋃ i in s, f ⟨i, ‹i in s›⟩
· 使用定理 `Set.iUnion_congr_Prop`：iUnion_congr_Prop {p q : Prop} {f₁ : p -> Set α} 
{f₂ : q -> Set α} (pq : p ↔ q) (f : forall x, f₁ (pq.mpr x) = f₂ x) : iUnion f₁ 
= iUnion f₂
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `exists_prop_congr`：∀ {p p' : Prop} {q q' : p → Prop}, (∀ (h : p), q h ↔ 
q' h) → ∀ (hp : p ↔ p'), Exists q ↔ ∃ (h : p'), q' ⋯
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用定理 `Subtype.ext_iff`：∀ {α : Sort u} {p : α → Prop} {a1 a2 : { x // p x }}, a
1 = a2 ↔ ↑a1 = ↑a2
· 使用引理 `SSet.relativeCellComplexOfMono.Cell.b_app_ι_app_objEquiv_symm_val`：b_app
_ι_app_objEquiv_symm_val (c : Cell i d) {n : SimplexCategory} (f : n ⟶ ⦋d⦌) : ds
imp% ((b i d).app _ (c.ιSigmaStdSimplex.app _ (stdSimpl…
-/
lemma sup_range_r_range_b :
    Subcomplex.range (r i d) ⊔ Subcomplex.range (b i d) = ⊤ := by
  rw [← top_le_iff]
  rintro ⟨n⟩ ⟨x, hx⟩ _
  simp only [skeletonOfMono_succ, Subfunctor.range_obj, Set.mem_range, not_exists,
    Subfunctor.max_obj, Subfunctor.iSup_obj, Set.iUnion_coe_set, Set.mem_union, Set.mem_iUnion,
    exists_prop] at hx
  simp only [Subfunctor.toFunctor_obj, Subfunctor.max_obj, Subfunctor.range_obj,
    Subfunctor.homOfLe_app, TypeCat.hom_ofHom]
  obtain hx | ⟨y, hy₁, hy₂, f, rfl⟩ := hx
  · exact Or.inl ⟨⟨x, hx⟩, rfl⟩
  · let c : Cell i d := { simplex := y, nonDegenerate := hy₁, notMem := by simpa }
    refine Or.inr ⟨c.ιSigmaStdSimplex.app _ (stdSimplex.objEquiv.symm f.unop), ?_⟩
    rw [Subtype.ext_iff]
    exact c.b_app_ι_app_objEquiv_symm_val _
/-
**SSet.relativeCellComplexOfMono.range_r_app_union_range_b_app** 是 Mathlib 中的一个引
理，位于命名空间 `SSet.relativeCellComplexOfMono`。
形式化陈述：range_r_app_union_range_b_app (n : SimplexCategoryᵒᵖ) : Set.range ((r i d)
.app n) union Set.range ((b i d).app n) = Set.univ
参数：n : SimplexCategoryᵒᵖ。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `SSet.relativeCellComplexOfMono.sup_range_r_range_b`：sup_range_r_range_b 
: Subcomplex.range (r i d) ⊔ Subcomplex.range (b i d) = ⊤
-/
lemma range_r_app_union_range_b_app (n : SimplexCategoryᵒᵖ) :
    Set.range ((r i d).app n) ∪
      Set.range ((b i d).app n) = Set.univ :=
  congr($(sup_range_r_range_b i d).obj n)

variable {i d} in
/-
**SSet.relativeCellComplexOfMono.isPushout_aux** 是 Mathlib 中的一个引理，位于命名空间 `SSet.r
elativeCellComplexOfMono`。
形式化陈述：isPushout_aux {n : Nat} (y : (sigmaStdSimplex i d) _⦋n⦌) (hy : y ∉ Set.ran
ge ((l i d).app (op ⦋n⦌))) : exists (c : Cell i d) (f : ⦋n⦌ ⟶ ⦋d⦌) (_ : Epi f), 
c.ιSigmaStdSimplex.app _ (stdSimplex.objEquiv.symm f) = y
参数：y : (sigmaStdSimplex i d) _⦋n⦌；hy : y ∉ Set.range ((l i d).app (op ⦋n⦌))。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用引理 `SSet.relativeCellComplexOfMono.ιSigmaStdSimplex_jointly_surjective`：ιSig
maStdSimplex_jointly_surjective {n : Nat} (a : (sigmaStdSimplex i d) _⦋n⦌) : exi
sts (c : Cell i d) (x : Δ[d] _⦋n⦌), c.ιSigmaStdSimplex.a…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `SSet.relativeCellComplexOfMono.Cell.ι_l`：ι_l (c : Cell i d) : c.ιSigmaBo
undary ≫ l i d = ∂Δ[d].ι ≫ c.ιSigmaStdSimplex
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `Equiv.symm_apply_apply`：∀ {α : Sort u} {β : Sort v} (e : α ≃ β) (x : α),
 e.symm (e x) = x
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma isPushout_aux {n : ℕ} (y : (sigmaStdSimplex i d) _⦋n⦌)
    (hy : y ∉ Set.range ((l i d).app (op ⦋n⦌))) :
    ∃ (c : Cell i d) (f : ⦋n⦌ ⟶ ⦋d⦌) (_ : Epi f),
      c.ιSigmaStdSimplex.app _ (stdSimplex.objEquiv.symm f) = y := by
  obtain ⟨c, s, rfl⟩ := ιSigmaStdSimplex_jointly_surjective y
  have hs : s ∉ ∂Δ[d].obj _ :=
    fun hs ↦ hy (⟨c.ιSigmaBoundary.app _ ⟨s, hs⟩, congr($(c.ι_l).app _ ⟨s, _⟩)⟩)
  refine ⟨c, stdSimplex.objEquiv s, ?_, by simp⟩
  simpa [SimplexCategory.epi_iff_surjective, boundary] using! hs

set_option backward.defeqAttrib.useBackward true in
/-
**SSet.relativeCellComplexOfMono.isPushout** 是 Mathlib 中的一个引理，位于命名空间 `SSet.relat
iveCellComplexOfMono`。
形式化陈述：isPushout : IsPushout (t i d) (l i d) (r i d) (b i d) where w
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `SSet.relativeCellComplexOfMono.w`：w : t i d ≫ r i d = l i d ≫ b i d
· 使用引理 `CategoryTheory.Limits.Types.isPushout_of_isPullback_of_mono'`：isPushout_
of_isPullback_of_mono' (h₁ : IsPullback t l r b) [Mono r] (h₂ : Set.range r ⊔ Se
t.range b = Set.univ) (H : forall (x₃ y₃ : X₃) (_ …
· 使用定理 `CategoryTheory.IsPullback.map`：∀ {C : Type u₁} [inst : CategoryTheory.Ca
tegory.{v₁, u₁} C] {D : Type u₂} [inst_1 : CategoryTheory.Category.{v₂, u₂} D]  
 (F : CategoryTheor…
· 使用定理 `UnivLE.small`：∀ [self : UnivLE.{u, v}] (α : Type u), Small.{v, u} α
· 使用引理 `SSet.relativeCellComplexOfMono.isPullback`：isPullback : IsPullback (t i 
d) (l i d) (r i d) (b i d) where w
· 使用定理 `CategoryTheory.Functor.map_mono`：∀ {C : Type u₁} [inst : CategoryTheory.
Category.{v₁, u₁} C] {D : Type u₂} [inst_1 : CategoryTheory.Category.{v₂, u₂} D]
   (F : CategoryTheor…
· 使用定理 `CategoryTheory.preservesMonomorphisms_of_preservesLimitsOfShape`：∀ {C : 
Type u₁} {D : Type u₂} [inst : CategoryTheory.Category.{v₁, u₁} C] [inst_1 : Cat
egoryTheory.Category.{v₂, u₂} D]   (F : CategoryTheor…
· 使用定理 `Finite.of_fintype`：∀ (α : Type u_4) [Fintype α], Finite α
· 使用定理 `CategoryTheory.Limits.hasFiniteWidePullbacks_of_hasFiniteLimits`：∀ (C : 
Type u) [inst : CategoryTheory.Category.{v, u} C] [CategoryTheory.Limits.HasFini
teLimits C],   CategoryTheory.Limits.HasFiniteWidePul…
· 使用定理 `CategoryTheory.Limits.hasFiniteLimits_of_hasLimits`：∀ (C : Type u) [inst
 : CategoryTheory.Category.{v, u} C] [CategoryTheory.Limits.HasLimits C],   Cate
goryTheory.Limits.HasFiniteLimits C
· 使用定理 `CategoryTheory.Limits.Types.hasLimitsOfSize`：∀ [UnivLE.{v, u}], Category
Theory.Limits.HasLimitsOfSize.{w, v, u, u + 1} (Type u)
· 使用引理 `SSet.relativeCellComplexOfMono.range_r_app_union_range_b_app`：range_r_ap
p_union_range_b_app (n : SimplexCategoryᵒᵖ) : Set.range ((r i d).app n) union Se
t.range ((b i d).app n) = Set.univ
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用引理 `SSet.relativeCellComplexOfMono.isPushout_aux`：isPushout_aux {n : Nat} (y
 : (sigmaStdSimplex i d) _⦋n⦌) (hy : y ∉ Set.range ((l i d).app (op ⦋n⦌))) : exi
sts (c : Cell i d) (f : ⦋n⦌ ⟶ ⦋d⦌)…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `SSet.relativeCellComplexOfMono.Cell.b_app_ι_app_objEquiv_symm_val`：b_app
_ι_app_objEquiv_symm_val (c : Cell i d) {n : SimplexCategory} (f : n ⟶ ⦋d⦌) : ds
imp% ((b i d).app _ (c.ιSigmaStdSimplex.app _ (stdSimpl…
· 使用定理 `Subtype.ext_iff`：∀ {α : Sort u} {p : α → Prop} {a1 a2 : { x // p x }}, a
1 = a2 ↔ ↑a1 = ↑a2
· 使用引理 `SSet.unique_nonDegenerate_map`：unique_nonDegenerate_map (x : X _⦋n⦌) {m 
: Nat} (f₁ : ⦋n⦌ ⟶ ⦋m⦌) [Epi f₁] (y₁ : X.nonDegenerate m) (hy₁ : x = X.map f₁.op
 y₁) (f₂ : ⦋n⦌ ⟶ ⦋m…
· 使用定理 `SSet.relativeCellComplexOfMono.Cell.nonDegenerate`：∀ {X Y : _root_.SSet}
 {i : X ⟶ Y} {d : ℕ} (self : SSet.relativeCellComplexOfMono.Cell i d),   self.si
mplex ∈ Y.nonDegenerate d
· 使用定理 `SSet.relativeCellComplexOfMono.Cell.ext`：∀ {X Y : _root_.SSet} {i : X ⟶ 
Y} {d : ℕ} {x y : SSet.relativeCellComplexOfMono.Cell i d}, x.simplex = y.simple
x → x = y
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用引理 `SSet.unique_nonDegenerate_simplex`：unique_nonDegenerate_simplex (x : X _
⦋n⦌) {m : Nat} (f₁ : ⦋n⦌ ⟶ ⦋m⦌) [Epi f₁] (y₁ : X.nonDegenerate m) (hy₁ : x = X.m
ap f₁.op y₁) (f₂ : ⦋n⦌ …
-/
lemma isPushout : IsPushout (t i d) (l i d) (r i d) (b i d) where
  w := w i d
  isColimit' := ⟨evaluationJointlyReflectsColimits _ (fun ⟨⟨n⟩⟩ ↦ by
    refine (isColimitMapCoconePushoutCoconeEquiv _ _).2
      (IsPushout.isColimit ?_)
    refine Types.isPushout_of_isPullback_of_mono' ?_ ?_ ?_
    · exact (isPullback i d).map ((evaluation _ _).obj _)
    · exact range_r_app_union_range_b_app _ _ _
    · dsimp
      intro x₁ x₂ hx₁ hx₂ h
      obtain ⟨c₁, f₁, _, rfl⟩ := isPushout_aux x₁ hx₁
      obtain ⟨c₂, f₂, _, rfl⟩ := isPushout_aux x₂ hx₂
      rw [Subtype.ext_iff] at h
      replace h : Y.map f₁.op c₁.simplex = Y.map f₂.op c₂.simplex := by
        rwa [← c₁.b_app_ι_app_objEquiv_symm_val f₁, ← c₂.b_app_ι_app_objEquiv_symm_val f₂]
      obtain rfl : c₁ = c₂ := by
        ext
        exact Subtype.ext_iff.mp
          (Y.unique_nonDegenerate_simplex _ f₁ ⟨_, c₁.nonDegenerate⟩ rfl
            f₂ ⟨_, c₂.nonDegenerate⟩ h)
      obtain rfl : f₁ = f₂ :=
        Y.unique_nonDegenerate_map _ f₁ ⟨_, c₁.nonDegenerate⟩ rfl
          f₂ ⟨_, c₁.nonDegenerate⟩ h
      rfl)⟩

end relativeCellComplexOfMono

set_option backward.defeqAttrib.useBackward true in
open relativeCellComplexOfMono in
/-- If `i : X ⟶ Y` is a monomorphism of simplicial sets, then it is a relative cell
complex with basic cells given by boundary inclusions `∂Δ[d] ⟶ Δ[d]`, one for each
nondegenerate `d`-simplex of `Y` not in the range of `X`. -/
@[simps]
/-
**SSet.relativeCellComplexOfMono** 是 Mathlib 中的一个定义，位于命名空间 `SSet`。
形式化陈述：relativeCellComplexOfMono [Mono i] : RelativeCellComplex.{u} (basicCell
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `instWellFoundedLTNat`：WellFoundedLT ℕ
· 使用定理 `SSet.Subcomplex.instIsIsoToRangeOfMono`：∀ {X Y : _root_.SSet} (f : X ⟶ Y
) [CategoryTheory.Mono f], CategoryTheory.IsIso (SSet.Subcomplex.toRange f)
· 使用引理 `SSet.iSup_skeletonOfMono`：iSup_skeletonOfMono : ⨆ (n : Nat), skeletonOfM
ono i n = ⊤
· 使用引理 `SSet.relativeCellComplexOfMono.isPushout`：isPushout : IsPushout (t i d) 
(l i d) (r i d) (b i d) where w

--- 原说明 ---
If `i : X ⟶ Y` is a monomorphism of simplicial sets, then it is a relative cell
complex with basic cells given by boundary inclusions `∂Δ[d] ⟶ Δ[d]`, one for ea
ch
nondegenerate `d`-simplex of `Y` not in the range of `X`.
-/
noncomputable def relativeCellComplexOfMono [Mono i] :
    RelativeCellComplex.{u} (basicCell := fun (n : ℕ) (_ : Unit) ↦ ∂Δ[n].ι) i where
  F := (skeletonOfMono i).monotone.functor ⋙ Subcomplex.toSSetFunctor
  isoBot := Subcomplex.eqToIso (by simp) ≪≫ (asIso (Subcomplex.toRange i)).symm
  incl.app _ := Subcomplex.ι _
  isColimit :=
    IsColimit.ofIsoColimit (isColimitOfPreserves (Subcomplex.toSSetFunctor)
      ((CompleteLattice.colimitCocone ((skeletonOfMono i).monotone.functor)).isColimit))
        (Cocone.ext (Subcomplex.eqToIso (iSup_skeletonOfMono i) ≪≫ Subcomplex.topIso Y) )
  attachCells d _ :=
    { ι := Cell i d
      π _ := .unit
      cofan₁ := _
      cofan₂ := _
      isColimit₁ := coproductIsCoproduct _
      isColimit₂ := coproductIsCoproduct _
      m := l i d
      g₁ := t i d
      g₂ := b i d
      isPushout := isPushout i d }

variable (X) in
/-- If `X` is a simplicial set, then the inclusion `(⊥ : SSet) ⟶ X` of the empty
subcomplex of `X` is a relative cell complex with basic cells given by boundary
inclusions `∂Δ[d] ⟶ Δ[d]`, one for each nondegenerate `d`-simplex of `X`. -/
/-
**SSet.relativeCellComplex** 是 Mathlib 中的一个缩写定义，位于命名空间 `SSet`。
形式化陈述：relativeCellComplex : RelativeCellComplex.{u} (basicCell
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If `X` is a simplicial set, then the inclusion `(⊥ : SSet) ⟶ X` of the empty
subcomplex of `X` is a relative cell complex with basic cells given by boundary
inclusions `∂Δ[d] ⟶ Δ[d]`, one for each nondegenerate `d`-simplex of `X`.
-/
noncomputable abbrev relativeCellComplex :
    RelativeCellComplex.{u} (basicCell := fun (n : ℕ) (_ : Unit) ↦ ∂Δ[n].ι)
      (Subcomplex.ι ⊥ : _ ⟶ X) :=
  relativeCellComplexOfMono _

/-- The bijection between the cells of `relativeCellComplex X` and the type
`X.N` of nondegenerate simplices of the simplicial set `X`. -/
@[simps]
/-
**SSet.relativeCellComplexCellsEquiv** 是 Mathlib 中的一个定义，位于命名空间 `SSet`。
形式化陈述：relativeCellComplexCellsEquiv : (relativeCellComplex X).Cells ≃ X.N where 
toFun c
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `instWellFoundedLTNat`：WellFoundedLT ℕ
· 使用定理 `SSet.N.nonDegenerate`：∀ {X : _root_.SSet} (self : X.N), self.simplex ∈ X
.nonDegenerate self.dim

--- 原说明 ---
The bijection between the cells of `relativeCellComplex X` and the type
`X.N` of nondegenerate simplices of the simplicial set `X`.
-/
def relativeCellComplexCellsEquiv : (relativeCellComplex X).Cells ≃ X.N where
  toFun c := N.mk _ c.k.nonDegenerate
  invFun s :=
    { j := s.dim
      hj := by simp
      k := { simplex := s.simplex, nonDegenerate := s.nonDegenerate, notMem := by simp } }

end SSet

