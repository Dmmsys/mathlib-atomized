/-
Copyright (c) 2023 Felix Weilacher. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Felix Weilacher, Yury Kudryashov, Rémy Degenne
-/
module

public import Mathlib.MeasureTheory.MeasurableSpace.Embedding
public import Mathlib.Data.Set.MemPartition
public import Mathlib.Order.Filter.CountableSeparatingOn

/-!
# Countably generated measurable spaces

We say a measurable space is countably generated if it can be generated by a countable set of sets.

In such a space, we can also build a sequence of finer and finer finite measurable partitions of
the space such that the measurable space is generated by the union of all partitions.

## Main definitions

* `MeasurableSpace.CountablyGenerated`: class stating that a measurable space is countably
  generated.
* `MeasurableSpace.countableGeneratingSet`: a countable set of sets that generates the σ-algebra.
* `MeasurableSpace.countablePartition`: sequences of finer and finer partitions of
  a countably generated space, defined by taking the `memPartition` of an enumeration of the sets in
  `countableGeneratingSet`.
* `MeasurableSpace.SeparatesPoints` : class stating that a measurable space separates points.

## Main statements

* `MeasurableSpace.measurableEquiv_nat_bool_of_countablyGenerated`: if a measurable space is
  countably generated and separates points, it is measure equivalent to a subset of the Cantor Space
  `ℕ → Bool` (equipped with the product sigma algebra).
* `MeasurableSpace.measurable_injection_nat_bool_of_countablySeparated`: If a measurable space
  admits a countable sequence of measurable sets separating points,
  it admits a measurable injection into the Cantor space `ℕ → Bool`
  `ℕ → Bool` (equipped with the product sigma algebra).

The file also contains measurability results about `memPartition`, from which the properties of
`countablePartition` are deduced.

-/

@[expose] public section

open Set MeasureTheory

namespace MeasurableSpace

variable {α β : Type*}

/-- We say a measurable space is countably generated
if it can be generated by a countable set of sets. -/
/-
**MeasurableSpace.CountablyGenerated** 是 Mathlib 中的一个归纳类型，位于命名空间 `MeasurableSpac
e`。
形式化陈述：(α : Type u_3) → [m : MeasurableSpace α] → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
We say a measurable space is countably generated
if it can be generated by a countable set of sets.
-/
class CountablyGenerated (α : Type*) [m : MeasurableSpace α] : Prop where
  isCountablyGenerated : ∃ b : Set (Set α), b.Countable ∧ m = generateFrom b

/-- A countable set of sets that generate the measurable space.
We insert `∅` to ensure it is nonempty. -/
-- Note: `Set` has no computational content, but Lean still attempts to compile it.
-- See https://github.com/leanprover/lean4/issues/14084.
noncomputable
/-
**MeasurableSpace.countableGeneratingSet** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSp
ace`。
形式化陈述：countableGeneratingSet (α : Type*) [MeasurableSpace α] [h : CountablyGener
ated α] : Set (Set α)
参数：α : Type*。
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.CountablyGenerated.isCountablyGenerated`：∀ {α : Type u_3
} {m : MeasurableSpace α} [self : MeasurableSpace.CountablyGenerated α],   ∃ b, 
b.Countable ∧ m = MeasurableSpace.generateFro…
-/
def countableGeneratingSet (α : Type*) [MeasurableSpace α] [h : CountablyGenerated α] :
    Set (Set α) :=
  insert ∅ h.isCountablyGenerated.choose
/-
**MeasurableSpace.countable_countableGeneratingSet** 是 Mathlib 中的一个引理，位于命名空间 `Me
asurableSpace`。
形式化陈述：countable_countableGeneratingSet [MeasurableSpace α] [h : CountablyGenerat
ed α] : Set.Countable (countableGeneratingSet α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.Countable.insert`：∀ {α : Type u} {s : Set α} (a : α), s.Countable → 
(insert a s).Countable
· 使用定理 `MeasurableSpace.CountablyGenerated.isCountablyGenerated`：∀ {α : Type u_3
} {m : MeasurableSpace α} [self : MeasurableSpace.CountablyGenerated α],   ∃ b, 
b.Countable ∧ m = MeasurableSpace.generateFro…
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `Exists.choose_spec`：∀ {α : Sort u_1} {p : α → Prop} (P : ∃ a, p a), p P.
choose
-/
lemma countable_countableGeneratingSet [MeasurableSpace α] [h : CountablyGenerated α] :
    Set.Countable (countableGeneratingSet α) :=
  Countable.insert _ h.isCountablyGenerated.choose_spec.1
/-
**MeasurableSpace.generateFrom_countableGeneratingSet** 是 Mathlib 中的一个引理，位于命名空间 
`MeasurableSpace`。
形式化陈述：generateFrom_countableGeneratingSet [m : MeasurableSpace α] [h : Countably
Generated α] : generateFrom (countableGeneratingSet α) = m
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `MeasurableSpace.CountablyGenerated.isCountablyGenerated`：∀ {α : Type u_3
} {m : MeasurableSpace α} [self : MeasurableSpace.CountablyGenerated α],   ∃ b, 
b.Countable ∧ m = MeasurableSpace.generateFro…
· 使用定理 `MeasurableSpace.generateFrom_insert_empty`：generateFrom_insert_empty (S 
: Set (Set α)) : generateFrom (insert ∅ S) = generateFrom S
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b
· 使用定理 `Exists.choose_spec`：∀ {α : Sort u_1} {p : α → Prop} (P : ∃ a, p a), p P.
choose
-/
lemma generateFrom_countableGeneratingSet [m : MeasurableSpace α] [h : CountablyGenerated α] :
    generateFrom (countableGeneratingSet α) = m :=
  (generateFrom_insert_empty _).trans <| h.isCountablyGenerated.choose_spec.2.symm
/-
**MeasurableSpace.empty_mem_countableGeneratingSet** 是 Mathlib 中的一个引理，位于命名空间 `Me
asurableSpace`。
形式化陈述：empty_mem_countableGeneratingSet [MeasurableSpace α] [CountablyGenerated α
] : ∅ in countableGeneratingSet α
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.mem_insert`：mem_insert (x : α) (s : Set α) : x in insert x s
· 使用定理 `MeasurableSpace.CountablyGenerated.isCountablyGenerated`：∀ {α : Type u_3
} {m : MeasurableSpace α} [self : MeasurableSpace.CountablyGenerated α],   ∃ b, 
b.Countable ∧ m = MeasurableSpace.generateFro…
-/
lemma empty_mem_countableGeneratingSet [MeasurableSpace α] [CountablyGenerated α] :
    ∅ ∈ countableGeneratingSet α := mem_insert _ _
/-
**MeasurableSpace.nonempty_countableGeneratingSet** 是 Mathlib 中的一个引理，位于命名空间 `Mea
surableSpace`。
形式化陈述：nonempty_countableGeneratingSet [MeasurableSpace α] [CountablyGenerated α]
 : Set.Nonempty (countableGeneratingSet α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.mem_insert`：mem_insert (x : α) (s : Set α) : x in insert x s
· 使用定理 `MeasurableSpace.CountablyGenerated.isCountablyGenerated`：∀ {α : Type u_3
} {m : MeasurableSpace α} [self : MeasurableSpace.CountablyGenerated α],   ∃ b, 
b.Countable ∧ m = MeasurableSpace.generateFro…
-/
lemma nonempty_countableGeneratingSet [MeasurableSpace α] [CountablyGenerated α] :
    Set.Nonempty (countableGeneratingSet α) :=
  ⟨∅, mem_insert _ _⟩
/-
**MeasurableSpace.measurableSet_countableGeneratingSet** 是 Mathlib 中的一个引理，位于命名空间
 `MeasurableSpace`。
形式化陈述：measurableSet_countableGeneratingSet [MeasurableSpace α] [CountablyGenerat
ed α] {s : Set α} (hs : s in countableGeneratingSet α) : MeasurableSet s
参数：hs : s in countableGeneratingSet α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `MeasurableSpace.generateFrom_countableGeneratingSet`：generateFrom_counta
bleGeneratingSet [m : MeasurableSpace α] [h : CountablyGenerated α] : generateFr
om (countableGeneratingSet α) = m
· 使用定理 `MeasurableSpace.measurableSet_generateFrom`：measurableSet_generateFrom {
s : Set (Set α)} {t : Set α} (ht : t in s) : MeasurableSet[generateFrom s] t
-/
lemma measurableSet_countableGeneratingSet [MeasurableSpace α] [CountablyGenerated α]
    {s : Set α} (hs : s ∈ countableGeneratingSet α) :
    MeasurableSet s := by
  rw [← generateFrom_countableGeneratingSet (α := α)]
  exact measurableSet_generateFrom hs

/-- A countable sequence of sets generating the measurable space. -/
-- Note: `Set` has no computational content, but Lean still attempts to compile it.
-- See https://github.com/leanprover/lean4/issues/14084.
noncomputable
/-
**MeasurableSpace.natGeneratingSequence** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSpa
ce`。
形式化陈述：natGeneratingSequence (α : Type*) [MeasurableSpace α] [CountablyGenerated 
α] : Nat -> (Set α)
参数：α : Type*。
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
-/
def natGeneratingSequence (α : Type*) [MeasurableSpace α] [CountablyGenerated α] : ℕ → (Set α) :=
  enumerateCountable (countable_countableGeneratingSet (α := α)) ∅
/-
**MeasurableSpace.generateFrom_natGeneratingSequence** 是 Mathlib 中的一个引理，位于命名空间 `
MeasurableSpace`。
形式化陈述：generateFrom_natGeneratingSequence (α : Type*) [m : MeasurableSpace α] [Co
untablyGenerated α] : generateFrom (range (natGeneratingSequence _)) = m
参数：α : Type*。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSpace.natGeneratingSequence.eq_1`：∀ (α : Type u_3) [inst : Mea
surableSpace α] [inst_1 : MeasurableSpace.CountablyGenerated α],   MeasurableSpa
ce.natGeneratingSequence α = Set…
· 使用引理 `Set.range_enumerateCountable_of_mem`：range_enumerateCountable_of_mem {s 
: Set α} (h : s.Countable) {default : α} (h_mem : default in s) : range (enumera
teCountable h default) = …
· 使用引理 `MeasurableSpace.empty_mem_countableGeneratingSet`：empty_mem_countableGen
eratingSet [MeasurableSpace α] [CountablyGenerated α] : ∅ in countableGenerating
Set α
· 使用引理 `MeasurableSpace.generateFrom_countableGeneratingSet`：generateFrom_counta
bleGeneratingSet [m : MeasurableSpace α] [h : CountablyGenerated α] : generateFr
om (countableGeneratingSet α) = m
-/
lemma generateFrom_natGeneratingSequence (α : Type*) [m : MeasurableSpace α]
    [CountablyGenerated α] : generateFrom (range (natGeneratingSequence _)) = m := by
  rw [natGeneratingSequence, range_enumerateCountable_of_mem _ empty_mem_countableGeneratingSet,
    generateFrom_countableGeneratingSet]
/-
**MeasurableSpace.measurableSet_natGeneratingSequence** 是 Mathlib 中的一个引理，位于命名空间 
`MeasurableSpace`。
形式化陈述：measurableSet_natGeneratingSequence [MeasurableSpace α] [CountablyGenerate
d α] (n : Nat) : MeasurableSet (natGeneratingSequence α n)
参数：n : Nat。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.measurableSet_countableGeneratingSet`：measurableSet_coun
tableGeneratingSet [MeasurableSpace α] [CountablyGenerated α] {s : Set α} (hs : 
s in countableGeneratingSet α) : Measurabl…
· 使用引理 `Set.enumerateCountable_mem`：enumerateCountable_mem {s : Set α} (h : s.Co
untable) {default : α} (h_mem : default in s) (n : Nat) : enumerateCountable h d
efault n in s
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
· 使用引理 `MeasurableSpace.empty_mem_countableGeneratingSet`：empty_mem_countableGen
eratingSet [MeasurableSpace α] [CountablyGenerated α] : ∅ in countableGenerating
Set α
-/
lemma measurableSet_natGeneratingSequence [MeasurableSpace α] [CountablyGenerated α] (n : ℕ) :
    MeasurableSet (natGeneratingSequence α n) :=
  measurableSet_countableGeneratingSet <| Set.enumerateCountable_mem _
    empty_mem_countableGeneratingSet n
/-
**MeasurableSpace.CountablyGenerated.comap** 是 Mathlib 中的一个定理，位于命名空间 `Measurable
Space.CountablyGenerated`。
形式化陈述：∀ {α : Type u_1} {β : Type u_2} [m : MeasurableSpace β] [h : MeasurableSpa
ce.CountablyGenerated β] (f : α → β),   MeasurableSpace.CountablyGenerated α
参数：f : α → β。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSpace.comap_generateFrom`：comap_generateFrom {f : α -> β} {s :
 Set (Set β)} : (generateFrom s).comap f = generateFrom (preimage f '' s)
· 使用定理 `Set.Countable.image`：∀ {α : Type u} {β : Type v} {s : Set α}, s.Countabl
e → ∀ (f : α → β), (f '' s).Countable
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
-/
theorem CountablyGenerated.comap [m : MeasurableSpace β] [h : CountablyGenerated β] (f : α → β) :
    @CountablyGenerated α (.comap f m) := by
  rcases h with ⟨⟨b, hbc, rfl⟩⟩
  rw [comap_generateFrom]
  let := generateFrom (preimage f '' b)
  exact ⟨_, hbc.image _, rfl⟩
/-
**MeasurableSpace.CountablyGenerated.sup** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSp
ace.CountablyGenerated`。
形式化陈述：∀ {β : Type u_2} {m₁ m₂ : MeasurableSpace β},   MeasurableSpace.CountablyG
enerated β → MeasurableSpace.CountablyGenerated β → MeasurableSpace.CountablyGen
erated β
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.Countable.union`：∀ {α : Type u} {s t : Set α}, s.Countable → t.Count
able → (s ∪ t).Countable
· 使用定理 `MeasurableSpace.generateFrom_sup_generateFrom`：generateFrom_sup_generate
From {s t : Set (Set α)} : generateFrom s ⊔ generateFrom t = generateFrom (s uni
on t)
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
-/
theorem CountablyGenerated.sup {m₁ m₂ : MeasurableSpace β} (h₁ : @CountablyGenerated β m₁)
    (h₂ : @CountablyGenerated β m₂) : @CountablyGenerated β (m₁ ⊔ m₂) := by
  rcases h₁ with ⟨⟨b₁, hb₁c, rfl⟩⟩
  rcases h₂ with ⟨⟨b₂, hb₂c, rfl⟩⟩
  exact @mk _ (_ ⊔ _) ⟨_, hb₁c.union hb₂c, generateFrom_sup_generateFrom⟩

/-- Any measurable space structure on a countable space is countably generated. -/
/-
**MeasurableSpace.** 是 Mathlib 中的一个实例，位于命名空间 `MeasurableSpace`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Any measurable space structure on a countable space is countably generated.
-/
instance (priority := 100) [MeasurableSpace α] [Countable α] : CountablyGenerated α where
  isCountablyGenerated := by
    refine ⟨⋃ y, {measurableAtom y}, countable_iUnion (fun i ↦ countable_singleton _), ?_⟩
    refine le_antisymm ?_ (generateFrom_le (by simp [MeasurableSet.measurableAtom_of_countable]))
    intro s hs
    have : s = ⋃ y ∈ s, measurableAtom y := by
      apply Subset.antisymm
      · intro x hx
        simpa using ⟨x, hx, by simp⟩
      · simp only [iUnion_subset_iff]
        intro x hx
        exact measurableAtom_subset hs hx
    rw [this]
    apply MeasurableSet.biUnion (to_countable s) (fun x _hx ↦ ?_)
    apply measurableSet_generateFrom
    simp
/-
**MeasurableSpace.** 是 Mathlib 中的一个实例，位于命名空间 `MeasurableSpace`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [MeasurableSpace α] [CountablyGenerated α] {p : α → Prop} :
    CountablyGenerated { x // p x } := .comap _
/-
**MeasurableSpace.** 是 Mathlib 中的一个实例，位于命名空间 `MeasurableSpace`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [MeasurableSpace α] [CountablyGenerated α] [MeasurableSpace β] [CountablyGenerated β] :
    CountablyGenerated (α × β) :=
  .sup (.comap Prod.fst) (.comap Prod.snd)

section CountablyGeneratedAtom

variable {mα : MeasurableSpace α} [CountablyGenerated α]

open scoped Classical in
/-- The atoms in a countably generated measurable space.

Some of those sets may be empty, but the nonempty ones are the atoms of the measurable space.
See `measurableAtom_eq_countablyGeneratedAtom_natGeneratingSequence`. -/
-- Note: `Set` has no computational content, but Lean still attempts to compile it.
-- See https://github.com/leanprover/lean4/issues/14084.
noncomputable
/-
**MeasurableSpace.countablyGeneratedAtom** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSp
ace`。
形式化陈述：countablyGeneratedAtom (α : Type*) [MeasurableSpace α] [CountablyGenerated
 α] : (Nat -> Prop) -> Set α
参数：α : Type*。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
def countablyGeneratedAtom (α : Type*) [MeasurableSpace α] [CountablyGenerated α] :
    (ℕ → Prop) → Set α :=
  fun p ↦ ⋂ n, if p n then natGeneratingSequence α n else (natGeneratingSequence α n)ᶜ
/-
**MeasurableSpace.measurableSet_countablyGeneratedAtom** 是 Mathlib 中的一个引理，位于命名空间
 `MeasurableSpace`。
形式化陈述：measurableSet_countablyGeneratedAtom (p : Nat -> Prop) : MeasurableSet (co
untablyGeneratedAtom α p)
参数：p : Nat -> Prop。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSet.iInter`：MeasurableSet.iInter [Countable ι] {f : ι -> Set α
} (h : forall b, MeasurableSet (f b)) : MeasurableSet (⋂ b, f b)
· 使用定理 `instCountableNat`：Countable ℕ
· 使用定理 `MeasurableSet.ite'`：MeasurableSet.ite' {s t : Set α} {p : Prop} (hs : p 
-> MeasurableSet s) (ht : ¬p -> MeasurableSet t) : MeasurableSet (ite p s t)
· 使用引理 `MeasurableSpace.measurableSet_natGeneratingSequence`：measurableSet_natGe
neratingSequence [MeasurableSpace α] [CountablyGenerated α] (n : Nat) : Measurab
leSet (natGeneratingSequence α n)
· 使用定理 `MeasurableSet.compl`：∀ {α : Type u_1} {s : Set α} {m : MeasurableSpace α
}, MeasurableSet s → MeasurableSet sᶜ
-/
lemma measurableSet_countablyGeneratedAtom (p : ℕ → Prop) :
    MeasurableSet (countablyGeneratedAtom α p) := by
  refine MeasurableSet.iInter fun n ↦ ?_
  exact MeasurableSet.ite' (fun _ ↦ measurableSet_natGeneratingSequence n)
    (fun _ ↦ (measurableSet_natGeneratingSequence n).compl)
/-
**MeasurableSpace.disjoint_countablyGeneratedAtom** 是 Mathlib 中的一个引理，位于命名空间 `Mea
surableSpace`。
形式化陈述：disjoint_countablyGeneratedAtom : Pairwise (Function.onFun Disjoint (count
ablyGeneratedAtom α))
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `iff_false`：∀ (p : Prop), (p ↔ False) = ¬p
-/
lemma disjoint_countablyGeneratedAtom :
    Pairwise (Function.onFun Disjoint (countablyGeneratedAtom α)) := by
  intro p q hpq s hsp hsq
  simp only [bot_eq_empty, subset_empty_iff] at hsp hsq ⊢
  ext x
  simp only [mem_empty_iff_false, iff_false]
  intro hxs
  obtain ⟨n, hn⟩ : ∃ n, p n ≠ q n := by grind
  specialize hsp hxs
  specialize hsq hxs
  simp only [countablyGeneratedAtom, mem_iInter] at hsp hsq
  grind
/-
**MeasurableSpace.iUnion_countablyGeneratedAtom** 是 Mathlib 中的一个引理，位于命名空间 `Measu
rableSpace`。
形式化陈述：iUnion_countablyGeneratedAtom : ⋃ p, countablyGeneratedAtom α p = univ
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `iff_true`：∀ (p : Prop), (p ↔ True) = p
-/
lemma iUnion_countablyGeneratedAtom : ⋃ p, countablyGeneratedAtom α p = univ := by
  ext x
  simp only [countablyGeneratedAtom, mem_iUnion, mem_iInter, mem_univ, iff_true]
  exact ⟨fun n ↦ x ∈ natGeneratingSequence α n, by grind⟩
/-
**MeasurableSpace.mem_countablyGeneratedAtom_natGeneratingSequence** 是 Mathlib 中
的一个引理，位于命名空间 `MeasurableSpace`。
形式化陈述：mem_countablyGeneratedAtom_natGeneratingSequence (x : α) : x in countablyG
eneratedAtom α (x in natGeneratingSequence α ·)
参数：x : α。
该定理/引理描述了相关对象所满足的性质。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma mem_countablyGeneratedAtom_natGeneratingSequence (x : α) :
    x ∈ countablyGeneratedAtom α (x ∈ natGeneratingSequence α ·) := by
  simp [countablyGeneratedAtom]; grind

open scoped Classical in
/-- Any measurable set in a countably generated measurable space can be expressed as a union of
atoms. -/
/-
**MeasurableSpace.exists_eq_iUnion_countablyGeneratedAtom** 是 Mathlib 中的一个引理，位于命
名空间 `MeasurableSpace`。
形式化陈述：exists_eq_iUnion_countablyGeneratedAtom {s : Set α} (hs : MeasurableSet s)
 : exists (q : (Nat -> Prop) -> Prop), s = ⋃ p, if q p then countablyGeneratedAt
om α p else ∅
参数：hs : MeasurableSet s。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.generateFrom_induction`：generateFrom_induction (C : Set 
(Set α)) (p : forall s : Set α, MeasurableSet[generateFrom C] s -> Prop) (hC : f
orall t in C, forall ht, p t…
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `ite_cond_eq_true`：∀ {α : Sort u} {c : Prop} {x : Decidable c} (a b : α),
 c = True → (if c then a else b) = a
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `ite_cond_eq_false`：∀ {α : Sort u} {c : Prop} {x : Decidable c} (a b : α)
, c = False → (if c then a else b) = b
· 使用定理 `Set.iUnion_empty`：iUnion_empty : (⋃ _ : ι, ∅ : Set α) = ∅
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `Set.compl_iUnion`：compl_iUnion (s : ι -> Set β) : (⋃ i, s i)ᶜ = ⋂ i, (s 
i)ᶜ
· 使用引理 `Mathlib.Tactic.Contrapose.contrapose₃`：contrapose₃ {p q : Prop} : (q -> 
¬ p) -> (p -> ¬ q)
· 使用定理 `Mathlib.Tactic.Push.not_forall_eq`：not_forall_eq : (¬ forall x, s x) = (
exists x, ¬ s x)
· 使用引理 `MeasurableSpace.mem_countablyGeneratedAtom_natGeneratingSequence`：mem_co
untablyGeneratedAtom_natGeneratingSequence (x : α) : x in countablyGeneratedAtom
 α (x in natGeneratingSequence α ·)
· 使用引理 `MeasurableSpace.disjoint_countablyGeneratedAtom`：disjoint_countablyGener
atedAtom : Pairwise (Function.onFun Disjoint (countablyGeneratedAtom α))
· 使用定理 `ite_congr`：∀ {α : Sort u_1} {b c : Prop} {x y u v : α} {s : Decidable b}
 [inst : Decidable c],   b = c → (c → x = u) → (¬c → y = v) → (if b then x else…
· 使用定理 `iSup_Prop_eq`：iSup_Prop_eq {p : ι -> Prop} : ⨆ i, p i = exists i, p i
· 使用定理 `Classical.choose_spec`：∀ {α : Sort u} {p : α → Prop} (h : ∃ x, p x), p (
Classical.choose h)
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `MeasurableSpace.generateFrom_natGeneratingSequence`：generateFrom_natGene
ratingSequence (α : Type*) [m : MeasurableSpace α] [CountablyGenerated α] : gene
rateFrom (range (natGeneratingSequence _…

--- 原说明 ---
Any measurable set in a countably generated measurable space can be expressed as
 a union of
atoms.
-/
lemma exists_eq_iUnion_countablyGeneratedAtom {s : Set α} (hs : MeasurableSet s) :
    ∃ (q : (ℕ → Prop) → Prop), s = ⋃ p, if q p then countablyGeneratedAtom α p else ∅ := by
  rw [← generateFrom_natGeneratingSequence α] at hs
  refine generateFrom_induction (range (natGeneratingSequence α)) _ ?_ ?_ ?_ ?_ s hs
  · simp only [mem_range, forall_exists_index, forall_apply_eq_imp_iff]
    refine fun n _ ↦ ⟨fun p ↦ p n, ?_⟩
    ext x
    simp only [mem_iUnion, mem_ite_empty_right]
    refine ⟨fun hx ↦ ?_, ?_⟩
    · exact ⟨(x ∈ natGeneratingSequence α ·), by simp [countablyGeneratedAtom]; grind⟩
    · rintro ⟨p, hpn, hpx⟩
      simp only [countablyGeneratedAtom, mem_iInter] at hpx
      specialize hpx n
      simpa [hpn] using hpx
  · exact ⟨fun _ ↦ False, by simp⟩
  · simp only [forall_exists_index]
    intro t ht q htq
    refine ⟨fun p ↦ ¬ q p, ?_⟩
    ext x
    simp only [htq, compl_iUnion, mem_iInter, mem_compl_iff, mem_ite_empty_right, not_and,
      mem_iUnion]
    refine ⟨fun h ↦ ?_, ?_⟩
    · refine ⟨fun n ↦ x ∈ natGeneratingSequence α n, ?_,
        mem_countablyGeneratedAtom_natGeneratingSequence x⟩
      contrapose! h
      refine ⟨fun n ↦ x ∈ natGeneratingSequence α n, h,
        mem_countablyGeneratedAtom_natGeneratingSequence x⟩
    · rintro ⟨p, hpq, hpx⟩ p' hqp' hx_mem
      have hpp' : p ≠ p' := by grind
      have h_disj : Disjoint (countablyGeneratedAtom α p) (countablyGeneratedAtom α p') :=
        disjoint_countablyGeneratedAtom hpp'
      grind
  · intro t ht h
    choose q hq using h
    refine ⟨fun p ↦ ⨆ n, q n p, ?_⟩
    ext
    simp [hq]
    grind

/-- The measurable atom of a point in a countably generated measurable space is given by
a `countablyGeneratedAtom`. -/
/-
**MeasurableSpace.measurableAtom_eq_countablyGeneratedAtom_natGeneratingSequence
** 是 Mathlib 中的一个引理，位于命名空间 `MeasurableSpace`。
形式化陈述：measurableAtom_eq_countablyGeneratedAtom_natGeneratingSequence (x : α) : m
easurableAtom x = countablyGeneratedAtom α (x in natGeneratingSequence α ·)
参数：x : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.measurableSet_natGeneratingSequence`：measurableSet_natGe
neratingSequence [MeasurableSpace α] [CountablyGenerated α] (n : Nat) : Measurab
leSet (natGeneratingSequence α n)
· 使用引理 `MeasurableSpace.generateFrom_natGeneratingSequence`：generateFrom_natGene
ratingSequence (α : Type*) [m : MeasurableSpace α] [CountablyGenerated α] : gene
rateFrom (range (natGeneratingSequence _…
· 使用定理 `subset_antisymm`：∀ {α : Type u_1} [UsesSetNotationForOrder α] [inst : Pa
rtialOrder α] {a b : α}, a ⊆ b → b ⊆ a → a = b
· 使用引理 `measurableAtom_subset`：measurableAtom_subset {s : Set β} {x : β} (hs : M
easurableSet s) (hx : x in s) : measurableAtom x subseteq s
· 使用引理 `MeasurableSpace.measurableSet_countablyGeneratedAtom`：measurableSet_coun
tablyGeneratedAtom (p : Nat -> Prop) : MeasurableSet (countablyGeneratedAtom α p
)
· 使用引理 `MeasurableSpace.mem_countablyGeneratedAtom_natGeneratingSequence`：mem_co
untablyGeneratedAtom_natGeneratingSequence (x : α) : x in countablyGeneratedAtom
 α (x in natGeneratingSequence α ·)
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用引理 `MeasurableSpace.exists_eq_iUnion_countablyGeneratedAtom`：exists_eq_iUnio
n_countablyGeneratedAtom {s : Set α} (hs : MeasurableSet s) : exists (q : (Nat -
> Prop) -> Prop), s = ⋃ p, if q p then counta…
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Decidable.byContradiction`：∀ {p : Prop} [dec : Decidable p], (¬p → False
) → p
· 使用引理 `MeasurableSpace.disjoint_countablyGeneratedAtom`：disjoint_countablyGener
atedAtom : Pairwise (Function.onFun Disjoint (countablyGeneratedAtom α))
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `subset_trans`：∀ {α : Type u_1} [UsesSetNotationForOrder α] [inst : Preor
der α] {a b c : α}, a ⊆ b → b ⊆ c → a ⊆ c
· 使用定理 `le_of_eq`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a = b → a ≤ b
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `ite_cond_eq_true`：∀ {α : Sort u} {c : Prop} {x : Decidable c} (a b : α),
 c = True → (if c then a else b) = a
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Set.subset_iUnion`：subset_iUnion : forall (s : ι -> Set β) (i : ι), s i 
subseteq ⋃ i, s i

--- 原说明 ---
The measurable atom of a point in a countably generated measurable space is give
n by
a `countablyGeneratedAtom`.
-/
lemma measurableAtom_eq_countablyGeneratedAtom_natGeneratingSequence (x : α) :
    measurableAtom x = countablyGeneratedAtom α (x ∈ natGeneratingSequence α ·) := by
  let A := natGeneratingSequence α
  have hA : ∀ n, MeasurableSet (A n) := measurableSet_natGeneratingSequence
  have hA_gen : generateFrom (range A) = mα := generateFrom_natGeneratingSequence α
  change measurableAtom x = countablyGeneratedAtom α (x ∈ A ·)
  refine subset_antisymm (measurableAtom_subset ?_ ?_) ?_
  · exact measurableSet_countablyGeneratedAtom _
  · exact mem_countablyGeneratedAtom_natGeneratingSequence x
  classical
  unfold measurableAtom
  simp only [subset_iInter_iff]
  intro s hxs hs
  obtain ⟨q, hq⟩ : ∃ (q : (ℕ → Prop) → Prop), s =
      ⋃ p, if q p then countablyGeneratedAtom α p else ∅ :=
    exists_eq_iUnion_countablyGeneratedAtom hs
  suffices q (x ∈ A ·) by
    rw [hq]
    refine subset_trans (le_of_eq ?_) (subset_iUnion _ (x ∈ A ·))
    simp [this]
  simp only [hq, mem_iUnion, mem_ite_empty_right] at hxs
  obtain ⟨p, hpq, hpx⟩ := hxs
  suffices p = (x ∈ A ·) by rwa [← this]
  by_contra! hp
  have h_disj : Disjoint (countablyGeneratedAtom α p) (countablyGeneratedAtom α (x ∈ A ·)) :=
    disjoint_countablyGeneratedAtom hp
  have hx_mem : x ∈ countablyGeneratedAtom α (x ∈ A ·) :=
    mem_countablyGeneratedAtom_natGeneratingSequence x
  grind

/-- In a countably generated measurable space, the atoms are measurable. -/
/-
**MeasurableSpace.measurableSet_measurableAtom** 是 Mathlib 中的一个引理，位于命名空间 `Measur
ableSpace`。
形式化陈述：measurableSet_measurableAtom (x : α) : MeasurableSet (measurableAtom x)
参数：x : α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `MeasurableSpace.measurableAtom_eq_countablyGeneratedAtom_natGeneratingSe
quence`：measurableAtom_eq_countablyGeneratedAtom_natGeneratingSequence (x : α) :
 measurableAtom x = countablyGeneratedAtom α (x in natGeneratingSequ…
· 使用引理 `MeasurableSpace.measurableSet_countablyGeneratedAtom`：measurableSet_coun
tablyGeneratedAtom (p : Nat -> Prop) : MeasurableSet (countablyGeneratedAtom α p
)

--- 原说明 ---
In a countably generated measurable space, the atoms are measurable.
-/
lemma measurableSet_measurableAtom (x : α) : MeasurableSet (measurableAtom x) := by
  rw [measurableAtom_eq_countablyGeneratedAtom_natGeneratingSequence]
  exact measurableSet_countablyGeneratedAtom _

end CountablyGeneratedAtom

section SeparatesPoints

/-- We say that a measurable space separates points if for any two distinct points,
there is a measurable set containing one but not the other. -/
/-
**MeasurableSpace.SeparatesPoints** 是 Mathlib 中的一个归纳类型，位于命名空间 `MeasurableSpace`。
形式化陈述：(α : Type u_3) → [m : MeasurableSpace α] → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
We say that a measurable space separates points if for any two distinct points,
there is a measurable set containing one but not the other.
-/
class SeparatesPoints (α : Type*) [m : MeasurableSpace α] : Prop where
  separates : ∀ x y : α, (∀ s, MeasurableSet s → (x ∈ s → y ∈ s)) → x = y
/-
**MeasurableSpace.separatesPoints_def** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSpace
`。
形式化陈述：separatesPoints_def [MeasurableSpace α] [hs : SeparatesPoints α] {x y : α}
 (h : forall s, MeasurableSet s -> (x in s -> y in s)) : x = y
参数：h : forall s, MeasurableSet s -> (x in s -> y in s)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.SeparatesPoints.separates`：∀ {α : Type u_3} {m : Measura
bleSpace α} [self : MeasurableSpace.SeparatesPoints α] (x y : α),   (∀ (s : Set 
α), MeasurableSet s → x ∈ s → y…
-/
theorem separatesPoints_def [MeasurableSpace α] [hs : SeparatesPoints α] {x y : α}
    (h : ∀ s, MeasurableSet s → (x ∈ s → y ∈ s)) : x = y := hs.separates _ _ h
/-
**MeasurableSpace.exists_measurableSet_of_ne** 是 Mathlib 中的一个定理，位于命名空间 `Measurab
leSpace`。
形式化陈述：exists_measurableSet_of_ne [MeasurableSpace α] [SeparatesPoints α] {x y : 
α} (h : x != y) : exists s, MeasurableSet s ∧ x in s ∧ y ∉ s
参数：h : x != y。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `Mathlib.Tactic.Contrapose.contrapose₂`：contrapose₂ {p q : Prop} : (¬ q -
> p) -> (¬ p -> q)
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `Mathlib.Tactic.Push.not_and_eq`：not_and_eq : (¬ (p ∧ q)) = (p -> ¬ q)
· 使用定理 `MeasurableSpace.separatesPoints_def`：separatesPoints_def [MeasurableSpac
e α] [hs : SeparatesPoints α] {x y : α} (h : forall s, MeasurableSet s -> (x in 
s -> y in s)) : x = y
-/
theorem exists_measurableSet_of_ne [MeasurableSpace α] [SeparatesPoints α] {x y : α}
    (h : x ≠ y) : ∃ s, MeasurableSet s ∧ x ∈ s ∧ y ∉ s := by
  contrapose! h
  exact separatesPoints_def h
/-
**MeasurableSpace.separatesPoints_iff** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSpace
`。
形式化陈述：separatesPoints_iff [MeasurableSpace α] : SeparatesPoints α ↔ forall x y :
 α, (forall s, MeasurableSet s -> (x in s ↔ y in s)) -> x = y
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.SeparatesPoints.separates`：∀ {α : Type u_3} {m : Measura
bleSpace α} [self : MeasurableSpace.SeparatesPoints α] (x y : α),   (∀ (s : Set 
α), MeasurableSet s → x ∈ s → y…
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `not_imp_not`：not_imp_not : ¬a -> ¬b ↔ b -> a
· 使用定理 `MeasurableSet.compl`：∀ {α : Type u_1} {s : Set α} {m : MeasurableSpace α
}, MeasurableSet s → MeasurableSet sᶜ
-/
theorem separatesPoints_iff [MeasurableSpace α] : SeparatesPoints α ↔
    ∀ x y : α, (∀ s, MeasurableSet s → (x ∈ s ↔ y ∈ s)) → x = y :=
  ⟨fun h ↦ fun _ _ hxy ↦ h.separates _ _ fun _ hs xs ↦ (hxy _ hs).mp xs,
    fun h ↦ ⟨fun _ _ hxy ↦ h _ _ fun _ hs ↦
    ⟨fun xs ↦ hxy _ hs xs, not_imp_not.mp fun xs ↦ hxy _ hs.compl xs⟩⟩⟩

/-- If the measurable space generated by `S` separates points,
then this is witnessed by sets in `S`. -/
/-
**MeasurableSpace.separating_of_generateFrom** 是 Mathlib 中的一个定理，位于命名空间 `Measurab
leSpace`。
形式化陈述：separating_of_generateFrom (S : Set (Set α)) [h : @SeparatesPoints α (gene
rateFrom S)] : forall x y : α, (forall s in S, x in s ↔ y in s) -> x = y
参数：S : Set (Set α)；generateFrom S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.separatesPoints_def`：separatesPoints_def [MeasurableSpac
e α] [hs : SeparatesPoints α] {x y : α} (h : forall s, MeasurableSet s -> (x in 
s -> y in s)) : x = y
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MeasurableSpace.forall_generateFrom_mem_iff_mem_iff`：forall_generateFrom
_mem_iff_mem_iff {S : Set (Set α)} {x y : α} : (forall s, MeasurableSet[generate
From S] s -> (x in s ↔ y in s)) ↔ (forall…

--- 原说明 ---
If the measurable space generated by `S` separates points,
then this is witnessed by sets in `S`.
-/
theorem separating_of_generateFrom (S : Set (Set α))
    [h : @SeparatesPoints α (generateFrom S)] :
    ∀ x y : α, (∀ s ∈ S, x ∈ s ↔ y ∈ s) → x = y := by
  let := generateFrom S
  intro x y hxy
  rw [← forall_generateFrom_mem_iff_mem_iff] at hxy
  exact separatesPoints_def <| fun _ hs ↦ (hxy _ hs).mp
/-
**MeasurableSpace.SeparatesPoints.mono** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSpac
e.SeparatesPoints`。
形式化陈述：∀ {α : Type u_1} {m m' : MeasurableSpace α} [hsep : MeasurableSpace.Separa
tesPoints α],   m ≤ m' → MeasurableSpace.SeparatesPoints α
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.SeparatesPoints.separates`：∀ {α : Type u_3} {m : Measura
bleSpace α} [self : MeasurableSpace.SeparatesPoints α] (x y : α),   (∀ (s : Set 
α), MeasurableSet s → x ∈ s → y…
-/
theorem SeparatesPoints.mono {m m' : MeasurableSpace α} [hsep : @SeparatesPoints _ m] (h : m ≤ m') :
    @SeparatesPoints _ m' := @SeparatesPoints.mk _ m' fun _ _ hxy ↦
    @SeparatesPoints.separates _ m hsep _ _ fun _ hs ↦ hxy _ (h _ hs)
/-
**MeasurableSpace._root_.eq_const_of_measurable_bot** 是 Mathlib 中的一个定理，位于命名空间 `M
easurableSpace`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem _root_.eq_const_of_measurable_bot [MeasurableSpace β] [Nonempty β]
    [SeparatesPoints β] {f : α → β} (hf : Measurable[⊥] f) :
    ∃ c, f = fun _ ↦ c := by
  have h (a₁ : α) (a₂ : α) : f a₁ = f a₂ := by
    by_contra! h
    obtain ⟨s, hs, hx, hy⟩ := exists_measurableSet_of_ne h
    obtain h' | h' := MeasurableSpace.measurableSet_bot_iff.mp (hf hs)
    · absurd hx
      simp [← mem_preimage, h']
    · absurd hy
      simp [← mem_preimage, h']
  obtain h' | h' := isEmpty_or_nonempty α
  · use (Classical.ofNonempty : β), funext (by simp)
  · use f (Classical.ofNonempty : α), funext (fun x ↦ h _ _)

/-- We say that a measurable space is countably separated if there is a
countable sequence of measurable sets separating points. -/
/-
**MeasurableSpace.CountablySeparated** 是 Mathlib 中的一个归纳类型，位于命名空间 `MeasurableSpac
e`。
形式化陈述：(α : Type u_3) → [MeasurableSpace α] → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
We say that a measurable space is countably separated if there is a
countable sequence of measurable sets separating points.
-/
class CountablySeparated (α : Type*) [MeasurableSpace α] : Prop where
  countably_separated : HasCountableSeparatingOn α MeasurableSet univ
/-
**MeasurableSpace.countablySeparated_of_hasCountableSeparatingOn** 是 Mathlib 中的一
个实例，位于命名空间 `MeasurableSpace`。
形式化陈述：countablySeparated_of_hasCountableSeparatingOn [MeasurableSpace α] [h : Ha
sCountableSeparatingOn α MeasurableSet univ] : CountablySeparated α
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance countablySeparated_of_hasCountableSeparatingOn [MeasurableSpace α]
    [h : HasCountableSeparatingOn α MeasurableSet univ] : CountablySeparated α := ⟨h⟩
/-
**MeasurableSpace.hasCountableSeparatingOn_of_countablySeparated** 是 Mathlib 中的一
个实例，位于命名空间 `MeasurableSpace`。
形式化陈述：hasCountableSeparatingOn_of_countablySeparated [MeasurableSpace α] [h : Co
untablySeparated α] : HasCountableSeparatingOn α MeasurableSet univ
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.CountablySeparated.countably_separated`：∀ {α : Type u_3}
 {inst : MeasurableSpace α} [self : MeasurableSpace.CountablySeparated α],   Has
CountableSeparatingOn α MeasurableSet Set.un…
-/
instance hasCountableSeparatingOn_of_countablySeparated [MeasurableSpace α]
    [h : CountablySeparated α] : HasCountableSeparatingOn α MeasurableSet univ :=
  h.countably_separated
/-
**MeasurableSpace.countablySeparated_def** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSp
ace`。
形式化陈述：countablySeparated_def [MeasurableSpace α] : CountablySeparated α ↔ HasCou
ntableSeparatingOn α MeasurableSet univ
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.CountablySeparated.countably_separated`：∀ {α : Type u_3}
 {inst : MeasurableSpace α} [self : MeasurableSpace.CountablySeparated α],   Has
CountableSeparatingOn α MeasurableSet Set.un…
-/
theorem countablySeparated_def [MeasurableSpace α] :
    CountablySeparated α ↔ HasCountableSeparatingOn α MeasurableSet univ :=
  ⟨fun h ↦ h.countably_separated, fun h ↦ ⟨h⟩⟩
/-
**MeasurableSpace.CountablySeparated.mono** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableS
pace.CountablySeparated`。
形式化陈述：∀ {α : Type u_1} {m m' : MeasurableSpace α} [hsep : MeasurableSpace.Counta
blySeparated α],   m ≤ m' → MeasurableSpace.CountablySeparated α
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem CountablySeparated.mono {m m' : MeasurableSpace α} [hsep : @CountablySeparated _ m]
    (h : m ≤ m') : @CountablySeparated _ m' := by
  simp_rw [countablySeparated_def] at *
  rcases hsep with ⟨S, Sct, Smeas, hS⟩
  use S, Sct, (fun s hs ↦ h _ <| Smeas _ hs), hS
/-
**MeasurableSpace.CountablySeparated.subtype_iff** 是 Mathlib 中的一个定理，位于命名空间 `Meas
urableSpace.CountablySeparated`。
形式化陈述：∀ {α : Type u_1} [inst : MeasurableSpace α] {s : Set α},   MeasurableSpace
.CountablySeparated ↑s ↔ HasCountableSeparatingOn α MeasurableSet s
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSpace.countablySeparated_def`：countablySeparated_def [Measurab
leSpace α] : CountablySeparated α ↔ HasCountableSeparatingOn α MeasurableSet uni
v
· 使用定理 `HasCountableSeparatingOn.subtype_iff`：HasCountableSeparatingOn.subtype_i
ff {α : Type*} {p : Set α -> Prop} {t : Set α} : HasCountableSeparatingOn t (fun
 u => exists v, p v ∧ (↑) …
-/
theorem CountablySeparated.subtype_iff [MeasurableSpace α] {s : Set α} :
    CountablySeparated s ↔ HasCountableSeparatingOn α MeasurableSet s := by
  rw [countablySeparated_def]
  exact HasCountableSeparatingOn.subtype_iff
/-
**MeasurableSpace.** 是 Mathlib 中的一个实例，位于命名空间 `MeasurableSpace`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) Subtype.separatesPoints [MeasurableSpace α] [h : SeparatesPoints α]
    {s : Set α} : SeparatesPoints s :=
  ⟨fun _ _ hxy ↦ Subtype.val_injective <| h.1 _ _ fun _ ht ↦ hxy _ <| measurable_subtype_coe ht⟩
/-
**MeasurableSpace.** 是 Mathlib 中的一个实例，位于命名空间 `MeasurableSpace`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) Subtype.countablySeparated [MeasurableSpace α]
    [h : CountablySeparated α] {s : Set α} : CountablySeparated s := by
  rw [CountablySeparated.subtype_iff]
  exact h.countably_separated.mono (fun s ↦ id) <| subset_univ _
/-
**MeasurableSpace.** 是 Mathlib 中的一个实例，位于命名空间 `MeasurableSpace`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) separatesPoints_of_measurableSingletonClass [MeasurableSpace α]
    [MeasurableSingletonClass α] : SeparatesPoints α := by
  refine ⟨fun x y h ↦ ?_⟩
  specialize h _ (MeasurableSet.singleton x)
  simp_rw [mem_singleton_iff, forall_true_left] at h
  exact h.symm
/-
**MeasurableSpace.** 是 Mathlib 中的一个实例，位于命名空间 `MeasurableSpace`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 50) MeasurableSingletonClass.of_separatesPoints [MeasurableSpace α]
    [Countable α] [SeparatesPoints α] : MeasurableSingletonClass α where
  measurableSet_singleton x := by
    choose s hsm hxs hys using fun y (h : x ≠ y) ↦ exists_measurableSet_of_ne h
    convert! MeasurableSet.iInter fun y ↦ .iInter fun h ↦ hsm y h
    ext y
    rcases eq_or_ne x y with rfl | h
    · simpa
    · simp only [mem_singleton_iff, h.symm, false_iff, mem_iInter, not_forall]
      exact ⟨y, h, hys y h⟩
/-
**MeasurableSpace.hasCountableSeparatingOn_of_countablySeparated_subtype** 是 Mat
hlib 中的一个实例，位于命名空间 `MeasurableSpace`。
形式化陈述：hasCountableSeparatingOn_of_countablySeparated_subtype [MeasurableSpace α]
 {s : Set α} [h : CountablySeparated s] : HasCountableSeparatingOn _ MeasurableS
et s
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `MeasurableSpace.CountablySeparated.subtype_iff`：∀ {α : Type u_1} [inst :
 MeasurableSpace α] {s : Set α},   MeasurableSpace.CountablySeparated ↑s ↔ HasCo
untableSeparatingOn α MeasurableSet …
-/
instance hasCountableSeparatingOn_of_countablySeparated_subtype
    [MeasurableSpace α] {s : Set α} [h : CountablySeparated s] :
    HasCountableSeparatingOn _ MeasurableSet s := CountablySeparated.subtype_iff.mp h
/-
**MeasurableSpace.countablySeparated_subtype_of_hasCountableSeparatingOn** 是 Mat
hlib 中的一个实例，位于命名空间 `MeasurableSpace`。
形式化陈述：countablySeparated_subtype_of_hasCountableSeparatingOn [MeasurableSpace α]
 {s : Set α} [h : HasCountableSeparatingOn _ MeasurableSet s] : CountablySeparat
ed s
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `MeasurableSpace.CountablySeparated.subtype_iff`：∀ {α : Type u_1} [inst :
 MeasurableSpace α] {s : Set α},   MeasurableSpace.CountablySeparated ↑s ↔ HasCo
untableSeparatingOn α MeasurableSet …
-/
instance countablySeparated_subtype_of_hasCountableSeparatingOn
    [MeasurableSpace α] {s : Set α} [h : HasCountableSeparatingOn _ MeasurableSet s] :
    CountablySeparated s := CountablySeparated.subtype_iff.mpr h
/-
**MeasurableSpace.countablySeparated_of_separatesPoints** 是 Mathlib 中的一个实例，位于命名空
间 `MeasurableSpace`。
形式化陈述：countablySeparated_of_separatesPoints [MeasurableSpace α] [h : CountablyGe
nerated α] [SeparatesPoints α] : CountablySeparated α
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `pi_congr`：∀ {α : Sort u} {β β' : α → Sort v}, (∀ (a : α), β a = β' a) → 
((a : α) → β a) = ((a : α) → β' a)
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `instNonemptyOfInhabited`：∀ {α : Sort u} [Inhabited α], Nonempty α
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `MeasurableSpace.separating_of_generateFrom`：separating_of_generateFrom (
S : Set (Set α)) [h : @SeparatesPoints α (generateFrom S)] : forall x y : α, (fo
rall s in S, x in s ↔ y in s) ->…
-/
instance countablySeparated_of_separatesPoints [MeasurableSpace α]
    [h : CountablyGenerated α] [SeparatesPoints α] : CountablySeparated α := by
  rcases h with ⟨b, hbc, hb⟩
  refine ⟨⟨b, hbc, fun t ht ↦ hb.symm ▸ .basic t ht, ?_⟩⟩
  rw [hb] at ‹SeparatesPoints _›
  convert! separating_of_generateFrom b
  simp

variable (α)

/-- If a measurable space admits a countable separating family of measurable sets,
there is a countably generated coarser space which still separates points. -/
/-
**MeasurableSpace.exists_countablyGenerated_le_of_countablySeparated** 是 Mathlib
 中的一个定理，位于命名空间 `MeasurableSpace`。
形式化陈述：exists_countablyGenerated_le_of_countablySeparated [m : MeasurableSpace α]
 [h : CountablySeparated α] : exists m' : MeasurableSpace α, @CountablyGenerated
 _ m' ∧ @SeparatesPoints _ m' ∧ m' <= m
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSpace.separatesPoints_iff`：separatesPoints_iff [MeasurableSpac
e α] : SeparatesPoints α ↔ forall x y : α, (forall s, MeasurableSet s -> (x in s
 ↔ y in s)) -> x = y
· 使用定理 `trivial`：True
· 使用定理 `MeasurableSpace.measurableSet_generateFrom`：measurableSet_generateFrom {
s : Set (Set α)} {t : Set α} (ht : t in s) : MeasurableSet[generateFrom s] t
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m

--- 原说明 ---
If a measurable space admits a countable separating family of measurable sets,
there is a countably generated coarser space which still separates points.
-/
theorem exists_countablyGenerated_le_of_countablySeparated [m : MeasurableSpace α]
    [h : CountablySeparated α] :
    ∃ m' : MeasurableSpace α, @CountablyGenerated _ m' ∧ @SeparatesPoints _ m' ∧ m' ≤ m := by
  rcases h with ⟨b, bct, hbm, hb⟩
  refine ⟨generateFrom b, ?_, ?_, generateFrom_le hbm⟩
  · use b
  rw [@separatesPoints_iff]
  exact fun x y hxy ↦ hb _ trivial _ trivial fun _ hs ↦ hxy _ <| measurableSet_generateFrom hs

open Function

open scoped Classical in
/-- A map from a measurable space to the Cantor space `ℕ → Bool` induced by a countable
sequence of sets generating the measurable space. -/
noncomputable
/-
**MeasurableSpace.mapNatBool** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSpace`。
形式化陈述：mapNatBool [MeasurableSpace α] [CountablyGenerated α] (x : α) (n : Nat) : 
Bool
参数：x : α；n : Nat。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
def mapNatBool [MeasurableSpace α] [CountablyGenerated α] (x : α) (n : ℕ) :
    Bool := x ∈ natGeneratingSequence α n
/-
**MeasurableSpace.measurable_mapNatBool** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSpa
ce`。
形式化陈述：measurable_mapNatBool [MeasurableSpace α] [CountablyGenerated α] : Measura
ble (mapNatBool α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `measurable_pi_iff`：measurable_pi_iff {g : α -> forall a, X a} : Measurab
le g ↔ forall a, Measurable fun x => g x a
· 使用定理 `measurable_to_bool`：measurable_to_bool {f : α -> Bool} (h : MeasurableSe
t (f ⁻¹' {true})) : Measurable f
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用引理 `MeasurableSpace.measurableSet_natGeneratingSequence`：measurableSet_natGe
neratingSequence [MeasurableSpace α] [CountablyGenerated α] (n : Nat) : Measurab
leSet (natGeneratingSequence α n)
-/
theorem measurable_mapNatBool [MeasurableSpace α] [CountablyGenerated α] :
    Measurable (mapNatBool α) := by
  rw [measurable_pi_iff]
  refine fun n ↦ measurable_to_bool ?_
  simp only [preimage, mem_singleton_iff, mapNatBool,
    Bool.decide_iff, ofPred_mem_eq]
  apply measurableSet_natGeneratingSequence
/-
**MeasurableSpace.injective_mapNatBool** 是 Mathlib 中的一个定理，位于命名空间 `MeasurableSpac
e`。
形式化陈述：injective_mapNatBool [MeasurableSpace α] [CountablyGenerated α] [Separates
Points α] : Injective (mapNatBool α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.separating_of_generateFrom`：separating_of_generateFrom (
S : Set (Set α)) [h : @SeparatesPoints α (generateFrom S)] : forall x y : α, (fo
rall s in S, x in s ↔ y in s) ->…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `MeasurableSpace.generateFrom_natGeneratingSequence`：generateFrom_natGene
ratingSequence (α : Type*) [m : MeasurableSpace α] [CountablyGenerated α] : gene
rateFrom (range (natGeneratingSequence _…
· 使用定理 `decide_eq_decide`：∀ {p q : Prop} {x : Decidable p} {x_1 : Decidable q}, 
decide p = decide q ↔ (p ↔ q)
· 使用定理 `congr_fun`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, f = g 
→ ∀ (a : α), f a = g a
-/
theorem injective_mapNatBool [MeasurableSpace α] [CountablyGenerated α]
    [SeparatesPoints α] : Injective (mapNatBool α) := by
  intro x y hxy
  rw [← generateFrom_natGeneratingSequence α] at *
  apply separating_of_generateFrom (range (natGeneratingSequence _))
  rintro - ⟨n, rfl⟩
  rw [← decide_eq_decide]
  exact congr_fun hxy n

/-- If a measurable space is countably generated and separates points, it is measure equivalent
to some subset of the Cantor space `ℕ → Bool` (equipped with the product sigma algebra).
Note: `s` need not be measurable, so this map need not be a `MeasurableEmbedding` to
the Cantor Space. -/
/-
**MeasurableSpace.measurableEquiv_nat_bool_of_countablyGenerated** 是 Mathlib 中的一
个定理，位于命名空间 `MeasurableSpace`。
形式化陈述：measurableEquiv_nat_bool_of_countablyGenerated [MeasurableSpace α] [Counta
blyGenerated α] [SeparatesPoints α] : exists s : Set (Nat -> Bool), Nonempty (α 
≃ᵐ s)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.injective_mapNatBool`：injective_mapNatBool [MeasurableSp
ace α] [CountablyGenerated α] [SeparatesPoints α] : Injective (mapNatBool α)
· 使用定理 `Measurable.subtype_mk`：Measurable.subtype_mk {p : β -> Prop} {f : α -> β
} (hf : Measurable f) {h : forall x, p (f x)} : Measurable fun x => (⟨f x, h x⟩ 
: Subtype p…
· 使用定理 `MeasurableSpace.measurable_mapNatBool`：measurable_mapNatBool [Measurable
Space α] [CountablyGenerated α] : Measurable (mapNatBool α)
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `MeasurableSpace.generateFrom_natGeneratingSequence`：generateFrom_natGene
ratingSequence (α : Type*) [m : MeasurableSpace α] [CountablyGenerated α] : gene
rateFrom (range (natGeneratingSequence _…
· 使用定理 `measurable_generateFrom`：measurable_generateFrom [MeasurableSpace α] {s 
: Set (Set β)} {f : α -> β} (h : forall t in s, MeasurableSet (f ⁻¹' t)) : @Meas
urable _ _ _ …
· 使用引理 `Equiv.image_eq_preimage_symm`：image_eq_preimage_symm (e : α ≃ β) (s : Se
t α) : e '' s = e.symm ⁻¹' s
· 使用引理 `Measurable.eq_const`：Measurable.eq_const {_ : MeasurableSpace α} [Measur
ableSpace β] [MeasurableSingletonClass β] {f : α -> β} (hf : Measurable f) (a : 
β) : Meas…
· 使用定理 `measurable_pi_apply`：measurable_pi_apply (a : δ) : Measurable fun f : fo
rall a, X a => f a
· 使用定理 `Equiv.preimage_eq_iff_eq_image`：preimage_eq_iff_eq_image {α β} (e : α ≃ 
β) (s t) : e ⁻¹' s = t ↔ s = e '' t
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Equiv.ofInjective_apply`：∀ {α : Sort u_3} {β : Type u_4} (f : α → β) (hf
 : Function.Injective f) (a : α), (Equiv.ofInjective f hf) a = ⟨f a, ⋯⟩
· 使用定理 `decide_eq_true_eq`：∀ {p : Prop} [inst : Decidable p], (decide p = true) 
= p
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True

--- 原说明 ---
If a measurable space is countably generated and separates points, it is measure
 equivalent
to some subset of the Cantor space `ℕ → Bool` (equipped with the product sigma a
lgebra).
Note: `s` need not be measurable, so this map need not be a `MeasurableEmbedding
` to
the Cantor Space.
-/
theorem measurableEquiv_nat_bool_of_countablyGenerated [MeasurableSpace α]
    [CountablyGenerated α] [SeparatesPoints α] :
    ∃ s : Set (ℕ → Bool), Nonempty (α ≃ᵐ s) := by
  use range (mapNatBool α), Equiv.ofInjective _ <|
    injective_mapNatBool _,
    Measurable.subtype_mk <| measurable_mapNatBool _
  simp_rw +instances [← generateFrom_natGeneratingSequence α]
  apply measurable_generateFrom
  rintro _ ⟨n, rfl⟩
  rw [← Equiv.image_eq_preimage_symm _ _]
  refine ⟨{y | y n}, by measurability, ?_⟩
  rw [← Equiv.preimage_eq_iff_eq_image]
  simp [mapNatBool]

/-- If a measurable space admits a countable sequence of measurable sets separating points,
it admits a measurable injection into the Cantor space `ℕ → Bool`
(equipped with the product sigma algebra). -/
/-
**MeasurableSpace.measurable_injection_nat_bool_of_countablySeparated** 是 Mathli
b 中的一个定理，位于命名空间 `MeasurableSpace`。
形式化陈述：measurable_injection_nat_bool_of_countablySeparated [MeasurableSpace α] [C
ountablySeparated α] : exists f : α -> Nat -> Bool, Measurable f ∧ Injective f
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.exists_countablyGenerated_le_of_countablySeparated`：exis
ts_countablyGenerated_le_of_countablySeparated [m : MeasurableSpace α] [h : Coun
tablySeparated α] : exists m' : MeasurableSpace α, @Coun…
· 使用定理 `Measurable.mono`：Measurable.mono {ma ma' : MeasurableSpace α} {mb mb' : 
MeasurableSpace β} {f : α -> β} (hf : @Measurable α β ma mb f) (ha : ma <= ma') 
(hb :…
· 使用定理 `MeasurableSpace.measurable_mapNatBool`：measurable_mapNatBool [Measurable
Space α] [CountablyGenerated α] : Measurable (mapNatBool α)
· 使用引理 `le_rfl`：le_rfl : a <= a
· 使用定理 `MeasurableSpace.injective_mapNatBool`：injective_mapNatBool [MeasurableSp
ace α] [CountablyGenerated α] [SeparatesPoints α] : Injective (mapNatBool α)

--- 原说明 ---
If a measurable space admits a countable sequence of measurable sets separating 
points,
it admits a measurable injection into the Cantor space `ℕ → Bool`
(equipped with the product sigma algebra).
-/
theorem measurable_injection_nat_bool_of_countablySeparated [MeasurableSpace α]
    [CountablySeparated α] : ∃ f : α → ℕ → Bool, Measurable f ∧ Injective f := by
  rcases exists_countablyGenerated_le_of_countablySeparated α with ⟨m', _, _, m'le⟩
  refine ⟨mapNatBool α, ?_, injective_mapNatBool _⟩
  exact (measurable_mapNatBool _).mono m'le le_rfl

variable {α}

--TODO: Make this an instance
/-
**MeasurableSpace.measurableSingletonClass_of_countablySeparated** 是 Mathlib 中的一
个定理，位于命名空间 `MeasurableSpace`。
形式化陈述：measurableSingletonClass_of_countablySeparated [MeasurableSpace α] [Counta
blySeparated α] : MeasurableSingletonClass α
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.measurable_injection_nat_bool_of_countablySeparated`：mea
surable_injection_nat_bool_of_countablySeparated [MeasurableSpace α] [CountablyS
eparated α] : exists f : α -> Nat -> Bool, Measurable f ∧…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Function.Injective.preimage_image`：∀ {α : Type u_1} {β : Type u_2} {f : 
α → β}, Function.Injective f → ∀ (s : Set α), f ⁻¹' f '' s = s
· 使用定理 `Set.image_singleton`：image_singleton {f : α -> β} {a : α} : f '' {a} = {
f a}
· 使用引理 `MeasurableSet.singleton`：MeasurableSet.singleton [MeasurableSpace α] [Me
asurableSingletonClass α] (a : α) : MeasurableSet {a}
· 使用定理 `instCountableNat`：Countable ℕ
-/
theorem measurableSingletonClass_of_countablySeparated
    [MeasurableSpace α] [CountablySeparated α] :
    MeasurableSingletonClass α := by
  rcases measurable_injection_nat_bool_of_countablySeparated α with ⟨f, fmeas, finj⟩
  refine ⟨fun x ↦ ?_⟩
  rw [← finj.preimage_image {x}, image_singleton]
  exact fmeas <| MeasurableSet.singleton _

end SeparatesPoints

section MeasurableMemPartition

/-
**MeasurableSpace.measurableSet_succ_memPartition** 是 Mathlib 中的一个引理，位于命名空间 `Mea
surableSpace`。
形式化陈述：measurableSet_succ_memPartition (t : Nat -> Set α) (n : Nat) {s : Set α} (
hs : s in memPartition t n) : MeasurableSet[generateFrom (memPartition t (n + 1)
)] s
参数：t : Nat -> Set α；n : Nat；hs : s in memPartition t n。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Set.sdiff_union_inter`：sdiff_union_inter (s t : Set α) : s \ t union s i
nter t = s
· 使用定理 `MeasurableSet.union`：∀ {α : Type u_1} {m : MeasurableSpace α} {s₁ s₂ : S
et α}, MeasurableSet s₁ → MeasurableSet s₂ → MeasurableSet (s₁ ∪ s₂)
· 使用定理 `MeasurableSpace.measurableSet_generateFrom`：measurableSet_generateFrom {
s : Set (Set α)} {t : Set α} (ht : t in s) : MeasurableSet[generateFrom s] t
· 使用引理 `memPartition_succ`：memPartition_succ (f : Nat -> Set α) (n : Nat) : memP
artition f (n + 1) = {s | exists u in memPartition f n, s = u inter f n ∨ s = u 
\ f n}
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `or_true`：∀ (p : Prop), (p ∨ True) = True
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `true_or`：∀ (p : Prop), (True ∨ p) = True
-/
lemma measurableSet_succ_memPartition (t : ℕ → Set α) (n : ℕ) {s : Set α}
    (hs : s ∈ memPartition t n) :
    MeasurableSet[generateFrom (memPartition t (n + 1))] s := by
  rw [← sdiff_union_inter s (t n)]
  refine MeasurableSet.union ?_ ?_ <;>
    · refine measurableSet_generateFrom ?_
      rw [memPartition_succ]
      exact ⟨s, hs, by simp⟩
/-
**MeasurableSpace.generateFrom_memPartition_le_succ** 是 Mathlib 中的一个引理，位于命名空间 `M
easurableSpace`。
形式化陈述：generateFrom_memPartition_le_succ (t : Nat -> Set α) (n : Nat) : generateF
rom (memPartition t n) <= generateFrom (memPartition t (n + 1))
参数：t : Nat -> Set α；n : Nat。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
· 使用引理 `MeasurableSpace.measurableSet_succ_memPartition`：measurableSet_succ_memP
artition (t : Nat -> Set α) (n : Nat) {s : Set α} (hs : s in memPartition t n) :
 MeasurableSet[generateFrom (memParti…
-/
lemma generateFrom_memPartition_le_succ (t : ℕ → Set α) (n : ℕ) :
    generateFrom (memPartition t n) ≤ generateFrom (memPartition t (n + 1)) :=
  generateFrom_le (fun _ hs ↦ measurableSet_succ_memPartition t n hs)
/-
**MeasurableSpace.measurableSet_generateFrom_memPartition_iff** 是 Mathlib 中的一个引理
，位于命名空间 `MeasurableSpace`。
形式化陈述：measurableSet_generateFrom_memPartition_iff (t : Nat -> Set α) (n : Nat) (
s : Set α) : MeasurableSet[generateFrom (memPartition t n)] s ↔ exists S : Finse
t (Set α), ↑S subseteq memPartition t n ∧ s = ⋃₀ S
参数：t : Nat -> Set α；n : Nat；s : Set α。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.generateFrom_induction`：generateFrom_induction (C : Set 
(Set α)) (p : forall s : Set α, MeasurableSet[generateFrom C] s -> Prop) (hC : f
orall t in C, forall ht, p t…
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Finset.coe_singleton`：coe_singleton (a : α) : (({a} : Finset α) : Set α)
 = {a}
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `Set.sUnion_singleton`：sUnion_singleton (s : Set α) : ⋃₀ {s} = s
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Finset.coe_empty`：coe_empty : ((∅ : Finset α) : Set α) = ∅
· 使用定理 `Set.sUnion_empty`：sUnion_empty : ⋃₀ ∅ = (∅ : Set α)
· 使用定理 `Finset.coe_sdiff`：coe_sdiff (s₁ s₂ : Finset α) : ↑(s₁ \ s₂) = (s₁ \ s₂ :
 Set α)
· 使用定理 `Set.coe_toFinset`：coe_toFinset (s : Set α) [Fintype s] : (↑s.toFinset : 
Set α) = s
· 使用定理 `Set.sdiff_subset`：sdiff_subset {s t : Set α} : s \ t subseteq s
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `IsCompl.eq_compl`：IsCompl.eq_compl (h : IsCompl a b) : a = bᶜ
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Set.disjoint_sUnion_right`：disjoint_sUnion_right {s : Set α} {S : Set (S
et α)} : Disjoint s (⋃₀ S) ↔ forall t in S, Disjoint s t
· 使用定理 `Set.disjoint_sUnion_left`：disjoint_sUnion_left {S : Set (Set α)} {t : Se
t α} : Disjoint (⋃₀ S) t ↔ forall s in S, Disjoint s t
· 使用引理 `disjoint_memPartition`：disjoint_memPartition (f : Nat -> Set α) (n : Nat
) {u v : Set α} (hu : u in memPartition f n) (hv : v in memPartition f n) (huv :
 u != v) : …
· 使用定理 `Set.mem_of_mem_sdiff`：mem_of_mem_sdiff {s t : Set α} {x : α} (h : x in s
 \ t) : x in s
· 使用定理 `Ne.symm`：∀ {α : Sort u} {a b : α}, a ≠ b → b ≠ a
· 使用定理 `ne_of_mem_of_not_mem`：∀ {α : Type u_1} {β : Type u_2} [inst : Membership
 α β] {s : β} {a b : α}, a ∈ s → b ∉ s → a ≠ b
· 使用定理 `Set.notMem_of_mem_sdiff`：notMem_of_mem_sdiff {s t : Set α} {x : α} (h : 
x in s \ t) : x ∉ t
· 使用定理 `codisjoint_iff`：∀ {α : Type u_1} [inst : SemilatticeSup α] [inst_1 : Ord
erTop α] {a b : α}, Codisjoint a b ↔ a ⊔ b = ⊤
· 使用引理 `sUnion_memPartition`：sUnion_memPartition (f : Nat -> Set α) (n : Nat) : 
⋃₀ memPartition f n = univ
· 使用定理 `Set.union_comm`：union_comm (a b : Set α) : a union b = b union a
· 使用定理 `Set.sUnion_union`：sUnion_union (S T : Set (Set α)) : ⋃₀ (S union T) = ⋃₀
 S union ⋃₀ T
· 使用定理 `Set.union_sdiff_cancel`：union_sdiff_cancel {s t : Set α} (h : s subseteq
 t) : s union t \ s = t
· 使用定理 `Set.Finite.subset`：∀ {α : Type u} {s : Set α}, s.Finite → ∀ {t : Set α},
 t ⊆ s → t.Finite
（共 41 条，此处仅展示前 30 条）
-/
lemma measurableSet_generateFrom_memPartition_iff (t : ℕ → Set α) (n : ℕ) (s : Set α) :
    MeasurableSet[generateFrom (memPartition t n)] s
      ↔ ∃ S : Finset (Set α), ↑S ⊆ memPartition t n ∧ s = ⋃₀ S := by
  refine ⟨fun h ↦ ?_, fun ⟨S, hS_subset, hS_eq⟩ ↦ ?_⟩
  · induction s, h using generateFrom_induction with
    | hC u hu _ => exact ⟨{u}, by simp [hu], by simp⟩
    | empty => exact ⟨∅, by simp, by simp⟩
    | compl u _ hu =>
      obtain ⟨S, hS_subset, rfl⟩ := hu
      refine ⟨(memPartition t n).toFinset \ S, ?_, ?_⟩
      · simp only [Finset.coe_sdiff, coe_toFinset]
        exact sdiff_subset
      · simp only [Finset.coe_sdiff, coe_toFinset]
        refine (IsCompl.eq_compl ⟨?_, ?_⟩).symm
        · refine Set.disjoint_sUnion_right.mpr fun u huS => ?_
          refine Set.disjoint_sUnion_left.mpr fun v huV => ?_
          refine disjoint_memPartition t n (mem_of_mem_sdiff huV) (hS_subset huS) ?_
          exact ne_of_mem_of_not_mem huS (notMem_of_mem_sdiff huV) |>.symm
        · rw [codisjoint_iff]
          simp only [sup_eq_union, top_eq_univ]
          rw [← sUnion_memPartition t n, union_comm, ← sUnion_union, union_sdiff_cancel hS_subset]
    | iUnion f _ h =>
      choose S hS_subset hS_eq using h
      have : Fintype (⋃ n, (S n : Set (Set α))) := by
        refine (Finite.subset (finite_memPartition t n) ?_).fintype
        simp only [iUnion_subset_iff]
        exact hS_subset
      refine ⟨(⋃ n, (S n : Set (Set α))).toFinset, ?_, ?_⟩
      · simp only [coe_toFinset, iUnion_subset_iff]
        exact hS_subset
      · simp only [coe_toFinset, sUnion_iUnion, hS_eq]
  · rw [hS_eq, sUnion_eq_biUnion]
    refine MeasurableSet.biUnion ?_ (fun t ht ↦ ?_)
    · exact S.countable_toSet
    · exact measurableSet_generateFrom (hS_subset ht)
/-
**MeasurableSpace.measurableSet_generateFrom_memPartition** 是 Mathlib 中的一个引理，位于命
名空间 `MeasurableSpace`。
形式化陈述：measurableSet_generateFrom_memPartition (t : Nat -> Set α) (n : Nat) : Mea
surableSet[generateFrom (memPartition t (n + 1))] (t n)
参数：t : Nat -> Set α；n : Nat。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用引理 `sUnion_memPartition`：sUnion_memPartition (f : Nat -> Set α) (n : Nat) : 
⋃₀ memPartition f n = univ
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Set.univ_inter`：univ_inter (a : Set α) : univ inter a = a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `MeasurableSet.biUnion`：∀ {α : Type u_1} {β : Type u_2} {m : MeasurableSp
ace α} {f : β → Set α} {s : Set β},   s.Countable → (∀ b ∈ s, MeasurableSet (f b
)) → Measur…
· 使用定理 `Set.Finite.countable`：∀ {α : Type u} {s : Set α}, s.Finite → s.Countable
· 使用引理 `finite_memPartition`：finite_memPartition (f : Nat -> Set α) (n : Nat) : 
Set.Finite (memPartition f n)
· 使用定理 `MeasurableSpace.measurableSet_generateFrom`：measurableSet_generateFrom {
s : Set (Set α)} {t : Set α} (ht : t in s) : MeasurableSet[generateFrom s] t
· 使用引理 `memPartition_succ`：memPartition_succ (f : Nat -> Set α) (n : Nat) : memP
artition f (n + 1) = {s | exists u in memPartition f n, s = u inter f n ∨ s = u 
\ f n}
-/
lemma measurableSet_generateFrom_memPartition (t : ℕ → Set α) (n : ℕ) :
    MeasurableSet[generateFrom (memPartition t (n + 1))] (t n) := by
  have : t n = ⋃ u ∈ memPartition t n, u ∩ t n := by
    simp_rw [← iUnion_inter, ← sUnion_eq_biUnion, sUnion_memPartition, univ_inter]
  rw [this]
  refine MeasurableSet.biUnion (finite_memPartition _ _).countable (fun v hv ↦ ?_)
  refine measurableSet_generateFrom ?_
  rw [memPartition_succ]
  exact ⟨v, hv, Or.inl rfl⟩
/-
**MeasurableSpace.generateFrom_iUnion_memPartition** 是 Mathlib 中的一个引理，位于命名空间 `Me
asurableSpace`。
形式化陈述：generateFrom_iUnion_memPartition (t : Nat -> Set α) : generateFrom (⋃ n, m
emPartition t n) = generateFrom (range t)
参数：t : Nat -> Set α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSet.univ`：∀ {α : Type u_1} {m : MeasurableSpace α}, Measurable
Set Set.univ
· 使用定理 `MeasurableSet.inter`：∀ {α : Type u_1} {m : MeasurableSpace α} {s₁ s₂ : S
et α}, MeasurableSet s₁ → MeasurableSet s₂ → MeasurableSet (s₁ ∩ s₂)
· 使用定理 `MeasurableSpace.measurableSet_generateFrom`：measurableSet_generateFrom {
s : Set (Set α)} {t : Set α} (ht : t in s) : MeasurableSet[generateFrom s] t
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MeasurableSet.diff`：∀ {α : Type u_1} {m : MeasurableSpace α} {s₁ s₂ : Se
t α}, MeasurableSet s₁ → MeasurableSet s₂ → MeasurableSet (s₁ \ s₂)
· 使用定理 `MeasurableSpace.generateFrom_mono`：generateFrom_mono {s t : Set (Set α)}
 (h : s subseteq t) : generateFrom s <= generateFrom t
· 使用定理 `Set.subset_iUnion`：subset_iUnion : forall (s : ι -> Set β) (i : ι), s i 
subseteq ⋃ i, s i
· 使用引理 `MeasurableSpace.measurableSet_generateFrom_memPartition`：measurableSet_g
enerateFrom_memPartition (t : Nat -> Set α) (n : Nat) : MeasurableSet[generateFr
om (memPartition t (n + 1))] (t n)
-/
lemma generateFrom_iUnion_memPartition (t : ℕ → Set α) :
    generateFrom (⋃ n, memPartition t n) = generateFrom (range t) := by
  refine le_antisymm (generateFrom_le fun u hu ↦ ?_) (generateFrom_le fun u hu ↦ ?_)
  · simp only [mem_iUnion] at hu
    obtain ⟨n, hun⟩ := hu
    induction n generalizing u with
    | zero =>
      simp only [memPartition_zero, mem_singleton_iff] at hun
      rw [hun]
      exact MeasurableSet.univ
    | succ n ih =>
      simp only [memPartition_succ, mem_ofPred_eq] at hun
      obtain ⟨v, hv, huv⟩ := hun
      rcases huv with rfl | rfl
      · exact (ih v hv).inter (measurableSet_generateFrom ⟨n, rfl⟩)
      · exact (ih v hv).diff (measurableSet_generateFrom ⟨n, rfl⟩)
  · simp only [mem_range] at hu
    obtain ⟨n, rfl⟩ := hu
    exact generateFrom_mono (subset_iUnion _ _) _ (measurableSet_generateFrom_memPartition t n)
/-
**MeasurableSpace.generateFrom_memPartition_le_range** 是 Mathlib 中的一个引理，位于命名空间 `
MeasurableSpace`。
形式化陈述：generateFrom_memPartition_le_range (t : Nat -> Set α) (n : Nat) : generate
From (memPartition t n) <= generateFrom (range t)
参数：t : Nat -> Set α；n : Nat。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `MeasurableSpace.generateFrom_iUnion_memPartition`：generateFrom_iUnion_me
mPartition (t : Nat -> Set α) : generateFrom (⋃ n, memPartition t n) = generateF
rom (range t)
· 使用定理 `MeasurableSpace.generateFrom_mono`：generateFrom_mono {s t : Set (Set α)}
 (h : s subseteq t) : generateFrom s <= generateFrom t
· 使用定理 `Set.subset_iUnion`：subset_iUnion : forall (s : ι -> Set β) (i : ι), s i 
subseteq ⋃ i, s i
-/
lemma generateFrom_memPartition_le_range (t : ℕ → Set α) (n : ℕ) :
    generateFrom (memPartition t n) ≤ generateFrom (range t) := by
  conv_rhs => rw [← generateFrom_iUnion_memPartition t]
  exact generateFrom_mono (subset_iUnion _ _)
/-
**MeasurableSpace.generateFrom_iUnion_memPartition_le** 是 Mathlib 中的一个引理，位于命名空间 
`MeasurableSpace`。
形式化陈述：generateFrom_iUnion_memPartition_le [m : MeasurableSpace α] {t : Nat -> Se
t α} (ht : forall n, MeasurableSet (t n)) : generateFrom (⋃ n, memPartition t n)
 <= m
参数：ht : forall n, MeasurableSet (t n)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans_le`：∀ {α : Type u_1} {a b c : α} [inst : LE α], a = b → b ≤ c →
 a ≤ c
· 使用引理 `MeasurableSpace.generateFrom_iUnion_memPartition`：generateFrom_iUnion_me
mPartition (t : Nat -> Set α) : generateFrom (⋃ n, memPartition t n) = generateF
rom (range t)
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
-/
lemma generateFrom_iUnion_memPartition_le [m : MeasurableSpace α] {t : ℕ → Set α}
    (ht : ∀ n, MeasurableSet (t n)) :
    generateFrom (⋃ n, memPartition t n) ≤ m := by
  refine (generateFrom_iUnion_memPartition t).trans_le (generateFrom_le ?_)
  rintro s ⟨i, rfl⟩
  exact ht i
/-
**MeasurableSpace.generateFrom_memPartition_le** 是 Mathlib 中的一个引理，位于命名空间 `Measur
ableSpace`。
形式化陈述：generateFrom_memPartition_le [m : MeasurableSpace α] {t : Nat -> Set α} (h
t : forall n, MeasurableSet (t n)) (n : Nat) : generateFrom (memPartition t n) <
= m
参数：ht : forall n, MeasurableSet (t n)；n : Nat。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用定理 `MeasurableSpace.generateFrom_mono`：generateFrom_mono {s t : Set (Set α)}
 (h : s subseteq t) : generateFrom s <= generateFrom t
· 使用定理 `Set.subset_iUnion`：subset_iUnion : forall (s : ι -> Set β) (i : ι), s i 
subseteq ⋃ i, s i
· 使用引理 `MeasurableSpace.generateFrom_iUnion_memPartition_le`：generateFrom_iUnion
_memPartition_le [m : MeasurableSpace α] {t : Nat -> Set α} (ht : forall n, Meas
urableSet (t n)) : generateFrom (⋃ n, mem…
-/
lemma generateFrom_memPartition_le [m : MeasurableSpace α] {t : ℕ → Set α}
    (ht : ∀ n, MeasurableSet (t n)) (n : ℕ) :
    generateFrom (memPartition t n) ≤ m :=
  (generateFrom_mono (subset_iUnion _ _)).trans (generateFrom_iUnion_memPartition_le ht)
/-
**MeasurableSpace.measurableSet_memPartition** 是 Mathlib 中的一个引理，位于命名空间 `Measurab
leSpace`。
形式化陈述：measurableSet_memPartition [MeasurableSpace α] {t : Nat -> Set α} (ht : fo
rall n, MeasurableSet (t n)) (n : Nat) {s : Set α} (hs : s in memPartition t n) 
: MeasurableSet s
参数：ht : forall n, MeasurableSet (t n)；n : Nat；hs : s in memPartition t n。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.generateFrom_memPartition_le`：generateFrom_memPartition_
le [m : MeasurableSpace α] {t : Nat -> Set α} (ht : forall n, MeasurableSet (t n
)) (n : Nat) : generateFrom (memPa…
· 使用定理 `MeasurableSpace.measurableSet_generateFrom`：measurableSet_generateFrom {
s : Set (Set α)} {t : Set α} (ht : t in s) : MeasurableSet[generateFrom s] t
-/
lemma measurableSet_memPartition [MeasurableSpace α] {t : ℕ → Set α}
    (ht : ∀ n, MeasurableSet (t n)) (n : ℕ) {s : Set α} (hs : s ∈ memPartition t n) :
    MeasurableSet s :=
  generateFrom_memPartition_le ht n _ (measurableSet_generateFrom hs)
/-
**MeasurableSpace.measurableSet_memPartitionSet** 是 Mathlib 中的一个引理，位于命名空间 `Measu
rableSpace`。
形式化陈述：measurableSet_memPartitionSet [MeasurableSpace α] {t : Nat -> Set α} (ht :
 forall n, MeasurableSet (t n)) (n : Nat) (a : α) : MeasurableSet (memPartitionS
et t n a)
参数：ht : forall n, MeasurableSet (t n)；n : Nat；a : α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.measurableSet_memPartition`：measurableSet_memPartition [
MeasurableSpace α] {t : Nat -> Set α} (ht : forall n, MeasurableSet (t n)) (n : 
Nat) {s : Set α} (hs : s in memP…
· 使用引理 `memPartitionSet_mem`：memPartitionSet_mem (f : Nat -> Set α) (n : Nat) (a
 : α) : memPartitionSet f n a in memPartition f n
-/
lemma measurableSet_memPartitionSet [MeasurableSpace α] {t : ℕ → Set α}
    (ht : ∀ n, MeasurableSet (t n)) (n : ℕ) (a : α) :
    MeasurableSet (memPartitionSet t n a) :=
  measurableSet_memPartition ht n (memPartitionSet_mem t n a)

end MeasurableMemPartition

variable [m : MeasurableSpace α] [h : CountablyGenerated α]

/-- For each `n : ℕ`, `countablePartition α n` is a partition of the space in at most
`2^n` sets. Each partition is finer than the preceding one. The measurable space generated by
the union of all those partitions is the measurable space on `α`. -/
-- Note: `Set` has no computational content, but Lean still attempts to compile it.
-- See https://github.com/leanprover/lean4/issues/14084.
/-
**MeasurableSpace.countablePartition** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSpace`
。
形式化陈述：countablePartition (α : Type*) [MeasurableSpace α] [CountablyGenerated α] 
: Nat -> Set (Set α)
参数：α : Type*。
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
-/
noncomputable def countablePartition (α : Type*) [MeasurableSpace α] [CountablyGenerated α] :
    ℕ → Set (Set α) :=
  memPartition (enumerateCountable countable_countableGeneratingSet ∅)
/-
**MeasurableSpace.measurableSet_enumerateCountable_countableGeneratingSet** 是 Ma
thlib 中的一个引理，位于命名空间 `MeasurableSpace`。
形式化陈述：measurableSet_enumerateCountable_countableGeneratingSet (α : Type*) [Measu
rableSpace α] [CountablyGenerated α] (n : Nat) : MeasurableSet (enumerateCountab
le (countable_countableGeneratingSet (α
参数：α : Type*；n : Nat。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.measurableSet_countableGeneratingSet`：measurableSet_coun
tableGeneratingSet [MeasurableSpace α] [CountablyGenerated α] {s : Set α} (hs : 
s in countableGeneratingSet α) : Measurabl…
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
· 使用引理 `Set.enumerateCountable_mem`：enumerateCountable_mem {s : Set α} (h : s.Co
untable) {default : α} (h_mem : default in s) (n : Nat) : enumerateCountable h d
efault n in s
· 使用引理 `MeasurableSpace.empty_mem_countableGeneratingSet`：empty_mem_countableGen
eratingSet [MeasurableSpace α] [CountablyGenerated α] : ∅ in countableGenerating
Set α
-/
lemma measurableSet_enumerateCountable_countableGeneratingSet
    (α : Type*) [MeasurableSpace α] [CountablyGenerated α] (n : ℕ) :
    MeasurableSet (enumerateCountable (countable_countableGeneratingSet (α := α)) ∅ n) :=
  measurableSet_countableGeneratingSet
    (enumerateCountable_mem _ (empty_mem_countableGeneratingSet) n)
/-
**MeasurableSpace.finite_countablePartition** 是 Mathlib 中的一个引理，位于命名空间 `Measurabl
eSpace`。
形式化陈述：finite_countablePartition (α : Type*) [MeasurableSpace α] [CountablyGenera
ted α] (n : Nat) : Set.Finite (countablePartition α n)
参数：α : Type*；n : Nat。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `finite_memPartition`：finite_memPartition (f : Nat -> Set α) (n : Nat) : 
Set.Finite (memPartition f n)
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
-/
lemma finite_countablePartition (α : Type*) [MeasurableSpace α] [CountablyGenerated α] (n : ℕ) :
    Set.Finite (countablePartition α n) :=
  finite_memPartition _ n
/-
**MeasurableSpace.instFinite_countablePartition** 是 Mathlib 中的一个实例，位于命名空间 `Measu
rableSpace`。
形式化陈述：instFinite_countablePartition (n : Nat) : Finite (countablePartition α n)
参数：n : Nat。
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Set.finite_coe_iff`：finite_coe_iff {s : Set α} : Finite s ↔ s.Finite
· 使用引理 `MeasurableSpace.finite_countablePartition`：finite_countablePartition (α 
: Type*) [MeasurableSpace α] [CountablyGenerated α] (n : Nat) : Set.Finite (coun
tablePartition α n)
-/
instance instFinite_countablePartition (n : ℕ) : Finite (countablePartition α n) :=
  Set.finite_coe_iff.mp (finite_countablePartition _ _)
/-
**MeasurableSpace.disjoint_countablePartition** 是 Mathlib 中的一个引理，位于命名空间 `Measura
bleSpace`。
形式化陈述：disjoint_countablePartition {n : Nat} {s t : Set α} (hs : s in countablePa
rtition α n) (ht : t in countablePartition α n) (hst : s != t) : Disjoint s t
参数：hs : s in countablePartition α n；ht : t in countablePartition α n；hst : s != 
t。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `disjoint_memPartition`：disjoint_memPartition (f : Nat -> Set α) (n : Nat
) {u v : Set α} (hu : u in memPartition f n) (hv : v in memPartition f n) (huv :
 u != v) : …
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
-/
lemma disjoint_countablePartition {n : ℕ} {s t : Set α}
    (hs : s ∈ countablePartition α n) (ht : t ∈ countablePartition α n) (hst : s ≠ t) :
    Disjoint s t :=
  disjoint_memPartition _ n hs ht hst
/-
**MeasurableSpace.sUnion_countablePartition** 是 Mathlib 中的一个引理，位于命名空间 `Measurabl
eSpace`。
形式化陈述：sUnion_countablePartition (α : Type*) [MeasurableSpace α] [CountablyGenera
ted α] (n : Nat) : ⋃₀ countablePartition α n = univ
参数：α : Type*；n : Nat。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `sUnion_memPartition`：sUnion_memPartition (f : Nat -> Set α) (n : Nat) : 
⋃₀ memPartition f n = univ
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
-/
lemma sUnion_countablePartition (α : Type*) [MeasurableSpace α] [CountablyGenerated α] (n : ℕ) :
    ⋃₀ countablePartition α n = univ :=
  sUnion_memPartition _ n
/-
**MeasurableSpace.measurableSet_generateFrom_countablePartition_iff** 是 Mathlib 
中的一个引理，位于命名空间 `MeasurableSpace`。
形式化陈述：measurableSet_generateFrom_countablePartition_iff (n : Nat) (s : Set α) : 
MeasurableSet[generateFrom (countablePartition α n)] s ↔ exists S : Finset (Set 
α), ↑S subseteq countablePartition α n ∧ s = ⋃₀ S
参数：n : Nat；s : Set α。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.measurableSet_generateFrom_memPartition_iff`：measurableS
et_generateFrom_memPartition_iff (t : Nat -> Set α) (n : Nat) (s : Set α) : Meas
urableSet[generateFrom (memPartition t n)] s ↔ ex…
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
-/
lemma measurableSet_generateFrom_countablePartition_iff (n : ℕ) (s : Set α) :
    MeasurableSet[generateFrom (countablePartition α n)] s
      ↔ ∃ S : Finset (Set α), ↑S ⊆ countablePartition α n ∧ s = ⋃₀ S :=
  measurableSet_generateFrom_memPartition_iff _ n s
/-
**MeasurableSpace.measurableSet_succ_countablePartition** 是 Mathlib 中的一个引理，位于命名空
间 `MeasurableSpace`。
形式化陈述：measurableSet_succ_countablePartition (n : Nat) {s : Set α} (hs : s in cou
ntablePartition α n) : MeasurableSet[generateFrom (countablePartition α (n + 1))
] s
参数：n : Nat；hs : s in countablePartition α n。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.measurableSet_succ_memPartition`：measurableSet_succ_memP
artition (t : Nat -> Set α) (n : Nat) {s : Set α} (hs : s in memPartition t n) :
 MeasurableSet[generateFrom (memParti…
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
-/
lemma measurableSet_succ_countablePartition (n : ℕ) {s : Set α} (hs : s ∈ countablePartition α n) :
    MeasurableSet[generateFrom (countablePartition α (n + 1))] s :=
  measurableSet_succ_memPartition _ _ hs
/-
**MeasurableSpace.generateFrom_countablePartition_le_succ** 是 Mathlib 中的一个引理，位于命
名空间 `MeasurableSpace`。
形式化陈述：generateFrom_countablePartition_le_succ (α : Type*) [MeasurableSpace α] [C
ountablyGenerated α] (n : Nat) : generateFrom (countablePartition α n) <= genera
teFrom (countablePartition α (n + 1))
参数：α : Type*；n : Nat。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.generateFrom_memPartition_le_succ`：generateFrom_memParti
tion_le_succ (t : Nat -> Set α) (n : Nat) : generateFrom (memPartition t n) <= g
enerateFrom (memPartition t (n + 1))
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
-/
lemma generateFrom_countablePartition_le_succ (α : Type*) [MeasurableSpace α] [CountablyGenerated α]
    (n : ℕ) :
    generateFrom (countablePartition α n) ≤ generateFrom (countablePartition α (n + 1)) :=
  generateFrom_memPartition_le_succ _ _
/-
**MeasurableSpace.generateFrom_iUnion_countablePartition** 是 Mathlib 中的一个引理，位于命名
空间 `MeasurableSpace`。
形式化陈述：generateFrom_iUnion_countablePartition (α : Type*) [m : MeasurableSpace α]
 [CountablyGenerated α] : generateFrom (⋃ n, countablePartition α n) = m
参数：α : Type*。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSpace.countablePartition.eq_1`：∀ (α : Type u_3) [inst : Measur
ableSpace α] [inst_1 : MeasurableSpace.CountablyGenerated α],   MeasurableSpace.
countablePartition α = memPar…
· 使用引理 `MeasurableSpace.generateFrom_iUnion_memPartition`：generateFrom_iUnion_me
mPartition (t : Nat -> Set α) : generateFrom (⋃ n, memPartition t n) = generateF
rom (range t)
· 使用引理 `Set.range_enumerateCountable_of_mem`：range_enumerateCountable_of_mem {s 
: Set α} (h : s.Countable) {default : α} (h_mem : default in s) : range (enumera
teCountable h default) = …
· 使用引理 `MeasurableSpace.empty_mem_countableGeneratingSet`：empty_mem_countableGen
eratingSet [MeasurableSpace α] [CountablyGenerated α] : ∅ in countableGenerating
Set α
· 使用引理 `MeasurableSpace.generateFrom_countableGeneratingSet`：generateFrom_counta
bleGeneratingSet [m : MeasurableSpace α] [h : CountablyGenerated α] : generateFr
om (countableGeneratingSet α) = m
-/
lemma generateFrom_iUnion_countablePartition (α : Type*) [m : MeasurableSpace α]
    [CountablyGenerated α] :
    generateFrom (⋃ n, countablePartition α n) = m := by
  rw [countablePartition, generateFrom_iUnion_memPartition,
    range_enumerateCountable_of_mem _ empty_mem_countableGeneratingSet,
    generateFrom_countableGeneratingSet]
/-
**MeasurableSpace.generateFrom_countablePartition_le** 是 Mathlib 中的一个引理，位于命名空间 `
MeasurableSpace`。
形式化陈述：generateFrom_countablePartition_le (α : Type*) [m : MeasurableSpace α] [Co
untablyGenerated α] (n : Nat) : generateFrom (countablePartition α n) <= m
参数：α : Type*；n : Nat。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.generateFrom_memPartition_le`：generateFrom_memPartition_
le [m : MeasurableSpace α] {t : Nat -> Set α} (ht : forall n, MeasurableSet (t n
)) (n : Nat) : generateFrom (memPa…
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
· 使用引理 `MeasurableSpace.measurableSet_enumerateCountable_countableGeneratingSet`
：measurableSet_enumerateCountable_countableGeneratingSet (α : Type*) [Measurable
Space α] [CountablyGenerated α] (n : Nat) : MeasurableSet (en…
-/
lemma generateFrom_countablePartition_le (α : Type*) [m : MeasurableSpace α] [CountablyGenerated α]
    (n : ℕ) :
    generateFrom (countablePartition α n) ≤ m :=
  generateFrom_memPartition_le (measurableSet_enumerateCountable_countableGeneratingSet α) n
/-
**MeasurableSpace.measurableSet_countablePartition** 是 Mathlib 中的一个引理，位于命名空间 `Me
asurableSpace`。
形式化陈述：measurableSet_countablePartition (n : Nat) {s : Set α} (hs : s in countabl
ePartition α n) : MeasurableSet s
参数：n : Nat；hs : s in countablePartition α n。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.generateFrom_countablePartition_le`：generateFrom_countab
lePartition_le (α : Type*) [m : MeasurableSpace α] [CountablyGenerated α] (n : N
at) : generateFrom (countablePartition α…
· 使用定理 `MeasurableSpace.measurableSet_generateFrom`：measurableSet_generateFrom {
s : Set (Set α)} {t : Set α} (ht : t in s) : MeasurableSet[generateFrom s] t
-/
lemma measurableSet_countablePartition (n : ℕ) {s : Set α} (hs : s ∈ countablePartition α n) :
    MeasurableSet s :=
  generateFrom_countablePartition_le α n _ (measurableSet_generateFrom hs)

/-- The set in `countablePartition α n` to which `a : α` belongs. -/
-- Note: `Set` has no computational content, but Lean still attempts to compile it.
-- See https://github.com/leanprover/lean4/issues/14084.
/-
**MeasurableSpace.countablePartitionSet** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSpa
ce`。
形式化陈述：countablePartitionSet (n : Nat) (a : α) : Set α
参数：n : Nat；a : α。
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
-/
noncomputable def countablePartitionSet (n : ℕ) (a : α) : Set α :=
  memPartitionSet (enumerateCountable countable_countableGeneratingSet ∅) n a
/-
**MeasurableSpace.countablePartitionSet_mem** 是 Mathlib 中的一个引理，位于命名空间 `Measurabl
eSpace`。
形式化陈述：countablePartitionSet_mem (n : Nat) (a : α) : countablePartitionSet n a in
 countablePartition α n
参数：n : Nat；a : α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `memPartitionSet_mem`：memPartitionSet_mem (f : Nat -> Set α) (n : Nat) (a
 : α) : memPartitionSet f n a in memPartition f n
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
-/
lemma countablePartitionSet_mem (n : ℕ) (a : α) :
    countablePartitionSet n a ∈ countablePartition α n :=
  memPartitionSet_mem _ _ _
/-
**MeasurableSpace.mem_countablePartitionSet** 是 Mathlib 中的一个引理，位于命名空间 `Measurabl
eSpace`。
形式化陈述：mem_countablePartitionSet (n : Nat) (a : α) : a in countablePartitionSet n
 a
参数：n : Nat；a : α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `mem_memPartitionSet`：mem_memPartitionSet (f : Nat -> Set α) (n : Nat) (a
 : α) : a in memPartitionSet f n a
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
-/
lemma mem_countablePartitionSet (n : ℕ) (a : α) : a ∈ countablePartitionSet n a :=
  mem_memPartitionSet _ _ _
/-
**MeasurableSpace.countablePartitionSet_eq_iff** 是 Mathlib 中的一个引理，位于命名空间 `Measur
ableSpace`。
形式化陈述：countablePartitionSet_eq_iff {n : Nat} (a : α) {s : Set α} (hs : s in coun
tablePartition α n) : countablePartitionSet n a = s ↔ a in s
参数：a : α；hs : s in countablePartition α n。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `memPartitionSet_eq_iff`：memPartitionSet_eq_iff {f : Nat -> Set α} {n : N
at} (a : α) {s : Set α} (hs : s in memPartition f n) : memPartitionSet f n a = s
 ↔ a in s
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
-/
lemma countablePartitionSet_eq_iff {n : ℕ} (a : α) {s : Set α} (hs : s ∈ countablePartition α n) :
    countablePartitionSet n a = s ↔ a ∈ s :=
  memPartitionSet_eq_iff _ hs
/-
**MeasurableSpace.countablePartitionSet_of_mem** 是 Mathlib 中的一个引理，位于命名空间 `Measur
ableSpace`。
形式化陈述：countablePartitionSet_of_mem {n : Nat} {a : α} {s : Set α} (hs : s in coun
tablePartition α n) (ha : a in s) : countablePartitionSet n a = s
参数：hs : s in countablePartition α n；ha : a in s。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `memPartitionSet_of_mem`：memPartitionSet_of_mem {f : Nat -> Set α} {n : N
at} {a : α} {s : Set α} (hs : s in memPartition f n) (ha : a in s) : memPartitio
nSet f n a =…
· 使用引理 `MeasurableSpace.countable_countableGeneratingSet`：countable_countableGen
eratingSet [MeasurableSpace α] [h : CountablyGenerated α] : Set.Countable (count
ableGeneratingSet α)
-/
lemma countablePartitionSet_of_mem {n : ℕ} {a : α} {s : Set α} (hs : s ∈ countablePartition α n)
    (ha : a ∈ s) :
    countablePartitionSet n a = s :=
  memPartitionSet_of_mem hs ha

@[measurability]
/-
**MeasurableSpace.measurableSet_countablePartitionSet** 是 Mathlib 中的一个引理，位于命名空间 
`MeasurableSpace`。
形式化陈述：measurableSet_countablePartitionSet (n : Nat) (a : α) : MeasurableSet (cou
ntablePartitionSet n a)
参数：n : Nat；a : α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasurableSpace.measurableSet_countablePartition`：measurableSet_countabl
ePartition (n : Nat) {s : Set α} (hs : s in countablePartition α n) : Measurable
Set s
· 使用引理 `MeasurableSpace.countablePartitionSet_mem`：countablePartitionSet_mem (n 
: Nat) (a : α) : countablePartitionSet n a in countablePartition α n
-/
lemma measurableSet_countablePartitionSet (n : ℕ) (a : α) :
    MeasurableSet (countablePartitionSet n a) :=
  measurableSet_countablePartition n (countablePartitionSet_mem n a)

section CountableOrCountablyGenerated

variable {α γ : Type*} [MeasurableSpace γ]

/-- A class registering that either `α` is countable or `β` is a countably generated
measurable space. -/
/-
**MeasurableSpace.CountableOrCountablyGenerated** 是 Mathlib 中的一个归纳类型，位于命名空间 `Mea
surableSpace`。
形式化陈述：Type u_5 → (β : Type u_6) → [MeasurableSpace β] → Prop
参数：β : Type u_6。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A class registering that either `α` is countable or `β` is a countably generated
measurable space.
-/
class CountableOrCountablyGenerated (α β : Type*) [MeasurableSpace β] :
    Prop where
  countableOrCountablyGenerated : Countable α ∨ MeasurableSpace.CountablyGenerated β
/-
**MeasurableSpace.instCountableOrCountablyGeneratedOfCountable** 是 Mathlib 中的一个实
例，位于命名空间 `MeasurableSpace`。
形式化陈述：instCountableOrCountablyGeneratedOfCountable [h1 : Countable α] [Measurabl
eSpace β] : CountableOrCountablyGenerated α β
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance instCountableOrCountablyGeneratedOfCountable [h1 : Countable α] [MeasurableSpace β] :
    CountableOrCountablyGenerated α β := ⟨Or.inl h1⟩
/-
**MeasurableSpace.instCountableOrCountablyGeneratedOfCountablyGenerated** 是 Math
lib 中的一个实例，位于命名空间 `MeasurableSpace`。
形式化陈述：instCountableOrCountablyGeneratedOfCountablyGenerated [MeasurableSpace β] 
[h : CountablyGenerated β] : CountableOrCountablyGenerated α β
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance instCountableOrCountablyGeneratedOfCountablyGenerated [MeasurableSpace β]
    [h : CountablyGenerated β] :
    CountableOrCountablyGenerated α β := ⟨Or.inr h⟩
/-
**MeasurableSpace.** 是 Mathlib 中的一个实例，位于命名空间 `MeasurableSpace`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [hα : CountableOrCountablyGenerated α γ] [hβ : CountableOrCountablyGenerated β γ] :
    CountableOrCountablyGenerated (α × β) γ := by
  rcases hα with (hα | hα) <;> rcases hβ with (hβ | hβ) <;> infer_instance
/-
**MeasurableSpace.countableOrCountablyGenerated_left_of_prod_left_of_nonempty** 
是 Mathlib 中的一个引理，位于命名空间 `MeasurableSpace`。
形式化陈述：countableOrCountablyGenerated_left_of_prod_left_of_nonempty [Nonempty β] [
h : CountableOrCountablyGenerated (α × β) γ] : CountableOrCountablyGenerated α γ
参数：α × β。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.CountableOrCountablyGenerated.countableOrCountablyGenera
ted`：∀ {α : Type u_5} {β : Type u_6} {inst : MeasurableSpace β} [self : Measurab
leSpace.CountableOrCountablyGenerated α β],   Countable α ∨ Measu…
· 使用引理 `countable_left_of_prod_of_nonempty`：countable_left_of_prod_of_nonempty [
Nonempty β] (h : Countable (α × β)) : Countable α
-/
lemma countableOrCountablyGenerated_left_of_prod_left_of_nonempty [Nonempty β]
    [h : CountableOrCountablyGenerated (α × β) γ] :
    CountableOrCountablyGenerated α γ := by
  rcases h.countableOrCountablyGenerated with (h | h)
  · have := countable_left_of_prod_of_nonempty h
    infer_instance
  · infer_instance
/-
**MeasurableSpace.countableOrCountablyGenerated_right_of_prod_left_of_nonempty**
 是 Mathlib 中的一个引理，位于命名空间 `MeasurableSpace`。
形式化陈述：countableOrCountablyGenerated_right_of_prod_left_of_nonempty [Nonempty α] 
[h : CountableOrCountablyGenerated (α × β) γ] : CountableOrCountablyGenerated β 
γ
参数：α × β。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.CountableOrCountablyGenerated.countableOrCountablyGenera
ted`：∀ {α : Type u_5} {β : Type u_6} {inst : MeasurableSpace β} [self : Measurab
leSpace.CountableOrCountablyGenerated α β],   Countable α ∨ Measu…
· 使用引理 `countable_right_of_prod_of_nonempty`：countable_right_of_prod_of_nonempty
 [Nonempty α] (h : Countable (α × β)) : Countable β
-/
lemma countableOrCountablyGenerated_right_of_prod_left_of_nonempty [Nonempty α]
    [h : CountableOrCountablyGenerated (α × β) γ] :
    CountableOrCountablyGenerated β γ := by
  rcases h.countableOrCountablyGenerated with (h | h)
  · have := countable_right_of_prod_of_nonempty h
    infer_instance
  · infer_instance
/-
**MeasurableSpace.countableOrCountablyGenerated_prod_left_swap** 是 Mathlib 中的一个引
理，位于命名空间 `MeasurableSpace`。
形式化陈述：countableOrCountablyGenerated_prod_left_swap [h : CountableOrCountablyGene
rated (α × β) γ] : CountableOrCountablyGenerated (β × α) γ
参数：α × β。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `countable_prod_swap`：countable_prod_swap [Countable (α × β)] : Countable
 (β × α)
-/
lemma countableOrCountablyGenerated_prod_left_swap [h : CountableOrCountablyGenerated (α × β) γ] :
    CountableOrCountablyGenerated (β × α) γ := by
  rcases h with (h | h)
  · refine ⟨Or.inl countable_prod_swap⟩
  · exact ⟨Or.inr h⟩

end CountableOrCountablyGenerated

end MeasurableSpace

