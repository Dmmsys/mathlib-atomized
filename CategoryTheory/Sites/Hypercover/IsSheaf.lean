/-
Copyright (c) 2024 Joël Riou. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Joël Riou
-/
module

public import Mathlib.CategoryTheory.Sites.Hypercover.One

/-!
# Characterization of sheaves using 1-hypercovers

In this file, given a Grothendieck topology `J` on a category `C`,
we define a type `J.OneHypercoverFamily` of families of 1-hypercovers.
When `H : J.OneHypercoverFamily`, we define a predicate `H.IsGenerating`
which means that any covering sieve contains the sieve generated by
the underlying covering of one of the 1-hypercovers in the family.
If this holds, we show in `OneHypercoverFamily.isSheaf_iff` that a
presheaf `P : Cᵒᵖ ⥤ A` is a sheaf iff for any 1-hypercover `E`
in the family, the multifork `E.multifork P` is a limit.

There is a universe parameter `w` attached to `OneHypercoverFamily` and
`OneHypercover`. This universe controls the "size" of the 1-hypercovers:
the index types involved in the 1-hypercovers have to be in `Type w`.
Then, we introduce a type class
`GrothendieckTopology.IsGeneratedByOneHypercovers.{w} J` as an abbreviation for
`OneHypercoverFamily.IsGenerating.{w} (J := J) ⊤`. We show
that if `C : Type u` and `Category.{v} C`, then
`GrothendieckTopology.IsGeneratedByOneHypercovers.{max u v} J` holds.

## TODO
* Show that functors which preserve 1-hypercovers are continuous.
* Refactor `DenseSubsite` using `1`-hypercovers.

-/

@[expose] public section

universe w v v' u u'

namespace CategoryTheory

open Category Limits

variable {C : Type u} [Category.{v} C] (J : GrothendieckTopology C)
  {A : Type u'} [Category.{v'} A]

namespace GrothendieckTopology

/-- A family of 1-hypercovers consists of the data of a predicate on
`OneHypercover.{w} J X` for all `X`. -/
/-
**CategoryTheory.GrothendieckTopology.OneHypercoverFamily** 是 Mathlib 中的一个缩写定义，位
于命名空间 `CategoryTheory.GrothendieckTopology`。
形式化陈述：OneHypercoverFamily
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A family of 1-hypercovers consists of the data of a predicate on
`OneHypercover.{w} J X` for all `X`.
-/
abbrev OneHypercoverFamily := ∀ ⦃X : C⦄, OneHypercover.{w} J X → Prop

namespace OneHypercoverFamily

variable {J}
variable (H : OneHypercoverFamily.{w} J)

/-- A family of 1-hypercovers generates the topology if any covering sieve
contains the sieve generated by the underlying covering of one of these 1-hypercovers.
See `OneHypercoverFamily.isSheaf_iff` for the characterization of sheaves. -/
/-
**CategoryTheory.GrothendieckTopology.OneHypercoverFamily.IsGenerating** 是 Mathl
ib 中的一个归纳类型，位于命名空间 `CategoryTheory.GrothendieckTopology.OneHypercoverFamily`。
形式化陈述：{C : Type u} →   [inst : CategoryTheory.Category.{v, u} C] → {J : Category
Theory.GrothendieckTopology C} → J.OneHypercoverFamily → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A family of 1-hypercovers generates the topology if any covering sieve
contains the sieve generated by the underlying covering of one of these 1-hyperc
overs.
See `OneHypercoverFamily.isSheaf_iff` for the characterization of sheaves.
-/
class IsGenerating : Prop where
  le {X : C} (S : Sieve X) (hS : S ∈ J X) :
    ∃ (E : J.OneHypercover X) (_ : H E), E.sieve₀ ≤ S
/-
**CategoryTheory.GrothendieckTopology.OneHypercoverFamily.exists_oneHypercover**
 是 Mathlib 中的一个引理，位于命名空间 `CategoryTheory.GrothendieckTopology.OneHypercoverFamil
y`。
形式化陈述：exists_oneHypercover [H.IsGenerating] {X : C} (S : Sieve X) (hS : S in J X
) : exists (E : J.OneHypercover X) (_ : H E), E.sieve₀ <= S
参数：S : Sieve X；hS : S in J X。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.GrothendieckTopology.OneHypercoverFamily.IsGenerating.le`
：∀ {C : Type u} {inst : CategoryTheory.Category.{v, u} C} {J : CategoryTheory.Gr
othendieckTopology C}   {H : J.OneHypercoverFamily} [self : H…
-/
lemma exists_oneHypercover [H.IsGenerating] {X : C} (S : Sieve X) (hS : S ∈ J X) :
    ∃ (E : J.OneHypercover X) (_ : H E), E.sieve₀ ≤ S :=
  IsGenerating.le _ hS

variable (P : Cᵒᵖ ⥤ A)

namespace IsSheafIff

variable (hP : ∀ ⦃X : C⦄ (E : J.OneHypercover X) (_ : H E), Nonempty (IsLimit (E.multifork P)))

include hP in
/-
**CategoryTheory.GrothendieckTopology.OneHypercoverFamily.IsSheafIff.hom_ext** 是
 Mathlib 中的一个引理，位于命名空间 `CategoryTheory.GrothendieckTopology.OneHypercoverFamily.
IsSheafIff`。
形式化陈述：hom_ext [H.IsGenerating] {X : C} (S : Sieve X) (hS : S in J X) {T : A} {x 
y : T ⟶ P.obj (Opposite.op X)} (h : forall ⦃Y : C⦄ (f : Y ⟶ X) (_ : S f), x ≫ P.
map f.op = y ≫ P.map f.op) : x = y
参数：S : Sieve X；hS : S in J X；Opposite.op X；h : forall ⦃Y : C⦄ (f : Y ⟶ X) (_ : S
 f), x ≫ P.map f.op = y ≫ P.map f.op。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `CategoryTheory.GrothendieckTopology.OneHypercoverFamily.exists_oneHyperc
over`：exists_oneHypercover [H.IsGenerating] {X : C} (S : Sieve X) (hS : S in J X
) : exists (E : J.OneHypercover X) (_ : H E), E.sieve₀ <= S
· 使用定理 `CategoryTheory.Limits.Multifork.IsLimit.hom_ext`：∀ {C : Type u} [inst : 
CategoryTheory.Category.{v, u} C] {J : CategoryTheory.Limits.MulticospanShape}  
 {I : CategoryTheory.Limits.Multicosp…
· 使用引理 `CategoryTheory.Sieve.ofArrows_mk`：ofArrows_mk (i : I) : ofArrows Y f (f 
i)
-/
lemma hom_ext [H.IsGenerating] {X : C} (S : Sieve X) (hS : S ∈ J X) {T : A}
    {x y : T ⟶ P.obj (Opposite.op X)}
    (h : ∀ ⦃Y : C⦄ (f : Y ⟶ X) (_ : S f), x ≫ P.map f.op = y ≫ P.map f.op) :
    x = y := by
  obtain ⟨E, hE, le⟩ := H.exists_oneHypercover S hS
  exact Multifork.IsLimit.hom_ext (hP E hE).some (fun j => h _ (le _ (Sieve.ofArrows_mk _ _ _)))

variable {P H}
variable {X : C} {S : Sieve X} {E : J.OneHypercover X} (hE : H E) (le : E.sieve₀ ≤ S)

section

variable (F : Multifork (Cover.index ⟨S, J.superset_covering le E.mem₀⟩ P))

/-- Auxiliary definition for `isLimit`. -/
/-
**CategoryTheory.GrothendieckTopology.OneHypercoverFamily.IsSheafIff.lift** 是 Ma
thlib 中的一个定义，位于命名空间 `CategoryTheory.GrothendieckTopology.OneHypercoverFamily.IsS
heafIff`。
形式化陈述：lift : F.pt ⟶ P.obj (Opposite.op X)
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Auxiliary definition for `isLimit`.
-/
noncomputable def lift : F.pt ⟶ P.obj (Opposite.op X) :=
  Multifork.IsLimit.lift (hP E hE).some
    (fun i => F.ι ⟨_, E.f i, le _ (Sieve.ofArrows_mk _ _ _)⟩)
    (fun ⟨⟨i₁, i₂⟩, j⟩ =>
      F.condition {
        fst := { hf := le _ (Sieve.ofArrows_mk _ _ i₁), .. }
        snd := { hf := le _ (Sieve.ofArrows_mk _ _ i₂), .. }
        r := { w := E.w j, .. }
      })

@[reassoc]
/-
**CategoryTheory.GrothendieckTopology.OneHypercoverFamily.IsSheafIff.fac'** 是 Ma
thlib 中的一个引理，位于命名空间 `CategoryTheory.GrothendieckTopology.OneHypercoverFamily.IsS
heafIff`。
形式化陈述：fac' (i : E.I₀) : lift hP hE le F ≫ P.map (E.f i).op = F.ι ⟨_, E.f i, le _
 (Sieve.ofArrows_mk _ _ _)⟩
参数：i : E.I₀。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.GrothendieckTopology.superset_covering`：superset_covering
 (Hss : S <= R) (sjx : S in J X) : R in J X
· 使用定理 `CategoryTheory.GrothendieckTopology.OneHypercover.mem₀`：∀ {C : Type u} [
inst : CategoryTheory.Category.{v, u} C] {J : CategoryTheory.GrothendieckTopolog
y C} {S : C}   (self : J.OneHypercover S), s…
· 使用定理 `CategoryTheory.Limits.Multifork.IsLimit.fac`：∀ {C : Type u} [inst : Cate
goryTheory.Category.{v, u} C] {J : CategoryTheory.Limits.MulticospanShape}   {I 
: CategoryTheory.Limits.Multicosp…
-/
lemma fac' (i : E.I₀) :
    lift hP hE le F ≫ P.map (E.f i).op =
      F.ι ⟨_, E.f i, le _ (Sieve.ofArrows_mk _ _ _)⟩ :=
  Multifork.IsLimit.fac (hP E hE).some _ _ i
/-
**CategoryTheory.GrothendieckTopology.OneHypercoverFamily.IsSheafIff.fac** 是 Mat
hlib 中的一个引理，位于命名空间 `CategoryTheory.GrothendieckTopology.OneHypercoverFamily.IsSh
eafIff`。
形式化陈述：fac [H.IsGenerating] {Y : C} (f : Y ⟶ X) (hf : S f) : lift hP hE le F ≫ P.
map f.op = F.ι ⟨Y, f, hf⟩
参数：f : Y ⟶ X；hf : S f。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.GrothendieckTopology.superset_covering`：superset_covering
 (Hss : S <= R) (sjx : S in J X) : R in J X
· 使用定理 `CategoryTheory.GrothendieckTopology.OneHypercover.mem₀`：∀ {C : Type u} [
inst : CategoryTheory.Category.{v, u} C] {J : CategoryTheory.GrothendieckTopolog
y C} {S : C}   (self : J.OneHypercover S), s…
· 使用引理 `CategoryTheory.GrothendieckTopology.OneHypercoverFamily.IsSheafIff.hom_e
xt`：hom_ext [H.IsGenerating] {X : C} (S : Sieve X) (hS : S in J X) {T : A} {x y 
: T ⟶ P.obj (Opposite.op X)} (h : forall ⦃Y : C⦄ (f : Y ⟶ X) (_ …
· 使用定理 `CategoryTheory.GrothendieckTopology.pullback_stable`：pullback_stable (f 
: Y ⟶ X) (hS : S in J X) : S.pullback f in J Y
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `CategoryTheory.Category.assoc`：∀ {obj : Type u} [self : CategoryTheory.C
ategory.{v, u} obj] {W X Y Z : obj} (f : W ⟶ X) (g : X ⟶ Y) (h : Y ⟶ Z),   Categ
oryTheory.CategoryS…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `CategoryTheory.Functor.map_comp`：∀ {C : Type u₁} [inst : CategoryTheory.
Category.{v₁, u₁} C] {D : Type u₂} [inst_1 : CategoryTheory.Category.{v₂, u₂} D]
   (self : CategoryTh…
· 使用定理 `CategoryTheory.op_comp`：op_comp {X Y Z : C} {f : X ⟶ Y} {g : Y ⟶ Z} : (f
 ≫ g).op = g.op ≫ f.op
· 使用引理 `CategoryTheory.Sieve.ofArrows_mk`：ofArrows_mk (i : I) : ofArrows Y f (f 
i)
· 使用定理 `CategoryTheory.GrothendieckTopology.OneHypercoverFamily.IsSheafIff.fac'_
assoc`：∀ {C : Type u} [inst : CategoryTheory.Category.{v, u} C] {J : CategoryThe
ory.GrothendieckTopology C} {A : Type u'}   [inst_1 : CategoryTheor…
· 使用定理 `CategoryTheory.Limits.Multifork.condition`：condition (b) : K.ι (J.fst b)
 ≫ I.fst b = K.ι (J.snd b) ≫ I.snd b
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
-/
lemma fac [H.IsGenerating] {Y : C} (f : Y ⟶ X) (hf : S f) :
    lift hP hE le F ≫ P.map f.op = F.ι ⟨Y, f, hf⟩ := by
  apply hom_ext H P hP _ (J.pullback_stable f E.mem₀)
  intro Z g
  rintro ⟨T, a, b, ⟨i⟩, fac⟩
  rw [assoc, ← P.map_comp, ← op_comp, ← fac,
    op_comp, P.map_comp, fac'_assoc]
  exact F.condition {
    fst := { hf := le _ (Sieve.ofArrows_mk _ _ _), .. }
    snd := { hf := hf, .. }
    r := { w := fac, .. }
  }

end

/-- Auxiliary definition for `OneHypercoverFamily.isSheaf_iff`. -/
/-
**CategoryTheory.GrothendieckTopology.OneHypercoverFamily.IsSheafIff.isLimit** 是
 Mathlib 中的一个定义，位于命名空间 `CategoryTheory.GrothendieckTopology.OneHypercoverFamily.
IsSheafIff`。
形式化陈述：isLimit [H.IsGenerating] : IsLimit (Cover.multifork ⟨S, J.superset_coverin
g le E.mem₀⟩ P)
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Auxiliary definition for `OneHypercoverFamily.isSheaf_iff`.
-/
noncomputable def isLimit [H.IsGenerating] :
    IsLimit (Cover.multifork ⟨S, J.superset_covering le E.mem₀⟩ P) :=
  Multifork.IsLimit.mk _
    (fun F => lift hP hE le F)
    (fun F => by
      rintro ⟨Y, f, hf⟩
      apply fac)
    (fun F m hm => by
      apply hom_ext H P hP S (J.superset_covering le E.mem₀)
      intro Y f hf
      rw [fac _ _ _ _ _ hf]
      exact hm ⟨_, _, hf⟩)

end IsSheafIff

/-
**CategoryTheory.GrothendieckTopology.OneHypercoverFamily.isSheaf_iff** 是 Mathli
b 中的一个引理，位于命名空间 `CategoryTheory.GrothendieckTopology.OneHypercoverFamily`。
形式化陈述：isSheaf_iff [H.IsGenerating] : Presheaf.IsSheaf J P ↔ forall ⦃X : C⦄ (E : 
J.OneHypercover X) (_ : H E), Nonempty (IsLimit (E.multifork P))
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `CategoryTheory.Presheaf.isSheaf_iff_multifork`：isSheaf_iff_multifork : I
sSheaf J P ↔ forall (X : C) (S : J.Cover X), Nonempty (IsLimit (S.multifork P))
· 使用引理 `CategoryTheory.GrothendieckTopology.OneHypercoverFamily.exists_oneHyperc
over`：exists_oneHypercover [H.IsGenerating] {X : C} (S : Sieve X) (hS : S in J X
) : exists (E : J.OneHypercover X) (_ : H E), E.sieve₀ <= S
-/
lemma isSheaf_iff [H.IsGenerating] :
    Presheaf.IsSheaf J P ↔ ∀ ⦃X : C⦄ (E : J.OneHypercover X)
      (_ : H E), Nonempty (IsLimit (E.multifork P)) := by
  constructor
  · intro hP X E _
    exact ⟨E.isLimitMultifork ⟨_, hP⟩⟩
  · intro hP
    rw [Presheaf.isSheaf_iff_multifork]
    rintro X ⟨S, hS⟩
    obtain ⟨E, hE, le⟩ := H.exists_oneHypercover S hS
    exact ⟨IsSheafIff.isLimit hP hE le⟩

end OneHypercoverFamily

/-- Condition that a topology is generated by 1-hypercovers of size `w`. -/
/-
**CategoryTheory.GrothendieckTopology.IsGeneratedByOneHypercovers** 是 Mathlib 中的
一个缩写定义，位于命名空间 `CategoryTheory.GrothendieckTopology`。
形式化陈述：IsGeneratedByOneHypercovers : Prop
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Condition that a topology is generated by 1-hypercovers of size `w`.
-/
abbrev IsGeneratedByOneHypercovers : Prop :=
  OneHypercoverFamily.IsGenerating.{w} (J := J) ⊤

set_option backward.isDefEq.respectTransparency.types false in
/-
**CategoryTheory.GrothendieckTopology.** 是 Mathlib 中的一个实例，位于命名空间 `CategoryTheory
.GrothendieckTopology`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : IsGeneratedByOneHypercovers.{max u v} J where
  le S hS := ⟨Cover.oneHypercover ⟨S, hS⟩, by simp, by simp⟩

end GrothendieckTopology

namespace Presheaf

/-
**CategoryTheory.Presheaf.isSheaf_iff_of_isGeneratedByOneHypercovers** 是 Mathlib
 中的一个引理，位于命名空间 `CategoryTheory.Presheaf`。
形式化陈述：isSheaf_iff_of_isGeneratedByOneHypercovers [GrothendieckTopology.IsGenerat
edByOneHypercovers.{w} J] (P : Cᵒᵖ ⥤ A) : IsSheaf J P ↔ forall ⦃X : C⦄ (E : Grot
hendieckTopology.OneHypercover.{w} J X), Nonempty (IsLimit (E.multifork P))
参数：P : Cᵒᵖ ⥤ A。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `instNonemptyOfInhabited`：∀ {α : Sort u} [Inhabited α], Nonempty α
· 使用引理 `CategoryTheory.GrothendieckTopology.OneHypercoverFamily.isSheaf_iff`：isS
heaf_iff [H.IsGenerating] : Presheaf.IsSheaf J P ↔ forall ⦃X : C⦄ (E : J.OneHype
rcover X) (_ : H E), Nonempty (IsLimit (E.multifork P))
-/
lemma isSheaf_iff_of_isGeneratedByOneHypercovers
    [GrothendieckTopology.IsGeneratedByOneHypercovers.{w} J] (P : Cᵒᵖ ⥤ A) :
    IsSheaf J P ↔ ∀ ⦃X : C⦄ (E : GrothendieckTopology.OneHypercover.{w} J X),
        Nonempty (IsLimit (E.multifork P)) := by
  simpa using GrothendieckTopology.OneHypercoverFamily.isSheaf_iff.{w} ⊤ P

end Presheaf

end CategoryTheory

