/-
Copyright (c) 2018 Kim Morrison. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Kim Morrison, Reid Barton
-/
module

public import Mathlib.CategoryTheory.Limits.Types.Colimits
public import Mathlib.CategoryTheory.Filtered.Basic

/-!
# Filtered colimits in the category of types.

We give a characterisation of the equality in filtered colimits in `Type` as a
lemma `CategoryTheory.Limits.Types.FilteredColimit.colimit_eq_iff`:
`colimit.ι F i xi = colimit.ι F j xj ↔ ∃ k (f : i ⟶ k) (g : j ⟶ k), F.map f xi = F.map g xj`.

-/

@[expose] public section

open CategoryTheory Limits ConcreteCategory

universe v u w

namespace CategoryTheory.Limits.Types.FilteredColimit

variable {J : Type v} [Category.{w} J] (F : J ⥤ Type u)

attribute [local instance] small_colimitType_of_hasColimit

/- For filtered colimits of types, we can give an explicit description
  of the equivalence relation generated by the relation used to form
  the colimit. -/

/-- An alternative relation on `Σ j, F.obj j`,
which generates the same equivalence relation as we use to define the colimit in `Type` above,
but that is more convenient when working with filtered colimits.

Elements in `F.obj j` and `F.obj j'` are equivalent if there is some `k : J` to the right
where their images are equal.
-/
/-
**CategoryTheory.Limits.Types.FilteredColimit.Rel** 是 Mathlib 中的一个定义，位于命名空间 `Cat
egoryTheory.Limits.Types.FilteredColimit`。
形式化陈述：{J : Type v} →   [inst : CategoryTheory.Category.{w, v} J] →     (F : Cate
goryTheory.Functor J (Type u)) → (j : J) × F.obj j → (j : J) × F.obj j → Prop
参数：F : CategoryTheory.Functor J (Type u)；j : J；j : J。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
An alternative relation on `Σ j, F.obj j`,
which generates the same equivalence relation as we use to define the colimit in
 `Type` above,
but that is more convenient when working with filtered colimits.

Elements in `F.obj j` and `F.obj j'` are equivalent if there is some `k : J` to 
the right
where their images are equal.
-/
protected def Rel (x y : Σ j, F.obj j) : Prop :=
  ∃ (k : _) (f : x.1 ⟶ k) (g : y.1 ⟶ k), F.map f x.2 = F.map g y.2
/-
**CategoryTheory.Limits.Types.FilteredColimit.rel_of_colimitTypeRel** 是 Mathlib 
中的一个定理，位于命名空间 `CategoryTheory.Limits.Types.FilteredColimit`。
形式化陈述：rel_of_colimitTypeRel (x y : Σ j, F.obj j) : F.ColimitTypeRel x y -> Filte
redColimit.Rel.{v, u} F x y
参数：x y : Σ j, F.obj j。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `CategoryTheory.Functor.map_id`：∀ {C : Type u₁} [inst : CategoryTheory.Ca
tegory.{v₁, u₁} C] {D : Type u₂} [inst_1 : CategoryTheory.Category.{v₂, u₂} D]  
 (self : CategoryTh…
· 使用定理 `CategoryTheory.id_apply`：∀ {C : Type u} [inst : CategoryTheory.Category.
{v, u} C] {FC : C → C → Type u_1} {CC : C → Type w}   [inst_1 : (X Y : C) → FunL
ike (FC X Y) …
-/
theorem rel_of_colimitTypeRel (x y : Σ j, F.obj j) :
    F.ColimitTypeRel x y → FilteredColimit.Rel.{v, u} F x y :=
  fun ⟨f, h⟩ => ⟨y.1, f, 𝟙 y.1, by rw [← h, Functor.map_id, id_apply]⟩
/-
**CategoryTheory.Limits.Types.FilteredColimit.eqvGen_colimitTypeRel_of_rel** 是 M
athlib 中的一个定理，位于命名空间 `CategoryTheory.Limits.Types.FilteredColimit`。
形式化陈述：eqvGen_colimitTypeRel_of_rel (x y : Σ j, F.obj j) : FilteredColimit.Rel.{v
, u} F x y -> Relation.EqvGen F.ColimitTypeRel x y
参数：x y : Σ j, F.obj j。
该定理/引理描述了相关对象所满足的性质。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem eqvGen_colimitTypeRel_of_rel (x y : Σ j, F.obj j) :
    FilteredColimit.Rel.{v, u} F x y → Relation.EqvGen F.ColimitTypeRel x y :=
  fun ⟨k, f, g, h⟩ => by
    refine Relation.EqvGen.trans _ ⟨k, F.map f x.2⟩ _ ?_ ?_
    · exact (Relation.EqvGen.rel _ _ ⟨f, rfl⟩)
    · exact (Relation.EqvGen.symm _ _ (Relation.EqvGen.rel _ _ ⟨g, h⟩))

set_option backward.defeqAttrib.useBackward true in
set_option backward.isDefEq.respectTransparency false in
/-- Recognizing filtered colimits of types. -/
/-
**CategoryTheory.Limits.Types.FilteredColimit.isColimitOf** 是 Mathlib 中的一个定义，位于命
名空间 `CategoryTheory.Limits.Types.FilteredColimit`。
形式化陈述：isColimitOf (t : Cocone F) (hsurj : forall x : t.pt, exists i xi, x = t.ι.
app i xi) (hinj : forall i j xi xj, t.ι.app i xi = t.ι.app j xj -> exists (k : _
) (f : i ⟶ k) (g : j ⟶ k), F.map f xi = F.map g xj) : IsColimit t
参数：t : Cocone F；hsurj : forall x : t.pt, exists i xi, x = t.ι.app i xi；hinj : fo
rall i j xi xj, t.ι.app i xi = t.ι.app j xj -> exists (k : _) (f : i ⟶ k) (g : j
 ⟶ k), F.map f xi = F.map g xj。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Recognizing filtered colimits of types.
-/
noncomputable def isColimitOf (t : Cocone F) (hsurj : ∀ x : t.pt, ∃ i xi, x = t.ι.app i xi)
    (hinj :
      ∀ i j xi xj,
        t.ι.app i xi = t.ι.app j xj → ∃ (k : _) (f : i ⟶ k) (g : j ⟶ k), F.map f xi = F.map g xj) :
    IsColimit t := by
  let α : t.pt → J := fun x => (hsurj x).choose
  let f : ∀ (x : t.pt), F.obj (α x) := fun x => (hsurj x).choose_spec.choose
  have hf : ∀ (x : t.pt), x = t.ι.app _ (f x) := fun x => (hsurj x).choose_spec.choose_spec
  exact
    { desc s := ↾fun x => s.ι.app _ (f x)
      fac := fun s j => by
        ext y
        obtain ⟨k, l, g, eq⟩ := hinj _ _ _ _ (hf (t.ι.app j y))
        have h := congr_hom (s.ι.naturality g) (f (t.ι.app j y))
        have h' := congr_hom (s.ι.naturality l) y
        simp only [Functor.const_obj_obj, comp_apply, Functor.const_obj_map, Category.comp_id,
          TypeCat.Fun.toFun_apply, hom_ofHom, TypeCat.Fun.coe_mk] at h h' ⊢
        rw [← h, ← eq, h']
      uniq := fun s m hm => by
        ext x
        dsimp
        nth_rw 1 [hf x]
        simp [← hm, comp_apply] }

variable [IsFilteredOrEmpty J]

#adaptation_note
/--
Note that the body of `isColimitOf'` locally enables `respectTransparency true` for a subterm.
The underlying reason is that there is one corner case where `respectTransparency true` unfolds
*more*, and we need that here.
The intended fix is to get rid of the outer `respectTransparency false`. After that, the inner
`respectTransparency true` is redundant and can be removed, too.
-/
set_option backward.defeqAttrib.useBackward true in
set_option backward.isDefEq.respectTransparency false in
/-- Recognizing filtered colimits of types. The injectivity condition here is
slightly easier to check as compared to `isColimitOf`. -/
/-
**CategoryTheory.Limits.Types.FilteredColimit.isColimitOf'** 是 Mathlib 中的一个定义，位于
命名空间 `CategoryTheory.Limits.Types.FilteredColimit`。
形式化陈述：isColimitOf' (t : Cocone F) (hsurj : forall x : t.pt, exists i xi, x = t.ι
.app i xi) (hinj : forall i x y, t.ι.app i x = t.ι.app i y -> exists (k : _) (f 
: i ⟶ k), F.map f x = F.map f y) : IsColimit t
参数：t : Cocone F；hsurj : forall x : t.pt, exists i xi, x = t.ι.app i xi；hinj : fo
rall i x y, t.ι.app i x = t.ι.app i y -> exists (k : _) (f : i ⟶ k), F.map f x =
 F.map f y。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Recognizing filtered colimits of types. The injectivity condition here is
slightly easier to check as compared to `isColimitOf`.
-/
noncomputable def isColimitOf' (t : Cocone F) (hsurj : ∀ x : t.pt, ∃ i xi, x = t.ι.app i xi)
    (hinj : ∀ i x y, t.ι.app i x = t.ι.app i y → ∃ (k : _) (f : i ⟶ k), F.map f x = F.map f y) :
    IsColimit t :=
  isColimitOf _ _ hsurj (fun i j xi xj h ↦ by
    obtain ⟨k, g, hg⟩ := hinj (IsFiltered.max i j) (F.map (IsFiltered.leftToMax i j) xi)
      (F.map (IsFiltered.rightToMax i j) xj)
      (by set_option backward.isDefEq.respectTransparency true in simp_all)
    exact ⟨k, IsFiltered.leftToMax i j ≫ g, IsFiltered.rightToMax i j ≫ g, by simpa using hg⟩)
/-
**CategoryTheory.Limits.Types.FilteredColimit.rel_equiv** 是 Mathlib 中的一个定理，位于命名空
间 `CategoryTheory.Limits.Types.FilteredColimit`。
形式化陈述：∀ {J : Type v} [inst : CategoryTheory.Category.{w, v} J] (F : CategoryTheo
ry.Functor J (Type u))   [CategoryTheory.IsFilteredOrEmpty J], Equivalence (Cate
goryTheory.Limits.Types.FilteredColimit.Rel F)
参数：F : CategoryTheory.Functor J (Type u)；CategoryTheory.Limits.Types.FilteredCol
imit.Rel F。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `CategoryTheory.IsFilteredOrEmpty.cocone_objs`：∀ {C : Type u} {inst : Cat
egoryTheory.Category.{v, u} C} [self : CategoryTheory.IsFilteredOrEmpty C] (X Y 
: C),   ∃ Z x x, True
· 使用定理 `CategoryTheory.IsFilteredOrEmpty.cocone_maps`：∀ {C : Type u} {inst : Cat
egoryTheory.Category.{v, u} C} [self : CategoryTheory.IsFilteredOrEmpty C] ⦃X Y 
: C⦄   (f g : X ⟶ Y), ∃ Z h, Categ…
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `CategoryTheory.types_congr_hom`：types_congr_hom {X Y : Type u} {f g : X 
⟶ Y} (h : f = g) (x : X) : f x = g x
· 使用定理 `CategoryTheory.Functor.map_comp`：∀ {C : Type u₁} [inst : CategoryTheory.
Category.{v₁, u₁} C] {D : Type u₂} [inst_1 : CategoryTheory.Category.{v₂, u₂} D]
   (self : CategoryTh…
· 使用定理 `CategoryTheory.comp_apply`：∀ {C : Type u} [inst : CategoryTheory.Categor
y.{v, u} C] {FC : C → C → Type u_1} {CC : C → Type w}   [inst_1 : (X Y : C) → Fu
nLike (FC X Y) …
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `CategoryTheory.Category.assoc`：∀ {obj : Type u} [self : CategoryTheory.C
ategory.{v, u} obj] {W X Y Z : obj} (f : W ⟶ X) (g : X ⟶ Y) (h : Y ⟶ Z),   Categ
oryTheory.CategoryS…
-/
protected theorem rel_equiv : _root_.Equivalence (FilteredColimit.Rel.{v, u} F) where
  refl x := ⟨x.1, 𝟙 x.1, 𝟙 x.1, rfl⟩
  symm := fun ⟨k, f, g, h⟩ => ⟨k, g, f, h.symm⟩
  trans {x y z} := fun ⟨k, f, g, h⟩ ⟨k', f', g', h'⟩ =>
    let ⟨l, fl, gl, _⟩ := IsFilteredOrEmpty.cocone_objs k k'
    let ⟨m, n, hn⟩ := IsFilteredOrEmpty.cocone_maps (g ≫ fl) (f' ≫ gl)
    ⟨m, f ≫ fl ≫ n, g' ≫ gl ≫ n,
      calc
        F.map (f ≫ fl ≫ n) x.2 = F.map (fl ≫ n) (F.map f x.2) := by simp
        _ = F.map (fl ≫ n) (F.map g y.2) := by rw [h]
        _ = F.map ((g ≫ fl) ≫ n) y.2 := by simp
        _ = F.map ((f' ≫ gl) ≫ n) y.2 := by rw [hn]
        _ = F.map (gl ≫ n) (F.map f' y.2) := by simp
        _ = F.map (gl ≫ n) (F.map g' z.2) := by rw [h']
        _ = F.map (g' ≫ gl ≫ n) z.2 := by simp⟩
/-
**CategoryTheory.Limits.Types.FilteredColimit.rel_eq_eqvGen_colimitTypeRel** 是 M
athlib 中的一个定理，位于命名空间 `CategoryTheory.Limits.Types.FilteredColimit`。
形式化陈述：∀ {J : Type v} [inst : CategoryTheory.Category.{w, v} J] (F : CategoryTheo
ry.Functor J (Type u))   [CategoryTheory.IsFilteredOrEmpty J],   CategoryTheory.
Limits.Types.FilteredColimit.Rel F = Relation.EqvGen F.ColimitTypeRel
参数：F : CategoryTheory.Functor J (Type u)。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `CategoryTheory.Limits.Types.FilteredColimit.eqvGen_colimitTypeRel_of_rel
`：eqvGen_colimitTypeRel_of_rel (x y : Σ j, F.obj j) : FilteredColimit.Rel.{v, u}
 F x y -> Relation.EqvGen F.ColimitTypeRel x y
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Equivalence.eqvGen_iff`：Equivalence.eqvGen_iff (h : Equivalence r) : Eqv
Gen r a b ↔ r a b
· 使用定理 `CategoryTheory.Limits.Types.FilteredColimit.rel_equiv`：∀ {J : Type v} [i
nst : CategoryTheory.Category.{w, v} J] (F : CategoryTheory.Functor J (Type u)) 
  [CategoryTheory.IsFilteredOrEmpty J], Equ…
· 使用定理 `Relation.EqvGen.mono`：mono {r p : α -> α -> Prop} (hrp : r <= p) : EqvGe
n r <= EqvGen p
· 使用定理 `CategoryTheory.Limits.Types.FilteredColimit.rel_of_colimitTypeRel`：rel_o
f_colimitTypeRel (x y : Σ j, F.obj j) : F.ColimitTypeRel x y -> FilteredColimit.
Rel.{v, u} F x y
-/
protected theorem rel_eq_eqvGen_colimitTypeRel :
    FilteredColimit.Rel.{v, u} F = Relation.EqvGen F.ColimitTypeRel := by
  ext ⟨j, x⟩ ⟨j', y⟩
  constructor
  · apply eqvGen_colimitTypeRel_of_rel
  · rw [← (FilteredColimit.rel_equiv F).eqvGen_iff]
    exact Relation.EqvGen.mono (rel_of_colimitTypeRel F) _ _

set_option backward.defeqAttrib.useBackward true in
set_option backward.isDefEq.respectTransparency false in
/-
**CategoryTheory.Limits.Types.FilteredColimit.colimit_eq_iff_aux** 是 Mathlib 中的一
个定理，位于命名空间 `CategoryTheory.Limits.Types.FilteredColimit`。
形式化陈述：colimit_eq_iff_aux [HasColimit F] {i j : J} {xi : F.obj i} {xj : F.obj j} 
: (colimitCocone F).ι.app i xi = (colimitCocone F).ι.app j xj ↔ FilteredColimit.
Rel.{v, u} F ⟨i, xi⟩ ⟨j, xj⟩
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Limits.Types.small_colimitType_of_hasColimit`：small_colim
itType_of_hasColimit (F : J ⥤ Type u) [HasColimit F] : Small.{u} F.ColimitType
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Function.Injective.eq_iff`：∀ {α : Sort u_1} {β : Sort u_2} {f : α → β}, 
Function.Injective f → ∀ {a b : α}, f a = f b ↔ a = b
· 使用定理 `Equiv.injective`：∀ {α : Sort u} {β : Sort v} (e : α ≃ β), Function.Injec
tive ⇑e
· 使用定理 `Equiv.symm_apply_apply`：∀ {α : Sort u} {β : Sort v} (e : α ≃ β) (x : α),
 e.symm (e x) = x
· 使用引理 `CategoryTheory.Functor.ιColimitType_eq_iff`：ιColimitType_eq_iff {j j' : 
J} (x : F.obj j) (y : F.obj j') : F.ιColimitType j x = F.ιColimitType j' y ↔ Rel
ation.EqvGen F.ColimitTypeRel ⟨j…
· 使用定理 `CategoryTheory.Limits.Types.FilteredColimit.rel_eq_eqvGen_colimitTypeRel
`：∀ {J : Type v} [inst : CategoryTheory.Category.{w, v} J] (F : CategoryTheory.F
unctor J (Type u))   [CategoryTheory.IsFilteredOrEmpty J],   C…
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem colimit_eq_iff_aux [HasColimit F] {i j : J} {xi : F.obj i} {xj : F.obj j} :
    (colimitCocone F).ι.app i xi = (colimitCocone F).ι.app j xj ↔
      FilteredColimit.Rel.{v, u} F ⟨i, xi⟩ ⟨j, xj⟩ := by
  dsimp
  rw [← (equivShrink _).symm.injective.eq_iff, Equiv.symm_apply_apply, Equiv.symm_apply_apply,
    Functor.ιColimitType_eq_iff, FilteredColimit.rel_eq_eqvGen_colimitTypeRel]

set_option backward.isDefEq.respectTransparency false in
/-
**CategoryTheory.Limits.Types.FilteredColimit.isColimit_eq_iff** 是 Mathlib 中的一个定
理，位于命名空间 `CategoryTheory.Limits.Types.FilteredColimit`。
形式化陈述：isColimit_eq_iff {t : Cocone F} (ht : IsColimit t) {i j : J} {xi : F.obj i
} {xj : F.obj j} : t.ι.app i xi = t.ι.app j xj ↔ exists (k : _) (f : i ⟶ k) (g :
 j ⟶ k), F.map f xi = F.map g xj
参数：ht : IsColimit t。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `CategoryTheory.Limits.Types.small_colimitType_of_hasColimit`：small_colim
itType_of_hasColimit (F : J ⥤ Type u) [HasColimit F] : Small.{u} F.ColimitType
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Function.Injective.eq_iff`：∀ {α : Sort u_1} {β : Sort u_2} {f : α → β}, 
Function.Injective f → ∀ {a b : α}, f a = f b ↔ a = b
· 使用定理 `Equiv.injective`：∀ {α : Sort u} {β : Sort v} (e : α ≃ β), Function.Injec
tive ⇑e
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `heq_of_eq`：∀ {α : Sort u_1} {a a' : α}, a = a' → a ≍ a'
· 使用定理 `CategoryTheory.ConcreteCategory.congr_hom`：congr_hom {X Y : C} {f g : X 
⟶ Y} (h : f = g) (x : ToType X) : f x = g x
· 使用定理 `CategoryTheory.Limits.IsColimit.comp_coconePointUniqueUpToIso_hom`：∀ {J 
: Type u₁} [inst : CategoryTheory.Category.{v₁, u₁} J] {C : Type u₃} [inst_1 : C
ategoryTheory.Category.{v₃, u₃} C]   {F : CategoryTheor…
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
· 使用定理 `CategoryTheory.Limits.Types.FilteredColimit.colimit_eq_iff_aux`：colimit_
eq_iff_aux [HasColimit F] {i j : J} {xi : F.obj i} {xj : F.obj j} : (colimitCoco
ne F).ι.app i xi = (colimitCocone F).ι.app j xj ↔ Fi…
-/
theorem isColimit_eq_iff {t : Cocone F} (ht : IsColimit t) {i j : J} {xi : F.obj i} {xj : F.obj j} :
    t.ι.app i xi = t.ι.app j xj ↔ ∃ (k : _) (f : i ⟶ k) (g : j ⟶ k), F.map f xi = F.map g xj := by
  have : HasColimit F := ⟨_, ht⟩
  refine Iff.trans ?_ (colimit_eq_iff_aux F)
  rw [← (IsColimit.coconePointUniqueUpToIso ht (colimitCoconeIsColimit F)).toEquiv.injective.eq_iff]
  convert! Iff.rfl
  · exact (congr_hom
      (IsColimit.comp_coconePointUniqueUpToIso_hom ht (colimitCoconeIsColimit F) _) xi).symm
  · exact (congr_hom
      (IsColimit.comp_coconePointUniqueUpToIso_hom ht (colimitCoconeIsColimit F) _) xj).symm

variable {F} in
/-
**CategoryTheory.Limits.Types.FilteredColimit.isColimit_eq_iff'** 是 Mathlib 中的一个
定理，位于命名空间 `CategoryTheory.Limits.Types.FilteredColimit`。
形式化陈述：isColimit_eq_iff' {t : Cocone F} (ht : IsColimit t) {i : J} (x y : F.obj i
) : t.ι.app i x = t.ι.app i y ↔ exists (j : _) (f : i ⟶ j), F.map f x = F.map f 
y
参数：ht : IsColimit t；x y : F.obj i。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `CategoryTheory.Limits.Types.FilteredColimit.isColimit_eq_iff`：isColimit_
eq_iff {t : Cocone F} (ht : IsColimit t) {i j : J} {xi : F.obj i} {xj : F.obj j}
 : t.ι.app i xi = t.ι.app j xj ↔ exists (k : _) (f…
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `CategoryTheory.IsFiltered.coeq_condition`：coeq_condition {j j' : C} (f f
' : j ⟶ j') : f ≫ coeqHom f f' = f' ≫ coeqHom f f'
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用引理 `CategoryTheory.types_congr_hom`：types_congr_hom {X Y : Type u} {f g : X 
⟶ Y} (h : f = g) (x : X) : f x = g x
· 使用定理 `CategoryTheory.Functor.map_comp`：∀ {C : Type u₁} [inst : CategoryTheory.
Category.{v₁, u₁} C] {D : Type u₂} [inst_1 : CategoryTheory.Category.{v₂, u₂} D]
   (self : CategoryTh…
· 使用定理 `CategoryTheory.comp_apply`：∀ {C : Type u} [inst : CategoryTheory.Categor
y.{v, u} C] {FC : C → C → Type u_1} {CC : C → Type w}   [inst_1 : (X Y : C) → Fu
nLike (FC X Y) …
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem isColimit_eq_iff' {t : Cocone F} (ht : IsColimit t) {i : J} (x y : F.obj i) :
    t.ι.app i x = t.ι.app i y ↔ ∃ (j : _) (f : i ⟶ j), F.map f x = F.map f y := by
  rw [isColimit_eq_iff _ ht]
  constructor
  · rintro ⟨k, f, g, h⟩
    refine ⟨IsFiltered.coeq f g, f ≫ IsFiltered.coeqHom f g, ?_⟩
    conv_rhs => rw [IsFiltered.coeq_condition]
    simp [h]
  · rintro ⟨j, f, h⟩
    exact ⟨j, f, f, h⟩
/-
**CategoryTheory.Limits.Types.FilteredColimit.colimit_eq_iff** 是 Mathlib 中的一个定理，
位于命名空间 `CategoryTheory.Limits.Types.FilteredColimit`。
形式化陈述：colimit_eq_iff [HasColimit F] {i j : J} {xi : F.obj i} {xj : F.obj j} : co
limit.ι F i xi = colimit.ι F j xj ↔ exists (k : _) (f : i ⟶ k) (g : j ⟶ k), F.ma
p f xi = F.map g xj
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Limits.Types.FilteredColimit.isColimit_eq_iff`：isColimit_
eq_iff {t : Cocone F} (ht : IsColimit t) {i j : J} {xi : F.obj i} {xj : F.obj j}
 : t.ι.app i xi = t.ι.app j xj ↔ exists (k : _) (f…
-/
theorem colimit_eq_iff [HasColimit F] {i j : J} {xi : F.obj i} {xj : F.obj j} :
    colimit.ι F i xi = colimit.ι F j xj ↔
      ∃ (k : _) (f : i ⟶ k) (g : j ⟶ k), F.map f xi = F.map g xj :=
  isColimit_eq_iff _ (colimit.isColimit F)

open IsFiltered in
variable {F} in
/-
**CategoryTheory.Limits.Types.FilteredColimit.jointly_surjective_of_isColimit** 
是 Mathlib 中的一个引理，位于命名空间 `CategoryTheory.Limits.Types.FilteredColimit`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma jointly_surjective_of_isColimit₂
    {t : Cocone F} (ht : IsColimit t) (x₁ x₂ : t.pt) :
    ∃ (j : J) (x₁' x₂' : F.obj j), t.ι.app j x₁' = x₁ ∧ t.ι.app j x₂' = x₂ := by
  obtain ⟨j₁, x₁, rfl⟩ := Types.jointly_surjective_of_isColimit ht x₁
  obtain ⟨j₂, x₂, rfl⟩ := Types.jointly_surjective_of_isColimit ht x₂
  exact ⟨max j₁ j₂, F.map (leftToMax _ _) x₁, F.map (rightToMax _ _) x₂,
    congr_hom (t.w (leftToMax j₁ j₂)) x₁, congr_hom (t.w (rightToMax j₁ j₂)) x₂⟩

end CategoryTheory.Limits.Types.FilteredColimit

