/-
Copyright (c) 2024 Michael Stoll. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Michael Stoll
-/
module

public import Mathlib.NumberTheory.MulChar.Basic
public import Mathlib.RingTheory.RootsOfUnity.Complex

/-!
# Further Results on multiplicative characters
-/

@[expose] public section

namespace MulChar

section CommMonoid

variable {R : Type*} [CommMonoid R] {S : Type*} [SetLike S R] [SubmonoidClass S R] (T : S)
  {R' : Type*} [CommMonoidWithZero R']

/-- Two multiplicative characters on a monoid whose unit group is generated by `g`
are equal if and only if they agree on `g`. -/
/-
**MulChar.eq_iff** 是 Mathlib 中的一个引理，位于命名空间 `MulChar`。
形式化陈述：eq_iff {g : Rˣ} (hg : forall x, x in Subgroup.zpowers g) (χ₁ χ₂ : MulChar 
R R') : χ₁ = χ₂ ↔ χ₁ g.val = χ₂ g.val
参数：hg : forall x, x in Subgroup.zpowers g；χ₁ χ₂ : MulChar R R'。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Equiv.apply_eq_iff_eq`：apply_eq_iff_eq (f : α ≃ β) {x y : α} : f x = f y
 ↔ x = y
· 使用引理 `MonoidHom.eq_iff_eq_on_generator`：MonoidHom.eq_iff_eq_on_generator (f₁ f
₂ : G ->* G') : f₁ = f₂ ↔ f₁ g = f₂ g
· 使用定理 `MulChar.coe_equivToUnitHom`：coe_equivToUnitHom (χ : MulChar R R') (a : R
ˣ) : ↑(equivToUnitHom χ a) = χ a
· 使用定理 `Units.ext_iff`：∀ {α : Type u} [inst : Monoid α] {u v : αˣ}, u = v ↔ ↑u =
 ↑v
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a

--- 原说明 ---
Two multiplicative characters on a monoid whose unit group is generated by `g`
are equal if and only if they agree on `g`.
-/
lemma eq_iff {g : Rˣ} (hg : ∀ x, x ∈ Subgroup.zpowers g) (χ₁ χ₂ : MulChar R R') :
    χ₁ = χ₂ ↔ χ₁ g.val = χ₂ g.val := by
  rw [← Equiv.apply_eq_iff_eq equivToUnitHom, MonoidHom.eq_iff_eq_on_generator hg,
    ← coe_equivToUnitHom, ← coe_equivToUnitHom, Units.ext_iff]
/-
**MulChar.domRestrict_ofUnitHom** 是 Mathlib 中的一个定理，位于命名空间 `MulChar`。
形式化陈述：domRestrict_ofUnitHom (f : Rˣ ->* R'ˣ) (S : Submonoid R) : domRestrict S (
ofUnitHom f) = ofUnitHom ((f.domRestrict S.units).comp S.unitsEquivUnitsType.sym
m)
参数：f : Rˣ ->* R'ˣ；S : Submonoid R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MulChar.ext`：ext {χ χ' : MulChar R R'} (h : forall a : Rˣ, χ a = χ' a) :
 χ = χ'
· 使用定理 `Submonoid.instSubmonoidClass`：∀ {M : Type u_1} [inst : MulOneClass M], S
ubmonoidClass (Submonoid M) M
· 使用定理 `SubgroupClass.toSubmonoidClass`：∀ {S : Type u_3} {G : outParam (Type u_4
)} {inst : DivInvMonoid G} {inst_1 : SetLike S G} [self : SubgroupClass S G],   
SubmonoidClass S G
· 使用定理 `Subgroup.instSubgroupClass`：∀ {G : Type u_1} [inst : Group G], SubgroupC
lass (Subgroup G) G
· 使用定理 `MulEquivClass.instMonoidHomClass`：∀ (F : Type u_1) {M : Type u_4} {N : T
ype u_5} [inst : EquivLike F M N] [inst_1 : MulOneClass M]   [inst_2 : MulOneCla
ss N] [MulEquivClass F…
· 使用定理 `MulEquiv.instMulEquivClass`：∀ {M : Type u_4} {N : Type u_5} [inst : Mul 
M] [inst_1 : Mul N], MulEquivClass (M ≃* N) M N
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `MulChar.domRestrict_apply`：∀ {R : Type u_1} [inst : CommMonoid R] {R' : 
Type u_2} [inst_1 : CommMonoidWithZero R'] {S : Type u_3}   [inst_2 : SetLike S 
R] [inst_3 : Su…
· 使用定理 `ite_congr`：∀ {α : Sort u_1} {b c : Prop} {x y u v : α} {s : Decidable b}
 [inst : Decidable c],   b = c → (c → x = u) → (¬c → y = v) → (if b then x else…
· 使用定理 `ite_cond_eq_true`：∀ {α : Sort u} {c : Prop} {x : Decidable c} (a b : α),
 c = True → (if c then a else b) = a
· 使用定理 `MulChar.equivToUnitHom_symm_coe`：equivToUnitHom_symm_coe (f : Rˣ ->* R'ˣ
) (a : Rˣ) : equivToUnitHom.symm f ↑a = f a
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Submonoid.val_unitsEquivUnitsType_symm_apply_coe`：∀ {M : Type u_1} [inst
 : Monoid M] (S : Submonoid M) (x : (↥S)ˣ), ↑↑(S.unitsEquivUnitsType.symm x) = ↑
↑x
-/
theorem domRestrict_ofUnitHom (f : Rˣ →* R'ˣ) (S : Submonoid R) : domRestrict S (ofUnitHom f) =
      ofUnitHom ((f.domRestrict S.units).comp S.unitsEquivUnitsType.symm) := by
  ext x
  simp only [ofUnitHom_eq, domRestrict_apply, Units.isUnit, reduceIte, equivToUnitHom_symm_coe,
    MonoidHom.coe_comp, MonoidHom.coe_coe, Function.comp_apply, MonoidHom.domRestrict_apply]
  rw [← Submonoid.val_unitsEquivUnitsType_symm_apply_coe S x, equivToUnitHom_symm_coe]

@[deprecated (since := "2026-07-19")] alias restrict_ofUnitHom := domRestrict_ofUnitHom

end CommMonoid

section Ring

variable {R R' : Type*} [CommRing R] [CommRing R']

/-- Define the conjugation (`star`) of a multiplicative character by conjugating pointwise. -/
@[simps!]
/-
**MulChar.starComp** 是 Mathlib 中的一个定义，位于命名空间 `MulChar`。
形式化陈述：starComp [StarRing R'] (χ : MulChar R R') : MulChar R R'
参数：χ : MulChar R R'。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Define the conjugation (`star`) of a multiplicative character by conjugating poi
ntwise.
-/
def starComp [StarRing R'] (χ : MulChar R R') : MulChar R R' :=
  χ.ringHomComp (starRingEnd R')
/-
**MulChar.instStarMul** 是 Mathlib 中的一个实例，位于命名空间 `MulChar`。
形式化陈述：instStarMul [StarRing R'] : StarMul (MulChar R R') where star
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance instStarMul [StarRing R'] : StarMul (MulChar R R') where
  star := starComp
  star_involutive χ := by
    ext1
    simp [starComp_apply]
  star_mul χ χ' := by
    ext1
    simp [starComp_apply, mul_comm]

@[simp]
/-
**MulChar.star_apply** 是 Mathlib 中的一个引理，位于命名空间 `MulChar`。
形式化陈述：star_apply [StarRing R'] (χ : MulChar R R') (a : R) : (star χ) a = star (χ
 a)
参数：χ : MulChar R R'；a : R。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma star_apply [StarRing R'] (χ : MulChar R R') (a : R) : (star χ) a = star (χ a) :=
  rfl

/-- The values of a multiplicative character on `R` are `n`th roots of unity, where `n = #Rˣ`. -/
/-
**MulChar.apply_mem_rootsOfUnity** 是 Mathlib 中的一个引理，位于命名空间 `MulChar`。
形式化陈述：apply_mem_rootsOfUnity [Fintype Rˣ] (a : Rˣ) {χ : MulChar R R'} : equivToU
nitHom χ a in rootsOfUnity (Fintype.card Rˣ) R'
参数：a : Rˣ。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mem_rootsOfUnity`：mem_rootsOfUnity (k : Nat) (ζ : Mˣ) : ζ in rootsOfUnit
y k M ↔ ζ ^ k = 1
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `map_pow`：∀ {G : Type u_7} {H : Type u_8} {F : Type u_9} [inst : FunLike 
F G H] [inst_1 : Monoid G] [inst_2 : Monoid H]   [MonoidHomClass F G H] (f : …
· 使用定理 `MonoidHom.map_one`：∀ {M : Type u_4} {N : Type u_5} [inst : MulOne M] [in
st_1 : MulOne N] (f : M →* N), f 1 = 1
· 使用定理 `pow_card_eq_one`：pow_card_eq_one : x ^ Fintype.card G = 1

--- 原说明 ---
The values of a multiplicative character on `R` are `n`th roots of unity, where 
`n = #Rˣ`.
-/
lemma apply_mem_rootsOfUnity [Fintype Rˣ] (a : Rˣ) {χ : MulChar R R'} :
    equivToUnitHom χ a ∈ rootsOfUnity (Fintype.card Rˣ) R' := by
  rw [mem_rootsOfUnity, ← map_pow, ← (equivToUnitHom χ).map_one, pow_card_eq_one]

variable [Finite Rˣ]

open Complex in
/-- The conjugate of a multiplicative character with values in `ℂ` is its inverse. -/
/-
**MulChar.star_eq_inv** 是 Mathlib 中的一个引理，位于命名空间 `MulChar`。
形式化陈述：star_eq_inv (χ : MulChar R Complex) : star χ = χ⁻¹
参数：χ : MulChar R Complex。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `nonempty_fintype`：nonempty_fintype (α : Type*) [Finite α] : Nonempty (Fi
ntype α)
· 使用定理 `MulChar.ext`：ext {χ χ' : MulChar R R'} (h : forall a : Rˣ, χ a = χ' a) :
 χ = χ'
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MulChar.inv_apply_eq_inv'`：inv_apply_eq_inv' {R' : Type*} [CommGroupWith
Zero R'] (χ : MulChar R R') (a : R) : χ⁻¹ a = (χ a)⁻¹
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Complex.inv_eq_conj`：∀ {z : ℂ}, ‖z‖ = 1 → z⁻¹ = (starRingEnd ℂ) z
· 使用引理 `Complex.norm_eq_one_of_mem_rootsOfUnity`：Complex.norm_eq_one_of_mem_root
sOfUnity {ζ : Complexˣ} {n : Nat} [NeZero n] (hζ : ζ in rootsOfUnity n Complex) 
: ‖(ζ : Complex)‖ = 1
· 使用定理 `Fintype.instNeZeroNatCardOfNonempty`：∀ {α : Type u_1} [inst : Fintype α]
 [Nonempty α], NeZero (Fintype.card α)
· 使用定理 `instNonemptyOfInhabited`：∀ {α : Sort u} [Inhabited α], Nonempty α
· 使用引理 `MulChar.apply_mem_rootsOfUnity`：apply_mem_rootsOfUnity [Fintype Rˣ] (a :
 Rˣ) {χ : MulChar R R'} : equivToUnitHom χ a in rootsOfUnity (Fintype.card Rˣ) R
'

--- 原说明 ---
The conjugate of a multiplicative character with values in `ℂ` is its inverse.
-/
lemma star_eq_inv (χ : MulChar R ℂ) : star χ = χ⁻¹ := by
  cases nonempty_fintype Rˣ
  ext1 a
  simp only [inv_apply_eq_inv']
  exact (inv_eq_conj <| norm_eq_one_of_mem_rootsOfUnity <| χ.apply_mem_rootsOfUnity a).symm
/-
**MulChar.star_apply'** 是 Mathlib 中的一个引理，位于命名空间 `MulChar`。
形式化陈述：star_apply' (χ : MulChar R Complex) (a : R) : star (χ a) = χ⁻¹ a
参数：χ : MulChar R Complex；a : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma star_apply' (χ : MulChar R ℂ) (a : R) : star (χ a) = χ⁻¹ a := by
  simp only [RCLike.star_def, ← star_eq_inv, star_apply]

end Ring

section IsCyclic

/-!
### Multiplicative characters on finite monoids with cyclic unit group
-/

variable {M : Type*} [CommMonoid M] [Fintype M] [DecidableEq M]
variable {R : Type*} [CommMonoidWithZero R]

/-- Given a finite monoid `M` with unit group `Mˣ` cyclic of order `n` and an `n`th root of
unity `ζ` in `R`, there is a multiplicative character `M → R` that sends a given generator
of `Mˣ` to `ζ`. -/
/-
**MulChar.ofRootOfUnity** 是 Mathlib 中的一个定义，位于命名空间 `MulChar`。
形式化陈述：ofRootOfUnity {ζ : Rˣ} (hζ : ζ in rootsOfUnity (Fintype.card Mˣ) R) {g : M
ˣ} (hg : forall x, x in Subgroup.zpowers g) : MulChar M R
参数：hζ : ζ in rootsOfUnity (Fintype.card Mˣ) R；hg : forall x, x in Subgroup.zpowe
rs g。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Given a finite monoid `M` with unit group `Mˣ` cyclic of order `n` and an `n`th 
root of
unity `ζ` in `R`, there is a multiplicative character `M → R` that sends a given
 generator
of `Mˣ` to `ζ`.
-/
noncomputable def ofRootOfUnity {ζ : Rˣ} (hζ : ζ ∈ rootsOfUnity (Fintype.card Mˣ) R)
    {g : Mˣ} (hg : ∀ x, x ∈ Subgroup.zpowers g) :
    MulChar M R := by
  have : orderOf ζ ∣ Fintype.card Mˣ :=
    orderOf_dvd_iff_pow_eq_one.mpr <| (mem_rootsOfUnity _ ζ).mp hζ
  refine ofUnitHom <| monoidHomOfForallMemZpowers hg <| this.trans <| dvd_of_eq ?_
  rw [orderOf_eq_card_of_forall_mem_zpowers hg, Nat.card_eq_fintype_card]
/-
**MulChar.ofRootOfUnity_spec** 是 Mathlib 中的一个引理，位于命名空间 `MulChar`。
形式化陈述：ofRootOfUnity_spec {ζ : Rˣ} (hζ : ζ in rootsOfUnity (Fintype.card Mˣ) R) {
g : Mˣ} (hg : forall x, x in Subgroup.zpowers g) : ofRootOfUnity hζ hg g = ζ
参数：hζ : ζ in rootsOfUnity (Fintype.card Mˣ) R；hg : forall x, x in Subgroup.zpowe
rs g。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用定理 `MulChar.equivToUnitHom_symm_coe`：equivToUnitHom_symm_coe (f : Rˣ ->* R'ˣ
) (a : Rˣ) : equivToUnitHom.symm f ↑a = f a
· 使用引理 `monoidHomOfForallMemZpowers_apply_gen`：monoidHomOfForallMemZpowers_apply
_gen : monoidHomOfForallMemZpowers hg hg' g = g'
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma ofRootOfUnity_spec {ζ : Rˣ} (hζ : ζ ∈ rootsOfUnity (Fintype.card Mˣ) R)
    {g : Mˣ} (hg : ∀ x, x ∈ Subgroup.zpowers g) :
    ofRootOfUnity hζ hg g = ζ := by
  simp only [ofRootOfUnity, ofUnitHom_eq, equivToUnitHom_symm_coe,
    monoidHomOfForallMemZpowers_apply_gen]

variable (M R) in
/-- The group of multiplicative characters on a finite monoid `M` with cyclic unit group `Mˣ`
of order `n` is isomorphic to the group of `n`th roots of unity in the target `R`. -/
/-
**MulChar.equiv_rootsOfUnity** 是 Mathlib 中的一个定义，位于命名空间 `MulChar`。
形式化陈述：equiv_rootsOfUnity [inst_cyc : IsCyclic Mˣ] : MulChar M R ≃* rootsOfUnity 
(Fintype.card Mˣ) R where toFun χ
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The group of multiplicative characters on a finite monoid `M` with cyclic unit g
roup `Mˣ`
of order `n` is isomorphic to the group of `n`th roots of unity in the target `R
`.
-/
noncomputable def equiv_rootsOfUnity [inst_cyc : IsCyclic Mˣ] :
    MulChar M R ≃* rootsOfUnity (Fintype.card Mˣ) R where
  toFun χ :=
    ⟨χ.toUnitHom <| Classical.choose inst_cyc.exists_generator, by
      simp only [toUnitHom_eq, mem_rootsOfUnity, ← map_pow, pow_card_eq_one, map_one]⟩
  invFun ζ := ofRootOfUnity ζ.prop <| Classical.choose_spec inst_cyc.exists_generator
  left_inv χ := by
    simp only [toUnitHom_eq, eq_iff <| Classical.choose_spec inst_cyc.exists_generator,
      ofRootOfUnity_spec, coe_equivToUnitHom]
  right_inv ζ := by
    ext
    simp only [toUnitHom_eq, coe_equivToUnitHom, ofRootOfUnity_spec]
  map_mul' x y := by
    simp only [toUnitHom_eq, equivToUnitHom_mul_apply, MulMemClass.mk_mul_mk]

end IsCyclic

section FiniteField

/-!
### Multiplicative characters on finite fields
-/

section Fintype

variable (F : Type*) [Field F] [Fintype F]
variable {R : Type*} [CommRing R]

/-- There is a character of order `n` on `F` if `#F ≡ 1 mod n` and the target contains
a primitive `n`th root of unity. -/
/-
**MulChar.exists_mulChar_orderOf** 是 Mathlib 中的一个引理，位于命名空间 `MulChar`。
形式化陈述：exists_mulChar_orderOf {n : Nat} (h : n ∣ Fintype.card F - 1) {ζ : R} (hζ 
: IsPrimitiveRoot ζ n) : exists χ : MulChar F R, orderOf χ = n
参数：h : n ∣ Fintype.card F - 1；hζ : IsPrimitiveRoot ζ n。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Nat.pos_of_ne_zero`：∀ {n : ℕ}, n ≠ 0 → 0 < n
· 使用定理 `LT.lt.false`：∀ {α : Type u_2} [inst : Preorder α] {a : α}, a < a → False
· 使用定理 `LT.lt.trans_le`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a < b 
→ b ≤ c → a < c
· 使用定理 `Fintype.one_lt_card`：one_lt_card [h : Nontrivial α] : 1 < Fintype.card α
· 使用定理 `EuclideanDomain.toNontrivial`：∀ {R : Type u} [self : EuclideanDomain R],
 Nontrivial R
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `instIsCyclicUnitsOfFinite`：∀ {R : Type u_1} [inst : CommRing R] [IsDomai
n R] [Finite Rˣ], IsCyclic Rˣ
· 使用定理 `instIsDomain`：∀ {R : Type u} [inst : Semifield R], IsDomain R
· 使用定理 `instFiniteUnits`：∀ {α : Type u_1} [inst : Monoid α] [Finite α], Finite α
ˣ
· 使用定理 `Finite.of_fintype`：∀ (α : Type u_4) [Fintype α], Finite α
· 使用定理 `IsPrimitiveRoot.isUnit`：isUnit (h : IsPrimitiveRoot ζ k) (h0 : k != 0) :
 IsUnit ζ
· 使用定理 `LT.lt.ne'`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, b < a → a ≠ b
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Units.ext_iff`：∀ {α : Type u} [inst : Monoid α] {u v : αˣ}, u = v ↔ ↑u =
 ↑v
· 使用定理 `IsPrimitiveRoot.pow_eq_one_iff_dvd`：pow_eq_one_iff_dvd (h : IsPrimitiveR
oot ζ k) (l : Nat) : ζ ^ l = 1 ↔ k ∣ l
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Fintype.card_units`：Fintype.card_units [GroupWithZero α] [Fintype α] [De
cidableEq α] : Fintype.card αˣ = Fintype.card α - 1
· 使用定理 `mem_rootsOfUnity`：mem_rootsOfUnity (k : Nat) (ζ : Mˣ) : ζ in rootsOfUnit
y k M ↔ ζ ^ k = 1
· 使用引理 `MulEquiv.orderOf_eq`：MulEquiv.orderOf_eq {H : Type*} [Monoid H] (e : G ≃
* H) (x : G) : orderOf (e x) = orderOf x
· 使用定理 `orderOf_eq_iff`：orderOf_eq_iff {n} (h : 0 < n) : orderOf x = n ↔ x ^ n =
 1 ∧ forall m, m < n -> 0 < m -> x ^ m != 1
· 使用定理 `Subtype.ext`：∀ {α : Sort u} {p : α → Prop} {a1 a2 : { x // p x }}, ↑a1 =
 ↑a2 → a1 = a2
· 使用定理 `Units.ext`：ext {u v : αˣ} (huv : u.val = v.val) : u = v
· 使用定理 `IsPrimitiveRoot.pow_eq_one`：∀ {M : Type u_1} [inst : CommMonoid M] {ζ : 
M} {k : ℕ}, IsPrimitiveRoot ζ k → ζ ^ k = 1
· 使用定理 `LE.le.trans_lt`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b 
→ b < c → a < c
· 使用定理 `Nat.le_of_dvd`：∀ {m n : ℕ}, 0 < n → m ∣ n → m ≤ n
· 使用定理 `IsPrimitiveRoot.dvd_of_pow_eq_one`：∀ {M : Type u_1} [inst : CommMonoid M
] {ζ : M} {k : ℕ}, IsPrimitiveRoot ζ k → ∀ (l : ℕ), ζ ^ l = 1 → k ∣ l
· 使用定理 `Subtype.ext_iff`：∀ {α : Sort u} {p : α → Prop} {a1 a2 : { x // p x }}, a
1 = a2 ↔ ↑a1 = ↑a2

--- 原说明 ---
There is a character of order `n` on `F` if `#F ≡ 1 mod n` and the target contai
ns
a primitive `n`th root of unity.
-/
lemma exists_mulChar_orderOf {n : ℕ} (h : n ∣ Fintype.card F - 1) {ζ : R}
    (hζ : IsPrimitiveRoot ζ n) :
    ∃ χ : MulChar F R, orderOf χ = n := by
  classical
  have hn₀ : 0 < n := by
    refine Nat.pos_of_ne_zero fun hn ↦ ?_
    simp only [hn, zero_dvd_iff, Nat.sub_eq_zero_iff_le] at h
    exact (Fintype.one_lt_card.trans_le h).false
  let e := MulChar.equiv_rootsOfUnity F R
  let ζ' : Rˣ := (hζ.isUnit hn₀.ne').unit
  have h' : ζ' ^ (Fintype.card Fˣ : ℕ) = 1 :=
    Units.ext_iff.mpr <| (hζ.pow_eq_one_iff_dvd _).mpr <| Fintype.card_units (α := F) ▸ h
  use e.symm ⟨ζ', (mem_rootsOfUnity (Fintype.card Fˣ) ζ').mpr h'⟩
  rw [e.symm.orderOf_eq, orderOf_eq_iff hn₀]
  refine ⟨?_, fun m hm hm₀ h ↦ ?_⟩
  · ext
    push_cast
    exact hζ.pow_eq_one
  · rw [Subtype.ext_iff, Units.ext_iff] at h
    push_cast at h
    exact ((Nat.le_of_dvd hm₀ <| hζ.dvd_of_pow_eq_one _ h).trans_lt hm).false

/-- If there is a multiplicative character of order `n` on `F`, then `#F ≡ 1 mod n`. -/
/-
**MulChar.orderOf_dvd_card_sub_one** 是 Mathlib 中的一个引理，位于命名空间 `MulChar`。
形式化陈述：orderOf_dvd_card_sub_one (χ : MulChar F R) : orderOf χ ∣ Fintype.card F - 
1
参数：χ : MulChar F R。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Fintype.card_units`：Fintype.card_units [GroupWithZero α] [Fintype α] [De
cidableEq α] : Fintype.card αˣ = Fintype.card α - 1
· 使用定理 `orderOf_dvd_of_pow_eq_one`：orderOf_dvd_of_pow_eq_one (h : x ^ n = 1) : o
rderOf x ∣ n
· 使用定理 `MulChar.pow_card_eq_one`：∀ {M : Type u_1} [inst : CommMonoid M] {R : Typ
e u_2} [inst_1 : CommMonoidWithZero R] [inst_2 : Fintype Mˣ]   (χ : MulChar M R)
, χ ^ Fintype…

--- 原说明 ---
If there is a multiplicative character of order `n` on `F`, then `#F ≡ 1 mod n`.
-/
lemma orderOf_dvd_card_sub_one (χ : MulChar F R) : orderOf χ ∣ Fintype.card F - 1 := by
  classical
  rw [← Fintype.card_units]
  exact orderOf_dvd_of_pow_eq_one χ.pow_card_eq_one

/-- There is always a character on `F` of order `#F-1` with values in a ring that has
a primitive `(#F-1)`th root of unity. -/
/-
**MulChar.exists_mulChar_orderOf_eq_card_units** 是 Mathlib 中的一个引理，位于命名空间 `MulCha
r`。
形式化陈述：exists_mulChar_orderOf_eq_card_units [DecidableEq F] {ζ : R} (hζ : IsPrimi
tiveRoot ζ (Fintype.card Fˣ)) : exists χ : MulChar F R, orderOf χ = Fintype.card
 Fˣ
参数：hζ : IsPrimitiveRoot ζ (Fintype.card Fˣ)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MulChar.exists_mulChar_orderOf`：exists_mulChar_orderOf {n : Nat} (h : n 
∣ Fintype.card F - 1) {ζ : R} (hζ : IsPrimitiveRoot ζ n) : exists χ : MulChar F 
R, orderOf χ = n
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Fintype.card_units`：Fintype.card_units [GroupWithZero α] [Fintype α] [De
cidableEq α] : Fintype.card αˣ = Fintype.card α - 1
· 使用定理 `dvd_refl`：dvd_refl (a : α) : a ∣ a

--- 原说明 ---
There is always a character on `F` of order `#F-1` with values in a ring that ha
s
a primitive `(#F-1)`th root of unity.
-/
lemma exists_mulChar_orderOf_eq_card_units [DecidableEq F]
    {ζ : R} (hζ : IsPrimitiveRoot ζ (Fintype.card Fˣ)) :
    ∃ χ : MulChar F R, orderOf χ = Fintype.card Fˣ :=
  exists_mulChar_orderOf F (by rw [Fintype.card_units]) hζ

end Fintype

variable {F : Type*} [Field F] [Finite F]
variable {R : Type*} [CommRing R]

/- The non-zero values of a multiplicative character of order `n` are `n`th roots of unity. -/
/-
**MulChar.apply_mem_rootsOfUnity_orderOf** 是 Mathlib 中的一个引理，位于命名空间 `MulChar`。
形式化陈述：apply_mem_rootsOfUnity_orderOf (χ : MulChar F R) {a : F} (ha : a != 0) : e
xists ζ in rootsOfUnity (orderOf χ) R, ζ = χ a
参数：χ : MulChar F R；ha : a != 0。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsUnit.map`：map [MonoidHomClass F M N] (f : F) {x : M} (h : IsUnit x) : 
IsUnit (f x)
· 使用定理 `MulCharClass.toMonoidHomClass`：∀ {F : Type u_3} {R : outParam (Type u_4)
} {R' : outParam (Type u_5)} {inst : CommMonoid R}   {inst_1 : CommMonoidWithZer
o R'} {inst_2 : Fun…
· 使用定理 `MulChar.instMulCharClass`：∀ {R : Type u_1} [inst : CommMonoid R] {R' : T
ype u_2} [inst_1 : CommMonoidWithZero R'],   MulCharClass (MulChar R R') R R'
· 使用定理 `Ne.isUnit`：∀ {G₀ : Type u_3} [inst : GroupWithZero G₀] {a : G₀}, a ≠ 0 →
 IsUnit a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mem_rootsOfUnity`：mem_rootsOfUnity (k : Nat) (ζ : Mˣ) : ζ in rootsOfUnit
y k M ↔ ζ ^ k = 1
· 使用定理 `Units.ext_iff`：∀ {α : Type u} [inst : Monoid α] {u v : αˣ}, u = v ↔ ↑u =
 ↑v
· 使用引理 `Units.val_pow_eq_pow_val`：val_pow_eq_pow_val (n : Nat) : ↑(a ^ n) = (a ^
 n : α)
· 使用定理 `Units.val_one`：val_one : ((1 : αˣ) : α) = 1
· 使用定理 `IsUnit.unit_spec`：unit_spec (h : IsUnit a) : ↑h.unit = a
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MulChar.pow_apply'`：pow_apply' (χ : MulChar R R') {n : Nat} (hn : n != 0
) (a : R) : (χ ^ n) a = χ a ^ n
· 使用定理 `LT.lt.ne'`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, b < a → a ≠ b
· 使用引理 `MulChar.orderOf_pos`：orderOf_pos [Finite Mˣ] (χ : MulChar M R) : 0 < ord
erOf χ
· 使用定理 `instFiniteUnits`：∀ {α : Type u_1} [inst : Monoid α] [Finite α], Finite α
ˣ
· 使用定理 `pow_orderOf_eq_one`：pow_orderOf_eq_one (x : G) : x ^ orderOf x = 1
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `isUnit_iff_ne_zero`：isUnit_iff_ne_zero : IsUnit a ↔ a != 0
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `MulChar.one_apply_coe`：one_apply_coe (a : Rˣ) : (1 : MulChar R R') a = 1

--- 原说明 ---
The non-zero values of a multiplicative character of order `n` are `n`th roots o
f unity.
-/
lemma apply_mem_rootsOfUnity_orderOf (χ : MulChar F R) {a : F} (ha : a ≠ 0) :
    ∃ ζ ∈ rootsOfUnity (orderOf χ) R, ζ = χ a := by
  have hu : IsUnit (χ a) := ha.isUnit.map χ
  refine ⟨hu.unit, ?_, hu.unit_spec⟩
  rw [mem_rootsOfUnity, Units.ext_iff, Units.val_pow_eq_pow_val, Units.val_one,
    IsUnit.unit_spec, ← χ.pow_apply' χ.orderOf_pos.ne', pow_orderOf_eq_one,
    show a = (isUnit_iff_ne_zero.mpr ha).unit by simp only [IsUnit.unit_spec],
    MulChar.one_apply_coe]

/-- The non-zero values of a multiplicative character `χ` such that `χ^n = 1`
are `n`th roots of unity. -/
/-
**MulChar.apply_mem_rootsOfUnity_of_pow_eq_one** 是 Mathlib 中的一个引理，位于命名空间 `MulCha
r`。
形式化陈述：apply_mem_rootsOfUnity_of_pow_eq_one {χ : MulChar F R} {n : Nat} (hχ : χ ^
 n = 1) {a : F} (ha : a != 0) : exists ζ in rootsOfUnity n R, ζ = χ a
参数：hχ : χ ^ n = 1；ha : a != 0。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MulChar.apply_mem_rootsOfUnity_orderOf`：apply_mem_rootsOfUnity_orderOf (
χ : MulChar F R) {a : F} (ha : a != 0) : exists ζ in rootsOfUnity (orderOf χ) R,
 ζ = χ a
· 使用定理 `rootsOfUnity_le_of_dvd`：rootsOfUnity_le_of_dvd (h : k ∣ l) : rootsOfUnit
y k M <= rootsOfUnity l M
· 使用定理 `orderOf_dvd_of_pow_eq_one`：orderOf_dvd_of_pow_eq_one (h : x ^ n = 1) : o
rderOf x ∣ n

--- 原说明 ---
The non-zero values of a multiplicative character `χ` such that `χ^n = 1`
are `n`th roots of unity.
-/
lemma apply_mem_rootsOfUnity_of_pow_eq_one {χ : MulChar F R} {n : ℕ} (hχ : χ ^ n = 1)
    {a : F} (ha : a ≠ 0) :
    ∃ ζ ∈ rootsOfUnity n R, ζ = χ a := by
  obtain ⟨μ, hμ₁, hμ₂⟩ := χ.apply_mem_rootsOfUnity_orderOf ha
  exact ⟨μ, rootsOfUnity_le_of_dvd (orderOf_dvd_of_pow_eq_one hχ) hμ₁, hμ₂⟩

-- Results involving primitive roots of unity require `R` to be an integral domain.
variable [IsDomain R]

/-- If `χ` is a multiplicative character with `χ^n = 1` and `μ` is a primitive `n`th root
of unity, then, for `a ≠ 0`, there is some `k` such that `χ a = μ^k`. -/
/-
**MulChar.exists_apply_eq_pow** 是 Mathlib 中的一个引理，位于命名空间 `MulChar`。
形式化陈述：exists_apply_eq_pow {χ : MulChar F R} {n : Nat} [NeZero n] (hχ : χ ^ n = 1
) {μ : R} (hμ : IsPrimitiveRoot μ n) {a : F} (ha : a != 0) : exists k < n, χ a =
 μ ^ k
参数：hχ : χ ^ n = 1；hμ : IsPrimitiveRoot μ n；ha : a != 0。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MulChar.apply_mem_rootsOfUnity_of_pow_eq_one`：apply_mem_rootsOfUnity_of_
pow_eq_one {χ : MulChar F R} {n : Nat} (hχ : χ ^ n = 1) {a : F} (ha : a != 0) : 
exists ζ in rootsOfUnity n R, ζ = …
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `mem_rootsOfUnity'`：mem_rootsOfUnity' (k : Nat) (ζ : Mˣ) : ζ in rootsOfUn
ity k M ↔ (ζ : M) ^ k = 1
· 使用定理 `IsPrimitiveRoot.eq_pow_of_pow_eq_one`：eq_pow_of_pow_eq_one {k : Nat} [Ne
Zero k] {ζ ξ : R} (h : IsPrimitiveRoot ζ k) (hξ : ξ ^ k = 1) : exists i < k, ζ ^
 i = ξ
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a

--- 原说明 ---
If `χ` is a multiplicative character with `χ^n = 1` and `μ` is a primitive `n`th
 root
of unity, then, for `a ≠ 0`, there is some `k` such that `χ a = μ^k`.
-/
lemma exists_apply_eq_pow {χ : MulChar F R} {n : ℕ} [NeZero n] (hχ : χ ^ n = 1) {μ : R}
    (hμ : IsPrimitiveRoot μ n) {a : F} (ha : a ≠ 0) :
    ∃ k < n, χ a = μ ^ k := by
  obtain ⟨ζ, hζ₁, hζ₂⟩ := apply_mem_rootsOfUnity_of_pow_eq_one hχ ha
  have hζ' : ζ.val ^ n = 1 := (mem_rootsOfUnity' n ↑ζ).mp hζ₁
  obtain ⟨k, hk₁, hk₂⟩ := hμ.eq_pow_of_pow_eq_one hζ'
  exact ⟨k, hk₁, (hζ₂ ▸ hk₂).symm⟩

/-- The values of a multiplicative character `χ` such that `χ^n = 1` are contained in `ℤ[μ]` when
`μ` is a primitive `n`th root of unity. -/
/-
**MulChar.apply_mem_algebraAdjoin_of_pow_eq_one** 是 Mathlib 中的一个引理，位于命名空间 `MulCh
ar`。
形式化陈述：apply_mem_algebraAdjoin_of_pow_eq_one {χ : MulChar F R} {n : Nat} [NeZero 
n] (hχ : χ ^ n = 1) {μ : R} (hμ : IsPrimitiveRoot μ n) (a : F) : χ a in Algebra.
adjoin Int {μ}
参数：hχ : χ ^ n = 1；hμ : IsPrimitiveRoot μ n；a : F。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `eq_or_ne`：eq_or_ne {α : Sort*} (x y : α) : x = y ∨ x != y
· 使用定理 `Subalgebra.zero_mem`：∀ {R : Type u} {A : Type v} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A] (S : Subalgebra R A),   0 ∈ S
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MulChar.map_zero`：∀ {R' : Type u_2} [inst : CommMonoidWithZero R'] {R : 
Type u_3} [inst_1 : CommMonoidWithZero R] [Nontrivial R]   (χ : MulChar R R'), χ
 0 = 0
· 使用定理 `EuclideanDomain.toNontrivial`：∀ {R : Type u} [self : EuclideanDomain R],
 Nontrivial R
· 使用引理 `MulChar.apply_mem_rootsOfUnity_of_pow_eq_one`：apply_mem_rootsOfUnity_of_
pow_eq_one {χ : MulChar F R} {n : Nat} (hχ : χ ^ n = 1) {a : F} (ha : a != 0) : 
exists ζ in rootsOfUnity n R, ζ = …
· 使用定理 `IsPrimitiveRoot.eq_pow_of_pow_eq_one`：eq_pow_of_pow_eq_one {k : Nat} [Ne
Zero k] {ζ ξ : R} (h : IsPrimitiveRoot ζ k) (hξ : ξ ^ k = 1) : exists i < k, ζ ^
 i = ξ
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `Units.val_pow_eq_pow_val`：val_pow_eq_pow_val (n : Nat) : ↑(a ^ n) = (a ^
 n : α)
· 使用定理 `Units.ext_iff`：∀ {α : Type u} [inst : Monoid α] {u v : αˣ}, u = v ↔ ↑u =
 ↑v
· 使用定理 `mem_rootsOfUnity`：mem_rootsOfUnity (k : Nat) (ζ : Mˣ) : ζ in rootsOfUnit
y k M ↔ ζ ^ k = 1
· 使用定理 `Subalgebra.pow_mem`：∀ {R : Type u} {A : Type v} [inst : CommSemiring R] 
[inst_1 : Semiring A] [inst_2 : Algebra R A] (S : Subalgebra R A)   {x : A}, x ∈
 S → ∀ (…
· 使用定理 `Algebra.self_mem_adjoin_singleton`：self_mem_adjoin_singleton (x : A) : x
 in R[x]

--- 原说明 ---
The values of a multiplicative character `χ` such that `χ^n = 1` are contained i
n `ℤ[μ]` when
`μ` is a primitive `n`th root of unity.
-/
lemma apply_mem_algebraAdjoin_of_pow_eq_one {χ : MulChar F R} {n : ℕ} [NeZero n] (hχ : χ ^ n = 1)
    {μ : R} (hμ : IsPrimitiveRoot μ n) (a : F) :
    χ a ∈ Algebra.adjoin ℤ {μ} := by
  rcases eq_or_ne a 0 with rfl | h
  · exact χ.map_zero ▸ Subalgebra.zero_mem _
  · obtain ⟨ζ, hζ₁, hζ₂⟩ := apply_mem_rootsOfUnity_of_pow_eq_one hχ h
    rw [mem_rootsOfUnity, Units.ext_iff, Units.val_pow_eq_pow_val] at hζ₁
    obtain ⟨k, _, hk⟩ := IsPrimitiveRoot.eq_pow_of_pow_eq_one hμ hζ₁
    exact hζ₂ ▸ hk ▸ Subalgebra.pow_mem _ (Algebra.self_mem_adjoin_singleton ℤ μ) k

/-- The values of a multiplicative character of order `n` are contained in `ℤ[μ]` when
`μ` is a primitive `n`th root of unity. -/
/-
**MulChar.apply_mem_algebraAdjoin** 是 Mathlib 中的一个引理，位于命名空间 `MulChar`。
形式化陈述：apply_mem_algebraAdjoin {χ : MulChar F R} {μ : R} (hμ : IsPrimitiveRoot μ 
(orderOf χ)) (a : F) : χ a in Algebra.adjoin Int {μ}
参数：hμ : IsPrimitiveRoot μ (orderOf χ)；a : F。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `LT.lt.ne'`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, b < a → a ≠ b
· 使用引理 `MulChar.orderOf_pos`：orderOf_pos [Finite Mˣ] (χ : MulChar M R) : 0 < ord
erOf χ
· 使用定理 `instFiniteUnits`：∀ {α : Type u_1} [inst : Monoid α] [Finite α], Finite α
ˣ
· 使用引理 `MulChar.apply_mem_algebraAdjoin_of_pow_eq_one`：apply_mem_algebraAdjoin_o
f_pow_eq_one {χ : MulChar F R} {n : Nat} [NeZero n] (hχ : χ ^ n = 1) {μ : R} (hμ
 : IsPrimitiveRoot μ n) (a : F) : χ…
· 使用定理 `pow_orderOf_eq_one`：pow_orderOf_eq_one (x : G) : x ^ orderOf x = 1

--- 原说明 ---
The values of a multiplicative character of order `n` are contained in `ℤ[μ]` wh
en
`μ` is a primitive `n`th root of unity.
-/
lemma apply_mem_algebraAdjoin {χ : MulChar F R} {μ : R} (hμ : IsPrimitiveRoot μ (orderOf χ))
    (a : F) :
    χ a ∈ Algebra.adjoin ℤ {μ} :=
  have : NeZero (orderOf χ) := ⟨χ.orderOf_pos.ne'⟩
  apply_mem_algebraAdjoin_of_pow_eq_one (pow_orderOf_eq_one χ) hμ a

end FiniteField

end MulChar

