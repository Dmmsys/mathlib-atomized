/-
Copyright (c) 2022 Anne Baanen. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Anne Baanen, Alex J. Best
-/
module

public import Mathlib.Algebra.GroupWithZero.Torsion
public import Mathlib.NumberTheory.RamificationInertia.Galois
public import Mathlib.RingTheory.DedekindDomain.Factorization
public import Mathlib.RingTheory.DedekindDomain.Instances
public import Mathlib.RingTheory.Ideal.Int
public import Mathlib.RingTheory.NormalClosure

/-!

# Ideal norms

This file defines the relative ideal norm `Ideal.spanNorm R (I : Ideal S) : Ideal S` as the ideal
spanned by the norms of elements in `I`.

## Main definitions

* `Ideal.spanNorm R (I : Ideal S)`: the ideal spanned by the norms of elements in `I`.
  This is used to define `Ideal.relNorm`.
* `Ideal.relNorm R (I : Ideal S)`: the relative ideal norm as a bundled monoid-with-zero morphism,
  defined as the ideal spanned by the norms of elements in `I`.

## Main results

* `map_mul Ideal.relNorm`: multiplicativity of the relative ideal norm
* `relNorm_relNorm`: transitivity of the relative ideal norm

-/

@[expose] public section

open Module
open scoped nonZeroDivisors

section SpanNorm

namespace Ideal

open Submodule

variable (R S : Type*) [CommRing R] [IsDomain R] {S : Type*} [CommRing S] [IsDomain S]
variable [IsIntegrallyClosed R] [IsIntegrallyClosed S] [Algebra R S] [Module.Finite R S]
variable [IsTorsionFree R S]

attribute [local instance] FractionRing.liftAlgebra

/-- `Ideal.spanNorm R (I : Ideal S)` is the ideal generated by mapping `Algebra.intNorm R S`
over `I`.

See also `Ideal.relNorm`.
-/
/-
**Ideal.spanNorm** 是 Mathlib 中的一个定义，位于命名空间 `Ideal`。
形式化陈述：spanNorm (I : Ideal S) : Ideal R
参数：I : Ideal S。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`Ideal.spanNorm R (I : Ideal S)` is the ideal generated by mapping `Algebra.intN
orm R S`
over `I`.

See also `Ideal.relNorm`.
-/
noncomputable def spanNorm (I : Ideal S) : Ideal R :=
  Ideal.map (Algebra.intNorm R S) I

@[simp]
/-
**Ideal.spanNorm_bot** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：spanNorm_bot : spanNorm R (⊥ : Ideal S) = ⊥
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Ideal.span_eq_bot`：span_eq_bot {s : Set α} : span s = ⊥ ↔ forall x in s,
 (x : α) = 0
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Set.image_singleton`：image_singleton {f : α -> β} {a : α} : f '' {a} = {
f a}
· 使用引理 `Algebra.intNorm_zero`：Algebra.intNorm_zero [FiniteDimensional (FractionR
ing A) (FractionRing B)] : Algebra.intNorm A B 0 = 0
· 使用定理 `instFiniteDimensionalFractionRingOfFinite`：∀ {R : Type u_1} {S : Type u_
2} [inst : CommRing R] [inst_1 : CommRing S] [inst_2 : Algebra R S] [inst_3 : Is
Domain R]   [inst_4 : IsDomain …
-/
theorem spanNorm_bot :
    spanNorm R (⊥ : Ideal S) = ⊥ := span_eq_bot.mpr fun x hx => by simpa using hx

variable {R} in
@[simp]
/-
**Ideal.spanNorm_eq_bot_iff** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：spanNorm_eq_bot_iff {I : Ideal S} : spanNorm R I = ⊥ ↔ I = ⊥
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `instFiniteDimensionalFractionRingOfFinite`：∀ {R : Type u_1} {S : Type u_
2} [inst : CommRing R] [inst_1 : CommRing S] [inst_2 : Algebra R S] [inst_3 : Is
Domain R]   [inst_4 : IsDomain …
· 使用定理 `eq_bot_iff`：∀ {α : Type u} [inst : PartialOrder α] [inst_1 : OrderBot α]
 {a : α}, a = ⊥ ↔ a ≤ ⊥
· 使用定理 `instIsConcreteLE`：∀ (A : Type u_1) (B : Type u_2) [inst : SetLike A B], 
IsConcreteLE A B
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
theorem spanNorm_eq_bot_iff {I : Ideal S} : spanNorm R I = ⊥ ↔ I = ⊥ := by
  simp only [spanNorm, span_eq_bot, Set.mem_image, SetLike.mem_coe, forall_exists_index, and_imp,
    forall_apply_eq_imp_iff₂, Algebra.intNorm_eq_zero, @eq_bot_iff _ _ _ I, SetLike.le_def, map,
    mem_bot]
/-
**Ideal.intNorm_mem_spanNorm** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：intNorm_mem_spanNorm {I : Ideal S} {x : S} (hx : x in I) : Algebra.intNorm
 R S x in I.spanNorm R
参数：hx : x in I。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.subset_span`：subset_span {s : Set α} : s subseteq span s
· 使用定理 `Set.mem_image_of_mem`：mem_image_of_mem (f : α -> β) {x : α} {a : Set α} 
(h : x in a) : f x in f '' a
-/
theorem intNorm_mem_spanNorm {I : Ideal S} {x : S} (hx : x ∈ I) :
    Algebra.intNorm R S x ∈ I.spanNorm R :=
  subset_span (Set.mem_image_of_mem _ hx)
/-
**Ideal.norm_mem_spanNorm** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：norm_mem_spanNorm [Module.Free R S] {I : Ideal S} (x : S) (hx : x in I) : 
Algebra.norm R x in I.spanNorm R
参数：x : S；hx : x in I。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.subset_span`：subset_span {s : Set α} : s subseteq span s
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `Algebra.intNorm_eq_norm`：Algebra.intNorm_eq_norm [Module.Free A B] [Modu
le.Finite A B] : Algebra.intNorm A B = Algebra.norm A
-/
theorem norm_mem_spanNorm [Module.Free R S] {I : Ideal S} (x : S) (hx : x ∈ I) :
    Algebra.norm R x ∈ I.spanNorm R := by
  refine subset_span ⟨x, hx, ?_⟩
  rw [Algebra.intNorm_eq_norm]

@[simp]
/-
**Ideal.spanNorm_singleton** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：spanNorm_singleton {r : S} : spanNorm R (span ({r} : Set S)) = span {Algeb
ra.intNorm R S r}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Ideal.span_le`：span_le {s : Set α} {I} : span s <= I ↔ s subseteq I
· 使用定理 `Ideal.mem_span_singleton`：mem_span_singleton {x y : α} : x in span ({y} 
: Set α) ↔ y ∣ x
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Set.mem_image`：mem_image (f : α -> β) (s : Set α) (y : β) : y in f '' s 
↔ exists x in s, f x = y
· 使用定理 `map_dvd`：∀ {M : Type u_1} {N : Type u_2} [inst : Semigroup M] [inst_1 : 
Semigroup N] {F : Type u_3} [inst_2 : FunLike F M N]   [MulHomClass F M N] (f…
· 使用定理 `MonoidHomClass.toMulHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `Ideal.span_singleton_le_iff_mem`：span_singleton_le_iff_mem {x : α} : spa
n {x} <= I ↔ x in I
· 使用定理 `Ideal.intNorm_mem_spanNorm`：intNorm_mem_spanNorm {I : Ideal S} {x : S} (
hx : x in I) : Algebra.intNorm R S x in I.spanNorm R
· 使用定理 `Ideal.mem_span_singleton_self`：mem_span_singleton_self (x : α) : x in sp
an ({x} : Set α)
-/
theorem spanNorm_singleton {r : S} :
    spanNorm R (span ({r} : Set S)) = span {Algebra.intNorm R S r} :=
  le_antisymm
    (span_le.mpr fun x hx =>
      mem_span_singleton.mpr
        (by
          obtain ⟨x, hx', rfl⟩ := (Set.mem_image _ _ _).mp hx
          exact map_dvd _ (mem_span_singleton.mp hx')))
    ((span_singleton_le_iff_mem _).mpr (intNorm_mem_spanNorm _ (mem_span_singleton_self _)))

@[simp]
/-
**Ideal.spanNorm_top** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：spanNorm_top : spanNorm R (⊤ : Ideal S) = ⊤
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.spanNorm.congr_simp`：∀ (R : Type u_1) [inst : CommRing R] [inst_1 
: IsDomain R] {S : Type u_3} [inst_2 : CommRing S] [inst_3 : IsDomain S]   [inst
_4 : IsIntegral…
· 使用定理 `Ideal.spanNorm_singleton`：spanNorm_singleton {r : S} : spanNorm R (span 
({r} : Set S)) = span {Algebra.intNorm R S r}
· 使用定理 `map_one`：map_one [OneHomClass F M N] (f : F) : f 1 = 1
· 使用定理 `MonoidHomClass.toOneHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem spanNorm_top : spanNorm R (⊤ : Ideal S) = ⊤ := by
  simp [← Ideal.span_singleton_one]
/-
**Ideal.map_spanIntNorm** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：map_spanIntNorm (I : Ideal S) {T : Type*} [Semiring T] (f : R ->+* T) : ma
p f (spanNorm R I) = span (f ∘ Algebra.intNorm R S '' (I : Set S))
参数：I : Ideal S；f : R ->+* T。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.spanNorm.eq_1`：∀ (R : Type u_1) [inst : CommRing R] [inst_1 : IsDo
main R] {S : Type u_3} [inst_2 : CommRing S] [inst_3 : IsDomain S]   [inst_4 : I
sIntegral…
· 使用定理 `Ideal.map.eq_1`：∀ {R : Type u} {S : Type v} {F : Type u_1} [inst : Semir
ing R] [inst_1 : Semiring S] [inst_2 : FunLike F R S] (f : F)   (I : Ideal R), I
deal…
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `Ideal.map_span`：map_span (s : Set R) : map f (span s) = span (f '' s)
· 使用定理 `Set.image_image`：image_image (g : β -> γ) (f : α -> β) (s : Set α) : g '
' f '' s = (fun x => g (f x)) '' s
· 使用定理 `Set.image_congr`：image_congr {f g : α -> β} {s : Set α} (h : forall a in
 s, f a = g a) : f '' s = g '' s
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem map_spanIntNorm (I : Ideal S) {T : Type*} [Semiring T] (f : R →+* T) :
    map f (spanNorm R I) = span (f ∘ Algebra.intNorm R S '' (I : Set S)) := by
  rw [spanNorm]
  nth_rw 2 [map]
  simp [map_span, Set.image_image]

@[gcongr, mono]
/-
**Ideal.spanNorm_mono** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：spanNorm_mono {I J : Ideal S} (h : I <= J) : spanNorm R I <= spanNorm R J
参数：h : I <= J。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.span_mono`：span_mono {s t : Set α} : s subseteq t -> span s <= spa
n t
· 使用引理 `Set.monotone_image`：monotone_image : Monotone (image f)
-/
theorem spanNorm_mono {I J : Ideal S} (h : I ≤ J) : spanNorm R I ≤ spanNorm R J :=
  Ideal.span_mono (Set.monotone_image h)

set_option backward.isDefEq.respectTransparency.types false in
/-
**Ideal.spanIntNorm_localization** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：spanIntNorm_localization (I : Ideal S) (M : Submonoid R) (hM : M <= R⁰) {R
ₘ : Type*} (Sₘ : Type*) [CommRing Rₘ] [Algebra R Rₘ] [CommRing Sₘ] [Algebra S Sₘ
] [Algebra Rₘ Sₘ] [Algebra R Sₘ] [IsScalarTower R Rₘ Sₘ] [IsScalarTower R S Sₘ] 
[IsLocalization M Rₘ] [IsLocalization (Algebra.algebraMapSubmonoid S M) Sₘ] [IsI
ntegrallyClosed Rₘ] [IsDomain Rₘ] [IsDomain Sₘ] [IsTorsionFree Rₘ Sₘ] [Module.Fi
nite Rₘ Sₘ] [IsIntegrallyClosed Sₘ] : spanNorm Rₘ (I.map (algebraMap S Sₘ)) = (s
panNorm R I).map (algebraM
参数：I : Ideal S；M : Submonoid R；hM : M <= R⁰；Sₘ : Type*；Algebra.algebraMapSubmono
id S M。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submonoid.map_le_of_le_comap`：map_le_of_le_comap {T : Submonoid N} {f : 
F} : S <= T.comap f -> S.map f <= T
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用定理 `nonZeroDivisors_le_comap_nonZeroDivisors_of_injective`：nonZeroDivisors_l
e_comap_nonZeroDivisors_of_injective [NoZeroDivisors M₀'] [MonoidWithZeroHomClas
s F M₀ M₀'] (f : F) (hf : Injective f) : M₀…
· 使用定理 `IsDomain.to_noZeroDivisors`：∀ (α : Type u_3) [inst : Semiring α] [IsDoma
in α], NoZeroDivisors α
· 使用引理 `FaithfulSMul.algebraMap_injective`：algebraMap_injective : Injective (alg
ebraMap R A)
· 使用定理 `Module.IsTorsionFree.to_faithfulSMul`：∀ {R : Type u_1} {A : Type u_2} [i
nst : CommRing R] [inst_1 : Ring A] [inst_2 : Algebra R A] [IsCancelMulZero R]  
 [Nontrivial A] [Module.Is…
· 使用定理 `IsDomain.toIsCancelMulZero`：∀ {α : Type u} {inst : Semiring α} [self : I
sDomain α], IsCancelMulZero α
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `FractionRing.instFaithfulSMul`：∀ (R : Type u_1) [inst : CommRing R] (A :
 Type u_4) [inst_1 : CommRing A] [inst_2 : Algebra R A] [FaithfulSMul R A],   Fa
ithfulSMul R (Fract…
· 使用定理 `IsScalarTower.of_algebraMap_eq'`：of_algebraMap_eq' [Algebra R A] (h : al
gebraMap R A = (algebraMap S A).comp (algebraMap R S)) : IsScalarTower R S A
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `RingHom.algebraMap_toAlgebra`：RingHom.algebraMap_toAlgebra {R S} [CommSe
miring R] [CommSemiring S] (i : R ->+* S) : @algebraMap R S _ _ i.toAlgebra = i
· 使用定理 `IsLocalization.map_comp`：map_comp : (map Q g hy).comp (algebraMap R S) =
 (algebraMap P Q).comp g
· 使用定理 `RingHomCompTriple.comp_eq`：∀ {R₁ : Type u_1} {R₂ : Type u_2} {R₃ : Type 
u_3} {inst : Semiring R₁} {inst_1 : Semiring R₂} {inst_2 : Semiring R₃}   {σ₁₂ :
 R₁ →+* R₂} {σ₂…
· 使用定理 `IsFractionRing.isFractionRing_of_isDomain_of_isLocalization`：isFractionR
ing_of_isDomain_of_isLocalization [IsDomain R] (S T : Type*) [CommRing S] [CommR
ing T] [Algebra R S] [Algebra R T] [Algebra S T] …
· 使用定理 `IsLocalization.ringHom_ext`：ringHom_ext {P : Type*} [Semiring P] ⦃j k : 
S ->+* P⦄ (h : j.comp (algebraMap R S) = k.comp (algebraMap R S)) : j = k
· 使用定理 `RingHom.comp_assoc`：comp_assoc {δ} {_ : NonAssocSemiring δ} (f : α ->+* 
β) (g : β ->+* γ) (h : γ ->+* δ) : (h.comp g).comp f = h.comp (g.comp f)
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `IsScalarTower.algebraMap_eq`：algebraMap_eq : algebraMap R A = (algebraMa
p S A).comp (algebraMap R S)
· 使用定理 `RingHom.comp_id`：comp_id (f : α ->+* β) : f.comp (id α) = f
· 使用定理 `FractionRing.instIsScalarTower`：∀ (R : Type u_1) [inst : CommRing R] (K 
: Type u_5) [inst_1 : Field K] [inst_2 : Algebra R K]   [inst_3 : FaithfulSMul R
 K] {R₀ : Type u_6} …
· 使用定理 `OreLocalization.instIsScalarTower`：∀ {R : Type u_1} {R' : Type u_2} {M :
 Type u_3} {X : Type u_4} [inst : Monoid M] {S : Submonoid M}   [inst_1 : OreLoc
alization.OreSet S] [in…
· 使用定理 `Ideal.map_spanIntNorm`：map_spanIntNorm (I : Ideal S) {T : Type*} [Semiri
ng T] (f : R ->+* T) : map f (spanNorm R I) = span (f ∘ Algebra.intNorm R S '' (
I : Set S))
· 使用定理 `Submodule.span_eq_span`：span_eq_span (hs : s subseteq span R t) (ht : t 
subseteq span R s) : span R s = span R t
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Set.image_subset_iff`：image_subset_iff {s : Set α} {t : Set β} {f : α ->
 β} : f '' s subseteq t ↔ s subseteq f ⁻¹' t
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
（共 63 条，此处仅展示前 30 条）
-/
theorem spanIntNorm_localization (I : Ideal S) (M : Submonoid R) (hM : M ≤ R⁰)
    {Rₘ : Type*} (Sₘ : Type*) [CommRing Rₘ] [Algebra R Rₘ] [CommRing Sₘ] [Algebra S Sₘ]
    [Algebra Rₘ Sₘ] [Algebra R Sₘ] [IsScalarTower R Rₘ Sₘ] [IsScalarTower R S Sₘ]
    [IsLocalization M Rₘ] [IsLocalization (Algebra.algebraMapSubmonoid S M) Sₘ]
    [IsIntegrallyClosed Rₘ] [IsDomain Rₘ] [IsDomain Sₘ] [IsTorsionFree Rₘ Sₘ]
    [Module.Finite Rₘ Sₘ] [IsIntegrallyClosed Sₘ] :
    spanNorm Rₘ (I.map (algebraMap S Sₘ)) = (spanNorm R I).map (algebraMap R Rₘ) := by
  let K := FractionRing R
  let f : Rₘ →+* K := IsLocalization.map _ (T := R⁰) (RingHom.id R) hM
  let L := FractionRing S
  let g : Sₘ →+* L := IsLocalization.map _ (M := Algebra.algebraMapSubmonoid S M) (T := S⁰)
      (RingHom.id S) (Submonoid.map_le_of_le_comap _ <| hM.trans
      (nonZeroDivisors_le_comap_nonZeroDivisors_of_injective _
        (FaithfulSMul.algebraMap_injective _ _)))
  algebraize [f, g, (algebraMap K L).comp f]
  have : IsScalarTower R Rₘ K := IsScalarTower.of_algebraMap_eq'
    (by rw [RingHom.algebraMap_toAlgebra, IsLocalization.map_comp, RingHomCompTriple.comp_eq])
  let _ := IsFractionRing.isFractionRing_of_isDomain_of_isLocalization M Rₘ K
  have : IsScalarTower S Sₘ L := IsScalarTower.of_algebraMap_eq'
    (by rw [RingHom.algebraMap_toAlgebra, IsLocalization.map_comp, RingHomCompTriple.comp_eq])
  have : IsScalarTower Rₘ Sₘ L := by
    apply IsScalarTower.of_algebraMap_eq'
    apply IsLocalization.ringHom_ext M
    rw [RingHom.algebraMap_toAlgebra, RingHom.algebraMap_toAlgebra (R := Sₘ), RingHom.comp_assoc,
      RingHom.comp_assoc, ← IsScalarTower.algebraMap_eq, IsScalarTower.algebraMap_eq R S Sₘ,
      IsLocalization.map_comp, RingHom.comp_id, ← RingHom.comp_assoc, IsLocalization.map_comp,
      RingHom.comp_id, ← IsScalarTower.algebraMap_eq, ← IsScalarTower.algebraMap_eq]
  let _ := IsFractionRing.isFractionRing_of_isDomain_of_isLocalization
    (Algebra.algebraMapSubmonoid S M) Sₘ L
  rw [map_spanIntNorm]
  refine span_eq_span (Set.image_subset_iff.mpr ?_) (Set.image_subset_iff.mpr ?_)
  · intro a' ha'
    simp only [Set.mem_preimage, submodule_span_eq, ← map_spanIntNorm, SetLike.mem_coe,
      IsLocalization.mem_map_algebraMap_iff (Algebra.algebraMapSubmonoid S M) Sₘ,
      IsLocalization.mem_map_algebraMap_iff M Rₘ, Prod.exists] at ha' ⊢
    obtain ⟨⟨a, ha⟩, ⟨_, ⟨s, hs, rfl⟩⟩, has⟩ := ha'
    refine ⟨⟨Algebra.intNorm R S a, intNorm_mem_spanNorm _ ha⟩,
      ⟨s ^ Module.finrank K L, pow_mem hs _⟩, ?_⟩
    simp only [map_pow] at has ⊢
    apply_fun algebraMap _ L at has
    apply_fun Algebra.norm K at has
    simp only [map_mul] at has
    rw [← IsScalarTower.algebraMap_apply, ← IsScalarTower.algebraMap_apply,
      ← IsScalarTower.algebraMap_apply,
      IsScalarTower.algebraMap_apply R K L,
      Algebra.norm_algebraMap] at has
    apply IsFractionRing.injective Rₘ K
    simp only [map_mul, map_pow]
    rwa [Algebra.algebraMap_intNorm (L := L), ← IsScalarTower.algebraMap_apply,
      ← IsScalarTower.algebraMap_apply, Algebra.algebraMap_intNorm (L := L)]
  · intro a ha
    rw [Set.mem_preimage, Function.comp_apply, Algebra.intNorm_eq_of_isLocalization M (Bₘ := Sₘ)]
    exact subset_span (Set.mem_image_of_mem _ (mem_map_of_mem _ ha))
/-
**Ideal.spanNorm_mul_spanNorm_le** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：spanNorm_mul_spanNorm_le (I J : Ideal S) : spanNorm R I * spanNorm R J <= 
spanNorm R (I * J)
参数：I J : Ideal S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.spanNorm.eq_1`：∀ (R : Type u_1) [inst : CommRing R] [inst_1 : IsDo
main R] {S : Type u_3} [inst_2 : CommRing S] [inst_3 : IsDomain S]   [inst_4 : I
sIntegral…
· 使用定理 `Ideal.map.eq_1`：∀ {R : Type u} {S : Type v} {F : Type u_1} [inst : Semir
ing R] [inst_1 : Semiring S] [inst_2 : FunLike F R S] (f : F)   (I : Ideal R), I
deal…
· 使用定理 `Ideal.span_mul_span'`：span_mul_span' (S T : Set R) [(span S).IsTwoSided]
 : span S * span T = span (S * T)
· 使用定理 `Ideal.instIsTwoSided_1`：∀ {α : Type u_1} [inst : CommRing α] (I : Ideal 
α), I.IsTwoSided
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Set.image_mul`：image_mul : m '' (s * t) = m '' s * m '' t
· 使用定理 `MonoidHomClass.toMulHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `Ideal.span_mono`：span_mono {s t : Set α} : s subseteq t -> span s <= spa
n t
· 使用引理 `Set.monotone_image`：monotone_image : Monotone (image f)
· 使用定理 `Ideal.mul_mem_mul`：mul_mem_mul {r s} (hr : r in I) (hs : s in J) : r * s
 in I * J
-/
theorem spanNorm_mul_spanNorm_le (I J : Ideal S) :
    spanNorm R I * spanNorm R J ≤ spanNorm R (I * J) := by
  rw [spanNorm, spanNorm, spanNorm]
  nth_rw 1 [map]; nth_rw 1 [map]
  rw [Ideal.span_mul_span', ← Set.image_mul]
  refine Ideal.span_mono (Set.monotone_image ?_)
  rintro _ ⟨x, hxI, y, hyJ, rfl⟩
  exact Ideal.mul_mem_mul hxI hyJ

/-- This condition `eq_bot_or_top` is equivalent to being a field.
However, `Ideal.spanNorm_mul_of_field` is harder to apply since we'd need to upgrade a `CommRing R`
/-
**Ideal.to** 是 Mathlib 中的一个实例，位于命名空间 `Ideal`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance to a `Field R` instance. -/
/-
**Ideal.spanNorm_mul_of_bot_or_top** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：spanNorm_mul_of_bot_or_top (eq_bot_or_top : forall I : Ideal R, I = ⊥ ∨ I 
= ⊤) (I J : Ideal S) : spanNorm R (I * J) = spanNorm R I * spanNorm R J
参数：eq_bot_or_top : forall I : Ideal R, I = ⊥ ∨ I = ⊤；I J : Ideal S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Ideal.spanNorm_eq_bot_iff`：spanNorm_eq_bot_iff {I : Ideal S} : spanNorm 
R I = ⊥ ↔ I = ⊥
· 使用定理 `Ideal.bot_mul`：bot_mul : ⊥ * I = ⊥
· 使用定理 `Ideal.spanNorm_bot`：spanNorm_bot : spanNorm R (⊥ : Ideal S) = ⊥
· 使用定理 `bot_le`：∀ {α : Type u} [inst : LE α] [inst_1 : OrderBot α] {a : α}, ⊥ ≤ 
a
· 使用定理 `Ideal.top_mul`：top_mul : ⊤ * I = I
· 使用定理 `Ideal.mul_bot`：mul_bot : I * ⊥ = ⊥
· 使用定理 `le_refl`：∀ {α : Type u_1} [inst : Preorder α] (a : α), a ≤ a
· 使用定理 `le_top`：le_top : a <= ⊤
· 使用定理 `Ideal.spanNorm_mul_spanNorm_le`：spanNorm_mul_spanNorm_le (I J : Ideal S)
 : spanNorm R I * spanNorm R J <= spanNorm R (I * J)

--- 原说明 ---
This condition `eq_bot_or_top` is equivalent to being a field.
However, `Ideal.spanNorm_mul_of_field` is harder to apply since we'd need to upg
rade a `CommRing R`
instance to a `Field R` instance.
-/
theorem spanNorm_mul_of_bot_or_top (eq_bot_or_top : ∀ I : Ideal R, I = ⊥ ∨ I = ⊤) (I J : Ideal S) :
    spanNorm R (I * J) = spanNorm R I * spanNorm R J := by
  refine le_antisymm ?_ (spanNorm_mul_spanNorm_le R _ _)
  rcases eq_bot_or_top (spanNorm R I) with hI | hI
  · rw [hI, spanNorm_eq_bot_iff.mp hI, bot_mul, spanNorm_bot]
    exact bot_le
  rw [hI, Ideal.top_mul]
  rcases eq_bot_or_top (spanNorm R J) with hJ | hJ
  · rw [hJ, spanNorm_eq_bot_iff.mp hJ, mul_bot, spanNorm_bot]
  rw [hJ]
  exact le_top
/-
**Ideal.spanNorm_le_comap** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：spanNorm_le_comap (I : Ideal S) : spanNorm R I <= comap (algebraMap R S) I
参数：I : Ideal S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.spanNorm.eq_1`：∀ (R : Type u_1) [inst : CommRing R] [inst_1 : IsDo
main R] {S : Type u_3} [inst_2 : CommRing S] [inst_3 : IsDomain S]   [inst_4 : I
sIntegral…
· 使用定理 `Ideal.map.eq_1`：∀ {R : Type u} {S : Type v} {F : Type u_1} [inst : Semir
ing R] [inst_1 : Semiring S] [inst_2 : FunLike F R S] (f : F)   (I : Ideal R), I
deal…
· 使用定理 `Ideal.span_le`：span_le {s : Set α} {I} : span s <= I ↔ s subseteq I
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Submodule.span_le`：span_le {p} : span R s <= p ↔ s subseteq p
· 使用定理 `Submodule.span_induction`：span_induction {p : (x : M) -> x in span R s -
> Prop} (mem : forall (x) (h : x in s), p x (subset_span h)) (zero : p 0 (Submod
ule.zero_mem _…
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Ideal.mem_comap`：mem_comap [RingHomClass F R S] {x} : x in comap f K ↔ f
 x in K
· 使用引理 `Ideal.mem_of_dvd`：mem_of_dvd (hab : a ∣ b) (ha : a in I) : b in I
· 使用定理 `Algebra.dvd_algebraMap_intNorm_self`：Algebra.dvd_algebraMap_intNorm_self
 (x : B) : x ∣ algebraMap A B (intNorm A B x)
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `AddSubmonoidClass.toZeroMemClass`：∀ {S : Type u_3} {M : outParam (Type u
_4)} {inst : AddZeroClass M} {inst_1 : SetLike S M}   [self : AddSubmonoidClass 
S M], ZeroMemClass S M
· 使用定理 `Submodule.add_mem`：∀ {R : Type u} {M : Type v} [inst : Semiring R] [inst
_1 : AddCommMonoid M] {module_M : _root_.Module R M}   (p : Submodule R M) {x y 
: M}, x…
· 使用定理 `Submodule.smul_mem`：smul_mem (r : R) (h : x in p) : r • x in p
-/
theorem spanNorm_le_comap (I : Ideal S) : spanNorm R I ≤ comap (algebraMap R S) I := by
  rw [spanNorm, Ideal.map, Ideal.span_le, ← Submodule.span_le]
  intro x hx
  induction hx using Submodule.span_induction with
  | mem _ h =>
      obtain ⟨x, hx, rfl⟩ := h
      exact mem_comap.mpr <| mem_of_dvd _ (Algebra.dvd_algebraMap_intNorm_self _ _ x) hx
  | zero => simp
  | add _ _ _ _ hx hy => exact Submodule.add_mem _ hx hy
  | smul _ _ _ hx => exact Submodule.smul_mem _ _ hx

set_option linter.overlappingInstances false

/-- Multiplicativity of `Ideal.spanNorm`. simp-normal form is `map_mul (Ideal.relNorm R)`. -/
/-
**Ideal.spanNorm_mul** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：spanNorm_mul [IsDedekindDomain R] [IsDedekindDomain S] (I J : Ideal S) : s
panNorm R (I * J) = spanNorm R I * spanNorm R J
参数：I J : Ideal S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `subsingleton_or_nontrivial`：subsingleton_or_nontrivial (α : Type*) : Sub
singleton α ∨ Nontrivial α
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `Subsingleton.elim`：∀ {α : Sort u} [h : Subsingleton α] (a b : α), a = b
· 使用定理 `Unique.instSubsingleton`：∀ {α : Sort u_1} [Unique α], Subsingleton α
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.spanNorm.congr_simp`：∀ (R : Type u_1) [inst : CommRing R] [inst_1 
: IsDomain R] {S : Type u_3} [inst_2 : CommRing S] [inst_3 : IsDomain S]   [inst
_4 : IsIntegral…
· 使用定理 `Ideal.mul_top`：mul_top [I.IsTwoSided] : I * ⊤ = I
· 使用定理 `Ideal.instIsTwoSided_1`：∀ {α : Type u_1} [inst : CommRing α] (I : Ideal 
α), I.IsTwoSided
· 使用定理 `Ideal.spanNorm_top`：spanNorm_top : spanNorm R (⊤ : Ideal S) = ⊤
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Ideal.eq_of_localization_maximal`：Ideal.eq_of_localization_maximal {I J 
: Ideal R} (h : forall (P : Ideal R) (_ : P.IsMaximal), Ideal.map (algebraMap R 
(Localization.AtPrime …
· 使用定理 `Ideal.IsMaximal.isPrime'`：∀ {α : Type u} [inst : CommSemiring α] (I : Id
eal α) [_H : I.IsMaximal], I.IsPrime
· 使用定理 `Ideal.spanNorm_mul_of_bot_or_top`：spanNorm_mul_of_bot_or_top (eq_bot_or_
top : forall I : Ideal R, I = ⊥ ∨ I = ⊤) (I J : Ideal S) : spanNorm R (I * J) = 
spanNorm R I * spanNor…
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Classical.or_iff_not_imp_right`：∀ {a b : Prop}, a ∨ b ↔ ¬b → a
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.IsMaximal.eq_of_le`：∀ {α : Type u} [inst : Semiring α] {I J : Idea
l α}, I.IsMaximal → J ≠ ⊤ → I ≤ J → I = J
· 使用定理 `bot_le`：∀ {α : Type u} [inst : LE α] [inst_1 : OrderBot α] {a : α}, ⊥ ≤ 
a
· 使用定理 `IsLocalization.instIsDomainLocalizationAlgebraMapSubmonoidPrimeComplOfFa
ithfulSMul`：∀ {R : Type u_1} [inst : CommRing R] (S : Type u_2) [inst_1 : CommRi
ng S] [inst_2 : Algebra R S] {P : Ideal R}   [inst_3 : P.IsPrime] [IsDom…
· 使用定理 `Module.Flat.instOfIsDedekindDomainOfIsTorsionFree`：∀ {R : Type u_1} {M :
 Type u_2} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module
 R M]   [IsDedekindDomain R] [Module.Is…
· 使用定理 `Module.IsTorsionFree.to_faithfulSMul`：∀ {R : Type u_1} {A : Type u_2} [i
nst : CommRing R] [inst_1 : Ring A] [inst_2 : Algebra R A] [IsCancelMulZero R]  
 [Nontrivial A] [Module.Is…
· 使用定理 `IsDomain.toIsCancelMulZero`：∀ {α : Type u} {inst : Semiring α} [self : I
sDomain α], IsCancelMulZero α
· 使用定理 `instIsDedekindDomainLocalizationAlgebraMapSubmonoidPrimeCompl`：∀ {R : Ty
pe u_1} (S : Type u_2) [inst : CommRing R] [inst_1 : CommRing S] [IsDomain R] [I
sDomain S]   [inst_4 : Algebra R S] {P : Ideal R} […
· 使用定理 `Module.Finite.instAtPrimeLocalizationAlgebraMapSubmonoidPrimeCompl`：∀ {R
 : Type u_1} {S : Type u_2} [inst : CommSemiring R] {P : Ideal R} [inst_1 : Comm
Semiring S] [inst_2 : Algebra R S]   [Module.Finite R S]…
· 使用定理 `Localization.instIsTorsionFreeAtPrimeAlgebraMapSubmonoidPrimeComplOfIsDo
main`：∀ {R : Type u_4} {S : Type u_5} [inst : CommRing R] [IsDomain R] {P : Idea
l R} [inst_2 : CommRing S]   [inst_3 : Algebra R S] [Module.IsTors…
· 使用定理 `Ideal.spanIntNorm_localization`：spanIntNorm_localization (I : Ideal S) (
M : Submonoid R) (hM : M <= R⁰) {Rₘ : Type*} (Sₘ : Type*) [CommRing Rₘ] [Algebra
 R Rₘ] [CommRing Sₘ]…
· 使用定理 `Ideal.primeCompl_le_nonZeroDivisors`：Ideal.primeCompl_le_nonZeroDivisors
 {R : Type*} [CommSemiring R] [NoZeroDivisors R] (P : Ideal R) [P.IsPrime] : P.p
rimeCompl <= nonZeroDivis…
（共 41 条，此处仅展示前 30 条）

--- 原说明 ---
Multiplicativity of `Ideal.spanNorm`. simp-normal form is `map_mul (Ideal.relNor
m R)`.
-/
theorem spanNorm_mul [IsDedekindDomain R] [IsDedekindDomain S] (I J : Ideal S) :
    spanNorm R (I * J) = spanNorm R I * spanNorm R J := by
  nontriviality R
  cases subsingleton_or_nontrivial S
  · have : ∀ I : Ideal S, I = ⊤ := fun I ↦ Subsingleton.elim I ⊤
    simp [this I, this J]
  refine eq_of_localization_maximal (fun P hP ↦ ?_)
  by_cases hP0 : P = ⊥
  · subst hP0
    rw [spanNorm_mul_of_bot_or_top]
    intro I
    exact or_iff_not_imp_right.mpr fun hI ↦ (hP.eq_of_le hI bot_le).symm
  have : NeZero P := ⟨hP0⟩
  let P' := Algebra.algebraMapSubmonoid S P.primeCompl
  simp only [Ideal.map_mul, ← spanIntNorm_localization (R := R) (Sₘ := Localization P')
    _ _ P.primeCompl_le_nonZeroDivisors]
  rw [← (I.map _).span_singleton_generator, ← (J.map _).span_singleton_generator,
    span_singleton_mul_span_singleton, spanNorm_singleton, spanNorm_singleton,
    spanNorm_singleton, span_singleton_mul_span_singleton, map_mul]

section spanNorm_spanNorm

variable (T : Type*) [CommRing T] [IsDomain T] [IsIntegrallyClosed T] [Algebra R T] [Algebra T S]
  [Module.Finite R T] [Module.Finite T S] [IsTorsionFree R T] [IsTorsionFree T S]
  [IsScalarTower R T S]

open _root_.Algebra

/-
**Ideal.le_spanNorm_spanNorm** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：le_spanNorm_spanNorm (I : Ideal S) : spanNorm R I <= spanNorm R (spanNorm 
T I)
参数：I : Ideal S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.span_mono`：span_mono {s t : Set α} : s subseteq t -> span s <= spa
n t
· 使用定理 `Ideal.subset_span`：subset_span {s : Set α} : s subseteq span s
· 使用定理 `Set.mem_image_of_mem`：mem_image_of_mem (f : α -> β) {x : α} {a : Set α} 
(h : x in a) : f x in f '' a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Algebra.intNorm_intNorm`：Algebra.intNorm_intNorm {C : Type*} [CommRing C
] [IsDomain C] [IsIntegrallyClosed C] [Algebra A C] [Algebra B C] [IsScalarTower
 A B C] [Alge…
-/
theorem le_spanNorm_spanNorm (I : Ideal S) : spanNorm R I ≤ spanNorm R (spanNorm T I) := by
  simp_rw [spanNorm, map]
  refine span_mono ?_
  rintro _ ⟨x, hx, rfl⟩
  exact ⟨intNorm T S x, subset_span <| Set.mem_image_of_mem _ hx, by rw [intNorm_intNorm]⟩

/--
This condition `eq_bot_or_top` is equivalent to being a field. However,
`Ideal.spanNorm_spanNorm_of_field` would be harder to apply since we'd need to upgrade
a `CommRing R` instance to a `Field R` instance.
-/
/-
**Ideal.spanNorm_spanNorm_of_bot_or_top** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：spanNorm_spanNorm_of_bot_or_top (eq_bot_or_top : forall I : Ideal R, I = ⊥
 ∨ I = ⊤) (I : Ideal S) : spanNorm R (spanNorm T I) = spanNorm R I
参数：eq_bot_or_top : forall I : Ideal R, I = ⊥ ∨ I = ⊤；I : Ideal S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.spanNorm_eq_bot_iff`：spanNorm_eq_bot_iff {I : Ideal S} : spanNorm 
R I = ⊥ ↔ I = ⊥
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Ideal.eq_top_iff_one`：eq_top_iff_one : I = ⊤ ↔ (1 : α) in I
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.le_spanNorm_spanNorm`：le_spanNorm_spanNorm (I : Ideal S) : spanNor
m R I <= spanNorm R (spanNorm T I)

--- 原说明 ---
This condition `eq_bot_or_top` is equivalent to being a field. However,
`Ideal.spanNorm_spanNorm_of_field` would be harder to apply since we'd need to u
pgrade
a `CommRing R` instance to a `Field R` instance.
-/
theorem spanNorm_spanNorm_of_bot_or_top (eq_bot_or_top : ∀ I : Ideal R, I = ⊥ ∨ I = ⊤)
    (I : Ideal S) : spanNorm R (spanNorm T I) = spanNorm R I := by
  obtain h | h := eq_bot_or_top (spanNorm R I)
  · rw [h, spanNorm_eq_bot_iff, spanNorm_eq_bot_iff, spanNorm_eq_bot_iff.mp h]
  · exact h ▸ (eq_top_iff_one _).mpr <| le_spanNorm_spanNorm R T I <| (eq_top_iff_one _).mp h

attribute [local instance] Localization.AtPrime.algebra_localization_localization
/-
**Ideal.spanNorm_spanNorm** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：spanNorm_spanNorm [IsDedekindDomain R] [IsDedekindDomain T] [IsDedekindDom
ain S] (I : Ideal S) : spanNorm R (spanNorm T I) = spanNorm R I
参数：I : Ideal S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.eq_of_localization_maximal`：Ideal.eq_of_localization_maximal {I J 
: Ideal R} (h : forall (P : Ideal R) (_ : P.IsMaximal), Ideal.map (algebraMap R 
(Localization.AtPrime …
· 使用定理 `Ideal.IsMaximal.isPrime'`：∀ {α : Type u} [inst : CommSemiring α] (I : Id
eal α) [_H : I.IsMaximal], I.IsPrime
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.spanNorm_spanNorm_of_bot_or_top`：spanNorm_spanNorm_of_bot_or_top (
eq_bot_or_top : forall I : Ideal R, I = ⊥ ∨ I = ⊤) (I : Ideal S) : spanNorm R (s
panNorm T I) = spanNorm R I
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Classical.or_iff_not_imp_right`：∀ {a b : Prop}, a ∨ b ↔ ¬b → a
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.IsMaximal.eq_of_le`：∀ {α : Type u} [inst : Semiring α] {I J : Idea
l α}, I.IsMaximal → J ≠ ⊤ → I ≤ J → I = J
· 使用定理 `bot_le`：∀ {α : Type u} [inst : LE α] [inst_1 : OrderBot α] {a : α}, ⊥ ≤ 
a
· 使用定理 `algebraMapSubmonoid_le_nonZeroDivisors_of_faithfulSMul`：algebraMapSubmon
oid_le_nonZeroDivisors_of_faithfulSMul {A : Type*} (B : Type*) [CommSemiring A] 
[CommSemiring B] [Algebra A B] [NoZeroDiviso…
· 使用定理 `IsDomain.to_noZeroDivisors`：∀ (α : Type u_3) [inst : Semiring α] [IsDoma
in α], NoZeroDivisors α
· 使用定理 `Module.IsTorsionFree.to_faithfulSMul`：∀ {R : Type u_1} {A : Type u_2} [i
nst : CommRing R] [inst_1 : Ring A] [inst_2 : Algebra R A] [IsCancelMulZero R]  
 [Nontrivial A] [Module.Is…
· 使用定理 `IsDomain.toIsCancelMulZero`：∀ {α : Type u} {inst : Semiring α} [self : I
sDomain α], IsCancelMulZero α
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `Ideal.primeCompl_le_nonZeroDivisors`：Ideal.primeCompl_le_nonZeroDivisors
 {R : Type*} [CommSemiring R] [NoZeroDivisors R] (P : Ideal R) [P.IsPrime] : P.p
rimeCompl <= nonZeroDivis…
· 使用定理 `IsLocalization.instIsDomainLocalizationAlgebraMapSubmonoidPrimeComplOfFa
ithfulSMul`：∀ {R : Type u_1} [inst : CommRing R] (S : Type u_2) [inst_1 : CommRi
ng S] [inst_2 : Algebra R S] {P : Ideal R}   [inst_3 : P.IsPrime] [IsDom…
· 使用定理 `Module.Flat.instOfIsDedekindDomainOfIsTorsionFree`：∀ {R : Type u_1} {M :
 Type u_2} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module
 R M]   [IsDedekindDomain R] [Module.Is…
· 使用定理 `instIsDedekindDomainLocalizationAlgebraMapSubmonoidPrimeCompl`：∀ {R : Ty
pe u_1} (S : Type u_2) [inst : CommRing R] [inst_1 : CommRing S] [IsDomain R] [I
sDomain S]   [inst_4 : Algebra R S] {P : Ideal R} […
· 使用定理 `Module.Finite.instAtPrimeLocalizationAlgebraMapSubmonoidPrimeCompl`：∀ {R
 : Type u_1} {S : Type u_2} [inst : CommSemiring R] {P : Ideal R} [inst_1 : Comm
Semiring S] [inst_2 : Algebra R S]   [Module.Finite R S]…
· 使用定理 `Localization.instIsTorsionFreeAtPrimeAlgebraMapSubmonoidPrimeComplOfIsDo
main`：∀ {R : Type u_4} {S : Type u_5} [inst : CommRing R] [IsDomain R] {P : Idea
l R} [inst_2 : CommRing S]   [inst_3 : Algebra R S] [Module.IsTors…
· 使用定理 `Ideal.spanIntNorm_localization`：spanIntNorm_localization (I : Ideal S) (
M : Submonoid R) (hM : M <= R⁰) {Rₘ : Type*} (Sₘ : Type*) [CommRing Rₘ] [Algebra
 R Rₘ] [CommRing Sₘ]…
· 使用定理 `instIsScalarTowerLocalizationAlgebraMapSubmonoid`：∀ {R : Type u_1} [inst
 : CommSemiring R] (M : Submonoid R) (S : Type u_2) [inst_1 : CommSemiring S]   
[inst_2 : Algebra R S], IsScalarTower …
· 使用定理 `OreLocalization.instIsScalarTower`：∀ {R : Type u_1} {R' : Type u_2} {M :
 Type u_3} {X : Type u_4} [inst : Monoid M] {S : Submonoid M}   [inst_1 : OreLoc
alization.OreSet S] [in…
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `instFiniteLocalizationAlgebraMapSubmonoidPrimeCompl`：∀ {R : Type u_1} (S
 : Type u_2) (T : Type u_3) [inst : CommRing R] [inst_1 : CommRing S] [inst_2 : 
CommRing T]   [inst_3 : Algebra R S] {P :…
· 使用定理 `instIsTorsionFreeLocalizationAlgebraMapSubmonoidPrimeCompl_2`：∀ {R : Typ
e u_1} (S : Type u_2) (T : Type u_3) [inst : CommRing R] [inst_1 : CommRing S] [
inst_2 : CommRing T]   [IsDomain R] [IsDomain S] […
· 使用定理 `instIsScalarTowerLocalizationAlgebraMapSubmonoidPrimeCompl`：∀ {R : Type 
u_1} (S : Type u_2) (T : Type u_3) [inst : CommRing R] [inst_1 : CommRing S] [in
st_2 : CommRing T]   [inst_3 : Algebra R S] {P :…
· 使用定理 `instIsLocalizationAlgebraMapSubmonoidPrimeComplLocalization`：∀ {R : Type
 u_1} (S : Type u_2) (T : Type u_3) [inst : CommRing R] [inst_1 : CommRing S] [i
nst_2 : CommRing T]   [inst_3 : Algebra R S] {P :…
· 使用定理 `IsPrincipalIdealRing.principal`：∀ {R : Type u} {inst : Semiring R} [self
 : IsPrincipalIdealRing R] (S : Ideal R), Submodule.IsPrincipal S
· 使用定理 `instIsPrincipalIdealRingLocalizationAlgebraMapSubmonoidPrimeComplOfIsDed
ekindDomainOfFiniteOfNeZeroIdeal`：∀ {R : Type u_1} (S : Type u_2) [inst : CommRi
ng R] [inst_1 : CommRing S] [IsDomain R] [IsDomain S]   [inst_4 : Algebra R S] {
P : Ideal R} […
（共 35 条，此处仅展示前 30 条）
-/
theorem spanNorm_spanNorm [IsDedekindDomain R] [IsDedekindDomain T] [IsDedekindDomain S]
    (I : Ideal S) : spanNorm R (spanNorm T I) = spanNorm R I := by
  refine eq_of_localization_maximal fun P hP ↦ ?_
  by_cases hP : P = ⊥
  · subst hP
    rw [spanNorm_spanNorm_of_bot_or_top]
    exact fun I ↦ or_iff_not_imp_right.mpr fun hI ↦ (hP.eq_of_le hI bot_le).symm
  let Rₚ := Localization.AtPrime P
  let Tₚ := Localization (algebraMapSubmonoid T P.primeCompl)
  let Sₚ := Localization (algebraMapSubmonoid S P.primeCompl)
  have : NeZero P := ⟨hP⟩
  have h : algebraMapSubmonoid T P.primeCompl ≤ T⁰ :=
      algebraMapSubmonoid_le_nonZeroDivisors_of_faithfulSMul _ (primeCompl_le_nonZeroDivisors P)
  rw [← spanIntNorm_localization R (spanNorm T I) _ (primeCompl_le_nonZeroDivisors P) Tₚ,
    ← spanIntNorm_localization T (Rₘ := Tₚ) I _ h Sₚ, ← spanIntNorm_localization R (Rₘ := Rₚ) I _
    (primeCompl_le_nonZeroDivisors P) Sₚ, ← (I.map _).span_singleton_generator, spanNorm_singleton,
    spanNorm_singleton, intNorm_intNorm, spanNorm_singleton]

end spanNorm_spanNorm

variable [IsDedekindDomain R] [IsDedekindDomain S]

/-- The relative norm `Ideal.relNorm R (I : Ideal S)`, where `R` and `S` are Dedekind domains,
and `S` is an extension of `R` that is finite and free as a module. -/
/-
**Ideal.relNorm** 是 Mathlib 中的一个定义，位于命名空间 `Ideal`。
形式化陈述：relNorm : Ideal S ->*₀ Ideal R where toFun
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.spanNorm_bot`：spanNorm_bot : spanNorm R (⊥ : Ideal S) = ⊥
· 使用定理 `Ideal.spanNorm_mul`：spanNorm_mul [IsDedekindDomain R] [IsDedekindDomain 
S] (I J : Ideal S) : spanNorm R (I * J) = spanNorm R I * spanNorm R J

--- 原说明 ---
The relative norm `Ideal.relNorm R (I : Ideal S)`, where `R` and `S` are Dedekin
d domains,
and `S` is an extension of `R` that is finite and free as a module.
-/
noncomputable def relNorm : Ideal S →*₀ Ideal R where
  toFun := spanNorm R
  map_zero' := spanNorm_bot R
  map_one' := by rw [one_eq_top, spanNorm_top R, one_eq_top]
  map_mul' := spanNorm_mul R
/-
**Ideal.relNorm_apply** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_apply (I : Ideal S) : relNorm R I = span (Algebra.intNorm R S '' (
I : Set S) : Set R)
参数：I : Ideal S。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem relNorm_apply (I : Ideal S) :
    relNorm R I = span (Algebra.intNorm R S '' (I : Set S) : Set R) :=
  rfl

@[simp]
/-
**Ideal.spanNorm_eq** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：spanNorm_eq (I : Ideal S) : spanNorm R I = relNorm R I
参数：I : Ideal S。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem spanNorm_eq (I : Ideal S) : spanNorm R I = relNorm R I := rfl

@[simp]
/-
**Ideal.relNorm_bot** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_bot : relNorm R (⊥ : Ideal S) = ⊥
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `map_zero`：∀ {M : Type u_4} {N : Type u_5} {F : Type u_9} [inst : Zero M]
 [inst_1 : Zero N] [inst_2 : FunLike F M N]   [ZeroHomClass F M N] (f : F), f …
· 使用定理 `MonoidWithZeroHomClass.toZeroHomClass`：∀ {F : Type u_7} {α : outParam (T
ype u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : MulZe
roOneClass β} {inst_2 : Fun…
-/
theorem relNorm_bot : relNorm R (⊥ : Ideal S) = ⊥ := by
  simpa only [zero_eq_bot] using map_zero (relNorm R : Ideal S →*₀ _)

@[simp]
/-
**Ideal.relNorm_top** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_top : relNorm R (⊤ : Ideal S) = ⊤
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.one_eq_top`：one_eq_top : (1 : Ideal R) = ⊤
· 使用定理 `map_one`：map_one [OneHomClass F M N] (f : F) : f 1 = 1
· 使用定理 `MonoidHomClass.toOneHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
-/
theorem relNorm_top : relNorm R (⊤ : Ideal S) = ⊤ := by
  simpa only [one_eq_top] using map_one (relNorm R : Ideal S →*₀ _)

variable {R} in
@[simp]
/-
**Ideal.relNorm_eq_bot_iff** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_eq_bot_iff {I : Ideal S} : relNorm R I = ⊥ ↔ I = ⊥
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.spanNorm_eq_bot_iff`：spanNorm_eq_bot_iff {I : Ideal S} : spanNorm 
R I = ⊥ ↔ I = ⊥
-/
theorem relNorm_eq_bot_iff {I : Ideal S} : relNorm R I = ⊥ ↔ I = ⊥ :=
  spanNorm_eq_bot_iff
/-
**Ideal.norm_mem_relNorm** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：norm_mem_relNorm [Module.Free R S] (I : Ideal S) {x : S} (hx : x in I) : A
lgebra.norm R x in relNorm R I
参数：I : Ideal S；hx : x in I。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.norm_mem_spanNorm`：norm_mem_spanNorm [Module.Free R S] {I : Ideal 
S} (x : S) (hx : x in I) : Algebra.norm R x in I.spanNorm R
-/
theorem norm_mem_relNorm [Module.Free R S] (I : Ideal S) {x : S} (hx : x ∈ I) :
    Algebra.norm R x ∈ relNorm R I :=
  norm_mem_spanNorm R x hx

@[simp]
/-
**Ideal.relNorm_singleton** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_singleton (r : S) : relNorm R (span ({r} : Set S)) = span {Algebra
.intNorm R S r}
参数：r : S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.spanNorm_singleton`：spanNorm_singleton {r : S} : spanNorm R (span 
({r} : Set S)) = span {Algebra.intNorm R S r}
-/
theorem relNorm_singleton (r : S) : relNorm R (span ({r} : Set S)) = span {Algebra.intNorm R S r} :=
  spanNorm_singleton R
/-
**Ideal.map_relNorm** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：map_relNorm (I : Ideal S) {T : Type*} [Semiring T] (f : R ->+* T) : map f 
(relNorm R I) = span (f ∘ Algebra.intNorm R S '' (I : Set S))
参数：I : Ideal S；f : R ->+* T。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.map_spanIntNorm`：map_spanIntNorm (I : Ideal S) {T : Type*} [Semiri
ng T] (f : R ->+* T) : map f (spanNorm R I) = span (f ∘ Algebra.intNorm R S '' (
I : Set S))
-/
theorem map_relNorm (I : Ideal S) {T : Type*} [Semiring T] (f : R →+* T) :
    map f (relNorm R I) = span (f ∘ Algebra.intNorm R S '' (I : Set S)) :=
  map_spanIntNorm R I f

@[gcongr, mono]
/-
**Ideal.relNorm_mono** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_mono {I J : Ideal S} (h : I <= J) : relNorm R I <= relNorm R J
参数：h : I <= J。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.spanNorm_mono`：spanNorm_mono {I J : Ideal S} (h : I <= J) : spanNo
rm R I <= spanNorm R J
-/
theorem relNorm_mono {I J : Ideal S} (h : I ≤ J) : relNorm R I ≤ relNorm R J :=
  spanNorm_mono R h

variable {R}
/-
**Ideal.relNorm_map_algEquiv_aux** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
private theorem relNorm_map_algEquiv_aux {T : Type*} [CommRing T] [IsDedekindDomain T]
    [IsIntegrallyClosed T] [Algebra R T] [Module.Finite R T] [IsTorsionFree R T]
    (σ : S ≃ₐ[R] T) (I : Ideal S) : relNorm R (I.map σ) ≤ relNorm R I :=
  span_mono fun _ ⟨x, hx₁, hx₂⟩ ↦ ⟨σ.toRingEquiv.symm x,
    by rwa [SetLike.mem_coe, Ideal.symm_apply_mem_of_equiv_iff],
    hx₂ ▸ Algebra.intNorm_map_algEquiv _ x σ.symm⟩

@[simp]
/-
**Ideal.relNorm_map_algEquiv** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_map_algEquiv {T : Type*} [CommRing T] [IsDedekindDomain T] [IsInte
grallyClosed T] [Algebra R T] [Module.Finite R T] [IsTorsionFree R T] (σ : S ≃ₐ[
R] T) (I : Ideal S) : relNorm R (I.map σ) = relNorm R I
参数：σ : S ≃ₐ[R] T；I : Ideal S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `IsDedekindDomain.toIsDomain`：∀ {A : Type u_2} {inst : CommRing A} [self 
: IsDedekindDomain A], IsDomain A
· 使用定理 `_private.Mathlib.RingTheory.Ideal.Norm.RelNorm.0.Ideal.relNorm_map_algEq
uiv_aux`：∀ {R : Type u_1} [inst : CommRing R] [inst_1 : IsDomain R] {S : Type u_
3} [inst_2 : CommRing S] [inst_3 : IsDomain S]   [inst_4 : IsIntegral…
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `Ideal.map_mapₐ`：map_mapₐ {R A B C : Type*} [CommSemiring R] [Semiring A]
 [Algebra R A] [Semiring B] [Algebra R B] [Semiring C] [Algebra R C] {I : Ideal 
A} (…
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `AlgEquiv.symm_comp`：symm_comp (e : A₁ ≃ₐ[R] A₂) : AlgHom.comp ↑e.symm (e
 : A₁ ->ₐ[R] A₂) = AlgHom.id R A₁
· 使用引理 `Ideal.map_idₐ`：map_idₐ {R S : Type*} [CommSemiring R] [Semiring S] [Alge
bra R S] (I : Ideal S) : Ideal.map (AlgHom.id R S) I = I
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem relNorm_map_algEquiv {T : Type*} [CommRing T] [IsDedekindDomain T] [IsIntegrallyClosed T]
    [Algebra R T] [Module.Finite R T] [IsTorsionFree R T] (σ : S ≃ₐ[R] T) (I : Ideal S) :
    relNorm R (I.map σ) = relNorm R I := by
  refine le_antisymm (relNorm_map_algEquiv_aux σ I) ?_
  convert! relNorm_map_algEquiv_aux σ.symm (I.map σ)
  change I = map σ.symm.toAlgHom (map σ.toAlgHom I)
  simp [map_mapₐ]

@[simp]
/-
**Ideal.relNorm_comap_algEquiv** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_comap_algEquiv {T : Type*} [CommRing T] [IsDedekindDomain T] [IsIn
tegrallyClosed T] [Algebra R T] [Module.Finite R T] [IsTorsionFree R T] (σ : S ≃
ₐ[R] T) (I : Ideal T) : relNorm R (I.comap σ) = relNorm R I
参数：σ : S ≃ₐ[R] T；I : Ideal T。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsDedekindDomain.toIsDomain`：∀ {A : Type u_2} {inst : CommRing A} [self 
: IsDedekindDomain A], IsDomain A
· 使用定理 `Ideal.relNorm_map_algEquiv`：relNorm_map_algEquiv {T : Type*} [CommRing T
] [IsDedekindDomain T] [IsIntegrallyClosed T] [Algebra R T] [Module.Finite R T] 
[IsTorsionFree R…
· 使用定理 `RingEquivClass.toRingHomClass`：∀ {F : Type u_1} {R : Type u_4} {S : Type
 u_5} [inst : EquivLike F R S] [inst_1 : NonAssocSemiring R]   [inst_2 : NonAsso
cSemiring S] [h : R…
· 使用定理 `RingEquiv.instRingEquivClass`：∀ {R : Type u_4} {S : Type u_5} [inst : Mu
l R] [inst_1 : Mul S] [inst_2 : Add R] [inst_3 : Add S],   RingEquivClass (R ≃+*
 S) R S
· 使用定理 `Ideal.map_symm`：map_symm {I : Ideal S} (f : R ≃+* S) : I.map f.symm = I.
comap f
-/
theorem relNorm_comap_algEquiv {T : Type*} [CommRing T] [IsDedekindDomain T] [IsIntegrallyClosed T]
    [Algebra R T] [Module.Finite R T] [IsTorsionFree R T] (σ : S ≃ₐ[R] T) (I : Ideal T) :
    relNorm R (I.comap σ) = relNorm R I := map_symm σ.toRingEquiv ▸ relNorm_map_algEquiv σ.symm I

variable (R)

open MulSemiringAction Pointwise in
@[simp]
/-
**Ideal.relNorm_smul** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_smul {G : Type*} [Group G] [MulSemiringAction G S] [SMulCommClass 
G R S] (g : G) (I : Ideal S) : relNorm R (g • I) = relNorm R I
参数：g : G；I : Ideal S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.relNorm_map_algEquiv`：relNorm_map_algEquiv {T : Type*} [CommRing T
] [IsDedekindDomain T] [IsIntegrallyClosed T] [Algebra R T] [Module.Finite R T] 
[IsTorsionFree R…
-/
theorem relNorm_smul {G : Type*} [Group G] [MulSemiringAction G S] [SMulCommClass G R S] (g : G)
    (I : Ideal S) : relNorm R (g • I) = relNorm R I := relNorm_map_algEquiv (toAlgEquiv R S g) I
/-
**Ideal.relNorm_le_comap** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_le_comap (I : Ideal S) : relNorm R I <= comap (algebraMap R S) I
参数：I : Ideal S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.spanNorm_le_comap`：spanNorm_le_comap (I : Ideal S) : spanNorm R I 
<= comap (algebraMap R S) I
-/
theorem relNorm_le_comap (I : Ideal S) : relNorm R I ≤ comap (algebraMap R S) I :=
  spanNorm_le_comap R I
/-
**Ideal.relNorm_relNorm** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_relNorm (T : Type*) [CommRing T] [IsDedekindDomain T] [IsIntegrall
yClosed T] [Algebra R T] [Algebra T S] [IsScalarTower R T S] [Module.Finite R T]
 [Module.Finite T S] [IsTorsionFree R T] [IsTorsionFree T S] (I : Ideal S) : rel
Norm R (relNorm T I) = relNorm R I
参数：T : Type*；I : Ideal S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.spanNorm_spanNorm`：spanNorm_spanNorm [IsDedekindDomain R] [IsDedek
indDomain T] [IsDedekindDomain S] (I : Ideal S) : spanNorm R (spanNorm T I) = sp
anNorm R I
· 使用定理 `IsDedekindDomain.toIsDomain`：∀ {A : Type u_2} {inst : CommRing A} [self 
: IsDedekindDomain A], IsDomain A
-/
theorem relNorm_relNorm (T : Type*) [CommRing T] [IsDedekindDomain T] [IsIntegrallyClosed T]
    [Algebra R T] [Algebra T S] [IsScalarTower R T S] [Module.Finite R T] [Module.Finite T S]
    [IsTorsionFree R T] [IsTorsionFree T S]
    (I : Ideal S) : relNorm R (relNorm T I) = relNorm R I :=
  spanNorm_spanNorm _ _ _

variable {R} (S)

attribute [local instance] Localization.AtPrime.liftAlgebra in
/-
**Ideal.relNorm_algebraMap** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_algebraMap (I : Ideal R) : relNorm R (I.map (algebraMap R S)) = I 
^ finrank R S
参数：I : Ideal R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.spanNorm_eq`：spanNorm_eq (I : Ideal S) : spanNorm R I = relNorm R 
I
· 使用定理 `Ideal.eq_of_localization_maximal`：Ideal.eq_of_localization_maximal {I J 
: Ideal R} (h : forall (P : Ideal R) (_ : P.IsMaximal), Ideal.map (algebraMap R 
(Localization.AtPrime …
· 使用定理 `Ideal.IsMaximal.isPrime'`：∀ {α : Type u} [inst : CommSemiring α] (I : Id
eal α) [_H : I.IsMaximal], I.IsPrime
· 使用定理 `IsLocalization.instIsDomainLocalizationAlgebraMapSubmonoidPrimeComplOfFa
ithfulSMul`：∀ {R : Type u_1} [inst : CommRing R] (S : Type u_2) [inst_1 : CommRi
ng S] [inst_2 : Algebra R S] {P : Ideal R}   [inst_3 : P.IsPrime] [IsDom…
· 使用定理 `Module.Flat.instOfIsDedekindDomainOfIsTorsionFree`：∀ {R : Type u_1} {M :
 Type u_2} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module
 R M]   [IsDedekindDomain R] [Module.Is…
· 使用定理 `Module.IsTorsionFree.to_faithfulSMul`：∀ {R : Type u_1} {A : Type u_2} [i
nst : CommRing R] [inst_1 : Ring A] [inst_2 : Algebra R A] [IsCancelMulZero R]  
 [Nontrivial A] [Module.Is…
· 使用定理 `IsDomain.toIsCancelMulZero`：∀ {α : Type u} {inst : Semiring α} [self : I
sDomain α], IsCancelMulZero α
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `instIsDedekindDomainLocalizationAlgebraMapSubmonoidPrimeCompl`：∀ {R : Ty
pe u_1} (S : Type u_2) [inst : CommRing R] [inst_1 : CommRing S] [IsDomain R] [I
sDomain S]   [inst_4 : Algebra R S] {P : Ideal R} […
· 使用定理 `Module.Finite.instAtPrimeLocalizationAlgebraMapSubmonoidPrimeCompl`：∀ {R
 : Type u_1} {S : Type u_2} [inst : CommSemiring R] {P : Ideal R} [inst_1 : Comm
Semiring S] [inst_2 : Algebra R S]   [Module.Finite R S]…
· 使用定理 `Localization.instIsTorsionFreeAtPrimeAlgebraMapSubmonoidPrimeComplOfIsDo
main`：∀ {R : Type u_4} {S : Type u_5} [inst : CommRing R] [IsDomain R] {P : Idea
l R} [inst_2 : CommRing S]   [inst_3 : Algebra R S] [Module.IsTors…
· 使用定理 `Ideal.spanIntNorm_localization`：spanIntNorm_localization (I : Ideal S) (
M : Submonoid R) (hM : M <= R⁰) {Rₘ : Type*} (Sₘ : Type*) [CommRing Rₘ] [Algebra
 R Rₘ] [CommRing Sₘ]…
· 使用定理 `Ideal.primeCompl_le_nonZeroDivisors`：Ideal.primeCompl_le_nonZeroDivisors
 {R : Type*} [CommSemiring R] [NoZeroDivisors R] (P : Ideal R) [P.IsPrime] : P.p
rimeCompl <= nonZeroDivis…
· 使用定理 `IsDomain.to_noZeroDivisors`：∀ (α : Type u_3) [inst : Semiring α] [IsDoma
in α], NoZeroDivisors α
· 使用定理 `instIsScalarTowerLocalizationAlgebraMapSubmonoid`：∀ {R : Type u_1} [inst
 : CommSemiring R] (M : Submonoid R) (S : Type u_2) [inst_1 : CommSemiring S]   
[inst_2 : Algebra R S], IsScalarTower …
· 使用定理 `OreLocalization.instIsScalarTower`：∀ {R : Type u_1} {R' : Type u_2} {M :
 Type u_3} {X : Type u_4} [inst : Monoid M] {S : Submonoid M}   [inst_1 : OreLoc
alization.OreSet S] [in…
· 使用定理 `Ideal.map_pow`：∀ {R : Type u} {S : Type v} {F : Type u_1} [inst : CommSe
miring R] [inst_1 : CommSemiring S] [inst_2 : FunLike F R S]   [rc : RingHomClas
s F…
· 使用定理 `Ideal.map_map`：map_map {T : Type*} [Semiring T] {I : Ideal R} (f : R ->+
* S) (g : S ->+* T) : (I.map f).map g = I.map (g.comp f)
· 使用定理 `IsScalarTower.algebraMap_eq`：algebraMap_eq : algebraMap R A = (algebraMa
p S A).comp (algebraMap R S)
· 使用定理 `IsPrincipalIdealRing.principal`：∀ {R : Type u} {inst : Semiring R} [self
 : IsPrincipalIdealRing R] (S : Ideal R), Submodule.IsPrincipal S
· 使用定理 `IsDedekindDomain.isPrincipalIdealRing`：∀ (R : Type u_1) [inst : CommRing
 R] [IsLocalRing R] [IsDedekindDomain R], IsPrincipalIdealRing R
· 使用定理 `Localization.AtPrime.isLocalRing`：∀ {R : Type u_1} [inst : CommSemiring 
R] (P : Ideal R) [hp : P.IsPrime], IsLocalRing (Localization P.primeCompl)
· 使用定理 `Ideal.span_singleton_generator`：∀ {R : Type u} [inst : Semiring R] (I : 
Ideal R) [inst_1 : Submodule.IsPrincipal I],   Ideal.span {Submodule.IsPrincipal
.generator I} = I
· 使用定理 `Ideal.map_span`：map_span (s : Set R) : map f (span s) = span (f '' s)
· 使用定理 `Set.image_singleton`：image_singleton {f : α -> β} {a : α} : f '' {a} = {
f a}
· 使用定理 `Ideal.spanNorm_singleton`：spanNorm_singleton {r : S} : spanNorm R (span 
({r} : Set S)) = span {Algebra.intNorm R S r}
· 使用定理 `Ideal.span_singleton_pow`：span_singleton_pow (s : R) [(span {s}).IsTwoSi
ded] (n : Nat) : span {s} ^ n = (span {s ^ n} : Ideal R)
（共 58 条，此处仅展示前 30 条）
-/
theorem relNorm_algebraMap (I : Ideal R) :
    relNorm R (I.map (algebraMap R S)) = I ^ finrank R S := by
  rw [← spanNorm_eq]
  refine eq_of_localization_maximal (fun P hPd ↦ ?_)
  let P' := Algebra.algebraMapSubmonoid S P.primeCompl
  let Rₚ := Localization.AtPrime P
  let K := FractionRing R
  rw [← spanIntNorm_localization R _ _ P.primeCompl_le_nonZeroDivisors (Localization P'),
      Ideal.map_pow, I.map_map, ← IsScalarTower.algebraMap_eq, IsScalarTower.algebraMap_eq R Rₚ,
      ← I.map_map, ← (I.map _).span_singleton_generator, Ideal.map_span, Set.image_singleton,
      spanNorm_singleton, Ideal.span_singleton_pow]
  congr 2
  apply IsFractionRing.injective Rₚ K
  rw [Algebra.algebraMap_intNorm (L := FractionRing S), ← IsScalarTower.algebraMap_apply,
    IsScalarTower.algebraMap_apply Rₚ K, Algebra.norm_algebraMap, map_pow,
    IsFractionRing.finrank_eq R (FractionRing R) S (FractionRing S)]

variable (R)

/-- A version of `relNorm_algebraMap` involving a tower of algebras `S/R/R'`. -/
/-
**Ideal.relNorm_algebraMap'** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_algebraMap' {R'} [CommRing R'] (I : Ideal R') [Algebra R' R] [Alge
bra R' S] [IsScalarTower R' R S] : relNorm R (I.map (algebraMap R' S)) = I.map (
algebraMap R' R) ^ finrank R S
参数：I : Ideal R'。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.relNorm_algebraMap`：relNorm_algebraMap (I : Ideal R) : relNorm R (
I.map (algebraMap R S)) = I ^ finrank R S
· 使用定理 `Ideal.map_map`：map_map {T : Type*} [Semiring T] {I : Ideal R} (f : R ->+
* S) (g : S ->+* T) : (I.map f).map g = I.map (g.comp f)
· 使用定理 `IsScalarTower.algebraMap_eq`：algebraMap_eq : algebraMap R A = (algebraMa
p S A).comp (algebraMap R S)

--- 原说明 ---
A version of `relNorm_algebraMap` involving a tower of algebras `S/R/R'`.
-/
theorem relNorm_algebraMap' {R'} [CommRing R'] (I : Ideal R') [Algebra R' R]
    [Algebra R' S] [IsScalarTower R' R S] :
    relNorm R (I.map (algebraMap R' S)) = I.map (algebraMap R' R) ^ finrank R S := by
  rw [← relNorm_algebraMap, Ideal.map_map, IsScalarTower.algebraMap_eq R' R S]

section relNorm_prime

variable {R} {S} (P : Ideal S) (p : Ideal R) [hPp : P.LiesOver p]

/--
See `Ideal.relNorm_eq_pow_of_isMaximal` for a more precise statement when `p` is a maximal ideal.
-/
/-
**Ideal.exists_relNorm_eq_pow_of_isPrime** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：exists_relNorm_eq_pow_of_isPrime [p.IsPrime] : exists s, relNorm R P = p ^
 s
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.eq_bot_of_liesOver_bot`：eq_bot_of_liesOver_bot [Nontrivial A] [IsD
omain B] [h : P.LiesOver (⊥ : Ideal A)] : P = ⊥
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `Ideal.relNorm_bot`：relNorm_bot : relNorm R (⊥ : Ideal S) = ⊥
· 使用定理 `Ideal.bot_pow`：bot_pow {n : Nat} (hn : n != 0) : (⊥ : Ideal R) ^ n = ⊥
· 使用定理 `one_ne_zero`：∀ {α : Type u_2} [inst : Zero α] [inst_1 : One α] [NeZero 1
], 1 ≠ 0
· 使用定理 `Nat.instNeZeroSucc`：∀ {n : ℕ}, NeZero (n + 1)
· 使用定理 `Ideal.relNorm_mono`：relNorm_mono {I J : Ideal S} (h : I <= J) : relNorm 
R I <= relNorm R J
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Ideal.map_le_iff_le_comap`：map_le_iff_le_comap [RingHomClass F R S] : ma
p f I <= K ↔ I <= comap f K
· 使用定理 `le_of_eq`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a = b → a ≤ b
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Ideal.liesOver_iff`：∀ {A : Type u_2} [inst : CommSemiring A] {B : Type u
_3} [inst_1 : Semiring B] [inst_2 : Algebra A B] (P : Ideal B)   (p : Ideal A), 
P.LiesOv…
· 使用定理 `dvd_prime_pow`：dvd_prime_pow [CommMonoidWithZero M] [IsCancelMulZero M] 
{p q : M} (hp : Prime p) (n : Nat) : q ∣ p ^ n ↔ exists i <= n, Associated q (p 
^ i…
· 使用定理 `Ideal.prime_of_isPrime`：prime_of_isPrime {P : Ideal A} (hP : P != ⊥) (h 
: IsPrime P) : Prime P
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.dvd_iff_le`：Ideal.dvd_iff_le {I J : Ideal A} : I ∣ J ↔ J <= I
· 使用定理 `Ideal.relNorm_algebraMap`：relNorm_algebraMap (I : Ideal R) : relNorm R (
I.map (algebraMap R S)) = I ^ finrank R S
· 使用定理 `associated_iff_eq`：associated_iff_eq {x y : M} : x ~ᵤ y ↔ x = y
· 使用定理 `Unique.instSubsingleton`：∀ {α : Sort u_1} [Unique α], Subsingleton α

--- 原说明 ---
See `Ideal.relNorm_eq_pow_of_isMaximal` for a more precise statement when `p` is
 a maximal ideal.
-/
theorem exists_relNorm_eq_pow_of_isPrime [p.IsPrime] : ∃ s, relNorm R P = p ^ s := by
  by_cases hp : p = ⊥
  · refine ⟨1, ?_⟩
    have : P.LiesOver ⊥ := hp ▸ hPp
    rw [hp, eq_bot_of_liesOver_bot R P, relNorm_bot, bot_pow (one_ne_zero)]
  have h : relNorm R (map (algebraMap R S) p) ≤ relNorm R P :=
    relNorm_mono _ <| map_le_iff_le_comap.mpr <| le_of_eq <| (liesOver_iff _ _).mp hPp
  rw [relNorm_algebraMap S, ← dvd_iff_le, dvd_prime_pow (prime_of_isPrime hp inferInstance)] at h
  obtain ⟨s, _, hs⟩ := h
  exact ⟨s, by rwa [associated_iff_eq] at hs⟩

/--
See `Ideal.relNorm_eq_pow_of_isMaximal` for a statement that does not require the extension to
be Galois.
-/
/-
**Ideal.relNorm_eq_pow_of_isPrime_isGalois** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_eq_pow_of_isPrime_isGalois [p.IsMaximal] [P.IsPrime] [IsGalois (Fr
actionRing R) (FractionRing S)] : relNorm R P = p ^ P.inertiaDeg R
参数：FractionRing R；FractionRing S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `FractionRing.instFaithfulSMul`：∀ (R : Type u_1) [inst : CommRing R] (A :
 Type u_4) [inst_1 : CommRing A] [inst_2 : Algebra R A] [FaithfulSMul R A],   Fa
ithfulSMul R (Fract…
· 使用定理 `Module.Flat.instOfIsDedekindDomainOfIsTorsionFree`：∀ {R : Type u_1} {M :
 Type u_2} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module
 R M]   [IsDedekindDomain R] [Module.Is…
· 使用定理 `Module.IsTorsionFree.to_faithfulSMul`：∀ {R : Type u_1} {A : Type u_2} [i
nst : CommRing R] [inst_1 : Ring A] [inst_2 : Algebra R A] [IsCancelMulZero R]  
 [Nontrivial A] [Module.Is…
· 使用定理 `IsDomain.toIsCancelMulZero`：∀ {α : Type u} {inst : Semiring α} [self : I
sDomain α], IsCancelMulZero α
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `Ideal.IsMaximal.of_liesOver_isMaximal`：∀ {A : Type u_1} [inst : CommRing
 A] {B : Type u_2} [inst_1 : CommRing B] [inst_2 : Algebra A B]   [Algebra.IsInt
egral A B] (P : Ideal B) (p…
· 使用定理 `FractionRing.instIsScalarTower`：∀ (R : Type u_1) [inst : CommRing R] (K 
: Type u_5) [inst_1 : Field K] [inst_2 : Algebra R K]   [inst_3 : FaithfulSMul R
 K] {R₀ : Type u_6} …
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `OreLocalization.instIsScalarTower`：∀ {R : Type u_1} {R' : Type u_2} {M :
 Type u_3} {X : Type u_4} [inst : Monoid M] {S : Submonoid M}   [inst_1 : OreLoc
alization.OreSet S] [in…
· 使用定理 `IsIntegralClosure.of_isIntegrallyClosed`：∀ (R : Type u_1) (S : Type u_2)
 [inst : CommRing R] [inst_1 : CommRing S] (K : Type u_3) [inst_2 : CommRing K] 
  [inst_3 : Algebra R K] [ifr…
· 使用定理 `FractionRing.instNontrivial`：∀ (R : Type u_1) [inst : CommRing R] [Nontr
ivial R], Nontrivial (FractionRing R)
· 使用定理 `IsGalois.to_isSeparable`：∀ {F : Type u_1} {inst : Field F} {E : Type u_2
} {inst_1 : Field E} {inst_2 : Algebra F E} [self : IsGalois F E],   Algebra.IsS
eparable F E
· 使用定理 `IsGaloisGroup.of_isFractionRing`：IsGaloisGroup.of_isFractionRing [hGKL :
 IsGaloisGroup G K L] [IsIntegrallyClosed A] [Algebra.IsIntegral A B] : IsGalois
Group G A B
· 使用定理 `instSMulDistribClassAlgEquiv`：∀ (A : Type u_1) (K : Type u_2) (L : Type 
u_3) (B : Type u_4) [inst : CommRing A] [inst_1 : CommRing B]   [inst_2 : Field 
K] [inst_3 : Field…
· 使用定理 `LT.lt.ne'`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, b < a → a ≠ b
· 使用定理 `Ideal.inertiaDeg_pos`：inertiaDeg_pos [hq : q.IsPrime] [Module.Finite R S
] : 0 < q.inertiaDeg R
· 使用定理 `Ideal.eq_bot_of_liesOver_bot`：eq_bot_of_liesOver_bot [Nontrivial A] [IsD
omain B] [h : P.LiesOver (⊥ : Ideal A)] : P = ⊥
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.relNorm_bot`：relNorm_bot : relNorm R (⊥ : Ideal S) = ⊥
· 使用定理 `Ideal.bot_pow`：bot_pow {n : Nat} (hn : n != 0) : (⊥ : Ideal R) ^ n = ⊥
· 使用定理 `Ideal.exists_relNorm_eq_pow_of_isPrime`：exists_relNorm_eq_pow_of_isPrime
 [p.IsPrime] : exists s, relNorm R P = p ^ s
· 使用定理 `Ideal.IsMaximal.isPrime'`：∀ {α : Type u} [inst : CommSemiring α] (I : Id
eal α) [_H : I.IsMaximal], I.IsPrime
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `Set.mem_toFinset`：mem_toFinset {s : Set α} [Fintype s] {a : α} : a in s.
toFinset ↔ a in s
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.ramificationIdxIn_eq_ramificationIdx`：ramificationIdxIn_eq_ramific
ationIdx : ramificationIdxIn p B = P.ramificationIdx A
· 使用定理 `Finite.algEquiv`：∀ {R : Type uR} {A₁ : Type uA₁} {A₂ : Type uA₂} [inst :
 CommSemiring R] [inst_1 : Semiring A₁] [inst_2 : Semiring A₂]   [inst_3 : Algeb
ra R …
· 使用定理 `instFiniteAlgHomOfFinite`：∀ (R : Type u_1) [inst : CommSemiring R] (K : 
Type u_2) [inst_1 : Field K] [inst_2 : Algebra R K] (S : Type u_3)   [inst_3 : S
emiring S] [in…
· 使用定理 `instFiniteDimensionalFractionRingOfFinite`：∀ {R : Type u_1} {S : Type u_
2} [inst : CommRing R] [inst_1 : CommRing S] [inst_2 : Algebra R S] [inst_3 : Is
Domain R]   [inst_4 : IsDomain …
（共 61 条，此处仅展示前 30 条）

--- 原说明 ---
See `Ideal.relNorm_eq_pow_of_isMaximal` for a statement that does not require th
e extension to
be Galois.
-/
theorem relNorm_eq_pow_of_isPrime_isGalois [p.IsMaximal] [P.IsPrime]
    [IsGalois (FractionRing R) (FractionRing S)] : relNorm R P = p ^ P.inertiaDeg R := by
  have : P.IsMaximal := IsMaximal.of_liesOver_isMaximal P p
  let G := Gal(FractionRing S/FractionRing R)
  let := IsIntegralClosure.MulSemiringAction R (FractionRing R) (FractionRing S) S
  have := IsGaloisGroup.of_isFractionRing G R S (FractionRing R) (FractionRing S)
  by_cases hp : p = ⊥
  · have h : P.inertiaDeg R ≠ 0 := (inertiaDeg_pos P R).ne'
    have hP : P = ⊥ := by
      rw [hp] at hPp
      exact eq_bot_of_liesOver_bot R P
    rw [hp, hP, relNorm_bot, bot_pow]
    rwa [hP] at h
  obtain ⟨s, hs⟩ := exists_relNorm_eq_pow_of_isPrime P p
  suffices s = P.inertiaDeg R by rwa [this] at hs
  have h₀ : ∀ Q ∈ (p.primesOver S).toFinset,
      relNorm R Q ^ Q.ramificationIdx R = p ^ ((p.ramificationIdxIn S) * s) := by
    intro Q hQ
    rw [Set.mem_toFinset] at hQ
    have : Q.IsPrime := hQ.1
    have : Q.LiesOver p := hQ.2
    rw [← ramificationIdxIn_eq_ramificationIdx p Q G]
    obtain ⟨σ, rfl⟩ := Ideal.exists_smul_eq_of_isGaloisGroup p P Q G
    rw [relNorm_smul, hs, ← pow_mul, mul_comm]
  have h := (congr_arg (relNorm R ·) <|
    map_algebraMap_eq_finsetProd_pow hp).symm.trans <| relNorm_algebraMap S p
  simp +contextual only [map_prod, map_pow, h₀, Finset.prod_const, ← pow_mul] at h
  rwa [← IsGaloisGroup.card_eq_finrank' G R S,
    ← Ideal.ncard_primesOver_mul_ramificationIdxIn_mul_inertiaDegIn p S G, mul_comm,
    ← Set.ncard_eq_toFinset_card',
    ((IsLeftCancelMulZero.mul_left_cancel_of_ne_zero hp).pow_injective _).eq_iff,
    mul_right_inj' (IsDedekindDomain.primesOver_ncard_ne_zero p S),
    mul_right_inj' (ramificationIdxIn_ne_zero G), inertiaDegIn_eq_inertiaDeg p P G] at h
  rw [one_eq_top]
  exact IsMaximal.ne_top inferInstance
/-
**Ideal.relNorm_eq_pow_of_isMaximal** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_eq_pow_of_isMaximal [PerfectField (FractionRing R)] [P.IsMaximal] 
[p.IsMaximal] : relNorm R P = p ^ P.inertiaDeg R
参数：FractionRing R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `Ideal.exists_maximal_ideal_liesOver_of_isIntegral`：exists_maximal_ideal_
liesOver_of_isIntegral [Algebra.IsIntegral R S] [FaithfulSMul R S] (P : Ideal R)
 [P.IsMaximal] : exists (Q : Ideal S), …
· 使用定理 `Ring.instFiniteNormalClosure`：∀ (R : Type u_1) (S : Type u_2) [inst : Co
mmRing R] [inst_1 : CommRing S] [inst_2 : IsDomain R] [inst_3 : IsDomain S]   [i
nst_4 : Algebra R …
· 使用定理 `Module.IsTorsionFree.to_faithfulSMul`：∀ {R : Type u_1} {A : Type u_2} [i
nst : CommRing R] [inst_1 : Ring A] [inst_2 : Algebra R A] [IsCancelMulZero R]  
 [Nontrivial A] [Module.Is…
· 使用定理 `IsDomain.toIsCancelMulZero`：∀ {α : Type u} {inst : Semiring α} [self : I
sDomain α], IsCancelMulZero α
· 使用定理 `Ring.instNontrivialNormalClosure`：∀ (R : Type u_1) (S : Type u_2) [inst 
: CommRing R] [inst_1 : CommRing S] [inst_2 : IsDomain R] [inst_3 : IsDomain S] 
  [inst_4 : Algebra R …
· 使用定理 `Ring.instIsTorsionFreeNormalClosure`：∀ (R : Type u_1) (S : Type u_2) [in
st : CommRing R] [inst_1 : CommRing S] [inst_2 : IsDomain R] [inst_3 : IsDomain 
S]   [inst_4 : Algebra R …
· 使用定理 `Ideal.LiesOver.trans`：∀ {A : Type u_2} [inst : CommSemiring A] {B : Type
 u_3} [inst_1 : CommSemiring B] {C : Type u_4} [inst_2 : Semiring C]   [inst_3 :
 Algebra A…
· 使用定理 `Ring.instIsScalarTowerNormalClosure`：∀ (R : Type u_1) (S : Type u_2) [in
st : CommRing R] [inst_1 : CommRing S] [inst_2 : IsDomain R] [inst_3 : IsDomain 
S]   [inst_4 : Algebra R …
· 使用定理 `Ring.instIsDomainNormalClosure`：∀ (R : Type u_1) (S : Type u_2) [inst : 
CommRing R] [inst_1 : CommRing S] [inst_2 : IsDomain R] [inst_3 : IsDomain S]   
[inst_4 : Algebra R …
· 使用定理 `Ring.instIsIntegrallyClosedNormalClosure`：∀ (R : Type u_1) (S : Type u_2
) [inst : CommRing R] [inst_1 : CommRing S] [inst_2 : IsDomain R] [inst_3 : IsDo
main S]   [inst_4 : Algebra R …
· 使用定理 `Ring.instFiniteNormalClosure_1`：∀ (R : Type u_1) (S : Type u_2) [inst : 
CommRing R] [inst_1 : CommRing S] [inst_2 : IsDomain R] [inst_3 : IsDomain S]   
[inst_4 : Algebra R …
· 使用定理 `Module.Flat.instOfIsDedekindDomainOfIsTorsionFree`：∀ {R : Type u_1} {M :
 Type u_2} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module
 R M]   [IsDedekindDomain R] [Module.Is…
· 使用定理 `FaithfulSMul.to_isTorsionFree`：∀ (R : Type u_1) (A : Type u_3) [inst : C
ommSemiring R] [inst_1 : Semiring A] [inst_2 : Algebra R A] [FaithfulSMul R A]  
 [Nontrivial R] [Is…
· 使用定理 `Ring.instFaithfulSMulNormalClosure`：∀ (R : Type u_1) (S : Type u_2) [ins
t : CommRing R] [inst_1 : CommRing S] [inst_2 : IsDomain R] [inst_3 : IsDomain S
]   [inst_4 : Algebra R …
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `Ring.instIsDedekindDomainNormalClosure`：∀ (R : Type u_1) (S : Type u_2) 
[inst : CommRing R] [inst_1 : CommRing S] [inst_2 : IsDomain R] [inst_3 : IsDoma
in S]   [inst_4 : Algebra R …
· 使用定理 `Ideal.relNorm_eq_pow_of_isPrime_isGalois`：relNorm_eq_pow_of_isPrime_isGa
lois [p.IsMaximal] [P.IsPrime] [IsGalois (FractionRing R) (FractionRing S)] : re
lNorm R P = p ^ P.inertiaDeg R
· 使用定理 `Ideal.IsMaximal.isPrime'`：∀ {α : Type u} [inst : CommSemiring α] (I : Id
eal α) [_H : I.IsMaximal], I.IsPrime
· 使用定理 `Ring.instIsGaloisFractionRingNormalClosure`：∀ (R : Type u_1) (S : Type u
_2) [inst : CommRing R] [inst_1 : CommRing S] [inst_2 : IsDomain R] [inst_3 : Is
Domain S]   [inst_4 : Algebra R …
· 使用定理 `FractionRing.instFaithfulSMul`：∀ (R : Type u_1) [inst : CommRing R] (A :
 Type u_4) [inst_1 : CommRing A] [inst_2 : Algebra R A] [FaithfulSMul R A],   Fa
ithfulSMul R (Fract…
· 使用定理 `IsGalois.tower_top_of_isGalois`：IsGalois.tower_top_of_isGalois [IsGalois
 F E] : IsGalois K E
· 使用定理 `FractionRing.instIsScalarTower_1`：∀ (A : Type u_4) [inst : CommRing A] [
IsDomain A] (k : Type u_6) (K : Type u_7) [inst_2 : Field k] [inst_3 : Field K] 
  [inst_4 : Algebra A …
· 使用定理 `FractionRing.instIsScalarTower`：∀ (R : Type u_1) [inst : CommRing R] (K 
: Type u_5) [inst_1 : Field K] [inst_2 : Algebra R K]   [inst_3 : FaithfulSMul R
 K] {R₀ : Type u_6} …
· 使用定理 `OreLocalization.instIsScalarTower`：∀ {R : Type u_1} {R' : Type u_2} {M :
 Type u_3} {X : Type u_4} [inst : Monoid M] {S : Submonoid M}   [inst_1 : OreLoc
alization.OreSet S] [in…
· 使用定理 `IsDedekindDomain.toIsDomain`：∀ {A : Type u_2} {inst : CommRing A} [self 
: IsDedekindDomain A], IsDomain A
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `pow_left_inj`：pow_left_inj (hn : n != 0) : a ^ n = b ^ n ↔ a = b
· 使用定理 `UniqueFactorizationMonoid.instIsMulTorsionFree`：∀ {M : Type u_1} [inst :
 CommMonoidWithZero M] [UniqueFactorizationMonoid M] [NormalizationMonoid M]   [
IsMulTorsionFree Mˣ], IsMulTorsionFr…
· 使用定理 `Subsingleton.to_isMulTorsionFree`：∀ {M : Type u_1} [inst : Monoid M] [Su
bsingleton M], IsMulTorsionFree M
（共 39 条，此处仅展示前 30 条）
-/
theorem relNorm_eq_pow_of_isMaximal [PerfectField (FractionRing R)] [P.IsMaximal] [p.IsMaximal] :
    relNorm R P = p ^ P.inertiaDeg R := by
  let T := Ring.NormalClosure R S
  obtain ⟨Q, hQ₁, hQ₂⟩ : ∃ Q : Ideal T, Q.IsMaximal ∧ Q.LiesOver P :=
    exists_maximal_ideal_liesOver_of_isIntegral P
  have : Q.LiesOver p := LiesOver.trans Q P p
  have h := relNorm_eq_pow_of_isPrime_isGalois Q p
  have : IsGalois (FractionRing S) (FractionRing T) :=
    IsGalois.tower_top_of_isGalois (FractionRing R) (FractionRing S) (FractionRing T)
  rwa [← relNorm_relNorm R S, relNorm_eq_pow_of_isPrime_isGalois Q P, map_pow,
    inertiaDeg_tower (R := R) P Q, pow_mul, pow_left_inj (inertiaDeg_pos Q S).ne'] at h

end relNorm_prime

section absNorm

variable [Module.Free ℤ R] [Module.Free ℤ S] [Module.Finite ℤ S]

open UniqueFactorizationMonoid in
/-
**Ideal.absNorm_relNorm** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：absNorm_relNorm [PerfectField (FractionRing R)] (I : Ideal S) : absNorm (r
elNorm R I) = absNorm I
参数：FractionRing R；I : Ideal S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Module.Finite.left`：left [IsDomain K] [Nontrivial A] : Module.Finite F K
· 使用定理 `IsDedekindDomainDvr.toIsNoetherian`：∀ {A : Type u_1} {inst : CommRing A}
 {inst_1 : IsDomain A} [self : IsDedekindDomainDvr A], IsNoetherian A A
· 使用定理 `Int.instIsDomain`：IsDomain ℤ
· 使用定理 `IsPrincipalIdealRing.isDedekindDomain`：∀ (A : Type u_2) [inst : CommRing
 A] [IsDomain A] [IsPrincipalIdealRing A], IsDedekindDomain A
· 使用定理 `EuclideanDomain.to_principal_ideal_domain`：∀ {R : Type u} [inst : Euclid
eanDomain R], IsPrincipalIdealRing R
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.relNorm_bot`：relNorm_bot : relNorm R (⊥ : Ideal S) = ⊥
· 使用定理 `Ideal.absNorm_bot`：absNorm_bot : absNorm (⊥ : Ideal S) = 0
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.prod_normalizedFactors_eq_self`：prod_normalizedFactors_eq_self (hI
 : I != ⊥) : (normalizedFactors I).prod = I
· 使用定理 `Multiset.prod_induction`：prod_induction (p : M -> Prop) (s : Multiset M)
 (p_mul : forall a b, p a -> p b -> p (a * b)) (p_one : p 1) (p_s : forall a in 
s, p a) : p s…
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `map_mul`：map_mul [MulHomClass F M N] (f : F) (x y : M) : f (x * y) = f x
 * f y
· 使用定理 `MonoidHomClass.toMulHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `Ideal.one_eq_top`：one_eq_top : (1 : Ideal R) = ⊤
· 使用定理 `Ideal.relNorm_top`：relNorm_top : relNorm R (⊤ : Ideal S) = ⊤
· 使用定理 `Ideal.absNorm_top`：absNorm_top : absNorm (⊤ : Ideal S) = 1
· 使用定理 `UniqueFactorizationMonoid.ne_zero_of_mem_normalizedFactors`：ne_zero_of_m
em_normalizedFactors {x a : α} (hx : x in normalizedFactors a) : x != 0
· 使用定理 `Ring.DimensionLEOne.maximalOfPrime`：∀ {R : Type u_1} {inst : CommRing R}
 [self : Ring.DimensionLEOne R] {p : Ideal R}, p ≠ ⊥ → p.IsPrime → p.IsMaximal
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `Ideal.mem_normalizedFactors_iff`：∀ {A : Type u_2} [inst : CommRing A] [i
nst_1 : IsDedekindDomain A] {p I : Ideal A},   I ≠ ⊥ → (p ∈ UniqueFactorizationM
onoid.normalizedFacto…
· 使用定理 `AddMonoid.fg_of_addGroup_fg`：∀ {G : Type u_3} [inst : AddGroup G] [AddGr
oup.FG G], AddMonoid.FG G
· 使用定理 `AddGroup.instFGInt`：AddGroup.FG ℤ
· 使用定理 `instIsTorsionFreeIntOfIsAddTorsionFree`：∀ {M : Type u_3} [inst : AddComm
Group M] [IsAddTorsionFree M], Module.IsTorsionFree ℤ M
（共 39 条，此处仅展示前 30 条）
-/
theorem absNorm_relNorm [PerfectField (FractionRing R)] (I : Ideal S) :
    absNorm (relNorm R I) = absNorm I := by
  have : Module.Finite ℤ R := Module.Finite.left ℤ R S
  by_cases hI : I = ⊥
  · simp [hI]
  rw [← prod_normalizedFactors_eq_self hI]
  refine Multiset.prod_induction (fun I ↦ absNorm (relNorm R I) = absNorm I) _ ?_ ?_ ?_
  · intro _ _ hx hy
    rw [map_mul, map_mul, map_mul, hx, hy]
  · simp
  · intro Q hQ
    have hQ' : Q ≠ ⊥ := ne_zero_of_mem_normalizedFactors hQ
    rw [Ideal.mem_normalizedFactors_iff hI] at hQ
    have : Q.IsMaximal := Ring.DimensionLEOne.maximalOfPrime hQ' hQ.1
    let P := under R Q
    let p := absNorm (under ℤ P)
    have : Q.LiesOver (span {(p : ℤ)}) := LiesOver.trans Q P _
    rw [relNorm_eq_pow_of_isMaximal Q P, map_pow, ← pow_inertiaDeg p, ← pow_inertiaDeg p,
      ← pow_mul, ← inertiaDeg_tower]
/-
**Ideal.relNorm_int** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：relNorm_int (I : Ideal S) : relNorm Int I = Ideal.span {(absNorm I : Int)}
参数：I : Ideal S。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Int.instIsDomain`：IsDomain ℤ
· 使用定理 `IsPrincipalIdealRing.isDedekindDomain`：∀ (A : Type u_2) [inst : CommRing
 A] [IsDomain A] [IsPrincipalIdealRing A], IsDedekindDomain A
· 使用定理 `EuclideanDomain.to_principal_ideal_domain`：∀ {R : Type u} [inst : Euclid
eanDomain R], IsPrincipalIdealRing R
· 使用定理 `Module.FaithfullyFlat.toFlat`：∀ {R : Type u} {M : Type v} {inst : CommRi
ng R} {inst_1 : AddCommGroup M} {inst_2 : _root_.Module R M}   [self : Module.Fa
ithfullyFlat R M],…
· 使用定理 `Module.Flat.instOfIsDedekindDomainOfIsTorsionFree`：∀ {R : Type u_1} {M :
 Type u_2} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module
 R M]   [IsDedekindDomain R] [Module.Is…
· 使用定理 `Module.Free.instFaithfulSMulOfNontrivial`：∀ (R : Type u) (M : Type v) [i
nst : Semiring R] [inst_1 : AddCommMonoid M] [inst_2 : _root_.Module R M]   [Mod
ule.Free R M] [Nontrivial M], …
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `AddMonoid.fg_of_addGroup_fg`：∀ {G : Type u_3} [inst : AddGroup G] [AddGr
oup.FG G], AddMonoid.FG G
· 使用定理 `AddGroup.instFGInt`：AddGroup.FG ℤ
· 使用定理 `instIsTorsionFreeIntOfIsAddTorsionFree`：∀ {M : Type u_3} [inst : AddComm
Group M] [IsAddTorsionFree M], Module.IsTorsionFree ℤ M
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Int.ideal_span_absNorm_eq_self`：Int.ideal_span_absNorm_eq_self (J : Idea
l Int) : span {(absNorm J : Int)} = J
· 使用定理 `Ideal.absNorm_relNorm`：absNorm_relNorm [PerfectField (FractionRing R)] (
I : Ideal S) : absNorm (relNorm R I) = absNorm I
-/
theorem relNorm_int (I : Ideal S) :
    relNorm ℤ I = Ideal.span {(absNorm I : ℤ)} := by
  rw [← Int.ideal_span_absNorm_eq_self (relNorm ℤ I), absNorm_relNorm]
/-
**Ideal.absNorm_algebraMap** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：absNorm_algebraMap (I : Ideal R) [Module.Finite Int R] : absNorm (I.map (a
lgebraMap R S)) = absNorm I ^ finrank R S
参数：I : Ideal R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsPrincipalIdealRing.isDedekindDomain`：∀ (A : Type u_2) [inst : CommRing
 A] [IsDomain A] [IsPrincipalIdealRing A], IsDedekindDomain A
· 使用定理 `Int.instIsDomain`：IsDomain ℤ
· 使用定理 `EuclideanDomain.to_principal_ideal_domain`：∀ {R : Type u} [inst : Euclid
eanDomain R], IsPrincipalIdealRing R
· 使用定理 `AddMonoid.fg_of_addGroup_fg`：∀ {G : Type u_3} [inst : AddGroup G] [AddGr
oup.FG G], AddMonoid.FG G
· 使用定理 `AddGroup.instFGInt`：AddGroup.FG ℤ
· 使用定理 `instIsTorsionFreeIntOfIsAddTorsionFree`：∀ {M : Type u_3} [inst : AddComm
Group M] [IsAddTorsionFree M], Module.IsTorsionFree ℤ M
· 使用定理 `Module.FaithfullyFlat.toFlat`：∀ {R : Type u} {M : Type v} {inst : CommRi
ng R} {inst_1 : AddCommGroup M} {inst_2 : _root_.Module R M}   [self : Module.Fa
ithfullyFlat R M],…
· 使用定理 `Module.Flat.instOfIsDedekindDomainOfIsTorsionFree`：∀ {R : Type u_1} {M :
 Type u_2} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module
 R M]   [IsDedekindDomain R] [Module.Is…
· 使用定理 `Module.Free.instFaithfulSMulOfNontrivial`：∀ (R : Type u) (M : Type v) [i
nst : Semiring R] [inst_1 : AddCommMonoid M] [inst_2 : _root_.Module R M]   [Mod
ule.Free R M] [Nontrivial M], …
· 使用定理 `IsDomain.toNontrivial`：∀ {α : Type u} {inst : Semiring α} [self : IsDoma
in α], Nontrivial α
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.absNorm_relNorm`：absNorm_relNorm [PerfectField (FractionRing R)] (
I : Ideal S) : absNorm (relNorm R I) = absNorm I
· 使用定理 `IsDedekindDomain.toIsDomain`：∀ {A : Type u_2} {inst : CommRing A} [self 
: IsDedekindDomain A], IsDomain A
· 使用定理 `Ideal.relNorm_relNorm`：relNorm_relNorm (T : Type*) [CommRing T] [IsDedek
indDomain T] [IsIntegrallyClosed T] [Algebra R T] [Algebra T S] [IsScalarTower R
 T S] [Modu…
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `Ideal.relNorm_algebraMap`：relNorm_algebraMap (I : Ideal R) : relNorm R (
I.map (algebraMap R S)) = I ^ finrank R S
· 使用定理 `map_pow`：∀ {G : Type u_7} {H : Type u_8} {F : Type u_9} [inst : FunLike 
F G H] [inst_1 : Monoid G] [inst_2 : Monoid H]   [MonoidHomClass F G H] (f : …
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
-/
theorem absNorm_algebraMap (I : Ideal R) [Module.Finite ℤ R] :
    absNorm (I.map (algebraMap R S)) = absNorm I ^ finrank R S := by
  rw [← absNorm_relNorm ℤ, ← relNorm_relNorm ℤ R, relNorm_algebraMap, absNorm_relNorm, map_pow]

end absNorm

end Ideal

end SpanNorm

