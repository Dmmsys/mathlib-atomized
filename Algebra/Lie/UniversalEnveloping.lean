/-
Copyright (c) 2020 Oliver Nash. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Oliver Nash
-/
module

public import Mathlib.Algebra.Lie.OfAssociative
public import Mathlib.LinearAlgebra.TensorAlgebra.Basic

/-!
# Universal enveloping algebra

Given a commutative ring `R` and a Lie algebra `L` over `R`, we construct the universal
enveloping algebra of `L`, together with its universal property.

## Main definitions

  * `UniversalEnvelopingAlgebra`: the universal enveloping algebra, endowed with an
    `R`-algebra structure.
  * `UniversalEnvelopingAlgebra.ι`: the Lie algebra morphism from `L` to its universal
    enveloping algebra.
  * `UniversalEnvelopingAlgebra.lift`: given an associative algebra `A`, together with a Lie
    algebra morphism `f : L →ₗ⁅R⁆ A`, `lift R L f : UniversalEnvelopingAlgebra R L →ₐ[R] A` is the
    unique morphism of algebras through which `f` factors.
  * `UniversalEnvelopingAlgebra.ι_comp_lift`: states that the lift of a morphism is indeed part
    of a factorisation.
  * `UniversalEnvelopingAlgebra.lift_unique`: states that lifts of morphisms are indeed unique.
  * `UniversalEnvelopingAlgebra.hom_ext`: a restatement of `lift_unique` as an extensionality
    lemma.

## Tags

lie algebra, universal enveloping algebra, tensor algebra
-/

@[expose] public section


universe u₁ u₂ u₃

variable (R : Type u₁) (L : Type u₂)
variable [CommRing R] [LieRing L] [LieAlgebra R L]

local notation "ιₜ" => TensorAlgebra.ι R

namespace UniversalEnvelopingAlgebra

/-- The quotient by the ideal generated by this relation is the universal enveloping algebra.

Note that we have avoided using the more natural expression:
```
| lie_compat (x y : L) : rel (ιₜ ⁅x, y⁆) ⁅ιₜ x, ιₜ y⁆
```
so that our construction needs only the semiring structure of the tensor algebra. -/
/-
**UniversalEnvelopingAlgebra.Rel** 是 Mathlib 中的一个归纳类型，位于命名空间 `UniversalEnvelopin
gAlgebra`。
形式化陈述：(R : Type u₁) →   (L : Type u₂) →     [inst : CommRing R] →       [inst_1 
: LieRing L] → [inst_2 : LieAlgebra R L] → TensorAlgebra R L → TensorAlgebra R L
 → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The quotient by the ideal generated by this relation is the universal enveloping
 algebra.

Note that we have avoided using the more natural expression:
```
| lie_compat (x y : L) : rel (ιₜ ⁅x, y⁆) ⁅ιₜ x, ιₜ y⁆
```
so that our construction needs only the semiring structure of the tensor algebra
.
-/
inductive Rel : TensorAlgebra R L → TensorAlgebra R L → Prop
  | lie_compat (x y : L) : Rel (ιₜ ⁅x, y⁆ + ιₜ y * ιₜ x) (ιₜ x * ιₜ y)

/-- `Rel` as a ring congruence, used to build the quotient. -/
/-
**UniversalEnvelopingAlgebra.ringCon** 是 Mathlib 中的一个定义，位于命名空间 `UniversalEnvelop
ingAlgebra`。
形式化陈述：(R : Type u₁) →   (L : Type u₂) → [inst : CommRing R] → [inst_1 : LieRing 
L] → [inst_2 : LieAlgebra R L] → RingCon (TensorAlgebra R L)
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`Rel` as a ring congruence, used to build the quotient.
-/
@[no_expose] def ringCon : RingCon (TensorAlgebra R L) := ringConGen (Rel R L)

end UniversalEnvelopingAlgebra

/-- The universal enveloping algebra of a Lie algebra. -/
/-
**UniversalEnvelopingAlgebra** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：UniversalEnvelopingAlgebra
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The universal enveloping algebra of a Lie algebra.
-/
def UniversalEnvelopingAlgebra := (UniversalEnvelopingAlgebra.ringCon R L).Quotient
deriving Inhabited, Ring, Algebra R

namespace UniversalEnvelopingAlgebra

/-- The quotient map from the tensor algebra to the universal enveloping algebra as a morphism of
associative algebras. -/
/-
**UniversalEnvelopingAlgebra.mkAlgHom** 是 Mathlib 中的一个定义，位于命名空间 `UniversalEnvelo
pingAlgebra`。
形式化陈述：mkAlgHom : TensorAlgebra R L ->ₐ[R] UniversalEnvelopingAlgebra R L
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The quotient map from the tensor algebra to the universal enveloping algebra as 
a morphism of
associative algebras.
-/
def mkAlgHom : TensorAlgebra R L →ₐ[R] UniversalEnvelopingAlgebra R L :=
  RingCon.mkₐ R _

variable {L}
attribute [local instance 100] LieRing.ofAssociativeRing

set_option backward.isDefEq.respectTransparency false in
/-- The natural Lie algebra morphism from a Lie algebra to its universal enveloping algebra. -/
@[simps!]
/-
**UniversalEnvelopingAlgebra.** 是 Mathlib 中的一个定义，位于命名空间 `UniversalEnvelopingAlge
bra`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The natural Lie algebra morphism from a Lie algebra to its universal enveloping 
algebra.
-/
def ι : L →ₗ⁅R⁆ UniversalEnvelopingAlgebra R L :=
  { (mkAlgHom R L).toLinearMap.comp ιₜ with
    map_lie' := fun {x y} => by
      suffices mkAlgHom R L (ιₜ ⁅x, y⁆ + ιₜ y * ιₜ x) = mkAlgHom R L (ιₜ x * ιₜ y) by
        rw [map_mul] at this; simp [LieRing.of_associative_ring_bracket, ← this]
      exact Quotient.sound <| RingCon.le_ringConGen _ _ (Rel.lie_compat x y) }

variable {A : Type u₃} [Ring A] [Algebra R A] (f : L →ₗ⁅R⁆ A)

set_option backward.isDefEq.respectTransparency false in
/-- The universal property of the universal enveloping algebra: Lie algebra morphisms into
associative algebras lift to associative algebra morphisms from the universal enveloping algebra. -/
/-
**UniversalEnvelopingAlgebra.lift** 是 Mathlib 中的一个定义，位于命名空间 `UniversalEnveloping
Algebra`。
形式化陈述：lift : (L ->ₗ⁅R⁆ A) ≃ (UniversalEnvelopingAlgebra R L ->ₐ[R] A) where toFu
n f
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The universal property of the universal enveloping algebra: Lie algebra morphism
s into
associative algebras lift to associative algebra morphisms from the universal en
veloping algebra.
-/
def lift : (L →ₗ⁅R⁆ A) ≃ (UniversalEnvelopingAlgebra R L →ₐ[R] A) where
  toFun f :=
    RingCon.liftₐ _
      (TensorAlgebra.lift R (f : L →ₗ[R] A)) <| by
        grw [ringCon, RingCon.ringConGen_le]
        intro a b h; induction h
        simp [LieRing.of_associative_ring_bracket]
  invFun F := (F : UniversalEnvelopingAlgebra R L →ₗ⁅R⁆ A).comp (ι R)
  left_inv f := by
    ext
    -- Porting note: was
    -- simp only [ι, mkAlgHom, TensorAlgebra.lift_ι_apply, LieHom.coe_toLinearMap,
    --   LinearMap.toFun_eq_coe, LinearMap.coe_comp, LieHom.coe_comp, AlgHom.coe_toLieHom,
    --   LieHom.coe_mk, Function.comp_apply, AlgHom.toLinearMap_apply,
    --   RingQuot.liftAlgHom_mkAlgHom_apply]
    simp only [LieHom.coe_comp, Function.comp_apply, AlgHom.coe_toLieHom,
      UniversalEnvelopingAlgebra.ι_apply, mkAlgHom]
    simp [UniversalEnvelopingAlgebra]
  right_inv F := by
    apply RingCon.Quotient.hom_extₐ
    ext
    -- Porting note: was
    -- simp only [ι, mkAlgHom, TensorAlgebra.lift_ι_apply, LieHom.coe_toLinearMap,
    --   LinearMap.toFun_eq_coe, LinearMap.coe_comp, LieHom.toLinearMap_comp,
    --   AlgHom.comp_toLinearMap, Function.comp_apply, AlgHom.toLinearMap_apply,
    --   RingQuot.liftAlgHom_mkAlgHom_apply, AlgHom.coe_toLieHom, LieHom.coe_mk]
    -- extra `rfl` after https://github.com/leanprover/lean4/pull/2644
    simp [mkAlgHom]; rfl

@[simp]
/-
**UniversalEnvelopingAlgebra.lift_symm_apply** 是 Mathlib 中的一个定理，位于命名空间 `Universa
lEnvelopingAlgebra`。
形式化陈述：lift_symm_apply (F : UniversalEnvelopingAlgebra R L ->ₐ[R] A) : (lift R).s
ymm F = (F : UniversalEnvelopingAlgebra R L ->ₗ⁅R⁆ A).comp (ι R)
参数：F : UniversalEnvelopingAlgebra R L ->ₐ[R] A。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
-/
theorem lift_symm_apply (F : UniversalEnvelopingAlgebra R L →ₐ[R] A) :
    (lift R).symm F = (F : UniversalEnvelopingAlgebra R L →ₗ⁅R⁆ A).comp (ι R) :=
  rfl

@[simp]
/-
**UniversalEnvelopingAlgebra.** 是 Mathlib 中的一个定理，位于命名空间 `UniversalEnvelopingAlge
bra`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem ι_comp_lift : lift R f ∘ ι R = f :=
  funext <| LieHom.ext_iff.mp <| (lift R).symm_apply_apply f

-- `simp`-normal form is `lift_ι_apply'`.
/-
**UniversalEnvelopingAlgebra.lift_** 是 Mathlib 中的一个定理，位于命名空间 `UniversalEnvelopin
gAlgebra`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem lift_ι_apply (x : L) : lift R f (ι R x) = f x := by
  rw [← Function.comp_apply (f := lift R f) (g := ι R) (x := x), ι_comp_lift]

@[simp]
/-
**UniversalEnvelopingAlgebra.lift_** 是 Mathlib 中的一个定理，位于命名空间 `UniversalEnvelopin
gAlgebra`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem lift_ι_apply' (x : L) :
    lift R f ((UniversalEnvelopingAlgebra.mkAlgHom R L) (ιₜ x)) = f x := by
  simpa using lift_ι_apply R f x
/-
**UniversalEnvelopingAlgebra.lift_unique** 是 Mathlib 中的一个定理，位于命名空间 `UniversalEnv
elopingAlgebra`。
形式化陈述：lift_unique (g : UniversalEnvelopingAlgebra R L ->ₐ[R] A) : g ∘ ι R = f ↔ 
g = lift R f
参数：g : UniversalEnvelopingAlgebra R L ->ₐ[R] A。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用定理 `LieHom.ext`：ext {f g : L₁ ->ₗ⁅R⁆ L₂} (h : forall x, f x = g x) : f = g
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `UniversalEnvelopingAlgebra.ι_apply`：∀ (R : Type u₁) {L : Type u₂} [inst 
: CommRing R] [inst_1 : LieRing L] [inst_2 : LieAlgebra R L] (x : L),   (Univers
alEnvelopingAlgebra.ι R)…
· 使用定理 `congrFun`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, f = g →
 ∀ (a : α), f a = g a
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `Equiv.symm_apply_eq`：symm_apply_eq {α β} (e : α ≃ β) {x y} : e.symm x = 
y ↔ x = e y
-/
theorem lift_unique (g : UniversalEnvelopingAlgebra R L →ₐ[R] A) : g ∘ ι R = f ↔ g = lift R f := by
  refine Iff.trans ?_ (lift R).symm_apply_eq
  constructor <;> · intro h; ext; simp [← h]

/-- See note [partially-applied ext lemmas]. -/
@[ext]
/-
**UniversalEnvelopingAlgebra.hom_ext** 是 Mathlib 中的一个定理，位于命名空间 `UniversalEnvelop
ingAlgebra`。
形式化陈述：hom_ext {g₁ g₂ : UniversalEnvelopingAlgebra R L ->ₐ[R] A} (h : (g₁ : Unive
rsalEnvelopingAlgebra R L ->ₗ⁅R⁆ A).comp (ι R) = (g₂ : UniversalEnvelopingAlgebr
a R L ->ₗ⁅R⁆ A).comp (ι R)) : g₁ = g₂
参数：h : (g₁ : UniversalEnvelopingAlgebra R L ->ₗ⁅R⁆ A).comp (ι R) = (g₂ : Univers
alEnvelopingAlgebra R L ->ₗ⁅R⁆ A).comp (ι R)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Equiv.injective`：∀ {α : Sort u} {β : Sort v} (e : α ≃ β), Function.Injec
tive ⇑e

--- 原说明 ---
See note [partially-applied ext lemmas].
-/
theorem hom_ext {g₁ g₂ : UniversalEnvelopingAlgebra R L →ₐ[R] A}
    (h :
      (g₁ : UniversalEnvelopingAlgebra R L →ₗ⁅R⁆ A).comp (ι R) =
        (g₂ : UniversalEnvelopingAlgebra R L →ₗ⁅R⁆ A).comp (ι R)) :
    g₁ = g₂ :=
  have h' : (lift R).symm g₁ = (lift R).symm g₂ := by simp [h]
  (lift R).symm.injective h'

end UniversalEnvelopingAlgebra

