/-
Copyright (c) 2024 Yakov Pechersky. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Yakov Pechersky
-/
module

public import Mathlib.Algebra.Order.Group.Units
public import Mathlib.Algebra.Order.Monoid.LocallyFiniteOrder
public import Mathlib.Data.Int.Interval
public import Mathlib.GroupTheory.Archimedean
public import Mathlib.GroupTheory.OrderOfElement
public import Mathlib.GroupTheory.SpecificGroups.Cyclic
public import Mathlib.Order.Interval.Finset.DenselyOrdered

/-!
# Archimedean groups are either discrete or densely ordered

This file proves a few additional facts about linearly ordered additive groups which satisfy the
`Archimedean` property -- they are either order-isomorphic and additively isomorphic to the
integers, or they are densely ordered.

They are placed here in a separate file (rather than incorporated as a continuation of
`GroupTheory.Archimedean`) because they rely on some imports from pointwise lemmas.
-/

@[expose] public section

open Set
open scoped WithZero

-- no earlier file imports the necessary requirements for the next three

/-
**exists_pow_lt** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：exists_pow_lt {a : R} (ha : a < 1) (b : R) : exists n : Nat, a ^ n < b
参数：ha : a < 1；b : R。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Exists.imp`：∀ {α : Sort u_1} {p q : α → Prop}, (∀ (a : α), p a → q a) → 
(∃ a, p a) → ∃ a, q a
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `inv_pow`：∀ {α : Type u_1} [inst : DivisionMonoid α] (a : α) (n : ℕ), a⁻¹
 ^ n = (a ^ n)⁻¹
· 使用定理 `instIsLeftCancelMulOfMulLeftReflectLE`：∀ {α : Type u_1} [inst : Mul α] [
inst_1 : PartialOrder α] [MulLeftReflectLE α], IsLeftCancelMul α
· 使用定理 `IsOrderedMonoid.toMulLeftMono`：∀ {α : Type u_1} [inst : CommMonoid α] [i
nst_1 : Preorder α] [IsOrderedMonoid α], MulLeftMono α
· 使用定理 `instIsRightCancelMulOfMulRightReflectLE`：∀ {α : Type u_1} [inst : Mul α]
 [inst_1 : PartialOrder α] [MulRightReflectLE α], IsRightCancelMul α
· 使用定理 `LeftCancelSemigroup.toIsLeftCancelMul`：∀ {G : Type u} [self : LeftCancel
Semigroup G], IsLeftCancelMul G
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
· 使用定理 `exists_lt_pow`：exists_lt_pow {a : R} (ha : 1 < a) (b : R) : exists n : N
at, b < a ^ n
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `one_lt_inv'`：∀ {α : Type u} [inst : Group α] [inst_1 : LT α] [MulLeftStr
ictMono α] {a : α}, 1 < a⁻¹ ↔ a < 1
-/
theorem exists_pow_lt₀ {G : Type*} [LinearOrderedCommGroupWithZero G] [MulArchimedean G]
    {a : G} (ha : a < 1) (b : Gˣ) : ∃ n : ℕ, a ^ n < b := by
  rcases eq_or_ne a 0 with rfl | ha'
  · use 1
    simp
  · lift a to Gˣ using IsUnit.mk0 _ ha'
    simpa using! exists_pow_lt ha b

/-- The subgroup generated by an element of a group equals the set of
integer powers of the element, such that each power is a unique element.
This is the stronger version of `Subgroup.mem_closure_singleton`. -/
@[to_additive /-- The additive subgroup generated by an element of an additive group equals the set
of integer multiples of the element, such that each multiple is a unique element.
This is the stronger version of `AddSubgroup.mem_closure_singleton`. -/]
/-
**Subgroup.mem_closure_singleton_iff_existsUnique_zpow** 是 Mathlib 中的一个引理，位于命名空间
 ``。
形式化陈述：Subgroup.mem_closure_singleton_iff_existsUnique_zpow {G : Type*} [CommGrou
p G] [LinearOrder G] [IsOrderedMonoid G] {a b : G} (ha : a != 1) : b in closure 
{a} ↔ exists! k : Int, a ^ k = b
参数：ha : a != 1。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Subgroup.mem_closure_singleton`：mem_closure_singleton {x y : G} : y in c
losure ({x} : Set G) ↔ exists n : Int, x ^ n = y
· 使用定理 `Ne.lt_or_gt`：Ne.lt_or_gt (h : a != b) : a < b ∨ b < a
· 使用定理 `StrictAnti.injective`：StrictAnti.injective (hf : StrictAnti f) : Injecti
ve f
· 使用引理 `zpow_right_strictAnti`：zpow_right_strictAnti (ha : a < 1) : StrictAnti f
un n : Int => a ^ n
· 使用定理 `StrictMono.injective`：StrictMono.injective (hf : StrictMono f) : Injecti
ve f
· 使用引理 `zpow_right_strictMono`：zpow_right_strictMono (ha : 1 < a) : StrictMono f
un n : Int => a ^ n
· 使用定理 `ExistsUnique.exists`：∀ {α : Sort u_1} {p : α → Prop}, (∃! x, p x) → ∃ x,
 p x
-/
lemma Subgroup.mem_closure_singleton_iff_existsUnique_zpow {G : Type*}
    [CommGroup G] [LinearOrder G] [IsOrderedMonoid G] {a b : G} (ha : a ≠ 1) :
    b ∈ closure {a} ↔ ∃! k : ℤ, a ^ k = b := by
  rw [mem_closure_singleton]
  constructor
  · suffices Function.Injective (a ^ · : ℤ → G) by
      rintro ⟨m, rfl⟩
      exact ⟨m, rfl, fun k hk ↦ this hk⟩
    rcases ha.lt_or_gt with ha | ha
    · exact (zpow_right_strictAnti ha).injective
    · exact (zpow_right_strictMono ha).injective
  · exact fun h ↦ h.exists
/-
**Int.addEquiv_eq_refl_or_neg** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：Int.addEquiv_eq_refl_or_neg (e : Int ≃+ Int) : e = .refl _ ∨ e = .neg _
参数：e : Int ≃+ Int。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `not_isOfFinAddOrder_of_isAddTorsionFree`：∀ {G : Type u_1} [inst : AddMon
oid G] {a : G} [IsAddTorsionFree G], a ≠ 0 → ¬IsOfFinAddOrder a
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `AddEquiv.map_ne_zero_iff`：∀ {M : Type u_4} {N : Type u_5} [inst : AddZer
oClass M] [inst_1 : AddZeroClass N] (h : M ≃+ N) {x : M}, h x ≠ 0 ↔ x ≠ 0
· 使用定理 `Int.one_ne_zero`：1 ≠ 0
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `AddSubgroup.zmultiples_eq_zmultiples_iff`：∀ {G : Type u_1} [inst : AddGr
oup G] {x y : G},   ¬IsOfFinAddOrder x → (AddSubgroup.zmultiples x = AddSubgroup
.zmultiples y ↔ x = y ∨ -x = y…
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用引理 `Int.zmultiples_one`：Int.zmultiples_one : AddSubgroup.zmultiples (1 : Int
) = ⊤
· 使用定理 `AddEquivClass.instAddMonoidHomClass`：∀ (F : Type u_1) {M : Type u_4} {N 
: Type u_5} [inst : EquivLike F M N] [inst_1 : AddZeroClass M]   [inst_2 : AddZe
roClass N] [AddEquivClass…
· 使用定理 `AddEquiv.instAddEquivClass`：∀ {M : Type u_4} {N : Type u_5} [inst : Add 
M] [inst_1 : Add N], AddEquivClass (M ≃+ N) M N
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `AddSubgroup.map_equiv_top`：∀ {G : Type u_1} [inst : AddGroup G] {N : Typ
e u_5} [inst_1 : AddGroup N] {F : Type u_7} [inst_2 : EquivLike F G N]   [inst_3
 : AddEquivClas…
· 使用定理 `AddMonoidHom.map_zmultiples`：∀ {G : Type u_1} [inst : AddGroup G] {N : T
ype u_3} [inst_1 : AddGroup N] (f : G →+ N) (x : G),   AddSubgroup.map f (AddSub
group.zmultiples …
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `AddEquiv.neg_apply`：∀ (G : Type u_6) [inst : SubtractionCommMonoid G] (a
 : G), (AddEquiv.neg G) a = -a
-/
lemma Int.addEquiv_eq_refl_or_neg (e : ℤ ≃+ ℤ) : e = .refl _ ∨ e = .neg _ := by
  suffices e 1 = 1 ∨ -e 1 = 1 by simpa [AddEquiv.ext_int_iff, neg_eq_iff_eq_neg]
  have he : ¬IsOfFinAddOrder (e 1) :=
    not_isOfFinAddOrder_of_isAddTorsionFree ((AddEquiv.map_ne_zero_iff e).mpr Int.one_ne_zero)
  rw [← AddSubgroup.zmultiples_eq_zmultiples_iff he]
  simpa [e.surjective, eq_comm] using (e : ℤ →+ ℤ).map_zmultiples 1
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : Fintype (ℤ ≃+ ℤ) where
  elems := .cons (.neg _) ({.refl _}) (by simp [AddEquiv.ext_int_iff])
  complete x := by
    obtain rfl | rfl := Int.addEquiv_eq_refl_or_neg x <;>
    simp

@[simp]
/-
**Int.univ_addEquiv** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：Int.univ_addEquiv : (Finset.univ : Finset (Int ≃+ Int)) = .cons (.neg Int)
 {.refl Int} (by simp [AddEquiv.ext_int_iff])
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma Int.univ_addEquiv :
    (Finset.univ : Finset (ℤ ≃+ ℤ)) = .cons (.neg ℤ) {.refl ℤ} (by simp [AddEquiv.ext_int_iff]) :=
  rfl
/-
**Int.card_fintype_addEquiv** 是 Mathlib 中的一个定理，位于命名空间 `Int`。
形式化陈述：Fintype.card (ℤ ≃+ ℤ) = 2
参数：ℤ ≃+ ℤ。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
@[simp] lemma Int.card_fintype_addEquiv : Fintype.card (ℤ ≃+ ℤ) = 2 := rfl
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : Unique (ℤ ≃+o ℤ) where
  uniq e := OrderAddMonoidIso.toAddEquiv_injective <|
    Int.addEquiv_eq_refl_or_neg e |>.resolve_right fun H => by
      replace H : e 1 = -1 := congr($H 1)
      have h1 : 0 < e 1 := by
        rw [← map_zero e, map_lt_map_iff]
        simp
      simp [H] at h1

open OrderDual in
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : Unique (ℤ ≃+o ℤᵒᵈ) where
  default := ⟨AddEquiv.neg ℤ |>.trans ⟨toDual, toDual_add⟩, by simp⟩
  uniq e := OrderAddMonoidIso.toAddEquiv_injective <| by
    simp only [OrderAddMonoidIso.toAddEquiv_eq_coe]
    refine Int.addEquiv_eq_refl_or_neg ((e : ℤ ≃+ ℤᵒᵈ).trans ⟨toDual, toDual_add⟩)
        |>.resolve_left fun H => by
      replace H : e 1 = 1 := congr($H 1)
      have h1 : 0 < e 1 := by
        rw [← map_zero e, map_lt_map_iff]
        simp
      simp [H, ← ofDual_lt_ofDual] at h1

open Subgroup in
/-- In two linearly ordered groups, the closure of an element of one group
is isomorphic (and order-isomorphic) to the closure of an element in the other group. -/
@[to_additive /-- In two linearly ordered additive groups, the closure of an element of one group
is isomorphic (and order-isomorphic) to the closure of an element in the other group. -/]
/-
**LinearOrderedCommGroup.closure_equiv_closure** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：LinearOrderedCommGroup.closure_equiv_closure {G G' : Type*} [CommGroup G] 
[LinearOrder G] [IsOrderedMonoid G] [CommGroup G'] [LinearOrder G'] [IsOrderedMo
noid G'] (x : G) (y : G') (hxy : x = 1 ↔ y = 1) : closure ({x} : Set G) ≃*o clos
ure ({y} : Set G')
参数：x : G；y : G'；hxy : x = 1 ↔ y = 1。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
noncomputable def LinearOrderedCommGroup.closure_equiv_closure {G G' : Type*}
    [CommGroup G] [LinearOrder G] [IsOrderedMonoid G]
    [CommGroup G'] [LinearOrder G'] [IsOrderedMonoid G'] (x : G) (y : G') (hxy : x = 1 ↔ y = 1) :
    closure ({x} : Set G) ≃*o closure ({y} : Set G') :=
  if hx : x = 1 then by
    refine ⟨⟨⟨fun _ ↦ ⟨1, by simp [hxy.mp hx]⟩, fun _ ↦ ⟨1, by simp [hx]⟩, ?_, ?_⟩, ?_⟩, ?_⟩
    · intro ⟨a, ha⟩
      simpa [hx, closure_singleton_one, eq_comm] using ha
    · intro ⟨a, ha⟩
      simpa [hxy.mp hx, closure_singleton_one, eq_comm] using ha
    · intros
      simp
    · intro ⟨a, ha⟩ ⟨b, hb⟩
      simp only [hx, closure_singleton_one, mem_bot] at ha hb
      simp [ha, hb]
  else by
    set x' := max x x⁻¹ with hx'
    have xpos : 1 < x' := by
      simp [hx', eq_comm, hx]
    set y' := max y y⁻¹ with hy'
    have ypos : 1 < y' := by
      simp [hy', eq_comm, ← hxy, hx]
    have hxc : closure {x} = closure {x'} := by rcases max_cases x x⁻¹ <;> simp_all
    have hyc : closure {y} = closure {y'} := by rcases max_cases y y⁻¹ <;> simp_all
    have Hx : ∀ (z : closure {x}), z ∈ zpowers ⟨x', by simp [hxc]⟩ := by
      simp_rw [Subtype.forall, hxc, ← zpowers_eq_closure, mem_zpowers_iff, Subtype.ext_iff,
        SubgroupClass.coe_zpow, imp_self, implies_true]
    have Hy : ∀ (z : closure {y}), z ∈ zpowers ⟨y', by simp [hyc]⟩ := by
      simp_rw [Subtype.forall, hyc, ← zpowers_eq_closure, mem_zpowers_iff, Subtype.ext_iff,
        SubgroupClass.coe_zpow, imp_self, implies_true]
    refine OrderMonoidIso.mk (mulEquivOfOrderOfEq Hx Hy ?_) ?_
    · rw [orderOf_mk, orderOf_mk]
      grind [orderOf_eq_one_iff, IsMulTorsionFree.orderOf_le_one]
    · intro a b
      obtain ⟨m, rfl⟩ := Hx a
      obtain ⟨n, rfl⟩ := Hx b
      simp only [MulEquiv.toEquiv_eq_coe, Equiv.toFun_as_coe, EquivLike.coe_coe, map_zpow,
        mulEquivOfOrderOfEq_apply_gen]
      simp_all [zpow_le_zpow_iff_right, ← Subtype.coe_lt_coe]

variable {G : Type*} [CommGroup G] [LinearOrder G] [IsOrderedMonoid G] [MulArchimedean G]

@[to_additive]
/-
**Subgroup.isLeast_of_closure_iff_eq_mabs** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：Subgroup.isLeast_of_closure_iff_eq_mabs {a b : G} : IsLeast {y : G | y in 
closure ({a} : Set G) ∧ 1 < y} b ↔ b = |a|ₘ ∧ 1 < b
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subgroup.cyclic_of_min`：Subgroup.cyclic_of_min {H : Subgroup G} {a : G} 
(ha : IsLeast { g : G | g in H ∧ 1 < g } a) : H = closure {a}
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Subgroup.mem_closure_singleton`：mem_closure_singleton {x y : G} : y in c
losure ({x} : Set G) ↔ exists n : Int, x ^ n = y
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用引理 `zpow_right_inj`：zpow_right_inj (ha : 1 < a) {m n : Int} : a ^ m = a ^ n 
↔ m = n
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b
· 使用引理 `zpow_mul'`：zpow_mul' (a : α) (m n : Int) : a ^ (m * n) = (a ^ n) ^ m
· 使用引理 `zpow_one`：zpow_one (a : G) : a ^ (1 : Int) = a
· 使用定理 `eq_comm`：∀ {α : Sort u_1} {a b : α}, a = b ↔ b = a
· 使用引理 `Int.mul_eq_one_iff_eq_one_or_neg_one`：mul_eq_one_iff_eq_one_or_neg_one :
 u * v = 1 ↔ u = 1 ∧ v = 1 ∨ u = -1 ∧ v = -1
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用引理 `zpow_ofNat`：zpow_ofNat (a : G) (n : Nat) : a ^ (ofNat(n) : Int) = a ^ Of
Nat.ofNat n
· 使用引理 `pow_one`：pow_one (a : M) : a ^ 1 = a
· 使用定理 `sup_of_le_left`：∀ {α : Type u} [inst : SemilatticeSup α] {a b : α}, b ≤ 
a → a ⊔ b = a
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `LT.lt.le`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a < b → a ≤ b
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `and_self`：∀ (p : Prop), (p ∧ p) = p
· 使用定理 `zpow_neg`：∀ {α : Type u_1} [inst : DivisionMonoid α] (a : α) (n : ℤ), a 
^ (-n) = (a ^ n)⁻¹
· 使用定理 `inv_inv`：inv_inv (a : G) : a⁻¹⁻¹ = a
· 使用定理 `sup_of_le_right`：∀ {α : Type u} [inst : SemilatticeSup α] {a b : α}, a ≤
 b → a ⊔ b = b
· 使用定理 `Classical.em`：∀ (p : Prop), p ∨ ¬p
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `sup_eq_left`：sup_eq_left : a ⊔ b = a ↔ b <= a
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
（共 47 条，此处仅展示前 30 条）
-/
lemma Subgroup.isLeast_of_closure_iff_eq_mabs {a b : G} :
    IsLeast {y : G | y ∈ closure ({a} : Set G) ∧ 1 < y} b ↔ b = |a|ₘ ∧ 1 < b := by
  constructor <;> intro h
  · have := Subgroup.cyclic_of_min h
    have ha : a ∈ closure ({b} : Set G) := by
      simp [← this]
    rw [mem_closure_singleton] at ha
    obtain ⟨n, rfl⟩ := ha
    have := h.left
    simp only [mem_closure_singleton, mem_ofPred_eq] at this
    obtain ⟨m, hm⟩ := this.left
    have key : m * n = 1 := by
      rw [← zpow_right_inj this.right, zpow_mul', hm, zpow_one]
    rw [Int.mul_eq_one_iff_eq_one_or_neg_one] at key
    rw [eq_comm]
    rcases key with ⟨rfl, rfl⟩ | ⟨rfl, rfl⟩ <;>
    simp [this.right.le, this.right, mabs]
  · wlog ha : 1 ≤ a generalizing a
    · convert! @this (a⁻¹) ?_ (by simpa using le_of_not_ge ha) using 4
      · simp
      · rwa [mabs_inv]
    rw [mabs, sup_eq_left.mpr ((inv_le_one'.mpr ha).trans ha)] at h
    rcases h with ⟨rfl, h⟩
    refine ⟨?_, ?_⟩
    · simp [h]
    · intro x
      simp only [mem_closure_singleton, mem_ofPred_eq, and_imp, forall_exists_index]
      rintro k rfl hk
      rw [← zpow_one b, ← zpow_mul, one_mul, zpow_le_zpow_iff_right h, ← zero_add 1,
          ← Int.lt_iff_add_one_le]
      contrapose! hk
      rw [← Left.one_le_inv_iff, ← zpow_neg]
      exact one_le_zpow ha (by simp [hk])

/-- If an element of a linearly ordered archimedean additive group is the least positive element,
then the whole group is isomorphic (and order-isomorphic) to the integers. -/
/-
**LinearOrderedAddCommGroup.int_orderAddMonoidIso_of_isLeast_pos** 是 Mathlib 中的一
个定义，位于命名空间 ``。
形式化陈述：LinearOrderedAddCommGroup.int_orderAddMonoidIso_of_isLeast_pos {G : Type*}
 [AddCommGroup G] [LinearOrder G] [IsOrderedAddMonoid G] [Archimedean G] {x : G}
 (h : IsLeast {y : G | 0 < y} x) : G ≃+o Int
参数：h : IsLeast {y : G | 0 < y} x。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If an element of a linearly ordered archimedean additive group is the least posi
tive element,
then the whole group is isomorphic (and order-isomorphic) to the integers.
-/
noncomputable def LinearOrderedAddCommGroup.int_orderAddMonoidIso_of_isLeast_pos {G : Type*}
    [AddCommGroup G] [LinearOrder G] [IsOrderedAddMonoid G] [Archimedean G] {x : G}
    (h : IsLeast {y : G | 0 < y} x) : G ≃+o ℤ := by
  have : IsLeast {y : G | y ∈ (⊤ : AddSubgroup G) ∧ 0 < y} x := by simpa using h
  replace this := AddSubgroup.cyclic_of_min this
  let e : G ≃+o (⊤ : AddSubgroup G) := ⟨AddSubsemigroup.topEquiv.symm,
    (AddEquiv.strictMono_symm AddSubsemigroup.strictMono_topEquiv).le_iff_le⟩
  let e' : (⊤ : AddSubgroup G) ≃+o AddSubgroup.closure {x} :=
    ⟨AddEquiv.subsemigroupCongr (by simp [this]),
     (AddEquiv.strictMono_subsemigroupCongr _).le_iff_le⟩
  let g : (⊤ : AddSubgroup ℤ) ≃+o ℤ := ⟨AddSubsemigroup.topEquiv,
    (AddSubsemigroup.strictMono_topEquiv).le_iff_le⟩
  let g' : AddSubgroup.closure ({1} : Set ℤ) ≃+o (⊤ : AddSubgroup ℤ) :=
    ⟨(.subsemigroupCongr (by simp)),
     (AddEquiv.strictMono_subsemigroupCongr _).le_iff_le⟩
  let f := closure_equiv_closure x (1 : ℤ) (by simp [h.left.ne'])
  exact ((((e.trans e').trans f).trans g').trans g : G ≃+o ℤ)

set_option backward.isDefEq.respectTransparency false in
/-- If an element of a linearly ordered mul-archimedean group is the least element greater than 1,
then the whole group is isomorphic (and order-isomorphic) to the multiplicative integers. -/
/-
**LinearOrderedCommGroup.multiplicative_int_orderMonoidIso_of_isLeast_one_lt** 是
 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：LinearOrderedCommGroup.multiplicative_int_orderMonoidIso_of_isLeast_one_lt
 {x : G} (h : IsLeast {y : G | 1 < y} x) : G ≃*o Multiplicative Int
参数：h : IsLeast {y : G | 1 < y} x。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If an element of a linearly ordered mul-archimedean group is the least element g
reater than 1,
then the whole group is isomorphic (and order-isomorphic) to the multiplicative 
integers.
-/
noncomputable def LinearOrderedCommGroup.multiplicative_int_orderMonoidIso_of_isLeast_one_lt
    {x : G} (h : IsLeast {y : G | 1 < y} x) : G ≃*o Multiplicative ℤ := by
  have : IsLeast {y : Additive G | 0 < y} (.ofMul x) := h
  let f' := LinearOrderedAddCommGroup.int_orderAddMonoidIso_of_isLeast_pos (G := Additive G) this
  exact f'.toMultiplicativeRight

/-- Any locally finite linear additive group is archimedean. -/
/-
**Archimedean.of_locallyFiniteOrder** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：Archimedean.of_locallyFiniteOrder {G : Type*} [AddCommGroup G] [LinearOrde
r G] [IsOrderedAddMonoid G] [LocallyFiniteOrder G] : Archimedean G
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Archimedean.comap`：∀ {G : Type u_1} {M : Type u_2} [inst : AddCommMonoid
 G] [inst_1 : LinearOrder G] [inst_2 : AddCommMonoid M]   [inst_3 : PartialOrder
 M] [Ar…
· 使用定理 `instArchimedeanInt`：Archimedean ℤ
· 使用引理 `LocallyFiniteOrder.orderAddMonoidHom_strictMono`：LocallyFiniteOrder.orde
rAddMonoidHom_strictMono : StrictMono (orderAddMonoidHom G)

--- 原说明 ---
Any locally finite linear additive group is archimedean.
-/
lemma Archimedean.of_locallyFiniteOrder {G : Type*} [AddCommGroup G] [LinearOrder G]
    [IsOrderedAddMonoid G] [LocallyFiniteOrder G] :
    Archimedean G :=
  .comap (LocallyFiniteOrder.addMonoidHom G) LocallyFiniteOrder.orderAddMonoidHom_strictMono

/-- Any locally finite linear group is mul-archimedean. -/
@[to_additive existing]
/-
**MulArchimedean.of_locallyFiniteOrder** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：MulArchimedean.of_locallyFiniteOrder {G : Type*} [CommGroup G] [LinearOrde
r G] [IsOrderedMonoid G] [LocallyFiniteOrder G] : MulArchimedean G
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MulArchimedean.comap`：MulArchimedean.comap [CommMonoid G] [LinearOrder G
] [CommMonoid M] [PartialOrder M] [MulArchimedean M] (f : G ->* M) (hf : StrictM
ono f) : M…
· 使用定理 `instArchimedeanInt`：Archimedean ℤ
· 使用引理 `LocallyFiniteOrder.orderMonoidHom_strictMono`：LocallyFiniteOrder.orderMo
noidHom_strictMono {G : Type*} [CommGroup G] [LinearOrder G] [IsOrderedMonoid G]
 [LocallyFiniteOrder G] : StrictMo…

--- 原说明 ---
Any locally finite linear group is mul-archimedean.
-/
lemma MulArchimedean.of_locallyFiniteOrder {G : Type*} [CommGroup G] [LinearOrder G]
    [IsOrderedMonoid G] [LocallyFiniteOrder G] :
    MulArchimedean G :=
  .comap (LocallyFiniteOrder.orderMonoidHom G).toMonoidHom
    LocallyFiniteOrder.orderMonoidHom_strictMono

/-- Any linearly ordered archimedean additive group is either isomorphic (and order-isomorphic)
to the integers, or is densely ordered. -/
/-
**LinearOrderedAddCommGroup.discrete_or_denselyOrdered** 是 Mathlib 中的一个引理，位于命名空间
 ``。
形式化陈述：LinearOrderedAddCommGroup.discrete_or_denselyOrdered (G : Type*) [AddCommG
roup G] [LinearOrder G] [IsOrderedAddMonoid G] [Archimedean G] : Nonempty (G ≃+o
 Int) ∨ DenselyOrdered G
参数：G : Type*。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `Mathlib.Tactic.Contrapose.contrapose₂`：contrapose₂ {p q : Prop} : (¬ q -
> p) -> (¬ p -> q)
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `Mathlib.Tactic.Push.not_and_eq`：not_and_eq : (¬ (p ∧ q)) = (p -> ¬ q)
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `IsRightCancelAdd.addRightStrictMono_of_addRightMono`：∀ (N : Type u_2) [i
nst : Add N] [IsRightCancelAdd N] [inst_2 : PartialOrder N] [AddRightMono N], Ad
dRightStrictMono N
· 使用定理 `instIsRightCancelAddOfAddRightReflectLE`：∀ {α : Type u_1} [inst : Add α]
 [inst_1 : PartialOrder α] [AddRightReflectLE α], IsRightCancelAdd α
· 使用定理 `addRightReflectLE_of_addLeftReflectLE`：∀ (N : Type u_2) [inst : AddCommS
emigroup N] [inst_1 : LE N] [AddLeftReflectLE N], AddRightReflectLE N
· 使用定理 `IsLeftCancelAdd.addLeftReflectLE_of_addLeftReflectLT`：∀ (N : Type u_2) [
inst : Add N] [IsLeftCancelAdd N] [inst_2 : PartialOrder N] [AddLeftReflectLT N]
, AddLeftReflectLE N
· 使用定理 `AddLeftCancelSemigroup.toIsLeftCancelAdd`：∀ {G : Type u} [self : AddLeft
CancelSemigroup G], IsLeftCancelAdd G
· 使用定理 `IsOrderedAddMonoid.toAddLeftMono`：∀ {α : Type u_1} [inst : AddCommMonoid
 α] [inst_1 : Preorder α] [IsOrderedAddMonoid α], AddLeftMono α
· 使用定理 `covariant_swap_add_of_covariant_add`：∀ (N : Type u_2) (r : N → N → Prop)
 [inst : AddCommSemigroup N] [CovariantClass N N (fun x1 x2 => x1 + x2) r],   Co
variantClass N N (Functio…
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `IsLeftCancelAdd.addLeftStrictMono_of_addLeftMono`：∀ (N : Type u_2) [inst
 : Add N] [IsLeftCancelAdd N] [inst_2 : PartialOrder N] [AddLeftMono N], AddLeft
StrictMono N
· 使用定理 `instIsLeftCancelAddOfAddLeftReflectLE`：∀ {α : Type u_1} [inst : Add α] [
inst_1 : PartialOrder α] [AddLeftReflectLE α], IsLeftCancelAdd α
· 使用定理 `AddGroup.addLeftReflectLE_of_addLeftMono`：∀ {N : Type u_2} [inst : AddGr
oup N] [inst_1 : LE N] [AddLeftMono N], AddLeftReflectLE N
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b

--- 原说明 ---
Any linearly ordered archimedean additive group is either isomorphic (and order-
isomorphic)
to the integers, or is densely ordered.
-/
lemma LinearOrderedAddCommGroup.discrete_or_denselyOrdered (G : Type*)
    [AddCommGroup G] [LinearOrder G] [IsOrderedAddMonoid G] [Archimedean G] :
    Nonempty (G ≃+o ℤ) ∨ DenselyOrdered G := by
  by_cases! H : ∃ x, IsLeast {y : G | 0 < y} x
  · obtain ⟨x, hx⟩ := H
    exact Or.inl ⟨(int_orderAddMonoidIso_of_isLeast_pos hx)⟩
  · refine Or.inr ⟨?_⟩
    intro x y hxy
    specialize H (y - x)
    obtain ⟨z, hz⟩ : ∃ z : G, 0 < z ∧ z < y - x := by
      contrapose! H
      refine ⟨by simp [hxy], fun _ ↦ H _⟩
    refine ⟨x + z, ?_, ?_⟩
    · simp [hz.left]
    · simpa [lt_sub_iff_add_lt'] using hz.right

/-- Any linearly ordered archimedean additive group is either isomorphic (and order-isomorphic)
to the integers, or is densely ordered, exclusively.

(See also `LinearOrderedAddCommGroup.isAddCyclic_iff_not_denselyOrdered`.) -/
/-
**LinearOrderedAddCommGroup.discrete_iff_not_denselyOrdered** 是 Mathlib 中的一个引理，位
于命名空间 ``。
形式化陈述：LinearOrderedAddCommGroup.discrete_iff_not_denselyOrdered (G : Type*) [Add
CommGroup G] [LinearOrder G] [IsOrderedAddMonoid G] [Archimedean G] : Nonempty (
G ≃+o Int) ↔ ¬ DenselyOrdered G
参数：G : Type*。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `exists_between`：exists_between [LT α] [DenselyOrdered α] {a₁ a₂ : α} : a
₁ < a₂ -> exists a, a₁ < a ∧ a < a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `denselyOrdered_iff_of_orderIsoClass`：denselyOrdered_iff_of_orderIsoClass
 {X Y F : Type*} [Preorder X] [Preorder Y] [EquivLike F X Y] [OrderIsoClass F X 
Y] (f : F) : DenselyOrder…
· 使用定理 `OrderAddMonoidIso.instOrderIsoClass`：∀ {α : Type u_2} {β : Type u_3} [in
st : Preorder α] [inst_1 : Preorder β] [inst_2 : Add α] [inst_3 : Add β],   Orde
rIsoClass (α ≃+o β) α β
· 使用定理 `one_pos`：∀ {α : Type u_1} [inst : Zero α] [inst_1 : One α] [inst_2 : Par
tialOrder α] [ZeroLEOneClass α] [NeZero 1], 0 < 1
· 使用定理 `Int.instNeZeroOfNatOfNat`：∀ {n : ℕ} [NeZero n], NeZero (OfNat.ofNat n)
· 使用定理 `Nat.instNeZeroSucc`：∀ {n : ℕ}, NeZero (n + 1)
· 使用引理 `LinearOrderedAddCommGroup.discrete_or_denselyOrdered`：LinearOrderedAddCo
mmGroup.discrete_or_denselyOrdered (G : Type*) [AddCommGroup G] [LinearOrder G] 
[IsOrderedAddMonoid G] [Archimedean G] : N…
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `eq_false`：∀ {p : Prop}, ¬p → p = False
· 使用定理 `not_false_eq_true`：(¬False) = True
· 使用定理 `iff_true`：∀ (p : Prop), (p ↔ True) = p
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `not_true_eq_false`：(¬True) = False
· 使用定理 `iff_false`：∀ (p : Prop), (p ↔ False) = ¬p

--- 原说明 ---
Any linearly ordered archimedean additive group is either isomorphic (and order-
isomorphic)
to the integers, or is densely ordered, exclusively.

(See also `LinearOrderedAddCommGroup.isAddCyclic_iff_not_denselyOrdered`.)
-/
lemma LinearOrderedAddCommGroup.discrete_iff_not_denselyOrdered (G : Type*)
    [AddCommGroup G] [LinearOrder G] [IsOrderedAddMonoid G] [Archimedean G] :
    Nonempty (G ≃+o ℤ) ↔ ¬ DenselyOrdered G := by
  suffices ∀ (_ : G ≃+o ℤ), ¬ DenselyOrdered G by
    rcases LinearOrderedAddCommGroup.discrete_or_denselyOrdered G with ⟨⟨h⟩⟩ | h
    · simpa [this h] using ⟨h⟩
    · simp only [h, not_true_eq_false, iff_false, not_nonempty_iff]
      exact ⟨fun H ↦ (this H) h⟩
  intro e H
  rw [denselyOrdered_iff_of_orderIsoClass e] at H
  obtain ⟨_, _⟩ := exists_between (one_pos (α := ℤ))
  lia

/-- Any non-trivial linearly ordered archimedean additive group is either cyclic, or densely
ordered, exclusively. -/
/-
**LinearOrderedAddCommGroup.isAddCyclic_iff_not_denselyOrdered** 是 Mathlib 中的一个引
理，位于命名空间 ``。
形式化陈述：LinearOrderedAddCommGroup.isAddCyclic_iff_not_denselyOrdered {A : Type*} [
AddCommGroup A] [LinearOrder A] [IsOrderedAddMonoid A] [Archimedean A] [Nontrivi
al A] : IsAddCyclic A ↔ ¬ DenselyOrdered A
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `LinearOrderedAddCommGroup.discrete_iff_not_denselyOrdered`：LinearOrdered
AddCommGroup.discrete_iff_not_denselyOrdered (G : Type*) [AddCommGroup G] [Linea
rOrder G] [IsOrderedAddMonoid G] [Archimedean G…
· 使用引理 `LinearOrderedAddCommGroup.isAddCyclic_iff_nonempty_equiv_int`：LinearOrde
redAddCommGroup.isAddCyclic_iff_nonempty_equiv_int {A : Type*} [AddCommGroup A] 
[LinearOrder A] [IsOrderedAddMonoid A] [Nontrivial…
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a

--- 原说明 ---
Any non-trivial linearly ordered archimedean additive group is either cyclic, or
 densely
ordered, exclusively.
-/
lemma LinearOrderedAddCommGroup.isAddCyclic_iff_not_denselyOrdered {A : Type*}
    [AddCommGroup A] [LinearOrder A] [IsOrderedAddMonoid A] [Archimedean A] [Nontrivial A] :
    IsAddCyclic A ↔ ¬ DenselyOrdered A := by
  rw [← discrete_iff_not_denselyOrdered, isAddCyclic_iff_nonempty_equiv_int]

set_option backward.isDefEq.respectTransparency false in
variable (G) in
/-- Any linearly ordered mul-archimedean group is either isomorphic (and order-isomorphic)
to the multiplicative integers, or is densely ordered. -/
/-
**LinearOrderedCommGroup.discrete_or_denselyOrdered** 是 Mathlib 中的一个引理，位于命名空间 ``
。
形式化陈述：LinearOrderedCommGroup.discrete_or_denselyOrdered : Nonempty (G ≃*o Multip
licative Int) ∨ DenselyOrdered G
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Equiv.nonempty_congr`：nonempty_congr (e : α ≃ β) : Nonempty α ↔ Nonempty
 β
· 使用引理 `LinearOrderedAddCommGroup.discrete_or_denselyOrdered`：LinearOrderedAddCo
mmGroup.discrete_or_denselyOrdered (G : Type*) [AddCommGroup G] [LinearOrder G] 
[IsOrderedAddMonoid G] [Archimedean G] : N…

--- 原说明 ---
Any linearly ordered mul-archimedean group is either isomorphic (and order-isomo
rphic)
to the multiplicative integers, or is densely ordered.
-/
lemma LinearOrderedCommGroup.discrete_or_denselyOrdered :
    Nonempty (G ≃*o Multiplicative ℤ) ∨ DenselyOrdered G := by
  rw [← OrderAddMonoidIso.toMultiplicativeRight.nonempty_congr]
  exact LinearOrderedAddCommGroup.discrete_or_denselyOrdered (Additive G)

set_option backward.isDefEq.respectTransparency false in
variable (G) in
/-- Any linearly ordered mul-archimedean group is either isomorphic (and order-isomorphic)
to the multiplicative integers, or is densely ordered, exclusively.

(See also `LinearOrderedCommGroup.isCyclic_iff_not_denselyOrdered`.) -/
/-
**LinearOrderedCommGroup.discrete_iff_not_denselyOrdered** 是 Mathlib 中的一个引理，位于命名
空间 ``。
形式化陈述：LinearOrderedCommGroup.discrete_iff_not_denselyOrdered : Nonempty (G ≃*o M
ultiplicative Int) ↔ ¬ DenselyOrdered G
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Equiv.nonempty_congr`：nonempty_congr (e : α ≃ β) : Nonempty α ↔ Nonempty
 β
· 使用引理 `LinearOrderedAddCommGroup.discrete_iff_not_denselyOrdered`：LinearOrdered
AddCommGroup.discrete_iff_not_denselyOrdered (G : Type*) [AddCommGroup G] [Linea
rOrder G] [IsOrderedAddMonoid G] [Archimedean G…
· 使用引理 `denselyOrdered_iff_of_orderIsoClass`：denselyOrdered_iff_of_orderIsoClass
 {X Y F : Type*} [Preorder X] [Preorder Y] [EquivLike F X Y] [OrderIsoClass F X 
Y] (f : F) : DenselyOrder…
· 使用定理 `OrderIso.instOrderIsoClass`：∀ {α : Type u_2} {β : Type u_3} [inst : LE α
] [inst_1 : LE β], OrderIsoClass (α ≃o β) α β
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a

--- 原说明 ---
Any linearly ordered mul-archimedean group is either isomorphic (and order-isomo
rphic)
to the multiplicative integers, or is densely ordered, exclusively.

(See also `LinearOrderedCommGroup.isCyclic_iff_not_denselyOrdered`.)
-/
lemma LinearOrderedCommGroup.discrete_iff_not_denselyOrdered :
    Nonempty (G ≃*o Multiplicative ℤ) ↔ ¬ DenselyOrdered G := by
  let e : G ≃o Additive G := .refl G
  rw [← OrderAddMonoidIso.toMultiplicativeRight.nonempty_congr,
    LinearOrderedAddCommGroup.discrete_iff_not_denselyOrdered,
    denselyOrdered_iff_of_orderIsoClass e]

set_option backward.isDefEq.respectTransparency false in
/-- Any non-trivial linearly ordered mul-archimedean group is either cyclic, or densely ordered,
exclusively. -/
@[to_additive existing]
/-
**LinearOrderedCommGroup.isCyclic_iff_not_denselyOrdered** 是 Mathlib 中的一个引理，位于命名
空间 ``。
形式化陈述：LinearOrderedCommGroup.isCyclic_iff_not_denselyOrdered [Nontrivial G] : Is
Cyclic G ↔ ¬ DenselyOrdered G
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `isAddCyclic_additive_iff`：isAddCyclic_additive_iff [DivInvMonoid α] : Is
AddCyclic (Additive α) ↔ IsCyclic α
· 使用引理 `LinearOrderedAddCommGroup.isAddCyclic_iff_not_denselyOrdered`：LinearOrde
redAddCommGroup.isAddCyclic_iff_not_denselyOrdered {A : Type*} [AddCommGroup A] 
[LinearOrder A] [IsOrderedAddMonoid A] [Archimedea…
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a

--- 原说明 ---
Any non-trivial linearly ordered mul-archimedean group is either cyclic, or dens
ely ordered,
exclusively.
-/
lemma LinearOrderedCommGroup.isCyclic_iff_not_denselyOrdered [Nontrivial G] :
    IsCyclic G ↔ ¬ DenselyOrdered G := by
  rw [← isAddCyclic_additive_iff, LinearOrderedAddCommGroup.isAddCyclic_iff_not_denselyOrdered]
  rfl

/-- Any nontrivial (has other than 0 and 1) linearly ordered mul-archimedean group with zero is
either isomorphic (and order-isomorphic) to `ℤᵐ⁰`, or is densely ordered. -/
/-
**LinearOrderedCommGroupWithZero.discrete_or_denselyOrdered** 是 Mathlib 中的一个引理，位
于命名空间 ``。
形式化陈述：LinearOrderedCommGroupWithZero.discrete_or_denselyOrdered (G : Type*) [Lin
earOrderedCommGroupWithZero G] [Nontrivial Gˣ] [MulArchimedean G] : Nonempty (G 
≃*o Intᵐ⁰) ∨ DenselyOrdered G
参数：G : Type*。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `denselyOrdered_units_iff`：denselyOrdered_units_iff [Nontrivial αˣ] : Den
selyOrdered αˣ ↔ DenselyOrdered α
· 使用定理 `Or.imp_left`：∀ {a b c : Prop}, (a → b) → a ∨ c → b ∨ c
· 使用引理 `LinearOrderedCommGroup.discrete_or_denselyOrdered`：LinearOrderedCommGrou
p.discrete_or_denselyOrdered : Nonempty (G ≃*o Multiplicative Int) ∨ DenselyOrde
red G
· 使用定理 `LinearOrderedCommMonoidWithZero.toIsOrderedMonoid`：∀ {α : Type u_1} [ins
t : LinearOrderedCommMonoidWithZero α], IsOrderedMonoid α

--- 原说明 ---
Any nontrivial (has other than 0 and 1) linearly ordered mul-archimedean group w
ith zero is
either isomorphic (and order-isomorphic) to `ℤᵐ⁰`, or is densely ordered.
-/
lemma LinearOrderedCommGroupWithZero.discrete_or_denselyOrdered (G : Type*)
    [LinearOrderedCommGroupWithZero G] [Nontrivial Gˣ] [MulArchimedean G] :
    Nonempty (G ≃*o ℤᵐ⁰) ∨ DenselyOrdered G := by
  rw [← denselyOrdered_units_iff]
  refine (LinearOrderedCommGroup.discrete_or_denselyOrdered Gˣ).imp_left ?_
  intro ⟨f⟩
  exact ⟨OrderMonoidIso.withZeroUnits.symm.trans f.withZero⟩

set_option backward.isDefEq.respectTransparency false in
open WithZero in
/-- Any nontrivial (has other than 0 and 1) linearly ordered mul-archimedean group with zero is
either isomorphic (and order-isomorphic) to `ℤᵐ⁰`, or is densely ordered, exclusively -/
/-
**LinearOrderedCommGroupWithZero.discrete_iff_not_denselyOrdered** 是 Mathlib 中的一
个引理，位于命名空间 ``。
形式化陈述：LinearOrderedCommGroupWithZero.discrete_iff_not_denselyOrdered (G : Type*)
 [LinearOrderedCommGroupWithZero G] [Nontrivial Gˣ] [MulArchimedean G] : Nonempt
y (G ≃*o Intᵐ⁰) ↔ ¬ DenselyOrdered G
参数：G : Type*。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `denselyOrdered_units_iff`：denselyOrdered_units_iff [Nontrivial αˣ] : Den
selyOrdered αˣ ↔ DenselyOrdered α
· 使用引理 `LinearOrderedCommGroup.discrete_iff_not_denselyOrdered`：LinearOrderedCom
mGroup.discrete_iff_not_denselyOrdered : Nonempty (G ≃*o Multiplicative Int) ↔ ¬
 DenselyOrdered G
· 使用定理 `LinearOrderedCommMonoidWithZero.toIsOrderedMonoid`：∀ {α : Type u_1} [ins
t : LinearOrderedCommMonoidWithZero α], IsOrderedMonoid α
· 使用定理 `Nonempty.congr`：∀ {α : Sort u_3} {β : Sort u_4} (f : α → β) (g : β → α),
 Nonempty α ↔ Nonempty β
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用定理 `OrderMonoidIso.instMulEquivClass`：∀ {α : Type u_2} {β : Type u_3} [inst 
: Preorder α] [inst_1 : Preorder β] [inst_2 : Mul α] [inst_3 : Mul β],   MulEqui
vClass (α ≃*o β) α β
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `of_eq_false`：∀ {p : Prop}, p = False → ¬p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `GroupWithZero.toNontrivial`：∀ {G₀ : Type u} [self : GroupWithZero G₀], N
ontrivial G₀
· 使用定理 `MulEquivClass.toMonoidWithZeroHomClass`：∀ {F : Type u_1} {α : Type u_2} 
{β : Type u_3} [inst : EquivLike F α β] [inst_1 : MulZeroOneClass α]   [inst_2 :
 MulZeroOneClass β] [MulEqui…
· 使用定理 `MulEquiv.instMulEquivClass`：∀ {M : Type u_4} {N : Type u_5} [inst : Mul 
M] [inst_1 : Mul N], MulEquivClass (M ≃* N) M N
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `dite_cond_eq_false`：∀ {α : Sort u} {c : Prop} {x : Decidable c} {t : c →
 α} {e : ¬c → α} (h : c = False), dite c t e = e ⋯
· 使用定理 `WithZero.unzero.congr_simp`：∀ {α : Type u} {x x_1 : WithZero α} (e_x : x
 = x_1) (a : x ≠ 0), WithZero.unzero a = WithZero.unzero ⋯
· 使用定理 `Equiv.mk.congr_simp`：∀ {α : Sort u_1} {β : Sort u_2} (toFun toFun_1 : α 
→ β) (e_toFun : toFun = toFun_1) (invFun invFun_1 : β → α)   (e_invFun : invFun 
= invFun_…
· 使用定理 `MulEquiv.mk.congr_simp`：∀ {M : Type u_9} {N : Type u_10} [inst : Mul M] 
[inst_1 : Mul N] (toEquiv toEquiv_1 : M ≃ N)   (e_toEquiv : toEquiv = toEquiv_1)
 (map_mul' :…
· 使用定理 `Units.val_le_val`：val_le_val [Monoid α] [Preorder α] {a b : αˣ} : (a : α
) <= b ↔ a <= b
· 使用定理 `OrderIsoClass.map_le_map_iff`：∀ {F : Type u_6} {α : outParam (Type u_7)}
 {β : outParam (Type u_8)} {inst : LE α} {inst_1 : LE β}   {inst_2 : EquivLike F
 α β} [self : Orde…
· 使用定理 `OrderMonoidIso.instOrderIsoClass`：∀ {α : Type u_2} {β : Type u_3} [inst 
: Preorder α] [inst_1 : Preorder β] [inst_2 : Mul α] [inst_3 : Mul β],   OrderIs
oClass (α ≃*o β) α β
· 使用定理 `WithZero.coe_le_coe`：∀ {α : Type u_1} [inst : LE α] {a b : α}, ↑a ≤ ↑b ↔
 a ≤ b
· 使用定理 `WithZero.coe_unzero`：∀ {α : Type u} {x : WithZero α} (hx : x ≠ 0), ↑(Wit
hZero.unzero hx) = x
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
· 使用定理 `dif_pos`：∀ {c : Prop} {h : Decidable c} (hc : c) {α : Sort u} {t : c → α
} {e : ¬c → α}, dite c t e = t hc
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `map_zero`：∀ {M : Type u_4} {N : Type u_5} {F : Type u_9} [inst : Zero M]
 [inst_1 : Zero N] [inst_2 : FunLike F M N]   [ZeroHomClass F M N] (f : F), f …
· 使用定理 `MonoidWithZeroHomClass.toZeroHomClass`：∀ {F : Type u_7} {α : outParam (T
ype u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : MulZe
roOneClass β} {inst_2 : Fun…
（共 35 条，此处仅展示前 30 条）

--- 原说明 ---
Any nontrivial (has other than 0 and 1) linearly ordered mul-archimedean group w
ith zero is
either isomorphic (and order-isomorphic) to `ℤᵐ⁰`, or is densely ordered, exclus
ively
-/
lemma LinearOrderedCommGroupWithZero.discrete_iff_not_denselyOrdered (G : Type*)
    [LinearOrderedCommGroupWithZero G] [Nontrivial Gˣ] [MulArchimedean G] :
    Nonempty (G ≃*o ℤᵐ⁰) ↔ ¬ DenselyOrdered G := by
  rw [← denselyOrdered_units_iff,
      ← LinearOrderedCommGroup.discrete_iff_not_denselyOrdered]
  refine Nonempty.congr ?_ ?_ <;> intro f
  · refine ⟨MulEquiv.withZero.symm (withZeroUnitsEquiv.trans f), ?_⟩
    intros
    simp only [MulEquiv.withZero, withZeroUnitsEquiv, MulEquiv.trans_apply,
      MulEquiv.coe_mk, Equiv.coe_fn_mk, recZeroCoe_coe, OrderMonoidIso.coe_mulEquiv,
      MulEquiv.symm_trans_apply, MulEquiv.symm_mk, Equiv.coe_fn_symm_mk, map_eq_zero, coe_ne_zero,
      ↓reduceDIte, unzero_coe, MulEquiv.toEquiv_eq_coe, Equiv.toFun_as_coe, EquivLike.coe_coe]
    rw [← Units.val_le_val, ← map_le_map_iff f, ← coe_le_coe, coe_unzero, coe_unzero]
  · refine ⟨withZeroUnitsEquiv.symm.trans (MulEquiv.withZero f), ?_⟩
    intros
    simp only [withZeroUnitsEquiv, MulEquiv.symm_mk, MulEquiv.withZero,
      MulEquiv.toEquiv_eq_coe, Equiv.toFun_as_coe, EquivLike.coe_coe,
      MulEquiv.trans_apply, MulEquiv.coe_mk, Equiv.coe_fn_symm_mk, Equiv.coe_fn_mk]
    split_ifs <;>
    simp_all [← Units.val_le_val]

section WellFounded

set_option backward.isDefEq.respectTransparency false in
/-
**LinearOrderedAddCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete*
* 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：LinearOrderedAddCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discr
ete {G : Type*} [AddCommGroup G] [LinearOrder G] [IsOrderedAddMonoid G] [Nontriv
ial G] {g : G} : Set.WellFoundedOn {x : G | g <= x} (· < ·) ↔ Nonempty (G ≃+o In
t)
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `LinearOrderedAddCommGroup.discrete_iff_not_denselyOrdered`：LinearOrdered
AddCommGroup.discrete_iff_not_denselyOrdered (G : Type*) [AddCommGroup G] [Linea
rOrder G] [IsOrderedAddMonoid G] [Archimedean G…
· 使用定理 `exists_ne`：exists_ne [Nontrivial α] (x : α) : exists y, y != x
· 使用定理 `Classical.em`：∀ (p : Prop), p ∨ ¬p
· 使用引理 `le_rfl`：le_rfl : a <= a
· 使用定理 `WellFounded.wellFounded_iff_has_min`：wellFounded_iff_has_min {r : α -> α
 -> Prop} : WellFounded r ↔ forall s : Set α, s.Nonempty -> exists m in s, foral
l x in s, ¬r x m
· 使用定理 `LT.lt.le`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a < b → a ≤ b
· 使用定理 `exists_between`：exists_between [LT α] [DenselyOrdered α] {a₁ a₂ : α} : a
₁ < a₂ -> exists a, a₁ < a ∧ a < a₂
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `eq_false`：∀ {p : Prop}, ¬p → p = False
· 使用定理 `not_false_eq_true`：(¬False) = True
· 使用定理 `IsLeftCancelAdd.addLeftStrictMono_of_addLeftMono`：∀ (N : Type u_2) [inst
 : Add N] [IsLeftCancelAdd N] [inst_2 : PartialOrder N] [AddLeftMono N], AddLeft
StrictMono N
· 使用定理 `instIsLeftCancelAddOfAddLeftReflectLE`：∀ {α : Type u_1} [inst : Add α] [
inst_1 : PartialOrder α] [AddLeftReflectLE α], IsLeftCancelAdd α
· 使用定理 `AddGroup.addLeftReflectLE_of_addLeftMono`：∀ {N : Type u_2} [inst : AddGr
oup N] [inst_1 : LE N] [AddLeftMono N], AddLeftReflectLE N
· 使用定理 `IsOrderedAddMonoid.toAddLeftMono`：∀ {α : Type u_1} [inst : AddCommMonoid
 α] [inst_1 : Preorder α] [IsOrderedAddMonoid α], AddLeftMono α
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用引理 `lt_of_le_of_ne`：lt_of_le_of_ne : a <= b -> a != b -> a < b
· 使用定理 `Mathlib.Tactic.Push.not_forall_eq`：not_forall_eq : (¬ forall x, s x) = (
exists x, ¬ s x)
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `sub_nonneg`：∀ {α : Type u} [inst : AddGroup α] [inst_1 : LE α] [AddRight
Mono α] {a b : α}, 0 ≤ a - b ↔ b ≤ a
· 使用定理 `covariant_swap_add_of_covariant_add`：∀ (N : Type u_2) (r : N → N → Prop)
 [inst : AddCommSemigroup N] [CovariantClass N N (fun x1 x2 => x1 + x2) r],   Co
variantClass N N (Functio…
· 使用定理 `Set.range_nonempty`：range_nonempty [h : Nonempty ι] (f : ι -> α) : (rang
e f).Nonempty
· 使用定理 `instNonemptyOfInhabited`：∀ {α : Sort u} [Inhabited α], Nonempty α
· 使用定理 `add_smul`：add_smul : (r + s) • x = r • x + s • x
· 使用引理 `one_smul`：one_smul (b : α) : (1 : M) • b = b
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `Subtype.mk.congr_simp`：∀ {α : Sort u} {p : α → Prop} (val val_1 : α) (e_
val : val = val_1) (property : p val), ⟨val, property⟩ = ⟨val_1, ⋯⟩
（共 45 条，此处仅展示前 30 条）
-/
lemma LinearOrderedAddCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete
    {G : Type*} [AddCommGroup G] [LinearOrder G] [IsOrderedAddMonoid G] [Nontrivial G] {g : G} :
    Set.WellFoundedOn {x : G | g ≤ x} (· < ·) ↔ Nonempty (G ≃+o ℤ) := by
  suffices Set.WellFoundedOn {x : G | 0 ≤ x} (· < ·) ↔ Nonempty (G ≃+o ℤ) by
    rw [← this]
    refine ⟨fun h ↦ (h.mapsTo (· + g) ?_).mono' ?_, fun h ↦ (h.mapsTo (· - g) ?_).mono' ?_⟩ <;>
    · try intro
      simp [Function.onFun]
  constructor
  · intro h
    replace h : WellFounded (α := {x : G | 0 ≤ x}) (· < ·) := h
    rw [WellFounded.wellFounded_iff_has_min] at h
    by_cases! H : ∀ (x : G) {y}, 0 < y → ∃ n : ℕ, x ≤ n • y -- Archimedean
    · replace H : Archimedean G := ⟨H⟩
      rw [LinearOrderedAddCommGroup.discrete_iff_not_denselyOrdered]
      intro hd
      obtain ⟨y, hy⟩ := exists_ne (0 : G)
      wlog! hy' : 0 < y generalizing y
      · refine this (-y) ?_ ?_
        · simp [hy]
        · simp [lt_of_le_of_ne hy' hy]
      obtain ⟨⟨z, hz⟩, hz', hz''⟩ := h ({x | ⟨0, le_rfl⟩ < x}) ⟨⟨y, hy'.le⟩, hy'⟩
      obtain ⟨w, hw, hw'⟩ := exists_between hz'
      exact hz'' ⟨w, hw.le⟩ hw hw'
    · exfalso
      obtain ⟨x, y, hy0, H⟩ := H
      obtain ⟨_, ⟨n, rfl⟩, hz⟩ :=
        h (Set.range (fun n : ℕ ↦ ⟨x - n • y, sub_nonneg.mpr (H _).le⟩)) (range_nonempty _)
      refine hz ⟨x - (n + 1) • y, sub_nonneg.mpr (H _).le⟩ ⟨_, rfl⟩ ?_
      simp [add_smul, hy0]
  · rintro ⟨f⟩
    have : LocallyFiniteOrder G := LocallyFiniteOrder.ofOrderIsoClass f
    exact BddBelow.wellFoundedOn_lt ⟨0, by simp [mem_lowerBounds]⟩

@[deprecated (since := "2026-07-09")]
alias LinearOrderedAddCommGroup.wellFoundedOn_setOf_le_lt_iff_nonempty_discrete :=
  LinearOrderedAddCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete
/-
**LinearOrderedAddCommGroup.wellFoundedOn_setOfPred_ge_gt_iff_nonempty_discrete*
* 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：LinearOrderedAddCommGroup.wellFoundedOn_setOfPred_ge_gt_iff_nonempty_discr
ete {G : Type*} [AddCommGroup G] [LinearOrder G] [IsOrderedAddMonoid G] [Nontriv
ial G] (g : G) : Set.WellFoundedOn {x : G | x <= g} (· > ·) ↔ Nonempty (G ≃+o In
t)
参数：g : G。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `LinearOrderedAddCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_dis
crete`：LinearOrderedAddCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_disc
rete {G : Type*} [AddCommGroup G] [LinearOrder G] [IsOrderedAddMono…
· 使用定理 `Set.WellFoundedOn.mono'`：mono' (h : forall (a) (_ : a in s) (b) (_ : b i
n s), r' a b -> r a b) : s.WellFoundedOn r -> s.WellFoundedOn r'
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `IsLeftCancelAdd.addLeftStrictMono_of_addLeftMono`：∀ (N : Type u_2) [inst
 : Add N] [IsLeftCancelAdd N] [inst_2 : PartialOrder N] [AddLeftMono N], AddLeft
StrictMono N
· 使用定理 `instIsLeftCancelAddOfAddLeftReflectLE`：∀ {α : Type u_1} [inst : Add α] [
inst_1 : PartialOrder α] [AddLeftReflectLE α], IsLeftCancelAdd α
· 使用定理 `AddGroup.addLeftReflectLE_of_addLeftMono`：∀ {N : Type u_2} [inst : AddGr
oup N] [inst_1 : LE N] [AddLeftMono N], AddLeftReflectLE N
· 使用定理 `IsOrderedAddMonoid.toAddLeftMono`：∀ {α : Type u_1} [inst : AddCommMonoid
 α] [inst_1 : Preorder α] [IsOrderedAddMonoid α], AddLeftMono α
· 使用定理 `IsRightCancelAdd.addRightStrictMono_of_addRightMono`：∀ (N : Type u_2) [i
nst : Add N] [IsRightCancelAdd N] [inst_2 : PartialOrder N] [AddRightMono N], Ad
dRightStrictMono N
· 使用定理 `instIsRightCancelAddOfAddRightReflectLE`：∀ {α : Type u_1} [inst : Add α]
 [inst_1 : PartialOrder α] [AddRightReflectLE α], IsRightCancelAdd α
· 使用定理 `addRightReflectLE_of_addLeftReflectLE`：∀ (N : Type u_2) [inst : AddCommS
emigroup N] [inst_1 : LE N] [AddLeftReflectLE N], AddRightReflectLE N
· 使用定理 `IsLeftCancelAdd.addLeftReflectLE_of_addLeftReflectLT`：∀ (N : Type u_2) [
inst : Add N] [IsLeftCancelAdd N] [inst_2 : PartialOrder N] [AddLeftReflectLT N]
, AddLeftReflectLE N
· 使用定理 `AddLeftCancelSemigroup.toIsLeftCancelAdd`：∀ {G : Type u} [self : AddLeft
CancelSemigroup G], IsLeftCancelAdd G
· 使用定理 `covariant_swap_add_of_covariant_add`：∀ (N : Type u_2) (r : N → N → Prop)
 [inst : AddCommSemigroup N] [CovariantClass N N (fun x1 x2 => x1 + x2) r],   Co
variantClass N N (Functio…
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
· 使用定理 `Set.WellFoundedOn.mapsTo`：∀ {α : Type u_6} {β : Type u_7} {r : α → α → P
rop} (f : β → α) {s : Set α} {t : Set β},   Set.MapsTo f t s → s.WellFoundedOn r
 → t.WellFound…
-/
lemma LinearOrderedAddCommGroup.wellFoundedOn_setOfPred_ge_gt_iff_nonempty_discrete
    {G : Type*} [AddCommGroup G] [LinearOrder G] [IsOrderedAddMonoid G] [Nontrivial G] (g : G) :
    Set.WellFoundedOn {x : G | x ≤ g} (· > ·) ↔ Nonempty (G ≃+o ℤ) := by
  rw [← wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete (g := -g)]
  refine ⟨fun h ↦ (h.mapsTo (- ·) ?_).mono' ?_, fun h ↦ (h.mapsTo (- ·) ?_).mono' ?_⟩ <;>
  · intro
    simp [Function.onFun, neg_le]

@[deprecated (since := "2026-07-09")]
alias LinearOrderedAddCommGroup.wellFoundedOn_setOf_ge_gt_iff_nonempty_discrete :=
  LinearOrderedAddCommGroup.wellFoundedOn_setOfPred_ge_gt_iff_nonempty_discrete

set_option backward.isDefEq.respectTransparency false in
/-
**LinearOrderedCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete** 是
 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：LinearOrderedCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete
 {G : Type*} [CommGroup G] [LinearOrder G] [IsOrderedMonoid G] [Nontrivial G] {g
 : G} : Set.WellFoundedOn {x : G | g <= x} (· < ·) ↔ Nonempty (G ≃*o Multiplicat
ive Int)
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.WellFoundedOn.mono'`：mono' (h : forall (a) (_ : a in s) (b) (_ : b i
n s), r' a b -> r a b) : s.WellFoundedOn r -> s.WellFoundedOn r'
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
· 使用定理 `Set.WellFoundedOn.mapsTo`：∀ {α : Type u_6} {β : Type u_7} {r : α → α → P
rop} (f : β → α) {s : Set α} {t : Set β},   Set.MapsTo f t s → s.WellFoundedOn r
 → t.WellFound…
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `OrderIso.le_symm_apply`：le_symm_apply (e : α ≃o β) {x : α} {y : β} : x <
= e.symm y ↔ e x <= y
· 使用定理 `OrderIso.instOrderIsoClass`：∀ {α : Type u_2} {β : Type u_3} [inst : LE α
] [inst_1 : LE β], OrderIsoClass (α ≃o β) α β
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `LinearOrderedAddCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_dis
crete`：LinearOrderedAddCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_disc
rete {G : Type*} [AddCommGroup G] [LinearOrder G] [IsOrderedAddMono…
· 使用定理 `Equiv.nonempty_congr`：nonempty_congr (e : α ≃ β) : Nonempty α ↔ Nonempty
 β
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma LinearOrderedCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete
    {G : Type*} [CommGroup G] [LinearOrder G] [IsOrderedMonoid G] [Nontrivial G] {g : G} :
    Set.WellFoundedOn {x : G | g ≤ x} (· < ·) ↔ Nonempty (G ≃*o Multiplicative ℤ) := by
  let e : G ≃o Additive G := OrderIso.refl G
  suffices Set.WellFoundedOn {x : G | g ≤ x} (· < ·) ↔ Set.WellFoundedOn {x | e g ≤ x} (· < ·) by
    rw [this, LinearOrderedAddCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete,
      OrderAddMonoidIso.toMultiplicativeRight.nonempty_congr]
  refine ⟨fun h ↦ (h.mapsTo e.symm fun _ ↦ e.le_symm_apply.mpr).mono' ?_,
    fun h ↦ (h.mapsTo e fun _ ↦ ?_).mono' ?_⟩ <;>
  simp [Function.onFun]

@[deprecated (since := "2026-07-09")]
alias LinearOrderedCommGroup.wellFoundedOn_setOf_le_lt_iff_nonempty_discrete :=
  LinearOrderedCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete
/-
**LinearOrderedCommGroup.wellFoundedOn_setOfPred_ge_gt_iff_nonempty_discrete** 是
 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：LinearOrderedCommGroup.wellFoundedOn_setOfPred_ge_gt_iff_nonempty_discrete
 {G : Type*} [CommGroup G] [LinearOrder G] [IsOrderedMonoid G] [Nontrivial G] (g
 : G) : Set.WellFoundedOn {x : G | x <= g} (· > ·) ↔ Nonempty (G ≃*o Multiplicat
ive Int)
参数：g : G。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `LinearOrderedCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discre
te`：LinearOrderedCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete {
G : Type*} [CommGroup G] [LinearOrder G] [IsOrderedMonoid G] [No…
· 使用定理 `Set.WellFoundedOn.mono'`：mono' (h : forall (a) (_ : a in s) (b) (_ : b i
n s), r' a b -> r a b) : s.WellFoundedOn r -> s.WellFoundedOn r'
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `instIsLeftCancelMulOfMulLeftReflectLE`：∀ {α : Type u_1} [inst : Mul α] [
inst_1 : PartialOrder α] [MulLeftReflectLE α], IsLeftCancelMul α
· 使用定理 `IsOrderedMonoid.toMulLeftMono`：∀ {α : Type u_1} [inst : CommMonoid α] [i
nst_1 : Preorder α] [IsOrderedMonoid α], MulLeftMono α
· 使用定理 `instIsRightCancelMulOfMulRightReflectLE`：∀ {α : Type u_1} [inst : Mul α]
 [inst_1 : PartialOrder α] [MulRightReflectLE α], IsRightCancelMul α
· 使用定理 `LeftCancelSemigroup.toIsLeftCancelMul`：∀ {G : Type u} [self : LeftCancel
Semigroup G], IsLeftCancelMul G
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
· 使用定理 `Set.WellFoundedOn.mapsTo`：∀ {α : Type u_6} {β : Type u_7} {r : α → α → P
rop} (f : β → α) {s : Set α} {t : Set β},   Set.MapsTo f t s → s.WellFoundedOn r
 → t.WellFound…
-/
lemma LinearOrderedCommGroup.wellFoundedOn_setOfPred_ge_gt_iff_nonempty_discrete
    {G : Type*} [CommGroup G] [LinearOrder G] [IsOrderedMonoid G] [Nontrivial G] (g : G) :
    Set.WellFoundedOn {x : G | x ≤ g} (· > ·) ↔ Nonempty (G ≃*o Multiplicative ℤ) := by
  rw [← wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete (g := g⁻¹)]
  refine ⟨fun h ↦ (h.mapsTo (·⁻¹) ?_).mono' ?_, fun h ↦ (h.mapsTo (·⁻¹) ?_).mono' ?_⟩ <;>
  · intro
    simp [Function.onFun, inv_le']

@[deprecated (since := "2026-07-09")]
alias LinearOrderedCommGroup.wellFoundedOn_setOf_ge_gt_iff_nonempty_discrete :=
  LinearOrderedCommGroup.wellFoundedOn_setOfPred_ge_gt_iff_nonempty_discrete

set_option backward.isDefEq.respectTransparency false in
/-
**LinearOrderedCommGroupWithZero.wellFoundedOn_setOfPred_le_lt_iff_nonempty_disc
rete_of_ne_zero** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：LinearOrderedCommGroupWithZero.wellFoundedOn_setOfPred_le_lt_iff_nonempty_
discrete_of_ne_zero {G₀ : Type*} [LinearOrderedCommGroupWithZero G₀] [Nontrivial
 G₀ˣ] {g : G₀} (hg : g != 0) : Set.WellFoundedOn {x : G₀ | g <= x} (· < ·) ↔ Non
empty (G₀ ≃*o Intᵐ⁰)
参数：hg : g != 0。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Set.wellFoundedOn_sdiff_singleton`：wellFoundedOn_sdiff_singleton : WellF
oundedOn (s \ {a}) r ↔ WellFoundedOn s r
· 使用定理 `instIsStrictOrderLt`：∀ {α : Type u} [inst : Preorder α], IsStrictOrder α
 fun x1 x2 => x1 < x2
· 使用定理 `Set.WellFoundedOn.mono'`：mono' (h : forall (a) (_ : a in s) (b) (_ : b i
n s), r' a b -> r a b) : s.WellFoundedOn r -> s.WellFoundedOn r'
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
· 使用定理 `Set.WellFoundedOn.mapsTo`：∀ {α : Type u_6} {β : Type u_7} {r : α → α → P
rop} (f : β → α) {s : Set α} {t : Set β},   Set.MapsTo f t s → s.WellFoundedOn r
 → t.WellFound…
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `GroupWithZero.toNontrivial`：∀ {G₀ : Type u} [self : GroupWithZero G₀], N
ontrivial G₀
· 使用定理 `not_false_eq_true`：(¬False) = True
· 使用定理 `and_true`：∀ (p : Prop), (p ∧ True) = p
· 使用定理 `of_eq_false`：∀ {p : Prop}, p = False → ¬p
· 使用定理 `eq_false`：∀ {p : Prop}, ¬p → p = False
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `dite_cond_eq_false`：∀ {α : Sort u} {c : Prop} {x : Decidable c} {t : c →
 α} {e : ¬c → α} (h : c = False), dite c t e = e ⋯
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `implies_congr_ctx`：∀ {p₁ p₂ q₁ q₂ : Prop}, p₁ = p₂ → (p₂ → q₁ = q₂) → (p
₁ → q₁) = (p₂ → q₂)
· 使用引理 `LinearOrderedCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discre
te`：LinearOrderedCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete {
G : Type*} [CommGroup G] [LinearOrder G] [IsOrderedMonoid G] [No…
· 使用定理 `LinearOrderedCommMonoidWithZero.toIsOrderedMonoid`：∀ {α : Type u_1} [ins
t : LinearOrderedCommMonoidWithZero α], IsOrderedMonoid α
· 使用定理 `Nonempty.congr`：∀ {α : Sort u_3} {β : Sort u_4} (f : α → β) (g : β → α),
 Nonempty α ↔ Nonempty β
· 使用定理 `OrderMonoidIso.instMulEquivClass`：∀ {α : Type u_2} {β : Type u_3} [inst 
: Preorder α] [inst_1 : Preorder β] [inst_2 : Mul α] [inst_3 : Mul β],   MulEqui
vClass (α ≃*o β) α β
· 使用定理 `eq_or_ne`：eq_or_ne {α : Sort*} (x y : α) : x = y ∨ x != y
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用引理 `MulEquiv.symm_map_mul`：symm_map_mul {M N : Type*} [Mul M] [Mul N] (h : M
 ≃* N) (x y : N) : h.symm (x * y) = h.symm x * h.symm y
· 使用定理 `dite_cond_eq_true`：∀ {α : Sort u} {c : Prop} {x : Decidable c} {t : c → 
α} {e : ¬c → α} (h : c = True), dite c t e = t ⋯
（共 45 条，此处仅展示前 30 条）
-/
lemma LinearOrderedCommGroupWithZero.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete_of_ne_zero
    {G₀ : Type*} [LinearOrderedCommGroupWithZero G₀] [Nontrivial G₀ˣ] {g : G₀} (hg : g ≠ 0) :
    Set.WellFoundedOn {x : G₀ | g ≤ x} (· < ·) ↔ Nonempty (G₀ ≃*o ℤᵐ⁰) := by
  suffices Set.WellFoundedOn {x : G₀ | g ≤ x} (· < ·) ↔
    Set.WellFoundedOn {x : G₀ˣ | Units.mk0 g hg ≤ x} (· < ·) by
    rw [this, LinearOrderedCommGroup.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete]
    refine Nonempty.congr (fun f ↦ ⟨?_, ?_⟩) (fun f ↦ ⟨?_, ?_⟩)
    · exact WithZero.withZeroUnitsEquiv.symm.trans f.withZero
    · intro a b
      rcases eq_or_ne a 0 with rfl | ha
      · simp [WithZero.withZeroUnitsEquiv]
      rcases eq_or_ne b 0 with rfl | hb
      · simp [WithZero.withZeroUnitsEquiv]
      simp [WithZero.withZeroUnitsEquiv, ha, hb, ← Units.val_le_val]
    · exact MulEquiv.withZero.symm (WithZero.withZeroUnitsEquiv.trans f)
    · intros
      rw [← WithZero.coe_le_coe]
      simp
  rw [← Set.wellFoundedOn_sdiff_singleton (a := 0)]
  refine ⟨fun h ↦ (h.mapsTo Units.val ?_).mono' ?_,
    fun h ↦ (h.mapsTo ?_ ?_).mono' ?_⟩
  · intro
    simp [← Units.val_le_val]
  · simp [Function.onFun]
  · exact fun x ↦ if h : x = 0 then 1 else Units.mk0 x h
  · simp +contextual [← Units.val_le_val, MapsTo]
  · simp only [mem_sdiff, mem_ofPred_eq, mem_singleton_iff, Function.onFun, and_imp]
    intro _ _ ha0 _ _ hb0 h
    simp [ha0, hb0, ← Units.val_lt_val, h]

@[deprecated (since := "2026-07-09")]
alias LinearOrderedCommGroupWithZero.wellFoundedOn_setOf_le_lt_iff_nonempty_discrete_of_ne_zero :=
  LinearOrderedCommGroupWithZero.wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete_of_ne_zero
/-
**LinearOrderedCommGroupWithZero.wellFoundedOn_setOfPred_ge_gt_iff_nonempty_disc
rete_of_ne_zero** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：LinearOrderedCommGroupWithZero.wellFoundedOn_setOfPred_ge_gt_iff_nonempty_
discrete_of_ne_zero {G₀ : Type*} [LinearOrderedCommGroupWithZero G₀] [Nontrivial
 G₀ˣ] {g : G₀} (hg : g != 0) : Set.WellFoundedOn {x : G₀ | x <= g} (· > ·) ↔ Non
empty (G₀ ≃*o Intᵐ⁰)
参数：hg : g != 0。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `eq_false`：∀ {p : Prop}, ¬p → p = False
· 使用定理 `not_false_eq_true`：(¬False) = True
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `LinearOrderedCommGroupWithZero.wellFoundedOn_setOfPred_le_lt_iff_nonempt
y_discrete_of_ne_zero`：LinearOrderedCommGroupWithZero.wellFoundedOn_setOfPred_le
_lt_iff_nonempty_discrete_of_ne_zero {G₀ : Type*} [LinearOrderedCommGroupWithZer
o G…
· 使用定理 `Set.wellFoundedOn_sdiff_singleton`：wellFoundedOn_sdiff_singleton : WellF
oundedOn (s \ {a}) r ↔ WellFoundedOn s r
· 使用定理 `instIsStrictOrderGt`：∀ {α : Type u} [inst : Preorder α], IsStrictOrder α
 fun x1 x2 => x2 < x1
· 使用定理 `Set.WellFoundedOn.mono'`：mono' (h : forall (a) (_ : a in s) (b) (_ : b i
n s), r' a b -> r a b) : s.WellFoundedOn r -> s.WellFoundedOn r'
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用引理 `inv_strictAnti₀`：inv_strictAnti₀ (hb : 0 < b) (hba : b < a) : a⁻¹ < b⁻¹
· 使用定理 `PosMulReflectLE.toPosMulReflectLT`：∀ {α : Type u_1} [inst : MulZeroClass
 α] [inst_1 : PartialOrder α] [PosMulReflectLE α], PosMulReflectLT α
· 使用定理 `PosMulStrictMono.toPosMulReflectLE`：∀ {α : Type u_1} [inst : Mul α] [ins
t_1 : Zero α] [inst_2 : LinearOrder α] [PosMulStrictMono α], PosMulReflectLE α
· 使用定理 `LinearOrderedCommMonoidWithZero.toPosMulStrictMono`：∀ {α : Type u_3} [se
lf : LinearOrderedCommMonoidWithZero α], PosMulStrictMono α
· 使用定理 `MulPosReflectLE.toMulPosReflectLT`：∀ {α : Type u_1} [inst : MulZeroClass
 α] [inst_1 : PartialOrder α] [MulPosReflectLE α], MulPosReflectLT α
· 使用定理 `MulPosStrictMono.toMulPosReflectLE`：∀ {α : Type u_1} [inst : Mul α] [ins
t_1 : Zero α] [inst_2 : LinearOrder α] [MulPosStrictMono α], MulPosReflectLE α
· 使用定理 `LinearOrderedCommMonoidWithZero.toMulPosStrictMono`：∀ {α : Type u_1} [in
st : LinearOrderedCommMonoidWithZero α], MulPosStrictMono α
· 使用引理 `Mathlib.Tactic.Contrapose.contrapose₁`：contrapose₁ {p q : Prop} : (¬ q -
> ¬ p) -> (p -> q)
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `LinearOrderedCommMonoidWithZero.toIsBotZeroClass`：∀ {α : Type u_3} [self
 : LinearOrderedCommMonoidWithZero α], IsBotZeroClass α
· 使用定理 `Set.WellFoundedOn.mapsTo`：∀ {α : Type u_6} {β : Type u_7} {r : α → α → P
rop} (f : β → α) {s : Set α} {t : Set β},   Set.MapsTo f t s → s.WellFoundedOn r
 → t.WellFound…
· 使用定理 `eq_or_ne`：eq_or_ne {α : Sort*} (x y : α) : x = y ∨ x != y
· 使用定理 `inv_zero`：∀ {G₀ : Type u} [inst : GroupWithZero G₀], 0⁻¹ = 0
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `not_true_eq_false`：(¬True) = False
· 使用定理 `and_false`：∀ (p : Prop), (p ∧ False) = False
· 使用定理 `and_true`：∀ (p : Prop), (p ∧ True) = p
（共 33 条，此处仅展示前 30 条）
-/
lemma LinearOrderedCommGroupWithZero.wellFoundedOn_setOfPred_ge_gt_iff_nonempty_discrete_of_ne_zero
    {G₀ : Type*} [LinearOrderedCommGroupWithZero G₀] [Nontrivial G₀ˣ] {g : G₀} (hg : g ≠ 0) :
    Set.WellFoundedOn {x : G₀ | x ≤ g} (· > ·) ↔ Nonempty (G₀ ≃*o ℤᵐ⁰) := by
  have hg' : g⁻¹ ≠ 0 := by simp [hg]
  rw [← wellFoundedOn_setOfPred_le_lt_iff_nonempty_discrete_of_ne_zero hg',
    ← Set.wellFoundedOn_sdiff_singleton (a := 0)]
  refine ⟨fun h ↦ (h.mapsTo (·⁻¹) ?_).mono' ?_, fun h ↦ (h.mapsTo (·⁻¹) ?_).mono' ?_⟩
  · intro x
    rcases eq_or_ne x 0 with rfl | hx
    · simp [hg]
    simp only [mem_ofPred_eq, mem_sdiff, mem_singleton_iff, inv_eq_zero, hx, not_false_eq_true,
      and_true]
    refine (inv_le_comm₀ ?_ ?_).mp <;>
    simp [zero_lt_iff, hg, hx]
  · simp only [mem_ofPred_eq, Function.onFun, gt_iff_lt]
    intro a ha b _
    refine inv_strictAnti₀ ?_
    contrapose! ha
    simp only [le_zero_iff] at ha
    simp [zero_lt_iff, ha, hg]
  · intro x
    simp only [mem_sdiff, mem_ofPred_eq, mem_singleton_iff, and_imp]
    intro hxg hx
    refine inv_anti₀ ?_ hxg
    simp [zero_lt_iff, hx]
  · simp only [mem_sdiff, mem_ofPred_eq, mem_singleton_iff, gt_iff_lt, Function.onFun, and_imp]
    intro a _ _ b _ hb0
    refine inv_strictAnti₀ ?_
    simp [zero_lt_iff, hb0]

@[deprecated (since := "2026-07-09")]
alias LinearOrderedCommGroupWithZero.wellFoundedOn_setOf_ge_gt_iff_nonempty_discrete_of_ne_zero :=
  LinearOrderedCommGroupWithZero.wellFoundedOn_setOfPred_ge_gt_iff_nonempty_discrete_of_ne_zero
/-
**instWellFoundedGTWithZeroMultiplicativeIntLeOne** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：instWellFoundedGTWithZeroMultiplicativeIntLeOne : WellFoundedGT { v : Intᵐ
⁰ // v <= 1 }
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用引理 `LinearOrderedCommGroupWithZero.wellFoundedOn_setOfPred_ge_gt_iff_nonempt
y_discrete_of_ne_zero`：LinearOrderedCommGroupWithZero.wellFoundedOn_setOfPred_ge
_gt_iff_nonempty_discrete_of_ne_zero {G₀ : Type*} [LinearOrderedCommGroupWithZer
o G…
· 使用定理 `WithZero.instNontrivialUnits`：∀ {α : Type u_1} [inst : Group α] [Nontriv
ial α], Nontrivial (WithZero α)ˣ
· 使用定理 `one_ne_zero`：∀ {α : Type u_2} [inst : Zero α] [inst_1 : One α] [NeZero 1
], 1 ≠ 0
· 使用定理 `WithZero.instNontrivial`：∀ {α : Type u} [Nonempty α], Nontrivial (WithZe
ro α)
· 使用定理 `instNonemptyOfInhabited`：∀ {α : Sort u} [Inhabited α], Nonempty α
-/
instance instWellFoundedGTWithZeroMultiplicativeIntLeOne :
    WellFoundedGT { v : ℤᵐ⁰ // v ≤ 1 } :=
  { wf :=
    (LinearOrderedCommGroupWithZero.wellFoundedOn_setOfPred_ge_gt_iff_nonempty_discrete_of_ne_zero
    one_ne_zero).mpr instNonemptyOfInhabited }

end WellFounded

@[to_additive]
/-
**OrderMonoidIso.mulArchimedean** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：OrderMonoidIso.mulArchimedean {α β} [CommMonoid α] [PartialOrder α] [CommM
onoid β] [PartialOrder β] (e : α ≃*o β) [MulArchimedean α] : MulArchimedean β
参数：e : α ≃*o β。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `map_lt_map_iff`：map_lt_map_iff (f : F) {a b : α} : f a < f b ↔ a < b
· 使用定理 `OrderMonoidIso.instOrderIsoClass`：∀ {α : Type u_2} {β : Type u_3} [inst 
: Preorder α] [inst_1 : Preorder β] [inst_2 : Mul α] [inst_3 : Mul β],   OrderIs
oClass (α ≃*o β) α β
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `map_one`：map_one [OneHomClass F M N] (f : F) : f 1 = 1
· 使用定理 `MonoidHomClass.toOneHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `MulEquivClass.instMonoidHomClass`：∀ (F : Type u_1) {M : Type u_4} {N : T
ype u_5} [inst : EquivLike F M N] [inst_1 : MulOneClass M]   [inst_2 : MulOneCla
ss N] [MulEquivClass F…
· 使用定理 `OrderMonoidIso.instMulEquivClass`：∀ {α : Type u_2} {β : Type u_3} [inst 
: Preorder α] [inst_1 : Preorder β] [inst_2 : Mul α] [inst_3 : Mul β],   MulEqui
vClass (α ≃*o β) α β
· 使用定理 `OrderMonoidIso.apply_symm_apply`：apply_symm_apply (e : α ≃*o β) (y : β) 
: e (e.symm y) = y
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `Exists.imp`：∀ {α : Sort u_1} {p q : α → Prop}, (∀ (a : α), p a → q a) → 
(∃ a, p a) → ∃ a, q a
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `OrderIsoClass.map_le_map_iff`：∀ {F : Type u_6} {α : outParam (Type u_7)}
 {β : outParam (Type u_8)} {inst : LE α} {inst_1 : LE β}   {inst_2 : EquivLike F
 α β} [self : Orde…
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
· 使用定理 `MulArchimedean.arch`：∀ {R : Type u_2} {inst : CommMonoid R} {inst_1 : Pa
rtialOrder R} [self : MulArchimedean R] (x : R) {y : R},   1 < y → ∃ n, x ≤ y ^ 
n
-/
lemma OrderMonoidIso.mulArchimedean {α β}
    [CommMonoid α] [PartialOrder α] [CommMonoid β] [PartialOrder β]
    (e : α ≃*o β) [MulArchimedean α] : MulArchimedean β := by
  constructor
  intro x y hxy
  replace hxy : 1 < e.symm y := by simp [← map_lt_map_iff e, hxy]
  refine (MulArchimedean.arch (e.symm x) hxy).imp ?_
  simp [← map_pow, ← map_le_map_iff e]
/-
**WithZero.mulArchimedean_iff** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：WithZero.mulArchimedean_iff {α} [CommGroup α] [PartialOrder α] : MulArchim
edean (WithZero α) ↔ MulArchimedean α
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `OrderMonoidIso.mulArchimedean`：OrderMonoidIso.mulArchimedean {α β} [Comm
Monoid α] [PartialOrder α] [CommMonoid β] [PartialOrder β] (e : α ≃*o β) [MulArc
himedean α] : MulAr…
-/
lemma WithZero.mulArchimedean_iff {α} [CommGroup α] [PartialOrder α] :
    MulArchimedean (WithZero α) ↔ MulArchimedean α := by
  constructor <;> intro _
  · exact OrderMonoidIso.unitsWithZero.mulArchimedean
  · infer_instance
/-
**Units.mulArchimedean_iff** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：Units.mulArchimedean_iff {G₀} [LinearOrderedCommGroupWithZero G₀] : MulArc
himedean G₀ˣ ↔ MulArchimedean G₀
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `OrderMonoidIso.mulArchimedean`：OrderMonoidIso.mulArchimedean {α β} [Comm
Monoid α] [PartialOrder α] [CommMonoid β] [PartialOrder β] (e : α ≃*o β) [MulArc
himedean α] : MulAr…
-/
lemma Units.mulArchimedean_iff {G₀} [LinearOrderedCommGroupWithZero G₀] :
    MulArchimedean G₀ˣ ↔ MulArchimedean G₀ := by
  constructor <;> intro _
  · exact OrderMonoidIso.withZeroUnits.mulArchimedean
  · infer_instance

section LocallyFiniteOrder

variable {X : Type*} [Preorder X] [LocallyFiniteOrder X]

/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : LocallyFiniteOrder (Multiplicative X) :=
  OrderIso.locallyFiniteOrder (⟨Multiplicative.toAdd, by simp⟩ : Multiplicative X ≃o X)
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : LocallyFiniteOrder (Additive X) :=
  OrderIso.locallyFiniteOrder (⟨Additive.toMul, by simp⟩ : Additive X ≃o X)

noncomputable
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [Monoid X] : LocallyFiniteOrder (Units X) :=
  OrderEmbedding.locallyFiniteOrder (⟨⟨Units.val, Units.val_injective⟩, by simp⟩ : Units X ↪o X)
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [Group X] : LocallyFiniteOrder (WithZero X)ˣ :=
  OrderIso.locallyFiniteOrder (OrderMonoidIso.unitsWithZero (α := X) : (WithZero X)ˣ ≃o X)

end LocallyFiniteOrder

section DenselyOrdered

variable {X : Type*} [LT X]

/-
**denselyOrdered_additive_iff** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：denselyOrdered_additive_iff : DenselyOrdered (Additive X) ↔ DenselyOrdered
 X
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma denselyOrdered_additive_iff : DenselyOrdered (Additive X) ↔ DenselyOrdered X := Iff.rfl
/-
**denselyOrdered_multiplicative_iff** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：denselyOrdered_multiplicative_iff : DenselyOrdered (Multiplicative X) ↔ De
nselyOrdered X
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma denselyOrdered_multiplicative_iff : DenselyOrdered (Multiplicative X) ↔ DenselyOrdered X :=
  Iff.rfl
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [DenselyOrdered X] : DenselyOrdered (Multiplicative X) :=
  denselyOrdered_multiplicative_iff.2 ‹_›
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [DenselyOrdered X] : DenselyOrdered (Additive X) :=
  denselyOrdered_additive_iff.2 ‹_›
/-
**WithZero.denselyOrdered_iff** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：WithZero.denselyOrdered_iff {M : Type*} [Preorder M] [NoMinOrder M] : Dens
elyOrdered (WithZero M) ↔ DenselyOrdered M
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `WithBot.denselyOrdered_iff`：denselyOrdered_iff [LT α] [NoMinOrder α] : D
enselyOrdered (WithBot α) ↔ DenselyOrdered α
-/
lemma WithZero.denselyOrdered_iff {M : Type*} [Preorder M] [NoMinOrder M] :
    DenselyOrdered (WithZero M) ↔ DenselyOrdered M :=
  WithBot.denselyOrdered_iff
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance {X : Type*} [Preorder X] [NoMinOrder X] [DenselyOrdered X] :
    DenselyOrdered (WithZero X) :=
  WithZero.denselyOrdered_iff.mpr inferInstance
/-
**Int.not_denselyOrdered** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：Int.not_denselyOrdered : ¬ DenselyOrdered Int
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用引理 `LinearOrderedAddCommGroup.discrete_iff_not_denselyOrdered`：LinearOrdered
AddCommGroup.discrete_iff_not_denselyOrdered (G : Type*) [AddCommGroup G] [Linea
rOrder G] [IsOrderedAddMonoid G] [Archimedean G…
· 使用定理 `instArchimedeanInt`：Archimedean ℤ
-/
lemma Int.not_denselyOrdered : ¬ DenselyOrdered ℤ :=
  (LinearOrderedAddCommGroup.discrete_iff_not_denselyOrdered ℤ).mp ⟨.refl _⟩
/-
**not_denselyOrdered_withZero_int** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：not_denselyOrdered_withZero_int : ¬ DenselyOrdered Intᵐ⁰
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用引理 `LinearOrderedCommGroupWithZero.discrete_iff_not_denselyOrdered`：LinearOr
deredCommGroupWithZero.discrete_iff_not_denselyOrdered (G : Type*) [LinearOrdere
dCommGroupWithZero G] [Nontrivial Gˣ] [MulArchimedea…
· 使用定理 `WithZero.instNontrivialUnits`：∀ {α : Type u_1} [inst : Group α] [Nontriv
ial α], Nontrivial (WithZero α)ˣ
· 使用定理 `instArchimedeanInt`：Archimedean ℤ
-/
lemma not_denselyOrdered_withZero_int : ¬ DenselyOrdered ℤᵐ⁰ :=
  (LinearOrderedCommGroupWithZero.discrete_iff_not_denselyOrdered _).mp ⟨.refl _⟩
/-
**WithZero.denselyOrdered_set_iff_subsingleton** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：WithZero.denselyOrdered_set_iff_subsingleton {X : Type*} [LinearOrder X] [
LocallyFiniteOrder X] {s : Set (WithZero X)} : DenselyOrdered s ↔ s.Subsingleton
参数：WithZero X。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `WithBot.denselyOrdered_set_iff_subsingleton`：WithBot.denselyOrdered_set_
iff_subsingleton {s : Set (WithBot X)} : DenselyOrdered s ↔ s.Subsingleton
-/
lemma WithZero.denselyOrdered_set_iff_subsingleton {X : Type*} [LinearOrder X]
    [LocallyFiniteOrder X] {s : Set (WithZero X)} :
    DenselyOrdered s ↔ s.Subsingleton :=
  WithBot.denselyOrdered_set_iff_subsingleton

end DenselyOrdered

