/-
Copyright (c) 2018 Jeremy Avigad. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Jeremy Avigad, Simon Hudon
-/
module

public import Mathlib.Data.PFunctor.Multivariate.W
public import Mathlib.Data.QPF.Multivariate.Basic

/-!
# The initial algebra of a multivariate qpf is again a qpf.

For an `(n+1)`-ary QPF `F (α₀,..,αₙ)`, we take the least fixed point of `F` with
regards to its last argument `αₙ`. The result is an `n`-ary functor: `Fix F (α₀,..,αₙ₋₁)`.
Making `Fix F` into a functor allows us to take the fixed point, compose with other functors
and take a fixed point again.

## Main definitions

* `Fix.mk`     - constructor
* `Fix.dest`    - destructor
* `Fix.rec`    - recursor: basis for defining functions by structural recursion on `Fix F α`
* `Fix.drec`   - dependent recursor: generalization of `Fix.rec` where
                  the result type of the function is allowed to depend on the `Fix F α` value
* `Fix.rec_eq` - defining equation for `recursor`
* `Fix.ind`    - induction principle for `Fix F α`

## Implementation notes

For `F` a `QPF`, we define `Fix F α` in terms of the W-type of the polynomial functor `P` of `F`.
We define the relation `WEquiv` and take its quotient as the definition of `Fix F α`.

See [avigad-carneiro-hudon2019] for more details.

## Reference

* Jeremy Avigad, Mario M. Carneiro and Simon Hudon.
  [*Data Types as Quotients of Polynomial Functors*][avigad-carneiro-hudon2019]
-/

@[expose] public section


universe u v

namespace MvQPF

open TypeVec

open MvFunctor (LiftP LiftR)

open MvFunctor

variable {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQPF F]


/-- `recF` is used as a basis for defining the recursor on `Fix F α`. `recF`
traverses recursively the W-type generated by `q.P` using a function on `F`
as a recursive step -/
/-
**MvQPF.recF** 是 Mathlib 中的一个定义，位于命名空间 `MvQPF`。
形式化陈述：recF {α : TypeVec n} {β : Type u} (g : F (α.append1 β) -> β) : q.P.W α -> 
β
参数：g : F (α.append1 β) -> β。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`recF` is used as a basis for defining the recursor on `Fix F α`. `recF`
traverses recursively the W-type generated by `q.P` using a function on `F`
as a recursive step
-/
def recF {α : TypeVec n} {β : Type u} (g : F (α.append1 β) → β) : q.P.W α → β :=
  q.P.wRec fun a f' _f rec => g (abs ⟨a, splitFun f' rec⟩)
/-
**MvQPF.recF_eq** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF`。
形式化陈述：recF_eq {α : TypeVec n} {β : Type u} (g : F (α.append1 β) -> β) (a : q.P.A
) (f' : q.P.drop.B a ⟹ α) (f : q.P.last.B a -> q.P.W α) : recF g (q.P.wMk a f' f
) = g (abs ⟨a, splitFun f' (recF g ∘ f)⟩)
参数：g : F (α.append1 β) -> β；a : q.P.A；f' : q.P.drop.B a ⟹ α；f : q.P.last.B a -> 
q.P.W α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MvQPF.recF.eq_1`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQPF
 F] {α : TypeVec.{u} n} {β : Type u} (g : F (α ::: β) → β),   MvQPF.recF g = (Mv
QPF.P…
· 使用定理 `MvPFunctor.wRec_eq`：wRec_eq {α : TypeVec n} {C : Sort*} (g : forall a : 
P.A, P.drop.B a ⟹ α -> (P.last.B a -> P.W α) -> (P.last.B a -> C) -> C) (a : P.A
) (f' : …
-/
theorem recF_eq {α : TypeVec n} {β : Type u} (g : F (α.append1 β) → β) (a : q.P.A)
    (f' : q.P.drop.B a ⟹ α) (f : q.P.last.B a → q.P.W α) :
    recF g (q.P.wMk a f' f) = g (abs ⟨a, splitFun f' (recF g ∘ f)⟩) := by
  rw [recF, MvPFunctor.wRec_eq]; rfl

set_option backward.isDefEq.respectTransparency false in
/-
**MvQPF.recF_eq'** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF`。
形式化陈述：recF_eq' {α : TypeVec n} {β : Type u} (g : F (α.append1 β) -> β) (x : q.P.
W α) : recF g x = g (abs (appendFun id (recF g) <$$> q.P.wDest' x))
参数：g : F (α.append1 β) -> β；x : q.P.W α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MvQPF.recF_eq`：recF_eq {α : TypeVec n} {β : Type u} (g : F (α.append1 β)
 -> β) (a : q.P.A) (f' : q.P.drop.B a ⟹ α) (f : q.P.last.B a -> q.P.W α) : recF 
g (…
· 使用定理 `MvPFunctor.wDest'_wMk`：∀ {n : ℕ} (P : MvPFunctor.{u} (n + 1)) {α : TypeV
ec.{u} n} (a : P.A) (f' : (P.drop.B a).Arrow α)   (f : P.last.B a → P.W α), P.wD
est' (P.wMk…
· 使用定理 `MvPFunctor.map_eq`：map_eq {α β : TypeVec n} (g : α ⟹ β) (a : P.A) (f : P
.B a ⟹ α) : @MvFunctor.map _ P.Obj _ _ _ g ⟨a, f⟩ = ⟨a, g ⊚ f⟩
· 使用定理 `TypeVec.appendFun_comp_splitFun`：appendFun_comp_splitFun {α γ : TypeVec 
n} {β δ : Type*} {ε : TypeVec (n + 1)} (f₀ : drop ε ⟹ α) (f₁ : α ⟹ γ) (g₀ : last
 ε -> β) (g₁ : β -> δ…
· 使用定理 `TypeVec.id_comp`：id_comp (f : α ⟹ β) : id ⊚ f = f
-/
theorem recF_eq' {α : TypeVec n} {β : Type u} (g : F (α.append1 β) → β) (x : q.P.W α) :
    recF g x = g (abs (appendFun id (recF g) <$$> q.P.wDest' x)) := by
  induction x using q.P.wCases
  case ih a f' f =>
    rw [recF_eq, q.P.wDest'_wMk, MvPFunctor.map_eq, appendFun_comp_splitFun, TypeVec.id_comp]

/-- Equivalence relation on W-types that represent the same `Fix F`
value -/
/-
**MvQPF.WEquiv** 是 Mathlib 中的一个归纳类型，位于命名空间 `MvQPF`。
形式化陈述：{n : ℕ} →   {F : TypeVec.{u} (n + 1) → Type u} → [q : MvQPF F] → {α : Type
Vec.{u} n} → (MvQPF.P F).W α → (MvQPF.P F).W α → Prop
参数：n + 1；MvQPF.P F；MvQPF.P F。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Equivalence relation on W-types that represent the same `Fix F`
value
-/
inductive WEquiv {α : TypeVec n} : q.P.W α → q.P.W α → Prop
  | ind (a : q.P.A) (f' : q.P.drop.B a ⟹ α) (f₀ f₁ : q.P.last.B a → q.P.W α) :
    (∀ x, WEquiv (f₀ x) (f₁ x)) → WEquiv (q.P.wMk a f' f₀) (q.P.wMk a f' f₁)
  | abs (a₀ : q.P.A) (f'₀ : q.P.drop.B a₀ ⟹ α) (f₀ : q.P.last.B a₀ → q.P.W α) (a₁ : q.P.A)
    (f'₁ : q.P.drop.B a₁ ⟹ α) (f₁ : q.P.last.B a₁ → q.P.W α) :
    abs ⟨a₀, q.P.appendContents f'₀ f₀⟩ = abs ⟨a₁, q.P.appendContents f'₁ f₁⟩ →
      WEquiv (q.P.wMk a₀ f'₀ f₀) (q.P.wMk a₁ f'₁ f₁)
  | trans (u v w : q.P.W α) : WEquiv u v → WEquiv v w → WEquiv u w

set_option backward.isDefEq.respectTransparency false in
/-
**MvQPF.recF_eq_of_wEquiv** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF`。
形式化陈述：recF_eq_of_wEquiv (α : TypeVec n) {β : Type u} (u : F (α.append1 β) -> β) 
(x y : q.P.W α) : WEquiv x y -> recF u x = recF u y
参数：α : TypeVec n；u : F (α.append1 β) -> β；x y : q.P.W α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MvQPF.recF_eq`：recF_eq {α : TypeVec n} {β : Type u} (g : F (α.append1 β)
 -> β) (a : q.P.A) (f' : q.P.drop.B a ⟹ α) (f : q.P.last.B a -> q.P.W α) : recF 
g (…
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `MvQPF.recF_eq'`：recF_eq' {α : TypeVec n} {β : Type u} (g : F (α.append1 
β) -> β) (x : q.P.W α) : recF g x = g (abs (appendFun id (recF g) <$$> q.P.wDest
' x)…
· 使用定理 `MvPFunctor.wDest'_wMk`：∀ {n : ℕ} (P : MvPFunctor.{u} (n + 1)) {α : TypeV
ec.{u} n} (a : P.A) (f' : (P.drop.B a).Arrow α)   (f : P.last.B a → P.W α), P.wD
est' (P.wMk…
· 使用定理 `MvQPF.abs_map`：∀ {n : ℕ} {F : TypeVec.{u} n → Type u_1} [self : MvQPF F]
 {α β : TypeVec.{u} n} (f : α.Arrow β) (p : ↑(MvQPF.P F) α),   MvQPF.abs (MvFunc
tor…
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem recF_eq_of_wEquiv (α : TypeVec n) {β : Type u} (u : F (α.append1 β) → β) (x y : q.P.W α) :
    WEquiv x y → recF u x = recF u y := by
  induction x using q.P.wCases
  case ih a₀ f'₀ f₀ =>
    induction y using q.P.wCases
    case ih a₁ f'₁ f₁ =>
      intro h
      -- Porting note: induction on h doesn't work.
      refine @WEquiv.recOn _ _ _ _ (fun a a' _ ↦ recF u a = recF u a') _ _ h ?_ ?_ ?_
      · intro a f' f₀ f₁ _h ih; simp only [recF_eq]
        congr 4; funext; apply ih
      · intro a₀ f'₀ f₀ a₁ f'₁ f₁ h; simp only [recF_eq', abs_map, MvPFunctor.wDest'_wMk, h]
      · intro x y z _e₁ _e₂ ih₁ ih₂; exact Eq.trans ih₁ ih₂
/-
**MvQPF.wEquiv.abs'** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF.wEquiv`。
形式化陈述：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQPF F] {α : TypeVec.{u
} n} (x y : (MvQPF.P F).W α),   MvQPF.abs ((MvQPF.P F).wDest' x) = MvQPF.abs ((M
vQPF.P F).wDest' y) → MvQPF.WEquiv x y
参数：n + 1；x y : (MvQPF.P F).W α；(MvQPF.P F).wDest' x；(MvQPF.P F).wDest' y。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem wEquiv.abs' {α : TypeVec n} (x y : q.P.W α)
    (h : MvQPF.abs (q.P.wDest' x) = MvQPF.abs (q.P.wDest' y)) :
    WEquiv x y := by
  revert h
  induction x using q.P.wCases
  case ih a₀ f'₀ f₀ =>
    induction y using q.P.wCases
    apply WEquiv.abs
/-
**MvQPF.wEquiv.refl** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF.wEquiv`。
形式化陈述：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQPF F] {α : TypeVec.{u
} n} (x : (MvQPF.P F).W α), MvQPF.WEquiv x x
参数：n + 1；x : (MvQPF.P F).W α。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MvQPF.wEquiv.abs'`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQ
PF F] {α : TypeVec.{u} n} (x y : (MvQPF.P F).W α),   MvQPF.abs ((MvQPF.P F).wDes
t' x) =…
-/
theorem wEquiv.refl {α : TypeVec n} (x : q.P.W α) : WEquiv x x := abs' x x rfl
/-
**MvQPF.wEquiv.symm** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF.wEquiv`。
形式化陈述：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQPF F] {α : TypeVec.{u
} n} (x y : (MvQPF.P F).W α),   MvQPF.WEquiv x y → MvQPF.WEquiv y x
参数：n + 1；x y : (MvQPF.P F).W α。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
-/
theorem wEquiv.symm {α : TypeVec n} (x y : q.P.W α) : WEquiv x y → WEquiv y x := by
  intro h; induction h with
  | ind a f' f₀ f₁ _h ih => exact WEquiv.ind _ _ _ _ ih
  | abs a₀ f'₀ f₀ a₁ f'₁ f₁ h => exact WEquiv.abs _ _ _ _ _ _ h.symm
  | trans x y z _e₁ _e₂ ih₁ ih₂ => exact MvQPF.WEquiv.trans _ _ _ ih₂ ih₁

/-- maps every element of the W type to a canonical representative -/
/-
**MvQPF.wrepr** 是 Mathlib 中的一个定义，位于命名空间 `MvQPF`。
形式化陈述：wrepr {α : TypeVec n} : q.P.W α -> q.P.W α
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
maps every element of the W type to a canonical representative
-/
def wrepr {α : TypeVec n} : q.P.W α → q.P.W α :=
  recF (q.P.wMk' ∘ repr)
/-
**MvQPF.wrepr_wMk** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF`。
形式化陈述：wrepr_wMk {α : TypeVec n} (a : q.P.A) (f' : q.P.drop.B a ⟹ α) (f : q.P.las
t.B a -> q.P.W α) : wrepr (q.P.wMk a f' f) = q.P.wMk' (repr (abs (appendFun id w
repr <$$> ⟨a, q.P.appendContents f' f⟩)))
参数：a : q.P.A；f' : q.P.drop.B a ⟹ α；f : q.P.last.B a -> q.P.W α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MvQPF.wrepr.eq_1`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQP
F F] {α : TypeVec.{u} n},   MvQPF.wrepr = MvQPF.recF ((MvQPF.P F).wMk' ∘ MvQPF.r
epr)
· 使用定理 `MvQPF.recF_eq'`：recF_eq' {α : TypeVec n} {β : Type u} (g : F (α.append1 
β) -> β) (x : q.P.W α) : recF g x = g (abs (appendFun id (recF g) <$$> q.P.wDest
' x)…
· 使用定理 `MvPFunctor.wDest'_wMk`：∀ {n : ℕ} (P : MvPFunctor.{u} (n + 1)) {α : TypeV
ec.{u} n} (a : P.A) (f' : (P.drop.B a).Arrow α)   (f : P.last.B a → P.W α), P.wD
est' (P.wMk…
-/
theorem wrepr_wMk {α : TypeVec n} (a : q.P.A) (f' : q.P.drop.B a ⟹ α)
    (f : q.P.last.B a → q.P.W α) :
    wrepr (q.P.wMk a f' f) =
      q.P.wMk' (repr (abs (appendFun id wrepr <$$> ⟨a, q.P.appendContents f' f⟩))) := by
  rw [wrepr, recF_eq', q.P.wDest'_wMk]; rfl

set_option backward.isDefEq.respectTransparency false in
/-
**MvQPF.wrepr_equiv** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF`。
形式化陈述：wrepr_equiv {α : TypeVec n} (x : q.P.W α) : WEquiv (wrepr x) x
参数：x : q.P.W α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MvQPF.wEquiv.abs'`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQ
PF F] {α : TypeVec.{u} n} (x y : (MvQPF.P F).W α),   MvQPF.abs ((MvQPF.P F).wDes
t' x) =…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MvQPF.wrepr_wMk`：wrepr_wMk {α : TypeVec n} (a : q.P.A) (f' : q.P.drop.B 
a ⟹ α) (f : q.P.last.B a -> q.P.W α) : wrepr (q.P.wMk a f' f) = q.P.wMk' (repr (
abs (…
· 使用定理 `MvPFunctor.wDest'_wMk'`：∀ {n : ℕ} (P : MvPFunctor.{u} (n + 1)) {α : Type
Vec.{u} n} (x : ↑P (α ::: P.W α)), P.wDest' (P.wMk' x) = x
· 使用定理 `MvQPF.abs_repr`：∀ {n : ℕ} {F : TypeVec.{u} n → Type u_1} [self : MvQPF F
] {α : TypeVec.{u} n} (x : F α), MvQPF.abs (MvQPF.repr x) = x
· 使用定理 `MvPFunctor.map_eq`：map_eq {α β : TypeVec n} (g : α ⟹ β) (a : P.A) (f : P
.B a ⟹ α) : @MvFunctor.map _ P.Obj _ _ _ g ⟨a, f⟩ = ⟨a, g ⊚ f⟩
· 使用定理 `MvPFunctor.wMk'.eq_1`：∀ {n : ℕ} (P : MvPFunctor.{u} (n + 1)) {α : TypeVe
c.{u} n} (a : P.A) (f : (P.B a).Arrow (α ::: P.W α)),   P.wMk' ⟨a, f⟩ = P.wMk a 
(TypeVec.d…
· 使用定理 `TypeVec.appendFun_comp_splitFun`：appendFun_comp_splitFun {α γ : TypeVec 
n} {β δ : Type*} {ε : TypeVec (n + 1)} (f₀ : drop ε ⟹ α) (f₁ : α ⟹ γ) (g₀ : last
 ε -> β) (g₁ : β -> δ…
· 使用定理 `TypeVec.id_comp`：id_comp (f : α ⟹ β) : id ⊚ f = f
-/
theorem wrepr_equiv {α : TypeVec n} (x : q.P.W α) : WEquiv (wrepr x) x := by
  induction x using q.P.wInd
  case ih a f' f ih =>
    apply WEquiv.trans _ (q.P.wMk' (appendFun id wrepr <$$> ⟨a, q.P.appendContents f' f⟩))
    · apply wEquiv.abs'
      rw [wrepr_wMk, q.P.wDest'_wMk', q.P.wDest'_wMk', abs_repr]
    rw [q.P.map_eq, MvPFunctor.wMk', appendFun_comp_splitFun, id_comp]
    apply WEquiv.ind; exact ih
/-
**MvQPF.wEquiv_map** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF`。
形式化陈述：wEquiv_map {α β : TypeVec n} (g : α ⟹ β) (x y : q.P.W α) : WEquiv x y -> W
Equiv (g <$$> x) (g <$$> y)
参数：g : α ⟹ β；x y : q.P.W α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MvPFunctor.w_map_wMk`：w_map_wMk {α β : TypeVec n} (g : α ⟹ β) (a : P.A) 
(f' : P.drop.B a ⟹ α) (f : P.last.B a -> P.W α) : g < > P.wMk a f' f = P.wMk a (
g ⊚ f') fu…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MvPFunctor.map_objAppend1`：map_objAppend1 {α γ : TypeVec n} (g : α ⟹ γ) 
(a : P.A) (f' : P.drop.B a ⟹ α) (f : P.last.B a -> P.W α) : appendFun g (P.wMap 
g) < > P.objApp…
· 使用定理 `MvQPF.abs_map`：∀ {n : ℕ} {F : TypeVec.{u} n → Type u_1} [self : MvQPF F]
 {α β : TypeVec.{u} n} (f : α.Arrow β) (p : ↑(MvQPF.P F) α),   MvQPF.abs (MvFunc
tor…
-/
theorem wEquiv_map {α β : TypeVec n} (g : α ⟹ β) (x y : q.P.W α) :
    WEquiv x y → WEquiv (g <$$> x) (g <$$> y) := by
  intro h; induction h with
  | ind a f' f₀ f₁ h ih => rw [q.P.w_map_wMk, q.P.w_map_wMk]; apply WEquiv.ind; exact ih
  | abs a₀ f'₀ f₀ a₁ f'₁ f₁ h =>
    rw [q.P.w_map_wMk, q.P.w_map_wMk]; apply WEquiv.abs
    change
      abs (q.P.objAppend1 a₀ (g ⊚ f'₀) fun x => q.P.wMap g (f₀ x)) =
        abs (q.P.objAppend1 a₁ (g ⊚ f'₁) fun x => q.P.wMap g (f₁ x))
    rw [← q.P.map_objAppend1, ← q.P.map_objAppend1, abs_map, abs_map, h]
  | trans x y z _ _ ih₁ ih₂ =>
    apply MvQPF.WEquiv.trans
    · apply ih₁
    · apply ih₂

/-- Define the fixed point as the quotient of trees under the equivalence relation.
-/
@[instance_reducible]
/-
**MvQPF.wSetoid** 是 Mathlib 中的一个定义，位于命名空间 `MvQPF`。
形式化陈述：wSetoid (α : TypeVec n) : Setoid (q.P.W α)
参数：α : TypeVec n。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Define the fixed point as the quotient of trees under the equivalence relation.
-/
def wSetoid (α : TypeVec n) : Setoid (q.P.W α) :=
  ⟨WEquiv, wEquiv.refl, wEquiv.symm _ _, WEquiv.trans _ _ _⟩

attribute [local instance] wSetoid

/-- Least fixed point of functor F. The result is a functor with one fewer parameters
than the input. For `F a b c` a ternary functor, `Fix F` is a binary functor such that

```lean
Fix F a b = F a b (Fix F a b)
```
-/
/-
**MvQPF.Fix** 是 Mathlib 中的一个定义，位于命名空间 `MvQPF`。
形式化陈述：Fix {n : Nat} (F : TypeVec (n + 1) -> Type*) [q : MvQPF F] (α : TypeVec n)
参数：F : TypeVec (n + 1) -> Type*；α : TypeVec n。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Least fixed point of functor F. The result is a functor with one fewer parameter
s
than the input. For `F a b c` a ternary functor, `Fix F` is a binary functor suc
h that

```lean
Fix F a b = F a b (Fix F a b)
```
-/
def Fix {n : ℕ} (F : TypeVec (n + 1) → Type*) [q : MvQPF F] (α : TypeVec n) :=
  Quotient (wSetoid α : Setoid (q.P.W α))

/-- `Fix F` is a functor -/
/-
**MvQPF.Fix.map** 是 Mathlib 中的一个定义，位于命名空间 `MvQPF.Fix`。
形式化陈述：{n : ℕ} →   {F : TypeVec.{u} (n + 1) → Type u} → [q : MvQPF F] → {α β : Ty
peVec.{u} n} → α.Arrow β → MvQPF.Fix F α → MvQPF.Fix F β
参数：n + 1。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`Fix F` is a functor
-/
def Fix.map {α β : TypeVec n} (g : α ⟹ β) : Fix F α → Fix F β :=
  Quotient.lift (fun x : q.P.W α => ⟦q.P.wMap g x⟧) fun _a _b h => Quot.sound (wEquiv_map _ _ _ h)
/-
**MvQPF.Fix.mvfunctor** 是 Mathlib 中的一个定义，位于命名空间 `MvQPF.Fix`。
形式化陈述：{n : ℕ} → {F : TypeVec.{u} (n + 1) → Type u} → [q : MvQPF F] → MvFunctor (
MvQPF.Fix F)
参数：n + 1；MvQPF.Fix F。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance Fix.mvfunctor : MvFunctor (Fix F) where map := Fix.map

variable {α : TypeVec.{u} n}

/-- Recursor for `Fix F` -/
/-
**MvQPF.Fix.rec** 是 Mathlib 中的一个定义，位于命名空间 `MvQPF.Fix`。
形式化陈述：{n : ℕ} →   {F : TypeVec.{u} (n + 1) → Type u} →     [q : MvQPF F] → {α : 
TypeVec.{u} n} → {β : Type u} → (F (α ::: β) → β) → MvQPF.Fix F α → β
参数：n + 1；F (α ::: β) → β。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `MvQPF.recF_eq_of_wEquiv`：recF_eq_of_wEquiv (α : TypeVec n) {β : Type u} 
(u : F (α.append1 β) -> β) (x y : q.P.W α) : WEquiv x y -> recF u x = recF u y

--- 原说明 ---
Recursor for `Fix F`
-/
def Fix.rec {β : Type u} (g : F (α ::: β) → β) : Fix F α → β :=
  Quot.lift (recF g) (recF_eq_of_wEquiv α g)

/-- Access W-type underlying `Fix F` -/
/-
**MvQPF.fixToW** 是 Mathlib 中的一个定义，位于命名空间 `MvQPF`。
形式化陈述：fixToW : Fix F α -> q.P.W α
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Access W-type underlying `Fix F`
-/
def fixToW : Fix F α → q.P.W α :=
  Quotient.lift wrepr (recF_eq_of_wEquiv α fun x => q.P.wMk' (repr x))

/-- Constructor for `Fix F` -/
/-
**MvQPF.Fix.mk** 是 Mathlib 中的一个定义，位于命名空间 `MvQPF.Fix`。
形式化陈述：{n : ℕ} →   {F : TypeVec.{u} (n + 1) → Type u} → [q : MvQPF F] → {α : Type
Vec.{u} n} → F (α ::: MvQPF.Fix F α) → MvQPF.Fix F α
参数：n + 1；α ::: MvQPF.Fix F α。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Constructor for `Fix F`
-/
def Fix.mk (x : F (append1 α (Fix F α))) : Fix F α :=
  Quot.mk _ (q.P.wMk' (appendFun id fixToW <$$> repr x))

/-- Destructor for `Fix F` -/
/-
**MvQPF.Fix.dest** 是 Mathlib 中的一个定义，位于命名空间 `MvQPF.Fix`。
形式化陈述：{n : ℕ} →   {F : TypeVec.{u} (n + 1) → Type u} → [q : MvQPF F] → {α : Type
Vec.{u} n} → MvQPF.Fix F α → F (α ::: MvQPF.Fix F α)
参数：n + 1；α ::: MvQPF.Fix F α。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Destructor for `Fix F`
-/
def Fix.dest : Fix F α → F (append1 α (Fix F α)) :=
  Fix.rec (MvFunctor.map (appendFun id Fix.mk))

set_option backward.isDefEq.respectTransparency false in
/-
**MvQPF.Fix.rec_eq** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF.Fix`。
形式化陈述：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQPF F] {α : TypeVec.{u
} n} {β : Type u} (g : F (α ::: β) → β)   (x : F (α ::: MvQPF.Fix F α)), MvQPF.F
ix.rec g (MvQPF.Fix.mk x) = g (MvFunctor.map (TypeVec.id ::: MvQPF.Fix.rec g) x)
参数：n + 1；g : F (α ::: β) → β；x : F (α ::: MvQPF.Fix F α)；MvQPF.Fix.mk x；MvFuncto
r.map (TypeVec.id ::: MvQPF.Fix.rec g) x。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Quotient.ind`：∀ {α : Sort u} {s : Setoid α} {motive : Quotient s → Prop}
, (∀ (a : α), motive ⟦a⟧) → ∀ (q : Quotient s), motive q
· 使用定理 `MvQPF.recF_eq_of_wEquiv`：recF_eq_of_wEquiv (α : TypeVec n) {β : Type u} 
(u : F (α.append1 β) -> β) (x y : q.P.W α) : WEquiv x y -> recF u x = recF u y
· 使用定理 `MvQPF.wrepr_equiv`：wrepr_equiv {α : TypeVec n} (x : q.P.W α) : WEquiv (w
repr x) x
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MvQPF.Fix.rec.eq_1`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : Mv
QPF F] {α : TypeVec.{u} n} {β : Type u} (g : F (α ::: β) → β),   MvQPF.Fix.rec g
 = Quot.…
· 使用定理 `MvQPF.Fix.mk.eq_1`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQ
PF F] {α : TypeVec.{u} n} (x : F (α ::: MvQPF.Fix F α)),   MvQPF.Fix.mk x =     
Quot.mk…
· 使用定理 `MvPFunctor.map_eq`：map_eq {α β : TypeVec n} (g : α ⟹ β) (a : P.A) (f : P
.B a ⟹ α) : @MvFunctor.map _ P.Obj _ _ _ g ⟨a, f⟩ = ⟨a, g ⊚ f⟩
· 使用定理 `MvQPF.recF_eq'`：recF_eq' {α : TypeVec n} {β : Type u} (g : F (α.append1 
β) -> β) (x : q.P.W α) : recF g x = g (abs (appendFun id (recF g) <$$> q.P.wDest
' x)…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MvPFunctor.wDest'_wMk'`：∀ {n : ℕ} (P : MvPFunctor.{u} (n + 1)) {α : Type
Vec.{u} n} (x : ↑P (α ::: P.W α)), P.wDest' (P.wMk' x) = x
· 使用定理 `MvPFunctor.comp_map`：∀ {n : ℕ} (P : MvPFunctor.{u} n) {α β γ : TypeVec.{
u} n} (f : α.Arrow β) (g : β.Arrow γ) (x : ↑P α),   MvFunctor.map (TypeVec.comp 
g f) x = …
· 使用定理 `MvQPF.abs_map`：∀ {n : ℕ} {F : TypeVec.{u} n → Type u_1} [self : MvQPF F]
 {α β : TypeVec.{u} n} (f : α.Arrow β) (p : ↑(MvQPF.P F) α),   MvQPF.abs (MvFunc
tor…
· 使用定理 `MvQPF.abs_repr`：∀ {n : ℕ} {F : TypeVec.{u} n → Type u_1} [self : MvQPF F
] {α : TypeVec.{u} n} (x : F α), MvQPF.abs (MvQPF.repr x) = x
· 使用定理 `TypeVec.appendFun_comp`：appendFun_comp {α₀ α₁ α₂ : TypeVec n} {β₀ β₁ β₂ 
: Type*} (f₀ : α₀ ⟹ α₁) (f₁ : α₁ ⟹ α₂) (g₀ : β₀ -> β₁) (g₁ : β₁ -> β₂) : (f₁ ⊚ f
₀ ::: g₁ ∘ g…
· 使用定理 `TypeVec.id_comp`：id_comp (f : α ⟹ β) : id ⊚ f = f
-/
theorem Fix.rec_eq {β : Type u} (g : F (append1 α β) → β) (x : F (append1 α (Fix F α))) :
    Fix.rec g (Fix.mk x) = g (appendFun id (Fix.rec g) <$$> x) := by
  have : recF g ∘ fixToW = Fix.rec g := by
    apply funext
    apply Quotient.ind
    intro x
    apply recF_eq_of_wEquiv
    apply wrepr_equiv
  conv =>
    lhs
    rw [Fix.rec, Fix.mk]
    dsimp
  rcases h : repr x with ⟨a, f⟩
  rw [MvPFunctor.map_eq, recF_eq', ← MvPFunctor.map_eq, MvPFunctor.wDest'_wMk']
  rw [← MvPFunctor.comp_map, abs_map, ← h, abs_repr, ← appendFun_comp, id_comp, this]

set_option backward.isDefEq.respectTransparency false in
/-
**MvQPF.Fix.ind_aux** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF.Fix`。
形式化陈述：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQPF F] {α : TypeVec.{u
} n} (a : (MvQPF.P F).A)   (f' : ((MvQPF.P F).drop.B a).Arrow α) (f : (MvQPF.P F
).last.B a → (MvQPF.P F).W α),   MvQPF.Fix.mk (MvQPF.abs ⟨a, (MvQPF.P F).appendC
ontents f' fun x => ⟦f x⟧⟩) = ⟦(MvQPF.P F).wMk a f' f⟧
参数：n + 1；a : (MvQPF.P F).A；f' : ((MvQPF.P F).drop.B a).Arrow α；f : (MvQPF.P F).l
ast.B a → (MvQPF.P F).W α；MvQPF.abs ⟨a, (MvQPF.P F).appendContents f' fun x => ⟦
f x⟧⟩；MvQPF.P F。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MvQPF.wEquiv.abs'`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQ
PF F] {α : TypeVec.{u} n} (x y : (MvQPF.P F).W α),   MvQPF.abs ((MvQPF.P F).wDes
t' x) =…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MvPFunctor.wDest'_wMk'`：∀ {n : ℕ} (P : MvPFunctor.{u} (n + 1)) {α : Type
Vec.{u} n} (x : ↑P (α ::: P.W α)), P.wDest' (P.wMk' x) = x
· 使用定理 `MvQPF.abs_map`：∀ {n : ℕ} {F : TypeVec.{u} n → Type u_1} [self : MvQPF F]
 {α β : TypeVec.{u} n} (f : α.Arrow β) (p : ↑(MvQPF.P F) α),   MvQPF.abs (MvFunc
tor…
· 使用定理 `MvQPF.abs_repr`：∀ {n : ℕ} {F : TypeVec.{u} n → Type u_1} [self : MvQPF F
] {α : TypeVec.{u} n} (x : F α), MvQPF.abs (MvQPF.repr x) = x
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MvPFunctor.map_eq`：map_eq {α β : TypeVec n} (g : α ⟹ β) (a : P.A) (f : P
.B a ⟹ α) : @MvFunctor.map _ P.Obj _ _ _ g ⟨a, f⟩ = ⟨a, g ⊚ f⟩
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `MvQPF.wrepr_wMk`：wrepr_wMk {α : TypeVec n} (a : q.P.A) (f' : q.P.drop.B 
a ⟹ α) (f : q.P.last.B a -> q.P.W α) : wrepr (q.P.wMk a f' f) = q.P.wMk' (repr (
abs (…
· 使用定理 `MvPFunctor.appendContents.eq_1`：∀ {n : ℕ} (P : MvPFunctor.{u} (n + 1)) {
α : TypeVec.{u_1} n} {β : Type u_1} {a : P.A} (f' : (P.drop.B a).Arrow α)   (f :
 P.last.B a → β), P.…
· 使用定理 `TypeVec.appendFun.eq_1`：∀ {n : ℕ} {α : TypeVec.{u_1} n} {α' : TypeVec.{u
_2} n} {β : Type u_1} {β' : Type u_2} (f : α.Arrow α') (g : β → β'),   (f ::: g)
 = TypeVec.s…
· 使用定理 `TypeVec.splitFun_comp`：splitFun_comp {α₀ α₁ α₂ : TypeVec (n + 1)} (f₀ : 
drop α₀ ⟹ drop α₁) (f₁ : drop α₁ ⟹ drop α₂) (g₀ : last α₀ -> last α₁) (g₁ : last
 α₁ -> last…
· 使用定理 `MvQPF.wrepr_equiv`：wrepr_equiv {α : TypeVec n} (x : q.P.W α) : WEquiv (w
repr x) x
-/
theorem Fix.ind_aux (a : q.P.A) (f' : q.P.drop.B a ⟹ α) (f : q.P.last.B a → q.P.W α) :
    Fix.mk (abs ⟨a, q.P.appendContents f' fun x => ⟦f x⟧⟩) = ⟦q.P.wMk a f' f⟧ := by
  have : Fix.mk (abs ⟨a, q.P.appendContents f' fun x => ⟦f x⟧⟩) = ⟦wrepr (q.P.wMk a f' f)⟧ := by
    apply Quot.sound; apply wEquiv.abs'
    rw [MvPFunctor.wDest'_wMk', abs_map, abs_repr, ← abs_map, MvPFunctor.map_eq]
    conv =>
      rhs
      rw [wrepr_wMk, q.P.wDest'_wMk', abs_repr, MvPFunctor.map_eq]
    congr 2; rw [MvPFunctor.appendContents, MvPFunctor.appendContents]
    rw [appendFun, appendFun, ← splitFun_comp, ← splitFun_comp]
    rfl
  rw [this]
  apply Quot.sound
  apply wrepr_equiv

set_option backward.isDefEq.respectTransparency false in
/-
**MvQPF.Fix.ind_rec** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF.Fix`。
形式化陈述：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQPF F] {α : TypeVec.{u
} n} {β : Type u} (g₁ g₂ : MvQPF.Fix F α → β),   (∀ (x : F (α ::: MvQPF.Fix F α)
),       MvFunctor.map (TypeVec.id ::: g₁) x = MvFunctor.map (TypeVec.id ::: g₂)
 x →         g₁ (MvQPF.Fix.mk x) = g₂ (MvQPF.Fix.mk x)) →     ∀ (x : MvQPF.Fix F
 α), g₁ x = g₂ x
参数：n + 1；g₁ g₂ : MvQPF.Fix F α → β；∀ (x : F (α ::: MvQPF.Fix F α)),       MvFunc
tor.map (TypeVec.id ::: g₁) x = MvFunctor.map (TypeVec.id ::: g₂) x →         g₁
 (MvQPF.Fix.mk x) = g₂ (MvQPF.Fix.mk x)；x : MvQPF.Fix F α。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MvQPF.Fix.ind_aux`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQ
PF F] {α : TypeVec.{u} n} (a : (MvQPF.P F).A)   (f' : ((MvQPF.P F).drop.B a).Arr
ow α) (…
· 使用定理 `MvQPF.abs_map`：∀ {n : ℕ} {F : TypeVec.{u} n → Type u_1} [self : MvQPF F]
 {α β : TypeVec.{u} n} (f : α.Arrow β) (p : ↑(MvQPF.P F) α),   MvQPF.abs (MvFunc
tor…
· 使用定理 `MvPFunctor.map_eq`：map_eq {α β : TypeVec n} (g : α ⟹ β) (a : P.A) (f : P
.B a ⟹ α) : @MvFunctor.map _ P.Obj _ _ _ g ⟨a, f⟩ = ⟨a, g ⊚ f⟩
· 使用定理 `MvPFunctor.appendContents.eq_1`：∀ {n : ℕ} (P : MvPFunctor.{u} (n + 1)) {
α : TypeVec.{u_1} n} {β : Type u_1} {a : P.A} (f' : (P.drop.B a).Arrow α)   (f :
 P.last.B a → β), P.…
· 使用定理 `TypeVec.appendFun.eq_1`：∀ {n : ℕ} {α : TypeVec.{u_1} n} {α' : TypeVec.{u
_2} n} {β : Type u_1} {β' : Type u_2} (f : α.Arrow α') (g : β → β'),   (f ::: g)
 = TypeVec.s…
· 使用定理 `TypeVec.splitFun_comp`：splitFun_comp {α₀ α₁ α₂ : TypeVec (n + 1)} (f₀ : 
drop α₀ ⟹ drop α₁) (f₁ : drop α₁ ⟹ drop α₂) (g₀ : last α₀ -> last α₁) (g₁ : last
 α₁ -> last…
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
-/
theorem Fix.ind_rec {β : Type u} (g₁ g₂ : Fix F α → β)
    (h :
      ∀ x : F (append1 α (Fix F α)),
        appendFun id g₁ <$$> x = appendFun id g₂ <$$> x → g₁ (Fix.mk x) = g₂ (Fix.mk x)) :
    ∀ x, g₁ x = g₂ x := by
  apply Quot.ind
  intro x
  induction x using q.P.wInd
  case ih a f' f ih =>
    change g₁ ⟦q.P.wMk a f' f⟧ = g₂ ⟦q.P.wMk a f' f⟧
    rw [← Fix.ind_aux a f' f]
    apply h
    rw [← abs_map, ← abs_map, MvPFunctor.map_eq, MvPFunctor.map_eq]
    congr 2
    rw [MvPFunctor.appendContents, appendFun, appendFun, ← splitFun_comp, ← splitFun_comp]
    have : (g₁ ∘ fun x => ⟦f x⟧) = g₂ ∘ fun x => ⟦f x⟧ := by
      ext x
      exact ih x
    rw [this]
/-
**MvQPF.Fix.rec_unique** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF.Fix`。
形式化陈述：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQPF F] {α : TypeVec.{u
} n} {β : Type u} (g : F (α ::: β) → β)   (h : MvQPF.Fix F α → β),   (∀ (x : F (
α ::: MvQPF.Fix F α)), h (MvQPF.Fix.mk x) = g (MvFunctor.map (TypeVec.id ::: h) 
x)) → MvQPF.Fix.rec g = h
参数：n + 1；g : F (α ::: β) → β；h : MvQPF.Fix F α → β；∀ (x : F (α ::: MvQPF.Fix F α
)), h (MvQPF.Fix.mk x) = g (MvFunctor.map (TypeVec.id ::: h) x)。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `MvQPF.Fix.ind_rec`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQ
PF F] {α : TypeVec.{u} n} {β : Type u} (g₁ g₂ : MvQPF.Fix F α → β),   (∀ (x : F 
(α ::: …
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MvQPF.Fix.rec_eq`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQP
F F] {α : TypeVec.{u} n} {β : Type u} (g : F (α ::: β) → β)   (x : F (α ::: MvQP
F.Fix …
-/
theorem Fix.rec_unique {β : Type u} (g : F (append1 α β) → β) (h : Fix F α → β)
    (hyp : ∀ x, h (Fix.mk x) = g (appendFun id h <$$> x)) : Fix.rec g = h := by
  ext x
  apply Fix.ind_rec
  intro x hyp'
  rw [hyp, ← hyp', Fix.rec_eq]
/-
**MvQPF.Fix.mk_dest** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF.Fix`。
形式化陈述：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQPF F] {α : TypeVec.{u
} n} (x : MvQPF.Fix F α),   MvQPF.Fix.mk x.dest = x
参数：n + 1；x : MvQPF.Fix F α。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MvQPF.Fix.ind_rec`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQ
PF F] {α : TypeVec.{u} n} {β : Type u} (g₁ g₂ : MvQPF.Fix F α → β),   (∀ (x : F 
(α ::: …
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MvQPF.Fix.dest.eq_1`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : M
vQPF F] {α : TypeVec.{u} n},   MvQPF.Fix.dest = MvQPF.Fix.rec (MvFunctor.map (Ty
peVec.id …
· 使用定理 `MvQPF.Fix.rec_eq`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQP
F F] {α : TypeVec.{u} n} {β : Type u} (g : F (α ::: β) → β)   (x : F (α ::: MvQP
F.Fix …
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MvQPF.comp_map`：comp_map {α β γ : TypeVec n} (f : α ⟹ β) (g : β ⟹ γ) (x 
: F α) : (g ⊚ f) < > x = g < > f < > x
· 使用定理 `TypeVec.appendFun_comp`：appendFun_comp {α₀ α₁ α₂ : TypeVec n} {β₀ β₁ β₂ 
: Type*} (f₀ : α₀ ⟹ α₁) (f₁ : α₁ ⟹ α₂) (g₀ : β₀ -> β₁) (g₁ : β₁ -> β₂) : (f₁ ⊚ f
₀ ::: g₁ ∘ g…
· 使用定理 `TypeVec.id_comp`：id_comp (f : α ⟹ β) : id ⊚ f = f
· 使用定理 `TypeVec.appendFun_id_id`：appendFun_id_id {α : TypeVec n} {β : Type*} : (
@TypeVec.id n α ::: @_root_.id β) = TypeVec.id
· 使用定理 `MvFunctor.id_map`：id_map (x : F α) : TypeVec.id < > x = x
· 使用定理 `MvQPF.lawfulMvFunctor`：∀ {n : ℕ} {F : TypeVec.{u} n → Type u_1} [q : MvQ
PF F], LawfulMvFunctor F
-/
theorem Fix.mk_dest (x : Fix F α) : Fix.mk (Fix.dest x) = x := by
  change (Fix.mk ∘ Fix.dest) x = x
  apply Fix.ind_rec
  intro x; dsimp
  rw [Fix.dest, Fix.rec_eq, ← comp_map, ← appendFun_comp, id_comp]
  intro h; rw [h]
  change Fix.mk (appendFun id id <$$> x) = Fix.mk x
  rw [appendFun_id_id, MvFunctor.id_map]
/-
**MvQPF.Fix.dest_mk** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF.Fix`。
形式化陈述：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQPF F] {α : TypeVec.{u
} n} (x : F (α ::: MvQPF.Fix F α)),   (MvQPF.Fix.mk x).dest = x
参数：n + 1；x : F (α ::: MvQPF.Fix F α)；MvQPF.Fix.mk x。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MvQPF.Fix.rec_eq`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQP
F F] {α : TypeVec.{u} n} {β : Type u} (g : F (α ::: β) → β)   (x : F (α ::: MvQP
F.Fix …
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MvQPF.Fix.dest.eq_1`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : M
vQPF F] {α : TypeVec.{u} n},   MvQPF.Fix.dest = MvQPF.Fix.rec (MvFunctor.map (Ty
peVec.id …
· 使用定理 `MvQPF.comp_map`：comp_map {α β γ : TypeVec n} (f : α ⟹ β) (g : β ⟹ γ) (x 
: F α) : (g ⊚ f) < > x = g < > f < > x
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `MvFunctor.id_map`：id_map (x : F α) : TypeVec.id < > x = x
· 使用定理 `MvQPF.lawfulMvFunctor`：∀ {n : ℕ} {F : TypeVec.{u} n → Type u_1} [q : MvQ
PF F], LawfulMvFunctor F
· 使用定理 `TypeVec.appendFun_comp`：appendFun_comp {α₀ α₁ α₂ : TypeVec n} {β₀ β₁ β₂ 
: Type*} (f₀ : α₀ ⟹ α₁) (f₁ : α₁ ⟹ α₂) (g₀ : β₀ -> β₁) (g₁ : β₁ -> β₂) : (f₁ ⊚ f
₀ ::: g₁ ∘ g…
· 使用定理 `TypeVec.id_comp`：id_comp (f : α ⟹ β) : id ⊚ f = f
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `MvQPF.Fix.mk_dest`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQ
PF F] {α : TypeVec.{u} n} (x : MvQPF.Fix F α),   MvQPF.Fix.mk x.dest = x
· 使用定理 `TypeVec.appendFun_id_id`：appendFun_id_id {α : TypeVec n} {β : Type*} : (
@TypeVec.id n α ::: @_root_.id β) = TypeVec.id
-/
theorem Fix.dest_mk (x : F (append1 α (Fix F α))) : Fix.dest (Fix.mk x) = x := by
  unfold Fix.dest
  rw [Fix.rec_eq, ← Fix.dest, ← comp_map]
  conv =>
    rhs
    rw [← MvFunctor.id_map x]
  rw [← appendFun_comp, id_comp]
  have : Fix.mk ∘ Fix.dest (F := F) (α := α) = _root_.id := by
    ext (x : Fix F α)
    apply Fix.mk_dest
  rw [this, appendFun_id_id]
/-
**MvQPF.Fix.ind** 是 Mathlib 中的一个定理，位于命名空间 `MvQPF.Fix`。
形式化陈述：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQPF F] {α : TypeVec.{u
} n} (p : MvQPF.Fix F α → Prop),   (∀ (x : F (α ::: MvQPF.Fix F α)), MvFunctor.L
iftP (α.PredLast p) x → p (MvQPF.Fix.mk x)) → ∀ (x : MvQPF.Fix F α), p x
参数：n + 1；p : MvQPF.Fix F α → Prop；∀ (x : F (α ::: MvQPF.Fix F α)), MvFunctor.Lif
tP (α.PredLast p) x → p (MvQPF.Fix.mk x)；x : MvQPF.Fix F α。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MvQPF.Fix.ind_aux`：∀ {n : ℕ} {F : TypeVec.{u} (n + 1) → Type u} [q : MvQ
PF F] {α : TypeVec.{u} n} (a : (MvQPF.P F).A)   (f' : ((MvQPF.P F).drop.B a).Arr
ow α) (…
· 使用定理 `MvQPF.liftP_iff`：liftP_iff {α : TypeVec n} (p : forall ⦃i⦄, α i -> Prop)
 (x : F α) : LiftP p x ↔ exists a f, x = abs ⟨a, f⟩ ∧ forall i j, p (f i j)
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
-/
theorem Fix.ind {α : TypeVec n} (p : Fix F α → Prop)
    (h : ∀ x : F (α.append1 (Fix F α)), LiftP (PredLast α p) x → p (Fix.mk x)) : ∀ x, p x := by
  apply Quot.ind
  intro x
  induction x using q.P.wInd
  case ih a f' f ih =>
    change p ⟦q.P.wMk a f' f⟧
    rw [← Fix.ind_aux a f' f]
    apply h
    rw [MvQPF.liftP_iff]
    refine ⟨_, _, rfl, ?_⟩
    intro i j
    cases i
    · apply ih
    · trivial
/-
**MvQPF.mvqpfFix** 是 Mathlib 中的一个实例，位于命名空间 `MvQPF`。
形式化陈述：mvqpfFix : MvQPF (Fix F) where P
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance mvqpfFix : MvQPF (Fix F) where
  P := q.P.wp
  abs α := Quot.mk WEquiv α
  repr α := fixToW α
  abs_repr := by
    intro α
    apply Quot.ind
    intro a
    apply Quot.sound
    apply wrepr_equiv
  abs_map := by
    intro α β g x
    conv =>
      rhs
      dsimp [MvFunctor.map]
    rfl

/-- Dependent recursor for `fix F` -/
/-
**MvQPF.Fix.drec** 是 Mathlib 中的一个定义，位于命名空间 `MvQPF.Fix`。
形式化陈述：{n : ℕ} →   {F : TypeVec.{u} (n + 1) → Type u} →     [q : MvQPF F] →      
 {α : TypeVec.{u} n} →         {β : MvQPF.Fix F α → Type u} →           ((x : F 
(α ::: Sigma β)) → β (MvQPF.Fix.mk (MvFunctor.map (TypeVec.id ::: Sigma.fst) x))
) →             (x : MvQPF.Fix F α) → β x
参数：n + 1；(x : F (α ::: Sigma β)) → β (MvQPF.Fix.mk (MvFunctor.map (TypeVec.id ::
: Sigma.fst) x))；x : MvQPF.Fix F α。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Dependent recursor for `fix F`
-/
def Fix.drec {β : Fix F α → Type u}
    (g : ∀ x : F (α ::: Sigma β), β (Fix.mk <| (id ::: Sigma.fst) <$$> x)) (x : Fix F α) : β x :=
  let y := @Fix.rec _ F _ α (Sigma β) (fun i => ⟨_, g i⟩) x
  have : x = y.1 := by
    symm
    dsimp [y]
    apply Fix.ind_rec _ id _ x
    intro x' ih
    rw [Fix.rec_eq]
    dsimp
    simp only [appendFun_id_id, MvFunctor.id_map] at ih
    congr
    conv =>
      rhs
      rw [← ih]
    rw [MvFunctor.map_map, ← appendFun_comp, id_comp]
    simp only [Function.comp_def]
  cast (by rw [this]) y.2

end MvQPF

