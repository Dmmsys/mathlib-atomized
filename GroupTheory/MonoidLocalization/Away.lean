/-
Copyright (c) 2019 Amelia Livingston. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Amelia Livingston
-/
module

public import Mathlib.Algebra.Group.Submonoid.Membership
public import Mathlib.GroupTheory.MonoidLocalization.Maps

/-!
# Localizing commutative monoids away from an element

We treat the special case of localizing away from an element in the sections
`AwayMap` and `Away`.

## Tags
localization, monoid localization, quotient monoid, congruence relation, characteristic predicate,
commutative monoid, grothendieck group
-/

@[expose] public section

assert_not_exists MonoidWithZero

open Function

section CommMonoid

variable {M : Type*} [CommMonoid M] {S : Submonoid M} {N : Type*} [CommMonoid N] {P : Type*}
  [CommMonoid P]

namespace Submonoid

namespace LocalizationMap

section AwayMap

variable {g : M →* P} (hg : ∀ y : S, IsUnit (g y))
variable (x : M)

/-- Given `x : M`, the type of `CommMonoid` homomorphisms `f : M →* N` such that `N`
is isomorphic to the Localization of `M` at the Submonoid generated by `x`. -/
@[to_additive
/-- Given `x : M`, the type of `AddCommMonoid` homomorphisms `f : M →+ N` such that `N` is
isomorphic to the localization of `M` at the AddSubmonoid generated by `x`. -/]
/-
**Submonoid.LocalizationMap.AwayMap** 是 Mathlib 中的一个缩写定义，位于命名空间 `Submonoid.Local
izationMap`。
形式化陈述：AwayMap (N' : Type*) [CommMonoid N']
参数：N' : Type*。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
abbrev AwayMap (N' : Type*) [CommMonoid N'] := LocalizationMap (powers x) N'

variable (F : AwayMap x N)

/-- Given `x : M` and a Localization map `F : M →* N` away from `x`, `invSelf` is `(F x)⁻¹`. -/
/-
**Submonoid.LocalizationMap.AwayMap.invSelf** 是 Mathlib 中的一个定义，位于命名空间 `Submonoid
.LocalizationMap.AwayMap`。
形式化陈述：{M : Type u_1} →   [inst : CommMonoid M] → {N : Type u_2} → [inst_1 : Comm
Monoid N] → (x : M) → Submonoid.LocalizationMap.AwayMap x N → N
参数：x : M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Given `x : M` and a Localization map `F : M →* N` away from `x`, `invSelf` is `(
F x)⁻¹`.
-/
noncomputable def AwayMap.invSelf : N := F.mk' 1 ⟨x, mem_powers _⟩

/-- Given `x : M`, a Localization map `F : M →* N` away from `x`, and a map of `CommMonoid`s
`g : M →* P` such that `g x` is invertible, the homomorphism induced from `N` to `P` sending
`z : N` to `g y * (g x)⁻ⁿ`, where `y : M, n : ℕ` are such that `z = F y * (F x)⁻ⁿ`. -/
/-
**Submonoid.LocalizationMap.AwayMap.lift** 是 Mathlib 中的一个定义，位于命名空间 `Submonoid.Lo
calizationMap.AwayMap`。
形式化陈述：{M : Type u_1} →   [inst : CommMonoid M] →     {N : Type u_2} →       [ins
t_1 : CommMonoid N] →         {P : Type u_3} →           [inst_2 : CommMonoid P]
 →             {g : M →* P} → (x : M) → Submonoid.LocalizationMap.AwayMap x N → 
IsUnit (g x) → N →* P
参数：x : M；g x。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Given `x : M`, a Localization map `F : M →* N` away from `x`, and a map of `Comm
Monoid`s
`g : M →* P` such that `g x` is invertible, the homomorphism induced from `N` to
 `P` sending
`z : N` to `g y * (g x)⁻ⁿ`, where `y : M, n : ℕ` are such that `z = F y * (F x)⁻
ⁿ`.
-/
noncomputable def AwayMap.lift (hg : IsUnit (g x)) : N →* P :=
  Submonoid.LocalizationMap.lift F fun y ↦
    show IsUnit (g y.1) by
      obtain ⟨n, hn⟩ := y.2
      rw [← hn, g.map_pow]
      exact IsUnit.pow n hg

@[simp]
/-
**Submonoid.LocalizationMap.AwayMap.lift_eq** 是 Mathlib 中的一个定理，位于命名空间 `Submonoid
.LocalizationMap.AwayMap`。
形式化陈述：∀ {M : Type u_1} [inst : CommMonoid M] {N : Type u_2} [inst_1 : CommMonoid
 N] {P : Type u_3} [inst_2 : CommMonoid P]   {g : M →* P} (x : M) (F : Submonoid
.LocalizationMap.AwayMap x N) (hg : IsUnit (g x)) (a : M),   (Submonoid.Localiza
tionMap.AwayMap.lift x F hg) (F a) = g a
参数：x : M；F : Submonoid.LocalizationMap.AwayMap x N；hg : IsUnit (g x)；a : M；Submo
noid.LocalizationMap.AwayMap.lift x F hg；F a。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submonoid.LocalizationMap.lift_eq`：lift_eq (x : M) : f.lift hg (f x) = g
 x
-/
theorem AwayMap.lift_eq (hg : IsUnit (g x)) (a : M) : F.lift x hg (F a) = g a :=
  Submonoid.LocalizationMap.lift_eq _ _ _

@[simp]
/-
**Submonoid.LocalizationMap.AwayMap.lift_comp** 是 Mathlib 中的一个定理，位于命名空间 `Submono
id.LocalizationMap.AwayMap`。
形式化陈述：∀ {M : Type u_1} [inst : CommMonoid M] {N : Type u_2} [inst_1 : CommMonoid
 N] {P : Type u_3} [inst_2 : CommMonoid P]   {g : M →* P} (x : M) (F : Submonoid
.LocalizationMap.AwayMap x N) (hg : IsUnit (g x)),   (Submonoid.LocalizationMap.
AwayMap.lift x F hg).comp ↑F = g
参数：x : M；F : Submonoid.LocalizationMap.AwayMap x N；hg : IsUnit (g x)；Submonoid.L
ocalizationMap.AwayMap.lift x F hg。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submonoid.LocalizationMap.lift_comp`：lift_comp : (f.lift hg).comp f.toMo
noidHom = g
-/
theorem AwayMap.lift_comp (hg : IsUnit (g x)) : (F.lift x hg).comp F = g :=
  Submonoid.LocalizationMap.lift_comp _ _

/-- Given `x y : M` and Localization maps `F : M →* N, G : M →* P` away from `x` and `x * y`
respectively, the homomorphism induced from `N` to `P`. -/
/-
**Submonoid.LocalizationMap.awayToAwayRight** 是 Mathlib 中的一个定义，位于命名空间 `Submonoid
.LocalizationMap`。
形式化陈述：awayToAwayRight (y : M) (G : AwayMap (x * y) P) : N ->* P
参数：y : M；G : AwayMap (x * y) P。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Given `x y : M` and Localization maps `F : M →* N, G : M →* P` away from `x` and
 `x * y`
respectively, the homomorphism induced from `N` to `P`.
-/
noncomputable def awayToAwayRight (y : M) (G : AwayMap (x * y) P) : N →* P :=
  F.lift x <|
    show IsUnit (G x) from
      .of_mul_eq_one (G.mk' y ⟨x * y, mem_powers _⟩) <| by
        rw [mul_mk'_eq_mk'_of_mul, mk'_self]

end AwayMap

end LocalizationMap

end Submonoid

namespace AddSubmonoid

namespace LocalizationMap

section AwayMap

variable {A : Type*} [AddCommMonoid A] (x : A) {B : Type*} [AddCommMonoid B] (F : AwayMap x B)
  {C : Type*} [AddCommMonoid C] {g : A →+ C}

/-- Given `x : A` and a Localization map `F : A →+ B` away from `x`, `neg_self` is `- (F x)`. -/
/-
**AddSubmonoid.LocalizationMap.AwayMap.negSelf** 是 Mathlib 中的一个定义，位于命名空间 `AddSub
monoid.LocalizationMap.AwayMap`。
形式化陈述：{A : Type u_4} →   [inst : AddCommMonoid A] →     (x : A) → {B : Type u_5}
 → [inst_1 : AddCommMonoid B] → AddSubmonoid.LocalizationMap.AwayMap x B → B
参数：x : A。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Given `x : A` and a Localization map `F : A →+ B` away from `x`, `neg_self` is `
- (F x)`.
-/
noncomputable def AwayMap.negSelf : B :=
  F.mk' 0 ⟨x, mem_multiples _⟩

/-- Given `x : A`, a localization map `F : A →+ B` away from `x`, and a map of `AddCommMonoid`s
`g : A →+ C` such that `g x` is invertible, the homomorphism induced from `B` to `C` sending
`z : B` to `g y - n • g x`, where `y : A, n : ℕ` are such that `z = F y - n • F x`. -/
/-
**AddSubmonoid.LocalizationMap.AwayMap.lift** 是 Mathlib 中的一个定义，位于命名空间 `AddSubmon
oid.LocalizationMap.AwayMap`。
形式化陈述：{A : Type u_4} →   [inst : AddCommMonoid A] →     (x : A) →       {B : Typ
e u_5} →         [inst_1 : AddCommMonoid B] →           AddSubmonoid.Localizatio
nMap.AwayMap x B →             {C : Type u_6} → [inst_2 : AddCommMonoid C] → {g 
: A →+ C} → IsAddUnit (g x) → B →+ C
参数：x : A；g x。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Given `x : A`, a localization map `F : A →+ B` away from `x`, and a map of `AddC
ommMonoid`s
`g : A →+ C` such that `g x` is invertible, the homomorphism induced from `B` to
 `C` sending
`z : B` to `g y - n • g x`, where `y : A, n : ℕ` are such that `z = F y - n • F 
x`.
-/
noncomputable def AwayMap.lift (hg : IsAddUnit (g x)) : B →+ C :=
  AddSubmonoid.LocalizationMap.lift F fun y ↦
    show IsAddUnit (g y.1) by
      obtain ⟨n, hn⟩ := y.2
      rw [← hn]
      dsimp
      rw [g.map_nsmul]
      exact IsAddUnit.map (nsmulAddMonoidHom n : C →+ C) hg

@[simp]
/-
**AddSubmonoid.LocalizationMap.AwayMap.lift_eq** 是 Mathlib 中的一个定理，位于命名空间 `AddSub
monoid.LocalizationMap.AwayMap`。
形式化陈述：∀ {A : Type u_4} [inst : AddCommMonoid A] (x : A) {B : Type u_5} [inst_1 :
 AddCommMonoid B]   (F : AddSubmonoid.LocalizationMap.AwayMap x B) {C : Type u_6
} [inst_2 : AddCommMonoid C] {g : A →+ C}   (hg : IsAddUnit (g x)) (a : A), (Add
Submonoid.LocalizationMap.AwayMap.lift x F hg) (F a) = g a
参数：x : A；F : AddSubmonoid.LocalizationMap.AwayMap x B；hg : IsAddUnit (g x)；a : A
；AddSubmonoid.LocalizationMap.AwayMap.lift x F hg；F a。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AddSubmonoid.LocalizationMap.lift_eq`：∀ {M : Type u_1} [inst : AddCommMo
noid M] {S : AddSubmonoid M} {N : Type u_2} [inst_1 : AddCommMonoid N] {P : Type
 u_3}   [inst_2 : AddCommM…
-/
theorem AwayMap.lift_eq (hg : IsAddUnit (g x)) (a : A) : F.lift x hg (F a) = g a :=
  AddSubmonoid.LocalizationMap.lift_eq _ _ _

@[simp]
/-
**AddSubmonoid.LocalizationMap.AwayMap.lift_comp** 是 Mathlib 中的一个定理，位于命名空间 `AddS
ubmonoid.LocalizationMap.AwayMap`。
形式化陈述：∀ {A : Type u_4} [inst : AddCommMonoid A] (x : A) {B : Type u_5} [inst_1 :
 AddCommMonoid B]   (F : AddSubmonoid.LocalizationMap.AwayMap x B) {C : Type u_6
} [inst_2 : AddCommMonoid C] {g : A →+ C}   (hg : IsAddUnit (g x)), (AddSubmonoi
d.LocalizationMap.AwayMap.lift x F hg).comp ↑F = g
参数：x : A；F : AddSubmonoid.LocalizationMap.AwayMap x B；hg : IsAddUnit (g x)；AddSu
bmonoid.LocalizationMap.AwayMap.lift x F hg。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AddSubmonoid.LocalizationMap.lift_comp`：∀ {M : Type u_1} [inst : AddComm
Monoid M] {S : AddSubmonoid M} {N : Type u_2} [inst_1 : AddCommMonoid N] {P : Ty
pe u_3}   [inst_2 : AddCommM…
-/
theorem AwayMap.lift_comp (hg : IsAddUnit (g x)) : (F.lift x hg).comp F = g :=
  AddSubmonoid.LocalizationMap.lift_comp _ _

/-- Given `x y : A` and Localization maps `F : A →+ B, G : A →+ C` away from `x` and `x + y`
respectively, the homomorphism induced from `B` to `C`. -/
/-
**AddSubmonoid.LocalizationMap.awayToAwayRight** 是 Mathlib 中的一个定义，位于命名空间 `AddSub
monoid.LocalizationMap`。
形式化陈述：awayToAwayRight (y : A) (G : AwayMap (x + y) C) : B ->+ C
参数：y : A；G : AwayMap (x + y) C。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Given `x y : A` and Localization maps `F : A →+ B, G : A →+ C` away from `x` and
 `x + y`
respectively, the homomorphism induced from `B` to `C`.
-/
noncomputable def awayToAwayRight (y : A) (G : AwayMap (x + y) C) : B →+ C :=
  F.lift x <|
    show IsAddUnit (G x) from
      .of_add_eq_zero (G.mk' y ⟨x + y, mem_multiples _⟩) <| by
        rw [add_mk'_eq_mk'_of_add, mk'_self]

end AwayMap

end LocalizationMap

end AddSubmonoid

namespace Localization

section Away

variable (x : M)

/-- Given `x : M`, the Localization of `M` at the Submonoid generated by `x`, as a quotient. -/
@[to_additive
/-- Given `x : M`, the Localization of `M` at the Submonoid generated by `x`, as a quotient. -/]
/-
**Localization.Away** 是 Mathlib 中的一个缩写定义，位于命名空间 `Localization`。
形式化陈述：Away
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
abbrev Away :=
  Localization (Submonoid.powers x)

/-- Given `x : M`, `invSelf` is `x⁻¹` in the Localization (as a quotient type) of `M` at the
Submonoid generated by `x`. -/
@[to_additive
/-- Given `x : M`, `negSelf` is `-x` in the Localization (as a quotient type) of `M` at the
Submonoid generated by `x`. -/]
/-
**Localization.Away.invSelf** 是 Mathlib 中的一个定义，位于命名空间 `Localization.Away`。
形式化陈述：{M : Type u_1} → [inst : CommMonoid M] → (x : M) → Localization.Away x
参数：x : M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
def Away.invSelf : Away x :=
  mk 1 ⟨x, Submonoid.mem_powers _⟩

/-- Given `x : M`, the natural hom sending `y : M`, `M` a `CommMonoid`, to the equivalence class
of `(y, 1)` in the Localization of `M` at the Submonoid generated by `x`. -/
@[to_additive
/-- Given `x : M`, the natural hom sending `y : M`, `M` an `AddCommMonoid`, to the equivalence
/-
**Localization.of** 是 Mathlib 中的一个类，位于命名空间 `Localization`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
class of `(y, 0)` in the Localization of `M` at the Submonoid generated by `x`. -/]
/-
**Localization.Away.monoidOf** 是 Mathlib 中的一个定义，位于命名空间 `Localization.Away`。
形式化陈述：{M : Type u_1} → [inst : CommMonoid M] → (x : M) → Submonoid.LocalizationM
ap.AwayMap x (Localization.Away x)
参数：x : M；Localization.Away x。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
abbrev Away.monoidOf : Submonoid.LocalizationMap.AwayMap x (Away x) :=
  Localization.monoidOf (Submonoid.powers x)

@[to_additive]
/-
**Localization.Away.mk_eq_monoidOf_mk'** 是 Mathlib 中的一个定理，位于命名空间 `Localization.A
way`。
形式化陈述：∀ {M : Type u_1} [inst : CommMonoid M] (x : M),   Localization.mk = Submon
oid.LocalizationMap.mk' (Localization.Away.monoidOf x)
参数：x : M；Localization.Away.monoidOf x。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Localization.mk_eq_monoidOf_mk'`：mk_eq_monoidOf_mk'_apply (x y) : mk x y
 = (monoidOf S).mk' x y
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem Away.mk_eq_monoidOf_mk' : mk = (Away.monoidOf x).mk' := by
  simp [Localization.mk_eq_monoidOf_mk']

/-- Given `x : M` and a Localization map `f : M →* N` away from `x`, we get an isomorphism between
the Localization of `M` at the Submonoid generated by `x` as a quotient type and `N`. -/
@[to_additive
/-- Given `x : M` and a Localization map `f : M →+ N` away from `x`, we get an isomorphism between
the Localization of `M` at the Submonoid generated by `x` as a quotient type and `N`. -/]
/-
**Localization.Away.mulEquivOfQuotient** 是 Mathlib 中的一个定义，位于命名空间 `Localization.A
way`。
形式化陈述：{M : Type u_1} →   [inst : CommMonoid M] →     {N : Type u_2} →       [ins
t_1 : CommMonoid N] → (x : M) → Submonoid.LocalizationMap.AwayMap x N → Localiza
tion.Away x ≃* N
参数：x : M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
noncomputable def Away.mulEquivOfQuotient (f : Submonoid.LocalizationMap.AwayMap x N) :
    Away x ≃* N :=
  Localization.mulEquivOfQuotient f

end Away

end Localization

end CommMonoid

