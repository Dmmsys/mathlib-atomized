/-
Copyright (c) 2017 Johannes Hölzl. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Johannes Hölzl, Yury Kudryashov, Kim Morrison
-/
module

public import Mathlib.Algebra.GroupWithZero.Action.TransferInstance
public import Mathlib.Algebra.Module.Defs
public import Mathlib.Data.Finsupp.Basic
public import Mathlib.Data.Finsupp.SMulWithZero

/-!
# Monoid algebras

When the domain of a `Finsupp` has a multiplicative or additive structure, we can define
a convolution product. To mathematicians this structure is known as the "monoid algebra",
i.e. the finite formal linear combinations over a given semiring of elements of a monoid `M`.
The "group ring" `ℤ[G]` or the "group algebra" `k[G]` are typical uses.

In fact the construction of the "monoid algebra" makes sense when `M` is not even a monoid, but
merely a magma, i.e., when `M` carries a multiplication which is not required to satisfy any
conditions at all. In this case the construction yields a not-necessarily-unital,
not-necessarily-associative algebra but it is still adjoint to the forgetful functor from such
algebras to magmas, and we prove this as `MonoidAlgebra.liftMagma`.

In this file we define `MonoidAlgebra R M` and `AddMonoidAlgebra R M` as one-field structures around
`M →₀ R`, and then define the convolution product on these.

When the domain is additive, this is used to define polynomials:
```
Polynomial R := AddMonoidAlgebra R ℕ
MvPolynomial σ α := AddMonoidAlgebra R (σ →₀ ℕ)
```

When the domain is multiplicative, e.g. a group, this will be used to define the group ring.

## Notation

We introduce the notation `R[M]` for both `MonoidAlgebra R M` and `AddMonoidAlgebra R M`.
The notations are scoped to their respective namespaces, and which one `R[M]` resolves to therefore
depends on which of the two namespaces is open.

## TODO

Use `coeff`/`ofCoeff` more widely. See
https://github.com/leanprover-community/mathlib4/pull/36746
https://github.com/leanprover-community/mathlib4/pull/25273
-/


assert_not_exists NonUnitalAlgHom AlgEquiv

@[expose] public noncomputable section

open Finsupp hiding single

variable {R S G M N O ι : Type*}

/-- The additive monoid algebra over a semiring `R` generated by the additive monoid `M`.

It is the type of finite formal `R`-linear combinations of terms of `M`,
endowed with the convolution product. -/
/-
**AddMonoidAlgebra** 是 Mathlib 中的一个归纳类型，位于命名空间 ``。
形式化陈述：(R : Type u_8) → Type u_9 → [Semiring R] → Type (max u_8 u_9)
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The additive monoid algebra over a semiring `R` generated by the additive monoid
 `M`.

It is the type of finite formal `R`-linear combinations of terms of `M`,
endowed with the convolution product.
-/
structure AddMonoidAlgebra (R M : Type*) [Semiring R] where
  /-- Construct an element of the additive monoid algebra `R[M]`
  from its coefficients `M →₀ R`. -/
  ofCoeff ::
  /-- The coefficients `M →₀ R` of an element of the additive monoid algebra `R[M]`. -/
  coeff : M →₀ R

/-- The monoid algebra over a semiring `R` generated by the monoid `M`.

It is the type of finite formal `R`-linear combinations of terms of `M`,
endowed with the convolution product. -/
@[to_additive (relevant_arg := M), to_additive_dont_translate]
/-
**MonoidAlgebra** 是 Mathlib 中的一个归纳类型，位于命名空间 ``。
形式化陈述：(R : Type u_8) → Type u_9 → [Semiring R] → Type (max u_8 u_9)
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The monoid algebra over a semiring `R` generated by the monoid `M`.

It is the type of finite formal `R`-linear combinations of terms of `M`,
endowed with the convolution product.
-/
structure MonoidAlgebra (R M : Type*) [Semiring R] where
  /-- Construct an element of the monoid algebra `R[M]`
  from its coefficients `M →₀ R`. -/
  ofCoeff ::
  /-- The coefficients `M →₀ R` of an element of the monoid algebra `R[M]`. -/
  coeff : M →₀ R

initialize_simps_projections AddMonoidAlgebra (as_prefix coeff)
initialize_simps_projections MonoidAlgebra (as_prefix coeff)

namespace AddMonoidAlgebra

open Lean.PrettyPrinter Delaborator

@[inherit_doc AddMonoidAlgebra]
scoped syntax:max (priority := high) term noWs "[" term "]" : term

macro_rules | `($R[$M]) => `(AddMonoidAlgebra $R $M)

/-- Unexpander for `AddMonoidAlgebra`. -/
@[scoped app_unexpander AddMonoidAlgebra]
meta def unexpander : Unexpander
  | `($_ $R $M) => `($R[$M])
  | _ => throw ()

/-- This prevents `ofCoeff x` being printed as `{ coeff := x }` by `delabStructureInstance`. -/
@[app_delab ofCoeff] meta def delabOfCoeff : Delab := delabApp

end AddMonoidAlgebra

namespace MonoidAlgebra
section Notation

open Lean.PrettyPrinter Delaborator

/-- This prevents `ofCoeff x` being printed as `{ coeff := x }` by `delabStructureInstance`. -/
@[app_delab ofCoeff] meta def delabOfCoeff : Delab := delabApp

@[inherit_doc MonoidAlgebra]
scoped syntax:max (priority := high) term noWs "[" term "]" : term

macro_rules | `($R[$M]) => `(MonoidAlgebra $R $M)

/-- Unexpander for `MonoidAlgebra`. -/
@[scoped app_unexpander MonoidAlgebra]
meta def unexpander : Unexpander
  | `($_ $R $M) => `($R[$M])
  | _ => throw ()

end Notation

section Semiring
variable [Semiring R] {x y : R[M]} {r r₁ r₂ : R} {m m' m₁ m₂ m₁' m₂' : M}

/-
**MonoidAlgebra.coeff_ofCoeff** 是 Mathlib 中的一个定理，位于命名空间 `MonoidAlgebra`。
形式化陈述：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] (x : M →₀ R), (MonoidA
lgebra.ofCoeff x).coeff = x
参数：x : M →₀ R；MonoidAlgebra.ofCoeff x。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
@[to_additive] lemma coeff_ofCoeff (x : M →₀ R) : (ofCoeff x).coeff = x := rfl
/-
**MonoidAlgebra.ofCoeff_coeff** 是 Mathlib 中的一个定理，位于命名空间 `MonoidAlgebra`。
形式化陈述：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] (x : MonoidAlgebra R M
), MonoidAlgebra.ofCoeff x.coeff = x
参数：x : MonoidAlgebra R M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
@[to_additive] lemma ofCoeff_coeff (x : R[M]) : ofCoeff x.coeff = x := rfl

/-- `MonoidAlgebra.coeff` as an equiv. -/
@[to_additive (attr := simps apply symm_apply)
/-- `AddMonoidAlgebra.coeff` as an equiv. -/]
/-
**MonoidAlgebra.coeffEquiv** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeffEquiv : R[M] ≃ (M ->₀ R) where toFun
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
def coeffEquiv : R[M] ≃ (M →₀ R) where
  toFun := coeff
  invFun := ofCoeff
  left_inv _ := rfl
  right_inv _ := rfl
/-
**MonoidAlgebra.** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
@[to_additive] lemma «forall» {P : R[M] → Prop} : (∀ p, P p) ↔ ∀ q, P (ofCoeff q) :=
  coeffEquiv.forall_congr_left
/-
**MonoidAlgebra.** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
@[to_additive] lemma «exists» {P : R[M] → Prop} : (∃ p, P p) ↔ ∃ q, P (ofCoeff q) :=
  coeffEquiv.exists_congr_left

@[to_additive]
/-
**MonoidAlgebra.coeff_injective** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_injective : (coeff : R[M] -> M ->₀ R).Injective
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.injective`：∀ {α : Sort u} {β : Sort v} (e : α ≃ β), Function.Injec
tive ⇑e
-/
lemma coeff_injective : (coeff : R[M] → M →₀ R).Injective := coeffEquiv.injective

@[to_additive]
/-
**MonoidAlgebra.ofCoeff_injective** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ofCoeff_injective : (ofCoeff : (M ->₀ R) -> R[M]).Injective
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.injective`：∀ {α : Sort u} {β : Sort v} (e : α ≃ β), Function.Injec
tive ⇑e
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
-/
lemma ofCoeff_injective : (ofCoeff : (M →₀ R) → R[M]).Injective :=
  coeffEquiv.symm.injective

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.coeff_inj** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_inj : x.coeff = y.coeff ↔ x = y
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Function.Injective.eq_iff`：∀ {α : Sort u_1} {β : Sort u_2} {f : α → β}, 
Function.Injective f → ∀ {a b : α}, f a = f b ↔ a = b
· 使用引理 `MonoidAlgebra.coeff_injective`：coeff_injective : (coeff : R[M] -> M ->₀ 
R).Injective
-/
lemma coeff_inj : x.coeff = y.coeff ↔ x = y := coeff_injective.eq_iff

@[to_additive]
/-
**MonoidAlgebra.ofCoeff_inj** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ofCoeff_inj {x y : M ->₀ R} : ofCoeff x = ofCoeff y ↔ x = y
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Function.Injective.eq_iff`：∀ {α : Sort u_1} {β : Sort u_2} {f : α → β}, 
Function.Injective f → ∀ {a b : α}, f a = f b ↔ a = b
· 使用引理 `MonoidAlgebra.ofCoeff_injective`：ofCoeff_injective : (ofCoeff : (M ->₀ R
) -> R[M]).Injective
-/
lemma ofCoeff_inj {x y : M →₀ R} : ofCoeff x = ofCoeff y ↔ x = y := ofCoeff_injective.eq_iff

@[to_additive (attr := ext)] alias ⟨ext, _⟩ := coeff_inj

@[to_additive]
/-
**MonoidAlgebra.instInhabited** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：instInhabited : Inhabited R[M]
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
-/
instance instInhabited : Inhabited R[M] := fast_instance% coeffEquiv.inhabited

@[to_additive]
/-
**MonoidAlgebra.instNontrivial** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：instNontrivial [Nontrivial R] [Nonempty M] : Nontrivial R[M]
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.nontrivial`：∀ {α : Type u_1} {β : Type u_2} (e : α ≃ β) [Nontrivia
l β], Nontrivial α
-/
instance instNontrivial [Nontrivial R] [Nonempty M] : Nontrivial R[M] := coeffEquiv.nontrivial

@[to_additive]
/-
**MonoidAlgebra.instUnique** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：instUnique [Subsingleton R] : Unique R[M]
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
-/
instance instUnique [Subsingleton R] : Unique R[M] := fast_instance% coeffEquiv.unique

@[to_additive]
/-
**MonoidAlgebra.instDecidableEq** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：instDecidableEq [DecidableEq R] [DecidableEq M] : DecidableEq R[M]
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance instDecidableEq [DecidableEq R] [DecidableEq M] : DecidableEq R[M] :=
  coeffEquiv.decidableEq

@[to_additive instAddMonoid]
/-
**MonoidAlgebra.instAddMonoid** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：instAddMonoid : AddMonoid R[M]
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance instAddMonoid : AddMonoid R[M] := fast_instance% coeffEquiv.addMonoid

@[to_additive instAddCommMonoid]
/-
**MonoidAlgebra.instAddCommMonoid** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：instAddCommMonoid : AddCommMonoid R[M]
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance instAddCommMonoid : AddCommMonoid R[M] := fast_instance% coeffEquiv.addCommMonoid

@[to_additive]
/-
**MonoidAlgebra.instIsCancelAdd** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：instIsCancelAdd [IsCancelAdd R] : IsCancelAdd R[M]
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.isCancelAdd`：∀ {α : Type u_2} {β : Type u_3} (e : α ≃ β) [inst : A
dd β] [IsCancelAdd β], IsCancelAdd α
· 使用定理 `Finsupp.instIsCancelAdd`：∀ {ι : Type u_1} {M : Type u_3} [inst : AddZero
Class M] [IsCancelAdd M], IsCancelAdd (ι →₀ M)
-/
instance instIsCancelAdd [IsCancelAdd R] : IsCancelAdd R[M] :=
  coeffEquiv.isCancelAdd

/-- `MonoidAlgebra.coeff` as an `AddEquiv`. -/
@[to_additive (attr := simps! apply symm_apply)
/-- `AddMonoidAlgebra.coeff` as an `AddEquiv`. -/]
/-
**MonoidAlgebra.coeffAddEquiv** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeffAddEquiv : R[M] ≃+ (M ->₀ R)
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
def coeffAddEquiv : R[M] ≃+ (M →₀ R) := coeffEquiv.addEquiv
/-
**MonoidAlgebra.coeff_zero** 是 Mathlib 中的一个定理，位于命名空间 `MonoidAlgebra`。
形式化陈述：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R], MonoidAlgebra.coeff 0
 = 0
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
@[to_additive (attr := simp)] lemma coeff_zero : coeff (0 : R[M]) = 0 := rfl
/-
**MonoidAlgebra.ofCoeff_zero** 是 Mathlib 中的一个定理，位于命名空间 `MonoidAlgebra`。
形式化陈述：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R], MonoidAlgebra.ofCoeff
 0 = 0
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
@[to_additive (attr := simp)] lemma ofCoeff_zero : (ofCoeff 0 : R[M]) = 0 := rfl
/-
**MonoidAlgebra.coeff_eq_zero** 是 Mathlib 中的一个定理，位于命名空间 `MonoidAlgebra`。
形式化陈述：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] {x : MonoidAlgebra R M
}, x.coeff = 0 ↔ x = 0
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.coeff_inj`：coeff_inj : x.coeff = y.coeff ↔ x = y
-/
@[to_additive (attr := simp)] lemma coeff_eq_zero : coeff x = 0 ↔ x = 0 := coeff_inj
/-
**MonoidAlgebra.ofCoeff_eq_zero** 是 Mathlib 中的一个定理，位于命名空间 `MonoidAlgebra`。
形式化陈述：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] {x : M →₀ R}, MonoidAl
gebra.ofCoeff x = 0 ↔ x = 0
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.ofCoeff_inj`：ofCoeff_inj {x y : M ->₀ R} : ofCoeff x = ofC
oeff y ↔ x = y
-/
@[to_additive (attr := simp)] lemma ofCoeff_eq_zero {x : M →₀ R} : ofCoeff x = 0 ↔ x = 0 :=
  ofCoeff_inj

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.coeff_add** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_add (x y : R[M]) : coeff (x + y) = coeff x + coeff y
参数：x y : R[M]。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma coeff_add (x y : R[M]) : coeff (x + y) = coeff x + coeff y := rfl

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.ofCoeff_add** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ofCoeff_add (x y : M ->₀ R) : ofCoeff (x + y) = ofCoeff x + ofCoeff y
参数：x y : M ->₀ R。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma ofCoeff_add (x y : M →₀ R) : ofCoeff (x + y) = ofCoeff x + ofCoeff y := rfl

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.coeff_sum** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_sum (s : Finset ι) (f : ι -> R[M]) : coeff (∑ i in s, f i) = ∑ i in 
s, coeff (f i)
参数：s : Finset ι；f : ι -> R[M]。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `map_sum`：∀ {ι : Type u_1} {M : Type u_3} {N : Type u_4} [inst : AddCommM
onoid M] [inst_1 : AddCommMonoid N] {G : Type u_7}   [inst_2 : FunLike G M N]…
· 使用定理 `AddEquivClass.instAddMonoidHomClass`：∀ (F : Type u_1) {M : Type u_4} {N 
: Type u_5} [inst : EquivLike F M N] [inst_1 : AddZeroClass M]   [inst_2 : AddZe
roClass N] [AddEquivClass…
· 使用定理 `AddEquiv.instAddEquivClass`：∀ {M : Type u_4} {N : Type u_5} [inst : Add 
M] [inst_1 : Add N], AddEquivClass (M ≃+ N) M N
-/
lemma coeff_sum (s : Finset ι) (f : ι → R[M]) :
    coeff (∑ i ∈ s, f i) = ∑ i ∈ s, coeff (f i) := map_sum coeffAddEquiv ..

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.ofCoeff_sum** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ofCoeff_sum (s : Finset ι) (f : ι -> M ->₀ R) : ofCoeff (∑ i in s, f i) = 
∑ i in s, ofCoeff (f i)
参数：s : Finset ι；f : ι -> M ->₀ R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `map_sum`：∀ {ι : Type u_1} {M : Type u_3} {N : Type u_4} [inst : AddCommM
onoid M] [inst_1 : AddCommMonoid N] {G : Type u_7}   [inst_2 : FunLike G M N]…
· 使用定理 `AddEquivClass.instAddMonoidHomClass`：∀ (F : Type u_1) {M : Type u_4} {N 
: Type u_5} [inst : EquivLike F M N] [inst_1 : AddZeroClass M]   [inst_2 : AddZe
roClass N] [AddEquivClass…
· 使用定理 `AddEquiv.instAddEquivClass`：∀ {M : Type u_4} {N : Type u_5} [inst : Add 
M] [inst_1 : Add N], AddEquivClass (M ≃+ N) M N
-/
lemma ofCoeff_sum (s : Finset ι) (f : ι → M →₀ R) :
    ofCoeff (∑ i ∈ s, f i) = ∑ i ∈ s, ofCoeff (f i) := map_sum coeffAddEquiv.symm ..

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.coeff_finsuppSum** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_finsuppSum [AddCommMonoid N] (f : ι ->₀ N) (g : ι -> N -> R[M]) : co
eff (f.sum g) = f.sum (fun i n => coeff (g i n))
参数：f : ι ->₀ N；g : ι -> N -> R[M]。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `map_finsuppSum`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10} {P : Typ
e u_11} [inst : Zero M] [inst_1 : AddCommMonoid N]   [inst_2 : AddCommMonoid P] 
{H :…
· 使用定理 `AddEquivClass.instAddMonoidHomClass`：∀ (F : Type u_1) {M : Type u_4} {N 
: Type u_5} [inst : EquivLike F M N] [inst_1 : AddZeroClass M]   [inst_2 : AddZe
roClass N] [AddEquivClass…
· 使用定理 `AddEquiv.instAddEquivClass`：∀ {M : Type u_4} {N : Type u_5} [inst : Add 
M] [inst_1 : Add N], AddEquivClass (M ≃+ N) M N
-/
lemma coeff_finsuppSum [AddCommMonoid N] (f : ι →₀ N) (g : ι → N → R[M]) :
    coeff (f.sum g) = f.sum (fun i n ↦ coeff (g i n)) := map_finsuppSum coeffAddEquiv ..

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.ofCoeff_finsuppSum** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ofCoeff_finsuppSum [AddCommMonoid N] (f : ι ->₀ N) (g : ι -> N -> M ->₀ R)
 : ofCoeff (f.sum g) = f.sum (fun i n => ofCoeff (g i n))
参数：f : ι ->₀ N；g : ι -> N -> M ->₀ R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `map_finsuppSum`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10} {P : Typ
e u_11} [inst : Zero M] [inst_1 : AddCommMonoid N]   [inst_2 : AddCommMonoid P] 
{H :…
· 使用定理 `AddEquivClass.instAddMonoidHomClass`：∀ (F : Type u_1) {M : Type u_4} {N 
: Type u_5} [inst : EquivLike F M N] [inst_1 : AddZeroClass M]   [inst_2 : AddZe
roClass N] [AddEquivClass…
· 使用定理 `AddEquiv.instAddEquivClass`：∀ {M : Type u_4} {N : Type u_5} [inst : Add 
M] [inst_1 : Add N], AddEquivClass (M ≃+ N) M N
-/
lemma ofCoeff_finsuppSum [AddCommMonoid N] (f : ι →₀ N) (g : ι → N → M →₀ R) :
    ofCoeff (f.sum g) = f.sum (fun i n ↦ ofCoeff (g i n)) := map_finsuppSum coeffAddEquiv.symm ..

/-- `MonoidAlgebra.single m r` for `m : M`, `r : R` is the element `rm : R[M]`. -/
@[to_additive
/-- `AddMonoidAlgebra.single m r` for `m : M`, `r : R` is the element `rm : R[M]`. -/]
/-
**MonoidAlgebra.single** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：single (m : M) (r : R) : R[M]
参数：m : M；r : R。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
def single (m : M) (r : R) : R[M] := .ofCoeff <| .single m r

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.coeff_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_single (m : M) (r : R) : (single m r).coeff = .single m r
参数：m : M；r : R。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma coeff_single (m : M) (r : R) : (single m r).coeff = .single m r := rfl

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.ofCoeff_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ofCoeff_single (m : M) (r : R) : ofCoeff (.single m r) = single m r
参数：m : M；r : R。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma ofCoeff_single (m : M) (r : R) : ofCoeff (.single m r) = single m r := rfl

@[to_additive]
/-
**MonoidAlgebra.single_inj** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_inj : single m₁ r₁ = single m₂ r₂ ↔ m₁ = m₂ ∧ r₁ = r₂ ∨ r₁ = 0 ∧ r₂
 = 0
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
lemma single_inj : single m₁ r₁ = single m₂ r₂ ↔ m₁ = m₂ ∧ r₁ = r₂ ∨ r₁ = 0 ∧ r₂ = 0 := by
  simp [← coeff_inj, Finsupp.single_eq_single_iff]

@[to_additive]
/-
**MonoidAlgebra.single_left_inj** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_left_inj (hr : r != 0) : single m₁ r = single m₂ r ↔ m₁ = m₂
参数：hr : r != 0。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `and_true`：∀ (p : Prop), (p ∧ True) = p
· 使用定理 `eq_false`：∀ {p : Prop}, ¬p → p = False
· 使用定理 `and_self`：∀ (p : Prop), (p ∧ p) = p
· 使用定理 `or_false`：∀ (p : Prop), (p ∨ False) = p
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
lemma single_left_inj (hr : r ≠ 0) : single m₁ r = single m₂ r ↔ m₁ = m₂ := by simp [single_inj, hr]

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.single_right_inj** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_right_inj : single m r₁ = single m r₂ ↔ r₁ = r₂
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `true_and`：∀ (p : Prop), (True ∧ p) = p
· 使用定理 `implies_congr_ctx`：∀ {p₁ p₂ q₁ q₂ : Prop}, p₁ = p₂ → (p₂ → q₁ = q₂) → (p
₁ → q₁) = (p₂ → q₂)
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
-/
lemma single_right_inj : single m r₁ = single m r₂ ↔ r₁ = r₂ := by simp +contextual [single_inj]

/-- `MonoidAlgebra.single m r` is injective in `m` if `r ≠ 0`. For injectivity in `r`, see
`MonoidAlgebra.single_injective`. -/
@[to_additive
/-- `AddMonoidAlgebra.single m r` is injective in `m` if `r ≠ 0`. For injectivity in `r`, see
`AddMonoidAlgebra.single_injective`.-/]
/-
**MonoidAlgebra.single_left_injective** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_left_injective (hr : r != 0) : Function.Injective fun m : M => sing
le m r
参数：hr : r != 0。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用引理 `MonoidAlgebra.single_left_inj`：single_left_inj (hr : r != 0) : single m₁
 r = single m₂ r ↔ m₁ = m₂
-/
lemma single_left_injective (hr : r ≠ 0) : Function.Injective fun m : M ↦ single m r :=
  fun _ _ ↦ (single_left_inj hr).1

@[to_additive]
/-
**MonoidAlgebra.single_right_injective** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`
。
形式化陈述：single_right_injective : (single m : R -> R[M]).Injective
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用引理 `MonoidAlgebra.single_right_inj`：single_right_inj : single m r₁ = single 
m r₂ ↔ r₁ = r₂
-/
lemma single_right_injective : (single m : R → R[M]).Injective := fun _ _ ↦ single_right_inj.1

@[to_additive]
/-
**MonoidAlgebra.single_add_single_inj** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_add_single_inj (hr₁ : r₁ != 0) (hr₂ : r₂ != 0) : single m₁ r₁ + sin
gle m₂ r₂ = single m₁' r₁ + single m₂' r₂ ↔ m₁ = m₁' ∧ m₂ = m₂' ∨ r₁ = r₂ ∧ m₁ =
 m₂' ∧ m₂ = m₁' ∨ r₁ + r₂ = 0 ∧ m₁ = m₂ ∧ m₁' = m₂'
参数：hr₁ : r₁ != 0；hr₂ : r₂ != 0。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `eq_false`：∀ {p : Prop}, ¬p → p = False
· 使用定理 `not_false_eq_true`：(¬False) = True
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
lemma single_add_single_inj (hr₁ : r₁ ≠ 0) (hr₂ : r₂ ≠ 0) :
    single m₁ r₁ + single m₂ r₂ = single m₁' r₁ + single m₂' r₂ ↔
      m₁ = m₁' ∧ m₂ = m₂' ∨ r₁ = r₂ ∧ m₁ = m₂' ∧ m₂ = m₁' ∨ r₁ + r₂ = 0 ∧ m₁ = m₂ ∧ m₁' = m₂' := by
  simp [← coeff_inj, single_add_single_eq_single_add_single, *]

/-- Remove a term from an element of the monoid algebra. -/
@[to_additive /-- Remove a term from an element of the additive monoid algebra. -/]
/-
**MonoidAlgebra.erase** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：erase (m : M) (x : R[M]) : R[M]
参数：m : M；x : R[M]。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Remove a term from an element of the monoid algebra.
-/
def erase (m : M) (x : R[M]) : R[M] := .ofCoeff <| .erase m x.coeff

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.coeff_erase** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_erase (m : M) (x : R[M]) : (x.erase m).coeff = x.coeff.erase m
参数：m : M；x : R[M]。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma coeff_erase (m : M) (x : R[M]) : (x.erase m).coeff = x.coeff.erase m := rfl

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.ofCoeff_erase** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ofCoeff_erase (m : M) (x : M ->₀ R) : ofCoeff (x.erase m) = (ofCoeff x).er
ase m
参数：m : M；x : M ->₀ R。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma ofCoeff_erase (m : M) (x : M →₀ R) : ofCoeff (x.erase m) = (ofCoeff x).erase m := rfl

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.erase_zero** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：erase_zero (m : M) : erase m (0 : R[M]) = 0
参数：m : M。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidAlgebra.ext`：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] {
x y : MonoidAlgebra R M}, x.coeff = y.coeff → x = y
· 使用定理 `Finsupp.ext`：ext {f g : α ->₀ M} (h : forall a, f a = g a) : f = g
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Finsupp.erase_of_notMem_support`：erase_of_notMem_support {f : α ->₀ M} {
a} (haf : a ∉ f.support) : erase a f = f
· 使用定理 `not_false_eq_true`：(¬False) = True
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma erase_zero (m : M) : erase m (0 : R[M]) = 0 := by ext; simp

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.erase_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：erase_single (m : M) (r : R) : erase m (single m r) = 0
参数：m : M；r : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidAlgebra.ext`：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] {
x y : MonoidAlgebra R M}, x.coeff = y.coeff → x = y
· 使用定理 `Finsupp.ext`：ext {f g : α ->₀ M} (h : forall a, f a = g a) : f = g
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Finsupp.erase_single`：erase_single {a : α} {b : M} : erase a (single a b
) = 0
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma erase_single (m : M) (r : R) : erase m (single m r) = 0 := by ext; simp

/-- Replace the `m`-th coefficient of an element `x` of the monoid algebra by a given value `r : R`.
If `r = 0`, this is equal to `x.erase m`. -/
@[to_additive
/-- Replace the `m`-th coefficient of an element `x` of the monoid algebra by a given value `r : R`.
If `r = 0`, this is equal to `x.erase m`. -/]
/-
**MonoidAlgebra.update** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：update (m : M) (r : R) (x : R[M]) : R[M]
参数：m : M；r : R；x : R[M]。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
def update (m : M) (r : R) (x : R[M]) : R[M] :=
  ofCoeff (x.coeff.update m r)

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.coeff_update** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_update (m : M) (r : R) (x : R[M]) : (x.update m r).coeff = x.coeff.u
pdate m r
参数：m : M；r : R；x : R[M]。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma coeff_update (m : M) (r : R) (x : R[M]) :
    (x.update m r).coeff = x.coeff.update m r := rfl

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.ofCoeff_update** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ofCoeff_update (m : M) (r : R) (x : M ->₀ R) : ofCoeff (x.update m r) = (o
fCoeff x).update m r
参数：m : M；r : R；x : M ->₀ R。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma ofCoeff_update (m : M) (r : R) (x : M →₀ R) :
    ofCoeff (x.update m r) = (ofCoeff x).update m r := rfl

section SMul

/-! ### Basic scalar multiplication instances

This section collects instances needed for the algebraic structure of `Polynomial`,
which is defined in terms of `MonoidAlgebra`.
Further results on scalar multiplication can be found in
`Mathlib/Algebra/MonoidAlgebra/Module.lean`.
-/

variable {A : Type*} [SMulZeroClass A R]

@[to_additive (dont_translate := A) smulZeroClass]
/-
**MonoidAlgebra.smulZeroClass** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：smulZeroClass : SMulZeroClass A R[M]
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance smulZeroClass : SMulZeroClass A R[M] := fast_instance% coeffEquiv.smulZeroClass _

section
-- Ensure that the different smul instances do not create a diamond.
/-
**MonoidAlgebra.** 是 Mathlib 中的一个示例，位于命名空间 `MonoidAlgebra`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
example : (smulZeroClass (A := ℕ) (R := R) (M := M)).smul = instAddCommMonoid.nsmul := by
  with_reducible_and_instances rfl

-- Ensure that smul has good defeq properties
private local instance {α} [Monoid M] [SMul M α] : SMul Mˣ α where smul m a := (m : M) • a
/-
**MonoidAlgebra.** 是 Mathlib 中的一个示例，位于命名空间 `MonoidAlgebra`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
example [Monoid A] (a : Units A) (x : R[M]) :
    a • x = (a : A) • x := by
  with_reducible_and_instances rfl
end

@[to_additive (attr := simp) (dont_translate := A) coeff_smul]
/-
**MonoidAlgebra.coeff_smul** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_smul (a : A) (x : R[M]) : coeff (a • x) = a • coeff x
参数：a : A；x : R[M]。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma coeff_smul (a : A) (x : R[M]) : coeff (a • x) = a • coeff x := rfl

@[to_additive (attr := simp) (dont_translate := A) ofCoeff_smul]
/-
**MonoidAlgebra.ofCoeff_smul** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ofCoeff_smul (a : A) (x : M ->₀ R) : ofCoeff (a • x) = a • ofCoeff x
参数：a : A；x : M ->₀ R。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma ofCoeff_smul (a : A) (x : M →₀ R) : ofCoeff (a • x) = a • ofCoeff x := rfl

@[to_additive (dont_translate := A) coeff_smul_apply]
/-
**MonoidAlgebra.coeff_smul_apply** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_smul_apply (a : A) (x : R[M]) (m : M) : coeff (a • x) m = a • coeff 
x m
参数：a : A；x : R[M]；m : M。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma coeff_smul_apply (a : A) (x : R[M]) (m : M) : coeff (a • x) m = a • coeff x m := rfl

@[deprecated (since := "2026-06-18")] alias smul_apply := coeff_smul_apply

set_option backward.isDefEq.respectTransparency false in
@[to_additive (attr := simp) (dont_translate := A) smul_single]
/-
**MonoidAlgebra.smul_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：smul_single (a : A) (m : M) (r : R) : a • single m r = single m (a • r)
参数：a : A；m : M；r : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidAlgebra.ext`：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] {
x y : MonoidAlgebra R M}, x.coeff = y.coeff → x = y
· 使用定理 `Finsupp.ext`：ext {f g : α ->₀ M} (h : forall a, f a = g a) : f = g
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Finsupp.smul_single`：smul_single [Zero M] [SMulZeroClass R M] (c : R) (a
 : α) (b : M) : c • Finsupp.single a b = Finsupp.single a (c • b)
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma smul_single (a : A) (m : M) (r : R) : a • single m r = single m (a • r) := by ext; simp

@[to_additive (dont_translate := R) smul_single']
/-
**MonoidAlgebra.smul_single'** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：smul_single' (r' : R) (m : M) (r : R) : r' • single m r = single m (r' * r
)
参数：r' : R；m : M；r : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.smul_single`：smul_single (a : A) (m : M) (r : R) : a • sin
gle m r = single m (a • r)
-/
lemma smul_single' (r' : R) (m : M) (r : R) : r' • single m r = single m (r' * r) := smul_single ..

@[to_additive (dont_translate := N) distribSMul]
/-
**MonoidAlgebra.distribSMul** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：distribSMul [DistribSMul N R] : DistribSMul N R[M]
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance distribSMul [DistribSMul N R] : DistribSMul N R[M] :=
  fast_instance% coeffEquiv.distribSMul _

@[to_additive (dont_translate := N) isScalarTower]
/-
**MonoidAlgebra.isScalarTower** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：isScalarTower [SMulZeroClass N R] [SMulZeroClass O R] [SMul N O] [IsScalar
Tower N O R] : IsScalarTower N O R[M]
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.isScalarTower`：∀ (M : Type u_1) (N : Type u_2) {α : Type u_4} {β :
 Type u_5} [inst : SMul M N] [inst_1 : SMul M β] [inst_2 : SMul N β]   (e : α ≃ 
β) [IsSca…
-/
instance isScalarTower [SMulZeroClass N R] [SMulZeroClass O R] [SMul N O] [IsScalarTower N O R] :
    IsScalarTower N O R[M] := coeffEquiv.isScalarTower ..

@[to_additive (dont_translate := N) smulCommClass]
/-
**MonoidAlgebra.smulCommClass** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：smulCommClass [SMulZeroClass N R] [SMulZeroClass O R] [SMulCommClass N O R
] : SMulCommClass N O R[M]
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.smulCommClass`：∀ (M : Type u_1) (N : Type u_2) {α : Type u_4} {β :
 Type u_5} [inst : SMul M β] [inst_1 : SMul N β] (e : α ≃ β)   [SMulCommClass M 
N β], SMu…
-/
instance smulCommClass [SMulZeroClass N R] [SMulZeroClass O R] [SMulCommClass N O R] :
    SMulCommClass N O R[M] := coeffEquiv.smulCommClass ..

@[to_additive (dont_translate := N) isCentralScalar]
/-
**MonoidAlgebra.isCentralScalar** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：isCentralScalar [SMulZeroClass N R] [SMulZeroClass Nᵐᵒᵖ R] [IsCentralScala
r N R] : IsCentralScalar N R[M]
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.isCentralScalar`：∀ (M : Type u_1) {α : Type u_4} {β : Type u_5} [i
nst : SMul M β] [inst_1 : SMul Mᵐᵒᵖ β] (e : α ≃ β)   [IsCentralScalar M β], IsCe
ntralScalar…
-/
instance isCentralScalar [SMulZeroClass N R] [SMulZeroClass Nᵐᵒᵖ R] [IsCentralScalar N R] :
    IsCentralScalar N R[M] := coeffEquiv.isCentralScalar _

end SMul

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.single_zero** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_zero (m : M) : (single m 0 : R[M]) = 0
参数：m : M。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Finsupp.single_zero`：single_zero (a : α) : (single a 0 : α ->₀ M) = 0
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma single_zero (m : M) : (single m 0 : R[M]) = 0 := by simp [single]

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.single_add** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_add (m : M) (r₁ r₂ : R) : single m (r₁ + r₂) = single m r₁ + single
 m r₂
参数：m : M；r₁ r₂ : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidAlgebra.ext`：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] {
x y : MonoidAlgebra R M}, x.coeff = y.coeff → x = y
· 使用定理 `Finsupp.ext`：ext {f g : α ->₀ M} (h : forall a, f a = g a) : f = g
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `Finsupp.single_add`：single_add (a : ι) (b₁ b₂ : M) : single a (b₁ + b₂) 
= single a b₁ + single a b₂
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma single_add (m : M) (r₁ r₂ : R) : single m (r₁ + r₂) = single m r₁ + single m r₂ := by
  ext; simp

@[to_additive (attr := deprecated coeff_add (since := "2026-06-18"))]
/-
**MonoidAlgebra.coe_add** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coe_add (f g : R[M]) : ⇑(f + g).coeff = f.coeff + g.coeff
参数：f g : R[M]。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma coe_add (f g : R[M]) : ⇑(f + g).coeff = f.coeff + g.coeff := rfl

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.single_add_erase** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_add_erase (m : M) (x : R[M]) : single m (x.coeff m) + x.erase m = x
参数：m : M；x : R[M]。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidAlgebra.ext`：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] {
x y : MonoidAlgebra R M}, x.coeff = y.coeff → x = y
· 使用定理 `Finsupp.ext`：ext {f g : α ->₀ M} (h : forall a, f a = g a) : f = g
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `Finsupp.single_add_erase`：single_add_erase (a : ι) (f : ι ->₀ M) : singl
e a (f a) + f.erase a = f
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma single_add_erase (m : M) (x : R[M]) : single m (x.coeff m) + x.erase m = x := by
  ext; simp [Finsupp.single_add_erase]

@[to_additive (attr := simp)]
  lemma erase_add_single (m : M) (x : R[M]) : x.erase m + single m (x.coeff m) = x := by
  ext; simp [Finsupp.erase_add_single]

/-- `MonoidAlgebra.single` as an `AddMonoidHom`.

TODO: Rename to `singleAddMonoidHom`. -/
@[to_additive (attr := simps)
/-- `AddMonoidAlgebra.single` as an `AddMonoidHom`.

TODO: Rename to `singleAddMonoidHom`. -/]
/-
**MonoidAlgebra.singleAddHom** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：singleAddHom (m : M) : R ->+ R[M] where toFun
参数：m : M。
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.single_zero`：single_zero (m : M) : (single m 0 : R[M]) = 0
· 使用引理 `MonoidAlgebra.single_add`：single_add (m : M) (r₁ r₂ : R) : single m (r₁ 
+ r₂) = single m r₁ + single m r₂
-/
def singleAddHom (m : M) : R →+ R[M] where
  toFun := single m
  map_zero' := single_zero _
  map_add' := single_add _

/-- If two additive homomorphisms from `R[M]` are equal on each `single r m`,
then they are equal. -/
@[to_additive AddMonoidAlgebra.addMonoidHom_ext
/-- If two additive homomorphisms from `R[M]` are equal on each `single r m`,
then they are equal. -/]
/-
**MonoidAlgebra.addMonoidHom_ext** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：addMonoidHom_ext [AddZeroClass N] ⦃f g : R[M] ->+ N⦄ (h : forall m r, f (s
ingle m r) = g (single m r)) : f = g
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Finsupp.addHom_ext`：addHom_ext [AddZeroClass N] ⦃f g : (α ->₀ M) ->+ N⦄ 
(H : forall x y, f (single x y) = g (single x y)) : f = g
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `AddMonoidHom.ext`：∀ {M : Type u_4} {N : Type u_5} [inst : AddZero M] [in
st_1 : AddZero N] ⦃f g : M →+ N⦄, (∀ (x : M), f x = g x) → f = g
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MonoidAlgebra.coeffAddEquiv_apply`：∀ {R : Type u_1} {M : Type u_4} [inst
 : Semiring R] (self : MonoidAlgebra R M),   MonoidAlgebra.coeffAddEquiv self = 
self.coeff
· 使用定理 `MonoidAlgebra.coeffAddEquiv_symm_apply`：∀ {R : Type u_1} {M : Type u_4} 
[inst : Semiring R] (coeff : M →₀ R),   MonoidAlgebra.coeffAddEquiv.symm coeff =
 MonoidAlgebra.ofCoeff coeff
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma addMonoidHom_ext [AddZeroClass N] ⦃f g : R[M] →+ N⦄
    (h : ∀ m r, f (single m r) = g (single m r)) : f = g := by
  have : f.comp coeffAddEquiv.symm.toAddMonoidHom = g.comp coeffAddEquiv.symm.toAddMonoidHom :=
    Finsupp.addHom_ext h
  convert congr(($this).comp coeffAddEquiv.toAddMonoidHom) <;> ext <;> simp

/-- If two additive homomorphisms from `R[M]` are equal on each `single r m`,
then they are equal.

We formulate this using equality of `AddMonoidHom`s so that `ext` tactic can apply a type-specific
extensionality lemma after this one.  E.g., if the fiber `M` is `ℕ` or `ℤ`, then it suffices to
verify `f (single a 1) = g (single a 1)`.

TODO: Rename to `addMonoidHom_ext'`. -/
@[to_additive (attr := ext high)
/-- If two additive homomorphisms from `R[M]` are equal on each `single r m`, then they are equal.

We formulate this using equality of `AddMonoidHom`s so that `ext` tactic can apply a type-specific
extensionality lemma after this one.  E.g., if the fiber `M` is `ℕ` or `ℤ`, then it suffices to
verify `f (single a 1) = g (single a 1)`.

TODO: Rename to `addMonoidHom_ext'`. -/]
/-
**MonoidAlgebra.addHom_ext'** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：addHom_ext' {N : Type*} [AddZeroClass N] ⦃f g : R[M] ->+ N⦄ (hfg : forall 
m, f.comp (singleAddHom m) = g.comp (singleAddHom m)) : f = g
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.addMonoidHom_ext`：addMonoidHom_ext [AddZeroClass N] ⦃f g :
 R[M] ->+ N⦄ (h : forall m r, f (single m r) = g (single m r)) : f = g
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MonoidAlgebra.singleAddHom_apply`：∀ {R : Type u_1} {M : Type u_4} [inst 
: Semiring R] (m : M) (r : R),   (MonoidAlgebra.singleAddHom m) r = MonoidAlgebr
a.single m r
-/
lemma addHom_ext' {N : Type*} [AddZeroClass N] ⦃f g : R[M] →+ N⦄
    (hfg : ∀ m, f.comp (singleAddHom m) = g.comp (singleAddHom m)) : f = g :=
  addMonoidHom_ext <| by simpa [DFunLike.ext_iff] using hfg

@[to_additive (attr := deprecated Finsupp.sum_single_index (since := "2026-06-18"))]
/-
**MonoidAlgebra.sum_single_index** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：sum_single_index [AddCommMonoid N] {m : M} {r : R} {h : M -> R -> N} (h_ze
ro : h m 0 = 0) : (single m r).coeff.sum h = h m r
参数：h_zero : h m 0 = 0。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Finsupp.sum_single_index`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10
} [inst : Zero M] [inst_1 : AddCommMonoid N] {a : α} {b : M}   {h : α → M → N}, 
h a 0 = 0 → (f…
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma sum_single_index [AddCommMonoid N] {m : M} {r : R} {h : M → R → N} (h_zero : h m 0 = 0) :
    (single m r).coeff.sum h = h m r := by
  simp [h_zero]

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.sum_coeff_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：sum_coeff_single (f : R[M]) : f.coeff.sum single = f
参数：f : R[M]。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidAlgebra.ext`：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] {
x y : MonoidAlgebra R M}, x.coeff = y.coeff → x = y
· 使用定理 `Finsupp.ext`：ext {f g : α ->₀ M} (h : forall a, f a = g a) : f = g
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `MonoidAlgebra.coeff_finsuppSum`：coeff_finsuppSum [AddCommMonoid N] (f : 
ι ->₀ N) (g : ι -> N -> R[M]) : coeff (f.sum g) = f.sum (fun i n => coeff (g i n
))
· 使用定理 `Finsupp.sum_single`：sum_single [AddCommMonoid M] (f : α ->₀ M) : f.sum s
ingle = f
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma sum_coeff_single (f : R[M]) : f.coeff.sum single = f := by ext; simp

@[to_additive (attr := deprecated sum_coeff_single (since := "2026-06-18"))]
alias sum_single := sum_coeff_single

@[to_additive (attr := deprecated Finsupp.single_apply (since := "2026-06-18"))]
/-
**MonoidAlgebra.coeff_single_apply** 是 Mathlib 中的一个定理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_single_apply {a a' : M} {b : R} [Decidable (a = a')] : (single a b).
coeff a' = if a = a' then b else 0
参数：a = a'。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Finsupp.single_apply`：single_apply [Decidable (a = a')] : single a b a' 
= if a = a' then b else 0
-/
theorem coeff_single_apply {a a' : M} {b : R} [Decidable (a = a')] :
    (single a b).coeff a' = if a = a' then b else 0 :=
  Finsupp.single_apply

@[deprecated (since := "2026-06-18")] protected alias single_apply := coeff_single_apply

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.single_eq_zero** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_eq_zero : single m r = 0 ↔ r = 0
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
lemma single_eq_zero : single m r = 0 ↔ r = 0 := by simp [← coeff_inj]

set_option backward.isDefEq.respectTransparency false in
/-
**MonoidAlgebra.single_ne_zero** 是 Mathlib 中的一个定理，位于命名空间 `MonoidAlgebra`。
形式化陈述：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] {r : R} {m : M}, Monoi
dAlgebra.single m r ≠ 0 ↔ r ≠ 0
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.not`：∀ {a b : Prop}, (a ↔ b) → (¬a ↔ ¬b)
· 使用引理 `MonoidAlgebra.single_eq_zero`：single_eq_zero : single m r = 0 ↔ r = 0
-/
@[to_additive] lemma single_ne_zero : single m r ≠ 0 ↔ r ≠ 0 := single_eq_zero.not

@[to_additive (attr := elab_as_elim)]
/-
**MonoidAlgebra.induction** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：induction {motive : R[M] -> Prop} (x : R[M]) (zero : motive 0) (single_add
 : forall m r x, m ∉ x.coeff.support -> r != 0 -> motive x -> motive (single m r
 + x)) : motive x
参数：x : R[M]；zero : motive 0；single_add : forall m r x, m ∉ x.coeff.support -> r 
!= 0 -> motive x -> motive (single m r + x)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Finsupp.induction`：∀ {ι : Type u_1} {M : Type u_3} [inst : AddZeroClass 
M] {motive : (ι →₀ M) → Prop} (f : ι →₀ M),   motive 0 →     (∀ (a : ι) (b : M) 
(f : ι …
-/
lemma induction {motive : R[M] → Prop} (x : R[M])
    (zero : motive 0)
    (single_add : ∀ m r x, m ∉ x.coeff.support → r ≠ 0 → motive x → motive (single m r + x)) :
    motive x :=
  Finsupp.induction (motive := fun x ↦ motive <| ofCoeff x) x.coeff
    (by simpa using zero) (fun m r x ↦ single_add m r (ofCoeff x))

@[to_additive (attr := elab_as_elim)]
/-
**MonoidAlgebra.induction_linear** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：induction_linear {motive : R[M] -> Prop} (x : R[M]) (zero : motive 0) (add
 : forall x y : R[M], motive x -> motive y -> motive (x + y)) (single : forall m
 r, motive (single m r)) : motive x
参数：x : R[M]；zero : motive 0；add : forall x y : R[M], motive x -> motive y -> mot
ive (x + y)；single : forall m r, motive (single m r)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `Finsupp.induction_linear`：induction_linear {motive : (ι ->₀ M) -> Prop} 
(f : ι ->₀ M) (zero : motive 0) (add : forall f g : ι ->₀ M, motive f -> motive 
g -> motive (f…
-/
lemma induction_linear {motive : R[M] → Prop} (x : R[M]) (zero : motive 0)
    (add : ∀ x y : R[M], motive x → motive y → motive (x + y))
    (single : ∀ m r, motive (single m r)) : motive x :=
  Finsupp.induction_linear (motive := (motive <| ofCoeff ·)) x.coeff zero (fun _ _ ↦ add _ _)
    (fun _ _ ↦ single _ _)

@[to_additive (attr := simp) addSubmonoidClosure_single]
/-
**MonoidAlgebra.addSubmonoidClosure_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlge
bra`。
形式化陈述：addSubmonoidClosure_single : AddSubmonoid.closure {x : R[M] | exists m r, 
x = single m r} = ⊤
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `top_unique`：top_unique (h : ⊤ <= a) : a = ⊤
· 使用引理 `MonoidAlgebra.induction`：induction {motive : R[M] -> Prop} (x : R[M]) (z
ero : motive 0) (single_add : forall m r x, m ∉ x.coeff.support -> r != 0 -> mot
ive x -> moti…
· 使用定理 `AddSubmonoid.zero_mem`：∀ {M : Type u_1} [inst : AddZeroClass M] (S : Add
Submonoid M), 0 ∈ S
· 使用定理 `AddSubmonoid.add_mem`：∀ {M : Type u_1} [inst : AddZeroClass M] (S : AddS
ubmonoid M) {x y : M}, x ∈ S → y ∈ S → x + y ∈ S
· 使用定理 `AddSubmonoid.subset_closure`：∀ {M : Type u_1} [inst : AddZeroClass M] {s
 : Set M}, s ⊆ ↑(AddSubmonoid.closure s)
-/
lemma addSubmonoidClosure_single :
    AddSubmonoid.closure {x : R[M] | ∃ m r, x = single m r} = ⊤ :=
  top_unique fun x _hx => induction x (AddSubmonoid.zero_mem _) fun a b _f _ha _hb =>
    AddSubmonoid.add_mem _ <| AddSubmonoid.subset_closure <| ⟨a, b, rfl⟩

section One
variable [One M]

/-- The unit of the multiplication is `single 1 1`,
i.e. the function that is `1` at `1` and `0` elsewhere. -/
@[to_additive (dont_translate := R)
/-- The unit of the multiplication is `single 1 1`,
i.e. the function that is `1` at `1` and `0` elsewhere. -/]
/-
**MonoidAlgebra.one** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：one : One R[M] where one
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance one : One R[M] where one := single 1 1

@[to_additive (dont_translate := R) one_def]
/-
**MonoidAlgebra.one_def** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：one_def : (1 : R[M]) = single 1 1
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma one_def : (1 : R[M]) = single 1 1 := rfl

@[to_additive (attr := simp) (dont_translate := R) coeff_one_zero]
/-
**MonoidAlgebra.coeff_one_one** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_one_one : (1 : R[M]).coeff 1 = 1
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Finsupp.single_eq_same`：single_eq_same : (single a b : α ->₀ M) a = b
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma coeff_one_one : (1 : R[M]).coeff 1 = 1 := by simp [one_def]

@[deprecated (since := "2026-07-15")]
alias _root_.AddMonoidAlgebra.coeff_zero_zero := AddMonoidAlgebra.coeff_one_zero

end One

section Mul
variable [Mul M]

/-- The multiplication in an additive monoid algebra.

We make it irreducible so that Lean doesn't unfold it when trying to unify two different things. -/
@[no_expose]
/-
**MonoidAlgebra._root_.AddMonoidAlgebra.mul'** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAl
gebra`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The multiplication in an additive monoid algebra.

We make it irreducible so that Lean doesn't unfold it when trying to unify two d
ifferent things.
-/
def _root_.AddMonoidAlgebra.mul' [Add M] (x y : AddMonoidAlgebra R M) : AddMonoidAlgebra R M :=
  x.coeff.sum fun m₁ r₁ ↦ y.coeff.sum fun m₂ r₂ ↦ .single (m₁ + m₂) (r₁ * r₂)
/-- The multiplication in a monoid algebra.

We make it irreducible so that Lean doesn't unfold it when trying to unify two different things. -/
@[to_additive existing mul', no_expose]
/-
**MonoidAlgebra.mul'** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：mul' (x y : R[M]) : R[M]
参数：x y : R[M]。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The multiplication in a monoid algebra.

We make it irreducible so that Lean doesn't unfold it when trying to unify two d
ifferent things.
-/
def mul' (x y : R[M]) : R[M] :=
  x.coeff.sum fun m₁ r₁ ↦ y.coeff.sum fun m₂ r₂ ↦ single (m₁ * m₂) (r₁ * r₂)

/-- The product of `x y : R[M]` is the finitely supported function whose value at `m`
is the sum of `x m₁ * y m₂` over all pairs `m₁, m₂` such that `m₁ * m₂ = m`. -/
@[to_additive instMul
/-- The product of `x y : R[M]` is the finitely supported function whose value at `m`
is the sum of `x m₁ * y m₂` over all pairs `m₁, m₂` such that `m₁ + m₂ = m`. -/]
/-
**MonoidAlgebra.instMul** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：instMul : Mul R[M] where mul
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance instMul : Mul R[M] where mul := mul'

@[to_additive (dont_translate := R) mul_def]
/-
**MonoidAlgebra.mul_def** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：mul_def (x y : R[M]) : x * y = x.coeff.sum fun m₁ r₁ => y.coeff.sum fun m₂
 r₂ => single (m₁ * m₂) (r₁ * r₂)
参数：x y : R[M]。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma mul_def (x y : R[M]) :
    x * y = x.coeff.sum fun m₁ r₁ ↦ y.coeff.sum fun m₂ r₂ ↦ single (m₁ * m₂) (r₁ * r₂) := by
  with_unfolding_all rfl

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.nonUnitalNonAssocSemiring** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgeb
ra`。
形式化陈述：nonUnitalNonAssocSemiring : NonUnitalNonAssocSemiring R[M] where zero_mul
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance nonUnitalNonAssocSemiring : NonUnitalNonAssocSemiring R[M] where
  zero_mul := by simp [mul_def]
  mul_zero := by simp [mul_def]
  left_distrib := by classical simp [mul_def, mul_add, sum_add, sum_add_index]
  right_distrib := by classical simp [mul_def, add_mul, sum_add, sum_add_index]

@[to_additive (dont_translate := R) coeff_mul]
/-
**MonoidAlgebra.coeff_mul** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_mul [DecidableEq M] (x y : R[M]) (m : M) : (x * y).coeff m = x.coeff
.sum fun m₁ r₁ => y.coeff.sum fun m₂ r₂ => if m₁ * m₂ = m then r₁ * r₂ else 0
参数：x y : R[M]；m : M。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `MonoidAlgebra.mul_def`：mul_def (x y : R[M]) : x * y = x.coeff.sum fun m₁
 r₁ => y.coeff.sum fun m₂ r₂ => single (m₁ * m₂) (r₁ * r₂)
· 使用引理 `MonoidAlgebra.coeff_finsuppSum`：coeff_finsuppSum [AddCommMonoid N] (f : 
ι ->₀ N) (g : ι -> N -> R[M]) : coeff (f.sum g) = f.sum (fun i n => coeff (g i n
))
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Finsupp.sum_apply`：sum_apply [Zero M] [AddCommMonoid N] {f : α ->₀ M} {g
 : α -> M -> β ->₀ N} {a₂ : β} : (f.sum g) a₂ = f.sum fun a₁ b => g a₁ b a₂
· 使用定理 `Finsupp.single_apply`：single_apply [Decidable (a = a')] : single a b a' 
= if a = a' then b else 0
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma coeff_mul [DecidableEq M] (x y : R[M]) (m : M) :
    (x * y).coeff m =
      x.coeff.sum fun m₁ r₁ ↦ y.coeff.sum fun m₂ r₂ ↦ if m₁ * m₂ = m then r₁ * r₂ else 0 := by
  simp [mul_def, Finsupp.single_apply]

@[to_additive (attr := deprecated coeff_mul (since := "2026-06-18")) (dont_translate := R)
  mul_apply]
alias mul_apply := coeff_mul

set_option backward.isDefEq.respectTransparency false in
open Finset in
@[to_additive (dont_translate := R) coeff_mul_antidiag]
/-
**MonoidAlgebra.coeff_mul_antidiag** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_mul_antidiag (x y : R[M]) (m : M) (s : Finset (M × M)) (hs : forall 
{p}, p in s ↔ p.1 * p.2 = m) : (x * y).coeff m = ∑ p in s, x.coeff p.1 * y.coeff
 p.2
参数：x y : R[M]；m : M；s : Finset (M × M)；hs : forall {p}, p in s ↔ p.1 * p.2 = m。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.coeff_mul`：coeff_mul [DecidableEq M] (x y : R[M]) (m : M) 
: (x * y).coeff m = x.coeff.sum fun m₁ r₁ => y.coeff.sum fun m₂ r₂ => if m₁ * m₂
 = m then r₁ …
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Finset.sum_filter`：∀ {ι : Type u_1} {M : Type u_4} {s : Finset ι} [inst 
: AddCommMonoid M] (p : ι → Prop) [inst_1 : DecidablePred p]   (f : ι → M), ∑ a 
∈ s wit…
· 使用定理 `Finset.sum_product`：∀ {α : Type u_3} {β : Type u_4} {γ : Type u_5} [inst
 : AddCommMonoid β] (s : Finset γ) (t : Finset α) (f : γ × α → β),   ∑ x ∈ s ×ˢ 
t, f x =…
· 使用定理 `Finset.sum_congr`：∀ {ι : Type u_1} {M : Type u_4} {s₁ s₂ : Finset ι} [in
st : AddCommMonoid M] {f g : ι → M},   s₁ = s₂ → (∀ x ∈ s₂, f x = g x) → s₁.sum 
f = s₂…
· 使用定理 `Finset.ext`：ext {s₁ s₂ : Finset α} (h : forall a, a in s₁ ↔ a in s₂) : s
₁ = s₂
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `Finset.sum_subset`：∀ {ι : Type u_1} {M : Type u_4} {s₁ s₂ : Finset ι} [i
nst : AddCommMonoid M] {f : ι → M},   s₁ ⊆ s₂ → (∀ x ∈ s₂, x ∉ s₁ → f x = 0) → ∑
 x ∈ s₁…
· 使用定理 `Finset.filter_subset`：∀ {α : Type u_1} (p : α → Prop) [inst : DecidableP
red p] (s : Finset α), Finset.filter p s ⊆ s
· 使用定理 `MulZeroClass.zero_mul`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, 0 * a = 0
· 使用定理 `Finset.filter_congr`：∀ {α : Type u_1} {p q : α → Prop} [inst : Decidable
Pred p] [inst_1 : DecidablePred q] {s : Finset α},   (∀ x ∈ s, p x ↔ q x) → Fins
et.filter…
· 使用定理 `Iff.of_eq`：∀ {a b : Prop}, a = b → (a ↔ b)
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `MulZeroClass.mul_zero`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, a * 0 = 0
-/
lemma coeff_mul_antidiag (x y : R[M]) (m : M) (s : Finset (M × M))
    (hs : ∀ {p}, p ∈ s ↔ p.1 * p.2 = m) : (x * y).coeff m = ∑ p ∈ s, x.coeff p.1 * y.coeff p.2 := by
  classical
  let F (p : M × M) : R := if p.1 * p.2 = m then x.coeff p.1 * y.coeff p.2 else 0
  calc
    (x * y).coeff m = ∑ m₁ ∈ x.coeff.support, ∑ m₂ ∈ y.coeff.support, F (m₁, m₂) := coeff_mul ..
    _ = ∑ p ∈ x.coeff.support ×ˢ y.coeff.support with p.1 * p.2 = m, x.coeff p.1 * y.coeff p.2 := by
      rw [Finset.sum_filter, Finset.sum_product]
    _ = ∑ p ∈ s with p.1 ∈ x.coeff.support ∧ p.2 ∈ y.coeff.support, x.coeff p.1 * y.coeff p.2 := by
      congr! 1; ext; simp only [mem_filter, mem_product, hs, and_comm]
    _ = ∑ p ∈ s, x.coeff p.1 * y.coeff p.2 :=
      sum_subset (filter_subset _ _) fun p hps hp => by
        simp only [mem_filter, mem_support_iff, not_and, Classical.not_not] at hp ⊢
        by_cases h1 : x.coeff p.1 = 0
        · rw [h1, zero_mul]
        · rw [hp hps h1, mul_zero]

@[to_additive (attr := deprecated coeff_mul_antidiag (since := "2026-06-18")) (dont_translate := R)
  mul_apply_antidiagonal]
alias mul_apply_antidiagonal := coeff_mul_antidiag

@[to_additive (attr := simp) (dont_translate := R) single_mul_single]
/-
**MonoidAlgebra.single_mul_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_mul_single (m₁ m₂ : M) (r₁ r₂ : R) : single m₁ r₁ * single m₂ r₂ = 
single (m₁ * m₂) (r₁ * r₂)
参数：m₁ m₂ : M；r₁ r₂ : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `MonoidAlgebra.mul_def`：mul_def (x y : R[M]) : x * y = x.coeff.sum fun m₁
 r₁ => y.coeff.sum fun m₂ r₂ => single (m₁ * m₂) (r₁ * r₂)
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Finsupp.sum_single_index`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10
} [inst : Zero M] [inst_1 : AddCommMonoid N] {a : α} {b : M}   {h : α → M → N}, 
h a 0 = 0 → (f…
· 使用定理 `MulZeroClass.mul_zero`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, a * 0 = 0
· 使用引理 `MonoidAlgebra.single_zero`：single_zero (m : M) : (single m 0 : R[M]) = 0
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `MulZeroClass.zero_mul`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, 0 * a = 0
-/
lemma single_mul_single (m₁ m₂ : M) (r₁ r₂ : R) :
    single m₁ r₁ * single m₂ r₂ = single (m₁ * m₂) (r₁ * r₂) := by simp [mul_def]

@[to_additive (attr := simp) (dont_translate := R) single_commute_single]
/-
**MonoidAlgebra.single_commute_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_commute_single (hm : Commute m₁ m₂) (hr : Commute r₁ r₂) : Commute 
(single m₁ r₁) (single m₂ r₂)
参数：hm : Commute m₁ m₂；hr : Commute r₁ r₂。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `MonoidAlgebra.single_mul_single`：single_mul_single (m₁ m₂ : M) (r₁ r₂ : 
R) : single m₁ r₁ * single m₂ r₂ = single (m₁ * m₂) (r₁ * r₂)
· 使用定理 `Commute.eq`：∀ {S : Type u_3} [inst : Mul S] {a b : S}, Commute a b → a *
 b = b * a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma single_commute_single (hm : Commute m₁ m₂) (hr : Commute r₁ r₂) :
    Commute (single m₁ r₁) (single m₂ r₂) := by simp [Commute, SemiconjBy, hm.eq, hr.eq]

@[to_additive (attr := simp) (dont_translate := R) single_commute]
/-
**MonoidAlgebra.single_commute** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_commute (hm : forall m', Commute m m') (hr : forall r', Commute r r
') (x : R[M]) : Commute (single m r) x
参数：hm : forall m', Commute m m'；hr : forall r', Commute r r'；x : R[M]。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.addHom_ext'`：addHom_ext' {N : Type*} [AddZeroClass N] ⦃f g
 : R[M] ->+ N⦄ (hfg : forall m, f.comp (singleAddHom m) = g.comp (singleAddHom m
)) : f = g
· 使用定理 `AddMonoidHom.ext`：∀ {M : Type u_4} {N : Type u_5} [inst : AddZero M] [in
st_1 : AddZero N] ⦃f g : M →+ N⦄, (∀ (x : M), f x = g x) → f = g
· 使用引理 `MonoidAlgebra.single_commute_single`：single_commute_single (hm : Commute
 m₁ m₂) (hr : Commute r₁ r₂) : Commute (single m₁ r₁) (single m₂ r₂)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
-/
lemma single_commute (hm : ∀ m', Commute m m') (hr : ∀ r', Commute r r') (x : R[M]) :
    Commute (single m r) x := by
  have : AddMonoidHom.mulLeft (single m r) = AddMonoidHom.mulRight (single m r) := by
    ext m' r' : 2; exact single_commute_single (hm m') (hr r')
  exact congr($this x)

set_option backward.isDefEq.respectTransparency false in
@[to_additive (dont_translate := R) coeff_mul_single_eq_coeff_mul]
/-
**MonoidAlgebra.coeff_mul_single_eq_coeff_mul** 是 Mathlib 中的一个引理，位于命名空间 `MonoidA
lgebra`。
形式化陈述：coeff_mul_single_eq_coeff_mul (m₂ : M) (H : forall m' in x.coeff.support, 
m' * m = m₁ ↔ m' = m₂) : (x * single m r).coeff m₁ = x.coeff m₂ * r
参数：m₂ : M；H : forall m' in x.coeff.support, m' * m = m₁ ↔ m' = m₂。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `MonoidAlgebra.coeff_mul`：coeff_mul [DecidableEq M] (x y : R[M]) (m : M) 
: (x * y).coeff m = x.coeff.sum fun m₁ r₁ => y.coeff.sum fun m₂ r₂ => if m₁ * m₂
 = m then r₁ …
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Finsupp.sum_single_index`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10
} [inst : Zero M] [inst_1 : AddCommMonoid N] {a : α} {b : M}   {h : α → M → N}, 
h a 0 = 0 → (f…
· 使用定理 `ite_congr`：∀ {α : Sort u_1} {b c : Prop} {x y u v : α} {s : Decidable b}
 [inst : Decidable c],   b = c → (c → x = u) → (¬c → y = v) → (if b then x else…
· 使用定理 `MulZeroClass.mul_zero`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, a * 0 = 0
· 使用定理 `ite_self`：∀ {α : Sort u} {c : Prop} {d : Decidable c} (a : α), (if c the
n a else a) = a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Finsupp.sum_congr`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10} [inst
 : Zero M] [inst_1 : AddCommMonoid N] {f : α →₀ M}   {g1 g2 : α → M → N}, (∀ x ∈
 f.supp…
· 使用定理 `Finsupp.sum_eq_single`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10} [
inst : Zero M] [inst_1 : AddCommMonoid N] {f : α →₀ M} (a : α)   {g : α → M → N}
, (∀ (b : α…
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr_ctx`：∀ {p₁ p₂ q₁ q₂ : Prop}, p₁ = p₂ → (p₂ → q₁ = q₂) → (p
₁ → q₁) = (p₂ → q₂)
· 使用定理 `ite_cond_eq_false`：∀ {α : Sort u} {c : Prop} {x : Decidable c} (a b : α)
, c = False → (if c then a else b) = b
· 使用定理 `eq_false`：∀ {p : Prop}, ¬p → p = False
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
· 使用定理 `ite_cond_eq_true`：∀ {α : Sort u} {c : Prop} {x : Decidable c} (a b : α),
 c = True → (if c then a else b) = a
· 使用定理 `MulZeroClass.zero_mul`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, 0 * a = 0
-/
lemma coeff_mul_single_eq_coeff_mul (m₂ : M) (H : ∀ m' ∈ x.coeff.support, m' * m = m₁ ↔ m' = m₂) :
    (x * single m r).coeff m₁ = x.coeff m₂ * r := by
  classical
  calc
    (x * single m r).coeff m₁
    _ = x.coeff.sum fun m' r' ↦ if m' * m = m₁ then r' * r else 0 := by simp [coeff_mul]
    _ = x.coeff.sum fun m' r' ↦ if m' = m₂ then r' * r else 0 := by gcongr; simp [*]
    _ = x.coeff m₂ * r := by simp +contextual [Finsupp.sum_eq_single m₂]

@[deprecated (since := "2026-06-18")] alias mul_single_apply_aux := coeff_mul_single_eq_coeff_mul

set_option backward.isDefEq.respectTransparency false in
@[to_additive (dont_translate := R) coeff_single_mul_eq_mul_coeff]
/-
**MonoidAlgebra.coeff_single_mul_eq_mul_coeff** 是 Mathlib 中的一个引理，位于命名空间 `MonoidA
lgebra`。
形式化陈述：coeff_single_mul_eq_mul_coeff (m₂ : M) (H : forall m' in x.coeff.support, 
m * m' = m₁ ↔ m' = m₂) : (single m r * x).coeff m₁ = r * x.coeff m₂
参数：m₂ : M；H : forall m' in x.coeff.support, m * m' = m₁ ↔ m' = m₂。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `MonoidAlgebra.coeff_mul`：coeff_mul [DecidableEq M] (x y : R[M]) (m : M) 
: (x * y).coeff m = x.coeff.sum fun m₁ r₁ => y.coeff.sum fun m₂ r₂ => if m₁ * m₂
 = m then r₁ …
· 使用定理 `Finsupp.sum_single_index`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10
} [inst : Zero M] [inst_1 : AddCommMonoid N] {a : α} {b : M}   {h : α → M → N}, 
h a 0 = 0 → (f…
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `ite_congr`：∀ {α : Sort u_1} {b c : Prop} {x y u v : α} {s : Decidable b}
 [inst : Decidable c],   b = c → (c → x = u) → (¬c → y = v) → (if b then x else…
· 使用定理 `MulZeroClass.zero_mul`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, 0 * a = 0
· 使用定理 `ite_self`：∀ {α : Sort u} {c : Prop} {d : Decidable c} (a : α), (if c the
n a else a) = a
· 使用定理 `Finsupp.sum_fun_zero`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10} [i
nst : Zero M] [inst_1 : AddCommMonoid N] (f : α →₀ M),   (f.sum fun x x_1 => 0) 
= 0
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Finsupp.sum_congr`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10} [inst
 : Zero M] [inst_1 : AddCommMonoid N] {f : α →₀ M}   {g1 g2 : α → M → N}, (∀ x ∈
 f.supp…
· 使用定理 `Finsupp.sum_eq_single`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10} [
inst : Zero M] [inst_1 : AddCommMonoid N] {f : α →₀ M} (a : α)   {g : α → M → N}
, (∀ (b : α…
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr_ctx`：∀ {p₁ p₂ q₁ q₂ : Prop}, p₁ = p₂ → (p₂ → q₁ = q₂) → (p
₁ → q₁) = (p₂ → q₂)
· 使用定理 `ite_cond_eq_false`：∀ {α : Sort u} {c : Prop} {x : Decidable c} (a b : α)
, c = False → (if c then a else b) = b
· 使用定理 `eq_false`：∀ {p : Prop}, ¬p → p = False
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
· 使用定理 `ite_cond_eq_true`：∀ {α : Sort u} {c : Prop} {x : Decidable c} (a b : α),
 c = True → (if c then a else b) = a
· 使用定理 `MulZeroClass.mul_zero`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, a * 0 = 0
-/
lemma coeff_single_mul_eq_mul_coeff (m₂ : M) (H : ∀ m' ∈ x.coeff.support, m * m' = m₁ ↔ m' = m₂) :
    (single m r * x).coeff m₁ = r * x.coeff m₂ := by
  classical
  calc
    (single m r * x).coeff m₁
    _ = x.coeff.sum fun m' r' ↦ if m * m' = m₁ then r * r' else 0 := by simp [coeff_mul]
    _ = x.coeff.sum fun m' r' ↦ if m' = m₂ then r * r' else 0 := by gcongr; simp [*]
    _ = r * x.coeff m₂ := by simp +contextual [Finsupp.sum_eq_single m₂]

@[deprecated (since := "2026-06-18")] alias single_mul_apply_aux := coeff_single_mul_eq_mul_coeff

@[to_additive (attr := simp) (dont_translate := R) coeff_mul_single_of_forall_add_ne]
/-
**MonoidAlgebra.coeff_mul_single_of_forall_mul_ne** 是 Mathlib 中的一个引理，位于命名空间 `Mon
oidAlgebra`。
形式化陈述：coeff_mul_single_of_forall_mul_ne (r : R) (x : R[M]) (h : forall d, d * m 
!= m') : (x * single m r).coeff m' = 0
参数：r : R；x : R[M]；h : forall d, d * m != m'。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `MonoidAlgebra.coeff_mul`：coeff_mul [DecidableEq M] (x y : R[M]) (m : M) 
: (x * y).coeff m = x.coeff.sum fun m₁ r₁ => y.coeff.sum fun m₂ r₂ => if m₁ * m₂
 = m then r₁ …
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Finsupp.sum_single_index`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10
} [inst : Zero M] [inst_1 : AddCommMonoid N] {a : α} {b : M}   {h : α → M → N}, 
h a 0 = 0 → (f…
· 使用定理 `ite_cond_eq_false`：∀ {α : Sort u} {c : Prop} {x : Decidable c} (a b : α)
, c = False → (if c then a else b) = b
· 使用定理 `eq_false`：∀ {p : Prop}, ¬p → p = False
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Finsupp.sum_fun_zero`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10} [i
nst : Zero M] [inst_1 : AddCommMonoid N] (f : α →₀ M),   (f.sum fun x x_1 => 0) 
= 0
-/
lemma coeff_mul_single_of_forall_mul_ne (r : R) (x : R[M]) (h : ∀ d, d * m ≠ m') :
    (x * single m r).coeff m' = 0 := by classical simp [coeff_mul, h]

set_option backward.isDefEq.respectTransparency false in
@[to_additive (attr := simp) (dont_translate := R) coeff_single_mul_of_forall_add_ne]
/-
**MonoidAlgebra.coeff_single_mul_of_forall_mul_ne** 是 Mathlib 中的一个引理，位于命名空间 `Mon
oidAlgebra`。
形式化陈述：coeff_single_mul_of_forall_mul_ne (r : R) (x : R[M]) (h : forall d, m * d 
!= m') : (single m r * x).coeff m' = 0
参数：r : R；x : R[M]；h : forall d, m * d != m'。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `MonoidAlgebra.coeff_mul`：coeff_mul [DecidableEq M] (x y : R[M]) (m : M) 
: (x * y).coeff m = x.coeff.sum fun m₁ r₁ => y.coeff.sum fun m₂ r₂ => if m₁ * m₂
 = m then r₁ …
· 使用定理 `Finsupp.sum_single_index`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10
} [inst : Zero M] [inst_1 : AddCommMonoid N] {a : α} {b : M}   {h : α → M → N}, 
h a 0 = 0 → (f…
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `ite_cond_eq_false`：∀ {α : Sort u} {c : Prop} {x : Decidable c} (a b : α)
, c = False → (if c then a else b) = b
· 使用定理 `eq_false`：∀ {p : Prop}, ¬p → p = False
· 使用定理 `Finsupp.sum_fun_zero`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10} [i
nst : Zero M] [inst_1 : AddCommMonoid N] (f : α →₀ M),   (f.sum fun x x_1 => 0) 
= 0
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma coeff_single_mul_of_forall_mul_ne (r : R) (x : R[M]) (h : ∀ d, m * d ≠ m') :
    (single m r * x).coeff m' = 0 := by classical simp [coeff_mul, h]

set_option backward.isDefEq.respectTransparency false in
@[to_additive (attr := deprecated coeff_mul_single_of_forall_mul_ne (since := "2026-06-18"))
  (dont_translate := R)]
/-
**MonoidAlgebra.mul_single_apply_of_not_exists_mul** 是 Mathlib 中的一个引理，位于命名空间 `Mo
noidAlgebra`。
形式化陈述：mul_single_apply_of_not_exists_mul (r : R) {g g' : M} (x : R[M]) (h : ¬exi
sts d, g' = d * g) : (x * single g r).coeff g' = 0
参数：r : R；x : R[M]；h : ¬exists d, g' = d * g。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.coeff_mul_single_of_forall_mul_ne`：coeff_mul_single_of_for
all_mul_ne (r : R) (x : R[M]) (h : forall d, d * m != m') : (x * single m r).coe
ff m' = 0
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
-/
lemma mul_single_apply_of_not_exists_mul (r : R) {g g' : M} (x : R[M])
    (h : ¬∃ d, g' = d * g) : (x * single g r).coeff g' = 0 :=
  coeff_mul_single_of_forall_mul_ne _ _ <| by simpa [eq_comm] using h

@[to_additive (attr := deprecated coeff_single_mul_of_forall_mul_ne (since := "2026-06-18"))
  (dont_translate := R)]
/-
**MonoidAlgebra.single_mul_apply_of_not_exists_mul** 是 Mathlib 中的一个引理，位于命名空间 `Mo
noidAlgebra`。
形式化陈述：single_mul_apply_of_not_exists_mul (r : R) {g g' : M} (x : R[M]) (h : ¬exi
sts d, g' = g * d) : (single g r * x).coeff g' = 0
参数：r : R；x : R[M]；h : ¬exists d, g' = g * d。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.coeff_single_mul_of_forall_mul_ne`：coeff_single_mul_of_for
all_mul_ne (r : R) (x : R[M]) (h : forall d, m * d != m') : (single m r * x).coe
ff m' = 0
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
-/
lemma single_mul_apply_of_not_exists_mul (r : R) {g g' : M} (x : R[M])
    (h : ¬∃ d, g' = g * d) : (single g r * x).coeff g' = 0 :=
  coeff_single_mul_of_forall_mul_ne _ _ <| by simpa [eq_comm] using h

variable (R M : Type*) [Semiring R] [Mul M] in
/-- The embedding of a magma into its magma algebra. -/
@[simps]
/-
**MonoidAlgebra.ofMagma** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：ofMagma : M ->ₙ* R[M] where toFun a
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The embedding of a magma into its magma algebra.
-/
def ofMagma : M →ₙ* R[M] where
  toFun a := single a 1
  map_mul' a b := by ext; simp [mul_def, Finsupp.sum_single_index]

end Mul

section Semigroup
variable [Semigroup M]

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.nonUnitalSemiring** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：nonUnitalSemiring : NonUnitalSemiring R[M] where mul_assoc
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance nonUnitalSemiring : NonUnitalSemiring R[M] where
  mul_assoc := by simp [mul_def, sum_sum_index, mul_add, add_mul, mul_assoc]

end Semigroup

section MulOneClass
variable [MulOneClass M]

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.nonAssocSemiring** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：nonAssocSemiring : NonAssocSemiring R[M] where natCast n
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance nonAssocSemiring : NonAssocSemiring R[M] where
  natCast n := single 1 n
  natCast_zero := by simp
  natCast_succ := by simp [one_def]
  one_mul := by simp [mul_def, one_def]
  mul_one := by simp [mul_def, one_def]

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.natCast_def** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：natCast_def (n : Nat) : (n : R[M]) = single 1 (n : R)
参数：n : Nat。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma natCast_def (n : ℕ) : (n : R[M]) = single 1 (n : R) := rfl

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.ofNat_def** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ofNat_def (n : Nat) [n.AtLeastTwo] : (ofNat(n) : R[M]) = single 1 ofNat(n)
参数：n : Nat。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma ofNat_def (n : ℕ) [n.AtLeastTwo] : (ofNat(n) : R[M]) = single 1 ofNat(n) := rfl

@[to_additive (dont_translate := R) (attr := simp)]
/-
**MonoidAlgebra.coeff_natCast** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_natCast (n : Nat) : (n : R[M]).coeff = .single 1 (n : R)
参数：n : Nat。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma coeff_natCast (n : ℕ) : (n : R[M]).coeff = .single 1 (n : R) := rfl

@[to_additive (dont_translate := R) (attr := simp)]
/-
**MonoidAlgebra.coeff_ofNat** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_ofNat (n : Nat) [n.AtLeastTwo] : (ofNat(n) : R[M]).coeff = .single 1
 ofNat(n)
参数：n : Nat。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma coeff_ofNat (n : ℕ) [n.AtLeastTwo] : (ofNat(n) : R[M]).coeff = .single 1 ofNat(n) := rfl

@[to_additive (dont_translate := R) coeff_mul_single_zero]
/-
**MonoidAlgebra.coeff_mul_single_one** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_mul_single_one (x : R[M]) (r : R) (m : M) : (x * single 1 r).coeff m
 = x.coeff m * r
参数：x : R[M]；r : R；m : M。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.coeff_mul_single_eq_coeff_mul`：coeff_mul_single_eq_coeff_m
ul (m₂ : M) (H : forall m' in x.coeff.support, m' * m = m₁ ↔ m' = m₂) : (x * sin
gle m r).coeff m₁ = x.coeff m₂ * …
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mul_one`：mul_one : forall a : M, a * 1 = a
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
-/
lemma coeff_mul_single_one (x : R[M]) (r : R) (m : M) :
    (x * single 1 r).coeff m = x.coeff m * r :=
  coeff_mul_single_eq_coeff_mul _ (by simp)

@[deprecated (since := "2026-06-18")] alias mul_single_one_apply := coeff_mul_single_one

@[to_additive (dont_translate := R) coeff_single_zero_mul]
/-
**MonoidAlgebra.coeff_single_one_mul** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_single_one_mul (x : R[M]) (r : R) (m : M) : (single 1 r * x).coeff m
 = r * x.coeff m
参数：x : R[M]；r : R；m : M。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.coeff_single_mul_eq_mul_coeff`：coeff_single_mul_eq_mul_coe
ff (m₂ : M) (H : forall m' in x.coeff.support, m * m' = m₁ ↔ m' = m₂) : (single 
m r * x).coeff m₁ = r * x.coeff m…
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `one_mul`：one_mul : forall a : M, 1 * a = a
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
-/
lemma coeff_single_one_mul (x : R[M]) (r : R) (m : M) :
    (single 1 r * x).coeff m = r * x.coeff m :=
  x.coeff_single_mul_eq_mul_coeff _ (by simp)

@[deprecated (since := "2026-06-18")] alias single_one_mul_apply := coeff_single_one_mul

variable (R M : Type*) [Semiring R] [MulOneClass M] in
/-- The embedding of a unital magma into its magma algebra. -/
@[simps! apply]
/-
**MonoidAlgebra.of** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：of : M ->* R[M] where __
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The embedding of a unital magma into its magma algebra.
-/
def of : M →* R[M] where
  __ := ofMagma R M
  map_one' := rfl
/-
**MonoidAlgebra.of_injective** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：of_injective [Nontrivial R] : Function.Injective (of R M)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MonoidAlgebra.of_apply`：∀ (R : Type u_8) (M : Type u_9) [inst : Semiring
 R] [inst_1 : MulOneClass M] (a : M),   (MonoidAlgebra.of R M) a = MonoidAlgebra
.single a 1
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `and_true`：∀ (p : Prop), (p ∧ True) = p
· 使用定理 `and_self`：∀ (p : Prop), (p ∧ p) = p
· 使用定理 `or_false`：∀ (p : Prop), (p ∨ False) = p
-/
lemma of_injective [Nontrivial R] : Function.Injective (of R M) := fun a b h ↦ by
  simpa [← coeff_inj, Finsupp.single_eq_single_iff] using h
/-
**MonoidAlgebra.of_commute** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：of_commute (h : forall m', Commute m m') (f : R[M]) : Commute (of R M m) f
参数：h : forall m', Commute m m'；f : R[M]。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.single_commute`：single_commute (hm : forall m', Commute m 
m') (hr : forall r', Commute r r') (x : R[M]) : Commute (single m r) x
· 使用定理 `Commute.one_left`：one_left (a : M) : Commute 1 a
-/
lemma of_commute (h : ∀ m', Commute m m') (f : R[M]) : Commute (of R M m) f :=
  single_commute h .one_left f

/-- `MonoidAlgebra.single` as a `MonoidHom` from the product type into the monoid algebra.

Note the order of the elements of the product are reversed compared to the arguments of
`MonoidAlgebra.single`. -/
@[simps]
/-
**MonoidAlgebra.singleHom** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：singleHom : R × M ->* R[M] where toFun a
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`MonoidAlgebra.single` as a `MonoidHom` from the product type into the monoid al
gebra.

Note the order of the elements of the product are reversed compared to the argum
ents of
`MonoidAlgebra.single`.
-/
def singleHom : R × M →* R[M] where
  toFun a := single a.2 a.1
  map_one' := rfl
  map_mul' _a _b := by simp

/-- `MonoidAlgebra.single 1` as a `RingHom` -/
@[to_additive (attr := simps) (dont_translate := R)
/-- `AddMonoidAlgebra.single 1` as a `RingHom` -/]
/-
**MonoidAlgebra.singleOneRingHom** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：singleOneRingHom : R ->+* R[M] where __
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
def singleOneRingHom : R →+* R[M] where
  __ := singleAddHom 1
  map_one' := rfl
  map_mul' := by simp

/-- If two ring homomorphisms from `R[M]` are equal on all `single m 1` and
`single 1 r`, then they are equal. -/
@[to_additive (dont_translate := R S)
/-- If two ring homomorphisms from `R[M]` are equal on all `single m 1` and `single 0 r`,
then they are equal. -/]
/-
**MonoidAlgebra.ringHom_ext** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ringHom_ext [Semiring S] {f g : R[M] ->+* S} (h₁ : forall r, f (single 1 r
) = g (single 1 r)) (h_of : forall m, f (single m 1) = g (single m 1)) : f = g
参数：h₁ : forall r, f (single 1 r) = g (single 1 r)；h_of : forall m, f (single m 1
) = g (single m 1)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `RingHom.coe_addMonoidHom_injective`：coe_addMonoidHom_injective : Injecti
ve (fun f : α ->+* β => (f : α ->+ β))
· 使用引理 `MonoidAlgebra.addMonoidHom_ext`：addMonoidHom_ext [AddZeroClass N] ⦃f g :
 R[M] ->+ N⦄ (h : forall m r, f (single m r) = g (single m r)) : f = g
· 使用定理 `RingHomClass.toAddMonoidHomClass`：∀ {F : Type u_5} {α : outParam (Type u
_6)} {β : outParam (Type u_7)} {inst : NonAssocSemiring α}   {inst_1 : NonAssocS
emiring β} {inst_2 : F…
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `NonUnitalRingHomClass.toMulHomClass`：∀ {F : Type u_5} {α : outParam (Typ
e u_6)} {β : outParam (Type u_7)} {inst : NonUnitalNonAssocSemiring α}   {inst_1
 : NonUnitalNonAssocSemir…
· 使用定理 `RingHomClass.toNonUnitalRingHomClass`：∀ {F : Type u_1} {α : Type u_2} {β
 : Type u_3} [inst : FunLike F α β] {x : NonAssocSemiring α}   {x_1 : NonAssocSe
miring β} [RingHomClass F …
· 使用引理 `MonoidAlgebra.single_mul_single`：single_mul_single (m₁ m₂ : M) (r₁ r₂ : 
R) : single m₁ r₁ * single m₂ r₂ = single (m₁ * m₂) (r₁ * r₂)
· 使用定理 `one_mul`：one_mul : forall a : M, 1 * a = a
· 使用定理 `mul_one`：mul_one : forall a : M, a * 1 = a
-/
lemma ringHom_ext [Semiring S] {f g : R[M] →+* S}
    (h₁ : ∀ r, f (single 1 r) = g (single 1 r)) (h_of : ∀ m, f (single m 1) = g (single m 1)) :
    f = g :=
  RingHom.coe_addMonoidHom_injective <| addMonoidHom_ext fun m r ↦ by
    simpa [← map_mul] using! congr($(h₁ r) * $(h_of m))

/-- If two ring homomorphisms from `R[M]` are equal on all `single m 1`
and `single 1 r`, then they are equal.

See note [partially-applied ext lemmas]. -/
@[ext high]
/-
**MonoidAlgebra.ringHom_ext'** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ringHom_ext' [Semiring S] {f g : R[M] ->+* S} (h₁ : f.comp singleOneRingHo
m = g.comp singleOneRingHom) (h_of : (f : R[M] ->* S).comp (of R M) = (g : R[M] 
->* S).comp (of R M)) : f = g
参数：h₁ : f.comp singleOneRingHom = g.comp singleOneRingHom；h_of : (f : R[M] ->* S
).comp (of R M) = (g : R[M] ->* S).comp (of R M)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用引理 `MonoidAlgebra.ringHom_ext`：ringHom_ext [Semiring S] {f g : R[M] ->+* S} 
(h₁ : forall r, f (single 1 r) = g (single 1 r)) (h_of : forall m, f (single m 1
) = g (single m…
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MonoidAlgebra.singleOneRingHom_apply`：∀ {R : Type u_1} {M : Type u_4} [i
nst : Semiring R] [inst_1 : MulOneClass M] (a : R),   MonoidAlgebra.singleOneRin
gHom a = (↑(MonoidAlgebra.…
· 使用定理 `MonoidAlgebra.singleAddHom_apply`：∀ {R : Type u_1} {M : Type u_4} [inst 
: Semiring R] (m : M) (r : R),   (MonoidAlgebra.singleAddHom m) r = MonoidAlgebr
a.single m r
· 使用定理 `MonoidAlgebra.of_apply`：∀ (R : Type u_8) (M : Type u_9) [inst : Semiring
 R] [inst_1 : MulOneClass M] (a : M),   (MonoidAlgebra.of R M) a = MonoidAlgebra
.single a 1

--- 原说明 ---
If two ring homomorphisms from `R[M]` are equal on all `single m 1`
and `single 1 r`, then they are equal.

See note [partially-applied ext lemmas].
-/
lemma ringHom_ext' [Semiring S] {f g : R[M] →+* S}
    (h₁ : f.comp singleOneRingHom = g.comp singleOneRingHom)
    (h_of : (f : R[M] →* S).comp (of R M) =
      (g : R[M] →* S).comp (of R M)) : f = g :=
  ringHom_ext (by simpa [DFunLike.ext_iff] using h₁) (by simpa [DFunLike.ext_iff] using h_of)

end MulOneClass

section Monoid
variable [Monoid M] [Monoid N]

@[to_additive]
/-
**MonoidAlgebra.semiring** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：{R : Type u_1} → {M : Type u_4} → [inst : Semiring R] → [Monoid M] → Semir
ing (MonoidAlgebra R M)
参数：MonoidAlgebra R M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance semiring : Semiring R[M] where

@[to_additive (attr := simp) (dont_translate := R) single_pow]
/-
**MonoidAlgebra.single_pow** 是 Mathlib 中的一个定理，位于命名空间 `MonoidAlgebra`。
形式化陈述：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] [inst_1 : Monoid M] (m
 : M) (r : R) (n : ℕ),   MonoidAlgebra.single m r ^ n = MonoidAlgebra.single (m 
^ n) (r ^ n)
参数：m : M；r : R；n : ℕ；m ^ n；r ^ n。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma single_pow (m : M) (r : R) : ∀ n : ℕ, single m r ^ n = single (m ^ n) (r ^ n)
  | 0 => by simp [one_def]
  | n + 1 => by simp [pow_succ, single_pow _ _ n]
/-
**MonoidAlgebra.induction_on** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：induction_on {motive : R[M] -> Prop} (x : R[M]) (of : forall m, motive (.o
f R M m)) (add : forall x y : R[M], motive x -> motive y -> motive (x + y)) (smu
l : forall (r : R) (x), motive x -> motive (r • x)) : motive x
参数：x : R[M]；of : forall m, motive (.of R M m)；add : forall x y : R[M], motive x 
-> motive y -> motive (x + y)；smul : forall (r : R) (x), motive x -> motive (r •
 x)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `Finsupp.induction_linear`：induction_linear {motive : (ι ->₀ M) -> Prop} 
(f : ι ->₀ M) (zero : motive 0) (add : forall f g : ι ->₀ M, motive f -> motive 
g -> motive (f…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `MonoidAlgebra.of_apply`：∀ (R : Type u_8) (M : Type u_9) [inst : Semiring
 R] [inst_1 : MulOneClass M] (a : M),   (MonoidAlgebra.of R M) a = MonoidAlgebra
.single a 1
· 使用引理 `MonoidAlgebra.smul_single`：smul_single (a : A) (m : M) (r : R) : a • sin
gle m r = single m (a • r)
· 使用定理 `mul_one`：mul_one : forall a : M, a * 1 = a
· 使用引理 `MonoidAlgebra.single_zero`：single_zero (m : M) : (single m 0 : R[M]) = 0
-/
lemma induction_on {motive : R[M] → Prop} (x : R[M])
    (of : ∀ m, motive (.of R M m)) (add : ∀ x y : R[M], motive x → motive y → motive (x + y))
    (smul : ∀ (r : R) (x), motive x → motive (r • x)) : motive x :=
  Finsupp.induction_linear (motive := fun x ↦ motive <| ofCoeff x) x.coeff
    (by simpa using smul 0 (.of R M 1) (of 1))
    (fun x y hf hg ↦ add (ofCoeff x) (ofCoeff y) hf hg)
    fun m r ↦ by simpa using smul r (.of R M m) (of m)

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.isLocalHom_singleOneRingHom** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlg
ebra`。
形式化陈述：isLocalHom_singleOneRingHom : IsLocalHom (singleOneRingHom (R
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `MonoidAlgebra.singleOneRingHom_apply`：∀ {R : Type u_1} {M : Type u_4} [i
nst : Semiring R] [inst_1 : MulOneClass M] (a : R),   MonoidAlgebra.singleOneRin
gHom a = (↑(MonoidAlgebra.…
· 使用定理 `MonoidAlgebra.singleAddHom_apply`：∀ {R : Type u_1} {M : Type u_4} [inst 
: Semiring R] (m : M) (r : R),   (MonoidAlgebra.singleAddHom m) r = MonoidAlgebr
a.single m r
· 使用引理 `MonoidAlgebra.coeff_single_one_mul`：coeff_single_one_mul (x : R[M]) (r :
 R) (m : M) : (single 1 r * x).coeff m = r * x.coeff m
· 使用引理 `MonoidAlgebra.coeff_one_one`：coeff_one_one : (1 : R[M]).coeff 1 = 1
· 使用引理 `MonoidAlgebra.coeff_mul_single_one`：coeff_mul_single_one (x : R[M]) (r :
 R) (m : M) : (x * single 1 r).coeff m = x.coeff m * r
-/
instance isLocalHom_singleOneRingHom : IsLocalHom (singleOneRingHom (R := R) (M := M)) where
  map_nonunit := by
    simp_rw [isUnit_iff_exists]
    rintro a ⟨x, hax, hxa⟩
    refine ⟨x.coeff 1, ?_, ?_⟩
    · simpa [coeff_single_one_mul] using congr(($hax).coeff 1)
    · simpa [coeff_mul_single_one] using congr(($hxa).coeff 1)

variable (M) in
/-- The trivial monoid algebra is the base ring. -/
@[to_additive (dont_translate := R) (attr := simps! apply)
/-- The trivial additive monoid algebra is the base ring. -/]
/-
**MonoidAlgebra.uniqueRingEquiv** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：uniqueRingEquiv [Subsingleton M] : R[M] ≃+* R where toAddEquiv
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
def uniqueRingEquiv [Subsingleton M] : R[M] ≃+* R where
  toAddEquiv := coeffAddEquiv.trans <| Finsupp.uniqueAddEquiv 1
  map_mul' x y := by
    let : Unique M := ⟨⟨1⟩, fun _ ↦ Subsingleton.elim ..⟩
    refine (coeff_mul ..).trans ?_
    simp [Finsupp.sum_unique, Unique.eq_default]

set_option backward.isDefEq.respectTransparency.types false in
variable (M) in
@[to_additive (dont_translate := R) (attr := simp)]
/-
**MonoidAlgebra.uniqueRingEquiv_symm_apply** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlge
bra`。
形式化陈述：uniqueRingEquiv_symm_apply [Subsingleton M] (r : R) : (uniqueRingEquiv M).
symm r = single 1 r
参数：r : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidAlgebra.ext`：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] {
x y : MonoidAlgebra R M}, x.coeff = y.coeff → x = y
· 使用定理 `Finsupp.ext`：ext {f g : α ->₀ M} (h : forall a, f a = g a) : f = g
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Finsupp.uniqueAddEquiv_symm_apply`：∀ {ι : Type u_1} {M : Type u_3} [inst
 : AddZeroClass M] (i : ι) [inst_1 : Subsingleton ι] (b : M),   (Finsupp.uniqueA
ddEquiv i).symm b = fun…
· 使用定理 `MonoidAlgebra.coeffAddEquiv_symm_apply`：∀ {R : Type u_1} {M : Type u_4} 
[inst : Semiring R] (coeff : M →₀ R),   MonoidAlgebra.coeffAddEquiv.symm coeff =
 MonoidAlgebra.ofCoeff coeff
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma uniqueRingEquiv_symm_apply [Subsingleton M] (r : R) :
    (uniqueRingEquiv M).symm r = single 1 r := by ext; simp [uniqueRingEquiv]

-- We want this lemma to fire before `uniqueRingEquiv_symm_apply`.
@[to_additive (dont_translate := R) (attr := simp↓ high)]
/-
**MonoidAlgebra.coeff_uniqueRingEquiv_symm** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlge
bra`。
形式化陈述：coeff_uniqueRingEquiv_symm [Subsingleton M] (r : R) (m : M) : ((uniqueRing
Equiv M).symm r).coeff m = r
参数：r : R；m : M。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用引理 `MonoidAlgebra.uniqueRingEquiv_symm_apply`：uniqueRingEquiv_symm_apply [Su
bsingleton M] (r : R) : (uniqueRingEquiv M).symm r = single 1 r
· 使用定理 `Subsingleton.elim`：∀ {α : Sort u} [h : Subsingleton α] (a b : α), a = b
· 使用定理 `Finsupp.single_eq_same`：single_eq_same : (single a b : α ->₀ M) a = b
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma coeff_uniqueRingEquiv_symm [Subsingleton M] (r : R) (m : M) :
    ((uniqueRingEquiv M).symm r).coeff m = r := by simp [Subsingleton.elim m 1]

@[deprecated (since := "2026-06-18")]
alias uniqueRingEquiv_symm_apply_apply := coeff_uniqueRingEquiv_symm

/-- A product monoid algebra is a nested monoid algebra. -/
@[to_additive (dont_translate := R)
/-- An additive product monoid algebra is a nested additive monoid algebra. -/]
/-
**MonoidAlgebra.curryAddEquiv** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：curryAddEquiv : R[M × N] ≃+ R[N][M]
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
def curryAddEquiv : R[M × N] ≃+ R[N][M] :=
  coeffAddEquiv.trans <| .trans Finsupp.curryAddEquiv <| .trans
    (Finsupp.mapRange.addEquiv coeffAddEquiv.symm) coeffAddEquiv.symm

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.curryAddEquiv_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：curryAddEquiv_single (m : M) (n : N) (r : R) : curryAddEquiv (single (m, n
) r) = single m (single n r)
参数：m : M；n : N；r : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MonoidAlgebra.coeffAddEquiv_apply`：∀ {R : Type u_1} {M : Type u_4} [inst
 : Semiring R] (self : MonoidAlgebra R M),   MonoidAlgebra.coeffAddEquiv self = 
self.coeff
· 使用引理 `Finsupp.curry_single`：curry_single (a : α × β) (m : M) : (single a m).cu
rry = single a.1 (single a.2 m)
· 使用定理 `Finsupp.mapRange.addEquiv_apply`：∀ {ι : Type u_1} {M : Type u_3} {N : Ty
pe u_4} [inst : AddCommMonoid M] [inst_1 : AddCommMonoid N] (em' : M ≃+ N)   (g 
: ι →₀ M), (Finsupp.m…
· 使用定理 `Finsupp.mapRange_single`：mapRange_single {f : M -> N} {hf : f 0 = 0} {a 
: α} {b : M} : mapRange f hf (single a b) = single a (f b)
· 使用定理 `MonoidAlgebra.coeffAddEquiv_symm_apply`：∀ {R : Type u_1} {M : Type u_4} 
[inst : Semiring R] (coeff : M →₀ R),   MonoidAlgebra.coeffAddEquiv.symm coeff =
 MonoidAlgebra.ofCoeff coeff
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma curryAddEquiv_single (m : M) (n : N) (r : R) :
    curryAddEquiv (single (m, n) r) = single m (single n r) := by simp [curryAddEquiv]

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.curryAddEquiv_symm_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgeb
ra`。
形式化陈述：curryAddEquiv_symm_single (m : M) (n : N) (r : R) : curryAddEquiv.symm (si
ngle m <| single n r) = (single (m, n) r)
参数：m : M；n : N；r : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MonoidAlgebra.coeffAddEquiv_apply`：∀ {R : Type u_1} {M : Type u_4} [inst
 : Semiring R] (self : MonoidAlgebra R M),   MonoidAlgebra.coeffAddEquiv self = 
self.coeff
· 使用定理 `Finsupp.mapRange.addEquiv_apply`：∀ {ι : Type u_1} {M : Type u_3} {N : Ty
pe u_4} [inst : AddCommMonoid M] [inst_1 : AddCommMonoid N] (em' : M ≃+ N)   (g 
: ι →₀ M), (Finsupp.m…
· 使用定理 `Finsupp.mapRange_single`：mapRange_single {f : M -> N} {hf : f 0 = 0} {a 
: α} {b : M} : mapRange f hf (single a b) = single a (f b)
· 使用定理 `Finsupp.curryAddEquiv_symm_apply`：∀ {α : Type u_1} {β : Type u_2} {M : T
ype u_5} [inst : AddZeroClass M] (f : α →₀ β →₀ M),   Finsupp.curryAddEquiv.symm
 f = f.uncurry
· 使用引理 `Finsupp.uncurry_single`：uncurry_single (a : α) (b : β) (m : M) : (single
 a (single b m)).uncurry = single (a, b) m
· 使用定理 `MonoidAlgebra.coeffAddEquiv_symm_apply`：∀ {R : Type u_1} {M : Type u_4} 
[inst : Semiring R] (coeff : M →₀ R),   MonoidAlgebra.coeffAddEquiv.symm coeff =
 MonoidAlgebra.ofCoeff coeff
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma curryAddEquiv_symm_single (m : M) (n : N) (r : R) :
    curryAddEquiv.symm (single m <| single n r) = (single (m, n) r) := by simp [curryAddEquiv]

/-- A product monoid algebra is a nested monoid algebra. -/
@[to_additive (dont_translate := R)
/-- An additive product monoid algebra is a nested additive monoid algebra. -/]
/-
**MonoidAlgebra.curryRingEquiv** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：curryRingEquiv : R[M × N] ≃+* R[N][M] where toAddEquiv
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
def curryRingEquiv : R[M × N] ≃+* R[N][M] where
  toAddEquiv := curryAddEquiv
  map_mul' := curryAddEquiv (M := M) (N := N).toAddMonoidHom.map_mul_iff.2 <| by
    ext ⟨m₁, n₁⟩ r₁ ⟨m₂, n₂⟩ r₂; simp

@[to_additive (dont_translate := R) (attr := simp)]
/-
**MonoidAlgebra.curryRingEquiv_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：curryRingEquiv_single (m : M) (n : N) (r : R) : curryRingEquiv (single (m,
 n) r) = single m (single n r)
参数：m : M；n : N；r : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `MonoidAlgebra.curryAddEquiv_single`：curryAddEquiv_single (m : M) (n : N)
 (r : R) : curryAddEquiv (single (m, n) r) = single m (single n r)
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma curryRingEquiv_single (m : M) (n : N) (r : R) :
    curryRingEquiv (single (m, n) r) = single m (single n r) := by simp [curryRingEquiv]

set_option backward.isDefEq.respectTransparency.types false in
@[to_additive (attr := simp)]
/-
**MonoidAlgebra.curryRingEquiv_symm_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlge
bra`。
形式化陈述：curryRingEquiv_symm_single (m : M) (n : N) (r : R) : curryRingEquiv.symm (
single m <| single n r) = (single (m, n) r)
参数：m : M；n : N；r : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `MonoidAlgebra.curryAddEquiv_symm_single`：curryAddEquiv_symm_single (m : 
M) (n : N) (r : R) : curryAddEquiv.symm (single m <| single n r) = (single (m, n
) r)
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma curryRingEquiv_symm_single (m : M) (n : N) (r : R) :
    curryRingEquiv.symm (single m <| single n r) = (single (m, n) r) := by
  simp [curryRingEquiv]

variable [IsCancelMul M]

@[to_additive (dont_translate := R) (attr := simp) coeff_mul_single_add]
/-
**MonoidAlgebra.coeff_mul_single_mul** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_mul_single_mul (x : R[M]) (r : R) (m m' : M) : (x * single m r).coef
f (m' * m) = x.coeff m' * r
参数：x : R[M]；r : R；m m' : M。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.coeff_mul_single_eq_coeff_mul`：coeff_mul_single_eq_coeff_m
ul (m₂ : M) (H : forall m' in x.coeff.support, m' * m = m₁ ↔ m' = m₂) : (x * sin
gle m r).coeff m₁ = x.coeff m₂ * …
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `IsCancelMul.toIsRightCancelMul`：∀ {G : Type u} {inst : Mul G} [self : Is
CancelMul G], IsRightCancelMul G
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
-/
lemma coeff_mul_single_mul (x : R[M]) (r : R) (m m' : M) :
    (x * single m r).coeff (m' * m) = x.coeff m' * r :=
  coeff_mul_single_eq_coeff_mul _ (by simp)

@[to_additive (dont_translate := R) (attr := simp) coeff_single_mul_add]
/-
**MonoidAlgebra.coeff_single_mul_mul** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_single_mul_mul (x : R[M]) (r : R) (m m' : M) : (single m r * x).coef
f (m * m') = r * x.coeff m'
参数：x : R[M]；r : R；m m' : M。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.coeff_single_mul_eq_mul_coeff`：coeff_single_mul_eq_mul_coe
ff (m₂ : M) (H : forall m' in x.coeff.support, m * m' = m₁ ↔ m' = m₂) : (single 
m r * x).coeff m₁ = r * x.coeff m…
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `IsCancelMul.toIsLeftCancelMul`：∀ {G : Type u} {inst : Mul G} [self : IsC
ancelMul G], IsLeftCancelMul G
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
-/
lemma coeff_single_mul_mul (x : R[M]) (r : R) (m m' : M) :
    (single m r * x).coeff (m * m') = r * x.coeff m' :=
  x.coeff_single_mul_eq_mul_coeff _ (by simp)

end Monoid

section Group
variable [Group G]

@[to_additive (attr := simp) (dont_translate := R) coeff_mul_single_apply]
/-
**MonoidAlgebra.coeff_mul_single_apply** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`
。
形式化陈述：coeff_mul_single_apply (x : R[G]) (r : R) (g h : G) : (x * single g r).coe
ff h = x.coeff (h * g⁻¹) * r
参数：x : R[G]；r : R；g h : G。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.coeff_mul_single_eq_coeff_mul`：coeff_mul_single_eq_coeff_m
ul (m₂ : M) (H : forall m' in x.coeff.support, m' * m = m₁ ↔ m' = m₂) : (x * sin
gle m r).coeff m₁ = x.coeff m₂ * …
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
-/
lemma coeff_mul_single_apply (x : R[G]) (r : R) (g h : G) :
    (x * single g r).coeff h = x.coeff (h * g⁻¹) * r :=
  coeff_mul_single_eq_coeff_mul _ <| by simp [eq_mul_inv_iff_mul_eq]

@[deprecated (since := "2026-06-18")] alias mul_single_apply := coeff_mul_single_apply

set_option backward.isDefEq.respectTransparency false in
@[to_additive (attr := simp) (dont_translate := R) coeff_single_mul_apply]
/-
**MonoidAlgebra.coeff_single_mul_apply** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`
。
形式化陈述：coeff_single_mul_apply (x : R[G]) (r : R) (g h : G) : (single g r * x).coe
ff h = r * x.coeff (g⁻¹ * h)
参数：x : R[G]；r : R；g h : G。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.coeff_single_mul_eq_mul_coeff`：coeff_single_mul_eq_mul_coe
ff (m₂ : M) (H : forall m' in x.coeff.support, m * m' = m₁ ↔ m' = m₂) : (single 
m r * x).coeff m₁ = r * x.coeff m…
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
-/
lemma coeff_single_mul_apply (x : R[G]) (r : R) (g h : G) :
    (single g r * x).coeff h = r * x.coeff (g⁻¹ * h) :=
  coeff_single_mul_eq_mul_coeff _ <| by simp [eq_inv_mul_iff_mul_eq]

@[deprecated (since := "2026-06-18")] alias single_mul_apply := coeff_single_mul_apply

set_option backward.isDefEq.respectTransparency false in
@[to_additive (dont_translate := R) coeff_mul_apply_left]
/-
**MonoidAlgebra.coeff_mul_apply_left** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_mul_apply_left (x y : R[G]) (g : G) : (x * y).coeff g = x.coeff.sum 
fun h r => r * y.coeff (h⁻¹ * g)
参数：x y : R[G]；g : G。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `MonoidAlgebra.coeff_mul`：coeff_mul [DecidableEq M] (x y : R[M]) (m : M) 
: (x * y).coeff m = x.coeff.sum fun m₁ r₁ => y.coeff.sum fun m₂ r₂ => if m₁ * m₂
 = m then r₁ …
· 使用定理 `Finsupp.sum_congr`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10} [inst
 : Zero M] [inst_1 : AddCommMonoid N] {f : α →₀ M}   {g1 g2 : α → M → N}, (∀ x ∈
 f.supp…
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `ite_congr`：∀ {α : Sort u_1} {b c : Prop} {x y u v : α} {s : Decidable b}
 [inst : Decidable c],   b = c → (c → x = u) → (¬c → y = v) → (if b then x else…
· 使用定理 `Finsupp.sum_ite_eq'`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10} [in
st : Zero M] [inst_1 : AddCommMonoid N] [inst_2 : DecidableEq α]   (f : α →₀ M) 
(a : α) (…
· 使用定理 `ite_not`：∀ {α : Sort u_1} (p : Prop) [inst : Decidable p] (x y : α), (if
 ¬p then x else y) = if p then y else x
· 使用定理 `implies_congr_ctx`：∀ {p₁ p₂ q₁ q₂ : Prop}, p₁ = p₂ → (p₂ → q₁ = q₂) → (p
₁ → q₁) = (p₂ → q₂)
· 使用定理 `MulZeroClass.mul_zero`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, a * 0 = 0
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
-/
lemma coeff_mul_apply_left (x y : R[G]) (g : G) :
    (x * y).coeff g = x.coeff.sum fun h r ↦ r * y.coeff (h⁻¹ * g) := by
  classical rw [coeff_mul]; gcongr; simp +contextual [← eq_inv_mul_iff_mul_eq]

@[deprecated (since := "2026-06-18")] alias mul_apply_left := coeff_mul_apply_left

set_option backward.isDefEq.respectTransparency false in
@[to_additive (dont_translate := R) coeff_mul_apply_right]
/-
**MonoidAlgebra.coeff_mul_apply_right** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_mul_apply_right (x y : R[G]) (g : G) : (x * y).coeff g = y.coeff.sum
 fun h r => x.coeff (g * h⁻¹) * r
参数：x y : R[G]；g : G。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `MonoidAlgebra.coeff_mul`：coeff_mul [DecidableEq M] (x y : R[M]) (m : M) 
: (x * y).coeff m = x.coeff.sum fun m₁ r₁ => y.coeff.sum fun m₂ r₂ => if m₁ * m₂
 = m then r₁ …
· 使用定理 `Finsupp.sum_comm`：∀ {α : Type u_1} {β : Type u_7} {M : Type u_8} {M' : T
ype u_9} {N : Type u_10} [inst : Zero M] [inst_1 : Zero M']   [inst_2 : AddCommM
onoid …
· 使用定理 `Finsupp.sum_congr`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10} [inst
 : Zero M] [inst_1 : AddCommMonoid N] {f : α →₀ M}   {g1 g2 : α → M → N}, (∀ x ∈
 f.supp…
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `ite_congr`：∀ {α : Sort u_1} {b c : Prop} {x y u v : α} {s : Decidable b}
 [inst : Decidable c],   b = c → (c → x = u) → (¬c → y = v) → (if b then x else…
· 使用定理 `Finsupp.sum_ite_eq'`：∀ {α : Type u_1} {M : Type u_8} {N : Type u_10} [in
st : Zero M] [inst_1 : AddCommMonoid N] [inst_2 : DecidableEq α]   (f : α →₀ M) 
(a : α) (…
· 使用定理 `ite_not`：∀ {α : Sort u_1} (p : Prop) [inst : Decidable p] (x y : α), (if
 ¬p then x else y) = if p then y else x
· 使用定理 `implies_congr_ctx`：∀ {p₁ p₂ q₁ q₂ : Prop}, p₁ = p₂ → (p₂ → q₁ = q₂) → (p
₁ → q₁) = (p₂ → q₂)
· 使用定理 `MulZeroClass.zero_mul`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, 0 * a = 0
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
-/
lemma coeff_mul_apply_right (x y : R[G]) (g : G) :
    (x * y).coeff g = y.coeff.sum fun h r ↦ x.coeff (g * h⁻¹) * r := by
  classical rw [coeff_mul, Finsupp.sum_comm]; gcongr; simp +contextual [← eq_mul_inv_iff_mul_eq]

@[deprecated (since := "2026-06-18")] alias mul_apply_right := coeff_mul_apply_right

end Group
end Semiring

section CommSemiring
variable [CommSemiring R]

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.nonUnitalCommSemiring** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：nonUnitalCommSemiring [CommSemigroup M] : NonUnitalCommSemiring R[M] where
 mul_comm f g
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance nonUnitalCommSemiring [CommSemigroup M] : NonUnitalCommSemiring R[M] where
  mul_comm f g := by simp [mul_def, Finsupp.sum, mul_comm, f.coeff.support.sum_comm]

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.single_one_comm** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_one_comm [MulOneClass M] (r : R) (f : R[M]) : single (1 : M) r * f 
= f * single (1 : M) r
参数：r : R；f : R[M]。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.single_commute`：single_commute (hm : forall m', Commute m 
m') (hr : forall r', Commute r r') (x : R[M]) : Commute (single m r) x
· 使用定理 `Commute.one_left`：one_left (a : M) : Commute 1 a
· 使用定理 `Commute.all`：∀ {S : Type u_3} [inst : CommMagma S] (a b : S), Commute a 
b
-/
lemma single_one_comm [MulOneClass M] (r : R) (f : R[M]) :
    single (1 : M) r * f = f * single (1 : M) r :=
  single_commute .one_left (.all _) f

section CommMonoid
variable [CommMonoid M]

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.commSemiring** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：{R : Type u_1} → {M : Type u_4} → [inst : CommSemiring R] → [CommMonoid M]
 → CommSemiring (MonoidAlgebra R M)
参数：MonoidAlgebra R M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance commSemiring : CommSemiring R[M] where

open Finset in
@[to_additive (dont_translate := R) (attr := simp) prod_single]
/-
**MonoidAlgebra.prod_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：prod_single (s : Finset ι) (m : ι -> M) (r : ι -> R) : ∏ i in s, single (m
 i) (r i) = single (∏ i in s, m i) (∏ i in s, r i)
参数：s : Finset ι；m : ι -> M；r : ι -> R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Finset.cons_induction_on`：cons_induction_on {α : Type*} {motive : Finset
 α -> Prop} (s : Finset α) (empty : motive ∅) (cons : forall (a : α) (s : Finset
 α) (h : a ∉ s…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Finset.prod_cons`：prod_cons (h : a ∉ s) : ∏ x in cons a s h, f x = f a *
 ∏ x in s, f x
· 使用引理 `MonoidAlgebra.single_mul_single`：single_mul_single (m₁ m₂ : M) (r₁ r₂ : 
R) : single m₁ r₁ * single m₂ r₂ = single (m₁ * m₂) (r₁ * r₂)
-/
lemma prod_single (s : Finset ι) (m : ι → M) (r : ι → R) :
    ∏ i ∈ s, single (m i) (r i) = single (∏ i ∈ s, m i) (∏ i ∈ s, r i) :=
  Finset.cons_induction_on s rfl fun i s hi ih ↦ by
    rw [prod_cons, ih, single_mul_single, prod_cons, prod_cons]

open Finset in
@[to_additive (dont_translate := R) (attr := simp) finsuppProd_single]
/-
**MonoidAlgebra.finsuppProd_single** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：finsuppProd_single [AddCommMonoid N] (f : ι ->₀ N) (m : ι -> N -> M) (r : 
ι -> N -> R) : f.prod (fun i n => single (m i n) (r i n)) = single (f.prod m) (f
.prod r)
参数：f : ι ->₀ N；m : ι -> N -> M；r : ι -> N -> R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MonoidAlgebra.prod_single`：prod_single (s : Finset ι) (m : ι -> M) (r : 
ι -> R) : ∏ i in s, single (m i) (r i) = single (∏ i in s, m i) (∏ i in s, r i)
-/
lemma finsuppProd_single [AddCommMonoid N] (f : ι →₀ N) (m : ι → N → M) (r : ι → N → R) :
    f.prod (fun i n ↦ single (m i n) (r i n)) = single (f.prod m) (f.prod r) := prod_single ..

end CommMonoid
end CommSemiring

section Ring
variable [Ring R]

@[to_additive (dont_translate := R) addCommGroup]
/-
**MonoidAlgebra.addCommGroup** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：addCommGroup : AddCommGroup R[M]
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance addCommGroup : AddCommGroup R[M] := fast_instance% coeffEquiv.addCommGroup

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.coeff_neg** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_neg (x : R[M]) : coeff (-x) = -coeff x
参数：x : R[M]。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma coeff_neg (x : R[M]) : coeff (-x) = -coeff x := rfl

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.ofCoeff_neg** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ofCoeff_neg (x : M ->₀ R) : ofCoeff (-x) = -ofCoeff x
参数：x : M ->₀ R。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma ofCoeff_neg (x : M →₀ R) : ofCoeff (-x) = -ofCoeff x := rfl

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.coeff_sub** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：coeff_sub (x y : R[M]) : coeff (x - y) = coeff x - coeff y
参数：x y : R[M]。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma coeff_sub (x y : R[M]) : coeff (x - y) = coeff x - coeff y := rfl

@[to_additive (attr := simp)]
/-
**MonoidAlgebra.ofCoeff_sub** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：ofCoeff_sub (x y : M ->₀ R) : ofCoeff (x - y) = ofCoeff x - ofCoeff y
参数：x y : M ->₀ R。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma ofCoeff_sub (x y : M →₀ R) : ofCoeff (x - y) = ofCoeff x - ofCoeff y := rfl

@[to_additive (attr := simp) (dont_translate := R)]
/-
**MonoidAlgebra.single_neg** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_neg (m : M) (r : R) : single m (-r) = -single m r
参数：m : M；r : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidAlgebra.ext`：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] {
x y : MonoidAlgebra R M}, x.coeff = y.coeff → x = y
· 使用定理 `Finsupp.ext`：ext {f g : α ->₀ M} (h : forall a, f a = g a) : f = g
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `Finsupp.single_neg`：single_neg (a : ι) (b : G) : single a (-b) = -single
 a b
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma single_neg (m : M) (r : R) : single m (-r) = -single m r := by ext; simp

@[to_additive (attr := simp) (dont_translate := R)]
/-
**MonoidAlgebra.single_sub** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：single_sub (m : M) (r s : R) : single m (r - s) = single m r - single m s
参数：m : M；r s : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidAlgebra.ext`：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] {
x y : MonoidAlgebra R M}, x.coeff = y.coeff → x = y
· 使用定理 `Finsupp.ext`：ext {f g : α ->₀ M} (h : forall a, f a = g a) : f = g
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `Finsupp.single_sub`：single_sub (a : ι) (b₁ b₂ : G) : single a (b₁ - b₂) 
= single a b₁ - single a b₂
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma single_sub (m : M) (r s : R) : single m (r - s) = single m r - single m s := by ext; simp

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.nonUnitalNonAssocRing** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：{R : Type u_1} → {M : Type u_4} → [inst : Ring R] → [Mul M] → NonUnitalNon
AssocRing (MonoidAlgebra R M)
参数：MonoidAlgebra R M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance nonUnitalNonAssocRing [Mul M] : NonUnitalNonAssocRing R[M] where

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.nonUnitalRing** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：{R : Type u_1} → {M : Type u_4} → [inst : Ring R] → [Semigroup M] → NonUni
talRing (MonoidAlgebra R M)
参数：MonoidAlgebra R M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance nonUnitalRing [Semigroup M] : NonUnitalRing R[M] where

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.nonAssocRing** 是 Mathlib 中的一个实例，位于命名空间 `MonoidAlgebra`。
形式化陈述：nonAssocRing [MulOneClass M] : NonAssocRing R[M] where intCast z
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance nonAssocRing [MulOneClass M] : NonAssocRing R[M] where
  intCast z := single 1 z
  intCast_ofNat n := by simp [natCast_def]
  intCast_negSucc n := by simp [natCast_def, one_def]

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.intCast_def** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：intCast_def [MulOneClass M] (z : Int) : (z : R[M]) = single 1 (z : R)
参数：z : Int。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma intCast_def [MulOneClass M] (z : ℤ) : (z : R[M]) = single 1 (z : R) := rfl

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.ring** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：{R : Type u_1} → {M : Type u_4} → [inst : Ring R] → [Monoid M] → Ring (Mon
oidAlgebra R M)
参数：MonoidAlgebra R M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance ring [Monoid M] : Ring R[M] where

@[deprecated coeff_neg (since := "2026-06-18")]
/-
**MonoidAlgebra.neg_apply** 是 Mathlib 中的一个引理，位于命名空间 `MonoidAlgebra`。
形式化陈述：neg_apply (m : M) (x : R[M]) : (-x).coeff m = -x.coeff m
参数：m : M；x : R[M]。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma neg_apply (m : M) (x : R[M]) : (-x).coeff m = -x.coeff m := rfl

end Ring

section CommRing
variable [CommRing R]

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.nonUnitalCommRing** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：{R : Type u_1} → {M : Type u_4} → [inst : CommRing R] → [CommSemigroup M] 
→ NonUnitalCommRing (MonoidAlgebra R M)
参数：MonoidAlgebra R M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance nonUnitalCommRing [CommSemigroup M] : NonUnitalCommRing R[M] where

@[to_additive (dont_translate := R)]
/-
**MonoidAlgebra.commRing** 是 Mathlib 中的一个定义，位于命名空间 `MonoidAlgebra`。
形式化陈述：{R : Type u_1} → {M : Type u_4} → [inst : CommRing R] → [CommMonoid M] → C
ommRing (MonoidAlgebra R M)
参数：MonoidAlgebra R M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance commRing [CommMonoid M] : CommRing R[M] where

end CommRing
end MonoidAlgebra

/-! ### Additive monoids -/

namespace AddMonoidAlgebra
variable [Semiring R]

section

variable (R M : Type*) [Semiring R]

/-- The embedding of an additive magma into its additive magma algebra. -/
@[simps]
/-
**AddMonoidAlgebra.ofMagma** 是 Mathlib 中的一个定义，位于命名空间 `AddMonoidAlgebra`。
形式化陈述：ofMagma [Add M] : Multiplicative M ->ₙ* R[M] where toFun a
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The embedding of an additive magma into its additive magma algebra.
-/
def ofMagma [Add M] : Multiplicative M →ₙ* R[M] where
  toFun a := single a.toAdd 1
  map_mul' := by simp [mul_def]

/-- Embedding of a magma with zero into its magma algebra. -/
/-
**AddMonoidAlgebra.of** 是 Mathlib 中的一个定义，位于命名空间 `AddMonoidAlgebra`。
形式化陈述：of [AddZeroClass M] : Multiplicative M ->* R[M] where __
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Embedding of a magma with zero into its magma algebra.
-/
def of [AddZeroClass M] : Multiplicative M →* R[M] where
  __ := ofMagma R M
  map_one' := rfl

/-- Embedding of a magma with zero `M`, into its magma algebra, having `M` as source. -/
/-
**AddMonoidAlgebra.of'** 是 Mathlib 中的一个定义，位于命名空间 `AddMonoidAlgebra`。
形式化陈述：of' : M -> R[M]
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Embedding of a magma with zero `M`, into its magma algebra, having `M` as source
.
-/
def of' : M → R[M] := fun m => single m 1

end

@[simp]
/-
**AddMonoidAlgebra.of_apply** 是 Mathlib 中的一个定理，位于命名空间 `AddMonoidAlgebra`。
形式化陈述：of_apply [AddZeroClass M] (a : Multiplicative M) : of R M a = single a.toA
dd 1
参数：a : Multiplicative M。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem of_apply [AddZeroClass M] (a : Multiplicative M) : of R M a = single a.toAdd 1 :=
  rfl

@[simp]
/-
**AddMonoidAlgebra.of'_apply** 是 Mathlib 中的一个定理，位于命名空间 `AddMonoidAlgebra`。
形式化陈述：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] (a : M), AddMonoidAlge
bra.of' R M a = AddMonoidAlgebra.single a 1
参数：a : M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem of'_apply (a : M) : of' R M a = single a 1 :=
  rfl
/-
**AddMonoidAlgebra.of'_eq_of** 是 Mathlib 中的一个定理，位于命名空间 `AddMonoidAlgebra`。
形式化陈述：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] [inst_1 : AddZeroClass
 M] (a : M),   AddMonoidAlgebra.of' R M a = (AddMonoidAlgebra.of R M) (Multiplic
ative.ofAdd a)
参数：a : M；AddMonoidAlgebra.of R M；Multiplicative.ofAdd a。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem of'_eq_of [AddZeroClass M] (a : M) : of' R M a = of R M (.ofAdd a) := rfl
/-
**AddMonoidAlgebra.of_injective** 是 Mathlib 中的一个定理，位于命名空间 `AddMonoidAlgebra`。
形式化陈述：of_injective [Nontrivial R] [AddZeroClass M] : Function.Injective (of R M)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `EquivLike.toEmbeddingLike`：∀ {E : Sort u_1} {α : Sort u_3} {β : Sort u_4
} [inst : EquivLike E α β], EmbeddingLike E α β
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `and_true`：∀ (p : Prop), (p ∧ True) = p
· 使用定理 `and_self`：∀ (p : Prop), (p ∧ p) = p
· 使用定理 `or_false`：∀ (p : Prop), (p ∨ False) = p
-/
theorem of_injective [Nontrivial R] [AddZeroClass M] : Function.Injective (of R M) := fun a b h ↦ by
  simpa [← coeff_inj, Finsupp.single_eq_single_iff] using h
/-
**AddMonoidAlgebra.of'_commute** 是 Mathlib 中的一个定理，位于命名空间 `AddMonoidAlgebra`。
形式化陈述：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R] [inst_1 : AddZeroClass
 M] {a : M},   (∀ (a' : M), AddCommute a a') → ∀ (f : AddMonoidAlgebra R M), Com
mute (AddMonoidAlgebra.of' R M a) f
参数：∀ (a' : M), AddCommute a a'；f : AddMonoidAlgebra R M；AddMonoidAlgebra.of' R M
 a。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AddMonoidAlgebra.single_commute`：∀ {R : Type u_1} {M : Type u_4} [inst :
 Semiring R] {r : R} {m : M} [inst_1 : Add M],   (∀ (m' : M), AddCommute m m') →
     (∀ (r' : R), Com…
· 使用定理 `Commute.one_left`：one_left (a : M) : Commute 1 a
-/
lemma of'_commute [AddZeroClass M] {a : M} (h : ∀ a', AddCommute a a') (f : AddMonoidAlgebra R M) :
    Commute (of' R M a) f :=
  single_commute h .one_left f

/-- `Finsupp.single` as a `MonoidHom` from the product type into the additive monoid algebra.

Note the order of the elements of the product are reversed compared to the arguments of
`Finsupp.single`.
-/
@[simps]
/-
**AddMonoidAlgebra.singleHom** 是 Mathlib 中的一个定义，位于命名空间 `AddMonoidAlgebra`。
形式化陈述：singleHom [AddZeroClass M] : R × Multiplicative M ->* R[M] where toFun a
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`Finsupp.single` as a `MonoidHom` from the product type into the additive monoid
 algebra.

Note the order of the elements of the product are reversed compared to the argum
ents of
`Finsupp.single`.
-/
def singleHom [AddZeroClass M] : R × Multiplicative M →* R[M] where
  toFun a := single a.2.toAdd a.1
  map_one' := rfl
  map_mul' _a _b := (single_mul_single ..).symm

set_option backward.isDefEq.respectTransparency false in
/-
**AddMonoidAlgebra.induction_on** 是 Mathlib 中的一个定理，位于命名空间 `AddMonoidAlgebra`。
形式化陈述：induction_on [AddMonoid M] {motive : R[M] -> Prop} (x : R[M]) (of : forall
 m, motive (.of R M <| .ofAdd m)) (add : forall x y : R[M], motive x -> motive y
 -> motive (x + y)) (smul : forall (r : R) (x), motive x -> motive (r • x)) : mo
tive x
参数：x : R[M]；of : forall m, motive (.of R M <| .ofAdd m)；add : forall x y : R[M],
 motive x -> motive y -> motive (x + y)；smul : forall (r : R) (x), motive x -> m
otive (r • x)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `Finsupp.induction_linear`：induction_linear {motive : (ι ->₀ M) -> Prop} 
(f : ι ->₀ M) (zero : motive 0) (add : forall f g : ι ->₀ M, motive f -> motive 
g -> motive (f…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `AddMonoidAlgebra.smul_single`：∀ {R : Type u_1} {M : Type u_4} [inst : Se
miring R] {A : Type u_8} [inst_1 : SMulZeroClass A R] (a : A) (m : M) (r : R),  
 a • AddMonoidAlge…
· 使用定理 `mul_one`：mul_one : forall a : M, a * 1 = a
· 使用定理 `AddMonoidAlgebra.single_zero`：∀ {R : Type u_1} {M : Type u_4} [inst : Se
miring R] (m : M), AddMonoidAlgebra.single m 0 = 0
-/
theorem induction_on [AddMonoid M] {motive : R[M] → Prop} (x : R[M])
    (of : ∀ m, motive (.of R M <| .ofAdd m))
    (add : ∀ x y : R[M], motive x → motive y → motive (x + y))
    (smul : ∀ (r : R) (x), motive x → motive (r • x)) : motive x :=
  Finsupp.induction_linear (motive := fun x ↦ motive (ofCoeff x)) x.coeff
    (by simpa using smul 0 (.of R M 1) (of 0))
    (fun x y hf hg ↦ add (ofCoeff x) (ofCoeff y) hf hg)
    fun m r ↦ by simpa using! smul r (.of R M m) (of m)

/-- If two ring homomorphisms from `R[M]` are equal on all `single m 1`
and `single 0 r`, then they are equal.

See note [partially-applied ext lemmas]. -/
@[ext high]
/-
**AddMonoidAlgebra.ringHom_ext'** 是 Mathlib 中的一个定理，位于命名空间 `AddMonoidAlgebra`。
形式化陈述：ringHom_ext' [Semiring S] [AddMonoid M] {f g : R[M] ->+* S} (h₁ : f.comp s
ingleZeroRingHom = g.comp singleZeroRingHom) (h_of : (f : R[M] ->* S).comp (of R
 M) = (g : R[M] ->* S).comp (of R M)) : f = g
参数：h₁ : f.comp singleZeroRingHom = g.comp singleZeroRingHom；h_of : (f : R[M] ->*
 S).comp (of R M) = (g : R[M] ->* S).comp (of R M)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MonoidWithZeroHomClass.toMonoidHomClass`：∀ {F : Type u_7} {α : outParam 
(Type u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : Mul
ZeroOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `AddMonoidAlgebra.ringHom_ext`：∀ {R : Type u_1} {S : Type u_2} {M : Type 
u_4} [inst : Semiring R] [inst_1 : AddZeroClass M] [inst_2 : Semiring S]   {f g 
: AddMonoidAlgebra…
· 使用定理 `RingHom.congr_fun`：∀ {α : Type u_2} {β : Type u_3} {x : NonAssocSemiring
 α} {x_1 : NonAssocSemiring β} {f g : α →+* β},   f = g → ∀ (x_2 : α), f x_2 = g
 x_2
· 使用定理 `DFunLike.congr_fun`：∀ {F : Sort u_1} {α : Sort u_2} {β : α → Sort u_3} [
i : DFunLike F α β] {f g : F}, f = g → ∀ (x : α), f x = g x

--- 原说明 ---
If two ring homomorphisms from `R[M]` are equal on all `single m 1`
and `single 0 r`, then they are equal.

See note [partially-applied ext lemmas].
-/
theorem ringHom_ext' [Semiring S] [AddMonoid M] {f g : R[M] →+* S}
    (h₁ : f.comp singleZeroRingHom = g.comp singleZeroRingHom)
    (h_of : (f : R[M] →* S).comp (of R M) = (g : R[M] →* S).comp (of R M)) :
    f = g :=
  ringHom_ext (RingHom.congr_fun h₁) (DFunLike.congr_fun h_of)

end AddMonoidAlgebra

