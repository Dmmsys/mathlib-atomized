/-
Copyright (c) 2025 Kexing Ying. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Kexing Ying
-/
module

public import Mathlib.Probability.Process.Adapted

/-!
# Predictable σ-algebra

This file defines the predictable σ-algebra associated to a filtration, as well as the
notion of predictable processes. We prove that predictable processes are progressively measurable
and adapted. We also give an equivalent characterization of predictability for discrete processes.

## Main definitions

* `Filtration.predictable` : The predictable σ-algebra associated to a filtration.
* `IsStronglyPredictable` : A process is predictable if it is measurable with respect to the
  predictable σ-algebra.

## Main results

* `IsStronglyPredictable.isStronglyProgressive` : A predictable process is progressively measurable.
* `IsStronglyPredictable.iff_measurable_add_one` : `u` is a discrete predictable process iff
  `u (n + 1)` is `𝓕 n`-measurable and `u 0` is `𝓕 0`-measurable.

## Tags

predictable, previsible

-/

@[expose] public section

open Filter Order TopologicalSpace

open scoped MeasureTheory NNReal ENNReal Topology

namespace MeasureTheory

variable {Ω ι : Type*} {m : MeasurableSpace Ω} {E : Type*}

section

namespace Filtration

/-- Given a filtration `𝓕`, the predictable σ-algebra is the σ-algebra on `ι × Ω` generated by
sets of the form `(t, ∞) × A` for `t ∈ ι` and `A ∈ 𝓕 t` and `{⊥} × A` for `A ∈ 𝓕 ⊥`. -/
@[instance_reducible]
/-
**MeasureTheory.Filtration.predictable** 是 Mathlib 中的一个定义，位于命名空间 `MeasureTheory.
Filtration`。
形式化陈述：predictable [Preorder ι] [OrderBot ι] (𝓕 : Filtration ι m) : MeasurableSpa
ce (ι × Ω)
参数：𝓕 : Filtration ι m。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Given a filtration `𝓕`, the predictable σ-algebra is the σ-algebra on `ι × Ω` ge
nerated by
sets of the form `(t, ∞) × A` for `t ∈ ι` and `A ∈ 𝓕 t` and `{⊥} × A` for `A ∈ 𝓕
 ⊥`.
-/
def predictable [Preorder ι] [OrderBot ι] (𝓕 : Filtration ι m) : MeasurableSpace (ι × Ω) :=
  MeasurableSpace.generateFrom <|
    {s | ∃ A, MeasurableSet[𝓕 ⊥] A ∧ s = {⊥} ×ˢ A} ∪
    {s | ∃ i A, MeasurableSet[𝓕 i] A ∧ s = Set.Ioi i ×ˢ A}

end Filtration

/-
**MeasureTheory.measurableSet_predictable_singleton_bot_prod** 是 Mathlib 中的一个引理，
位于命名空间 `MeasureTheory`。
形式化陈述：measurableSet_predictable_singleton_bot_prod [LinearOrder ι] [OrderBot ι] 
{𝓕 : Filtration ι m} {s : Set Ω} (hs : MeasurableSet[𝓕 ⊥] s) : MeasurableSet[𝓕.p
redictable] {⊥} ×ˢ s
参数：hs : MeasurableSet[𝓕 ⊥] s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.measurableSet_generateFrom`：measurableSet_generateFrom {
s : Set (Set α)} {t : Set α} (ht : t in s) : MeasurableSet[generateFrom s] t
-/
lemma measurableSet_predictable_singleton_bot_prod [LinearOrder ι] [OrderBot ι]
    {𝓕 : Filtration ι m} {s : Set Ω} (hs : MeasurableSet[𝓕 ⊥] s) :
    MeasurableSet[𝓕.predictable] <| {⊥} ×ˢ s :=
  MeasurableSpace.measurableSet_generateFrom <| Or.inl ⟨s, hs, rfl⟩
/-
**MeasureTheory.measurableSet_predictable_Ioi_prod** 是 Mathlib 中的一个引理，位于命名空间 `Me
asureTheory`。
形式化陈述：measurableSet_predictable_Ioi_prod [LinearOrder ι] [OrderBot ι] {𝓕 : Filtr
ation ι m} {i : ι} {s : Set Ω} (hs : MeasurableSet[𝓕 i] s) : MeasurableSet[𝓕.pre
dictable] Set.Ioi i ×ˢ s
参数：hs : MeasurableSet[𝓕 i] s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.measurableSet_generateFrom`：measurableSet_generateFrom {
s : Set (Set α)} {t : Set α} (ht : t in s) : MeasurableSet[generateFrom s] t
-/
lemma measurableSet_predictable_Ioi_prod [LinearOrder ι] [OrderBot ι]
    {𝓕 : Filtration ι m} {i : ι} {s : Set Ω} (hs : MeasurableSet[𝓕 i] s) :
    MeasurableSet[𝓕.predictable] <| Set.Ioi i ×ˢ s :=
  MeasurableSpace.measurableSet_generateFrom <| Or.inr ⟨i, s, hs, rfl⟩

/-- Sets of the form `(i, j] × A` for any `A ∈ 𝓕 i` are measurable with respect to the predictable
σ-algebra. -/
/-
**MeasureTheory.measurableSet_predictable_Ioc_prod** 是 Mathlib 中的一个引理，位于命名空间 `Me
asureTheory`。
形式化陈述：measurableSet_predictable_Ioc_prod [LinearOrder ι] [OrderBot ι] {𝓕 : Filtr
ation ι m} (i j : ι) {s : Set Ω} (hs : MeasurableSet[𝓕 i] s) : MeasurableSet[𝓕.p
redictable] Set.Ioc i j ×ˢ s
参数：i j : ι；hs : MeasurableSet[𝓕 i] s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `le_total`：∀ {α : Type u_1} [inst : LinearOrder α] (a b : α), a ≤ b ∨ b ≤
 a
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `Set.Ioc_eq_empty`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, ¬b < a
 → Set.Ioc b a = ∅
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `Set.empty_prod`：empty_prod : (∅ : Set α) ×ˢ t = ∅
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Set.Ioi_sdiff_Ioi`：Ioi_sdiff_Ioi : Ioi a \ Ioi b = Ioc a b
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `sdiff_self`：∀ {α : Type u_2} [inst : GeneralizedCoheytingAlgebra α] {a :
 α}, a \ a = ⊥
· 使用定理 `Set.prod_empty`：prod_empty : s ×ˢ (∅ : Set β) = ∅
· 使用定理 `Set.empty_union`：empty_union (a : Set α) : ∅ union a = a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Set.prod_sdiff_prod`：prod_sdiff_prod : s ×ˢ t \ s₁ ×ˢ t₁ = s ×ˢ (t \ t₁)
 union (s \ s₁) ×ˢ t
· 使用定理 `MeasurableSet.diff`：∀ {α : Type u_1} {m : MeasurableSpace α} {s₁ s₂ : Se
t α}, MeasurableSet s₁ → MeasurableSet s₂ → MeasurableSet (s₁ \ s₂)
· 使用引理 `MeasureTheory.measurableSet_predictable_Ioi_prod`：measurableSet_predicta
ble_Ioi_prod [LinearOrder ι] [OrderBot ι] {𝓕 : Filtration ι m} {i : ι} {s : Set 
Ω} (hs : MeasurableSet[𝓕 i] s) : Measu…
· 使用定理 `MeasureTheory.Filtration.mono`：∀ {Ω : Type u_1} {ι : Type u_2} {m : Meas
urableSpace Ω} [inst : Preorder ι] {i j : ι}   (f : MeasureTheory.Filtration ι m
), i ≤ j → ↑f i ≤ ↑…

--- 原说明 ---
Sets of the form `(i, j] × A` for any `A ∈ 𝓕 i` are measurable with respect to t
he predictable
σ-algebra.
-/
lemma measurableSet_predictable_Ioc_prod [LinearOrder ι] [OrderBot ι]
    {𝓕 : Filtration ι m} (i j : ι) {s : Set Ω} (hs : MeasurableSet[𝓕 i] s) :
    MeasurableSet[𝓕.predictable] <| Set.Ioc i j ×ˢ s := by
  obtain hij | hij := le_total j i
  · simp [hij]
  · rw [← Set.Ioi_sdiff_Ioi, (by simp : (Set.Ioi i \ Set.Ioi j) ×ˢ s
      = Set.Ioi i ×ˢ (s \ s) ∪ (Set.Ioi i \ Set.Ioi j) ×ˢ s), ← Set.prod_sdiff_prod]
    exact (measurableSet_predictable_Ioi_prod hs).diff
      (measurableSet_predictable_Ioi_prod <| 𝓕.mono hij _ hs)
/-
**MeasureTheory.measurableSet_predictable_singleton_prod** 是 Mathlib 中的一个引理，位于命名
空间 `MeasureTheory`。
形式化陈述：measurableSet_predictable_singleton_prod {𝓕 : Filtration Nat m} {n : Nat} 
{s : Set Ω} (hs : MeasurableSet[𝓕 n] s) : MeasurableSet[𝓕.predictable] {n + 1} ×
ˢ s
参数：hs : MeasurableSet[𝓕 n] s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用引理 `MeasureTheory.measurableSet_predictable_Ioc_prod`：measurableSet_predicta
ble_Ioc_prod [LinearOrder ι] [OrderBot ι] {𝓕 : Filtration ι m} (i j : ι) {s : Se
t Ω} (hs : MeasurableSet[𝓕 i] s) : Mea…
-/
lemma measurableSet_predictable_singleton_prod
    {𝓕 : Filtration ℕ m} {n : ℕ} {s : Set Ω} (hs : MeasurableSet[𝓕 n] s) :
    MeasurableSet[𝓕.predictable] <| {n + 1} ×ˢ s := by
  rw [(_ : {n + 1} = Set.Ioc n (n + 1))]
  · exact measurableSet_predictable_Ioc_prod _ _ hs
  · ext m
    simp only [Set.mem_singleton_iff, Set.mem_Ioc]
    lia
/-
**MeasureTheory.measurableSet_prodMk_add_one_of_predictable** 是 Mathlib 中的一个引理，位
于命名空间 `MeasureTheory`。
形式化陈述：measurableSet_prodMk_add_one_of_predictable {𝓕 : Filtration Nat m} {s : Se
t (Nat × Ω)} (hs : MeasurableSet[𝓕.predictable] s) (n : Nat) : MeasurableSet[𝓕 n
] {ω | (n + 1, ω) in s}
参数：Nat × Ω；hs : MeasurableSet[𝓕.predictable] s；n : Nat。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `measurableSet_preimage`：measurableSet_preimage {t : Set β} (hf : Measura
ble f) (ht : MeasurableSet t) : MeasurableSet (f ⁻¹' t)
· 使用定理 `measurable_prodMk_left`：measurable_prodMk_left {x : α} : Measurable (@Pr
od.mk _ β x)
· 使用定理 `measurable_iff_comap_le`：measurable_iff_comap_le {m₁ : MeasurableSpace α
} {m₂ : MeasurableSpace β} {f : α -> β} : Measurable f ↔ m₂.comap f <= m₁
· 使用定理 `MeasurableSpace.comap_le_iff_le_map`：comap_le_iff_le_map {f : α -> β} : 
m'.comap f <= m ↔ m' <= m.map f
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
· 使用引理 `MeasurableSpace.map_def`：map_def {s : Set β} : MeasurableSet[m.map f] s 
↔ MeasurableSet[m] (f ⁻¹' s)
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `iff_false`：∀ (p : Prop), (p ↔ False) = ¬p
· 使用定理 `Nat.succ_ne_zero`：∀ (n : ℕ), n.succ ≠ 0
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Subtype.property`：∀ {α : Sort u} {p : α → Prop} (self : Subtype p), p ↑s
elf
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `lt_or_ge`：∀ {α : Type u_1} [inst : LinearOrder α] (a b : α), a < b ∨ b ≤
 a
· 使用定理 `Subtype.ext`：∀ {α : Sort u} {p : α → Prop} {a1 a2 : { x // p x }}, ↑a1 =
 ↑a2 → a1 = a2
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Order.lt_add_one_iff`：lt_add_one_iff [NoMaxOrder α] : x < y + 1 ↔ x <= y
· 使用定理 `MeasurableSet.prod`：∀ {α : Type u_1} {β : Type u_2} {m : MeasurableSpace
 α} {mβ : MeasurableSpace β} {s : Set α} {t : Set β},   MeasurableSet s → Measur
ableSet …
· 使用定理 `MeasurableSet.of_subtype_image`：MeasurableSet.of_subtype_image {s : Set 
α} {t : Set s} (h : MeasurableSet (Subtype.val '' t)) : MeasurableSet t
· 使用定理 `trivial`：True
· 使用定理 `MeasureTheory.Filtration.mono`：∀ {Ω : Type u_1} {ι : Type u_2} {m : Meas
urableSpace Ω} [inst : Preorder ι] {i j : ι}   (f : MeasureTheory.Filtration ι m
), i ≤ j → ↑f i ≤ ↑…
-/
lemma measurableSet_prodMk_add_one_of_predictable {𝓕 : Filtration ℕ m} {s : Set (ℕ × Ω)}
    (hs : MeasurableSet[𝓕.predictable] s) (n : ℕ) :
    MeasurableSet[𝓕 n] {ω | (n + 1, ω) ∈ s} := by
  rw [(by aesop : {ω | (n + 1, ω) ∈ s} = (Prod.mk (α := Set.singleton (n + 1)) (β := Ω)
      ⟨n + 1, rfl⟩) ⁻¹' ((fun (p : Set.singleton (n + 1) × Ω) ↦ ((p.1 : ℕ), p.2)) ⁻¹' s))]
  refine measurableSet_preimage (mβ := Subtype.instMeasurableSpace.prod (𝓕 n))
    measurable_prodMk_left <| measurableSet_preimage ?_ hs
  rw [measurable_iff_comap_le, MeasurableSpace.comap_le_iff_le_map]
  refine MeasurableSpace.generateFrom_le ?_
  rintro - (⟨A, hA, rfl⟩ | ⟨i, A, hA, rfl⟩)
  · rw [MeasurableSpace.map_def,
      (_ : (fun (p : Set.singleton (n + 1) × Ω) ↦ ((p.1 : ℕ), p.2)) ⁻¹' ({⊥} ×ˢ A) = ∅)]
    · simp
    · ext p
      simp only [Nat.bot_eq_zero, Set.mem_preimage, Set.mem_prod, Set.mem_singleton_iff,
        Set.mem_empty_iff_false, iff_false, not_and]
      exact fun hp1 ↦ False.elim <| Nat.succ_ne_zero n (hp1 ▸ p.1.2.symm)
  · rw [MeasurableSpace.map_def]
    obtain hni | hin := lt_or_ge n i
    · rw [(_ : (fun (p : Set.singleton (n + 1) × Ω) ↦ ((p.1 : ℕ), p.2)) ⁻¹' (Set.Ioi i ×ˢ A) = ∅)]
      · simp
      · ext p
        simp only [Set.mem_preimage, Set.mem_prod, Set.mem_Ioi, Set.mem_empty_iff_false,
          iff_false, not_and]
        rw [p.1.2]
        grind
    · rw [(_ : (fun (p : Set.singleton (n + 1) × Ω) ↦ ((p.1 : ℕ), p.2)) ⁻¹' (Set.Ioi i ×ˢ A)
          = {⟨n + 1, rfl⟩} ×ˢ A)]
      · exact MeasurableSet.prod (MeasurableSet.of_subtype_image trivial) (𝓕.mono hin _ hA)
      · ext p
        simp only [Set.mem_preimage, Set.mem_prod, Set.mem_Ioi, Set.mem_singleton_iff,
          and_congr_left_iff]
        intro hp2
        rw [p.1.2]
        exact ⟨fun _ ↦ by aesop, fun _ ↦ lt_add_one_iff.2 hin⟩
/-
**MeasureTheory.measurableSpace_le_predictable_of_measurableSet** 是 Mathlib 中的一个
引理，位于命名空间 `MeasureTheory`。
形式化陈述：measurableSpace_le_predictable_of_measurableSet [Preorder ι] [OrderBot ι] 
{𝓕 : Filtration ι m} {m' : MeasurableSpace (ι × Ω)} (hm'bot : forall A, Measurab
leSet[𝓕 ⊥] A -> MeasurableSet[m'] ({⊥} ×ˢ A)) (hm' : forall i A, MeasurableSet[𝓕
 i] A -> MeasurableSet[m'] ((Set.Ioi i) ×ˢ A)) : 𝓕.predictable <= m'
参数：ι × Ω；hm'bot : forall A, MeasurableSet[𝓕 ⊥] A -> MeasurableSet[m'] ({⊥} ×ˢ A)
；hm' : forall i A, MeasurableSet[𝓕 i] A -> MeasurableSet[m'] ((Set.Ioi i) ×ˢ A)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
-/
lemma measurableSpace_le_predictable_of_measurableSet [Preorder ι] [OrderBot ι]
    {𝓕 : Filtration ι m} {m' : MeasurableSpace (ι × Ω)}
    (hm'bot : ∀ A, MeasurableSet[𝓕 ⊥] A → MeasurableSet[m'] ({⊥} ×ˢ A))
    (hm' : ∀ i A, MeasurableSet[𝓕 i] A → MeasurableSet[m'] ((Set.Ioi i) ×ˢ A)) :
    𝓕.predictable ≤ m' := by
  refine MeasurableSpace.generateFrom_le ?_
  rintro - (⟨A, hA, rfl⟩ | ⟨i, A, hA, rfl⟩)
  · exact hm'bot A hA
  · exact hm' i A hA

/-- The inclusion map from [0,i] × Ω with the subtype × 𝓕 i σ-algebra) to ι × Ω with the
predictable σ-algebra is measurable -/
/-
**MeasureTheory.measurable_inclusion_predictable** 是 Mathlib 中的一个引理，位于命名空间 `Meas
ureTheory`。
形式化陈述：measurable_inclusion_predictable [LinearOrder ι] [OrderBot ι] [MeasurableS
pace ι] [TopologicalSpace ι] [OpensMeasurableSpace ι] [OrderClosedTopology ι] {𝓕
 : Filtration ι m} {i} : @Measurable (Set.Iic i × Ω) (ι × Ω) (Subtype.instMeasur
ableSpace.prod (𝓕 i)) 𝓕.predictable fun x => ⟨x.1.val, x.2⟩
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `measurable_iff_comap_le`：measurable_iff_comap_le {m₁ : MeasurableSpace α
} {m₂ : MeasurableSpace β} {f : α -> β} : Measurable f ↔ m₂.comap f <= m₁
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `MeasurableSpace.comap_le_iff_le_map`：comap_le_iff_le_map {f : α -> β} : 
m'.comap f <= m ↔ m' <= m.map f
· 使用引理 `MeasureTheory.measurableSpace_le_predictable_of_measurableSet`：measurabl
eSpace_le_predictable_of_measurableSet [Preorder ι] [OrderBot ι] {𝓕 : Filtration
 ι m} {m' : MeasurableSpace (ι × Ω)} (hm'bot : fora…
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `MeasurableSet.prod`：∀ {α : Type u_1} {β : Type u_2} {m : MeasurableSpace
 α} {mβ : MeasurableSpace β} {s : Set α} {t : Set β},   MeasurableSet s → Measur
ableSet …
· 使用定理 `MeasurableSingletonClass.measurableSet_singleton`：∀ {α : Type u_7} {inst
 : MeasurableSpace α} [self : MeasurableSingletonClass α] (x : α), MeasurableSet
 {x}
· 使用定理 `OpensMeasurableSpace.toMeasurableSingletonClass`：∀ {α : Type u_1} [inst 
: TopologicalSpace α] [inst_1 : MeasurableSpace α] [OpensMeasurableSpace α] [T1S
pace α],   MeasurableSingletonClass α
· 使用定理 `T2Space.t1Space`：∀ {X : Type u_1} [inst : TopologicalSpace X] [T2Space X
], T1Space X
· 使用定理 `OrderClosedTopology.to_t2Space`：∀ {α : Type u} [inst : TopologicalSpace 
α] [inst_1 : PartialOrder α] [t : OrderClosedTopology α], T2Space α
· 使用定理 `MeasureTheory.Filtration.mono`：∀ {Ω : Type u_1} {ι : Type u_2} {m : Meas
urableSpace Ω} [inst : Preorder ι] {i j : ι}   (f : MeasureTheory.Filtration ι m
), i ≤ j → ↑f i ≤ ↑…
· 使用定理 `bot_le`：∀ {α : Type u} [inst : LE α] [inst_1 : OrderBot α] {a : α}, ⊥ ≤ 
a
· 使用定理 `le_total`：∀ {α : Type u_1} [inst : LinearOrder α] (a b : α), a ≤ b ∨ b ≤
 a
· 使用定理 `measurable_subtype_coe`：measurable_subtype_coe {p : α -> Prop} : Measura
ble ((↑) : Subtype p -> α)
· 使用定理 `measurableSet_Ioc`：measurableSet_Ioc [ClosedIicTopology α] : MeasurableS
et (Ioc a b)
· 使用定理 `instClosedIicTopology`：∀ {α : Type u} [inst : TopologicalSpace α] [inst_
1 : Preorder α] [t : OrderClosedTopology α], ClosedIicTopology α

--- 原说明 ---
The inclusion map from [0,i] × Ω with the subtype × 𝓕 i σ-algebra) to ι × Ω with
 the
predictable σ-algebra is measurable
-/
lemma measurable_inclusion_predictable [LinearOrder ι] [OrderBot ι] [MeasurableSpace ι]
    [TopologicalSpace ι] [OpensMeasurableSpace ι] [OrderClosedTopology ι] {𝓕 : Filtration ι m} {i} :
    @Measurable (Set.Iic i × Ω) (ι × Ω) (Subtype.instMeasurableSpace.prod (𝓕 i)) 𝓕.predictable
    fun x ↦ ⟨x.1.val, x.2⟩ := by
  rw [measurable_iff_comap_le]
  refine MeasurableSpace.comap_le_iff_le_map.2 <|
    measurableSpace_le_predictable_of_measurableSet ?_ ?_
  · intros A hA
    simp only [MeasurableSpace.map_def,
      (by aesop : (fun (p : Set.Iic i × Ω) ↦ ((p.1 : ι), p.2)) ⁻¹' ({⊥} ×ˢ A) = {⊥} ×ˢ A)]
    exact (measurableSet_singleton _).prod <| 𝓕.mono bot_le _ hA
  · intros j A hA
    simp only [MeasurableSpace.map_def]
    obtain hji | hij := le_total j i
    · rw [(by grind : (fun (p : Set.Iic i × Ω) ↦ ((p.1 : ι), p.2)) ⁻¹' Set.Ioi j ×ˢ A
        = (Subtype.val ⁻¹' (Set.Ioc j i)) ×ˢ A)]
      exact (measurable_subtype_coe measurableSet_Ioc).prod (𝓕.mono hji _ hA)
    · simp [(by grind : (fun (p : Set.Iic i × Ω) ↦ ((p.1 : ι), p.2)) ⁻¹' Set.Ioi j ×ˢ A = ∅)]

end

variable [TopologicalSpace E]

/-- A process is said to be predictable if it is measurable with respect to the predictable
σ-algebra. -/
/-
**MeasureTheory.IsStronglyPredictable** 是 Mathlib 中的一个定义，位于命名空间 `MeasureTheory`。
形式化陈述：IsStronglyPredictable [Preorder ι] [OrderBot ι] (𝓕 : Filtration ι m) (u : 
ι -> Ω -> E)
参数：𝓕 : Filtration ι m；u : ι -> Ω -> E。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A process is said to be predictable if it is measurable with respect to the pred
ictable
σ-algebra.
-/
def IsStronglyPredictable [Preorder ι] [OrderBot ι] (𝓕 : Filtration ι m) (u : ι → Ω → E) :=
  StronglyMeasurable[𝓕.predictable] <| Function.uncurry u

namespace IsStronglyPredictable

open Filtration

variable [LinearOrder ι] [OrderBot ι] [MeasurableSpace ι] [TopologicalSpace ι]
    [OpensMeasurableSpace ι] [OrderClosedTopology ι]

/-- A predictable process is progressively measurable. -/
/-
**MeasureTheory.IsStronglyPredictable.isStronglyProgressive** 是 Mathlib 中的一个引理，位
于命名空间 `MeasureTheory.IsStronglyPredictable`。
形式化陈述：isStronglyProgressive {𝓕 : Filtration ι m} {u : ι -> Ω -> E} (h𝓕 : IsStron
glyPredictable 𝓕 u) : IsStronglyProgressive 𝓕 u
参数：h𝓕 : IsStronglyPredictable 𝓕 u。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `MeasureTheory.measurable_inclusion_predictable`：measurable_inclusion_pre
dictable [LinearOrder ι] [OrderBot ι] [MeasurableSpace ι] [TopologicalSpace ι] [
OpensMeasurableSpace ι] [OrderClosed…
· 使用定理 `MeasureTheory.SimpleFunc.measurableSet_fiber`：measurableSet_fiber (f : α
 ->ₛ β) (x : β) : MeasurableSet (f ⁻¹' {x})
· 使用定理 `Set.Finite.subset`：∀ {α : Type u} {s : Set α}, s.Finite → ∀ {t : Set α},
 t ⊆ s → t.Finite
· 使用定理 `MeasureTheory.SimpleFunc.finite_range`：finite_range (f : α ->ₛ β) : (Set
.range f).Finite
· 使用定理 `MeasureTheory.StronglyMeasurable.tendsto_approx`：∀ {α : Type u_1} {β : T
ype u_2} {f : α → β} [inst : TopologicalSpace β] {x : MeasurableSpace α}   (hf :
 MeasureTheory.StronglyMeasurable f) …

--- 原说明 ---
A predictable process is progressively measurable.
-/
lemma isStronglyProgressive {𝓕 : Filtration ι m} {u : ι → Ω → E} (h𝓕 : IsStronglyPredictable 𝓕 u) :
    IsStronglyProgressive 𝓕 u := by
  intro i
  let : MeasurableSpace (ι × Ω) := 𝓕.predictable
  let : MeasurableSpace (Set.Iic i × Ω) := Subtype.instMeasurableSpace.prod (𝓕 i)
  let X m (x : Set.Iic i × Ω) := h𝓕.approx m ⟨x.1, x.2⟩
  refine ⟨fun m ↦ SimpleFunc.mk (X m) ?_ ?_, ?_⟩
  · exact fun e ↦ measurable_inclusion_predictable <| (h𝓕.approx m).measurableSet_fiber e
  · exact Set.Finite.subset (h𝓕.approx m).finite_range (by grind)
  · exact fun n ↦ by apply h𝓕.tendsto_approx

/-- A predictable process is adapted. -/
/-
**MeasureTheory.IsStronglyPredictable.stronglyAdapted** 是 Mathlib 中的一个引理，位于命名空间 
`MeasureTheory.IsStronglyPredictable`。
形式化陈述：stronglyAdapted {𝓕 : Filtration ι m} {u : ι -> Ω -> E} (h𝓕 : IsStronglyPre
dictable 𝓕 u) : StronglyAdapted 𝓕 u
参数：h𝓕 : IsStronglyPredictable 𝓕 u。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasureTheory.IsStronglyProgressive.stronglyAdapted`：∀ {Ω : Type u_1} {ι
 : Type u_2} {m : MeasurableSpace Ω} [inst : Preorder ι] {f : MeasureTheory.Filt
ration ι m}   {β : Type u_3} [inst_1 : To…
· 使用引理 `MeasureTheory.IsStronglyPredictable.isStronglyProgressive`：isStronglyPro
gressive {𝓕 : Filtration ι m} {u : ι -> Ω -> E} (h𝓕 : IsStronglyPredictable 𝓕 u)
 : IsStronglyProgressive 𝓕 u

--- 原说明 ---
A predictable process is adapted.
-/
lemma stronglyAdapted {𝓕 : Filtration ι m} {u : ι → Ω → E} (h𝓕 : IsStronglyPredictable 𝓕 u) :
    StronglyAdapted 𝓕 u :=
  h𝓕.isStronglyProgressive.stronglyAdapted

section Discrete

/-- If `u` is a discrete predictable process, then `u (n + 1)` is `𝓕 n`-measurable. -/
/-
**MeasureTheory.IsStronglyPredictable.measurable_add_one** 是 Mathlib 中的一个引理，位于命名
空间 `MeasureTheory.IsStronglyPredictable`。
形式化陈述：measurable_add_one {𝓕 : Filtration Nat m} {u : Nat -> Ω -> E} (h𝓕 : IsStro
nglyPredictable 𝓕 u) (n : Nat) : StronglyMeasurable[𝓕 n] (u (n + 1))
参数：h𝓕 : IsStronglyPredictable 𝓕 u；n : Nat。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用引理 `MeasureTheory.measurableSet_prodMk_add_one_of_predictable`：measurableSet
_prodMk_add_one_of_predictable {𝓕 : Filtration Nat m} {s : Set (Nat × Ω)} (hs : 
MeasurableSet[𝓕.predictable] s) (n : Nat) : Mea…
· 使用定理 `MeasureTheory.SimpleFunc.measurableSet_fiber`：measurableSet_fiber (f : α
 ->ₛ β) (x : β) : MeasurableSet (f ⁻¹' {x})
· 使用定理 `Set.Finite.subset`：∀ {α : Type u} {s : Set α}, s.Finite → ∀ {t : Set α},
 t ⊆ s → t.Finite
· 使用定理 `MeasureTheory.SimpleFunc.finite_range`：finite_range (f : α ->ₛ β) : (Set
.range f).Finite
· 使用定理 `Set.range_subset_iff`：range_subset_iff : range f subseteq s ↔ forall y, 
f y in s
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `MeasureTheory.StronglyMeasurable.tendsto_approx`：∀ {α : Type u_1} {β : T
ype u_2} {f : α → β} [inst : TopologicalSpace β] {x : MeasurableSpace α}   (hf :
 MeasureTheory.StronglyMeasurable f) …

--- 原说明 ---
If `u` is a discrete predictable process, then `u (n + 1)` is `𝓕 n`-measurable.
-/
lemma measurable_add_one {𝓕 : Filtration ℕ m} {u : ℕ → Ω → E}
    (h𝓕 : IsStronglyPredictable 𝓕 u) (n : ℕ) : StronglyMeasurable[𝓕 n] (u (n + 1)) := by
  let : MeasurableSpace (ℕ × Ω) := 𝓕.predictable
  let : MeasurableSpace Ω := 𝓕 n
  let X m := (Function.curry (h𝓕.approx m) (n + 1))
  refine ⟨(fun m ↦ SimpleFunc.mk (X m) ?_ ?_), (fun ω ↦ h𝓕.tendsto_approx ⟨(n + 1), ω⟩)⟩
  · intro s
    rw [(by aesop : X m ⁻¹' {s} = {ω | (n + 1, ω) ∈ h𝓕.approx m ⁻¹' {s}})]
    apply measurableSet_prodMk_add_one_of_predictable
    apply (h𝓕.approx m).measurableSet_fiber
  · apply (h𝓕.approx m).finite_range.subset
    rw [Set.range_subset_iff]
    aesop
/-
**MeasureTheory.IsStronglyPredictable.of_measurable_add_one** 是 Mathlib 中的一个引理，位
于命名空间 `MeasureTheory.IsStronglyPredictable`。
形式化陈述：of_measurable_add_one {𝓕 : Filtration Nat m} {u : Nat -> Ω -> E} (h₀ : Str
onglyMeasurable[𝓕 0] (u 0)) (h : forall n, StronglyMeasurable[𝓕 n] (u (n + 1))) 
: IsStronglyPredictable 𝓕 u
参数：h₀ : StronglyMeasurable[𝓕 0] (u 0)；h : forall n, StronglyMeasurable[𝓕 n] (u (
n + 1))。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `ite_congr`：∀ {α : Sort u_1} {b c : Prop} {x y u v : α} {s : Decidable b}
 [inst : Decidable c],   b = c → (c → x = u) → (¬c → y = v) → (if b then x else…
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `MeasurableSet.iUnion`：∀ {α : Type u_1} {ι : Sort u_6} {m : MeasurableSpa
ce α} [Countable ι] ⦃f : ι → Set α⦄,   (∀ (b : ι), MeasurableSet (f b)) → Measur
ableSet (⋃…
· 使用定理 `instCountableNat`：Countable ℕ
· 使用引理 `MeasureTheory.measurableSet_predictable_singleton_bot_prod`：measurableSe
t_predictable_singleton_bot_prod [LinearOrder ι] [OrderBot ι] {𝓕 : Filtration ι 
m} {s : Set Ω} (hs : MeasurableSet[𝓕 ⊥] s) : Mea…
· 使用定理 `MeasureTheory.SimpleFunc.measurableSet_fiber`：measurableSet_fiber (f : α
 ->ₛ β) (x : β) : MeasurableSet (f ⁻¹' {x})
· 使用引理 `MeasureTheory.measurableSet_predictable_singleton_prod`：measurableSet_pr
edictable_singleton_prod {𝓕 : Filtration Nat m} {n : Nat} {s : Set Ω} (hs : Meas
urableSet[𝓕 n] s) : MeasurableSet[𝓕.predicta…
· 使用定理 `ite_cond_eq_true`：∀ {α : Sort u} {c : Prop} {x : Decidable c} (a b : α),
 c = True → (if c then a else b) = a
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `MeasureTheory.Filtration.mono`：∀ {Ω : Type u_1} {ι : Type u_2} {m : Meas
urableSpace Ω} [inst : Preorder ι] {i j : ι}   (f : MeasureTheory.Filtration ι m
), i ≤ j → ↑f i ≤ ↑…
· 使用定理 `LinearOrderedCommMonoidWithZero.toIsBotZeroClass`：∀ {α : Type u_3} [self
 : LinearOrderedCommMonoidWithZero α], IsBotZeroClass α
· 使用定理 `Set.Finite.subset`：∀ {α : Type u} {s : Set α}, s.Finite → ∀ {t : Set α},
 t ⊆ s → t.Finite
· 使用定理 `Set.Finite.biUnion'`：∀ {α : Type u} {ι : Type u_1} {s : Set ι},   s.Fini
te →     ∀ {t : (i : ι) → i ∈ s → Set α}, (∀ (i : ι) (hi : i ∈ s), (t i hi).Fini
te) → (⋃ …
· 使用定理 `Finset.coe_range`：coe_range (n : Nat) : (range n : Set Nat) = Set.Iio n
· 使用定理 `MeasureTheory.SimpleFunc.finite_range`：finite_range (f : α ->ₛ β) : (Set
.range f).Finite
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Set.mem_range`：∀ {α : Type u} {ι : Sort u_1} {f : ι → α} {x : α}, x ∈ Se
t.range f ↔ ∃ y, f y = x
· 使用定理 `exists_prop_congr`：∀ {p p' : Prop} {q q' : p → Prop}, (∀ (h : p), q h ↔ 
q' h) → ∀ (hp : p ↔ p'), Exists q ↔ ∃ (h : p'), q' ⋯
· 使用定理 `Iff.of_eq`：∀ {a b : Prop}, a = b → (a ↔ b)
· 使用定理 `if_neg`：∀ {c : Prop} {h : Decidable c}, ¬c → ∀ {α : Sort u} {t e : α}, (
if c then t else e) = e
· 使用定理 `if_pos`：∀ {c : Prop} {h : Decidable c}, c → ∀ {α : Sort u} {t e : α}, (i
f c then t else e) = t
· 使用定理 `ite_self`：∀ {α : Sort u} {c : Prop} {d : Decidable c} (a : α), (if c the
n a else a) = a
（共 40 条，此处仅展示前 30 条）
-/
lemma of_measurable_add_one {𝓕 : Filtration ℕ m} {u : ℕ → Ω → E}
    (h₀ : StronglyMeasurable[𝓕 0] (u 0)) (h : ∀ n, StronglyMeasurable[𝓕 n] (u (n + 1))) :
    IsStronglyPredictable 𝓕 u := by
  let : MeasurableSpace (ℕ × Ω) := 𝓕.predictable
  -- first layer of approximation
  let X m (x : ℕ × Ω) := match x.1 with
    | 0 => h₀.approx m x.2
    | n + 1 => (h n).approx m x.2
  -- second layer of approximation (to ensure the whole process has finite range)
  let Y m (x : ℕ × Ω) := if x.1 ≤ m then X m x else X m ⟨0, x.2⟩
  refine ⟨fun m ↦ SimpleFunc.mk (Y m) ?_ ?_, ?_⟩
  · intro s
    rw [(by aesop : Y m ⁻¹' {s} = ⋃ n : ℕ, {n} ×ˢ ((Function.curry (Y m) n) ⁻¹' {s}))]
    refine MeasurableSet.iUnion <| fun n ↦ ?_
    rcases n with rfl | n
    · apply measurableSet_predictable_singleton_bot_prod
      let : MeasurableSpace Ω := 𝓕 0
      exact (h₀.approx m).measurableSet_fiber s
    · apply measurableSet_predictable_singleton_prod
      by_cases! hmk : n + 1 ≤ m
      · rw [(by aesop : Function.curry (Y m) (n + 1) = Function.curry (X m) (n + 1))]
        let : MeasurableSpace Ω := 𝓕 n
        exact ((h n).approx m).measurableSet_fiber s
      · rw [(by aesop : Function.curry (Y m) (n + 1) = Function.curry (X m) 0)]
        apply 𝓕.mono (i := 0) (by simp)
        let : MeasurableSpace Ω := 𝓕 0
        exact (h₀.approx m).measurableSet_fiber s
  · apply Set.Finite.subset (s := ⋃ k ∈ Finset.range (m + 1), Set.range (Function.curry (X m) k))
    · refine Set.Finite.biUnion' (by aesop) (fun n hn ↦ ?_)
      rcases n with rfl | n
      · exact @(h₀.approx m).finite_range
      · exact @((h n).approx m).finite_range
    · intro e he
      obtain ⟨⟨n, ω⟩, hnω⟩ := Set.mem_range.mp he
      simp_rw [Set.mem_iUnion, Set.mem_range]
      exact ⟨if n ≤ m then n else 0, by aesop, ω, by aesop⟩
  · intro ⟨n, ω⟩
    rw [tendsto_congr' (f₂ := fun m ↦ X m ⟨n, ω⟩)]
    · rcases n with rfl | n
      · exact h₀.tendsto_approx ω
      · exact (h n).tendsto_approx ω
    · filter_upwards [eventually_ge_atTop n] with m hm using by simp; grind

/-- A discrete process `u` is predictable iff `u (n + 1)` is `𝓕 n`-measurable for all `n` and
`u 0` is `𝓕 0`-measurable. -/
/-
**MeasureTheory.IsStronglyPredictable.iff_measurable_add_one** 是 Mathlib 中的一个引理，
位于命名空间 `MeasureTheory.IsStronglyPredictable`。
形式化陈述：iff_measurable_add_one {𝓕 : Filtration Nat m} {u : Nat -> Ω -> E} : IsStro
nglyPredictable 𝓕 u ↔ StronglyMeasurable[𝓕 0] (u 0) ∧ forall n, StronglyMeasurab
le[𝓕 n] (u (n + 1))
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasureTheory.IsStronglyProgressive.stronglyAdapted`：∀ {Ω : Type u_1} {ι
 : Type u_2} {m : MeasurableSpace Ω} [inst : Preorder ι] {f : MeasureTheory.Filt
ration ι m}   {β : Type u_3} [inst_1 : To…
· 使用引理 `MeasureTheory.IsStronglyPredictable.isStronglyProgressive`：isStronglyPro
gressive {𝓕 : Filtration ι m} {u : ι -> Ω -> E} (h𝓕 : IsStronglyPredictable 𝓕 u)
 : IsStronglyProgressive 𝓕 u
· 使用定理 `BorelSpace.opensMeasurable`：∀ {α : Type u_6} [inst : TopologicalSpace α]
 [inst_1 : MeasurableSpace α] [BorelSpace α], OpensMeasurableSpace α
· 使用定理 `OrderTopology.to_orderClosedTopology`：∀ {α : Type u} [inst : Topological
Space α] [inst_1 : LinearOrder α] [OrderTopology α], OrderClosedTopology α
· 使用定理 `instDiscreteTopologyNat`：DiscreteTopology ℕ
· 使用引理 `MeasureTheory.IsStronglyPredictable.measurable_add_one`：measurable_add_o
ne {𝓕 : Filtration Nat m} {u : Nat -> Ω -> E} (h𝓕 : IsStronglyPredictable 𝓕 u) (
n : Nat) : StronglyMeasurable[𝓕 n] (u (n + 1…
· 使用引理 `MeasureTheory.IsStronglyPredictable.of_measurable_add_one`：of_measurable
_add_one {𝓕 : Filtration Nat m} {u : Nat -> Ω -> E} (h₀ : StronglyMeasurable[𝓕 0
] (u 0)) (h : forall n, StronglyMeasurable[𝓕 n]…
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b

--- 原说明 ---
A discrete process `u` is predictable iff `u (n + 1)` is `𝓕 n`-measurable for al
l `n` and
`u 0` is `𝓕 0`-measurable.
-/
lemma iff_measurable_add_one {𝓕 : Filtration ℕ m} {u : ℕ → Ω → E} :
    IsStronglyPredictable 𝓕 u ↔
    StronglyMeasurable[𝓕 0] (u 0) ∧ ∀ n, StronglyMeasurable[𝓕 n] (u (n + 1)) :=
  ⟨fun h𝓕 ↦ ⟨h𝓕.isStronglyProgressive.stronglyAdapted 0, h𝓕.measurable_add_one⟩,
    fun h ↦ .of_measurable_add_one h.1 h.2⟩

end Discrete

end IsStronglyPredictable

@[deprecated IsStronglyPredictable.stronglyAdapted (since := "2026-01-05")]
/-
**MeasureTheory.Predictable.stronglyAdapted** 是 Mathlib 中的一个定理，位于命名空间 `MeasureTh
eory.Predictable`。
形式化陈述：∀ {Ω : Type u_1} {m : MeasurableSpace Ω} {β : Type u_4} [inst : Topologica
lSpace β] {f : MeasureTheory.Filtration ℕ m}   {u : ℕ → Ω → β},   (MeasureTheory
.StronglyAdapted f fun n => u (n + 1)) →     MeasureTheory.StronglyMeasurable (u
 0) → MeasureTheory.StronglyAdapted f u
参数：MeasureTheory.StronglyAdapted f fun n => u (n + 1)；u 0。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasureTheory.StronglyMeasurable.mono`：∀ {α : Type u_1} {β : Type u_2} {
f : α → β} {m m' : MeasurableSpace α} [inst : TopologicalSpace β],   MeasureTheo
ry.StronglyMeasurable f → m…
· 使用定理 `MeasureTheory.Filtration.mono`：∀ {Ω : Type u_1} {ι : Type u_2} {m : Meas
urableSpace Ω} [inst : Preorder ι] {i j : ι}   (f : MeasureTheory.Filtration ι m
), i ≤ j → ↑f i ≤ ↑…
· 使用定理 `Nat.le_succ`：∀ (n : ℕ), n ≤ n.succ
-/
theorem Predictable.stronglyAdapted {β : Type*} [TopologicalSpace β] {f : Filtration ℕ m}
    {u : ℕ → Ω → β} (hu : StronglyAdapted f fun n => u (n + 1))
    (hu0 : StronglyMeasurable[f 0] (u 0)) :
    StronglyAdapted f u := fun n =>
  match n with
  | 0 => hu0
  | n + 1 => (hu n).mono (f.mono n.le_succ)

@[deprecated (since := "2026-04-24")]
alias IsPredictable := IsStronglyPredictable

@[deprecated (since := "2026-04-24")]
alias IsPredictable.progMeasurable := IsStronglyPredictable.isStronglyProgressive

@[deprecated (since := "2026-04-24")]
alias IsPredictable.adapted := IsStronglyPredictable.stronglyAdapted

@[deprecated (since := "2026-04-24")]
alias IsPredictable.measurable_add_one := IsStronglyPredictable.measurable_add_one

@[deprecated (since := "2026-04-24")]
alias IsPredictable.of_measurable_add_one := IsStronglyPredictable.of_measurable_add_one

@[deprecated (since := "2026-04-24")]
alias IsPredictable.iff_measurable_add_one := IsStronglyPredictable.iff_measurable_add_one

end MeasureTheory

