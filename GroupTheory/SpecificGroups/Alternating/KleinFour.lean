/-
Copyright (c) 2025 Antoine Chambert-Loir. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Antoine Chambert-Loir
-/
module

public import Mathlib.GroupTheory.SpecificGroups.Alternating.Centralizer
public import Mathlib.GroupTheory.SpecificGroups.KleinFour
public import Mathlib.GroupTheory.Sylow

/-! # The Klein Four subgroup of an alternating group on 4 letters

Let `α` be a finite type such that `Nat.card α = 4`.

* `alternatingGroup.kleinFour` : the subgroup of `alternatingGroup α` generated by permutations of
  cycle type (2, 2).

## Main results

* `alternatingGroup.kleinFour_isKleinFour`: `alternatingGroup.kleinFour α` satisfies `IsKleinFour`.
  (When `4 < Nat.card α`, it is equal to `⊤`, when `Nat.card α < 4`, it is trivial.)

* `alternatingGroup.two_sylow_eq_kleinFour_of_card_eq_four`:
  All `2`-sylow subgroups of `alternatingGroup α` are equal to `kleinFour α`.

* `alternatingGroup.characteristic_kleinFour`:
  the subgroup `alternatingGroup.kleinFour α` is characteristic.

* `alternatingGroup.kleinFour_eq_commutator`:
  the subgroup `alternatingGroup.kleinFour α` is the commutator subgroup of `alternatingGroup α`.
  (When `4 < Nat.card α`, the commutator subgroup of `alternatingGroup α` is equal to `⊤`;
  when `Nat.card α < 3`, it is trivial.)

## Remark on implementation

We start from `alternatingGroup.kleinFour α` as given by its definition.
We first prove that it is equal to any 2-sylow subgroup:
the elements of such a sylow subgroup `S` have order a power of 2,
and this implies that their cycle type is () or (2,2).
Since there are exactly 4 elements of that cycle type,
we conclude that `alternatingGroup.kleinFour α = S`.
Since all 2-sylow subgroups are conjugate, we conclude
that there is only one 2-sylow subgroup and that it is normal,
and then characteristic.

## TODO :

Prove `alternatingGroup.kleinFour α = commutator (alternatingGroup α)`
without any assumption on `Nat.card α`.

-/

@[expose] public section

namespace alternatingGroup

open Equiv.Perm Equiv

variable {α : Type*} [DecidableEq α] [Fintype α]

variable (α) in
/-- The subgroup of `alternatingGroup α` generated by permutations of cycle type (2, 2).

When `Nat.card α = 4`, it will effectively be of Klein Four type. -/
/-
**alternatingGroup.kleinFour** 是 Mathlib 中的一个定义，位于命名空间 `alternatingGroup`。
形式化陈述：kleinFour : Subgroup (alternatingGroup α)
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The subgroup of `alternatingGroup α` generated by permutations of cycle type (2,
 2).

When `Nat.card α = 4`, it will effectively be of Klein Four type.
-/
def kleinFour : Subgroup (alternatingGroup α) :=
  Subgroup.closure {g : alternatingGroup α | (g : Equiv.Perm α).cycleType = {2, 2}}
/-
**alternatingGroup.mem_kleinFour_of_order_two_pow** 是 Mathlib 中的一个定理，位于命名空间 `alt
ernatingGroup`。
形式化陈述：mem_kleinFour_of_order_two_pow (hα4 : Nat.card α = 4) {g : Perm α} (hg0 : 
g in alternatingGroup α) {n : Nat} (hg : orderOf g ∣ 2 ^ n) : g.cycleType = { } 
∨ g.cycleType = {2, 2}
参数：hα4 : Nat.card α = 4；hg0 : g in alternatingGroup α；hg : orderOf g ∣ 2 ^ n。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Equiv.Perm.sum_cycleType`：sum_cycleType (σ : Perm α) : σ.cycleType.sum =
 #σ.support
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Nat.card_eq_fintype_card`：card_eq_fintype_card [Fintype α] : Nat.card α 
= Fintype.card α
· 使用定理 `Finset.card_le_univ`：Finset.card_le_univ [Fintype α] (s : Finset α) : #s
 <= Fintype.card α
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用定理 `Multiset.le_sum_of_mem`：∀ {α : Type u_2} [inst : AddCommMonoid α] {m : M
ultiset α} {a : α},   a ∈ m → ∀ [inst_1 : Preorder α] [CanonicallyOrderedAdd α],
 a ≤ m.sum
· 使用定理 `Equiv.Perm.one_lt_of_mem_cycleType`：one_lt_of_mem_cycleType {σ : Perm α}
 {n : Nat} (h : n in σ.cycleType) : 1 < n
· 使用引理 `Mathlib.Tactic.Contrapose.contrapose₁`：contrapose₁ {p q : Prop} : (¬ q -
> ¬ p) -> (p -> q)
· 使用定理 `Multiset.exists_cons_of_mem`：exists_cons_of_mem {s : Multiset α} {a : α}
 : a in s -> exists t, s = a ::ₘ t
· 使用定理 `Nat.not_succ_lt_self`：∀ {n : ℕ}, ¬n.succ < n
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `Multiset.eq_replicate_card`：eq_replicate_card {a : α} {s : Multiset α} :
 s = replicate (card s) a ↔ forall b in s, b = a
· 使用定理 `Multiset.sum_eq_zero_iff`：∀ {α : Type u_2} [inst : AddCommMonoid α] {m :
 Multiset α} [inst_1 : PartialOrder α] [CanonicallyOrderedAdd α]   [IsOrderedAdd
Monoid α], m.s…
· 使用定理 `le_zero_iff`：∀ {α : Type u_1} {a : α} [inst : PartialOrder α] [inst_1 : 
Zero α] [IsBotZeroClass α], a ≤ 0 ↔ a = 0
· 使用定理 `LinearOrderedCommMonoidWithZero.toIsBotZeroClass`：∀ {α : Type u_3} [self
 : LinearOrderedCommMonoidWithZero α], IsBotZeroClass α
· 使用定理 `add_le_iff_nonpos_right`：∀ {α : Type u_1} [inst : AddZeroClass α] [inst_
1 : LE α] [AddLeftMono α] [AddLeftReflectLE α] (a : α) {b : α},   a + b ≤ a ↔ b 
≤ 0
· 使用定理 `IsOrderedAddMonoid.toAddLeftMono`：∀ {α : Type u_1} [inst : AddCommMonoid
 α] [inst_1 : Preorder α] [IsOrderedAddMonoid α], AddLeftMono α
· 使用定理 `IsLeftCancelAdd.addLeftReflectLE_of_addLeftReflectLT`：∀ (N : Type u_2) [
inst : Add N] [IsLeftCancelAdd N] [inst_2 : PartialOrder N] [AddLeftReflectLT N]
, AddLeftReflectLE N
· 使用定理 `instIsLeftCancelAddOfAddLeftReflectLE`：∀ {α : Type u_1} [inst : Add α] [
inst_1 : PartialOrder α] [AddLeftReflectLE α], IsLeftCancelAdd α
· 使用定理 `IsOrderedCancelAddMonoid.toAddLeftReflectLE`：∀ {α : Type u_2} [inst : Ad
dCommMonoid α] [inst_1 : Preorder α] [IsOrderedCancelAddMonoid α], AddLeftReflec
tLE α
· 使用定理 `Multiset.sum_cons`：∀ {M : Type u_3} [inst : AddCommMonoid M] (a : M) (s 
: Multiset M), (a ::ₘ s).sum = a + s.sum
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `Nat.instAtLeastTwoHAddOfNat`：∀ (n : ℕ) [NeZero n], (n + 1).AtLeastTwo
· 使用定理 `Nat.instNeZeroSucc`：∀ {n : ℕ}, NeZero (n + 1)
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `and_true`：∀ (p : Prop), (p ∧ True) = p
· 使用定理 `false_or`：∀ (p : Prop), (False ∨ p) = p
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
（共 57 条，此处仅展示前 30 条）
-/
theorem mem_kleinFour_of_order_two_pow (hα4 : Nat.card α = 4) {g : Perm α}
    (hg0 : g ∈ alternatingGroup α) {n : ℕ} (hg : orderOf g ∣ 2 ^ n) :
    g.cycleType = { } ∨ g.cycleType = {2, 2} := by
  rw [mem_alternatingGroup, sign_of_cycleType] at hg0
  have h1 : g.cycleType.sum ≤ 4 := by
    rw [sum_cycleType, ← hα4, Nat.card_eq_fintype_card]
    exact g.support.card_le_univ
  have h2 : ∀ k ∈ g.cycleType, k = 2 := by
    intro k hk
    have hk4 := (Multiset.le_sum_of_mem hk).trans h1
    have hk1 := one_lt_of_mem_cycleType hk
    interval_cases k
    · rfl
    · rw [← lcm_cycleType, Multiset.lcm_dvd] at hg
      exact Nat.prime_eq_prime_of_dvd_pow Nat.prime_three Nat.prime_two (hg 3 hk)
    · contrapose! hg0
      obtain ⟨t, ht⟩ := Multiset.exists_cons_of_mem hk
      rw [ht, Multiset.sum_cons, add_le_iff_nonpos_right, le_zero_iff,
        Multiset.sum_eq_zero_iff, ← Multiset.eq_replicate_card] at h1
      have h : 0 ∉ g.cycleType := Nat.not_succ_lt_self ∘ one_lt_of_mem_cycleType
      replace h : t = 0 := by simpa [h1 ▸ ht, Multiset.mem_replicate] using h
      simp [ht, h, Units.ext_iff]
  rw [← Multiset.eq_replicate_card] at h2
  rw [h2, Multiset.sum_replicate, smul_eq_mul, ← Nat.le_div_iff_mul_le two_pos] at h1
  interval_cases g.cycleType.card <;> simp [h2, Units.ext_iff] at hg0 ⊢
/-
**alternatingGroup.card_of_card_eq_four** 是 Mathlib 中的一个定理，位于命名空间 `alternatingGr
oup`。
形式化陈述：card_of_card_eq_four (hα4 : Nat.card α = 4) : Nat.card (alternatingGroup α
) = 12
参数：hα4 : Nat.card α = 4。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Finite.one_lt_card_iff_nontrivial`：one_lt_card_iff_nontrivial [Finite α]
 : 1 < Nat.card α ↔ Nontrivial α
· 使用定理 `Finite.of_fintype`：∀ (α : Type u_4) [Fintype α], Finite α
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Nat.instAtLeastTwoHAddOfNat`：∀ (n : ℕ) [NeZero n], (n + 1).AtLeastTwo
· 使用定理 `Nat.instNeZeroSucc`：∀ {n : ℕ}, NeZero (n + 1)
· 使用定理 `IsOrderedAddMonoid.toAddLeftMono`：∀ {α : Type u_1} [inst : AddCommMonoid
 α] [inst_1 : Preorder α] [IsOrderedAddMonoid α], AddLeftMono α
· 使用定理 `nat_card_alternatingGroup`：nat_card_alternatingGroup [Nontrivial α] : Na
t.card (alternatingGroup α) = (Nat.card α).factorial / 2
· 使用定理 `of_decide_eq_true`：∀ {p : Prop} [inst : Decidable p], decide p = true → 
p
-/
theorem card_of_card_eq_four (hα4 : Nat.card α = 4) :
    Nat.card (alternatingGroup α) = 12 := by
  have : Nontrivial α := by
    rw [← Finite.one_lt_card_iff_nontrivial, hα4]
    simp
  rw [nat_card_alternatingGroup, hα4]
  decide
/-
**alternatingGroup.card_two_sylow_of_card_eq_four** 是 Mathlib 中的一个定理，位于命名空间 `alt
ernatingGroup`。
形式化陈述：card_two_sylow_of_card_eq_four (hα4 : Nat.card α = 4) (S : Sylow 2 (altern
atingGroup α)) : Nat.card S = 4
参数：hα4 : Nat.card α = 4；S : Sylow 2 (alternatingGroup α)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Sylow.card_eq_multiplicity`：card_eq_multiplicity [Finite G] {p : Nat} [h
p : Fact p.Prime] (P : Sylow p G) : Nat.card P = p ^ Nat.factorization (Nat.card
 G) p
· 使用定理 `Subgroup.instFiniteSubtypeMem`：∀ {G : Type u_1} [inst : Group G] (K : Su
bgroup G) [Finite G], Finite ↥K
· 使用定理 `Finite.of_fintype`：∀ (α : Type u_4) [Fintype α], Finite α
· 使用定理 `alternatingGroup.card_of_card_eq_four`：card_of_card_eq_four (hα4 : Nat.c
ard α = 4) : Nat.card (alternatingGroup α) = 12
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Nat.factorization_mul_apply_of_coprime`：factorization_mul_apply_of_copri
me {p a b : Nat} (hab : Coprime a b) : (a * b).factorization p = a.factorization
 p + b.factorization p
· 使用定理 `of_decide_eq_true`：∀ {p : Prop} [inst : Decidable p], decide p = true → 
p
· 使用定理 `Nat.factorization_pow`：factorization_pow (n k : Nat) : factorization (n 
^ k) = k • n.factorization
· 使用定理 `Finsupp.coe_smul`：coe_smul [Zero M] [SMulZeroClass R M] (b : R) (v : α -
>₀ M) : ⇑(b • v) = b • ⇑v
· 使用定理 `Pi.smul_apply`：∀ {ι : Type u_1} {α : Type u_2} {M : ι → Type u_5} [inst 
: (i : ι) → SMul α (M i)] (a : α) (f : (i : ι) → M i) (i : ι),   (a • f) i = a •
 f …
· 使用引理 `smul_eq_mul`：smul_eq_mul {α : Type*} [Mul α] (a b : α) : a • b = a * b
· 使用定理 `Nat.Prime.factorization_self`：∀ {p : ℕ}, Nat.Prime p → p.factorization p
 = 1
· 使用定理 `Nat.prime_two`：prime_two : Prime 2
· 使用定理 `Nat.factorization_eq_zero_of_not_dvd`：factorization_eq_zero_of_not_dvd {
n p : Nat} (h : ¬p ∣ n) : n.factorization p = 0
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `mul_one`：mul_one : forall a : M, a * 1 = a
· 使用定理 `add_zero`：∀ {M : Type u} [inst : AddZeroClass M] (a : M), a + 0 = a
-/
theorem card_two_sylow_of_card_eq_four (hα4 : Nat.card α = 4) (S : Sylow 2 (alternatingGroup α)) :
    Nat.card S = 4 := by
  rw [Sylow.card_eq_multiplicity, card_of_card_eq_four hα4]
  have : 12 = 2 ^ 2 * 3 := by simp
  rw [this, Nat.factorization_mul_apply_of_coprime (by decide), Nat.factorization_pow,
    Finsupp.coe_smul, Pi.smul_apply, smul_eq_mul, Nat.prime_two.factorization_self,
    Nat.factorization_eq_zero_of_not_dvd (by decide)]
  simp
/-
**alternatingGroup.coe_two_sylow_of_card_eq_four** 是 Mathlib 中的一个定理，位于命名空间 `alte
rnatingGroup`。
形式化陈述：coe_two_sylow_of_card_eq_four (hα4 : Nat.card α = 4) (S : Sylow 2 (alterna
tingGroup α)) : (S : Set (alternatingGroup α)) = {1} union {g : alternatingGroup
 α | (g : Perm α).cycleType = {2, 2}}
参数：hα4 : Nat.card α = 4；S : Sylow 2 (alternatingGroup α)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.eq_of_subset_of_card_le`：eq_of_subset_of_card_le {s t : Set α} [Fint
ype s] [Fintype t] (hsub : s subseteq t) (hcard : Fintype.card t <= Fintype.card
 s) : s = t
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `IsPGroup.iff_orderOf`：iff_orderOf [Fact p.Prime] : IsPGroup p G ↔ forall
 g : G, exists k, orderOf g = p ^ k
· 使用定理 `Sylow.isPGroup'`：∀ {p : ℕ} {G : Type u_1} [inst : Group G] (self : Sylow
 p G), IsPGroup p ↥↑self
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `orderOf_submonoid`：orderOf_submonoid {H : Submonoid G} (y : H) : orderOf
 (y : G) = orderOf y
· 使用引理 `Subgroup.orderOf_mk`：orderOf_mk (a : G) (ha) : orderOf (⟨a, ha⟩ : H) = o
rderOf a
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `SubmonoidClass.toOneMemClass`：∀ {S : Type u_3} {M : outParam (Type u_4)}
 {inst : MulOneClass M} {inst_1 : SetLike S M} [self : SubmonoidClass S M],   On
eMemClass S M
· 使用定理 `SubgroupClass.toSubmonoidClass`：∀ {S : Type u_3} {G : outParam (Type u_4
)} {inst : DivInvMonoid G} {inst_1 : SetLike S G} [self : SubgroupClass S G],   
SubmonoidClass S G
· 使用定理 `Subgroup.instSubgroupClass`：∀ {G : Type u_1} [inst : Group G], SubgroupC
lass (Subgroup G) G
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `alternatingGroup.mem_kleinFour_of_order_two_pow`：mem_kleinFour_of_order_
two_pow (hα4 : Nat.card α = 4) {g : Perm α} (hg0 : g in alternatingGroup α) {n :
 Nat} (hg : orderOf g ∣ 2 ^ n) : g.cy…
· 使用定理 `Subtype.property`：∀ {α : Sort u} {p : α → Prop} (self : Subtype p), p ↑s
elf
· 使用定理 `Eq.dvd`：∀ {α : Type u_1} [inst : Monoid α] {a b : α}, a = b → a ∣ b
· 使用定理 `Eq.trans_ge`：∀ {α : Type u_1} {a b c : α} [inst : LE α], a = b → c ≤ b →
 c ≤ a
· 使用定理 `alternatingGroup.card_two_sylow_of_card_eq_four`：card_two_sylow_of_card_
eq_four (hα4 : Nat.card α = 4) (S : Sylow 2 (alternatingGroup α)) : Nat.card S =
 4
· 使用引理 `Nat.card_eq_card_toFinset`：card_eq_card_toFinset (s : Set α) [Fintype s]
 : Nat.card s = s.toFinset.card
· 使用定理 `Set.toFinset_union`：toFinset_union [Fintype (s union t : Set _)] : (s un
ion t).toFinset = s.toFinset union t.toFinset
· 使用定理 `Set.toFinset_singleton`：toFinset_singleton (a : α) [Fintype ({a} : Set α
)] : ({a} : Set α).toFinset = {a}
· 使用定理 `Set.toFinset_ofPred`：toFinset_ofPred [Fintype α] (p : α -> Prop) [Decida
blePred p] [Fintype { x | p x }] : Set.toFinset {x | p x} = Finset.univ.filter p
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用定理 `Finset.card_union_le`：card_union_le (s t : Finset α) : #(s union t) <= #
s + #t
· 使用定理 `Finset.card_singleton`：card_singleton (a : α) : #{a} = 1
· 使用定理 `AlternatingGroup.card_of_cycleType`：card_of_cycleType (m : Multiset Nat)
 : #{g : alternatingGroup α | (g : Equiv.Perm α).cycleType = m} = if (m.sum <= F
intype.card α ∧ forall a…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Nat.card_eq_fintype_card`：card_eq_fintype_card [Fintype α] : Nat.card α 
= Fintype.card α
（共 31 条，此处仅展示前 30 条）
-/
theorem coe_two_sylow_of_card_eq_four
    (hα4 : Nat.card α = 4) (S : Sylow 2 (alternatingGroup α)) :
    (S : Set (alternatingGroup α)) =
      {1} ∪ {g : alternatingGroup α | (g : Perm α).cycleType = {2, 2}} := by
  classical
  refine Set.eq_of_subset_of_card_le (fun k hk ↦ ?_) ?_
  · -- inclusion S ⊆ {1} ∪ {g |  cycleType g = { 2, 2 }}
    obtain ⟨n, hn⟩ := (IsPGroup.iff_orderOf.mp S.isPGroup') ⟨k, hk⟩
    replace hn : (orderOf (k : Perm α)) = 2 ^ n := by simpa using hn
    convert! mem_kleinFour_of_order_two_pow hα4 k.2 hn.dvd
    simp
  · -- card (kleinFour α) ≤ card S
    simp_rw [← Nat.card_eq_fintype_card]
    refine (card_two_sylow_of_card_eq_four hα4 S).trans_ge ?_
    rw [Nat.card_eq_card_toFinset, Set.toFinset_union, Set.toFinset_singleton, Set.toFinset_ofPred]
    apply (Finset.card_union_le _ _).trans
    rw [Finset.card_singleton, AlternatingGroup.card_of_cycleType, ← Nat.card_eq_fintype_card, hα4]
    decide
/-
**alternatingGroup.two_sylow_eq_kleinFour_of_card_eq_four** 是 Mathlib 中的一个定理，位于命
名空间 `alternatingGroup`。
形式化陈述：two_sylow_eq_kleinFour_of_card_eq_four (hα4 : Nat.card α = 4) (S : Sylow 2
 (alternatingGroup α)) : S = kleinFour α
参数：hα4 : Nat.card α = 4；S : Sylow 2 (alternatingGroup α)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `alternatingGroup.kleinFour.eq_1`：∀ (α : Type u_1) [inst : DecidableEq α]
 [inst_1 : Fintype α],   alternatingGroup.kleinFour α = Subgroup.closure {g | (↑
g).cycleType = {2, 2}…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Subgroup.closure_insert_one`：closure_insert_one (s : Set G) : closure (i
nsert 1 s) = closure s
· 使用定理 `Set.singleton_union`：singleton_union : {a} union s = insert a s
· 使用定理 `alternatingGroup.coe_two_sylow_of_card_eq_four`：coe_two_sylow_of_card_eq
_four (hα4 : Nat.card α = 4) (S : Sylow 2 (alternatingGroup α)) : (S : Set (alte
rnatingGroup α)) = {1} union {g : al…
· 使用定理 `Subgroup.closure_eq`：closure_eq : closure (K : Set G) = K
-/
theorem two_sylow_eq_kleinFour_of_card_eq_four
    (hα4 : Nat.card α = 4) (S : Sylow 2 (alternatingGroup α)) :
    S = kleinFour α := by
  rw [kleinFour, ← Subgroup.closure_insert_one,
    ← Set.singleton_union, ← coe_two_sylow_of_card_eq_four hα4]
  exact S.closure_eq.symm
/-
**alternatingGroup.subsingleton_two_sylow** 是 Mathlib 中的一个定理，位于命名空间 `alternating
Group`。
形式化陈述：subsingleton_two_sylow (hα4 : Nat.card α = 4) : Subsingleton (Sylow 2 (alt
ernatingGroup α)) where allEq S T
参数：hα4 : Nat.card α = 4。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Sylow.ext_iff`：∀ {p : ℕ} {G : Type u_1} [inst : Group G] {P Q : Sylow p 
G}, P = Q ↔ ↑P = ↑Q
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `alternatingGroup.two_sylow_eq_kleinFour_of_card_eq_four`：two_sylow_eq_kl
einFour_of_card_eq_four (hα4 : Nat.card α = 4) (S : Sylow 2 (alternatingGroup α)
) : S = kleinFour α
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem subsingleton_two_sylow (hα4 : Nat.card α = 4) :
    Subsingleton (Sylow 2 (alternatingGroup α)) where
  allEq S T := by
    rw [Sylow.ext_iff]
    simp [two_sylow_eq_kleinFour_of_card_eq_four hα4]
/-
**alternatingGroup.characteristic_kleinFour** 是 Mathlib 中的一个定理，位于命名空间 `alternati
ngGroup`。
形式化陈述：characteristic_kleinFour (hα4 : Nat.card α = 4) : (kleinFour α).Characteri
stic
参数：hα4 : Nat.card α = 4。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `alternatingGroup.subsingleton_two_sylow`：subsingleton_two_sylow (hα4 : N
at.card α = 4) : Subsingleton (Sylow 2 (alternatingGroup α)) where allEq S T
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `alternatingGroup.two_sylow_eq_kleinFour_of_card_eq_four`：two_sylow_eq_kl
einFour_of_card_eq_four (hα4 : Nat.card α = 4) (S : Sylow 2 (alternatingGroup α)
) : S = kleinFour α
-/
theorem characteristic_kleinFour (hα4 : Nat.card α = 4) :
    (kleinFour α).Characteristic := by
  obtain ⟨S : Sylow 2 (alternatingGroup α)⟩ := Sylow.nonempty (G := alternatingGroup α)
  have _ := subsingleton_two_sylow hα4
  rw [← two_sylow_eq_kleinFour_of_card_eq_four hα4 S]
  exact Sylow.characteristic_of_subsingleton _
/-
**alternatingGroup.normal_kleinFour** 是 Mathlib 中的一个定理，位于命名空间 `alternatingGroup`
。
形式化陈述：normal_kleinFour (hα4 : Nat.card α = 4) : (kleinFour α).Normal
参数：hα4 : Nat.card α = 4。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `alternatingGroup.characteristic_kleinFour`：characteristic_kleinFour (hα4
 : Nat.card α = 4) : (kleinFour α).Characteristic
· 使用定理 `Subgroup.normal_of_characteristic`：∀ {G : Type u_1} [inst : Group G] (H 
: Subgroup G) [h : H.Characteristic], H.Normal
-/
theorem normal_kleinFour (hα4 : Nat.card α = 4) :
    (kleinFour α).Normal := by
  have _ := characteristic_kleinFour hα4
  apply Subgroup.normal_of_characteristic
/-
**alternatingGroup.kleinFour_card_of_card_eq_four** 是 Mathlib 中的一个定理，位于命名空间 `alt
ernatingGroup`。
形式化陈述：kleinFour_card_of_card_eq_four (hα4 : Nat.card α = 4) : Nat.card (kleinFou
r α) = 4
参数：hα4 : Nat.card α = 4。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `alternatingGroup.two_sylow_eq_kleinFour_of_card_eq_four`：two_sylow_eq_kl
einFour_of_card_eq_four (hα4 : Nat.card α = 4) (S : Sylow 2 (alternatingGroup α)
) : S = kleinFour α
· 使用定理 `alternatingGroup.card_two_sylow_of_card_eq_four`：card_two_sylow_of_card_
eq_four (hα4 : Nat.card α = 4) (S : Sylow 2 (alternatingGroup α)) : Nat.card S =
 4
-/
theorem kleinFour_card_of_card_eq_four (hα4 : Nat.card α = 4) :
    Nat.card (kleinFour α) = 4 := by
  obtain ⟨S : Sylow 2 (alternatingGroup α)⟩ := Sylow.nonempty (G := alternatingGroup α)
  rw [← two_sylow_eq_kleinFour_of_card_eq_four hα4 S, card_two_sylow_of_card_eq_four hα4 S]
/-
**alternatingGroup.coe_kleinFour_of_card_eq_four** 是 Mathlib 中的一个定理，位于命名空间 `alte
rnatingGroup`。
形式化陈述：coe_kleinFour_of_card_eq_four (hα4 : Nat.card α = 4) : (kleinFour α : Set 
(alternatingGroup α)) = {1} union {g : alternatingGroup α | (g : Equiv.Perm α).c
ycleType = {2, 2}}
参数：hα4 : Nat.card α = 4。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `alternatingGroup.two_sylow_eq_kleinFour_of_card_eq_four`：two_sylow_eq_kl
einFour_of_card_eq_four (hα4 : Nat.card α = 4) (S : Sylow 2 (alternatingGroup α)
) : S = kleinFour α
· 使用定理 `alternatingGroup.coe_two_sylow_of_card_eq_four`：coe_two_sylow_of_card_eq
_four (hα4 : Nat.card α = 4) (S : Sylow 2 (alternatingGroup α)) : (S : Set (alte
rnatingGroup α)) = {1} union {g : al…
-/
theorem coe_kleinFour_of_card_eq_four (hα4 : Nat.card α = 4) :
    (kleinFour α : Set (alternatingGroup α)) =
      {1} ∪ {g : alternatingGroup α | (g : Equiv.Perm α).cycleType = {2, 2}} := by
  obtain ⟨S : Sylow 2 (alternatingGroup α)⟩ := Sylow.nonempty (G := alternatingGroup α)
  rw [← two_sylow_eq_kleinFour_of_card_eq_four hα4 S]
  exact coe_two_sylow_of_card_eq_four hα4 S
/-
**alternatingGroup.exponent_kleinFour_of_card_eq_four** 是 Mathlib 中的一个定理，位于命名空间 
`alternatingGroup`。
形式化陈述：exponent_kleinFour_of_card_eq_four (hα4 : Nat.card α = 4) : Monoid.exponen
t (kleinFour α) = 2
参数：hα4 : Nat.card α = 4。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Monoid.exponent_dvd`：exponent_dvd {n : Nat} : exponent G ∣ n ↔ forall g 
: G, orderOf g ∣ n
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用引理 `Subgroup.orderOf_mk`：orderOf_mk (a : G) (ha) : orderOf (⟨a, ha⟩ : H) = o
rderOf a
· 使用定理 `alternatingGroup.coe_kleinFour_of_card_eq_four`：coe_kleinFour_of_card_eq
_four (hα4 : Nat.card α = 4) : (kleinFour α : Set (alternatingGroup α)) = {1} un
ion {g : alternatingGroup α | (g : E…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `SetLike.mem_coe`：mem_coe {x : B} : x in (p : Set B) ↔ x in p
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `one_pow`：one_pow {a : R} (b : Nat) (ha : IsNat a 1) : a ^ b = a
· 使用定理 `Equiv.Perm.lcm_cycleType`：lcm_cycleType (σ : Perm α) : σ.cycleType.lcm =
 orderOf σ
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Multiset.lcm_cons`：lcm_cons (a : α) (s : Multiset α) : (a ::ₘ s).lcm = G
CDMonoid.lcm a s.lcm
· 使用定理 `Multiset.lcm_singleton`：lcm_singleton {a : α} : ({a} : Multiset α).lcm =
 normalize a
· 使用定理 `normalize_eq`：normalize_eq (x : α) : normalize x = x
· 使用定理 `Unique.instSubsingleton`：∀ {α : Sort u_1} [Unique α], Subsingleton α
· 使用定理 `lcm_same`：lcm_same [NormalizedGCDMonoid α] (a : α) : lcm a a = normalize
 a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `pow_orderOf_eq_one`：pow_orderOf_eq_one (x : G) : x ^ orderOf x = 1
· 使用定理 `Or.resolve_left`：∀ {a b : Prop}, a ∨ b → ¬a → b
· 使用定理 `Nat.dvd_prime`：dvd_prime {p m : Nat} (pp : Prime p) : m ∣ p ↔ m = 1 ∨ m 
= p
· 使用定理 `Nat.prime_two`：prime_two : Prime 2
· 使用定理 `Monoid.exp_eq_one_iff`：exp_eq_one_iff : exponent G = 1 ↔ Subsingleton G
· 使用定理 `Finite.card_le_one_iff_subsingleton`：card_le_one_iff_subsingleton [Finit
e α] : Nat.card α <= 1 ↔ Subsingleton α
· 使用定理 `Subgroup.instFiniteSubtypeMem`：∀ {G : Type u_1} [inst : Group G] (K : Su
bgroup G) [Finite G], Finite ↥K
· 使用定理 `Finite.of_fintype`：∀ (α : Type u_4) [Fintype α], Finite α
· 使用定理 `alternatingGroup.kleinFour_card_of_card_eq_four`：kleinFour_card_of_card_
eq_four (hα4 : Nat.card α = 4) : Nat.card (kleinFour α) = 4
· 使用定理 `of_decide_eq_true`：∀ {p : Prop} [inst : Decidable p], decide p = true → 
p
-/
theorem exponent_kleinFour_of_card_eq_four (hα4 : Nat.card α = 4) :
    Monoid.exponent (kleinFour α) = 2 := by
  have : Monoid.exponent (kleinFour α) ∣ 2 := by
    rw [Monoid.exponent_dvd]
    rintro ⟨⟨g, hg⟩, hg'⟩
    simp only [Subgroup.orderOf_mk, orderOf_dvd_iff_pow_eq_one]
    rw [← SetLike.mem_coe, coe_kleinFour_of_card_eq_four hα4] at hg'
    rcases hg' with hg' | hg'
    · convert! one_pow _
      simpa only [Set.mem_singleton_iff, Subgroup.mk_eq_one] using hg'
    · convert! pow_orderOf_eq_one g
      rw [← Equiv.Perm.lcm_cycleType, hg']
      simp
  rw [Nat.dvd_prime Nat.prime_two] at this
  apply Or.resolve_left this
  rw [Monoid.exp_eq_one_iff, ← Finite.card_le_one_iff_subsingleton,
    kleinFour_card_of_card_eq_four hα4]
  decide
/-
**alternatingGroup.kleinFour_isKleinFour** 是 Mathlib 中的一个定理，位于命名空间 `alternatingG
roup`。
形式化陈述：kleinFour_isKleinFour (hα4 : Nat.card α = 4) : IsKleinFour (kleinFour α) w
here card_four
参数：hα4 : Nat.card α = 4。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `alternatingGroup.kleinFour_card_of_card_eq_four`：kleinFour_card_of_card_
eq_four (hα4 : Nat.card α = 4) : Nat.card (kleinFour α) = 4
· 使用定理 `alternatingGroup.exponent_kleinFour_of_card_eq_four`：exponent_kleinFour_
of_card_eq_four (hα4 : Nat.card α = 4) : Monoid.exponent (kleinFour α) = 2
-/
theorem kleinFour_isKleinFour (hα4 : Nat.card α = 4) :
    IsKleinFour (kleinFour α) where
  card_four := kleinFour_card_of_card_eq_four hα4
  exponent_two := exponent_kleinFour_of_card_eq_four hα4
/-
**alternatingGroup.kleinFour_eq_commutator** 是 Mathlib 中的一个定理，位于命名空间 `alternatin
gGroup`。
形式化陈述：kleinFour_eq_commutator (hα4 : Nat.card α = 4) : kleinFour α = commutator 
(alternatingGroup α)
参数：hα4 : Nat.card α = 4。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `alternatingGroup.normal_kleinFour`：normal_kleinFour (hα4 : Nat.card α = 
4) : (kleinFour α).Normal
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Nat.mul_left_inj`：∀ {a b c : ℕ}, a ≠ 0 → (b * a = c * a ↔ b = c)
· 使用定理 `alternatingGroup.kleinFour_card_of_card_eq_four`：kleinFour_card_of_card_
eq_four (hα4 : Nat.card α = 4) : Nat.card (kleinFour α) = 4
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Nat.instAtLeastTwoHAddOfNat`：∀ (n : ℕ) [NeZero n], (n + 1).AtLeastTwo
· 使用定理 `Nat.instNeZeroSucc`：∀ {n : ℕ}, NeZero (n + 1)
· 使用定理 `not_false_eq_true`：(¬False) = True
· 使用定理 `Subgroup.card_eq_card_quotient_mul_card_subgroup`：card_eq_card_quotient_
mul_card_subgroup (s : Subgroup α) : Nat.card α = Nat.card (α ⧸ s) * Nat.card s
· 使用定理 `alternatingGroup.card_of_card_eq_four`：card_of_card_eq_four (hα4 : Nat.c
ard α = 4) : Nat.card (alternatingGroup α) = 12
· 使用定理 `Subgroup.Normal.quotient_commutative_iff_commutator_le`：Subgroup.Normal.
quotient_commutative_iff_commutator_le {N : Subgroup G} [N.Normal] : IsMulCommut
ative (G ⧸ N) ↔ _root_.commutator G <= N
· 使用定理 `isCyclic_of_prime_card`：isCyclic_of_prime_card {p : Nat} [hp : Fact p.Pr
ime] (h : Nat.card α = p) : IsCyclic α
· 使用定理 `ne_eq`：∀ {α : Sort u_1} (a b : α), (a ≠ b) = ¬a = b
· 使用定理 `commutator_eq_bot_iff_center_eq_top`：commutator_eq_bot_iff_center_eq_top
 : commutator G = ⊥ ↔ Subgroup.center G = ⊤
· 使用定理 `alternatingGroup.center_eq_bot`：center_eq_bot (hα4 : 4 <= Nat.card α) : 
Subgroup.center (alternatingGroup α) = ⊥
· 使用定理 `le_of_eq`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a = b → a ≤ b
· 使用定理 `bot_ne_top`：bot_ne_top : (⊥ : α) != ⊤
· 使用定理 `Subgroup.nontrivial_iff`：nontrivial_iff : Nontrivial (Subgroup G) ↔ Nont
rivial G
· 使用定理 `Finite.one_lt_card_iff_nontrivial`：one_lt_card_iff_nontrivial [Finite α]
 : 1 < Nat.card α ↔ Nontrivial α
· 使用定理 `Subgroup.instFiniteSubtypeMem`：∀ {G : Type u_1} [inst : Group G] (K : Su
bgroup G) [Finite G], Finite ↥K
· 使用定理 `Finite.of_fintype`：∀ (α : Type u_4) [Fintype α], Finite α
· 使用定理 `IsOrderedAddMonoid.toAddLeftMono`：∀ {α : Type u_1} [inst : AddCommMonoid
 α] [inst_1 : Preorder α] [IsOrderedAddMonoid α], AddLeftMono α
· 使用定理 `Or.resolve_left`：∀ {a b : Prop}, a ∨ b → ¬a → b
· 使用定理 `Subgroup.bot_or_exists_ne_one`：bot_or_exists_ne_one (H : Subgroup G) : H
 = ⊥ ∨ exists x in H, x != (1 : G)
· 使用定理 `Set.mem_ofPred_eq`：mem_ofPred_eq {x : α} {p : α -> Prop} : (x in {y | p 
y}) = p x
· 使用定理 `Set.mem_singleton_iff`：mem_singleton_iff {a b : α} : a in ({b} : Set α) 
↔ a = b
· 使用定理 `Set.mem_union`：mem_union (x : α) (a b : Set α) : x in a union b ↔ x in a
 ∨ x in b
· 使用定理 `alternatingGroup.coe_kleinFour_of_card_eq_four`：coe_kleinFour_of_card_eq
_four (hα4 : Nat.card α = 4) : (kleinFour α : Set (alternatingGroup α)) = {1} un
ion {g : alternatingGroup α | (g : E…
（共 42 条，此处仅展示前 30 条）
-/
theorem kleinFour_eq_commutator (hα4 : Nat.card α = 4) :
    kleinFour α = commutator (alternatingGroup α) := by
  have _ : (kleinFour α).Normal := normal_kleinFour hα4
  have : Nat.card (alternatingGroup α ⧸ kleinFour α) = 3 := by
    rw [← Nat.mul_left_inj (a := Nat.card (kleinFour α))]
    · rw [← Subgroup.card_eq_card_quotient_mul_card_subgroup]
      rw [card_of_card_eq_four hα4, kleinFour_card_of_card_eq_four hα4]
    rw [kleinFour_card_of_card_eq_four hα4]; simp
  have comm_le : commutator (alternatingGroup α) ≤ kleinFour α := by
    rw [← Subgroup.Normal.quotient_commutative_iff_commutator_le]
    exact (isCyclic_of_prime_card this).isMulCommutative
  have comm_ne_bot : commutator (alternatingGroup α) ≠ ⊥ := by
    rw [ne_eq, commutator_eq_bot_iff_center_eq_top,
      center_eq_bot (le_of_eq hα4.symm)]
    apply @bot_ne_top _ _ _ ?_
    rw [Subgroup.nontrivial_iff, ← Finite.one_lt_card_iff_nontrivial,
      card_of_card_eq_four hα4]
    simp
  obtain ⟨k, hk, hk'⟩ := Or.resolve_left (Subgroup.bot_or_exists_ne_one _) comm_ne_bot
  suffices hk22 : (k : Equiv.Perm α).cycleType = {2, 2} by
    refine le_antisymm ?_ comm_le
    intro g hg
    rw [← SetLike.mem_coe, coe_kleinFour_of_card_eq_four hα4,
      Set.mem_union, Set.mem_singleton_iff, Set.mem_ofPred_eq] at hg
    rcases hg with ⟨rfl⟩ | hg
    · exact Subgroup.one_mem _
    · rw [← hg, ← Equiv.Perm.isConj_iff_cycleType_eq, isConj_iff] at hk22
      obtain ⟨c, hc⟩ := hk22
      rw [← MulAut.conjNormal_apply, Subtype.coe_inj] at hc
      simp only [commutator, ← hc]
      let fc : MulAut (alternatingGroup α) := MulAut.conjNormal c
      suffices (⊤ : Subgroup (alternatingGroup α)) =
        Subgroup.map fc.toMonoidHom (⊤ : Subgroup (alternatingGroup α)) by
        rw [this, ← Subgroup.map_commutator]
        refine Subgroup.mem_map_of_mem _ hk
      simp
  have hk2 := comm_le hk
  rw [← SetLike.mem_coe, coe_kleinFour_of_card_eq_four hα4,
    Set.mem_union, Set.mem_singleton_iff, Set.mem_ofPred_eq] at hk2
  exact hk2.resolve_left hk'

end alternatingGroup

