/-
Copyright (c) 2024 Yury Kudryashov. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Yury Kudryashov
-/
module

public import Mathlib.NumberTheory.Real.Irrational
public import Mathlib.Topology.Instances.AddCircle.Defs
public import Mathlib.Topology.Algebra.Order.Archimedean

/-!
# Irrational rotation is minimal

In this file we prove that the multiples of an irrational element of an `AddCircle` are dense.
Moreover, an additive subgroup of the circle is dense
iff it is not generated by a single element of finite additive order.
-/

public section

open Set Filter
open scoped Pointwise Topology

/-- The additive subgroup of real numbers generated by `a` and `b` is dense
iff `a / b` is an irrational number.

Here we rely on the fact that `a / 0 = 0` in Mathlib,
so we don't need to add `b ≠ 0` as an assumption. -/
/-
**dense_addSubgroupClosure_pair_iff** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：dense_addSubgroupClosure_pair_iff {a b : Real} : Dense (AddSubgroup.closur
e {a, b} : Set Real) ↔ Irrational (a / b)
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `eq_or_ne`：eq_or_ne {α : Sort*} (x y : α) : x = y ∨ x != y
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Set.pair_comm`：pair_comm (a b : α) : ({a, b} : Set α) = {b, a}
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `AddSubgroup.closure_insert_zero`：∀ {G : Type u_1} [inst : AddGroup G] (s
 : Set G), AddSubgroup.closure (insert 0 s) = AddSubgroup.closure s
· 使用定理 `div_zero`：div_zero (a : G₀) : a / 0 = 0
· 使用定理 `iff_false`：∀ (p : Prop), (p ↔ False) = ¬p
· 使用定理 `not_denseRange_zsmul`：∀ {G : Type u_1} [inst : TopologicalSpace G] [inst
_1 : AddCommGroup G] [inst_2 : LinearOrder G] [IsOrderedAddMonoid G]   [OrderTop
ology G] […
· 使用定理 `instOrderTopologyReal`：OrderTopology ℝ
· 使用定理 `LinearOrderedSemiField.toDenselyOrdered`：∀ {α : Type u_2} [inst : Semifi
eld α] [inst_1 : PartialOrder α] [PosMulReflectLT α] [IsStrictOrderedRing α],   
DenselyOrdered α
· 使用定理 `PosMulReflectLE.toPosMulReflectLT`：∀ {α : Type u_1} [inst : MulZeroClass
 α] [inst_1 : PartialOrder α] [PosMulReflectLE α], PosMulReflectLT α
· 使用定理 `PosMulStrictMono.toPosMulReflectLE`：∀ {α : Type u_1} [inst : Mul α] [ins
t_1 : Zero α] [inst_2 : LinearOrder α] [PosMulStrictMono α], PosMulReflectLE α
· 使用定理 `IsStrictOrderedRing.toPosMulStrictMono`：∀ {R : Type u_1} {inst : Semirin
g R} {inst_1 : PartialOrder R} [self : IsStrictOrderedRing R], PosMulStrictMono 
R
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Dense.mono`：Dense.mono (h : s₁ subseteq s₂) (hd : Dense s₁) : Dense s₂
· 使用定理 `AddSubgroup.coe_zmultiples`：∀ {G : Type u_1} [inst : AddGroup G] (g : G)
, ↑(AddSubgroup.zmultiples g) = Set.range fun x => x • g
· 使用定理 `SetLike.coe_subset_coe`：∀ {A : Type u_1} {B : Type u_2} [inst : SetLike 
A B] [inst_1 : LE A] [IsConcreteLE A B] {S T : A}, ↑S ⊆ ↑T ↔ S ≤ T
· 使用定理 `instIsConcreteLE`：∀ (A : Type u_1) (B : Type u_2) [inst : SetLike A B], 
IsConcreteLE A B
· 使用定理 `AddSubgroup.closure_le`：∀ {G : Type u_1} [inst : AddGroup G] (K : AddSub
group G) {k : Set G}, AddSubgroup.closure k ≤ K ↔ k ⊆ ↑K
· 使用定理 `Set.pair_subset_iff`：pair_subset_iff : {a, b} subseteq s ↔ a in s ∧ b in
 s
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `zsmul_eq_mul`：∀ {α : Type u_3} [inst : NonAssocRing α] (a : α) (n : ℤ), 
n • a = ↑n * a
· 使用定理 `mul_div_left_comm`：mul_div_left_comm : a * (b / c) = b * (a / c)
· 使用定理 `Mathlib.Tactic.FieldSimp.eq_eq_cancel_eq`：eq_eq_cancel_eq {M : Type*} [M
onoidWithZero M] [IsLeftCancelMulZero M] {e₁ e₂ f₁ f₂ L : M} (H₁ : e₁ = L * f₁) 
(H₂ : e₂ = L * f₂) (HL : L != …
· 使用定理 `IsCancelMulZero.toIsLeftCancelMulZero`：∀ {M₀ : Type u} {inst : Mul M₀} {
inst_1 : Zero M₀} [self : IsCancelMulZero M₀], IsLeftCancelMulZero M₀
· 使用定理 `instIsCancelMulZero`：∀ {G₀ : Type u_2} [inst : GroupWithZero G₀], IsCanc
elMulZero G₀
· 使用定理 `Mathlib.Tactic.FieldSimp.eq_mul_of_eq_eq_eq_mul`：eq_mul_of_eq_eq_eq_mul 
{M : Type*} [Mul M] {a b c D e f : M} (h₁ : a = b) (h₂ : b = c) (h₃ : c = D * e)
 (h₄ : e = f) : a = D * f
· 使用定理 `congr_arg₂`：∀ {α : Sort u_1} {β : Sort u_2} {γ : Sort u_3} (f : α → β → 
γ) {x x' : α} {y y' : β}, x = x' → y = y' → f x y = f x' y'
（共 71 条，此处仅展示前 30 条）

--- 原说明 ---
The additive subgroup of real numbers generated by `a` and `b` is dense
iff `a / b` is an irrational number.

Here we rely on the fact that `a / 0 = 0` in Mathlib,
so we don't need to add `b ≠ 0` as an assumption.
-/
theorem dense_addSubgroupClosure_pair_iff {a b : ℝ} :
    Dense (AddSubgroup.closure {a, b} : Set ℝ) ↔ Irrational (a / b) := by
  rcases eq_or_ne b 0 with rfl | hb
  · rw [pair_comm]
    simpa [← AddSubgroup.zmultiples_eq_closure] using! not_denseRange_zsmul
  constructor
  · rintro hd ⟨r, hr⟩
    refine not_denseRange_zsmul (a := b / r.den) <| hd.mono ?_
    rw [← AddSubgroup.coe_zmultiples, SetLike.coe_subset_coe, AddSubgroup.closure_le,
      AddSubgroup.coe_zmultiples, pair_subset_iff]
    refine ⟨⟨r.num, ?_⟩, r.den, ?_⟩
    · simp [field, mul_div_left_comm _ b, ← Rat.cast_def, hr]
    · simp [field]
  · intro h
    contrapose h
    rcases (AddSubgroup.dense_or_cyclic _).resolve_left h with ⟨c, hc⟩
    have : {a, b} ⊆ range (· • c : ℤ → ℝ) := by
      rw [← AddSubgroup.coe_zmultiples, AddSubgroup.zmultiples_eq_closure, ← hc]
      apply AddSubgroup.subset_closure
    rcases pair_subset_iff.1 this with ⟨⟨m, rfl⟩, n, rfl⟩
    simp_all [mul_div_mul_right]

namespace AddCircle

/-- The multiples of a number `a` are dense on a circle of length `p` iff `a / p` is irrational. -/
/-
**AddCircle.denseRange_zsmul_coe_iff** 是 Mathlib 中的一个定理，位于命名空间 `AddCircle`。
形式化陈述：denseRange_zsmul_coe_iff {a p : Real} : DenseRange (· • a : Int -> AddCirc
le p) ↔ Irrational (a / p)
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AddSubgroup.normal_of_isAddCommutative`：∀ {G : Type u_1} [inst : AddGrou
p G] [IsAddCommutative G] (H : AddSubgroup G), H.Normal
· 使用定理 `AddCommMagma.to_isCommutative`：∀ {G : Type u_1} [inst : AddCommMagma G],
 IsAddCommutative G
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `dense_addSubgroupClosure_pair_iff`：dense_addSubgroupClosure_pair_iff {a 
b : Real} : Dense (AddSubgroup.closure {a, b} : Set Real) ↔ Irrational (a / b)
· 使用定理 `DenseRange.eq_1`：∀ {X : Type u} [inst : TopologicalSpace X] {α : Type u_
1} (f : α → X), DenseRange f = Dense (Set.range f)
· 使用定理 `QuotientAddGroup.dense_preimage_mk`：∀ {G : Type u_1} [inst : Topological
Space G] [inst_1 : AddGroup G] [SeparatelyContinuousAdd G] {N : AddSubgroup G}  
 {s : Set (G ⧸ N)}, Dens…
· 使用定理 `instSeparatelyContinuousAddOfContinuousAdd`：∀ {M : Type u_1} [inst : Top
ologicalSpace M] [inst_1 : Add M] [ContinuousAdd M], SeparatelyContinuousAdd M
· 使用定理 `IsSemitopologicalSemiring.toContinuousAdd`：∀ {R : Type u_2} {inst : Topo
logicalSpace R} {inst_1 : NonUnitalNonAssocSemiring R}   [self : IsSemitopologic
alSemiring R], ContinuousAdd R
· 使用定理 `IsSemitopologicalRing.toIsSemitopologicalSemiring`：∀ {R : Type u_2} {ins
t : TopologicalSpace R} {inst_1 : NonUnitalNonAssocRing R} [self : IsSemitopolog
icalRing R],   IsSemitopologicalSemirin…
· 使用定理 `IsTopologicalRing.toIsSemitopologicalRing`：∀ (R : Type u_2) [inst : Topo
logicalSpace R] [inst_1 : NonUnitalNonAssocRing R] [IsTopologicalRing R],   IsSe
mitopologicalRing R
· 使用定理 `instIsTopologicalRingReal`：IsTopologicalRing ℝ
· 使用定理 `QuotientAddGroup.coe_mk'`：∀ {G : Type u_1} [inst : AddGroup G] (N : AddS
ubgroup G) [nN : N.Normal],   ⇑(QuotientAddGroup.mk' N) = QuotientAddGroup.mk
· 使用定理 `AddSubgroup.coe_zmultiples`：∀ {G : Type u_1} [inst : AddGroup G] (g : G)
, ↑(AddSubgroup.zmultiples g) = Set.range fun x => x • g
· 使用定理 `AddSubgroup.coe_comap`：∀ {G : Type u_1} [inst : AddGroup G] {N : Type u_
5} [inst_1 : AddGroup N] (K : AddSubgroup N) (f : G →+ N),   ↑(AddSubgroup.comap
 f K) = ⇑f …
· 使用定理 `AddMonoidHom.map_zmultiples`：∀ {G : Type u_1} [inst : AddGroup G] {N : T
ype u_3} [inst_1 : AddGroup N] (f : G →+ N) (x : G),   AddSubgroup.map f (AddSub
group.zmultiples …
· 使用定理 `AddSubgroup.comap_map_eq`：∀ {G : Type u_1} [inst : AddGroup G] {N : Type
 u_5} [inst_1 : AddGroup N] (f : G →+ N) (H : AddSubgroup G),   AddSubgroup.coma
p f (AddSubgro…
· 使用定理 `QuotientAddGroup.ker_mk'`：∀ {G : Type u_1} [inst : AddGroup G] (N : AddS
ubgroup G) [nN : N.Normal], (QuotientAddGroup.mk' N).ker = N
· 使用定理 `AddSubgroup.zmultiples_eq_closure`：∀ {G : Type u_1} [inst : AddGroup G] 
(g : G), AddSubgroup.zmultiples g = AddSubgroup.closure {g}
· 使用定理 `AddSubgroup.closure_union`：∀ {G : Type u_1} [inst : AddGroup G] (s t : S
et G),   AddSubgroup.closure (s ∪ t) = AddSubgroup.closure s ⊔ AddSubgroup.closu
re t
· 使用定理 `Set.insert_eq`：insert_eq (x : α) (s : Set α) : insert x s = ({x} : Set α
) union s
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a

--- 原说明 ---
The multiples of a number `a` are dense on a circle of length `p` iff `a / p` is
 irrational.
-/
theorem denseRange_zsmul_coe_iff {a p : ℝ} :
    DenseRange (· • a : ℤ → AddCircle p) ↔ Irrational (a / p) := by
  rw [← dense_addSubgroupClosure_pair_iff, DenseRange,
    ← QuotientAddGroup.dense_preimage_mk, ← QuotientAddGroup.coe_mk',
    ← AddSubgroup.coe_zmultiples, ← AddSubgroup.coe_comap, ← AddMonoidHom.map_zmultiples,
    AddSubgroup.comap_map_eq, QuotientAddGroup.ker_mk', AddSubgroup.zmultiples_eq_closure,
    AddSubgroup.zmultiples_eq_closure, ← AddSubgroup.closure_union, insert_eq]

/-- The multiples of a number `a` are dense on a circle of length `p > 0`
iff `a` has infinite additive order. -/
/-
**AddCircle.denseRange_zsmul_iff** 是 Mathlib 中的一个定理，位于命名空间 `AddCircle`。
形式化陈述：denseRange_zsmul_iff {p : Real} [Fact (0 < p)] {a : AddCircle p} : DenseRa
nge (· • a : Int -> AddCircle p) ↔ addOrderOf a = 0
参数：0 < p。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AddSubgroup.normal_of_isAddCommutative`：∀ {G : Type u_1} [inst : AddGrou
p G] [IsAddCommutative G] (H : AddSubgroup G), H.Normal
· 使用定理 `AddCommMagma.to_isCommutative`：∀ {G : Type u_1} [inst : AddCommMagma G],
 IsAddCommutative G
· 使用定理 `QuotientAddGroup.mk_surjective`：∀ {α : Type u_1} [inst : AddGroup α] {s 
: AddSubgroup α}, Function.Surjective QuotientAddGroup.mk
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True

--- 原说明 ---
The multiples of a number `a` are dense on a circle of length `p > 0`
iff `a` has infinite additive order.
-/
theorem denseRange_zsmul_iff {p : ℝ} [Fact (0 < p)] {a : AddCircle p} :
    DenseRange (· • a : ℤ → AddCircle p) ↔ addOrderOf a = 0 := by
  rcases QuotientAddGroup.mk_surjective a with ⟨a, rfl⟩
  simp [denseRange_zsmul_coe_iff, isOfFinAddOrder_iff_exists_rat_eq_div, Irrational]

/-- A subgroup of the circle `ℝ⧸pℤ`, `p > 0`, is dense
iff it is not generated by a single element of finite additive order. -/
/-
**AddCircle.dense_addSubgroup_iff_ne_zmultiples** 是 Mathlib 中的一个定理，位于命名空间 `AddCi
rcle`。
形式化陈述：dense_addSubgroup_iff_ne_zmultiples {p : Real} [Fact (0 < p)] {s : AddSubg
roup (AddCircle p)} : Dense (s : Set (AddCircle p)) ↔ forall a, addOrderOf a != 
0 -> s != .zmultiples a
参数：0 < p；AddCircle p。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AddSubgroup.normal_of_isAddCommutative`：∀ {G : Type u_1} [inst : AddGrou
p G] [IsAddCommutative G] (H : AddSubgroup G), H.Normal
· 使用定理 `AddCommMagma.to_isCommutative`：∀ {G : Type u_1} [inst : AddCommMagma G],
 IsAddCommutative G
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `AddCircle.denseRange_zsmul_iff`：denseRange_zsmul_iff {p : Real} [Fact (0
 < p)] {a : AddCircle p} : DenseRange (· • a : Int -> AddCircle p) ↔ addOrderOf 
a = 0
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `DenseRange.eq_1`：∀ {X : Type u} [inst : TopologicalSpace X] {α : Type u_
1} (f : α → X), DenseRange f = Dense (Set.range f)
· 使用定理 `AddSubgroup.coe_zmultiples`：∀ {G : Type u_1} [inst : AddGroup G] (g : G)
, ↑(AddSubgroup.zmultiples g) = Set.range fun x => x • g
· 使用引理 `Mathlib.Tactic.Contrapose.contrapose₁`：contrapose₁ {p q : Prop} : (¬ q -
> ¬ p) -> (p -> q)
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Mathlib.Tactic.Push.not_forall_eq`：not_forall_eq : (¬ forall x, s x) = (
exists x, ¬ s x)
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `xor_iff_not_iff'`：xor_iff_not_iff' : Xor a b ↔ (¬a ↔ b)
· 使用定理 `AddSubgroup.dense_xor_cyclic`：∀ {G : Type u_1} [inst : AddCommGroup G] [
inst_1 : LinearOrder G] [IsOrderedAddMonoid G] [inst_3 : TopologicalSpace G]   [
OrderTopology G] […
· 使用定理 `instOrderTopologyReal`：OrderTopology ℝ
· 使用定理 `LinearOrderedSemiField.toDenselyOrdered`：∀ {α : Type u_2} [inst : Semifi
eld α] [inst_1 : PartialOrder α] [PosMulReflectLT α] [IsStrictOrderedRing α],   
DenselyOrdered α
· 使用定理 `PosMulReflectLE.toPosMulReflectLT`：∀ {α : Type u_1} [inst : MulZeroClass
 α] [inst_1 : PartialOrder α] [PosMulReflectLE α], PosMulReflectLT α
· 使用定理 `PosMulStrictMono.toPosMulReflectLE`：∀ {α : Type u_1} [inst : Mul α] [ins
t_1 : Zero α] [inst_2 : LinearOrder α] [PosMulStrictMono α], PosMulReflectLE α
· 使用定理 `IsStrictOrderedRing.toPosMulStrictMono`：∀ {R : Type u_1} {inst : Semirin
g R} {inst_1 : PartialOrder R} [self : IsStrictOrderedRing R], PosMulStrictMono 
R
· 使用定理 `AddSubgroup.coe_comap`：∀ {G : Type u_1} [inst : AddGroup G] {N : Type u_
5} [inst_1 : AddGroup N] (K : AddSubgroup N) (f : G →+ N),   ↑(AddSubgroup.comap
 f K) = ⇑f …
· 使用定理 `QuotientAddGroup.coe_mk'`：∀ {G : Type u_1} [inst : AddGroup G] (N : AddS
ubgroup G) [nN : N.Normal],   ⇑(QuotientAddGroup.mk' N) = QuotientAddGroup.mk
· 使用定理 `QuotientAddGroup.dense_preimage_mk`：∀ {G : Type u_1} [inst : Topological
Space G] [inst_1 : AddGroup G] [SeparatelyContinuousAdd G] {N : AddSubgroup G}  
 {s : Set (G ⧸ N)}, Dens…
· 使用定理 `instSeparatelyContinuousAddOfContinuousAdd`：∀ {M : Type u_1} [inst : Top
ologicalSpace M] [inst_1 : Add M] [ContinuousAdd M], SeparatelyContinuousAdd M
· 使用定理 `IsSemitopologicalSemiring.toContinuousAdd`：∀ {R : Type u_2} {inst : Topo
logicalSpace R} {inst_1 : NonUnitalNonAssocSemiring R}   [self : IsSemitopologic
alSemiring R], ContinuousAdd R
· 使用定理 `IsSemitopologicalRing.toIsSemitopologicalSemiring`：∀ {R : Type u_2} {ins
t : TopologicalSpace R} {inst_1 : NonUnitalNonAssocRing R} [self : IsSemitopolog
icalRing R],   IsSemitopologicalSemirin…
· 使用定理 `IsTopologicalRing.toIsSemitopologicalRing`：∀ (R : Type u_2) [inst : Topo
logicalSpace R] [inst_1 : NonUnitalNonAssocRing R] [IsTopologicalRing R],   IsSe
mitopologicalRing R
· 使用定理 `instIsTopologicalRingReal`：IsTopologicalRing ℝ
· 使用定理 `AddMonoidHom.map_zmultiples`：∀ {G : Type u_1} [inst : AddGroup G] {N : T
ype u_3} [inst_1 : AddGroup N] (f : G →+ N) (x : G),   AddSubgroup.map f (AddSub
group.zmultiples …
· 使用定理 `AddSubgroup.map_comap_eq_self_of_surjective`：∀ {G : Type u_1} [inst : Ad
dGroup G] {N : Type u_5} [inst_1 : AddGroup N] {f : G →+ N},   Function.Surjecti
ve ⇑f → ∀ (H : AddSubgroup N), Ad…
（共 32 条，此处仅展示前 30 条）

--- 原说明 ---
A subgroup of the circle `ℝ⧸pℤ`, `p > 0`, is dense
iff it is not generated by a single element of finite additive order.
-/
theorem dense_addSubgroup_iff_ne_zmultiples {p : ℝ} [Fact (0 < p)] {s : AddSubgroup (AddCircle p)} :
    Dense (s : Set (AddCircle p)) ↔ ∀ a, addOrderOf a ≠ 0 → s ≠ .zmultiples a := by
  constructor
  · rintro hd a ha rfl
    rw [AddSubgroup.coe_zmultiples, ← DenseRange, denseRange_zsmul_iff] at hd
    exact ha hd
  · intro h
    contrapose! h
    obtain ⟨a, rfl⟩ : ∃ a, s = .zmultiples a := by
      rw [← QuotientAddGroup.dense_preimage_mk, ← QuotientAddGroup.coe_mk',
        ← AddSubgroup.coe_comap, xor_iff_not_iff'.1 (AddSubgroup.dense_xor_cyclic _)] at h
      rcases h with ⟨a, ha⟩
      use a
      rw [← QuotientAddGroup.coe_mk', ← AddMonoidHom.map_zmultiples, ← ha,
        AddSubgroup.map_comap_eq_self_of_surjective]
      exact Quot.mk_surjective
    exact ⟨a, denseRange_zsmul_iff.not.mp h, rfl⟩

end AddCircle

