/-
Copyright (c) 2020 Chris Hughes. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Chris Hughes
-/
module

public import Mathlib.Algebra.Divisibility.Basic
public import Mathlib.Algebra.Group.Subgroup.Map
public import Mathlib.Algebra.Group.Int.Defs

/-!
# Subgroups generated by an element

## Tags
subgroup, subgroups

-/

@[expose] public section

variable {G : Type*} [Group G]
variable {A : Type*} [AddGroup A]
variable {N : Type*} [Group N]

namespace Subgroup

/-- The subgroup generated by an element. -/
@[to_additive /-- The additive subgroup generated by an element. -/]
/-
**Subgroup.zpowers** 是 Mathlib 中的一个定义，位于命名空间 `Subgroup`。
形式化陈述：zpowers (g : G) : Subgroup G where carrier
参数：g : G。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The subgroup generated by an element.
-/
def zpowers (g : G) : Subgroup G where
  carrier := Set.range (g ^ · : ℤ → G)
  one_mem' := ⟨0, zpow_zero g⟩
  mul_mem' := by rintro _ _ ⟨a, rfl⟩ ⟨b, rfl⟩; exact ⟨a + b, zpow_add ..⟩
  inv_mem' := by rintro _ ⟨a, rfl⟩; exact ⟨-a, zpow_neg ..⟩

@[to_additive (attr := simp)]
/-
**Subgroup.mem_zpowers** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：mem_zpowers (g : G) : g in zpowers g
参数：g : G。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `zpow_one`：zpow_one (a : G) : a ^ (1 : Int) = a
-/
theorem mem_zpowers (g : G) : g ∈ zpowers g :=
  ⟨1, zpow_one _⟩

@[to_additive (attr := norm_cast)] -- TODO: simp?
/-
**Subgroup.coe_zpowers** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：coe_zpowers (g : G) : ↑(zpowers g) = Set.range (g ^ · : Int -> G)
参数：g : G。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem coe_zpowers (g : G) : ↑(zpowers g) = Set.range (g ^ · : ℤ → G) :=
  rfl

@[to_additive] -- TODO: delete?
/-
**Subgroup.decidableMemZPowers** 是 Mathlib 中的一个实例，位于命名空间 `Subgroup`。
形式化陈述：decidableMemZPowers {a : G} : DecidablePred (· in Subgroup.zpowers a)
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
noncomputable instance decidableMemZPowers {a : G} : DecidablePred (· ∈ Subgroup.zpowers a) :=
  Classical.decPred _

@[to_additive]
/-
**Subgroup.zpowers_eq_closure** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：zpowers_eq_closure (g : G) : zpowers g = closure {g}
参数：g : G。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subgroup.ext`：ext {H K : Subgroup G} (h : forall x, x in H ↔ x in K) : H
 = K
· 使用定理 `Iff.symm`：∀ {a b : Prop}, (a ↔ b) → (b ↔ a)
· 使用定理 `Subgroup.mem_closure_singleton`：mem_closure_singleton {x y : G} : y in c
losure ({x} : Set G) ↔ exists n : Int, x ^ n = y
-/
theorem zpowers_eq_closure (g : G) : zpowers g = closure {g} := by
  ext
  exact mem_closure_singleton.symm

@[to_additive]
/-
**Subgroup.mem_zpowers_iff** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：mem_zpowers_iff {g h : G} : h in zpowers g ↔ exists k : Int, g ^ k = h
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem mem_zpowers_iff {g h : G} : h ∈ zpowers g ↔ ∃ k : ℤ, g ^ k = h :=
  Iff.rfl

@[to_additive (attr := simp)]
/-
**Subgroup.zpow_mem_zpowers** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：zpow_mem_zpowers (g : G) (k : Int) : g ^ k in zpowers g
参数：g : G；k : Int。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Subgroup.mem_zpowers_iff`：mem_zpowers_iff {g h : G} : h in zpowers g ↔ e
xists k : Int, g ^ k = h
-/
theorem zpow_mem_zpowers (g : G) (k : ℤ) : g ^ k ∈ zpowers g :=
  mem_zpowers_iff.mpr ⟨k, rfl⟩

@[to_additive (attr := simp)]
/-
**Subgroup.npow_mem_zpowers** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：npow_mem_zpowers (g : G) (k : Nat) : g ^ k in zpowers g
参数：g : G；k : Nat。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subgroup.zpow_mem_zpowers`：zpow_mem_zpowers (g : G) (k : Int) : g ^ k in
 zpowers g
· 使用定理 `zpow_natCast`：zpow_natCast (a : G) : forall n : Nat, a ^ (n : Int) = a ^
 n | 0 => (zpow_zero _).trans (pow_zero _).symm | n + 1 => calc a ^ (↑(n + 1) : 
In…
-/
theorem npow_mem_zpowers (g : G) (k : ℕ) : g ^ k ∈ zpowers g :=
  zpow_natCast g k ▸ zpow_mem_zpowers g k

-- increasing simp priority. Better lemma than `Subtype.exists`
@[to_additive (attr := simp 1100)]
/-
**Subgroup.forall_zpowers** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：forall_zpowers {x : G} {p : zpowers x -> Prop} : (forall g, p g) ↔ forall 
m : Int, p ⟨x ^ m, m, rfl⟩
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.forall_subtype_range_iff`：forall_subtype_range_iff {p : range f -> P
rop} : (forall a : range f, p a) ↔ forall i, p ⟨f i, mem_range_self _⟩
-/
theorem forall_zpowers {x : G} {p : zpowers x → Prop} : (∀ g, p g) ↔ ∀ m : ℤ, p ⟨x ^ m, m, rfl⟩ :=
  Set.forall_subtype_range_iff

-- increasing simp priority. Better lemma than `Subtype.exists`
@[to_additive (attr := simp 1100)]
/-
**Subgroup.exists_zpowers** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：exists_zpowers {x : G} {p : zpowers x -> Prop} : (exists g, p g) ↔ exists 
m : Int, p ⟨x ^ m, m, rfl⟩
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.exists_subtype_range_iff`：exists_subtype_range_iff {p : range f -> P
rop} : (exists a : range f, p a) ↔ exists i, p ⟨f i, mem_range_self _⟩
-/
theorem exists_zpowers {x : G} {p : zpowers x → Prop} : (∃ g, p g) ↔ ∃ m : ℤ, p ⟨x ^ m, m, rfl⟩ :=
  Set.exists_subtype_range_iff

@[to_additive]
/-
**Subgroup.forall_mem_zpowers** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：forall_mem_zpowers {x : G} {p : G -> Prop} : (forall g in zpowers x, p g) 
↔ forall m : Int, p (x ^ m)
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.forall_mem_range`：forall_mem_range {p : α -> Prop} : (forall a in ra
nge f, p a) ↔ forall i, p (f i)
-/
theorem forall_mem_zpowers {x : G} {p : G → Prop} : (∀ g ∈ zpowers x, p g) ↔ ∀ m : ℤ, p (x ^ m) :=
  Set.forall_mem_range

@[to_additive]
/-
**Subgroup.exists_mem_zpowers** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：exists_mem_zpowers {x : G} {p : G -> Prop} : (exists g in zpowers x, p g) 
↔ exists m : Int, p (x ^ m)
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.exists_range_iff`：exists_range_iff {p : α -> Prop} : (exists a in ra
nge f, p a) ↔ exists i, p (f i)
-/
theorem exists_mem_zpowers {x : G} {p : G → Prop} : (∃ g ∈ zpowers x, p g) ↔ ∃ m : ℤ, p (x ^ m) :=
  Set.exists_range_iff

end Subgroup

@[to_additive (attr := simp)]
/-
**MonoidHom.map_zpowers** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：MonoidHom.map_zpowers (f : G ->* N) (x : G) : (Subgroup.zpowers x).map f =
 Subgroup.zpowers (f x)
参数：f : G ->* N；x : G。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Subgroup.zpowers_eq_closure`：zpowers_eq_closure (g : G) : zpowers g = cl
osure {g}
· 使用定理 `MonoidHom.map_closure`：map_closure (f : G ->* N) (s : Set G) : (closure 
s).map f = closure (f '' s)
· 使用定理 `Set.image_singleton`：image_singleton {f : α -> β} {a : α} : f '' {a} = {
f a}
-/
theorem MonoidHom.map_zpowers (f : G →* N) (x : G) :
    (Subgroup.zpowers x).map f = Subgroup.zpowers (f x) := by
  rw [Subgroup.zpowers_eq_closure, Subgroup.zpowers_eq_closure, f.map_closure, Set.image_singleton]
/-
**Int.mem_zmultiples_iff** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：Int.mem_zmultiples_iff {a b : Int} : b in AddSubgroup.zmultiples a ↔ a ∣ b
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `exists_congr`：∀ {α : Sort u_1} {p q : α → Prop}, (∀ (a : α), p a ↔ q a) 
→ ((∃ a, p a) ↔ ∃ a, q a)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mul_comm`：mul_comm : forall a b : G, a * b = b * a
· 使用定理 `eq_comm`：∀ {α : Sort u_1} {a b : α}, a = b ↔ b = a
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `smul_eq_mul`：smul_eq_mul {α : Type*} [Mul α] (a b : α) : a • b = a * b
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem Int.mem_zmultiples_iff {a b : ℤ} : b ∈ AddSubgroup.zmultiples a ↔ a ∣ b :=
  exists_congr fun k => by rw [mul_comm, eq_comm, ← smul_eq_mul]

@[simp]
/-
**Int.zmultiples_one** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：Int.zmultiples_one : AddSubgroup.zmultiples (1 : Int) = ⊤
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AddSubgroup.ext`：∀ {G : Type u_1} [inst : AddGroup G] {H K : AddSubgroup
 G}, (∀ (x : G), x ∈ H ↔ x ∈ K) → H = K
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
lemma Int.zmultiples_one : AddSubgroup.zmultiples (1 : ℤ) = ⊤ := by
  ext z
  simp [Int.mem_zmultiples_iff]
/-
**ofMul_image_zpowers_eq_zmultiples_ofMul** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ofMul_image_zpowers_eq_zmultiples_ofMul {x : G} : Additive.ofMul '' (Subgr
oup.zpowers x : Set G) = AddSubgroup.zmultiples (Additive.ofMul x)
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `Set.mem_image_iff_of_inverse`：mem_image_iff_of_inverse {f : α -> β} {g :
 β -> α} {b : β} {s : Set α} (h₁ : LeftInverse g f) (h₂ : RightInverse g f) : b 
in f '' s ↔ g b in…
· 使用定理 `congrFun`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, f = g →
 ∀ (a : α), f a = g a
-/
theorem ofMul_image_zpowers_eq_zmultiples_ofMul {x : G} :
    Additive.ofMul '' (Subgroup.zpowers x : Set G) = AddSubgroup.zmultiples (Additive.ofMul x) := by
  ext
  exact Set.mem_image_iff_of_inverse (congrFun rfl) (congrFun rfl)
/-
**ofAdd_image_zmultiples_eq_zpowers_ofAdd** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ofAdd_image_zmultiples_eq_zpowers_ofAdd {x : A} : Multiplicative.ofAdd '' 
(AddSubgroup.zmultiples x : Set A) = Subgroup.zpowers (Multiplicative.ofAdd x)
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Equiv.eq_image_iff_symm_image_eq`：eq_image_iff_symm_image_eq {α β} (e : 
α ≃ β) (s : Set α) (t : Set β) : t = e '' s ↔ e.symm '' t = s
· 使用定理 `ofMul_image_zpowers_eq_zmultiples_ofMul`：ofMul_image_zpowers_eq_zmultipl
es_ofMul {x : G} : Additive.ofMul '' (Subgroup.zpowers x : Set G) = AddSubgroup.
zmultiples (Additive.ofMul x)
-/
theorem ofAdd_image_zmultiples_eq_zpowers_ofAdd {x : A} :
    Multiplicative.ofAdd '' (AddSubgroup.zmultiples x : Set A) =
      Subgroup.zpowers (Multiplicative.ofAdd x) := by
  symm
  rw [Equiv.eq_image_iff_symm_image_eq]
  exact ofMul_image_zpowers_eq_zmultiples_ofMul

namespace Subgroup

variable {s : Set G} {g : G}

set_option backward.isDefEq.respectTransparency false in
@[to_additive]
/-
**Subgroup.zpowers_isMulCommutative** 是 Mathlib 中的一个实例，位于命名空间 `Subgroup`。
形式化陈述：zpowers_isMulCommutative (g : G) : IsMulCommutative (zpowers g)
参数：g : G。
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `MulMemClass.mul_mem`：∀ {S : Type u_3} {M : outParam (Type u_4)} {inst : 
Mul M} {inst_1 : SetLike S M} [self : MulMemClass S M] {s : S}   {a b : M}, a ∈ 
s → b ∈ s…
· 使用定理 `SubmonoidClass.toMulMemClass`：∀ {S : Type u_3} {M : outParam (Type u_4)}
 {inst : MulOneClass M} {inst_1 : SetLike S M} [self : SubmonoidClass S M],   Mu
lMemClass S M
· 使用定理 `SubgroupClass.toSubmonoidClass`：∀ {S : Type u_3} {G : outParam (Type u_4
)} {inst : DivInvMonoid G} {inst_1 : SetLike S G} [self : SubgroupClass S G],   
SubmonoidClass S G
· 使用定理 `Subgroup.instSubgroupClass`：∀ {G : Type u_1} [inst : Group G], SubgroupC
lass (Subgroup G) G
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `zpow_mul_comm`：∀ {G : Type u_3} [inst : Group G] (a : G) (m n : ℤ), a ^ 
m * a ^ n = a ^ n * a ^ m
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Subtype.mk.congr_simp`：∀ {α : Sort u} {p : α → Prop} (val val_1 : α) (e_
val : val = val_1) (property : p val), ⟨val, property⟩ = ⟨val_1, ⋯⟩
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
instance zpowers_isMulCommutative (g : G) : IsMulCommutative (zpowers g) :=
  ⟨⟨fun ⟨_, _, h₁⟩ ⟨_, _, h₂⟩ ↦ by simp [← h₁, ← h₂, zpow_mul_comm]⟩⟩

@[to_additive (attr := simp)]
/-
**Subgroup.zpowers_le** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：zpowers_le {g : G} {H : Subgroup G} : zpowers g <= H ↔ g in H
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Subgroup.zpowers_eq_closure`：zpowers_eq_closure (g : G) : zpowers g = cl
osure {g}
· 使用定理 `Subgroup.closure_le`：closure_le : closure k <= K ↔ k subseteq K
· 使用定理 `Set.singleton_subset_iff`：singleton_subset_iff {a : α} {s : Set α} : {a}
 subseteq s ↔ a in s
· 使用定理 `SetLike.mem_coe`：mem_coe {x : B} : x in (p : Set B) ↔ x in p
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem zpowers_le {g : G} {H : Subgroup G} : zpowers g ≤ H ↔ g ∈ H := by
  rw [zpowers_eq_closure, closure_le, Set.singleton_subset_iff, SetLike.mem_coe]

@[to_additive]
alias ⟨_, zpowers_le_of_mem⟩ := zpowers_le

@[to_additive (attr := simp)]
/-
**Subgroup.zpowers_eq_bot** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：zpowers_eq_bot {g : G} : zpowers g = ⊥ ↔ g = 1
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `eq_bot_iff`：∀ {α : Type u} [inst : PartialOrder α] [inst_1 : OrderBot α]
 {a : α}, a = ⊥ ↔ a ≤ ⊥
· 使用定理 `Subgroup.zpowers_le`：zpowers_le {g : G} {H : Subgroup G} : zpowers g <= 
H ↔ g in H
· 使用定理 `Subgroup.mem_bot`：mem_bot {x : G} : x in (⊥ : Subgroup G) ↔ x = 1
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem zpowers_eq_bot {g : G} : zpowers g = ⊥ ↔ g = 1 := by rw [eq_bot_iff, zpowers_le, mem_bot]

@[to_additive]
/-
**Subgroup.zpowers_ne_bot** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：zpowers_ne_bot : zpowers g != ⊥ ↔ g != 1
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.not`：∀ {a b : Prop}, (a ↔ b) → (¬a ↔ ¬b)
· 使用定理 `Subgroup.zpowers_eq_bot`：zpowers_eq_bot {g : G} : zpowers g = ⊥ ↔ g = 1
-/
theorem zpowers_ne_bot : zpowers g ≠ ⊥ ↔ g ≠ 1 :=
  zpowers_eq_bot.not

@[to_additive (attr := simp)]
/-
**Subgroup.zpowers_one_eq_bot** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：zpowers_one_eq_bot : Subgroup.zpowers (1 : G) = ⊥
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Subgroup.zpowers_eq_bot`：zpowers_eq_bot {g : G} : zpowers g = ⊥ ↔ g = 1
-/
theorem zpowers_one_eq_bot : Subgroup.zpowers (1 : G) = ⊥ :=
  Subgroup.zpowers_eq_bot.mpr rfl

@[to_additive (attr := simp)]
/-
**Subgroup.zpowers_inv** 是 Mathlib 中的一个定理，位于命名空间 `Subgroup`。
形式化陈述：zpowers_inv : zpowers g⁻¹ = zpowers g
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `eq_of_forall_ge_iff`：∀ {α : Type u_2} [inst : PartialOrder α] {a b : α},
 (∀ (c : α), a ≤ c ↔ b ≤ c) → a = b
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `SubgroupClass.toInvMemClass`：∀ {S : Type u_3} {G : outParam (Type u_4)} 
{inst : DivInvMonoid G} {inst_1 : SetLike S G} [self : SubgroupClass S G],   Inv
MemClass S G
· 使用定理 `Subgroup.instSubgroupClass`：∀ {G : Type u_1} [inst : Group G], SubgroupC
lass (Subgroup G) G
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
theorem zpowers_inv : zpowers g⁻¹ = zpowers g :=
  eq_of_forall_ge_iff fun _ ↦ by simp only [zpowers_le, inv_mem_iff]

end Subgroup

/-
**Int.zmultiples_natAbs** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：Int.zmultiples_natAbs (a : Int) : AddSubgroup.zmultiples (a.natAbs : Int) 
= AddSubgroup.zmultiples a
参数：a : Int。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `and_self`：∀ (p : Prop), (p ∧ p) = p
-/
theorem Int.zmultiples_natAbs (a : ℤ) :
    AddSubgroup.zmultiples (a.natAbs : ℤ) = AddSubgroup.zmultiples a := by
  simp [le_antisymm_iff, Int.mem_zmultiples_iff, Int.dvd_natAbs, Int.natAbs_dvd]
/-
**Int.addSubgroupClosure_one** 是 Mathlib 中的一个定理，位于命名空间 `Int`。
形式化陈述：AddSubgroup.closure {1} = ⊤
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AddSubgroup.ext`：∀ {G : Type u_1} [inst : AddGroup G] {H K : AddSubgroup
 G}, (∀ (x : G), x ∈ H ↔ x ∈ K) → H = K
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `mul_one`：mul_one : forall a : M, a * 1 = a
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
@[simp] lemma Int.addSubgroupClosure_one : AddSubgroup.closure ({1} : Set ℤ) = ⊤ := by
  ext
  simp [AddSubgroup.mem_closure_singleton]
