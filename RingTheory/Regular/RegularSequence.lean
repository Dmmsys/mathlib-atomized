/-
Copyright (c) 2024 Brendan Murphy. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Brendan Murphy
-/
module

public import Mathlib.RingTheory.Artinian.Module
public import Mathlib.RingTheory.LocalRing.MaximalIdeal.Basic
public import Mathlib.RingTheory.Nakayama
public import Mathlib.RingTheory.Regular.IsSMulRegular

/-!
# Regular sequences and weakly regular sequences

The notion of a regular sequence is fundamental in commutative algebra.
Properties of regular sequences encode information about singularities of a
ring and regularity of a sequence can be tested homologically.
However the notion of a regular sequence is only really sensible for Noetherian local rings.

TODO: Koszul regular sequences, `H_1`-regular sequences, quasi-regular sequences, depth.

## Tags

module, regular element, regular sequence, commutative algebra
-/

@[expose] public section

universe u v

open scoped Pointwise

variable {R S M M₂ M₃ M₄ : Type*}

namespace Ideal

variable [Semiring R] [Semiring S]

/-- The ideal generated by a list of elements. -/
/-
**Ideal.ofList** 是 Mathlib 中的一个缩写定义，位于命名空间 `Ideal`。
形式化陈述：ofList (rs : List R)
参数：rs : List R。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The ideal generated by a list of elements.
-/
abbrev ofList (rs : List R) := span { r | r ∈ rs }
/-
**Ideal.ofList_nil** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：∀ {R : Type u_1} [inst : Semiring R], Ideal.ofList [] = ⊥
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.eq_empty_of_forall_notMem`：eq_empty_of_forall_notMem (h : forall x, 
x ∉ s) : s = ∅
· 使用定理 `List.not_mem_nil`：∀ {α : Type u_1} {a : α}, a ∉ []
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.span_empty`：span_empty : span (∅ : Set α) = ⊥
-/
@[simp] lemma ofList_nil : (ofList [] : Ideal R) = ⊥ :=
  have : { r | r ∈ [] } = ∅ := Set.eq_empty_of_forall_notMem (fun _ => List.not_mem_nil)
  Eq.trans (congrArg span this) span_empty
/-
**Ideal.ofList_append** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：∀ {R : Type u_1} [inst : Semiring R] (rs₁ rs₂ : List R), Ideal.ofList (rs₁
 ++ rs₂) = Ideal.ofList rs₁ ⊔ Ideal.ofList rs₂
参数：rs₁ rs₂ : List R；rs₁ ++ rs₂。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `List.mem_append`：∀ {α : Type u_1} {a : α} {s t : List α}, a ∈ s ++ t ↔ a
 ∈ s ∨ a ∈ t
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.span_union`：span_union (s t : Set α) : span (s union t) = span s ⊔
 span t
-/
@[simp] lemma ofList_append (rs₁ rs₂ : List R) :
    ofList (rs₁ ++ rs₂) = ofList rs₁ ⊔ ofList rs₂ :=
  have : { r | r ∈ rs₁ ++ rs₂ } = _ := Set.ext (fun _ => List.mem_append)
  Eq.trans (congrArg span this) (span_union _ _)
/-
**Ideal.ofList_singleton** 是 Mathlib 中的一个引理，位于命名空间 `Ideal`。
形式化陈述：ofList_singleton (r : R) : ofList [r] = span {r}
参数：r : R。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `List.mem_singleton`：∀ {α : Type u_1} {a b : α}, a ∈ [b] ↔ a = b
-/
lemma ofList_singleton (r : R) : ofList [r] = span {r} :=
  congrArg span (Set.ext fun _ => List.mem_singleton)
/-
**Ideal.ofList_cons** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：∀ {R : Type u_1} [inst : Semiring R] (r : R) (rs : List R), Ideal.ofList (
r :: rs) = Ideal.span {r} ⊔ Ideal.ofList rs
参数：r : R；rs : List R；r :: rs。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Ideal.ofList_append`：∀ {R : Type u_1} [inst : Semiring R] (rs₁ rs₂ : Lis
t R), Ideal.ofList (rs₁ ++ rs₂) = Ideal.ofList rs₁ ⊔ Ideal.ofList rs₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `Ideal.ofList_singleton`：ofList_singleton (r : R) : ofList [r] = span {r}
-/
@[simp] lemma ofList_cons (r : R) (rs : List R) :
    ofList (r::rs) = span {r} ⊔ ofList rs :=
  Eq.trans (ofList_append [r] rs) (congrArg (· ⊔ _) (ofList_singleton r))
/-
**Ideal.map_ofList** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：∀ {R : Type u_1} {S : Type u_2} [inst : Semiring R] [inst_1 : Semiring S] 
(f : R →+* S) (rs : List R),   Ideal.map f (Ideal.ofList rs) = Ideal.ofList (Lis
t.map (⇑f) rs)
参数：f : R →+* S；rs : List R；Ideal.ofList rs；List.map (⇑f) rs。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Ideal.map_span`：map_span (s : Set R) : map f (span s) = span (f '' s)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `Iff.symm`：∀ {a b : Prop}, (a ↔ b) → (b ↔ a)
· 使用定理 `List.mem_map`：∀ {α : Type u_1} {β : Type u_2} {b : β} {f : α → β} {l : L
ist α}, b ∈ List.map f l ↔ ∃ a ∈ l, f a = b
-/
@[simp] lemma map_ofList (f : R →+* S) (rs : List R) :
    map f (ofList rs) = ofList (rs.map f) :=
  Eq.trans (map_span f { r | r ∈ rs }) <| congrArg span <|
    Set.ext (fun _ => List.mem_map.symm)
/-
**Ideal.ofList_cons_smul** 是 Mathlib 中的一个引理，位于命名空间 `Ideal`。
形式化陈述：ofList_cons_smul {R} [CommSemiring R] (r : R) (rs : List R) {M} [AddCommMo
noid M] [Module R M] (N : Submodule R M) : ofList (r :: rs) • N = r • N ⊔ ofList
 rs • N
参数：r : R；rs : List R；N : Submodule R M。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.ofList_cons`：∀ {R : Type u_1} [inst : Semiring R] (r : R) (rs : Li
st R), Ideal.ofList (r :: rs) = Ideal.span {r} ⊔ Ideal.ofList rs
· 使用定理 `Submodule.sup_smul`：sup_smul : (I ⊔ J) • N = I • N ⊔ J • N
· 使用定理 `Submodule.ideal_span_singleton_smul`：ideal_span_singleton_smul (r : R) (
N : Submodule R M) : (Ideal.span {r} : Ideal R) • N = r • N
-/
lemma ofList_cons_smul {R} [CommSemiring R] (r : R) (rs : List R) {M}
    [AddCommMonoid M] [Module R M] (N : Submodule R M) :
    ofList (r :: rs) • N = r • N ⊔ ofList rs • N := by
  rw [ofList_cons, Submodule.sup_smul, Submodule.ideal_span_singleton_smul]

end Ideal

namespace Submodule

variable (M) [CommRing R] [AddCommGroup M] [AddCommGroup M₂]
    [Module R M] [Module R M₂] (r : R) (rs : List R)

/-- The equivalence between M ⧸ (r₀, r₁, …, rₙ)M and (M ⧸ r₀M) ⧸ (r₁, …, rₙ) (M ⧸ r₀M). -/
/-
**Submodule.quotOfListConsSMulTopEquivQuotSMulTopInner** 是 Mathlib 中的一个定义，位于命名空间
 `Submodule`。
形式化陈述：quotOfListConsSMulTopEquivQuotSMulTopInner : (M ⧸ (Ideal.ofList (r :: rs) 
• ⊤ : Submodule R M)) ≃ₗ[R] QuotSMulTop r M ⧸ (Ideal.ofList rs • ⊤ : Submodule R
 (QuotSMulTop r M))
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The equivalence between M ⧸ (r₀, r₁, …, rₙ)M and (M ⧸ r₀M) ⧸ (r₁, …, rₙ) (M ⧸ r₀
M).
-/
def quotOfListConsSMulTopEquivQuotSMulTopInner :
    (M ⧸ (Ideal.ofList (r :: rs) • ⊤ : Submodule R M)) ≃ₗ[R]
      QuotSMulTop r M ⧸ (Ideal.ofList rs • ⊤ : Submodule R (QuotSMulTop r M)) :=
  quotEquivOfEq _ _ (Ideal.ofList_cons_smul r rs ⊤) ≪≫ₗ
    (quotientQuotientEquivQuotientSup (r • ⊤) (Ideal.ofList rs • ⊤)).symm ≪≫ₗ
      quotEquivOfEq _ _ (by rw [map_smul'', map_top, range_mkQ])

/-- The equivalence between M ⧸ (r₀, r₁, …, rₙ)M and (M ⧸ (r₁, …, rₙ)) ⧸ r₀ (M ⧸ (r₁, …, rₙ)). -/
/-
**Submodule.quotOfListConsSMulTopEquivQuotSMulTopOuter** 是 Mathlib 中的一个定义，位于命名空间
 `Submodule`。
形式化陈述：quotOfListConsSMulTopEquivQuotSMulTopOuter : (M ⧸ (Ideal.ofList (r :: rs) 
• ⊤ : Submodule R M)) ≃ₗ[R] QuotSMulTop r (M ⧸ (Ideal.ofList rs • ⊤ : Submodule 
R M))
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The equivalence between M ⧸ (r₀, r₁, …, rₙ)M and (M ⧸ (r₁, …, rₙ)) ⧸ r₀ (M ⧸ (r₁
, …, rₙ)).
-/
def quotOfListConsSMulTopEquivQuotSMulTopOuter :
    (M ⧸ (Ideal.ofList (r :: rs) • ⊤ : Submodule R M)) ≃ₗ[R]
      QuotSMulTop r (M ⧸ (Ideal.ofList rs • ⊤ : Submodule R M)) :=
  quotEquivOfEq _ _ (Eq.trans (Ideal.ofList_cons_smul r rs ⊤) (sup_comm _ _)) ≪≫ₗ
    (quotientQuotientEquivQuotientSup (Ideal.ofList rs • ⊤) (r • ⊤)).symm ≪≫ₗ
      quotEquivOfEq _ _ (by rw [map_pointwise_smul, map_top, range_mkQ])

variable {M}
/-
**Submodule.quotOfListConsSMulTopEquivQuotSMulTopInner_naturality** 是 Mathlib 中的
一个引理，位于命名空间 `Submodule`。
形式化陈述：quotOfListConsSMulTopEquivQuotSMulTopInner_naturality (f : M ->ₗ[R] M₂) : 
(quotOfListConsSMulTopEquivQuotSMulTopInner M₂ r rs).toLinearMap ∘ₗ mapQ _ _ _ (
smul_top_le_comap_smul_top (Ideal.ofList (r :: rs)) f) = mapQ _ _ _ (smul_top_le
_comap_smul_top _ (QuotSMulTop.map r f)) ∘ₗ (quotOfListConsSMulTopEquivQuotSMulT
opInner M r rs).toLinearMap
参数：f : M ->ₗ[R] M₂。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submodule.quot_hom_ext`：quot_hom_ext (f g : (M ⧸ p) ->ₗ[R] M₂) (h : fora
ll x : M, f (Quotient.mk x) = g (Quotient.mk x)) : f = g
· 使用引理 `Submodule.smul_top_le_comap_smul_top`：smul_top_le_comap_smul_top [AddCom
mMonoid M] [AddCommMonoid N] [Module R M] [Module R N] (I : Ideal R) (f : M ->ₗ[
R] N) : I • ⊤ <= comap f (…
-/
lemma quotOfListConsSMulTopEquivQuotSMulTopInner_naturality (f : M →ₗ[R] M₂) :
    (quotOfListConsSMulTopEquivQuotSMulTopInner M₂ r rs).toLinearMap ∘ₗ
        mapQ _ _ _ (smul_top_le_comap_smul_top (Ideal.ofList (r :: rs)) f) =
      mapQ _ _ _ (smul_top_le_comap_smul_top _ (QuotSMulTop.map r f)) ∘ₗ
        (quotOfListConsSMulTopEquivQuotSMulTopInner M r rs).toLinearMap :=
  quot_hom_ext _ _ _ fun _ => rfl
/-
**Submodule.top_eq_ofList_cons_smul_iff** 是 Mathlib 中的一个引理，位于命名空间 `Submodule`。
形式化陈述：top_eq_ofList_cons_smul_iff : (⊤ : Submodule R M) = Ideal.ofList (r :: rs)
 • ⊤ ↔ (⊤ : Submodule R (QuotSMulTop r M)) = Ideal.ofList rs • ⊤
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `eq_comm`：∀ {α : Sort u_1} {a b : α}, a = b ↔ b = a
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Submodule.Quotient.subsingleton_iff`：∀ {R : Type u_1} {M : Type u_2} [in
st : Ring R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M]   {p : Submo
dule R M}, Subsingleton (…
· 使用定理 `Equiv.subsingleton_congr`：subsingleton_congr (e : α ≃ β) : Subsingleton 
α ↔ Subsingleton β
-/
lemma top_eq_ofList_cons_smul_iff :
    (⊤ : Submodule R M) = Ideal.ofList (r :: rs) • ⊤ ↔
      (⊤ : Submodule R (QuotSMulTop r M)) = Ideal.ofList rs • ⊤ := by
  conv => congr <;> rw [eq_comm, ← Quotient.subsingleton_iff]
  exact (quotOfListConsSMulTopEquivQuotSMulTopInner M r rs).toEquiv.subsingleton_congr

end Submodule

namespace RingTheory.Sequence

open scoped TensorProduct List
open Function Submodule QuotSMulTop

variable (S M)

section Definitions

/-
In theory, regularity of `rs : List α` on `M` makes sense as soon as
`[Monoid α]`, `[AddCommGroup M]`, and `[DistribMulAction α M]`.
Instead of `Ideal.ofList (rs.take i) • (⊤ : Submodule R M)` we use
`⨆ (j : Fin i), rs[j] • (⊤ : AddSubgroup M)`.
However it's not clear that this is a useful generalization.
If we add the assumption `[SMulCommClass α α M]` this is essentially the same
as focusing on the commutative ring case, by passing to the monoid ring
`ℤ[abelianization of α]`.
-/
variable [CommRing R] [AddCommGroup M] [Module R M]

open Ideal

/-- A sequence `[r₁, …, rₙ]` is weakly regular on `M` iff `rᵢ` is regular on
`M⧸(r₁, …, rᵢ₋₁)M` for all `1 ≤ i ≤ n`. -/
@[mk_iff]
/-
**RingTheory.Sequence.IsWeaklyRegular** 是 Mathlib 中的一个归纳类型，位于命名空间 `RingTheory.Se
quence`。
形式化陈述：{R : Type u_1} → (M : Type u_3) → [inst : CommRing R] → [inst_1 : AddCommG
roup M] → [_root_.Module R M] → List R → Prop
参数：M : Type u_3。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A sequence `[r₁, …, rₙ]` is weakly regular on `M` iff `rᵢ` is regular on
`M⧸(r₁, …, rᵢ₋₁)M` for all `1 ≤ i ≤ n`.
-/
structure IsWeaklyRegular (rs : List R) : Prop where
  regular_mod_prev : ∀ i (h : i < rs.length),
    IsSMulRegular (M ⧸ (ofList (rs.take i) • ⊤ : Submodule R M)) rs[i]
/-
**RingTheory.Sequence.isWeaklyRegular_iff_Fin** 是 Mathlib 中的一个引理，位于命名空间 `RingThe
ory.Sequence`。
形式化陈述：isWeaklyRegular_iff_Fin (rs : List R) : IsWeaklyRegular M rs ↔ forall (i :
 Fin rs.length), IsSMulRegular (M ⧸ (ofList (rs.take i) • ⊤ : Submodule R M)) rs
[i]
参数：rs : List R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `RingTheory.Sequence.isWeaklyRegular_iff`：∀ {R : Type u_1} (M : Type u_3)
 [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M]   (r
s : List R),   RingTheory.Seq…
· 使用定理 `Iff.symm`：∀ {a b : Prop}, (a ↔ b) → (b ↔ a)
· 使用定理 `Fin.forall_iff`：∀ {n : ℕ} {p : Fin n → Prop}, (∀ (i : Fin n), p i) ↔ ∀ (
i : ℕ) (h : i < n), p ⟨i, h⟩
-/
lemma isWeaklyRegular_iff_Fin (rs : List R) :
    IsWeaklyRegular M rs ↔ ∀ (i : Fin rs.length),
      IsSMulRegular (M ⧸ (ofList (rs.take i) • ⊤ : Submodule R M)) rs[i] :=
  Iff.trans (isWeaklyRegular_iff M rs) (Iff.symm Fin.forall_iff)

/-- A weakly regular sequence `rs` on `M` is regular if also `M/rsM ≠ 0`. -/
@[mk_iff]
/-
**RingTheory.Sequence.IsRegular** 是 Mathlib 中的一个归纳类型，位于命名空间 `RingTheory.Sequence
`。
形式化陈述：{R : Type u_1} → (M : Type u_3) → [inst : CommRing R] → [inst_1 : AddCommG
roup M] → [_root_.Module R M] → List R → Prop
参数：M : Type u_3。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A weakly regular sequence `rs` on `M` is regular if also `M/rsM ≠ 0`.
-/
structure IsRegular (rs : List R) : Prop extends IsWeaklyRegular M rs where
  top_ne_smul : (⊤ : Submodule R M) ≠ Ideal.ofList rs • ⊤

end Definitions

section Congr

variable {S M} [CommRing R] [CommRing S] [AddCommGroup M] [AddCommGroup M₂]
    [Module R M] [Module S M₂]
    {σ : R →+* S} {σ' : S →+* R} [RingHomInvPair σ σ'] [RingHomInvPair σ' σ]

set_option backward.isDefEq.respectTransparency.types false in
open DistribMulAction AddSubgroup in
/-
**RingTheory.Sequence._root_.AddHom.map_smul_top_toAddSubgroup_of_surjective** 是
 Mathlib 中的一个引理，位于命名空间 `RingTheory.Sequence`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
private lemma _root_.AddHom.map_smul_top_toAddSubgroup_of_surjective
    {f : M →+ M₂} {as : List R} {bs : List S} (hf : Function.Surjective f)
    (h : List.Forall₂ (fun r s => ∀ x, f (r • x) = s • f x) as bs) :
    (Ideal.ofList as • ⊤ : Submodule R M).toAddSubgroup.map f =
      (Ideal.ofList bs • ⊤ : Submodule S M₂).toAddSubgroup := by
  induction h with
  | nil =>
    convert! AddSubgroup.map_bot f using 1 <;>
      rw [Ideal.ofList_nil, bot_smul, bot_toAddSubgroup]
  | @cons r s _ _ h _ ih =>
    conv => congr <;> rw [Ideal.ofList_cons, sup_smul, sup_toAddSubgroup,
      ideal_span_singleton_smul, pointwise_smul_toAddSubgroup,
      top_toAddSubgroup, AddSubgroup.pointwise_smul_def]
    apply DFunLike.ext (f.comp (toAddMonoidEnd R M r))
      ((toAddMonoidEnd S M₂ s).comp f) at h
    rw [AddSubgroup.map_sup, ih, map_map, h, ← map_map,
      map_top_of_surjective f hf]
/-
**RingTheory.Sequence._root_.AddEquiv.isWeaklyRegular_congr** 是 Mathlib 中的一个引理，位
于命名空间 `RingTheory.Sequence`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma _root_.AddEquiv.isWeaklyRegular_congr {e : M ≃+ M₂} {as bs}
    (h : List.Forall₂ (fun (r : R) (s : S) => ∀ x, e (r • x) = s • e x) as bs) :
    IsWeaklyRegular M as ↔ IsWeaklyRegular M₂ bs := by
  conv => congr <;> rw [isWeaklyRegular_iff_Fin]
  let e' i : (M ⧸ (Ideal.ofList (as.take i) • ⊤ : Submodule R M)) ≃+
      M₂ ⧸ (Ideal.ofList (bs.take i) • ⊤ : Submodule S M₂) :=
    QuotientAddGroup.congr _ _ e <|
      AddHom.map_smul_top_toAddSubgroup_of_surjective e.surjective <|
        List.forall₂_take i h
  refine (finCongr h.length_eq).forall_congr @fun _ => (e' _).isSMulRegular_congr ?_
  exact (mkQ_surjective _).forall.mpr fun _ => congrArg (mkQ _) (h.get _ _ _)
/-
**RingTheory.Sequence._root_.LinearEquiv.isWeaklyRegular_congr'** 是 Mathlib 中的一个
引理，位于命名空间 `RingTheory.Sequence`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma _root_.LinearEquiv.isWeaklyRegular_congr' (e : M ≃ₛₗ[σ] M₂) (rs : List R) :
    IsWeaklyRegular M rs ↔ IsWeaklyRegular M₂ (rs.map σ) :=
  e.toAddEquiv.isWeaklyRegular_congr <| List.forall₂_map_right_iff.mpr <|
    List.forall₂_same.mpr fun r _ x => e.map_smul' r x
/-
**RingTheory.Sequence._root_.LinearEquiv.isWeaklyRegular_congr** 是 Mathlib 中的一个引
理，位于命名空间 `RingTheory.Sequence`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma _root_.LinearEquiv.isWeaklyRegular_congr [Module R M₂] (e : M ≃ₗ[R] M₂) (rs : List R) :
    IsWeaklyRegular M rs ↔ IsWeaklyRegular M₂ rs :=
  Iff.trans (e.isWeaklyRegular_congr' rs) <| iff_of_eq <| congrArg _ rs.map_id
/-
**RingTheory.Sequence._root_.AddEquiv.isRegular_congr** 是 Mathlib 中的一个引理，位于命名空间 
`RingTheory.Sequence`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma _root_.AddEquiv.isRegular_congr {e : M ≃+ M₂} {as bs}
    (h : List.Forall₂ (fun (r : R) (s : S) => ∀ x, e (r • x) = s • e x) as bs) :
    IsRegular M as ↔ IsRegular M₂ bs := by
  conv => congr <;> rw [isRegular_iff, ne_comm, ← Quotient.nontrivial_iff]
  let e' := QuotientAddGroup.congr _ _ e <|
    AddHom.map_smul_top_toAddSubgroup_of_surjective e.surjective h
  exact and_congr (e.isWeaklyRegular_congr h) e'.nontrivial_congr
/-
**RingTheory.Sequence._root_.LinearEquiv.isRegular_congr'** 是 Mathlib 中的一个引理，位于命
名空间 `RingTheory.Sequence`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma _root_.LinearEquiv.isRegular_congr' (e : M ≃ₛₗ[σ] M₂) (rs : List R) :
    IsRegular M rs ↔ IsRegular M₂ (rs.map σ) :=
  e.toAddEquiv.isRegular_congr <| List.forall₂_map_right_iff.mpr <|
    List.forall₂_same.mpr fun r _ x => e.map_smul' r x
/-
**RingTheory.Sequence._root_.LinearEquiv.isRegular_congr** 是 Mathlib 中的一个引理，位于命名
空间 `RingTheory.Sequence`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma _root_.LinearEquiv.isRegular_congr [Module R M₂] (e : M ≃ₗ[R] M₂) (rs : List R) :
    IsRegular M rs ↔ IsRegular M₂ rs :=
  Iff.trans (e.isRegular_congr' rs) <| iff_of_eq <| congrArg _ rs.map_id

end Congr

/-
**RingTheory.Sequence.isWeaklyRegular_map_algebraMap_iff** 是 Mathlib 中的一个引理，位于命名
空间 `RingTheory.Sequence`。
形式化陈述：isWeaklyRegular_map_algebraMap_iff [CommRing R] [CommRing S] [Algebra R S]
 [AddCommGroup M] [Module R M] [Module S M] [IsScalarTower R S M] (rs : List R) 
: IsWeaklyRegular M (rs.map (algebraMap R S)) ↔ IsWeaklyRegular M rs
参数：rs : List R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `AddEquiv.isWeaklyRegular_congr`：∀ {R : Type u_1} {S : Type u_2} {M : Typ
e u_3} {M₂ : Type u_4} [inst : CommRing R] [inst_1 : CommRing S]   [inst_2 : Add
CommGroup M] [inst_3…
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `List.forall₂_map_left_iff`：∀ {α : Type u_1} {β : Type u_2} {γ : Type u_3
} {R : α → β → Prop} {f : γ → α} {l : List γ} {u : List β},   List.Forall₂ R (Li
st.map f l) u ↔…
· 使用定理 `List.forall₂_same`：∀ {α : Type u_1} {Rₐ : α → α → Prop} {l : List α}, Li
st.Forall₂ Rₐ l l ↔ ∀ x ∈ l, Rₐ x x
· 使用定理 `algebraMap_smul`：algebraMap_smul (r : R) (m : M) : (algebraMap R A) r • 
m = r • m
-/
lemma isWeaklyRegular_map_algebraMap_iff [CommRing R] [CommRing S]
    [Algebra R S] [AddCommGroup M] [Module R M] [Module S M]
    [IsScalarTower R S M] (rs : List R) :
    IsWeaklyRegular M (rs.map (algebraMap R S)) ↔ IsWeaklyRegular M rs :=
  (AddEquiv.refl M).isWeaklyRegular_congr <| List.forall₂_map_left_iff.mpr <|
    List.forall₂_same.mpr fun r _ => algebraMap_smul S r

variable [CommRing R] [AddCommGroup M] [AddCommGroup M₂] [AddCommGroup M₃]
    [AddCommGroup M₄] [Module R M] [Module R M₂] [Module R M₃] [Module R M₄]

@[simp]
/-
**RingTheory.Sequence.isWeaklyRegular_cons_iff** 是 Mathlib 中的一个引理，位于命名空间 `RingTh
eory.Sequence`。
形式化陈述：isWeaklyRegular_cons_iff (r : R) (rs : List R) : IsWeaklyRegular M (r :: r
s) ↔ IsSMulRegular M r ∧ IsWeaklyRegular (QuotSMulTop r M) rs
参数：r : R；rs : List R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.ofList_nil`：∀ {R : Type u_1} [inst : Semiring R], Ideal.ofList [] 
= ⊥
· 使用定理 `Submodule.bot_smul`：bot_smul : (⊥ : Submodule R A) • N = ⊥
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用引理 `RingTheory.Sequence.isWeaklyRegular_iff_Fin`：isWeaklyRegular_iff_Fin (rs
 : List R) : IsWeaklyRegular M rs ↔ forall (i : Fin rs.length), IsSMulRegular (M
 ⧸ (ofList (rs.take i) • ⊤ : Subm…
· 使用定理 `instNeZeroNatHAdd_1`：∀ {n m : ℕ} [h : NeZero m], NeZero (n + m)
· 使用定理 `Nat.instNeZeroSucc`：∀ {n : ℕ}, NeZero (n + 1)
· 使用定理 `Fin.forall_iff_succ`：∀ {n : ℕ} {P : Fin (n + 1) → Prop}, (∀ (i : Fin (n 
+ 1)), P i) ↔ P 0 ∧ ∀ (i : Fin n), P i.succ
· 使用定理 `and_congr`：∀ {a c b d : Prop}, (a ↔ c) → (b ↔ d) → (a ∧ b ↔ c ∧ d)
· 使用引理 `LinearEquiv.isSMulRegular_congr`：LinearEquiv.isSMulRegular_congr [AddCom
mMonoid N] [Module R N] (e : M ≃ₗ[R] N) (r : R) : IsSMulRegular M r ↔ IsSMulRegu
lar N r
· 使用定理 `forall_congr'`：∀ {α : Sort u_1} {p q : α → Prop}, (∀ (a : α), p a ↔ q a)
 → ((∀ (a : α), p a) ↔ ∀ (a : α), q a)
· 使用定理 `Iff.symm`：∀ {a b : Prop}, (a ↔ b) → (b ↔ a)
-/
lemma isWeaklyRegular_cons_iff (r : R) (rs : List R) :
    IsWeaklyRegular M (r :: rs) ↔
      IsSMulRegular M r ∧ IsWeaklyRegular (QuotSMulTop r M) rs :=
  have := Eq.trans (congrArg (· • ⊤) Ideal.ofList_nil) (bot_smul ⊤)
  let e i := quotOfListConsSMulTopEquivQuotSMulTopInner M r (rs.take i)
  Iff.trans (isWeaklyRegular_iff_Fin _ _) <| Iff.trans Fin.forall_iff_succ <|
    and_congr ((quotEquivOfEqBot _ this).isSMulRegular_congr r) <|
      Iff.trans (forall_congr' fun i => (e i).isSMulRegular_congr (rs.get i))
        (isWeaklyRegular_iff_Fin _ _).symm
/-
**RingTheory.Sequence.isWeaklyRegular_cons_iff'** 是 Mathlib 中的一个引理，位于命名空间 `RingT
heory.Sequence`。
形式化陈述：isWeaklyRegular_cons_iff' (r : R) (rs : List R) : IsWeaklyRegular M (r :: 
rs) ↔ IsSMulRegular M r ∧ IsWeaklyRegular (QuotSMulTop r M) (rs.map (Ideal.Quoti
ent.mk (Ideal.span {r})))
参数：r : R；rs : List R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `Ideal.instIsTwoSided_1`：∀ {α : Type u_1} [inst : CommRing α] (I : Ideal 
α), I.IsTwoSided
· 使用引理 `RingTheory.Sequence.isWeaklyRegular_cons_iff`：isWeaklyRegular_cons_iff (
r : R) (rs : List R) : IsWeaklyRegular M (r :: rs) ↔ IsSMulRegular M r ∧ IsWeakl
yRegular (QuotSMulTop r M) rs
· 使用定理 `and_congr_right'`：∀ {b c a : Prop}, (b ↔ c) → (a ∧ b ↔ a ∧ c)
· 使用定理 `Iff.symm`：∀ {a b : Prop}, (a ↔ b) → (b ↔ a)
· 使用引理 `RingTheory.Sequence.isWeaklyRegular_map_algebraMap_iff`：isWeaklyRegular_
map_algebraMap_iff [CommRing R] [CommRing S] [Algebra R S] [AddCommGroup M] [Mod
ule R M] [Module S M] [IsScalarTower R S M] …
· 使用定理 `Module.IsTorsionBySet.isScalarTower`：∀ {R : Type u_1} {M : Type u_2} [in
st : Ring R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M] {I : Ideal R
}   (hM : Module.IsTorsio…
· 使用引理 `Module.isTorsionBy_quotient_element_smul`：isTorsionBy_quotient_element_s
mul : IsTorsionBy R (M ⧸ r • (⊤ : Submodule R M)) r
-/
lemma isWeaklyRegular_cons_iff' (r : R) (rs : List R) :
    IsWeaklyRegular M (r :: rs) ↔
      IsSMulRegular M r ∧
        IsWeaklyRegular (QuotSMulTop r M)
          (rs.map (Ideal.Quotient.mk (Ideal.span {r}))) :=
  Iff.trans (isWeaklyRegular_cons_iff M r rs) <| and_congr_right' <|
    Iff.symm <| isWeaklyRegular_map_algebraMap_iff (R ⧸ Ideal.span {r}) _ rs

@[simp]
/-
**RingTheory.Sequence.isRegular_cons_iff** 是 Mathlib 中的一个引理，位于命名空间 `RingTheory.S
equence`。
形式化陈述：isRegular_cons_iff (r : R) (rs : List R) : IsRegular M (r :: rs) ↔ IsSMulR
egular M r ∧ IsRegular (QuotSMulTop r M) rs
参数：r : R；rs : List R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `RingTheory.Sequence.isRegular_iff`：∀ {R : Type u_1} (M : Type u_3) [inst
 : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M]   (rs : Li
st R), RingTheory.Seque…
· 使用引理 `RingTheory.Sequence.isWeaklyRegular_cons_iff`：isWeaklyRegular_cons_iff (
r : R) (rs : List R) : IsWeaklyRegular M (r :: rs) ↔ IsSMulRegular M r ∧ IsWeakl
yRegular (QuotSMulTop r M) rs
· 使用定理 `ne_eq`：∀ {α : Sort u_1} (a b : α), (a ≠ b) = ¬a = b
· 使用引理 `Submodule.top_eq_ofList_cons_smul_iff`：top_eq_ofList_cons_smul_iff : (⊤ 
: Submodule R M) = Ideal.ofList (r :: rs) • ⊤ ↔ (⊤ : Submodule R (QuotSMulTop r 
M)) = Ideal.ofList rs • ⊤
· 使用定理 `and_assoc`：∀ {a b c : Prop}, (a ∧ b) ∧ c ↔ a ∧ b ∧ c
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma isRegular_cons_iff (r : R) (rs : List R) :
    IsRegular M (r :: rs) ↔
      IsSMulRegular M r ∧ IsRegular (QuotSMulTop r M) rs := by
  rw [isRegular_iff, isRegular_iff, isWeaklyRegular_cons_iff M r rs,
    ne_eq, top_eq_ofList_cons_smul_iff, and_assoc]
/-
**RingTheory.Sequence.isRegular_cons_iff'** 是 Mathlib 中的一个引理，位于命名空间 `RingTheory.
Sequence`。
形式化陈述：isRegular_cons_iff' (r : R) (rs : List R) : IsRegular M (r :: rs) ↔ IsSMul
Regular M r ∧ IsRegular (QuotSMulTop r M) (rs.map (Ideal.Quotient.mk (Ideal.span
 {r})))
参数：r : R；rs : List R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.instIsTwoSided_1`：∀ {α : Type u_1} [inst : CommRing α] (I : Ideal 
α), I.IsTwoSided
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `RingTheory.Sequence.isRegular_iff`：∀ {R : Type u_1} (M : Type u_3) [inst
 : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M]   (rs : Li
st R), RingTheory.Seque…
· 使用定理 `ne_eq`：∀ {α : Sort u_1} (a b : α), (a ≠ b) = ¬a = b
· 使用引理 `RingTheory.Sequence.isWeaklyRegular_cons_iff'`：isWeaklyRegular_cons_iff'
 (r : R) (rs : List R) : IsWeaklyRegular M (r :: rs) ↔ IsSMulRegular M r ∧ IsWea
klyRegular (QuotSMulTop r M) (rs.ma…
· 使用定理 `Module.IsTorsionBySet.isScalarTower`：∀ {R : Type u_1} {M : Type u_2} [in
st : Ring R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M] {I : Ideal R
}   (hM : Module.IsTorsio…
· 使用引理 `Module.isTorsionBy_quotient_element_smul`：isTorsionBy_quotient_element_s
mul : IsTorsionBy R (M ⧸ r • (⊤ : Submodule R M)) r
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Submodule.restrictScalars_inj`：restrictScalars_inj {V₁ V₂ : Submodule R 
M} : restrictScalars S V₁ = restrictScalars S V₂ ↔ V₁ = V₂
· 使用定理 `Ideal.map_ofList`：∀ {R : Type u_1} {S : Type u_2} [inst : Semiring R] [i
nst_1 : Semiring S] (f : R →+* S) (rs : List R),   Ideal.map f (Ideal.ofList rs)
 = Ide…
· 使用定理 `Ideal.Quotient.algebraMap_eq`：∀ {R : Type u_5} [inst : CommRing R] (I : 
Ideal R), algebraMap R (R ⧸ I) = Ideal.Quotient.mk I
· 使用定理 `Ideal.smul_restrictScalars`：smul_restrictScalars {R S M} [CommSemiring R
] [CommSemiring S] [Algebra R S] [AddCommMonoid M] [Module R M] [Module S M] [Is
ScalarTower R S …
· 使用定理 `Submodule.restrictScalars_top`：restrictScalars_top : restrictScalars S (
⊤ : Submodule R M) = ⊤
· 使用引理 `Submodule.top_eq_ofList_cons_smul_iff`：top_eq_ofList_cons_smul_iff : (⊤ 
: Submodule R M) = Ideal.ofList (r :: rs) • ⊤ ↔ (⊤ : Submodule R (QuotSMulTop r 
M)) = Ideal.ofList rs • ⊤
· 使用定理 `and_assoc`：∀ {a b c : Prop}, (a ∧ b) ∧ c ↔ a ∧ b ∧ c
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma isRegular_cons_iff' (r : R) (rs : List R) :
    IsRegular M (r :: rs) ↔
      IsSMulRegular M r ∧ IsRegular (QuotSMulTop r M)
          (rs.map (Ideal.Quotient.mk (Ideal.span {r}))) := by
  conv => congr <;> rw [isRegular_iff, ne_eq]
  rw [isWeaklyRegular_cons_iff', ← restrictScalars_inj R (R ⧸ _),
    ← Ideal.map_ofList, ← Ideal.Quotient.algebraMap_eq, Ideal.smul_restrictScalars,
    restrictScalars_top, top_eq_ofList_cons_smul_iff, and_assoc]

variable {M}

namespace IsWeaklyRegular

variable (R M) in
/-
**RingTheory.Sequence.IsWeaklyRegular.nil** 是 Mathlib 中的一个定理，位于命名空间 `RingTheory.
Sequence.IsWeaklyRegular`。
形式化陈述：∀ (R : Type u_1) (M : Type u_3) [inst : CommRing R] [inst_1 : AddCommGroup
 M] [inst_2 : _root_.Module R M],   RingTheory.Sequence.IsWeaklyRegular M []
参数：R : Type u_1；M : Type u_3。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Nat.not_lt_zero`：∀ (n : ℕ), ¬n < 0
-/
@[simp] lemma nil : IsWeaklyRegular M ([] : List R) :=
  .mk (False.elim <| Nat.not_lt_zero · ·)
/-
**RingTheory.Sequence.IsWeaklyRegular.cons** 是 Mathlib 中的一个引理，位于命名空间 `RingTheory
.Sequence.IsWeaklyRegular`。
形式化陈述：cons {r : R} {rs : List R} (h1 : IsSMulRegular M r) (h2 : IsWeaklyRegular 
(QuotSMulTop r M) rs) : IsWeaklyRegular M (r :: rs)
参数：h1 : IsSMulRegular M r；h2 : IsWeaklyRegular (QuotSMulTop r M) rs。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用引理 `RingTheory.Sequence.isWeaklyRegular_cons_iff`：isWeaklyRegular_cons_iff (
r : R) (rs : List R) : IsWeaklyRegular M (r :: rs) ↔ IsSMulRegular M r ∧ IsWeakl
yRegular (QuotSMulTop r M) rs
-/
lemma cons {r : R} {rs : List R} (h1 : IsSMulRegular M r)
    (h2 : IsWeaklyRegular (QuotSMulTop r M) rs) : IsWeaklyRegular M (r :: rs) :=
  (isWeaklyRegular_cons_iff M r rs).mpr ⟨h1, h2⟩
/-
**RingTheory.Sequence.IsWeaklyRegular.cons'** 是 Mathlib 中的一个引理，位于命名空间 `RingTheor
y.Sequence.IsWeaklyRegular`。
形式化陈述：cons' {r : R} {rs : List R} (h1 : IsSMulRegular M r) (h2 : IsWeaklyRegular
 (QuotSMulTop r M) (rs.map (Ideal.Quotient.mk (Ideal.span {r})))) : IsWeaklyRegu
lar M (r :: rs)
参数：h1 : IsSMulRegular M r；h2 : IsWeaklyRegular (QuotSMulTop r M) (rs.map (Ideal.
Quotient.mk (Ideal.span {r})))。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.instIsTwoSided_1`：∀ {α : Type u_1} [inst : CommRing α] (I : Ideal 
α), I.IsTwoSided
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用引理 `RingTheory.Sequence.isWeaklyRegular_cons_iff'`：isWeaklyRegular_cons_iff'
 (r : R) (rs : List R) : IsWeaklyRegular M (r :: rs) ↔ IsSMulRegular M r ∧ IsWea
klyRegular (QuotSMulTop r M) (rs.ma…
-/
lemma cons' {r : R} {rs : List R} (h1 : IsSMulRegular M r)
    (h2 : IsWeaklyRegular (QuotSMulTop r M)
            (rs.map (Ideal.Quotient.mk (Ideal.span {r})))) :
    IsWeaklyRegular M (r :: rs) :=
  (isWeaklyRegular_cons_iff' M r rs).mpr ⟨h1, h2⟩

/-- Weakly regular sequences can be inductively characterized by:
* The empty sequence is weakly regular on any module.
* If `r` is regular on `M` and `rs` is a weakly regular sequence on `M⧸rM` then
  the sequence obtained from `rs` by prepending `r` is weakly regular on `M`.

This is the induction principle produced by the inductive definition above.
The motive will usually be valued in `Prop`, but `Sort*` works too. -/
@[induction_eliminator]
/-
**RingTheory.Sequence.IsWeaklyRegular.recIterModByRegular** 是 Mathlib 中的一个定义，位于命
名空间 `RingTheory.Sequence.IsWeaklyRegular`。
形式化陈述：recIterModByRegular {motive : (M : Type v) -> [AddCommGroup M] -> [Module 
R M] -> (rs : List R) -> IsWeaklyRegular M rs -> Sort*} (nil : (M : Type v) -> [
AddCommGroup M] -> [Module R M] -> motive M [] (nil R M)) (cons : {M : Type v} -
> [AddCommGroup M] -> [Module R M] -> (r : R) -> (rs : List R) -> (h1 : IsSMulRe
gular M r) -> (h2 : IsWeaklyRegular (QuotSMulTop r M) rs) -> (ih : motive (QuotS
MulTop r M) rs h2) -> motive M (r :: rs) (cons h1 h2)) : {M : Type v} -> [AddCom
mGroup M] -> [Module R M] 
参数：M : Type v；rs : List R；nil : (M : Type v) -> [AddCommGroup M] -> [Module R M]
 -> motive M [] (nil R M)；cons : {M : Type v} -> [AddCommGroup M] -> [Module R M
] -> (r : R) -> (rs : List R) -> (h1 : IsSMulRegular M r) -> (h2 : IsWeaklyRegul
ar (QuotSMulTop r M) rs) -> (ih : motive (QuotSMulTop r M) rs h2) -> motive M (r
 :: rs) (cons h1 h2)。
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `RingTheory.Sequence.IsWeaklyRegular.nil`：∀ (R : Type u_1) (M : Type u_3)
 [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M],   R
ingTheory.Sequence.IsWeaklyRe…
· 使用引理 `RingTheory.Sequence.IsWeaklyRegular.cons`：cons {r : R} {rs : List R} (h1
 : IsSMulRegular M r) (h2 : IsWeaklyRegular (QuotSMulTop r M) rs) : IsWeaklyRegu
lar M (r :: rs)

--- 原说明 ---
Weakly regular sequences can be inductively characterized by:
* The empty sequence is weakly regular on any module.
* If `r` is regular on `M` and `rs` is a weakly regular sequence on `M⧸rM` then
  the sequence obtained from `rs` by prepending `r` is weakly regular on `M`.

This is the induction principle produced by the inductive definition above.
The motive will usually be valued in `Prop`, but `Sort*` works too.
-/
def recIterModByRegular
    {motive : (M : Type v) → [AddCommGroup M] → [Module R M] → (rs : List R) →
      IsWeaklyRegular M rs → Sort*}
    (nil : (M : Type v) → [AddCommGroup M] → [Module R M] → motive M [] (nil R M))
    (cons : {M : Type v} → [AddCommGroup M] → [Module R M] → (r : R) →
      (rs : List R) → (h1 : IsSMulRegular M r) →
      (h2 : IsWeaklyRegular (QuotSMulTop r M) rs) →
      (ih : motive (QuotSMulTop r M) rs h2) → motive M (r :: rs) (cons h1 h2)) :
    {M : Type v} → [AddCommGroup M] → [Module R M] → {rs : List R} →
    (h : IsWeaklyRegular M rs) → motive M rs h
  | M, _, _, [], _ => nil M
  | M, _, _, r :: rs, h =>
    let ⟨h1, h2⟩ := (isWeaklyRegular_cons_iff M r rs).mp h
    cons r rs h1 h2 (recIterModByRegular nil cons h2)

/-- A simplified version of `IsWeaklyRegular.recIterModByRegular` where the
motive is not allowed to depend on the proof of `IsWeaklyRegular`. -/
/-
**RingTheory.Sequence.IsWeaklyRegular.ndrecIterModByRegular** 是 Mathlib 中的一个定义，位
于命名空间 `RingTheory.Sequence.IsWeaklyRegular`。
形式化陈述：ndrecIterModByRegular {motive : (M : Type v) -> [AddCommGroup M] -> [Modul
e R M] -> (rs : List R) -> Sort*} (nil : (M : Type v) -> [AddCommGroup M] -> [Mo
dule R M] -> motive M []) (cons : {M : Type v} -> [AddCommGroup M] -> [Module R 
M] -> (r : R) -> (rs : List R) -> IsSMulRegular M r -> IsWeaklyRegular (QuotSMul
Top r M) rs -> motive (QuotSMulTop r M) rs -> motive M (r :: rs)) {M} [AddCommGr
oup M] [Module R M] {rs} : IsWeaklyRegular M rs -> motive M rs
参数：M : Type v；rs : List R；nil : (M : Type v) -> [AddCommGroup M] -> [Module R M]
 -> motive M []；cons : {M : Type v} -> [AddCommGroup M] -> [Module R M] -> (r : 
R) -> (rs : List R) -> IsSMulRegular M r -> IsWeaklyRegular (QuotSMulTop r M) rs
 -> motive (QuotSMulTop r M) rs -> motive M (r :: rs)。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A simplified version of `IsWeaklyRegular.recIterModByRegular` where the
motive is not allowed to depend on the proof of `IsWeaklyRegular`.
-/
def ndrecIterModByRegular
    {motive : (M : Type v) → [AddCommGroup M] → [Module R M] → (rs : List R) → Sort*}
    (nil : (M : Type v) → [AddCommGroup M] → [Module R M] → motive M [])
    (cons : {M : Type v} → [AddCommGroup M] → [Module R M] → (r : R) →
      (rs : List R) → IsSMulRegular M r → IsWeaklyRegular (QuotSMulTop r M) rs →
      motive (QuotSMulTop r M) rs → motive M (r :: rs))
    {M} [AddCommGroup M] [Module R M] {rs} :
    IsWeaklyRegular M rs → motive M rs :=
  recIterModByRegular (motive := fun M _ _ rs _ => motive M rs) nil cons

/-- An alternate induction principle from `IsWeaklyRegular.recIterModByRegular`
where we mod out by successive elements in both the module and the base ring.
This is useful for propagating certain properties of the initial `M`, e.g.
faithfulness or freeness, throughout the induction. -/
/-
**RingTheory.Sequence.IsWeaklyRegular.recIterModByRegularWithRing** 是 Mathlib 中的
一个定义，位于命名空间 `RingTheory.Sequence.IsWeaklyRegular`。
形式化陈述：recIterModByRegularWithRing {motive : (R : Type u) -> [CommRing R] -> (M :
 Type v) -> [AddCommGroup M] -> [Module R M] -> (rs : List R) -> IsWeaklyRegular
 M rs -> Sort*} (nil : (R : Type u) -> [CommRing R] -> (M : Type v) -> [AddCommG
roup M] -> [Module R M] -> motive R M [] (nil R M)) (cons : {R : Type u} -> [Com
mRing R] -> {M : Type v} -> [AddCommGroup M] -> [Module R M] -> (r : R) -> (rs :
 List R) -> (h1 : IsSMulRegular M r) -> (h2 : IsWeaklyRegular (QuotSMulTop r M) 
(rs.map (Ideal.Quotient.mk
参数：R : Type u；M : Type v；rs : List R；nil : (R : Type u) -> [CommRing R] -> (M : 
Type v) -> [AddCommGroup M] -> [Module R M] -> motive R M [] (nil R M)。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `RingTheory.Sequence.IsWeaklyRegular.nil`：∀ (R : Type u_1) (M : Type u_3)
 [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M],   R
ingTheory.Sequence.IsWeaklyRe…
· 使用引理 `RingTheory.Sequence.IsWeaklyRegular.cons'`：cons' {r : R} {rs : List R} (
h1 : IsSMulRegular M r) (h2 : IsWeaklyRegular (QuotSMulTop r M) (rs.map (Ideal.Q
uotient.mk (Ideal.span {r})))) …

--- 原说明 ---
An alternate induction principle from `IsWeaklyRegular.recIterModByRegular`
where we mod out by successive elements in both the module and the base ring.
This is useful for propagating certain properties of the initial `M`, e.g.
faithfulness or freeness, throughout the induction.
-/
def recIterModByRegularWithRing
    {motive : (R : Type u) → [CommRing R] → (M : Type v) → [AddCommGroup M] →
      [Module R M] → (rs : List R) → IsWeaklyRegular M rs → Sort*}
    (nil : (R : Type u) → [CommRing R] → (M : Type v) → [AddCommGroup M] →
      [Module R M] → motive R M [] (nil R M))
    (cons : {R : Type u} → [CommRing R] → {M : Type v} → [AddCommGroup M] →
      [Module R M] → (r : R) → (rs : List R) → (h1 : IsSMulRegular M r) →
      (h2 : IsWeaklyRegular (QuotSMulTop r M)
              (rs.map (Ideal.Quotient.mk (Ideal.span {r})))) →
      (ih : motive (R ⧸ Ideal.span {r}) (QuotSMulTop r M)
              (rs.map (Ideal.Quotient.mk (Ideal.span {r}))) h2) →
            motive R M (r :: rs) (cons' h1 h2)) :
    {R : Type u} → [CommRing R] → {M : Type v} → [AddCommGroup M] →
    [Module R M] → {rs : List R} → (h : IsWeaklyRegular M rs) → motive R M rs h
  | R, _, M, _, _, [], _ => nil R M
  | _, _, M, _, _, r :: rs, h =>
    let ⟨h1, h2⟩ := (isWeaklyRegular_cons_iff' M r rs).mp h
    cons r rs h1 h2 (recIterModByRegularWithRing nil cons h2)
  termination_by _ _ _ _ _ rs => List.length rs

/-- A simplified version of `IsWeaklyRegular.recIterModByRegularWithRing` where
the motive is not allowed to depend on the proof of `IsWeaklyRegular`. -/
/-
**RingTheory.Sequence.IsWeaklyRegular.ndrecWithRing** 是 Mathlib 中的一个定义，位于命名空间 `R
ingTheory.Sequence.IsWeaklyRegular`。
形式化陈述：ndrecWithRing {motive : (R : Type u) -> [CommRing R] -> (M : Type v) -> [A
ddCommGroup M] -> [Module R M] -> (rs : List R) -> Sort*} (nil : (R : Type u) ->
 [CommRing R] -> (M : Type v) -> [AddCommGroup M] -> [Module R M] -> motive R M 
[]) (cons : {R : Type u} -> [CommRing R] -> {M : Type v} -> [AddCommGroup M] -> 
[Module R M] -> (r : R) -> (rs : List R) -> IsSMulRegular M r -> IsWeaklyRegular
 (QuotSMulTop r M) (rs.map (Ideal.Quotient.mk (Ideal.span {r}))) -> motive (R ⧸ 
Ideal.span {r}) (QuotSMulT
参数：R : Type u；M : Type v；rs : List R；nil : (R : Type u) -> [CommRing R] -> (M : 
Type v) -> [AddCommGroup M] -> [Module R M] -> motive R M []。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A simplified version of `IsWeaklyRegular.recIterModByRegularWithRing` where
the motive is not allowed to depend on the proof of `IsWeaklyRegular`.
-/
def ndrecWithRing
    {motive : (R : Type u) → [CommRing R] → (M : Type v) →
      [AddCommGroup M] → [Module R M] → (rs : List R) → Sort*}
    (nil : (R : Type u) → [CommRing R] → (M : Type v) →
      [AddCommGroup M] → [Module R M] → motive R M [])
    (cons : {R : Type u} → [CommRing R] → {M : Type v} → [AddCommGroup M] →
      [Module R M] → (r : R) → (rs : List R) → IsSMulRegular M r →
      IsWeaklyRegular (QuotSMulTop r M)
        (rs.map (Ideal.Quotient.mk (Ideal.span {r}))) →
      motive (R ⧸ Ideal.span {r}) (QuotSMulTop r M)
        (rs.map (Ideal.Quotient.mk (Ideal.span {r}))) → motive R M (r :: rs))
    {R} [CommRing R] {M} [AddCommGroup M] [Module R M] {rs} :
    IsWeaklyRegular M rs → motive R M rs :=
  recIterModByRegularWithRing (motive := fun R _ M _ _ rs _ => motive R M rs)
    nil cons

end IsWeaklyRegular

section

variable (M)

/-
**RingTheory.Sequence.isWeaklyRegular_singleton_iff** 是 Mathlib 中的一个引理，位于命名空间 `R
ingTheory.Sequence`。
形式化陈述：isWeaklyRegular_singleton_iff (r : R) : IsWeaklyRegular M [r] ↔ IsSMulRegu
lar M r
参数：r : R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用引理 `RingTheory.Sequence.isWeaklyRegular_cons_iff`：isWeaklyRegular_cons_iff (
r : R) (rs : List R) : IsWeaklyRegular M (r :: rs) ↔ IsSMulRegular M r ∧ IsWeakl
yRegular (QuotSMulTop r M) rs
· 使用定理 `and_iff_left`：∀ {b a : Prop}, b → (a ∧ b ↔ a)
· 使用定理 `RingTheory.Sequence.IsWeaklyRegular.nil`：∀ (R : Type u_1) (M : Type u_3)
 [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M],   R
ingTheory.Sequence.IsWeaklyRe…
-/
lemma isWeaklyRegular_singleton_iff (r : R) :
    IsWeaklyRegular M [r] ↔ IsSMulRegular M r :=
  Iff.trans (isWeaklyRegular_cons_iff M r []) (and_iff_left (.nil R _))
/-
**RingTheory.Sequence.isWeaklyRegular_append_iff** 是 Mathlib 中的一个引理，位于命名空间 `Ring
Theory.Sequence`。
形式化陈述：isWeaklyRegular_append_iff (rs₁ rs₂ : List R) : IsWeaklyRegular M (rs₁ ++ 
rs₂) ↔ IsWeaklyRegular M rs₁ ∧ IsWeaklyRegular (M ⧸ (Ideal.ofList rs₁ • ⊤ : Subm
odule R M)) rs₂
参数：rs₁ rs₂ : List R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.symm`：∀ {a b : Prop}, (a ↔ b) → (b ↔ a)
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `and_iff_right`：∀ {a b : Prop}, a → (a ∧ b ↔ b)
· 使用定理 `RingTheory.Sequence.IsWeaklyRegular.nil`：∀ (R : Type u_1) (M : Type u_3)
 [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M],   R
ingTheory.Sequence.IsWeaklyRe…
· 使用定理 `LinearEquiv.isWeaklyRegular_congr`：∀ {R : Type u_1} {M : Type u_3} {M₂ :
 Type u_4} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : AddCommGroup 
M₂]   [inst_3 : _root_.…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.ofList_nil`：∀ {R : Type u_1} [inst : Semiring R], Ideal.ofList [] 
= ⊥
· 使用定理 `Submodule.bot_smul`：bot_smul : (⊥ : Submodule R A) • N = ⊥
· 使用定理 `List.cons_append`：∀ {α : Type u} {a : α} {as bs : List α}, a :: as ++ bs
 = a :: (as ++ bs)
· 使用引理 `RingTheory.Sequence.isWeaklyRegular_cons_iff`：isWeaklyRegular_cons_iff (
r : R) (rs : List R) : IsWeaklyRegular M (r :: rs) ↔ IsSMulRegular M r ∧ IsWeakl
yRegular (QuotSMulTop r M) rs
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `and_assoc`：∀ {a b c : Prop}, (a ∧ b) ∧ c ↔ a ∧ b ∧ c
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma isWeaklyRegular_append_iff (rs₁ rs₂ : List R) :
    IsWeaklyRegular M (rs₁ ++ rs₂) ↔
      IsWeaklyRegular M rs₁ ∧
        IsWeaklyRegular (M ⧸ (Ideal.ofList rs₁ • ⊤ : Submodule R M)) rs₂ := by
  induction rs₁ generalizing M with
  | nil =>
    refine Iff.symm <| Iff.trans (and_iff_right (.nil R M)) ?_
    refine (quotEquivOfEqBot _ ?_).isWeaklyRegular_congr rs₂
    rw [Ideal.ofList_nil, bot_smul]
  | cons r rs₁ ih =>
    let e := quotOfListConsSMulTopEquivQuotSMulTopInner M r rs₁
    rw [List.cons_append, isWeaklyRegular_cons_iff, isWeaklyRegular_cons_iff,
      ih, ← and_assoc, ← e.isWeaklyRegular_congr rs₂]
/-
**RingTheory.Sequence.isWeaklyRegular_append_iff'** 是 Mathlib 中的一个引理，位于命名空间 `Rin
gTheory.Sequence`。
形式化陈述：isWeaklyRegular_append_iff' (rs₁ rs₂ : List R) : IsWeaklyRegular M (rs₁ ++
 rs₂) ↔ IsWeaklyRegular M rs₁ ∧ IsWeaklyRegular (M ⧸ (Ideal.ofList rs₁ • ⊤ : Sub
module R M)) (rs₂.map (Ideal.Quotient.mk (Ideal.ofList rs₁)))
参数：rs₁ rs₂ : List R。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `Ideal.instIsTwoSided_1`：∀ {α : Type u_1} [inst : CommRing α] (I : Ideal 
α), I.IsTwoSided
· 使用引理 `RingTheory.Sequence.isWeaklyRegular_append_iff`：isWeaklyRegular_append_i
ff (rs₁ rs₂ : List R) : IsWeaklyRegular M (rs₁ ++ rs₂) ↔ IsWeaklyRegular M rs₁ ∧
 IsWeaklyRegular (M ⧸ (Ideal.ofList …
· 使用定理 `and_congr_right'`：∀ {b c a : Prop}, (b ↔ c) → (a ∧ b ↔ a ∧ c)
· 使用定理 `Iff.symm`：∀ {a b : Prop}, (a ↔ b) → (b ↔ a)
· 使用引理 `RingTheory.Sequence.isWeaklyRegular_map_algebraMap_iff`：isWeaklyRegular_
map_algebraMap_iff [CommRing R] [CommRing S] [Algebra R S] [AddCommGroup M] [Mod
ule R M] [Module S M] [IsScalarTower R S M] …
· 使用定理 `Module.IsTorsionBySet.isScalarTower`：∀ {R : Type u_1} {M : Type u_2} [in
st : Ring R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M] {I : Ideal R
}   (hM : Module.IsTorsio…
· 使用引理 `Module.isTorsionBySet_quotient_ideal_smul`：isTorsionBySet_quotient_ideal
_smul : IsTorsionBySet R (M ⧸ I • (⊤ : Submodule R M)) I
-/
lemma isWeaklyRegular_append_iff' (rs₁ rs₂ : List R) :
    IsWeaklyRegular M (rs₁ ++ rs₂) ↔
      IsWeaklyRegular M rs₁ ∧
        IsWeaklyRegular (M ⧸ (Ideal.ofList rs₁ • ⊤ : Submodule R M))
          (rs₂.map (Ideal.Quotient.mk (Ideal.ofList rs₁))) :=
  Iff.trans (isWeaklyRegular_append_iff M rs₁ rs₂) <| and_congr_right' <|
    Iff.symm <| isWeaklyRegular_map_algebraMap_iff (R ⧸ Ideal.ofList rs₁) _ rs₂

end

namespace IsRegular

variable (R M) in
/-
**RingTheory.Sequence.IsRegular.nil** 是 Mathlib 中的一个引理，位于命名空间 `RingTheory.Sequen
ce.IsRegular`。
形式化陈述：nil [Nontrivial M] : IsRegular M ([] : List R) where toIsWeaklyRegular
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `RingTheory.Sequence.IsWeaklyRegular.nil`：∀ (R : Type u_1) (M : Type u_3)
 [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M],   R
ingTheory.Sequence.IsWeaklyRe…
· 使用定理 `not_subsingleton`：not_subsingleton (α) [Nontrivial α] : ¬Subsingleton α
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Submodule.subsingleton_iff`：subsingleton_iff : Subsingleton (Submodule R
 M) ↔ Subsingleton M
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `subsingleton_iff_bot_eq_top`：subsingleton_iff_bot_eq_top : (⊥ : α) = (⊤ 
: α) ↔ Subsingleton α
· 使用定理 `eq_comm`：∀ {α : Sort u_1} {a b : α}, a = b ↔ b = a
· 使用定理 `Submodule.bot_smul`：bot_smul : (⊥ : Submodule R A) • N = ⊥
· 使用定理 `Ideal.ofList_nil`：∀ {R : Type u_1} [inst : Semiring R], Ideal.ofList [] 
= ⊥
-/
lemma nil [Nontrivial M] : IsRegular M ([] : List R) where
  toIsWeaklyRegular := IsWeaklyRegular.nil R M
  top_ne_smul h := by
    rw [Ideal.ofList_nil, bot_smul, eq_comm, subsingleton_iff_bot_eq_top] at h
    exact not_subsingleton M ((Submodule.subsingleton_iff _).mp h)
/-
**RingTheory.Sequence.IsRegular.cons** 是 Mathlib 中的一个引理，位于命名空间 `RingTheory.Seque
nce.IsRegular`。
形式化陈述：cons {r : R} {rs : List R} (h1 : IsSMulRegular M r) (h2 : IsRegular (QuotS
MulTop r M) rs) : IsRegular M (r :: rs)
参数：h1 : IsSMulRegular M r；h2 : IsRegular (QuotSMulTop r M) rs。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用引理 `RingTheory.Sequence.isRegular_cons_iff`：isRegular_cons_iff (r : R) (rs :
 List R) : IsRegular M (r :: rs) ↔ IsSMulRegular M r ∧ IsRegular (QuotSMulTop r 
M) rs
-/
lemma cons {r : R} {rs : List R} (h1 : IsSMulRegular M r)
    (h2 : IsRegular (QuotSMulTop r M) rs) : IsRegular M (r :: rs) :=
  (isRegular_cons_iff M r rs).mpr ⟨h1, h2⟩
/-
**RingTheory.Sequence.IsRegular.cons'** 是 Mathlib 中的一个引理，位于命名空间 `RingTheory.Sequ
ence.IsRegular`。
形式化陈述：cons' {r : R} {rs : List R} (h1 : IsSMulRegular M r) (h2 : IsRegular (Quot
SMulTop r M) (rs.map (Ideal.Quotient.mk (Ideal.span {r})))) : IsRegular M (r :: 
rs)
参数：h1 : IsSMulRegular M r；h2 : IsRegular (QuotSMulTop r M) (rs.map (Ideal.Quotie
nt.mk (Ideal.span {r})))。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.instIsTwoSided_1`：∀ {α : Type u_1} [inst : CommRing α] (I : Ideal 
α), I.IsTwoSided
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用引理 `RingTheory.Sequence.isRegular_cons_iff'`：isRegular_cons_iff' (r : R) (rs
 : List R) : IsRegular M (r :: rs) ↔ IsSMulRegular M r ∧ IsRegular (QuotSMulTop 
r M) (rs.map (Ideal.Quotient.…
-/
lemma cons' {r : R} {rs : List R} (h1 : IsSMulRegular M r)
    (h2 : IsRegular (QuotSMulTop r M) (rs.map (Ideal.Quotient.mk (Ideal.span {r})))) :
    IsRegular M (r :: rs) :=
  (isRegular_cons_iff' M r rs).mpr ⟨h1, h2⟩

/-- Regular sequences can be inductively characterized by:
* The empty sequence is regular on any nonzero module.
* If `r` is regular on `M` and `rs` is a regular sequence on `M⧸rM` then the
  sequence obtained from `rs` by prepending `r` is regular on `M`.

This is the induction principle produced by the inductive definition above.
The motive will usually be valued in `Prop`, but `Sort*` works too. -/
@[induction_eliminator]
/-
**RingTheory.Sequence.IsRegular.recIterModByRegular** 是 Mathlib 中的一个定义，位于命名空间 `R
ingTheory.Sequence.IsRegular`。
形式化陈述：recIterModByRegular {motive : (M : Type v) -> [AddCommGroup M] -> [Module 
R M] -> (rs : List R) -> IsRegular M rs -> Sort*} (nil : (M : Type v) -> [AddCom
mGroup M] -> [Module R M] -> [Nontrivial M] -> motive M [] (nil R M)) (cons : {M
 : Type v} -> [AddCommGroup M] -> [Module R M] -> (r : R) -> (rs : List R) -> (h
1 : IsSMulRegular M r) -> (h2 : IsRegular (QuotSMulTop r M) rs) -> (ih : motive 
(QuotSMulTop r M) rs h2) -> motive M (r :: rs) (cons h1 h2)) {M} [AddCommGroup M
] [Module R M] {rs} (h : I
参数：M : Type v；rs : List R；nil : (M : Type v) -> [AddCommGroup M] -> [Module R M]
 -> [Nontrivial M] -> motive M [] (nil R M)；cons : {M : Type v} -> [AddCommGroup
 M] -> [Module R M] -> (r : R) -> (rs : List R) -> (h1 : IsSMulRegular M r) -> (
h2 : IsRegular (QuotSMulTop r M) rs) -> (ih : motive (QuotSMulTop r M) rs h2) ->
 motive M (r :: rs) (cons h1 h2)。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用引理 `RingTheory.Sequence.IsRegular.nil`：nil [Nontrivial M] : IsRegular M ([] 
: List R) where toIsWeaklyRegular
· 使用引理 `RingTheory.Sequence.IsRegular.cons`：cons {r : R} {rs : List R} (h1 : IsS
MulRegular M r) (h2 : IsRegular (QuotSMulTop r M) rs) : IsRegular M (r :: rs)
· 使用定理 `RingTheory.Sequence.IsRegular.toIsWeaklyRegular`：∀ {R : Type u_1} {M : T
ype u_3} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R
 M]   {rs : List R}, RingTheory.Seque…
· 使用定理 `RingTheory.Sequence.IsRegular.top_ne_smul`：∀ {R : Type u_1} {M : Type u_
3} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M]   
{rs : List R}, RingTheory.Seque…

--- 原说明 ---
Regular sequences can be inductively characterized by:
* The empty sequence is regular on any nonzero module.
* If `r` is regular on `M` and `rs` is a regular sequence on `M⧸rM` then the
  sequence obtained from `rs` by prepending `r` is regular on `M`.

This is the induction principle produced by the inductive definition above.
The motive will usually be valued in `Prop`, but `Sort*` works too.
-/
def recIterModByRegular
    {motive : (M : Type v) → [AddCommGroup M] → [Module R M] → (rs : List R) →
      IsRegular M rs → Sort*}
    (nil : (M : Type v) → [AddCommGroup M] → [Module R M] → [Nontrivial M] →
      motive M [] (nil R M))
    (cons : {M : Type v} → [AddCommGroup M] → [Module R M] → (r : R) →
      (rs : List R) → (h1 : IsSMulRegular M r) → (h2 : IsRegular (QuotSMulTop r M) rs) →
      (ih : motive (QuotSMulTop r M) rs h2) → motive M (r :: rs) (cons h1 h2))
    {M} [AddCommGroup M] [Module R M] {rs} (h : IsRegular M rs) : motive M rs h :=
  h.toIsWeaklyRegular.recIterModByRegular
    (motive := fun N _ _ rs' h' => ∀ h'', motive N rs' ⟨h', h''⟩)
    (fun N _ _ h' =>
      haveI := (nontrivial_iff R).mp (nontrivial_of_ne _ _ h'); nil N)
    (fun r rs' h1 h2 h3 h4 =>
      have ⟨h5, h6⟩ := (isRegular_cons_iff _ _ _).mp ⟨h2.cons h1, h4⟩
      cons r rs' h5 h6 (h3 h6.top_ne_smul))
    h.top_ne_smul

/-- A simplified version of `IsRegular.recIterModByRegular` where the motive is
not allowed to depend on the proof of `IsRegular`. -/
/-
**RingTheory.Sequence.IsRegular.ndrecIterModByRegular** 是 Mathlib 中的一个定义，位于命名空间 
`RingTheory.Sequence.IsRegular`。
形式化陈述：ndrecIterModByRegular {motive : (M : Type v) -> [AddCommGroup M] -> [Modul
e R M] -> (rs : List R) -> Sort*} (nil : (M : Type v) -> [AddCommGroup M] -> [Mo
dule R M] -> [Nontrivial M] -> motive M []) (cons : {M : Type v} -> [AddCommGrou
p M] -> [Module R M] -> (r : R) -> (rs : List R) -> IsSMulRegular M r -> IsRegul
ar (QuotSMulTop r M) rs -> motive (QuotSMulTop r M) rs -> motive M (r :: rs)) {M
} [AddCommGroup M] [Module R M] {rs} : IsRegular M rs -> motive M rs
参数：M : Type v；rs : List R；nil : (M : Type v) -> [AddCommGroup M] -> [Module R M]
 -> [Nontrivial M] -> motive M []；cons : {M : Type v} -> [AddCommGroup M] -> [Mo
dule R M] -> (r : R) -> (rs : List R) -> IsSMulRegular M r -> IsRegular (QuotSMu
lTop r M) rs -> motive (QuotSMulTop r M) rs -> motive M (r :: rs)。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A simplified version of `IsRegular.recIterModByRegular` where the motive is
not allowed to depend on the proof of `IsRegular`.
-/
def ndrecIterModByRegular
    {motive : (M : Type v) → [AddCommGroup M] → [Module R M] → (rs : List R) → Sort*}
    (nil : (M : Type v) → [AddCommGroup M] → [Module R M] → [Nontrivial M] → motive M [])
    (cons : {M : Type v} → [AddCommGroup M] → [Module R M] → (r : R) →
      (rs : List R) → IsSMulRegular M r → IsRegular (QuotSMulTop r M) rs →
      motive (QuotSMulTop r M) rs → motive M (r :: rs))
    {M} [AddCommGroup M] [Module R M] {rs} : IsRegular M rs → motive M rs :=
  recIterModByRegular (motive := fun M _ _ rs _ => motive M rs) nil cons

/-- An alternate induction principle from `IsRegular.recIterModByRegular` where
we mod out by successive elements in both the module and the base ring. This is
useful for propagating certain properties of the initial `M`, e.g. faithfulness
or freeness, throughout the induction. -/
/-
**RingTheory.Sequence.IsRegular.recIterModByRegularWithRing** 是 Mathlib 中的一个定义，位
于命名空间 `RingTheory.Sequence.IsRegular`。
形式化陈述：recIterModByRegularWithRing {motive : (R : Type u) -> [CommRing R] -> (M :
 Type v) -> [AddCommGroup M] -> [Module R M] -> (rs : List R) -> IsRegular M rs 
-> Sort*} (nil : (R : Type u) -> [CommRing R] -> (M : Type v) -> [AddCommGroup M
] -> [Module R M] -> [Nontrivial M] -> motive R M [] (nil R M)) (cons : {R : Typ
e u} -> [CommRing R] -> {M : Type v} -> [AddCommGroup M] -> [Module R M] -> (r :
 R) -> (rs : List R) -> (h1 : IsSMulRegular M r) -> (h2 : IsRegular (QuotSMulTop
 r M) (rs.map (Ideal.Quoti
参数：R : Type u；M : Type v；rs : List R；nil : (R : Type u) -> [CommRing R] -> (M : 
Type v) -> [AddCommGroup M] -> [Module R M] -> [Nontrivial M] -> motive R M [] (
nil R M)。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用引理 `RingTheory.Sequence.IsRegular.nil`：nil [Nontrivial M] : IsRegular M ([] 
: List R) where toIsWeaklyRegular
· 使用引理 `RingTheory.Sequence.IsRegular.cons'`：cons' {r : R} {rs : List R} (h1 : I
sSMulRegular M r) (h2 : IsRegular (QuotSMulTop r M) (rs.map (Ideal.Quotient.mk (
Ideal.span {r})))) : IsRe…
· 使用定理 `RingTheory.Sequence.IsRegular.toIsWeaklyRegular`：∀ {R : Type u_1} {M : T
ype u_3} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R
 M]   {rs : List R}, RingTheory.Seque…
· 使用定理 `RingTheory.Sequence.IsRegular.top_ne_smul`：∀ {R : Type u_1} {M : Type u_
3} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M]   
{rs : List R}, RingTheory.Seque…

--- 原说明 ---
An alternate induction principle from `IsRegular.recIterModByRegular` where
we mod out by successive elements in both the module and the base ring. This is
useful for propagating certain properties of the initial `M`, e.g. faithfulness
or freeness, throughout the induction.
-/
def recIterModByRegularWithRing
    {motive : (R : Type u) → [CommRing R] → (M : Type v) → [AddCommGroup M] →
      [Module R M] → (rs : List R) → IsRegular M rs → Sort*}
    (nil : (R : Type u) → [CommRing R] → (M : Type v) → [AddCommGroup M] →
      [Module R M] → [Nontrivial M] → motive R M [] (nil R M))
    (cons : {R : Type u} → [CommRing R] → {M : Type v} → [AddCommGroup M] →
      [Module R M] → (r : R) → (rs : List R) → (h1 : IsSMulRegular M r) →
      (h2 : IsRegular (QuotSMulTop r M)
              (rs.map (Ideal.Quotient.mk (Ideal.span {r})))) →
      (ih : motive (R ⧸ Ideal.span {r}) (QuotSMulTop r M)
              (rs.map (Ideal.Quotient.mk (Ideal.span {r}))) h2) →
            motive R M (r :: rs) (cons' h1 h2))
    {R} [CommRing R] {M} [AddCommGroup M] [Module R M] {rs}
    (h : IsRegular M rs) : motive R M rs h :=
  h.toIsWeaklyRegular.recIterModByRegularWithRing
    (motive := fun R _ N _ _ rs' h' => ∀ h'', motive R N rs' ⟨h', h''⟩)
    (fun R _ N _ _ h' =>
      haveI := (nontrivial_iff R).mp (nontrivial_of_ne _ _ h'); nil R N)
    (fun r rs' h1 h2 h3 h4 =>
      have ⟨h5, h6⟩ := (isRegular_cons_iff' _ _ _).mp ⟨h2.cons' h1, h4⟩
      cons r rs' h5 h6 <| h3 h6.top_ne_smul)
    h.top_ne_smul

/-- A simplified version of `IsRegular.recIterModByRegularWithRing` where the
motive is not allowed to depend on the proof of `IsRegular`. -/
/-
**RingTheory.Sequence.IsRegular.ndrecIterModByRegularWithRing** 是 Mathlib 中的一个定义
，位于命名空间 `RingTheory.Sequence.IsRegular`。
形式化陈述：ndrecIterModByRegularWithRing {motive : (R : Type u) -> [CommRing R] -> (M
 : Type v) -> [AddCommGroup M] -> [Module R M] -> (rs : List R) -> Sort*} (nil :
 (R : Type u) -> [CommRing R] -> (M : Type v) -> [AddCommGroup M] -> [Module R M
] -> [Nontrivial M] -> motive R M []) (cons : {R : Type u} -> [CommRing R] -> {M
 : Type v} -> [AddCommGroup M] -> [Module R M] -> (r : R) -> (rs : List R) -> Is
SMulRegular M r -> IsRegular (QuotSMulTop r M) (rs.map (Ideal.Quotient.mk (Ideal
.span {r}))) -> motive (R 
参数：R : Type u；M : Type v；rs : List R；nil : (R : Type u) -> [CommRing R] -> (M : 
Type v) -> [AddCommGroup M] -> [Module R M] -> [Nontrivial M] -> motive R M []。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A simplified version of `IsRegular.recIterModByRegularWithRing` where the
motive is not allowed to depend on the proof of `IsRegular`.
-/
def ndrecIterModByRegularWithRing
    {motive : (R : Type u) → [CommRing R] → (M : Type v) →
      [AddCommGroup M] → [Module R M] → (rs : List R) → Sort*}
    (nil : (R : Type u) → [CommRing R] → (M : Type v) →
      [AddCommGroup M] → [Module R M] → [Nontrivial M] → motive R M [])
    (cons : {R : Type u} → [CommRing R] → {M : Type v} →
      [AddCommGroup M] → [Module R M] → (r : R) → (rs : List R) →
      IsSMulRegular M r →
      IsRegular (QuotSMulTop r M)
        (rs.map (Ideal.Quotient.mk (Ideal.span {r}))) →
      motive (R ⧸ Ideal.span {r}) (QuotSMulTop r M)
        (rs.map (Ideal.Quotient.mk (Ideal.span {r}))) →
      motive R M (r :: rs))
    {R} [CommRing R] {M} [AddCommGroup M] [Module R M] {rs} :
    IsRegular M rs → motive R M rs :=
  recIterModByRegularWithRing (motive := fun R _ M _ _ rs _ => motive R M rs)
    nil cons
/-
**RingTheory.Sequence.IsRegular.quot_ofList_smul_nontrivial** 是 Mathlib 中的一个引理，位
于命名空间 `RingTheory.Sequence.IsRegular`。
形式化陈述：quot_ofList_smul_nontrivial {rs : List R} (h : IsRegular M rs) (N : Submod
ule R M) : Nontrivial (M ⧸ Ideal.ofList rs • N)
参数：h : IsRegular M rs；N : Submodule R M。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Submodule.Quotient.nontrivial_iff`：∀ {R : Type u_1} {M : Type u_2} [inst
 : Ring R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M]   {p : Submodu
le R M}, Nontrivial (M …
· 使用定理 `ne_top_of_le_ne_top`：ne_top_of_le_ne_top (hb : b != ⊤) (hab : a <= b) : 
a != ⊤
· 使用定理 `Ne.symm`：∀ {α : Sort u} {a b : α}, a ≠ b → b ≠ a
· 使用定理 `RingTheory.Sequence.IsRegular.top_ne_smul`：∀ {R : Type u_1} {M : Type u_
3} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M]   
{rs : List R}, RingTheory.Seque…
· 使用定理 `smul_mono_right`：smul_mono_right [SMul M α] [Preorder α] [CovariantClass
 M α HSMul.hSMul LE.le] (m : M) : Monotone (HSMul.hSMul m : α -> α)
· 使用定理 `Submodule.instCovariantClassHSMulLe_1`：∀ {R : Type u} [inst : Semiring R
] {A : Type v} [inst_1 : Semiring A] [inst_2 : _root_.Module R A] {M : Type u_1}
   [inst_3 : AddCommMonoid …
· 使用定理 `le_top`：le_top : a <= ⊤
-/
lemma quot_ofList_smul_nontrivial {rs : List R} (h : IsRegular M rs)
    (N : Submodule R M) : Nontrivial (M ⧸ Ideal.ofList rs • N) :=
  Submodule.Quotient.nontrivial_iff.mpr <|
    ne_top_of_le_ne_top h.top_ne_smul.symm (smul_mono_right _ le_top)
/-
**RingTheory.Sequence.IsRegular.nontrivial** 是 Mathlib 中的一个引理，位于命名空间 `RingTheory
.Sequence.IsRegular`。
形式化陈述：nontrivial {rs : List R} (h : IsRegular M rs) : Nontrivial M
参数：h : IsRegular M rs。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Function.Surjective.nontrivial`：∀ {α : Type u_1} {β : Type u_2} [Nontriv
ial β] {f : α → β}, Function.Surjective f → Nontrivial α
· 使用引理 `RingTheory.Sequence.IsRegular.quot_ofList_smul_nontrivial`：quot_ofList_s
mul_nontrivial {rs : List R} (h : IsRegular M rs) (N : Submodule R M) : Nontrivi
al (M ⧸ Ideal.ofList rs • N)
· 使用定理 `Submodule.mkQ_surjective`：mkQ_surjective : Function.Surjective p.mkQ
-/
lemma nontrivial {rs : List R} (h : IsRegular M rs) : Nontrivial M :=
  haveI := quot_ofList_smul_nontrivial h ⊤
  (mkQ_surjective (Ideal.ofList rs • ⊤ : Submodule R M)).nontrivial

end IsRegular

/-
**RingTheory.Sequence.isRegular_iff_isWeaklyRegular_of_subset_jacobson_annihilat
or** 是 Mathlib 中的一个引理，位于命名空间 `RingTheory.Sequence`。
形式化陈述：isRegular_iff_isWeaklyRegular_of_subset_jacobson_annihilator [Nontrivial M
] [Module.Finite R M] {rs : List R} (h : forall r in rs, r in Ideal.jacobson (Mo
dule.annihilator R M)) : IsRegular M rs ↔ IsWeaklyRegular M rs
参数：h : forall r in rs, r in Ideal.jacobson (Module.annihilator R M)。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `RingTheory.Sequence.isRegular_iff`：∀ {R : Type u_1} (M : Type u_3) [inst
 : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M]   (rs : Li
st R), RingTheory.Seque…
· 使用定理 `and_iff_left`：∀ {b a : Prop}, b → (a ∧ b ↔ a)
· 使用引理 `Submodule.top_ne_ideal_smul_of_le_jacobson_annihilator`：top_ne_ideal_smu
l_of_le_jacobson_annihilator [Nontrivial M] [Module.Finite R M] {I} (h : I <= (M
odule.annihilator R M).jacobson) : (⊤ : Subm…
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Ideal.span_le`：span_le {s : Set α} {I} : span s <= I ↔ s subseteq I
-/
lemma isRegular_iff_isWeaklyRegular_of_subset_jacobson_annihilator
    [Nontrivial M] [Module.Finite R M] {rs : List R}
    (h : ∀ r ∈ rs, r ∈ Ideal.jacobson (Module.annihilator R M)) :
    IsRegular M rs ↔ IsWeaklyRegular M rs :=
  Iff.trans (isRegular_iff M rs) <| and_iff_left <|
    top_ne_ideal_smul_of_le_jacobson_annihilator <| Ideal.span_le.mpr h
/-
**RingTheory.Sequence._root_.IsLocalRing.isRegular_iff_isWeaklyRegular_of_subset
_maximalIdeal** 是 Mathlib 中的一个引理，位于命名空间 `RingTheory.Sequence`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma _root_.IsLocalRing.isRegular_iff_isWeaklyRegular_of_subset_maximalIdeal
    [IsLocalRing R] [Nontrivial M] [Module.Finite R M] {rs : List R}
    (h : ∀ r ∈ rs, r ∈ IsLocalRing.maximalIdeal R) :
    IsRegular M rs ↔ IsWeaklyRegular M rs :=
  have H h' := bot_ne_top.symm <| annihilator_eq_top_iff.mp <|
    Eq.trans annihilator_top h'
  isRegular_iff_isWeaklyRegular_of_subset_jacobson_annihilator fun r hr =>
    IsLocalRing.jacobson_eq_maximalIdeal (Module.annihilator R M) H ▸ h r hr

open IsWeaklyRegular IsArtinian in
/-
**RingTheory.Sequence.eq_nil_of_isRegular_on_artinian** 是 Mathlib 中的一个定理，位于命名空间 
`RingTheory.Sequence`。
形式化陈述：∀ {R : Type u_1} {M : Type u_3} [inst : CommRing R] [inst_1 : AddCommGroup
 M] [inst_2 : _root_.Module R M]   [IsArtinian R M] {rs : List R}, RingTheory.Se
quence.IsRegular M rs → rs = []
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Submodule.map_top`：map_top [RingHomSurjective τ₁₂] (f : M ->ₛₗ[τ₁₂] M₂) 
: map f ⊤ = range f
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `LinearMap.range_eq_top`：range_eq_top [RingHomSurjective τ₁₂] {f : M ->ₛₗ
[τ₁₂] M₂} : range f = ⊤ ↔ Surjective f
· 使用定理 `IsArtinian.surjective_of_injective_endomorphism`：surjective_of_injective
_endomorphism (f : M ->ₗ[R] M) (s : Injective f) : Surjective f
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `RingTheory.Sequence.isWeaklyRegular_cons_iff`：isWeaklyRegular_cons_iff (
r : R) (rs : List R) : IsWeaklyRegular M (r :: rs) ↔ IsSMulRegular M r ∧ IsWeakl
yRegular (QuotSMulTop r M) rs
· 使用定理 `Submodule.ideal_span_singleton_smul`：ideal_span_singleton_smul (r : R) (
N : Submodule R M) : (Ideal.span {r} : Ideal R) • N = r • N
· 使用定理 `Submodule.sup_smul`：sup_smul : (I ⊔ J) • N = I • N ⊔ J • N
· 使用定理 `Ideal.ofList_cons`：∀ {R : Type u_1} [inst : Semiring R] (r : R) (rs : Li
st R), Ideal.ofList (r :: rs) = Ideal.span {r} ⊔ Ideal.ofList rs
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `lt_top_iff_ne_top`：lt_top_iff_ne_top : a < ⊤ ↔ a != ⊤
· 使用定理 `ne_comm`：∀ {α : Sort u_1} {a b : α}, a ≠ b ↔ b ≠ a
· 使用定理 `RingTheory.Sequence.isRegular_iff`：∀ {R : Type u_1} (M : Type u_3) [inst
 : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M]   (rs : Li
st R), RingTheory.Seque…
· 使用引理 `ne_of_lt`：ne_of_lt (h : a < b) : a != b
· 使用引理 `lt_of_le_of_lt`：lt_of_le_of_lt (hab : a <= b) (hbc : b < c) : a < c
· 使用定理 `le_sup_left`：le_sup_left : a <= a ⊔ b
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b
-/
lemma eq_nil_of_isRegular_on_artinian [IsArtinian R M] :
    {rs : List R} → IsRegular M rs → rs = []
  | [], _ => rfl
  | r :: rs, h => by
    rw [isRegular_iff, ne_comm, ← lt_top_iff_ne_top, Ideal.ofList_cons,
      sup_smul, ideal_span_singleton_smul, isWeaklyRegular_cons_iff] at h
    refine absurd ?_ (ne_of_lt (lt_of_le_of_lt le_sup_left h.right))
    exact Eq.trans (Submodule.map_top _) <| LinearMap.range_eq_top.mpr <|
      surjective_of_injective_endomorphism (LinearMap.lsmul R M r) h.left.left
/-
**RingTheory.Sequence.IsWeaklyRegular.isWeaklyRegular_lTensor** 是 Mathlib 中的一个定理
，位于命名空间 `RingTheory.Sequence.IsWeaklyRegular`。
形式化陈述：∀ {R : Type u_1} {M : Type u_3} {M₂ : Type u_4} [inst : CommRing R] [inst_
1 : AddCommGroup M] [inst_2 : AddCommGroup M₂]   [inst_3 : _root_.Module R M] [i
nst_4 : _root_.Module R M₂] [Module.Flat R M₂] {rs : List R},   RingTheory.Seque
nce.IsWeaklyRegular M rs → RingTheory.Sequence.IsWeaklyRegular (TensorProduct R 
M₂ M) rs
参数：TensorProduct R M₂ M。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `RingTheory.Sequence.IsWeaklyRegular.nil`：∀ (R : Type u_1) (M : Type u_3)
 [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M],   R
ingTheory.Sequence.IsWeaklyRe…
· 使用引理 `RingTheory.Sequence.IsWeaklyRegular.cons`：cons {r : R} {rs : List R} (h1
 : IsSMulRegular M r) (h2 : IsWeaklyRegular (QuotSMulTop r M) rs) : IsWeaklyRegu
lar M (r :: rs)
· 使用引理 `IsSMulRegular.lTensor`：IsSMulRegular.lTensor : IsSMulRegular (M otimes[R
] M') r
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `LinearEquiv.isWeaklyRegular_congr`：∀ {R : Type u_1} {M : Type u_3} {M₂ :
 Type u_4} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : AddCommGroup 
M₂]   [inst_3 : _root_.…
-/
lemma IsWeaklyRegular.isWeaklyRegular_lTensor [Module.Flat R M₂]
    {rs : List R} (h : IsWeaklyRegular M rs) :
    IsWeaklyRegular (M₂ ⊗[R] M) rs := by
  induction h with
  | nil N => exact nil R (M₂ ⊗[R] N)
  | @cons N _ _ r rs' h1 _ ih =>
    let e := tensorQuotSMulTopEquivQuotSMulTop r M₂ N
    exact ((e.isWeaklyRegular_congr rs').mp ih).cons (h1.lTensor M₂)
/-
**RingTheory.Sequence.IsWeaklyRegular.isWeaklyRegular_rTensor** 是 Mathlib 中的一个定理
，位于命名空间 `RingTheory.Sequence.IsWeaklyRegular`。
形式化陈述：∀ {R : Type u_1} {M : Type u_3} {M₂ : Type u_4} [inst : CommRing R] [inst_
1 : AddCommGroup M] [inst_2 : AddCommGroup M₂]   [inst_3 : _root_.Module R M] [i
nst_4 : _root_.Module R M₂] [Module.Flat R M₂] {rs : List R},   RingTheory.Seque
nce.IsWeaklyRegular M rs → RingTheory.Sequence.IsWeaklyRegular (TensorProduct R 
M M₂) rs
参数：TensorProduct R M M₂。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `RingTheory.Sequence.IsWeaklyRegular.nil`：∀ (R : Type u_1) (M : Type u_3)
 [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M],   R
ingTheory.Sequence.IsWeaklyRe…
· 使用引理 `RingTheory.Sequence.IsWeaklyRegular.cons`：cons {r : R} {rs : List R} (h1
 : IsSMulRegular M r) (h2 : IsWeaklyRegular (QuotSMulTop r M) rs) : IsWeaklyRegu
lar M (r :: rs)
· 使用引理 `IsSMulRegular.rTensor`：IsSMulRegular.rTensor : IsSMulRegular (M' otimes[
R] M) r
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `LinearEquiv.isWeaklyRegular_congr`：∀ {R : Type u_1} {M : Type u_3} {M₂ :
 Type u_4} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : AddCommGroup 
M₂]   [inst_3 : _root_.…
-/
lemma IsWeaklyRegular.isWeaklyRegular_rTensor [Module.Flat R M₂]
    {rs : List R} (h : IsWeaklyRegular M rs) :
    IsWeaklyRegular (M ⊗[R] M₂) rs := by
  induction h with
  | nil N => exact nil R (N ⊗[R] M₂)
  | @cons N _ _ r rs' h1 _ ih =>
    let e := quotSMulTopTensorEquivQuotSMulTop r M₂ N
    exact ((e.isWeaklyRegular_congr rs').mp ih).cons (h1.rTensor M₂)
-- TODO: apply the above to localization and completion (Corollary 1.1.3 in B&H)
/-
**RingTheory.Sequence.map_first_exact_on_four_term_right_exact_of_isSMulRegular_
last** 是 Mathlib 中的一个引理，位于命名空间 `RingTheory.Sequence`。
形式化陈述：map_first_exact_on_four_term_right_exact_of_isSMulRegular_last {rs : List 
R} {f₁ : M ->ₗ[R] M₂} {f₂ : M₂ ->ₗ[R] M₃} {f₃ : M₃ ->ₗ[R] M₄} (h₁₂ : Exact f₁ f₂
) (h₂₃ : Exact f₂ f₃) (h₃ : Surjective f₃) (h₄ : IsWeaklyRegular M₄ rs) : Exact 
(mapQ _ _ _ (smul_top_le_comap_smul_top (Ideal.ofList rs) f₁)) (mapQ _ _ _ (smul
_top_le_comap_smul_top (Ideal.ofList rs) f₂))
参数：h₁₂ : Exact f₁ f₂；h₂₃ : Exact f₂ f₃；h₃ : Surjective f₃；h₄ : IsWeaklyRegular M
₄ rs。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `Submodule.smul_top_le_comap_smul_top`：smul_top_le_comap_smul_top [AddCom
mMonoid M] [AddCommMonoid N] [Module R M] [Module R N] (I : Ideal R) (f : M ->ₗ[
R] N) : I • ⊤ <= comap f (…
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用引理 `Function.Exact.iff_of_ladder_linearEquiv`：iff_of_ladder_linearEquiv (h₁₂
 : g₁₂ ∘ₗ e₁ = e₂ ∘ₗ f₁₂) (h₂₃ : g₂₃ ∘ₗ e₂ = e₃ ∘ₗ f₂₃) : Exact g₁₂ g₂₃ ↔ Exact 
f₁₂ f₂₃
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.ofList_nil`：∀ {R : Type u_1} [inst : Semiring R], Ideal.ofList [] 
= ⊥
· 使用定理 `Submodule.bot_smul`：bot_smul : (⊥ : Submodule R A) • N = ⊥
· 使用定理 `Submodule.quot_hom_ext`：quot_hom_ext (f g : (M ⧸ p) ->ₗ[R] M₂) (h : fora
ll x : M, f (Quotient.mk x) = g (Quotient.mk x)) : f = g
· 使用引理 `Submodule.quotOfListConsSMulTopEquivQuotSMulTopInner_naturality`：quotOfL
istConsSMulTopEquivQuotSMulTopInner_naturality (f : M ->ₗ[R] M₂) : (quotOfListCo
nsSMulTopEquivQuotSMulTopInner M₂ r rs).toLinearMap ∘…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `QuotSMulTop.map_first_exact_on_four_term_exact_of_isSMulRegular_last`：Qu
otSMulTop.map_first_exact_on_four_term_exact_of_isSMulRegular_last {M'''} [AddCo
mmGroup M'''] [Module R M'''] {r : R} {f₁ : M ->ₗ[R] M'} {…
· 使用引理 `QuotSMulTop.map_exact`：map_exact {f : M ->ₗ[R] M'} {g : M' ->ₗ[R] M''} (
hfg : Exact f g) (hg : Surjective g) : Exact (map r f) (map r g)
· 使用引理 `QuotSMulTop.map_surjective`：map_surjective {f : M ->ₗ[R] M'} (hf : Surje
ctive f) : Surjective (map r f)
-/
lemma map_first_exact_on_four_term_right_exact_of_isSMulRegular_last
    {rs : List R} {f₁ : M →ₗ[R] M₂} {f₂ : M₂ →ₗ[R] M₃} {f₃ : M₃ →ₗ[R] M₄}
    (h₁₂ : Exact f₁ f₂) (h₂₃ : Exact f₂ f₃) (h₃ : Surjective f₃)
    (h₄ : IsWeaklyRegular M₄ rs) :
    Exact (mapQ _ _ _ (smul_top_le_comap_smul_top (Ideal.ofList rs) f₁))
          (mapQ _ _ _ (smul_top_le_comap_smul_top (Ideal.ofList rs) f₂)) := by
  induction h₄ generalizing M M₂ M₃ with
  | nil =>
    apply (Exact.iff_of_ladder_linearEquiv ?_ ?_).mp h₁₂
    any_goals exact quotEquivOfEqBot _ <|
      Eq.trans (congrArg (· • ⊤) Ideal.ofList_nil) (bot_smul ⊤)
    all_goals exact quot_hom_ext _ _ _ fun _ => rfl
  | cons r rs h₄ _ ih =>
    specialize ih
      (map_first_exact_on_four_term_exact_of_isSMulRegular_last h₁₂ h₂₃ h₄)
      (map_exact r h₂₃ h₃) (map_surjective r h₃)
    have H₁ := quotOfListConsSMulTopEquivQuotSMulTopInner_naturality r rs f₁
    have H₂ := quotOfListConsSMulTopEquivQuotSMulTopInner_naturality r rs f₂
    exact (Exact.iff_of_ladder_linearEquiv H₁.symm H₂.symm).mp ih

-- todo: modding out a complex by a regular sequence (prop 1.1.5 in B&H)

section Perm

set_option backward.isDefEq.respectTransparency.types false in
open _root_.LinearMap in
/-
**RingTheory.Sequence.IsWeaklyRegular.swap** 是 Mathlib 中的一个引理，位于命名空间 `RingTheory
.Sequence`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
private lemma IsWeaklyRegular.swap {a b : R} (h1 : IsWeaklyRegular M [a, b])
    (h2 : torsionBy R M b = a • torsionBy R M b → torsionBy R M b = ⊥) :
    IsWeaklyRegular M [b, a] := by
  rw [isWeaklyRegular_cons_iff, isWeaklyRegular_singleton_iff] at h1 ⊢
  obtain ⟨ha, hb⟩ := h1
  rw [← isSMulRegular_iff_torsionBy_eq_bot] at h2
  specialize h2 (le_antisymm ?_ (smul_le_self_of_tower a (torsionBy R M b)))
  · refine le_of_eq_of_le ?_ <| smul_top_inf_eq_smul_of_isSMulRegular_on_quot <|
      ha.of_injective _ <| ker_eq_bot.mp <| ker_liftQ_eq_bot' _ (lsmul R M b) rfl
    rw [← (isSMulRegular_on_quot_iff_lsmul_comap_eq _ _).mp hb]
    exact (inf_eq_right.mpr (ker_le_comap _)).symm
  · rwa [ha.isSMulRegular_on_quot_iff_smul_top_inf_eq_smul, inf_comm, smul_comm,
      ← h2.isSMulRegular_on_quot_iff_smul_top_inf_eq_smul, and_iff_left hb]

-- TODO: Equivalence of permutability of regular sequences to regularity of
-- subsequences and regularity on poly ring. See [07DW] in stacks project
-- We need a theory of multivariate polynomial modules first
/-
**RingTheory.Sequence.IsWeaklyRegular.prototype_perm** 是 Mathlib 中的一个定理，位于命名空间 `
RingTheory.Sequence.IsWeaklyRegular`。
形式化陈述：∀ {R : Type u_1} {M : Type u_3} [inst : CommRing R] [inst_1 : AddCommGroup
 M] [inst_2 : _root_.Module R M]   {rs : List R},   RingTheory.Sequence.IsWeakly
Regular M rs →     ∀ {rs' : List R},       rs.Perm rs' →         (∀ (a b : R) (r
s' : List R),             (a :: b :: rs').Subperm rs →               have K := S
ubmodule.torsionBy R (M ⧸ Ideal.ofList rs' • ⊤) b;               K = a • K → K =
 ⊥) →           RingTheory.Sequence.IsWeaklyRegular M rs'
参数：∀ (a b : R) (rs' : List R),             (a :: b :: rs').Subperm rs →         
      have K := Submodule.torsionBy R (M ⧸ Ideal.ofList rs' • ⊤) b;             
  K = a • K → K = ⊥。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `LinearEquiv.isWeaklyRegular_congr`：∀ {R : Type u_1} {M : Type u_3} {M₂ :
 Type u_4} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : AddCommGroup 
M₂]   [inst_3 : _root_.…
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.ofList_nil`：∀ {R : Type u_1} [inst : Semiring R], Ideal.ofList [] 
= ⊥
· 使用定理 `Submodule.bot_smul`：bot_smul : (⊥ : Submodule R A) • N = ⊥
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `_private.Mathlib.RingTheory.Regular.RegularSequence.0.RingTheory.Sequenc
e.IsWeaklyRegular.prototype_perm.aux`：∀ {R : Type u_1} {M : Type u_3} [inst : Co
mmRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M]   {rs : List R}
,   (∀ (a b : R) (…
· 使用定理 `List.Subperm.refl`：∀ {α : Type u_1} (l : List α), l.Subperm l
· 使用定理 `List.Perm.subperm`：∀ {α : Type u_1} {l₁ l₂ : List α}, l₁.Perm l₂ → l₁.Su
bperm l₂
· 使用定理 `List.Perm.symm`：∀ {α : Type u_1} {l₁ l₂ : List α}, l₁.Perm l₂ → l₂.Perm 
l₁
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
-/
lemma IsWeaklyRegular.prototype_perm {rs : List R} (h : IsWeaklyRegular M rs)
    {rs'} (h'' : rs ~ rs') (h' : ∀ a b rs', (a :: b :: rs') <+~ rs →
      let K := torsionBy R (M ⧸ (Ideal.ofList rs' • ⊤ : Submodule R M)) b
      K = a • K → K = ⊥) : IsWeaklyRegular M rs' :=
  have H := LinearEquiv.isWeaklyRegular_congr <| quotEquivOfEqBot _ <|
    Eq.trans (congrArg (· • ⊤) Ideal.ofList_nil) (bot_smul ⊤)
  (H rs').mp <| (aux [] h'' (.refl rs) (h''.symm.subperm)) <| (H rs).mpr h
  where aux {rs₁ rs₂} (rs₀ : List R)
    (h₁₂ : rs₁ ~ rs₂) (H₁ : rs₀ ++ rs₁ <+~ rs) (H₃ : rs₀ ++ rs₂ <+~ rs)
    (h : IsWeaklyRegular (M ⧸ (Ideal.ofList rs₀ • ⊤ : Submodule R M)) rs₁) :
    IsWeaklyRegular (M ⧸ (Ideal.ofList rs₀ • ⊤ : Submodule R M)) rs₂ := by {
  induction h₁₂ generalizing rs₀ with
  | nil => exact .nil R _
  | cons r _ ih =>
    let e := quotOfListConsSMulTopEquivQuotSMulTopOuter M r rs₀
    rw [isWeaklyRegular_cons_iff, ← e.isWeaklyRegular_congr] at h ⊢
    refine h.imp_right (ih (r :: rs₀) ?_ ?_) <;>
      exact List.perm_middle.subperm_right.mp ‹_›
  | swap a b t =>
    rw [show ∀ x y z, x :: y :: z = [x, y] ++ z from fun _ _ _ => rfl,
      isWeaklyRegular_append_iff] at h ⊢
    have : Ideal.ofList [b, a] = Ideal.ofList [a, b] :=
      congrArg Ideal.span <| Set.ext fun _ => (List.Perm.swap a b []).mem_iff
    rw [(quotEquivOfEq _ _ (congrArg₂ _ this rfl)).isWeaklyRegular_congr] at h
    rw [List.append_cons, List.append_cons, List.append_assoc _ [b] [a]] at H₁
    apply (List.sublist_append_left (rs₀ ++ [b, a]) _).subperm.trans at H₁
    apply List.perm_append_comm.subperm.trans at H₁
    exact h.imp_left (swap · (h' b a rs₀ H₁))
  | trans h₁₂ _ ih₁₂ ih₂₃ =>
    have H₂ := (h₁₂.append_left rs₀).subperm_right.mp H₁
    exact ih₂₃ rs₀ H₂ H₃ (ih₁₂ rs₀ H₁ H₂ h) }

-- putting `{rs' : List R}` and `h2` after `h3` would be better for partial
-- application, but this argument order seems nicer overall
/-
**RingTheory.Sequence.IsWeaklyRegular.of_perm_of_subset_jacobson_annihilator** 是
 Mathlib 中的一个定理，位于命名空间 `RingTheory.Sequence.IsWeaklyRegular`。
形式化陈述：∀ {R : Type u_1} {M : Type u_3} [inst : CommRing R] [inst_1 : AddCommGroup
 M] [inst_2 : _root_.Module R M]   [IsNoetherian R M] {rs rs' : List R},   RingT
heory.Sequence.IsWeaklyRegular M rs →     rs.Perm rs' → (∀ r ∈ rs, r ∈ (Module.a
nnihilator R M).jacobson) → RingTheory.Sequence.IsWeaklyRegular M rs'
参数：∀ r ∈ rs, r ∈ (Module.annihilator R M).jacobson。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `RingTheory.Sequence.IsWeaklyRegular.prototype_perm`：∀ {R : Type u_1} {M 
: Type u_3} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Modul
e R M]   {rs : List R},   RingTheory.Seq…
· 使用引理 `Submodule.eq_bot_of_eq_pointwise_smul_of_mem_jacobson_annihilator`：eq_bo
t_of_eq_pointwise_smul_of_mem_jacobson_annihilator {r : R} {N : Submodule R M} (
hN : FG N) (hrN : N = r • N) (hrJac : r in N.annihilato…
· 使用定理 `IsNoetherian.noetherian`：∀ {R : Type u_1} {M : Type u_2} {inst : Semirin
g R} {inst_1 : AddCommMonoid M} {inst_2 : _root_.Module R M}   [self : IsNoether
ian R M] (s :…
· 使用定理 `Ideal.jacobson_mono`：jacobson_mono {I J : Ideal R} : I <= J -> I.jacobso
n <= J.jacobson
· 使用引理 `le_trans`：le_trans : a <= b -> b <= c -> a <= c
· 使用定理 `LinearMap.annihilator_le_of_surjective`：LinearMap.annihilator_le_of_surj
ective (f : M ->ₗ[R] M') (hf : Function.Surjective f) : Module.annihilator R M <
= Module.annihilator R M'
· 使用定理 `Submodule.mkQ_surjective`：mkQ_surjective : Function.Surjective p.mkQ
· 使用定理 `LinearMap.annihilator_le_of_injective`：LinearMap.annihilator_le_of_injec
tive (f : M ->ₗ[R] M') (hf : Function.Injective f) : Module.annihilator R M' <= 
Module.annihilator R M
· 使用定理 `Submodule.injective_subtype`：injective_subtype : Injective p.subtype
· 使用定理 `List.Subperm.subset`：∀ {α : Type u_1} {l₁ l₂ : List α}, l₁.Subperm l₂ → 
l₁ ⊆ l₂
· 使用定理 `List.mem_cons_self`：∀ {α : Type u_1} {a : α} {l : List α}, a ∈ a :: l
-/
lemma IsWeaklyRegular.of_perm_of_subset_jacobson_annihilator [IsNoetherian R M]
    {rs rs' : List R} (h1 : IsWeaklyRegular M rs) (h2 : List.Perm rs rs')
    (h3 : ∀ r ∈ rs, r ∈ (Module.annihilator R M).jacobson) :
    IsWeaklyRegular M rs' :=
  h1.prototype_perm h2 fun r _ _ h h' =>
    eq_bot_of_eq_pointwise_smul_of_mem_jacobson_annihilator
      (IsNoetherian.noetherian _) h'
      (Ideal.jacobson_mono
        (le_trans
          -- The named argument `(R := R)` below isn't necessary, but
          -- typechecking is much slower without it
          (LinearMap.annihilator_le_of_surjective (R := R) _ (mkQ_surjective _))
          (LinearMap.annihilator_le_of_injective _ (injective_subtype _)))
        (h3 r (h.subset List.mem_cons_self)))

end Perm

/-
**RingTheory.Sequence.IsRegular.of_perm_of_subset_jacobson_annihilator** 是 Mathl
ib 中的一个定理，位于命名空间 `RingTheory.Sequence.IsRegular`。
形式化陈述：∀ {R : Type u_1} {M : Type u_3} [inst : CommRing R] [inst_1 : AddCommGroup
 M] [inst_2 : _root_.Module R M]   [IsNoetherian R M] {rs rs' : List R},   RingT
heory.Sequence.IsRegular M rs →     rs.Perm rs' → (∀ r ∈ rs, r ∈ (Module.annihil
ator R M).jacobson) → RingTheory.Sequence.IsRegular M rs'
参数：∀ r ∈ rs, r ∈ (Module.annihilator R M).jacobson。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `RingTheory.Sequence.IsWeaklyRegular.of_perm_of_subset_jacobson_annihilat
or`：∀ {R : Type u_1} {M : Type u_3} [inst : CommRing R] [inst_1 : AddCommGroup M
] [inst_2 : _root_.Module R M]   [IsNoetherian R M] {rs rs' : Li…
· 使用定理 `RingTheory.Sequence.IsRegular.toIsWeaklyRegular`：∀ {R : Type u_1} {M : T
ype u_3} [inst : CommRing R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R
 M]   {rs : List R}, RingTheory.Seque…
· 使用引理 `Submodule.top_ne_ideal_smul_of_le_jacobson_annihilator`：top_ne_ideal_smu
l_of_le_jacobson_annihilator [Nontrivial M] [Module.Finite R M] {I} (h : I <= (M
odule.annihilator R M).jacobson) : (⊤ : Subm…
· 使用引理 `RingTheory.Sequence.IsRegular.nontrivial`：nontrivial {rs : List R} (h : 
IsRegular M rs) : Nontrivial M
· 使用定理 `Module.IsNoetherian.finite`：∀ (R : Type u_1) (M : Type u_3) [inst : Semi
ring R] [inst_1 : AddCommMonoid M] [inst_2 : _root_.Module R M]   [IsNoetherian 
R M], Module.Fin…
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Ideal.span_le`：span_le {s : Set α} {I} : span s <= I ↔ s subseteq I
· 使用定理 `List.Perm.mem_iff`：∀ {α : Type u_1} {a : α} {l₁ l₂ : List α}, l₁.Perm l₂
 → (a ∈ l₁ ↔ a ∈ l₂)
-/
lemma IsRegular.of_perm_of_subset_jacobson_annihilator [IsNoetherian R M]
    {rs rs' : List R} (h1 : IsRegular M rs) (h2 : List.Perm rs rs')
    (h3 : ∀ r ∈ rs, r ∈ (Module.annihilator R M).jacobson) : IsRegular M rs' :=
  ⟨h1.toIsWeaklyRegular.of_perm_of_subset_jacobson_annihilator h2 h3,
    letI := h1.nontrivial
    top_ne_ideal_smul_of_le_jacobson_annihilator <|
      Ideal.span_le.mpr (h3 · <| h2.mem_iff.mpr ·)⟩
/-
**RingTheory.Sequence._root_.IsLocalRing.isWeaklyRegular_of_perm_of_subset_maxim
alIdeal** 是 Mathlib 中的一个引理，位于命名空间 `RingTheory.Sequence`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma _root_.IsLocalRing.isWeaklyRegular_of_perm_of_subset_maximalIdeal
    [IsLocalRing R] [IsNoetherian R M] {rs rs' : List R}
    (h1 : IsWeaklyRegular M rs) (h2 : List.Perm rs rs')
    (h3 : ∀ r ∈ rs, r ∈ IsLocalRing.maximalIdeal R) : IsWeaklyRegular M rs' :=
  IsWeaklyRegular.of_perm_of_subset_jacobson_annihilator h1 h2 fun r hr =>
    IsLocalRing.maximalIdeal_le_jacobson _ (h3 r hr)
/-
**RingTheory.Sequence._root_.IsLocalRing.isRegular_of_perm** 是 Mathlib 中的一个引理，位于
命名空间 `RingTheory.Sequence`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma _root_.IsLocalRing.isRegular_of_perm [IsLocalRing R] [IsNoetherian R M]
    {rs rs' : List R} (h1 : IsRegular M rs) (h2 : List.Perm rs rs') :
    IsRegular M rs' := by
  obtain ⟨h3, h4⟩ := h1
  refine ⟨IsLocalRing.isWeaklyRegular_of_perm_of_subset_maximalIdeal h3 h2 ?_, ?_⟩
  · intro x (h6 : x ∈ { r | r ∈ rs })
    refine IsLocalRing.le_maximalIdeal ?_ (Ideal.subset_span h6)
    exact h4 ∘ Eq.trans (top_smul _).symm ∘ Eq.symm ∘ congrArg (· • ⊤)
  · refine ne_of_ne_of_eq h4 (congrArg (Ideal.span · • ⊤) ?_)
    exact Set.ext fun _ => h2.mem_iff

end RingTheory.Sequence

section IsLocalRing

variable {R : Type*} [CommRing R] [IsLocalRing R]
variable (L : Type*) [AddCommGroup L] [Module R L] [Module.Finite R L] [Nontrivial L]

open IsLocalRing

/-
**nontrivial_quotSMulTop_of_mem_maximalIdeal** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：nontrivial_quotSMulTop_of_mem_maximalIdeal {x : R} (mem : x in maximalIdea
l R) : Nontrivial (QuotSMulTop x L)
参数：mem : x in maximalIdeal R。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Submodule.Quotient.nontrivial_iff`：∀ {R : Type u_1} {M : Type u_2} [inst
 : Ring R] [inst_1 : AddCommGroup M] [inst_2 : _root_.Module R M]   {p : Submodu
le R M}, Nontrivial (M …
· 使用定理 `Ne.symm`：∀ {α : Sort u} {a b : α}, a ≠ b → b ≠ a
· 使用引理 `Submodule.top_ne_pointwise_smul_of_mem_jacobson_annihilator`：top_ne_poin
twise_smul_of_mem_jacobson_annihilator [Nontrivial M] [Module.Finite R M] {r} (h
 : r in (Module.annihilator R M).jacobson) : (⊤ :…
· 使用定理 `IsLocalRing.maximalIdeal_le_jacobson`：maximalIdeal_le_jacobson (I : Idea
l R) : IsLocalRing.maximalIdeal R <= I.jacobson
-/
lemma nontrivial_quotSMulTop_of_mem_maximalIdeal {x : R} (mem : x ∈ maximalIdeal R) :
    Nontrivial (QuotSMulTop x L) := by
  apply Submodule.Quotient.nontrivial_iff.mpr (Ne.symm _)
  exact Submodule.top_ne_pointwise_smul_of_mem_jacobson_annihilator (maximalIdeal_le_jacobson _ mem)
/-
**RingTheory.Sequence.IsRegular.of_isWeaklyRegular_of_mem_maximalIdeal** 是 Mathl
ib 中的一个引理，位于命名空间 ``。
形式化陈述：RingTheory.Sequence.IsRegular.of_isWeaklyRegular_of_mem_maximalIdeal {rs :
 List R} (mem : forall r in rs, r in maximalIdeal R) (reg : IsWeaklyRegular L rs
) : IsRegular L rs
参数：mem : forall r in rs, r in maximalIdeal R；reg : IsWeaklyRegular L rs。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `Submodule.top_ne_ideal_smul_of_le_jacobson_annihilator`：top_ne_ideal_smu
l_of_le_jacobson_annihilator [Nontrivial M] [Module.Finite R M] {I} (h : I <= (M
odule.annihilator R M).jacobson) : (⊤ : Subm…
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Ideal.span_le`：span_le {s : Set α} {I} : span s <= I ↔ s subseteq I
· 使用定理 `IsLocalRing.maximalIdeal_le_jacobson`：maximalIdeal_le_jacobson (I : Idea
l R) : IsLocalRing.maximalIdeal R <= I.jacobson
-/
lemma RingTheory.Sequence.IsRegular.of_isWeaklyRegular_of_mem_maximalIdeal {rs : List R}
    (mem : ∀ r ∈ rs, r ∈ maximalIdeal R) (reg : IsWeaklyRegular L rs) :
    IsRegular L rs :=
  ⟨reg, Submodule.top_ne_ideal_smul_of_le_jacobson_annihilator
    ((Ideal.span_le.mpr mem).trans (maximalIdeal_le_jacobson _))⟩

end IsLocalRing

