/-
Copyright (c) 2025 Jiedong Jiang, Christian Merten. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Johan Commelin, Jiedong Jiang, Christian Merten
-/
module

public import Mathlib.Topology.Spectral.Basic
public import Mathlib.Topology.WithTopology
public import Mathlib.Topology.JacobsonSpace
public import Mathlib.Data.Set.Card

/-!
# Constructible topology

In this file we define the constructible topology on a topological space. This is the topology
generated by compact open subsets and their complements.

## Main definitions and results

- `WithConstructibleTopology`: `X` equipped with its constructible topology.
- `compactSpace_withConstructibleTopology`: If `X` is quasi-separated, quasi-sober,
  prespectral and quasi-compact, then `X` is still quasi-compact in the constructible topology.

## TODOs

- If `X` is a spectral space, show that `X` is T2 and totally disconnected (@chrisflav).
-/

@[expose] public section

variable {X : Type*} [TopologicalSpace X]

/-- The subbasis of the constructible topology on a topological space `X`: It consists
of the open and compact sets of `X` and their complements. -/
/-
**constructibleTopologySubbasis** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：constructibleTopologySubbasis (X : Type*) [TopologicalSpace X] : Set (Set 
X)
参数：X : Type*。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The subbasis of the constructible topology on a topological space `X`: It consis
ts
of the open and compact sets of `X` and their complements.
-/
def constructibleTopologySubbasis (X : Type*) [TopologicalSpace X] : Set (Set X) :=
  { s | IsOpen s ∧ IsCompact s } ∪ { s | IsClosed s ∧ IsCompact sᶜ }

/-- The constructible topology on a topological space `X` has as a subbasis
the open and compact sets of `X` and their complements. -/
@[instance_reducible]
/-
**constructibleTopology** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：constructibleTopology (X : Type*) [TopologicalSpace X] : TopologicalSpace 
X
参数：X : Type*。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The constructible topology on a topological space `X` has as a subbasis
the open and compact sets of `X` and their complements.
-/
def constructibleTopology (X : Type*) [TopologicalSpace X] : TopologicalSpace X :=
  .generateFrom (constructibleTopologySubbasis X)

/-- A type synonym for `X` that is equipped with the constructible topology of `X`. -/
@[nolint unusedArguments]
/-
**WithConstructibleTopology** 是 Mathlib 中的一个缩写定义，位于命名空间 ``。
形式化陈述：WithConstructibleTopology (X : Type*) [TopologicalSpace X] : Type _
参数：X : Type*。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A type synonym for `X` that is equipped with the constructible topology of `X`.
-/
abbrev WithConstructibleTopology (X : Type*) [TopologicalSpace X] : Type _ :=
  WithTopology X (constructibleTopology X)

open Topology
/-
**WithConstructibleTopology.isOpen_iff** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：WithConstructibleTopology.isOpen_iff {s : Set (WithConstructibleTopology X
)} : IsOpen s ↔ IsOpen[constructibleTopology X] (WithTopology.toTopology _ ⁻¹' s
)
参数：WithConstructibleTopology X。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `WithTopology.isOpen_iff`：isOpen_iff {s : Set (WithTopology X t)} : IsOpe
n s ↔ IsOpen[t] (toTopology t ⁻¹' s)
-/
lemma WithConstructibleTopology.isOpen_iff {s : Set (WithConstructibleTopology X)} :
    IsOpen s ↔ IsOpen[constructibleTopology X] (WithTopology.toTopology _ ⁻¹' s) :=
  WithTopology.isOpen_iff _
/-
**WithConstructibleTopology.isClosed_iff** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：WithConstructibleTopology.isClosed_iff {s : Set (WithConstructibleTopology
 X)} : IsClosed s ↔ IsClosed[constructibleTopology X] (WithTopology.toTopology _
 ⁻¹' s)
参数：WithConstructibleTopology X。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `WithTopology.isClosed_iff`：isClosed_iff {s : Set (WithTopology X t)} : I
sClosed s ↔ IsClosed[t] (toTopology t ⁻¹' s)
-/
lemma WithConstructibleTopology.isClosed_iff {s : Set (WithConstructibleTopology X)} :
    IsClosed s ↔ IsClosed[constructibleTopology X] (WithTopology.toTopology _ ⁻¹' s) :=
  WithTopology.isClosed_iff _
/-
**IsCompact.isOpen_constructibleTopology_of_isOpen** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：IsCompact.isOpen_constructibleTopology_of_isOpen {s : Set X} (hs : IsCompa
ct s) (ho : IsOpen s) : IsOpen[constructibleTopology X] s
参数：hs : IsCompact s；ho : IsOpen s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `TopologicalSpace.isOpen_generateFrom_of_mem`：isOpen_generateFrom_of_mem 
{g : Set (Set α)} {s : Set α} (hs : s in g) : IsOpen[generateFrom g] s
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `and_self`：∀ (p : Prop), (p ∧ p) = p
· 使用定理 `true_or`：∀ (p : Prop), (True ∨ p) = True
-/
lemma IsCompact.isOpen_constructibleTopology_of_isOpen {s : Set X}
    (hs : IsCompact s) (ho : IsOpen s) : IsOpen[constructibleTopology X] s := by
  apply TopologicalSpace.isOpen_generateFrom_of_mem
  simp [constructibleTopologySubbasis, ho, hs]
/-
**IsCompact.isOpen_constructibleTopology_of_isClosed** 是 Mathlib 中的一个引理，位于命名空间 `
`。
形式化陈述：IsCompact.isOpen_constructibleTopology_of_isClosed {s : Set X} (hs : IsCom
pact sᶜ) (ho : IsClosed s) : IsOpen[constructibleTopology X] s
参数：hs : IsCompact sᶜ；ho : IsClosed s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `TopologicalSpace.isOpen_generateFrom_of_mem`：isOpen_generateFrom_of_mem 
{g : Set (Set α)} {s : Set α} (hs : s in g) : IsOpen[generateFrom g] s
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `and_self`：∀ (p : Prop), (p ∧ p) = p
· 使用定理 `or_true`：∀ (p : Prop), (p ∨ True) = True
-/
lemma IsCompact.isOpen_constructibleTopology_of_isClosed {s : Set X}
    (hs : IsCompact sᶜ) (ho : IsClosed s) : IsOpen[constructibleTopology X] s := by
  apply TopologicalSpace.isOpen_generateFrom_of_mem
  simp [constructibleTopologySubbasis, ho, hs]

@[simp]
/-
**compl_mem_constructibleTopologySubbasis_iff** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：compl_mem_constructibleTopologySubbasis_iff {s : Set X} : sᶜ in constructi
bleTopologySubbasis X ↔ s in constructibleTopologySubbasis X
该定理/引理刻画了左右两侧的等价关系。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma compl_mem_constructibleTopologySubbasis_iff {s : Set X} :
    sᶜ ∈ constructibleTopologySubbasis X ↔ s ∈ constructibleTopologySubbasis X := by
  grind [constructibleTopologySubbasis, isClosed_compl_iff, compl_compl]
/-
**isCompact_of_mem_constructibleTopologySubbasis** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：isCompact_of_mem_constructibleTopologySubbasis [CompactSpace X] {s : Set X
} (hs : s in constructibleTopologySubbasis X) : IsCompact s
参数：hs : s in constructibleTopologySubbasis X。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b
· 使用定理 `IsClosed.isCompact`：IsClosed.isCompact [CompactSpace X] (h : IsClosed s)
 : IsCompact s
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
-/
lemma isCompact_of_mem_constructibleTopologySubbasis [CompactSpace X] {s : Set X}
    (hs : s ∈ constructibleTopologySubbasis X) : IsCompact s := by
  obtain (hs | hs) := hs
  · exact hs.2
  · exact hs.1.isCompact
/-
**isCompact_sInter_of_subset_constructibleTopologySubbasis** 是 Mathlib 中的一个引理，位于
命名空间 ``。
形式化陈述：isCompact_sInter_of_subset_constructibleTopologySubbasis [CompactSpace X] 
[QuasiSeparatedSpace X] {s : Set (Set X)} (hs : s subseteq constructibleTopology
Subbasis X) (hf : s.Finite) : IsCompact (⋂₀ s)
参数：Set X；hs : s subseteq constructibleTopologySubbasis X；hf : s.Finite。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `QuasiSeparatedSpace.isCompact_sInter`：QuasiSeparatedSpace.isCompact_sInt
er [CompactSpace α] {s : Set (Set α)} (hf : s.Finite) (ho : forall t in s, IsOpe
n t ∨ IsClosed t) (hc : fo…
· 使用定理 `Or.imp`：∀ {a c b d : Prop}, (a → c) → (b → d) → a ∨ b → c ∨ d
· 使用引理 `isCompact_of_mem_constructibleTopologySubbasis`：isCompact_of_mem_constru
ctibleTopologySubbasis [CompactSpace X] {s : Set X} (hs : s in constructibleTopo
logySubbasis X) : IsCompact s
-/
lemma isCompact_sInter_of_subset_constructibleTopologySubbasis [CompactSpace X]
    [QuasiSeparatedSpace X] {s : Set (Set X)} (hs : s ⊆ constructibleTopologySubbasis X)
    (hf : s.Finite) : IsCompact (⋂₀ s) := by
  refine QuasiSeparatedSpace.isCompact_sInter hf (fun t ht ↦ ?_) (fun t ht ↦ ?_)
  · apply (hs ht).imp <;> grind
  · exact isCompact_of_mem_constructibleTopologySubbasis (hs ht)

/-- If `X` is quasi-separated, quasi-sober, prespectral and quasi-compact, then `X`
is still quasi-compact in the constructible topology. This holds in particular for spectral
spaces. -/
@[stacks 0901 "quasi-compactness"]
/-
**compactSpace_withConstructibleTopology** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：compactSpace_withConstructibleTopology [CompactSpace X] [QuasiSober X] [Pr
espectralSpace X] [QuasiSeparatedSpace X] : CompactSpace (WithConstructibleTopol
ogy X)
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用引理 `compactSpace_generateFrom_of_compl_mem`：compactSpace_generateFrom_of_com
pl_mem [T : TopologicalSpace X] (𝔅 : Set (Set X)) (hT : T = TopologicalSpace.gen
erateFrom 𝔅) (h𝔅 : forall s …
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
· 使用定理 `Decidable.byContradiction`：∀ {p : Prop} [dec : Decidable p], (¬p → False
) → p
· 使用定理 `zorn_subset_nonempty`：zorn_subset_nonempty (S : Set (Set α)) (H : forall
 c subseteq S, IsChain (· subseteq ·) c -> c.Nonempty -> exists ub in S, forall 
s in c, s …
· 使用定理 `DirectedOn.exists_mem_subset_of_finite_of_subset_sUnion`：DirectedOn.exis
ts_mem_subset_of_finite_of_subset_sUnion {α : Type*} {c : Set (Set α)} (hn : c.N
onempty) (hc : DirectedOn (· subseteq ·) c) {…
· 使用定理 `IsChain.directedOn`：IsChain.directedOn (H : IsChain r s) : DirectedOn r 
s
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b
· 使用定理 `Set.subset_sUnion_of_subset`：subset_sUnion_of_subset {s : Set α} (t : Se
t (Set α)) (u : Set α) (h₁ : s subseteq u) (h₂ : u in t) : s subseteq ⋃₀ t
· 使用定理 `subset_rfl`：∀ {α : Type u_1} [UsesSetNotationForOrder α] [inst : Preorde
r α] {a : α}, a ⊆ a
· 使用定理 `isClosed_sInter`：isClosed_sInter {s : Set (Set X)} : (forall t in s, IsC
losed t) -> IsClosed (⋂₀ s)
· 使用引理 `CompactSpace.nonempty_sInter`：CompactSpace.nonempty_sInter [CompactSpace
 X] {s : Set (Set X)} (hsc : forall t in s, IsClosed t) (hs : forall t subseteq 
s, t.Finite -> (⋂₀…
· 使用定理 `Maximal.prop`：∀ {α : Type u_1} [inst : LE α] {P : α → Prop} {x : α}, Max
imal P x → P x
· 使用定理 `subset_trans`：∀ {α : Type u_1} [UsesSetNotationForOrder α] [inst : Preor
der α] {a b c : α}, a ⊆ b → b ⊆ c → a ⊆ c
· 使用定理 `IsIrreducible.isGenericPoint_genericPoint`：IsIrreducible.isGenericPoint_
genericPoint [QuasiSober α] {S : Set α} (hS : IsIrreducible S) (hS' : IsClosed S
) : IsGenericPoint hS.genericPo…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Set.mem_sInter`：mem_sInter {x : α} {S : Set (Set α)} : x in ⋂₀ S ↔ foral
l t in S, x in t
· 使用定理 `Set.sInter_subset_of_mem`：sInter_subset_of_mem {S : Set (Set α)} {t : Se
t α} (tS : t in S) : ⋂₀ S subseteq t
· 使用定理 `IsGenericPoint.mem`：∀ {α : Type u_1} [inst : TopologicalSpace α] {x : α}
 {S : Set α}, IsGenericPoint x S → x ∈ S
· 使用定理 `IsGenericPoint.mem_open_set_iff`：mem_open_set_iff (h : IsGenericPoint x 
S) (hU : IsOpen U) : x in U ↔ (S inter U).Nonempty
· 使用定理 `Set.inter_comm`：inter_comm (a b : Set α) : a inter b = b inter a
· 使用引理 `IsCompact.nonempty_inter_sInter`：IsCompact.nonempty_inter_sInter (hs : I
sCompact s) {t : Set (Set X)} (ht : forall a in t, IsClosed a) (h : forall a sub
seteq t, a.Finite -> …
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Set.sInter_insert`：sInter_insert (s : Set α) (T : Set (Set α)) : ⋂₀ inse
rt s T = s inter ⋂₀ T
· 使用引理 `PrespectralSpace.exists_isClosed_of_not_isPreirreducible`：PrespectralSpa
ce.exists_isClosed_of_not_isPreirreducible [PrespectralSpace X] (Z : Set X) (hZ 
: ¬ IsPreirreducible Z) : exists (A B : Set X)…
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
（共 49 条，此处仅展示前 30 条）

--- 原说明 ---
If `X` is quasi-separated, quasi-sober, prespectral and quasi-compact, then `X`
is still quasi-compact in the constructible topology. This holds in particular f
or spectral
spaces.
-/
instance compactSpace_withConstructibleTopology [CompactSpace X] [QuasiSober X]
    [PrespectralSpace X] [QuasiSeparatedSpace X] :
    CompactSpace (WithConstructibleTopology X) := by
  suffices h : @CompactSpace X (constructibleTopology X) from
    @Function.Surjective.compactSpace _ _ (constructibleTopology X) _ _
      (WithTopology.continuous_toTopology _) _ (WithTopology.toTopology_surjective _)
  let 𝔅 := constructibleTopologySubbasis X
  /- It suffices to check that any subset of `𝔅` for which every finite subset has non-empty
  intersection, has non-empty intersection. We argue by contradiction and by Zorn's
  lemma, we may take a maximal counterexample `B`. -/
  apply compactSpace_generateFrom_of_compl_mem (T := constructibleTopology X) 𝔅 rfl (by simp [𝔅])
  let 𝒮 := {P : Set (Set X) | P ⊆ 𝔅 ∧ (∀ Q ⊆ P, Q.Finite → (⋂₀ Q).Nonempty) ∧ (⋂₀ P) = ∅}
  suffices 𝒮 = ∅ by contrapose! this; tauto
  by_contra! h𝒮
  obtain ⟨s, hs⟩ := h𝒮
  obtain ⟨B, hsB, hB⟩ := by
    refine zorn_subset_nonempty 𝒮 (fun c hc𝒮 hc hne@⟨x, hxc⟩ ↦ ?_) s hs
    refine ⟨⋃₀ c, ?_, fun S hSc ↦ Set.subset_sUnion_of_subset c S subset_rfl hSc⟩
    refine ⟨by grind, fun a hac ha ↦ ?_, by grind⟩
    obtain ⟨i, hmem, hsub⟩ := hc.directedOn.exists_mem_subset_of_finite_of_subset_sUnion hne ha hac
    exact (hc𝒮 hmem).2.1 a hsub ha
  let B' := {s | s ∈ B ∧ IsClosed s}
  /- Let `Z` be the intersection of all closed sets in `B`. We argue
  by cases if `Z` is irreducible or not. -/
  let Z := ⋂₀ B'
  have hZ_closed : IsClosed Z := isClosed_sInter fun b hb ↦ hb.2
  have hZ_nonempty : Z.Nonempty :=
    CompactSpace.nonempty_sInter (fun _ ht ↦ ht.2) fun t ht hf ↦
      hB.prop.2.1 _ (subset_trans ht (by simp [B'])) hf
  by_cases hZ_irred : IsIrreducible Z
  · -- If `Z` is irreducible, the intersection `⋂₀ B` contains the generic point of `Z`.
    suffices (⋂₀ B).Nonempty by simp_all [hB.prop.2.2]
    have hη := hZ_irred.isGenericPoint_genericPoint (isClosed_sInter fun b hb ↦ hb.2)
    refine ⟨hZ_irred.genericPoint, ?_⟩
    rw [Set.mem_sInter]
    intro i hi
    by_cases hiB' : i ∈ B'
    · exact Set.sInter_subset_of_mem hiB' hη.mem
    · have hi_cmpt : IsCompact i := by grind [hB.prop, constructibleTopologySubbasis]
      rw [hη.mem_open_set_iff (by grind [hB.prop, constructibleTopologySubbasis]), Set.inter_comm]
      refine hi_cmpt.nonempty_inter_sInter (fun _ hb ↦ hb.2) fun a ha hfa ↦ ?_
      rw [← Set.sInter_insert]
      exact hB.prop.2.1 _ (by grind) (by simpa)
  · -- If `Z` is reducible, there exist closed compacts `Y₁` and `Y₂` such that `Z ⊆ Y₁ ∪ Y₂`.
    simp only [IsIrreducible, hZ_nonempty, true_and] at hZ_irred
    obtain ⟨Y₁, Y₂, hY₁_cl, hY₂_cl, hc₁, hc₂, hsub, ⟨x₁, hx₁⟩, ⟨x₂, hx₂⟩⟩ :=
      PrespectralSpace.exists_isClosed_of_not_isPreirreducible _ hZ_irred
    have hY₁ : Y₁ ∈ 𝔅 := .inr ⟨hY₁_cl, hc₁⟩
    have hY₂ : Y₂ ∈ 𝔅 := .inr ⟨hY₂_cl, hc₂⟩
    have hY₁_inter_B : Y₁ ∩ ⋂₀ B = ∅ := by grind
    have hY₂_inter_B : Y₂ ∩ ⋂₀ B = ∅ := by grind
    -- By maximality of `B`, neither `B ∪ {Y₁}` nor `B ∪ {Y₂}` is contained in `𝒮`
    have hY₁B : insert Y₁ B ∉ 𝒮 := by
      intro hY₁B
      grind [show insert Y₁ B ⊆ B from hB.le_of_ge hY₁B (Set.subset_insert Y₁ B)]
    have hY₂B : insert Y₂ B ∉ 𝒮 := by
      intro hY₂B
      grind [show insert Y₂ B ⊆ B from hB.le_of_ge hY₂B (Set.subset_insert Y₂ B)]
    dsimp [𝒮] at hY₁B hY₂B
    simp only [Set.insert_subset_iff, hY₁, hB.prop.1, and_self, Set.sInter_insert, hY₁_inter_B,
      and_true, true_and, not_forall, Set.not_nonempty_iff_eq_empty, hY₂, hY₂_inter_B] at hY₁B hY₂B
    -- Hence there exist finitely many `{Aᵢ} ⊆ B` such that `Z ∩ ⋂ Aᵢ = ∅`.
    obtain ⟨A₁, hA₁, hA₁', hA₁''⟩ := hY₁B
    obtain ⟨A₂, hA₂, hA₂', hA₂''⟩ := hY₂B
    have : Z ∩ ⋂₀ (A₁ \ {Y₁} ∪ A₂ \ {Y₂}) = ∅ := by grind
    rw [← Set.not_nonempty_iff_eq_empty] at this
    apply this
    rw [Set.inter_comm]
    refine IsCompact.nonempty_inter_sInter ?_ (fun _ hb ↦ hb.2) fun F hsub hF ↦ ?_
    · apply isCompact_sInter_of_subset_constructibleTopologySubbasis _ (hA₁'.sdiff.union hA₂'.sdiff)
      grind [hB.prop, Set.union_subset_iff]
    · rw [← Set.sInter_union]
      refine hB.prop.2.1 (_ ∪ F) ?_ <| (hA₁'.sdiff.union hA₂'.sdiff).union hF
      grind [Set.sdiff_singleton_subset_iff, Set.union_subset_iff]
