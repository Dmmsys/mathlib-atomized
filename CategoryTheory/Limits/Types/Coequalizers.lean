/-
Copyright (c) 2020 Kim Morrison. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Kim Morrison
-/
module

public import Mathlib.CategoryTheory.Limits.Shapes.Equalizers
public import Mathlib.CategoryTheory.Limits.Types.Colimits
public import Mathlib.Logic.Function.Coequalizer
public import Mathlib.Tactic.CategoryTheory.Elementwise

/-!
# Coequalizers in Type

The coequalizer of a pair of maps `(f, g)` from `X` to `Y`
is the quotient of `Y` by `∀ x : Y, f x ~ g x`

-/

@[expose] public section

universe v u

open CategoryTheory Limits ConcreteCategory

namespace CategoryTheory.Limits.Types

variable {X Y Z : Type u} (f g : X ⟶ Y)

/-- Show that the quotient by the relation generated by `f(x) ~ g(x)`
is a coequalizer for the pair `(f, g)`.
-/
/-
**CategoryTheory.Limits.Types.coequalizerColimit** 是 Mathlib 中的一个定义，位于命名空间 `Cate
goryTheory.Limits.Types`。
形式化陈述：coequalizerColimit : Limits.ColimitCocone (parallelPair f g) where cocone
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Show that the quotient by the relation generated by `f(x) ~ g(x)`
is a coequalizer for the pair `(f, g)`.
-/
def coequalizerColimit : Limits.ColimitCocone (parallelPair f g) where
  cocone :=
    Cofork.ofπ (↾(Function.Coequalizer.mk f g))
      (by ext x; exact Function.Coequalizer.condition f g x)
  isColimit :=
    Cofork.IsColimit.mk _
      (fun s ↦ ↾(Function.Coequalizer.desc f g s.π
        (by ext x; exact ConcreteCategory.congr_hom s.condition x)))
      (fun _ ↦ rfl)
      (fun _ _ hm ↦ by ext x; exact Quot.inductionOn x (congr_hom hm))

/-- If `π : Y ⟶ Z` is a coequalizer for `(f, g)`, and `U ⊆ Y` such that `f ⁻¹' U = g ⁻¹' U`,
then `π ⁻¹' π '' U = U`.
-/
/-
**CategoryTheory.Limits.Types.coequalizer_preimage_image_eq_of_preimage_eq** 是 M
athlib 中的一个定理，位于命名空间 `CategoryTheory.Limits.Types`。
形式化陈述：coequalizer_preimage_image_eq_of_preimage_eq (π : Y ⟶ Z) (e : f ≫ π = g ≫ 
π) (h : IsColimit (Cofork.ofπ π e)) (U : Set Y) (H : f ⁻¹' U = g ⁻¹' U) : π ⁻¹' 
π '' U = U
参数：π : Y ⟶ Z；e : f ≫ π = g ≫ π；h : IsColimit (Cofork.ofπ π e)；U : Set Y；H : f ⁻¹
' U = g ⁻¹' U。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `CategoryTheory.Limits.IsColimit.comp_coconePointUniqueUpToIso_inv`：∀ {J 
: Type u₁} [inst : CategoryTheory.Category.{v₁, u₁} J] {C : Type u₃} [inst_1 : C
ategoryTheory.Category.{v₃, u₃} C]   {F : CategoryTheor…
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `CategoryTheory.mono_iff_injective`：mono_iff_injective {X Y : Type u} (f 
: X ⟶ Y) : Mono f ↔ Function.Injective f
· 使用定理 `CategoryTheory.StrongMono.mono`：∀ {C : Type u} {inst : CategoryTheory.Ca
tegory.{v, u} C} {P Q : C} {f : P ⟶ Q} [self : CategoryTheory.StrongMono f],   C
ategoryTheory.Mono f
· 使用定理 `CategoryTheory.strongMono_of_isIso`：∀ {C : Type u} [inst : CategoryTheor
y.Category.{v, u} C] {P Q : C} (f : Q ⟶ P) [CategoryTheory.IsIso f],   CategoryT
heory.StrongMono f
· 使用定理 `CategoryTheory.Iso.isIso_inv`：∀ {C : Type u} [inst : CategoryTheory.Cate
gory.{v, u} C] {X Y : C} (e : X ≅ Y), CategoryTheory.IsIso e.inv
· 使用定理 `Equivalence.eqvGen_iff`：Equivalence.eqvGen_iff (h : Equivalence r) : Eqv
Gen r a b ↔ r a b
· 使用定理 `Relation.EqvGen.mono`：mono {r p : α -> α -> Prop} (hrp : r <= p) : EqvGe
n r <= EqvGen p
· 使用定理 `Quot.eqvGen_exact`：Quot.eqvGen_exact (H : Quot.mk r a = Quot.mk r b) : E
qvGen r a b
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `heq_of_eq`：∀ {α : Sort u_1} {a a' : α}, a = a' → a ≍ a'

--- 原说明 ---
If `π : Y ⟶ Z` is a coequalizer for `(f, g)`, and `U ⊆ Y` such that `f ⁻¹' U = g
 ⁻¹' U`,
then `π ⁻¹' π '' U = U`.
-/
theorem coequalizer_preimage_image_eq_of_preimage_eq (π : Y ⟶ Z) (e : f ≫ π = g ≫ π)
    (h : IsColimit (Cofork.ofπ π e)) (U : Set Y) (H : f ⁻¹' U = g ⁻¹' U) : π ⁻¹' π '' U = U := by
  have lem : ∀ x y, Function.Coequalizer.Rel f g x y → (x ∈ U ↔ y ∈ U) := by
    rintro _ _ ⟨x⟩
    change x ∈ f ⁻¹' U ↔ x ∈ g ⁻¹' U
    rw [H]
  have eqv : _root_.Equivalence fun x y => x ∈ U ↔ y ∈ U := by
    aesop (add safe constructors _root_.Equivalence)
  ext
  constructor
  · rw [←
      show _ = π from
        h.comp_coconePointUniqueUpToIso_inv (coequalizerColimit f g).2
          WalkingParallelPair.one]
    rintro ⟨y, hy, e'⟩
    dsimp at e'
    have e'' :=
      (mono_iff_injective
            (h.coconePointUniqueUpToIso (coequalizerColimit f g).isColimit).inv).mp
        inferInstance
    refine (eqv.eqvGen_iff.mp (Relation.EqvGen.mono lem y _ (Quot.eqvGen_exact ?_))).mp hy
    apply e''
    convert! e'
  · exact fun hx => ⟨_, hx, rfl⟩

/-- The categorical coequalizer in `Type u` is the quotient by `f g ~ g x`. -/
/-
**CategoryTheory.Limits.Types.coequalizerIso** 是 Mathlib 中的一个定义，位于命名空间 `Category
Theory.Limits.Types`。
形式化陈述：coequalizerIso : coequalizer f g ≅ (Function.Coequalizer f g)
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The categorical coequalizer in `Type u` is the quotient by `f g ~ g x`.
-/
noncomputable def coequalizerIso : coequalizer f g ≅ (Function.Coequalizer f g) :=
  colimit.isoColimitCocone (coequalizerColimit f g)

@[elementwise (attr := simp)]
/-
**CategoryTheory.Limits.Types.coequalizerIso_** 是 Mathlib 中的一个定理，位于命名空间 `Categor
yTheory.Limits.Types`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem coequalizerIso_π_comp_hom :
    coequalizer.π f g ≫ (coequalizerIso f g).hom = ↾(Function.Coequalizer.mk f g) :=
  colimit.isoColimitCocone_ι_hom (coequalizerColimit f g) WalkingParallelPair.one

@[elementwise (attr := simp)]
/-
**CategoryTheory.Limits.Types.coequalizerIso_quot_comp_inv** 是 Mathlib 中的一个定理，位于
命名空间 `CategoryTheory.Limits.Types`。
形式化陈述：coequalizerIso_quot_comp_inv : ↾(Function.Coequalizer.mk f g) ≫ (coequaliz
erIso f g).inv = coequalizer.π f g
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `UnivLE.small`：∀ [self : UnivLE.{u, v}] (α : Type u), Small.{v, u} α
-/
theorem coequalizerIso_quot_comp_inv :
    ↾(Function.Coequalizer.mk f g) ≫ (coequalizerIso f g).inv = coequalizer.π f g :=
  rfl

end CategoryTheory.Limits.Types

