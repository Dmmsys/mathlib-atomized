/-
Copyright (c) 2024 Scott Carnahan. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Scott Carnahan
-/
module

public import Mathlib.LinearAlgebra.RootSystem.Hom
public import Mathlib.RepresentationTheory.Basic
public import Mathlib.Tactic.CrossRefAttribute

/-!
# The Weyl group of a root pairing

This file defines the Weyl group of a root pairing as the subgroup of automorphisms generated by
reflection automorphisms. This deviates from the existing literature, which typically defines the
Weyl group as the subgroup of linear transformations of the weight space generated by linear
reflections. However, the automorphism group of a root pairing comes with a permutation
representation on the set indexing roots and faithful linear representations on the weight space and
coweight space.  Thus, our formalism gives us an isomorphism to the traditional Weyl group together
with the natural dual representation generated by coreflections and the permutation representation
on roots.

## Main definitions
* `RootPairing.weylGroup` : The group of automorphisms generated by reflections.
* `RootPairing.weylGroupToPerm` : The permutation representation of the Weyl group on roots.

## Results
* `RootPairing.range_weylGroup_weightHom` : The image of the weight space representation is equal
  to the subgroup generated by linear reflections.
* `RootPairing.range_weylGroup_coweightHom` : The image of the coweight space representation is
  equal to the subgroup generated by linear coreflections.
* `RootPairing.range_weylGroupToPerm` : The image of the permutation representation is equal to the
  subgroup generated by reflection permutations.

## TODO
* faithfulness of `weylGroupToPerm` when multiplication by 2 is injective on the weight space.
-/

@[expose] public section

open Set Function

variable {ι R M N : Type*}

noncomputable section

namespace RootPairing

variable [CommRing R] [AddCommGroup M] [Module R M] [AddCommGroup N] [Module R N]
  (P : RootPairing ι R M N) (i : ι)

/-- The `Weyl group` of a root pairing is the group of automorphisms of the root pairing generated
by reflections. -/
@[wikidata Q768074]
/-
**RootPairing.weylGroup** 是 Mathlib 中的一个定义，位于命名空间 `RootPairing`。
形式化陈述：weylGroup : Subgroup (Aut P)
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The `Weyl group` of a root pairing is the group of automorphisms of the root pai
ring generated
by reflections.
-/
def weylGroup : Subgroup (Aut P) :=
  Subgroup.closure (range (Equiv.reflection P))
/-
**RootPairing.reflection_mem_weylGroup** 是 Mathlib 中的一个引理，位于命名空间 `RootPairing`。
形式化陈述：reflection_mem_weylGroup : Equiv.reflection P i in P.weylGroup
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Subgroup.subset_closure`：subset_closure : k subseteq closure k
· 使用定理 `Set.mem_range_self`：∀ {α : Type u} {ι : Sort u_1} {f : ι → α} (i : ι), f
 i ∈ Set.range f
-/
lemma reflection_mem_weylGroup : Equiv.reflection P i ∈ P.weylGroup :=
  Subgroup.subset_closure <| mem_range_self i

/-- The `ith` reflection as a term of the Weyl group. -/
/-
**RootPairing.weylGroup.ofIdx** 是 Mathlib 中的一个定义，位于命名空间 `RootPairing.weylGroup`。
形式化陈述：{ι : Type u_1} →   {R : Type u_2} →     {M : Type u_3} →       {N : Type u
_4} →         [inst : CommRing R] →           [inst_1 : AddCommGroup M] →       
      [inst_2 : _root_.Module R M] →               [inst_3 : AddCommGroup N] → [
inst_4 : _root_.Module R N] → (P : RootPairing ι R M N) → ι → ↥P.weylGroup
参数：P : RootPairing ι R M N。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用引理 `RootPairing.reflection_mem_weylGroup`：reflection_mem_weylGroup : Equiv.r
eflection P i in P.weylGroup

--- 原说明 ---
The `ith` reflection as a term of the Weyl group.
-/
def weylGroup.ofIdx (i : ι) : P.weylGroup := ⟨_, P.reflection_mem_weylGroup i⟩
/-
**RootPairing.weylGroup.ofIdx_smul** 是 Mathlib 中的一个定理，位于命名空间 `RootPairing.weylGr
oup`。
形式化陈述：∀ {ι : Type u_1} {R : Type u_2} {M : Type u_3} {N : Type u_4} [inst : Comm
Ring R] [inst_1 : AddCommGroup M]   [inst_2 : _root_.Module R M] [inst_3 : AddCo
mmGroup N] [inst_4 : _root_.Module R N] (P : RootPairing ι R M N) (i : ι)   (m :
 M), RootPairing.weylGroup.ofIdx P i • m = RootPairing.Equiv.reflection P i • m
参数：P : RootPairing ι R M N；i : ι；m : M。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
@[simp] lemma weylGroup.ofIdx_smul (i : ι) (m : M) :
    weylGroup.ofIdx P i • m = Equiv.reflection P i • m :=
  rfl

/-- Usually `RootPairing.weylGroup.induction` will be more useful than this lemma. -/
/-
**RootPairing.weylGroup_toSubmonoid** 是 Mathlib 中的一个引理，位于命名空间 `RootPairing`。
形式化陈述：weylGroup_toSubmonoid : P.weylGroup.toSubmonoid = Submonoid.closure (range
 (Equiv.reflection P))
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用引理 `RootPairing.Equiv.reflection_inv`：reflection_inv (P : RootPairing ι R M 
N) (i : ι) : (reflection P i)⁻¹ = (reflection P i)
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `RootPairing.weylGroup.eq_1`：∀ {ι : Type u_1} {R : Type u_2} {M : Type u_
3} {N : Type u_4} [inst : CommRing R] [inst_1 : AddCommGroup M]   [inst_2 : _roo
t_.Module R M] […
· 使用定理 `Subgroup.closure_toSubmonoid`：closure_toSubmonoid (S : Set G) : (closure
 S).toSubmonoid = Submonoid.closure (S union S⁻¹)
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Set.union_self`：union_self (a : Set α) : a union a = a

--- 原说明 ---
Usually `RootPairing.weylGroup.induction` will be more useful than this lemma.
-/
lemma weylGroup_toSubmonoid :
    P.weylGroup.toSubmonoid = Submonoid.closure (range (Equiv.reflection P)) := by
  suffices range (Equiv.reflection P) = (range (Equiv.reflection P))⁻¹ by
    rw [weylGroup, Subgroup.closure_toSubmonoid, ← this, union_self]
  ext; simp [← inv_eq_iff_eq_inv]

@[elab_as_elim]
/-
**RootPairing.weylGroup.induction** 是 Mathlib 中的一个定理，位于命名空间 `RootPairing.weylGro
up`。
形式化陈述：∀ {ι : Type u_1} {R : Type u_2} {M : Type u_3} {N : Type u_4} [inst : Comm
Ring R] [inst_1 : AddCommGroup M]   [inst_2 : _root_.Module R M] [inst_3 : AddCo
mmGroup N] [inst_4 : _root_.Module R N] (P : RootPairing ι R M N)   {pred : (g :
 P.Aut) → g ∈ P.weylGroup → Prop},   (∀ (i : ι), pred (RootPairing.Equiv.reflect
ion P i) ⋯) →     pred 1 ⋯ →       (∀ (x y : P.Aut) (hx : x ∈ P.weylGroup) (hy :
 y ∈ P.weylGroup), pred x hx → pred y hy → pred (x * y) ⋯) →         ∀ {x : P.Au
t} (hx : x ∈ P.weylGroup), pred x hx
参数：P : RootPairing ι R M N；g : P.Aut；∀ (i : ι), pred (RootPairing.Equiv.reflecti
on P i) ⋯；∀ (x y : P.Aut) (hx : x ∈ P.weylGroup) (hy : y ∈ P.weylGroup), pred x 
hx → pred y hy → pred (x * y) ⋯；hx : x ∈ P.weylGroup。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `RootPairing.reflection_mem_weylGroup`：reflection_mem_weylGroup : Equiv.r
eflection P i in P.weylGroup
· 使用定理 `OneMemClass.one_mem`：∀ {S : Type u_3} {M : outParam (Type u_4)} {inst : 
One M} {inst_1 : SetLike S M} [self : OneMemClass S M] (s : S), 1 ∈ s
· 使用定理 `SubmonoidClass.toOneMemClass`：∀ {S : Type u_3} {M : outParam (Type u_4)}
 {inst : MulOneClass M} {inst_1 : SetLike S M} [self : SubmonoidClass S M],   On
eMemClass S M
· 使用定理 `SubgroupClass.toSubmonoidClass`：∀ {S : Type u_3} {G : outParam (Type u_4
)} {inst : DivInvMonoid G} {inst_1 : SetLike S G} [self : SubgroupClass S G],   
SubmonoidClass S G
· 使用定理 `Subgroup.instSubgroupClass`：∀ {G : Type u_1} [inst : Group G], SubgroupC
lass (Subgroup G) G
· 使用定理 `MulMemClass.mul_mem`：∀ {S : Type u_3} {M : outParam (Type u_4)} {inst : 
Mul M} {inst_1 : SetLike S M} [self : MulMemClass S M] {s : S}   {a b : M}, a ∈ 
s → b ∈ s…
· 使用定理 `SubmonoidClass.toMulMemClass`：∀ {S : Type u_3} {M : outParam (Type u_4)}
 {inst : MulOneClass M} {inst_1 : SetLike S M} [self : SubmonoidClass S M],   Mu
lMemClass S M
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `RootPairing.weylGroup_toSubmonoid`：weylGroup_toSubmonoid : P.weylGroup.t
oSubmonoid = Submonoid.closure (range (Equiv.reflection P))
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Submonoid.closure_induction`：closure_induction {s : Set M} {motive : (x 
: M) -> x in closure s -> Prop} (mem : forall (x) (h : x in s), motive x (subset
_closure h)) (one…
· 使用定理 `Submonoid.subset_closure`：subset_closure : s subseteq closure s
-/
lemma weylGroup.induction {pred : (g : Aut P) → g ∈ P.weylGroup → Prop}
    (mem : ∀ i, pred (Equiv.reflection P i) (P.reflection_mem_weylGroup i))
    (one : pred 1 (one_mem _))
    (mul : ∀ x y hx hy, pred x hx → pred y hy → pred (x * y) (mul_mem hx hy))
    {x} (hx : x ∈ P.weylGroup) :
    pred x hx := by
  let pred' : (g : Aut P) → g ∈ Submonoid.closure (range (Equiv.reflection P)) → Prop :=
    fun g hg ↦ pred g <| by change g ∈ P.weylGroup.toSubmonoid; rwa [weylGroup_toSubmonoid]
  have hx' : x ∈ Submonoid.closure (range (Equiv.reflection P)) := by rwa [← weylGroup_toSubmonoid]
  suffices pred' x hx' from this
  apply Submonoid.closure_induction
  · rintro - ⟨i, rfl⟩
    exact mem i
  · exact one
  · intro x y hx hy hx' hy'
    rw [← weylGroup_toSubmonoid] at hx hy
    exact mul x y hx hy hx' hy'

@[elab_as_elim]
/-
**RootPairing.weylGroup.induction'** 是 Mathlib 中的一个定理，位于命名空间 `RootPairing.weylGr
oup`。
形式化陈述：∀ {ι : Type u_1} {R : Type u_2} {M : Type u_3} {N : Type u_4} [inst : Comm
Ring R] [inst_1 : AddCommGroup M]   [inst_2 : _root_.Module R M] [inst_3 : AddCo
mmGroup N] [inst_4 : _root_.Module R N] (P : RootPairing ι R M N)   [Nonempty ι]
 {pred : (g : P.Aut) → g ∈ P.weylGroup → Prop},   (∀ (i : ι), pred (RootPairing.
Equiv.reflection P i) ⋯) →     (∀ (x y : P.Aut) (hx : x ∈ P.weylGroup) (hy : y ∈
 P.weylGroup), pred x hx → pred y hy → pred (x * y) ⋯) →       ∀ {x : P.Aut} (hx
 : x ∈ P.weylGroup), pred x hx
参数：P : RootPairing ι R M N；g : P.Aut；∀ (i : ι), pred (RootPairing.Equiv.reflecti
on P i) ⋯；∀ (x y : P.Aut) (hx : x ∈ P.weylGroup) (hy : y ∈ P.weylGroup), pred x 
hx → pred y hy → pred (x * y) ⋯；hx : x ∈ P.weylGroup。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `RootPairing.reflection_mem_weylGroup`：reflection_mem_weylGroup : Equiv.r
eflection P i in P.weylGroup
· 使用定理 `MulMemClass.mul_mem`：∀ {S : Type u_3} {M : outParam (Type u_4)} {inst : 
Mul M} {inst_1 : SetLike S M} [self : MulMemClass S M] {s : S}   {a b : M}, a ∈ 
s → b ∈ s…
· 使用定理 `SubmonoidClass.toMulMemClass`：∀ {S : Type u_3} {M : outParam (Type u_4)}
 {inst : MulOneClass M} {inst_1 : SetLike S M} [self : SubmonoidClass S M],   Mu
lMemClass S M
· 使用定理 `SubgroupClass.toSubmonoidClass`：∀ {S : Type u_3} {G : outParam (Type u_4
)} {inst : DivInvMonoid G} {inst_1 : SetLike S G} [self : SubgroupClass S G],   
SubmonoidClass S G
· 使用定理 `Subgroup.instSubgroupClass`：∀ {G : Type u_1} [inst : Group G], SubgroupC
lass (Subgroup G) G
· 使用定理 `RootPairing.weylGroup.induction`：∀ {ι : Type u_1} {R : Type u_2} {M : Ty
pe u_3} {N : Type u_4} [inst : CommRing R] [inst_1 : AddCommGroup M]   [inst_2 :
 _root_.Module R M] […
· 使用定理 `OneMemClass.one_mem`：∀ {S : Type u_3} {M : outParam (Type u_4)} {inst : 
One M} {inst_1 : SetLike S M} [self : OneMemClass S M] (s : S), 1 ∈ s
· 使用定理 `SubmonoidClass.toOneMemClass`：∀ {S : Type u_3} {M : outParam (Type u_4)}
 {inst : MulOneClass M} {inst_1 : SetLike S M} [self : SubmonoidClass S M],   On
eMemClass S M
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `sq`：∀ {M : Type u_2} [inst : Monoid M] (a : M), a ^ 2 = a * a
· 使用定理 `mul_eq_one_iff_inv_eq`：mul_eq_one_iff_inv_eq : a * b = 1 ↔ a⁻¹ = b
· 使用引理 `RootPairing.Equiv.reflection_inv`：reflection_inv (P : RootPairing ι R M 
N) (i : ι) : (reflection P i)⁻¹ = (reflection P i)
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
-/
lemma weylGroup.induction' [Nonempty ι] {pred : (g : Aut P) → g ∈ P.weylGroup → Prop}
    (mem : ∀ i, pred (Equiv.reflection P i) (P.reflection_mem_weylGroup i))
    (mul : ∀ x y hx hy, pred x hx → pred y hy → pred (x * y) (mul_mem hx hy))
    {x} (hx : x ∈ P.weylGroup) :
    pred x hx := by
  refine weylGroup.induction P mem ?_ mul hx
  obtain ⟨i⟩ : Nonempty ι := inferInstance
  have : (Equiv.reflection P i) ^ 2 = 1 := by
    rw [sq, mul_eq_one_iff_inv_eq, Equiv.reflection_inv P i]
  simpa [sq, ← this] using mul _ _ _ _ (mem i) (mem i)

set_option backward.isDefEq.respectTransparency false in
/-
**RootPairing.range_weylGroup_weightHom** 是 Mathlib 中的一个引理，位于命名空间 `RootPairing`。
形式化陈述：range_weylGroup_weightHom : MonoidHom.range ((Equiv.weightHom P).domRestri
ct P.weylGroup) = Subgroup.closure (range P.reflection)
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `SubgroupClass.toSubmonoidClass`：∀ {S : Type u_3} {G : outParam (Type u_4
)} {inst : DivInvMonoid G} {inst_1 : SetLike S G} [self : SubgroupClass S G],   
SubmonoidClass S G
· 使用定理 `Subgroup.instSubgroupClass`：∀ {G : Type u_1} [inst : Group G], SubgroupC
lass (Subgroup G) G
· 使用定理 `Subgroup.closure_eq_of_le`：closure_eq_of_le (h₁ : k subseteq K) (h₂ : K 
<= closure k) : closure k = K
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MonoidHom.domRestrict_range`：domRestrict_range (f : G ->* N) : (f.domRes
trict K).range = K.map f
· 使用定理 `Set.image_congr`：image_congr {f g : α -> β} {s : Set α} (h : forall a in
 s, f a = g a) : f '' s = g '' s
· 使用定理 `RootPairing.Equiv.weightHom_apply`：∀ {ι : Type u_1} {R : Type u_2} {M : 
Type u_3} {N : Type u_4} [inst : CommRing R] [inst_1 : AddCommGroup M]   [inst_2
 : _root_.Module R M] […
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用引理 `RootPairing.reflection_mem_weylGroup`：reflection_mem_weylGroup : Equiv.r
eflection P i in P.weylGroup
· 使用引理 `RootPairing.Equiv.reflection_weightEquiv`：reflection_weightEquiv (P : Ro
otPairing ι R M N) (i : ι) : (reflection P i).weightEquiv = P.reflection i
· 使用定理 `Subgroup.closure_induction''`：closure_induction'' {p : (g : G) -> g in c
losure s -> Prop} (mem : forall x (hx : x in s), p x (subset_closure hx)) (inv_m
em : forall x (hx …
· 使用定理 `Subgroup.subset_closure`：subset_closure : k subseteq closure k
· 使用定理 `Set.mem_range_self`：∀ {α : Type u} {ι : Sort u_1} {f : ι → α} (i : ι), f
 i ∈ Set.range f
· 使用定理 `InvMemClass.inv_mem`：∀ {S : Type u_3} {G : outParam (Type u_4)} {inst : 
Inv G} {inst_1 : SetLike S G} [self : InvMemClass S G] {s : S}   {x : G}, x ∈ s 
→ x⁻¹ ∈ s
· 使用定理 `SubgroupClass.toInvMemClass`：∀ {S : Type u_3} {G : outParam (Type u_4)} 
{inst : DivInvMonoid G} {inst_1 : SetLike S G} [self : SubgroupClass S G],   Inv
MemClass S G
· 使用引理 `RootPairing.Equiv.reflection_inv`：reflection_inv (P : RootPairing ι R M 
N) (i : ι) : (reflection P i)⁻¹ = (reflection P i)
· 使用定理 `Subtype.mk.congr_simp`：∀ {α : Sort u} {p : α → Prop} (val val_1 : α) (e_
val : val = val_1) (property : p val), ⟨val, property⟩ = ⟨val_1, ⋯⟩
· 使用定理 `OneMemClass.one_mem`：∀ {S : Type u_3} {M : outParam (Type u_4)} {inst : 
One M} {inst_1 : SetLike S M} [self : OneMemClass S M] (s : S), 1 ∈ s
· 使用定理 `SubmonoidClass.toOneMemClass`：∀ {S : Type u_3} {M : outParam (Type u_4)}
 {inst : MulOneClass M} {inst_1 : SetLike S M} [self : SubmonoidClass S M],   On
eMemClass S M
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `map_one`：map_one [OneHomClass F M N] (f : F) : f 1 = 1
· 使用定理 `MonoidHomClass.toOneHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `MulMemClass.mul_mem`：∀ {S : Type u_3} {M : outParam (Type u_4)} {inst : 
Mul M} {inst_1 : SetLike S M} [self : MulMemClass S M] {s : S}   {a b : M}, a ∈ 
s → b ∈ s…
· 使用定理 `SubmonoidClass.toMulMemClass`：∀ {S : Type u_3} {M : outParam (Type u_4)}
 {inst : MulOneClass M} {inst_1 : SetLike S M} [self : SubmonoidClass S M],   Mu
lMemClass S M
· 使用定理 `map_mul`：map_mul [MulHomClass F M N] (f : F) (x y : M) : f (x * y) = f x
 * f y
· 使用定理 `MonoidHomClass.toMulHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `Subgroup.mul_mem`：∀ {G : Type u_1} [inst : Group G] (H : Subgroup G) {x 
y : G}, x ∈ H → y ∈ H → x * y ∈ H
-/
lemma range_weylGroup_weightHom :
    MonoidHom.range ((Equiv.weightHom P).domRestrict P.weylGroup) =
      Subgroup.closure (range P.reflection) := by
  refine (Subgroup.closure_eq_of_le _ ?_ ?_).symm
  · rintro - ⟨i, rfl⟩
    simp only [MonoidHom.domRestrict_range, Subgroup.coe_map, Equiv.weightHom_apply, mem_image,
      SetLike.mem_coe]
    use Equiv.reflection P i
    exact ⟨reflection_mem_weylGroup P i, Equiv.reflection_weightEquiv P i⟩
  · rintro fg ⟨⟨w, hw⟩, rfl⟩
    induction hw using Subgroup.closure_induction'' with
    | one =>
      change ((Equiv.weightHom P).domRestrict P.weylGroup) 1 ∈ _
      simp only [map_one, one_mem]
    | mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [MonoidHom.domRestrict_apply, Equiv.weightHom_apply, Equiv.reflection_weightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | inv_mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [Equiv.reflection_inv, MonoidHom.domRestrict_apply, Equiv.weightHom_apply,
        Equiv.reflection_weightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | mul w₁ w₂ hw₁ hw₂ h₁ h₂ =>
      simpa only [← Submonoid.mk_mul_mk _ w₁ w₂ hw₁ hw₂, map_mul] using! Subgroup.mul_mem _ h₁ h₂

set_option backward.isDefEq.respectTransparency false in
/-
**RootPairing.range_weylGroup_coweightHom** 是 Mathlib 中的一个引理，位于命名空间 `RootPairing
`。
形式化陈述：range_weylGroup_coweightHom : MonoidHom.range ((Equiv.coweightHom P).domRe
strict P.weylGroup) = Subgroup.closure (range (MulOpposite.op ∘ P.coreflection))
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `SubgroupClass.toSubmonoidClass`：∀ {S : Type u_3} {G : outParam (Type u_4
)} {inst : DivInvMonoid G} {inst_1 : SetLike S G} [self : SubgroupClass S G],   
SubmonoidClass S G
· 使用定理 `Subgroup.instSubgroupClass`：∀ {G : Type u_1} [inst : Group G], SubgroupC
lass (Subgroup G) G
· 使用定理 `Subgroup.closure_eq_of_le`：closure_eq_of_le (h₁ : k subseteq K) (h₂ : K 
<= closure k) : closure k = K
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MonoidHom.domRestrict_range`：domRestrict_range (f : G ->* N) : (f.domRes
trict K).range = K.map f
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用引理 `RootPairing.reflection_mem_weylGroup`：reflection_mem_weylGroup : Equiv.r
eflection P i in P.weylGroup
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `RootPairing.Equiv.coweightHom_apply`：∀ {ι : Type u_1} {R : Type u_2} {M 
: Type u_3} {N : Type u_4} [inst : CommRing R] [inst_1 : AddCommGroup M]   [inst
_2 : _root_.Module R M] […
· 使用引理 `RootPairing.Equiv.reflection_coweightEquiv`：reflection_coweightEquiv (P 
: RootPairing ι R M N) (i : ι) : (reflection P i).coweightEquiv = P.coreflection
 i
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Subgroup.closure_induction''`：closure_induction'' {p : (g : G) -> g in c
losure s -> Prop} (mem : forall x (hx : x in s), p x (subset_closure hx)) (inv_m
em : forall x (hx …
· 使用定理 `Subgroup.subset_closure`：subset_closure : k subseteq closure k
· 使用定理 `Set.mem_range_self`：∀ {α : Type u} {ι : Sort u_1} {f : ι → α} (i : ι), f
 i ∈ Set.range f
· 使用定理 `InvMemClass.inv_mem`：∀ {S : Type u_3} {G : outParam (Type u_4)} {inst : 
Inv G} {inst_1 : SetLike S G} [self : InvMemClass S G] {s : S}   {x : G}, x ∈ s 
→ x⁻¹ ∈ s
· 使用定理 `SubgroupClass.toInvMemClass`：∀ {S : Type u_3} {G : outParam (Type u_4)} 
{inst : DivInvMonoid G} {inst_1 : SetLike S G} [self : SubgroupClass S G],   Inv
MemClass S G
· 使用引理 `RootPairing.Equiv.reflection_inv`：reflection_inv (P : RootPairing ι R M 
N) (i : ι) : (reflection P i)⁻¹ = (reflection P i)
· 使用定理 `Subtype.mk.congr_simp`：∀ {α : Sort u} {p : α → Prop} (val val_1 : α) (e_
val : val = val_1) (property : p val), ⟨val, property⟩ = ⟨val_1, ⋯⟩
· 使用定理 `OneMemClass.one_mem`：∀ {S : Type u_3} {M : outParam (Type u_4)} {inst : 
One M} {inst_1 : SetLike S M} [self : OneMemClass S M] (s : S), 1 ∈ s
· 使用定理 `SubmonoidClass.toOneMemClass`：∀ {S : Type u_3} {M : outParam (Type u_4)}
 {inst : MulOneClass M} {inst_1 : SetLike S M} [self : SubmonoidClass S M],   On
eMemClass S M
· 使用定理 `map_one`：map_one [OneHomClass F M N] (f : F) : f 1 = 1
· 使用定理 `MonoidHomClass.toOneHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `MulMemClass.mul_mem`：∀ {S : Type u_3} {M : outParam (Type u_4)} {inst : 
Mul M} {inst_1 : SetLike S M} [self : MulMemClass S M] {s : S}   {a b : M}, a ∈ 
s → b ∈ s…
· 使用定理 `SubmonoidClass.toMulMemClass`：∀ {S : Type u_3} {M : outParam (Type u_4)}
 {inst : MulOneClass M} {inst_1 : SetLike S M} [self : SubmonoidClass S M],   Mu
lMemClass S M
· 使用定理 `map_mul`：map_mul [MulHomClass F M N] (f : F) (x y : M) : f (x * y) = f x
 * f y
· 使用定理 `MonoidHomClass.toMulHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `Subgroup.mul_mem`：∀ {G : Type u_1} [inst : Group G] (H : Subgroup G) {x 
y : G}, x ∈ H → y ∈ H → x * y ∈ H
-/
lemma range_weylGroup_coweightHom :
    MonoidHom.range ((Equiv.coweightHom P).domRestrict P.weylGroup) =
      Subgroup.closure (range (MulOpposite.op ∘ P.coreflection)) := by
  refine (Subgroup.closure_eq_of_le _ ?_ ?_).symm
  · rintro - ⟨i, rfl⟩
    simp only [MonoidHom.domRestrict_range, Subgroup.coe_map, mem_image,
      SetLike.mem_coe]
    use Equiv.reflection P i
    refine ⟨reflection_mem_weylGroup P i, by simp⟩
  · rintro fg ⟨⟨w, hw⟩, rfl⟩
    induction hw using Subgroup.closure_induction'' with
    | one =>
      change ((Equiv.coweightHom P).domRestrict P.weylGroup) 1 ∈ _
      simp only [map_one, one_mem]
    | mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [MonoidHom.domRestrict_apply, Equiv.coweightHom_apply,
        Equiv.reflection_coweightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | inv_mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [Equiv.reflection_inv, MonoidHom.domRestrict_apply, Equiv.coweightHom_apply,
        Equiv.reflection_coweightEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | mul w₁ w₂ hw₁ hw₂ h₁ h₂ =>
      simpa only [← Submonoid.mk_mul_mk _ w₁ w₂ hw₁ hw₂, map_mul] using! Subgroup.mul_mem _ h₁ h₂

/-- The permutation representation of the Weyl group induced by `reflectionPerm`. -/
/-
**RootPairing.weylGroupToPerm** 是 Mathlib 中的一个缩写定义，位于命名空间 `RootPairing`。
形式化陈述：weylGroupToPerm
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The permutation representation of the Weyl group induced by `reflectionPerm`.
-/
abbrev weylGroupToPerm := (Equiv.indexHom P).domRestrict P.weylGroup
/-
**RootPairing.range_weylGroupToPerm** 是 Mathlib 中的一个引理，位于命名空间 `RootPairing`。
形式化陈述：range_weylGroupToPerm : P.weylGroupToPerm.range = Subgroup.closure (range 
P.reflectionPerm)
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Subgroup.closure_eq_of_le`：closure_eq_of_le (h₁ : k subseteq K) (h₂ : K 
<= closure k) : closure k = K
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MonoidHom.domRestrict_range`：domRestrict_range (f : G ->* N) : (f.domRes
trict K).range = K.map f
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用引理 `RootPairing.reflection_mem_weylGroup`：reflection_mem_weylGroup : Equiv.r
eflection P i in P.weylGroup
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `RootPairing.Equiv.indexHom_apply`：∀ {ι : Type u_1} {R : Type u_2} {M : T
ype u_3} {N : Type u_4} [inst : CommRing R] [inst_1 : AddCommGroup M]   [inst_2 
: _root_.Module R M] […
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Subgroup.closure_induction''`：closure_induction'' {p : (g : G) -> g in c
losure s -> Prop} (mem : forall x (hx : x in s), p x (subset_closure hx)) (inv_m
em : forall x (hx …
· 使用定理 `Subgroup.subset_closure`：subset_closure : k subseteq closure k
· 使用定理 `Set.mem_range_self`：∀ {α : Type u} {ι : Sort u_1} {f : ι → α} (i : ι), f
 i ∈ Set.range f
· 使用定理 `InvMemClass.inv_mem`：∀ {S : Type u_3} {G : outParam (Type u_4)} {inst : 
Inv G} {inst_1 : SetLike S G} [self : InvMemClass S G] {s : S}   {x : G}, x ∈ s 
→ x⁻¹ ∈ s
· 使用定理 `SubgroupClass.toInvMemClass`：∀ {S : Type u_3} {G : outParam (Type u_4)} 
{inst : DivInvMonoid G} {inst_1 : SetLike S G} [self : SubgroupClass S G],   Inv
MemClass S G
· 使用定理 `Subgroup.instSubgroupClass`：∀ {G : Type u_1} [inst : Group G], SubgroupC
lass (Subgroup G) G
· 使用引理 `RootPairing.Equiv.reflection_inv`：reflection_inv (P : RootPairing ι R M 
N) (i : ι) : (reflection P i)⁻¹ = (reflection P i)
· 使用定理 `Subtype.mk.congr_simp`：∀ {α : Sort u} {p : α → Prop} (val val_1 : α) (e_
val : val = val_1) (property : p val), ⟨val, property⟩ = ⟨val_1, ⋯⟩
· 使用定理 `OneMemClass.one_mem`：∀ {S : Type u_3} {M : outParam (Type u_4)} {inst : 
One M} {inst_1 : SetLike S M} [self : OneMemClass S M] (s : S), 1 ∈ s
· 使用定理 `SubmonoidClass.toOneMemClass`：∀ {S : Type u_3} {M : outParam (Type u_4)}
 {inst : MulOneClass M} {inst_1 : SetLike S M} [self : SubmonoidClass S M],   On
eMemClass S M
· 使用定理 `SubgroupClass.toSubmonoidClass`：∀ {S : Type u_3} {G : outParam (Type u_4
)} {inst : DivInvMonoid G} {inst_1 : SetLike S G} [self : SubgroupClass S G],   
SubmonoidClass S G
· 使用定理 `map_one`：map_one [OneHomClass F M N] (f : F) : f 1 = 1
· 使用定理 `MonoidHomClass.toOneHomClass`：∀ {F : Type u_10} {M : outParam (Type u_11
)} {N : outParam (Type u_12)} {inst : MulOne M} {inst_1 : MulOne N}   {inst_2 : 
FunLike F M N} [se…
· 使用定理 `Subgroup.mul_mem`：∀ {G : Type u_1} [inst : Group G] (H : Subgroup G) {x 
y : G}, x ∈ H → y ∈ H → x * y ∈ H
-/
lemma range_weylGroupToPerm :
    P.weylGroupToPerm.range = Subgroup.closure (range P.reflectionPerm) := by
  refine (Subgroup.closure_eq_of_le _ ?_ ?_).symm
  · rintro - ⟨i, rfl⟩
    simp only [MonoidHom.domRestrict_range, Subgroup.coe_map, mem_image, SetLike.mem_coe]
    use Equiv.reflection P i
    refine ⟨reflection_mem_weylGroup P i, by simp⟩
  · rintro fg ⟨⟨w, hw⟩, rfl⟩
    induction hw using Subgroup.closure_induction'' with
    | one =>
      change ((Equiv.indexHom P).domRestrict P.weylGroup) 1 ∈ _
      simp only [map_one, one_mem]
    | mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [MonoidHom.domRestrict_apply, Equiv.indexHom_apply, Equiv.reflection_indexEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | inv_mem w' hw' =>
      obtain ⟨i, rfl⟩ := hw'
      simp only [Equiv.reflection_inv, MonoidHom.domRestrict_apply, Equiv.indexHom_apply,
        Equiv.reflection_indexEquiv]
      simpa only [reflection_mem_weylGroup] using! Subgroup.subset_closure (mem_range_self i)
    | mul w₁ w₂ hw₁ hw₂ h₁ h₂ =>
      simpa only [← Submonoid.mk_mul_mk _ w₁ w₂ hw₁ hw₂, map_mul] using! Subgroup.mul_mem _ h₁ h₂

/-- The natural representation of the Weyl group on the root space. -/
/-
**RootPairing.weylGroupRootRep** 是 Mathlib 中的一个定义，位于命名空间 `RootPairing`。
形式化陈述：weylGroupRootRep : Representation R P.weylGroup M
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The natural representation of the Weyl group on the root space.
-/
def weylGroupRootRep : Representation R P.weylGroup M :=
  Representation.ofDistribMulAction R P.weylGroup M

/-- The natural representation of the Weyl group on the coroot space. -/
/-
**RootPairing.weylGroupCorootRep** 是 Mathlib 中的一个定义，位于命名空间 `RootPairing`。
形式化陈述：weylGroupCorootRep : Representation R P.weylGroup.op N
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The natural representation of the Weyl group on the coroot space.
-/
def weylGroupCorootRep : Representation R P.weylGroup.op N :=
  Representation.ofDistribMulAction R P.weylGroup.op N
/-
**RootPairing.weylGroup_apply_root** 是 Mathlib 中的一个引理，位于命名空间 `RootPairing`。
形式化陈述：weylGroup_apply_root (g : P.weylGroup) (i : ι) : g • P.root i = P.root (P.
weylGroupToPerm g i)
参数：g : P.weylGroup；i : ι。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `RootPairing.Hom.root_weightMap_apply`：root_weightMap_apply {ι₂ M₂ N₂ : T
ype*} [AddCommGroup M₂] [Module R M₂] [AddCommGroup N₂] [Module R N₂] (P : RootP
airing ι R M N) (Q : RootP…
-/
lemma weylGroup_apply_root (g : P.weylGroup) (i : ι) :
    g • P.root i = P.root (P.weylGroupToPerm g i) :=
  Hom.root_weightMap_apply _ _ _ _

@[simp]
/-
**RootPairing.InvariantForm.apply_weylGroup_smul** 是 Mathlib 中的一个定理，位于命名空间 `Root
Pairing.InvariantForm`。
形式化陈述：∀ {ι : Type u_1} {R : Type u_2} {M : Type u_3} {N : Type u_4} [inst : Comm
Ring R] [inst_1 : AddCommGroup M]   [inst_2 : _root_.Module R M] [inst_3 : AddCo
mmGroup N] [inst_4 : _root_.Module R N] (P : RootPairing ι R M N)   {B : P.Invar
iantForm} (g : ↥P.weylGroup) (x y : M), (B.form (g • x)) (g • y) = (B.form x) y
参数：P : RootPairing ι R M N；g : ↥P.weylGroup；x y : M；B.form (g • x)；g • y；B.form 
x。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `RootPairing.weylGroup.induction`：∀ {ι : Type u_1} {R : Type u_2} {M : Ty
pe u_3} {N : Type u_4} [inst : CommRing R] [inst_1 : AddCommGroup M]   [inst_2 :
 _root_.Module R M] […
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用引理 `RootPairing.reflection_mem_weylGroup`：reflection_mem_weylGroup : Equiv.r
eflection P i in P.weylGroup
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `RootPairing.InvariantForm.apply_reflection_reflection`：apply_reflection_
reflection (x y : M) : B.form (P.reflection i x) (P.reflection i y) = B.form x y
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `implies_true`：∀ (α : Sort u), (∀ (a : α), True) = True
· 使用定理 `OneMemClass.one_mem`：∀ {S : Type u_3} {M : outParam (Type u_4)} {inst : 
One M} {inst_1 : SetLike S M} [self : OneMemClass S M] (s : S), 1 ∈ s
· 使用定理 `SubmonoidClass.toOneMemClass`：∀ {S : Type u_3} {M : outParam (Type u_4)}
 {inst : MulOneClass M} {inst_1 : SetLike S M} [self : SubmonoidClass S M],   On
eMemClass S M
· 使用定理 `SubgroupClass.toSubmonoidClass`：∀ {S : Type u_3} {G : outParam (Type u_4
)} {inst : DivInvMonoid G} {inst_1 : SetLike S G} [self : SubgroupClass S G],   
SubmonoidClass S G
· 使用定理 `Subgroup.instSubgroupClass`：∀ {G : Type u_1} [inst : Group G], SubgroupC
lass (Subgroup G) G
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用引理 `one_smul`：one_smul (b : α) : (1 : M) • b = b
· 使用定理 `MulMemClass.mul_mem`：∀ {S : Type u_3} {M : outParam (Type u_4)} {inst : 
Mul M} {inst_1 : SetLike S M} [self : MulMemClass S M] {s : S}   {a b : M}, a ∈ 
s → b ∈ s…
· 使用定理 `SubmonoidClass.toMulMemClass`：∀ {S : Type u_3} {M : outParam (Type u_4)}
 {inst : MulOneClass M} {inst_1 : SetLike S M} [self : SubmonoidClass S M],   Mu
lMemClass S M
· 使用定理 `Submonoid.mul_mem`：∀ {M : Type u_1} [inst : MulOneClass M] (S : Submonoi
d M) {x y : M}, x ∈ S → y ∈ S → x * y ∈ S
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Submonoid.mk_mul_mk`：mk_mul_mk (x y : M) (hx : x in S) (hy : y in S) : (
⟨x, hx⟩ : S) * ⟨y, hy⟩ = ⟨x * y, S.mul_mem hx hy⟩
· 使用定理 `SemigroupAction.mul_smul`：∀ {α : Type u_9} {β : Type u_10} {inst : Semig
roup α} [self : SemigroupAction α β] (x y : α) (b : β),   (x * y) • b = x • y • 
b
-/
lemma InvariantForm.apply_weylGroup_smul {B : P.InvariantForm} (g : P.weylGroup) (x y : M) :
    B.form (g • x) (g • y) = B.form x y := by
  revert x y
  obtain ⟨g, hg⟩ := g
  induction hg using weylGroup.induction with
  | mem i => simp
  | one => simp
  | mul g₁ g₂ hg₁ hg₂ hg₁' hg₂' =>
    intro x y
    rw [← Submonoid.mk_mul_mk _ _ _ hg₁ hg₂, mul_smul, mul_smul, hg₁', hg₂']

end RootPairing

