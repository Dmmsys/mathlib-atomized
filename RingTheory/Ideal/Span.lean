/-
Copyright (c) 2018 Kenny Lau. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Kenny Lau, Chris Hughes, Mario Carneiro
-/
module

public import Mathlib.Algebra.GroupWithZero.Associated
public import Mathlib.Algebra.Ring.Idempotent
public import Mathlib.LinearAlgebra.Span.Basic
public import Mathlib.RingTheory.Ideal.Lattice
public import Mathlib.Tactic.Ring

/-!
# Ideals generated by a set of elements

This file defines `Ideal.span s` as the ideal generated by the subset `s` of the ring.

## TODO

Support right ideals, and two-sided ideals over non-commutative rings.
-/

@[expose] public section

universe u

variable {α : Type u}

open Set Pointwise

/-- A ring is a principal ideal ring if all (left) ideals are principal. -/
@[mk_iff]
/-
**IsPrincipalIdealRing** 是 Mathlib 中的一个归纳类型，位于命名空间 ``。
形式化陈述：(R : Type u) → [Semiring R] → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A ring is a principal ideal ring if all (left) ideals are principal.
-/
class IsPrincipalIdealRing (R : Type u) [Semiring R] : Prop where
  principal : ∀ S : Ideal R, S.IsPrincipal

attribute [instance] IsPrincipalIdealRing.principal

namespace Ideal

section Semiring

variable [Semiring α] (I : Ideal α)

/-- The ideal generated by a subset of a ring -/
/-
**Ideal.span** 是 Mathlib 中的一个缩写定义，位于命名空间 `Ideal`。
形式化陈述：span (s : Set α) : Ideal α
参数：s : Set α。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The ideal generated by a subset of a ring
-/
abbrev span (s : Set α) : Ideal α :=
  Submodule.span α s
/-
**Ideal.submodule_span_eq** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：submodule_span_eq {s : Set α} : Submodule.span α s = Ideal.span s
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem submodule_span_eq {s : Set α} : Submodule.span α s = Ideal.span s := by simp
/-
**Ideal.span_empty** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_empty : span (∅ : Set α) = ⊥
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Submodule.span_empty`：span_empty : span R (∅ : Set M) = ⊥
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem span_empty : span (∅ : Set α) = ⊥ := by simp
/-
**Ideal.span_univ** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_univ : span (Set.univ : Set α) = ⊤
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Submodule.span_univ`：span_univ : span R (univ : Set M) = ⊤
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem span_univ : span (Set.univ : Set α) = ⊤ := by simp
/-
**Ideal.span_union** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_union (s t : Set α) : span (s union t) = span s ⊔ span t
参数：s t : Set α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submodule.span_union`：span_union (s t : Set M) : span R (s union t) = sp
an R s ⊔ span R t
-/
theorem span_union (s t : Set α) : span (s ∪ t) = span s ⊔ span t :=
  Submodule.span_union _ _
/-
**Ideal.span_iUnion** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_iUnion {ι} (s : ι -> Set α) : span (⋃ i, s i) = ⨆ i, span (s i)
参数：s : ι -> Set α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submodule.span_iUnion`：span_iUnion {ι} (s : ι -> Set M) : span R (⋃ i, s
 i) = ⨆ i, span R (s i)
-/
theorem span_iUnion {ι} (s : ι → Set α) : span (⋃ i, s i) = ⨆ i, span (s i) :=
  Submodule.span_iUnion _
/-
**Ideal.iSup_eq_span** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：iSup_eq_span {ι} (p : ι -> Ideal α) : ⨆ i, p i = span (⋃ i, ↑(p i))
参数：p : ι -> Ideal α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submodule.iSup_eq_span`：iSup_eq_span {ι : Sort*} (p : ι -> Submodule R M
) : ⨆ i, p i = span R (⋃ i, ↑(p i))
-/
theorem iSup_eq_span {ι} (p : ι → Ideal α) :
    ⨆ i, p i = span (⋃ i, ↑(p i)) :=
  Submodule.iSup_eq_span p
/-
**Ideal.mem_span** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：mem_span {s : Set α} (x) : x in span s ↔ forall p : Ideal α, s subseteq p 
-> x in p
参数：x。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.mem_iInter₂`：mem_iInter₂ {x : γ} {s : forall i, κ i -> Set γ} : (x i
n ⋂ (i) (j), s i j) ↔ forall i j, x in s i j
-/
theorem mem_span {s : Set α} (x) : x ∈ span s ↔ ∀ p : Ideal α, s ⊆ p → x ∈ p :=
  mem_iInter₂
/-
**Ideal.subset_span** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：subset_span {s : Set α} : s subseteq span s
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submodule.subset_span`：subset_span : s subseteq span R s
-/
theorem subset_span {s : Set α} : s ⊆ span s :=
  Submodule.subset_span
/-
**Ideal.span_le** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_le {s : Set α} {I} : span s <= I ↔ s subseteq I
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submodule.span_le`：span_le {p} : span R s <= p ↔ s subseteq p
-/
theorem span_le {s : Set α} {I} : span s ≤ I ↔ s ⊆ I :=
  Submodule.span_le

@[gcongr]
/-
**Ideal.span_mono** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_mono {s t : Set α} : s subseteq t -> span s <= span t
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submodule.span_mono`：∀ {R : Type u_1} {M : Type u_4} [inst : Semiring R]
 [inst_1 : AddCommMonoid M] [inst_2 : _root_.Module R M]   {s t : Set M}, s ⊆ t 
→ Submodu…
-/
theorem span_mono {s t : Set α} : s ⊆ t → span s ≤ span t :=
  Submodule.span_mono
/-
**Ideal.span_eq** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_eq : span (I : Set α) = I
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Submodule.span_coe_eq_restrictScalars`：span_coe_eq_restrictScalars [Semi
ring S] [SMul S R] [Module S M] [IsScalarTower S R M] : span S (p : Set M) = p.r
estrictScalars S
· 使用定理 `Submodule.restrictScalars_self`：restrictScalars_self (V : Submodule R M)
 : V.restrictScalars R = V
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem span_eq : span (I : Set α) = I := by simp

@[simp]
/-
**Ideal.span_singleton_one** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_singleton_one : span ({1} : Set α) = ⊤
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Ideal.eq_top_iff_one`：eq_top_iff_one : I = ⊤ ↔ (1 : α) in I
· 使用定理 `Ideal.subset_span`：subset_span {s : Set α} : s subseteq span s
· 使用定理 `Set.mem_singleton`：mem_singleton (a : α) : a in ({a} : Set α)
-/
theorem span_singleton_one : span ({1} : Set α) = ⊤ :=
  (eq_top_iff_one _).2 <| subset_span <| mem_singleton _
/-
**Ideal.isCompactElement_top** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：isCompactElement_top : IsCompactElement (⊤ : Ideal α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Submodule.singleton_span_isCompactElement`：singleton_span_isCompactEleme
nt (x : M) : IsCompactElement (span R {x} : Submodule R M)
-/
theorem isCompactElement_top : IsCompactElement (⊤ : Ideal α) := by
  simpa only [← span_singleton_one] using Submodule.singleton_span_isCompactElement 1
/-
**Ideal.mem_span_insert** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：mem_span_insert {s : Set α} {x y} : x in span (insert y s) ↔ exists a, exi
sts z in span s, x = a * y + z
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submodule.mem_span_insert`：mem_span_insert {y} : x in span R (insert y s
) ↔ exists a : R, exists z in span R s, x = a • y + z
-/
theorem mem_span_insert {s : Set α} {x y} :
    x ∈ span (insert y s) ↔ ∃ a, ∃ z ∈ span s, x = a * y + z :=
  Submodule.mem_span_insert
/-
**Ideal.mem_span_range_self** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：mem_span_range_self {β : Type*} {f : β -> α} {x : β} : f x in span (range 
f)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.subset_span`：subset_span {s : Set α} : s subseteq span s
· 使用定理 `Set.mem_range_self`：∀ {α : Type u} {ι : Sort u_1} {f : ι → α} (i : ι), f
 i ∈ Set.range f
-/
theorem mem_span_range_self {β : Type*} {f : β → α} {x : β} : f x ∈ span (range f) :=
  subset_span (mem_range_self x)
/-
**Ideal.mem_span_singleton'** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：mem_span_singleton' {x y : α} : x in span ({y} : Set α) ↔ exists a, a * y 
= x
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submodule.mem_span_singleton`：mem_span_singleton {y : M} : x in R ∙ y ↔ 
exists a : R, a • y = x
-/
theorem mem_span_singleton' {x y : α} : x ∈ span ({y} : Set α) ↔ ∃ a, a * y = x :=
  Submodule.mem_span_singleton
/-
**Ideal.mem_span_singleton_self** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：mem_span_singleton_self (x : α) : x in span ({x} : Set α)
参数：x : α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
-/
theorem mem_span_singleton_self (x : α) : x ∈ span ({x} : Set α) := by simp
/-
**Ideal.span_singleton_le_iff_mem** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_singleton_le_iff_mem {x : α} : span {x} <= I ↔ x in I
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
theorem span_singleton_le_iff_mem {x : α} : span {x} ≤ I ↔ x ∈ I := by simp
/-
**Ideal.span_singleton_mul_left_unit** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_singleton_mul_left_unit {a : α} (h2 : IsUnit a) (x : α) : span ({a * 
x} : Set α) = span {x}
参数：h2 : IsUnit a；x : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submodule.span_singleton_smul_eq`：span_singleton_smul_eq {r : R} (hr : I
sUnit r) (x : M) : R ∙ r • x = R ∙ x
-/
theorem span_singleton_mul_left_unit {a : α} (h2 : IsUnit a) (x : α) :
    span ({a * x} : Set α) = span {x} :=
  Submodule.span_singleton_smul_eq h2 x
/-
**Ideal.span_insert** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_insert (x) (s : Set α) : span (insert x s) = span ({x} : Set α) ⊔ spa
n s
参数：x；s : Set α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submodule.span_insert`：span_insert (x) (s : Set M) : span R (insert x s)
 = R ∙ x ⊔ span R s
-/
theorem span_insert (x) (s : Set α) : span (insert x s) = span ({x} : Set α) ⊔ span s :=
  Submodule.span_insert x s
/-
**Ideal.span_eq_bot** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_eq_bot {s : Set α} : span s = ⊥ ↔ forall x in s, (x : α) = 0
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
theorem span_eq_bot {s : Set α} : span s = ⊥ ↔ ∀ x ∈ s, (x : α) = 0 := by simp
/-
**Ideal.span_singleton_eq_bot** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_singleton_eq_bot {x} : span ({x} : Set α) = ⊥ ↔ x = 0
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
theorem span_singleton_eq_bot {x} : span ({x} : Set α) = ⊥ ↔ x = 0 := by simp
/-
**Ideal.span_singleton_zero** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_singleton_zero : span ({0} : Set α) = ⊥
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Submodule.span_zero_singleton`：span_zero_singleton : R ∙ (0 : M) = ⊥
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem span_singleton_zero : span ({0} : Set α) = ⊥ := by simp
/-
**Ideal.span_singleton_ne_top** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_singleton_ne_top {α : Type*} [CommSemiring α] {x : α} (hx : ¬IsUnit x
) : Ideal.span ({x} : Set α) != ⊤
参数：hx : ¬IsUnit x。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Ideal.ne_top_iff_one`：ne_top_iff_one : I != ⊤ ↔ (1 : α) ∉ I
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Ideal.mem_span_singleton'`：mem_span_singleton' {x y : α} : x in span ({y
} : Set α) ↔ exists a, a * y = x
· 使用定理 `mul_comm`：mul_comm : forall a b : G, a * b = b * a
-/
theorem span_singleton_ne_top {α : Type*} [CommSemiring α] {x : α} (hx : ¬IsUnit x) :
    Ideal.span ({x} : Set α) ≠ ⊤ :=
  (Ideal.ne_top_iff_one _).mpr fun h1 =>
    let ⟨y, hy⟩ := Ideal.mem_span_singleton'.mp h1
    hx ⟨⟨x, y, mul_comm y x ▸ hy, hy⟩, rfl⟩
/-
**Ideal.span_zero** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_zero : span (0 : Set α) = ⊥
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Submodule.span_zero`：span_zero : span R (0 : Set M) = ⊥
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem span_zero : span (0 : Set α) = ⊥ := by simp
/-
**Ideal.span_insert_zero** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_insert_zero {s : Set α} : span (insert (0 : α) s) = span s
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Submodule.span_insert_zero`：span_insert_zero : span R (insert (0 : M) s)
 = span R s
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem span_insert_zero {s : Set α} : span (insert (0 : α) s) = span s := by simp
/-
**Ideal.span_sdiff_singleton_zero** 是 Mathlib 中的一个引理，位于命名空间 `Ideal`。
形式化陈述：span_sdiff_singleton_zero {s : Set α} : span (s \ {0}) = span s
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `Submodule.span_sdiff_singleton_zero`：span_sdiff_singleton_zero : span R 
(s \ {0}) = span R s
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma span_sdiff_singleton_zero {s : Set α} : span (s \ {0}) = span s := by simp

@[simp]
/-
**Ideal.span_one** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_one : span (1 : Set α) = ⊤
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Set.singleton_one`：singleton_one : ({1} : Set α) = 1
· 使用定理 `Ideal.span_singleton_one`：span_singleton_one : span ({1} : Set α) = ⊤
-/
theorem span_one : span (1 : Set α) = ⊤ := by rw [← Set.singleton_one, span_singleton_one]
/-
**Ideal.span_eq_top_iff_finite** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_eq_top_iff_finite (s : Set α) : span s = ⊤ ↔ exists s' : Finset α, ↑s
' subseteq s ∧ span (s' : Set α) = ⊤
参数：s : Set α。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Submodule.mem_span_finite_of_mem_span`：mem_span_finite_of_mem_span {S : 
Set M} {x : M} (hx : x in span R S) : exists T : Finset M, ↑T subseteq S ∧ x in 
span R (T : Set M)
· 使用定理 `Ideal.span_mono`：span_mono {s t : Set α} : s subseteq t -> span s <= spa
n t
-/
theorem span_eq_top_iff_finite (s : Set α) :
    span s = ⊤ ↔ ∃ s' : Finset α, ↑s' ⊆ s ∧ span (s' : Set α) = ⊤ := by
  simp_rw [eq_top_iff_one]
  exact ⟨Submodule.mem_span_finite_of_mem_span, fun ⟨s', h₁, h₂⟩ => span_mono h₁ h₂⟩
/-
**Ideal.mem_span_singleton_sup** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：mem_span_singleton_sup {x y : α} {I : Ideal α} : x in Ideal.span {y} ⊔ I ↔
 exists a : α, exists b in I, a * y + b = x
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Submodule.mem_sup`：mem_sup : x in p ⊔ p' ↔ exists y in p, exists z in p'
, y + z = x
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Ideal.mem_span_singleton'`：mem_span_singleton' {x y : α} : x in span ({y
} : Set α) ↔ exists a, a * y = x
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
-/
theorem mem_span_singleton_sup {x y : α} {I : Ideal α} :
    x ∈ Ideal.span {y} ⊔ I ↔ ∃ a : α, ∃ b ∈ I, a * y + b = x := by
  rw [Submodule.mem_sup]
  constructor
  · rintro ⟨ya, hya, b, hb, rfl⟩
    obtain ⟨a, rfl⟩ := mem_span_singleton'.mp hya
    exact ⟨a, b, hb, rfl⟩
  · rintro ⟨a, b, hb, rfl⟩
    exact ⟨a * y, Ideal.mem_span_singleton'.mpr ⟨a, rfl⟩, b, hb, rfl⟩

/-- The ideal generated by an arbitrary binary relation. -/
/-
**Ideal.ofRel** 是 Mathlib 中的一个定义，位于命名空间 `Ideal`。
形式化陈述：ofRel (r : α -> α -> Prop) : Ideal α
参数：r : α -> α -> Prop。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The ideal generated by an arbitrary binary relation.
-/
def ofRel (r : α → α → Prop) : Ideal α :=
  Submodule.span α { x | ∃ a b, r a b ∧ x + b = a }
/-
**Ideal.zero_ne_one_of_proper** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：zero_ne_one_of_proper {I : Ideal α} (h : I != ⊤) : (0 : α) != 1
参数：h : I != ⊤。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Ideal.ne_top_iff_one`：ne_top_iff_one : I != ⊤ ↔ (1 : α) ∉ I
· 使用定理 `Ideal.zero_mem`：∀ {α : Type u} [inst : Semiring α] (I : Ideal α), 0 ∈ I
-/
theorem zero_ne_one_of_proper {I : Ideal α} (h : I ≠ ⊤) : (0 : α) ≠ 1 := fun hz =>
  I.ne_top_iff_one.1 h <| hz ▸ I.zero_mem
/-
**Ideal.span_pair_comm** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_comm {x y : α} : (span {x, y} : Ideal α) = span {y, x}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.span_insert`：span_insert (x) (s : Set α) : span (insert x s) = spa
n ({x} : Set α) ⊔ span s
· 使用定理 `sup_comm`：sup_comm (a b : α) : a ⊔ b = b ⊔ a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem span_pair_comm {x y : α} : (span {x, y} : Ideal α) = span {y, x} := by
  simp only [span_insert, sup_comm]
/-
**Ideal.mem_span_pair** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：mem_span_pair {x y z : α} : z in span ({x, y} : Set α) ↔ exists a b, a * x
 + b * y = z
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submodule.mem_span_pair`：mem_span_pair {x y z : M} : z in span R ({x, y}
 : Set M) ↔ exists a b : R, a • x + b • y = z
-/
theorem mem_span_pair {x y z : α} : z ∈ span ({x, y} : Set α) ↔ ∃ a b, a * x + b * y = z :=
  Submodule.mem_span_pair

@[simp]
/-
**Ideal.span_pair_zero** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_zero (x : α) : span {x, 0} = span {x}
参数：x : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.span_pair_comm`：span_pair_comm {x y : α} : (span {x, y} : Ideal α)
 = span {y, x}
· 使用定理 `Ideal.span_insert_zero`：span_insert_zero {s : Set α} : span (insert (0 :
 α) s) = span s
-/
theorem span_pair_zero (x : α) : span {x, 0} = span {x} := by
  rw [span_pair_comm, span_insert_zero]

variable {ι : Type*}
/-
**Ideal.span_range_eq_iSup** 是 Mathlib 中的一个引理，位于命名空间 `Ideal`。
形式化陈述：span_range_eq_iSup (x : ι -> α) : span (range x) = ⨆ i, span {x i}
参数：x : ι -> α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.span_iUnion`：span_iUnion {ι} (s : ι -> Set α) : span (⋃ i, s i) = 
⨆ i, span (s i)
· 使用定理 `Set.iUnion_singleton_eq_range`：iUnion_singleton_eq_range (f : α -> β) : 
⋃ x : α, {f x} = range f
-/
lemma span_range_eq_iSup (x : ι → α) : span (range x) = ⨆ i, span {x i} := by
  rw [← span_iUnion, iUnion_singleton_eq_range]
/-
**Ideal.span_range_eq_span_range_support** 是 Mathlib 中的一个引理，位于命名空间 `Ideal`。
形式化陈述：span_range_eq_span_range_support (x : ι -> α) : span (range x) = span (ran
ge fun i : x.support => x i.val)
参数：x : ι -> α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `Ideal.span_sdiff_singleton_zero`：span_sdiff_singleton_zero {s : Set α} :
 span (s \ {0}) = span s
· 使用定理 `Function.support.eq_1`：∀ {ι : Type u_1} {M : Type u_3} [inst : Zero M] (
f : ι → M), Function.support f = {x | f x ≠ 0}
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Set.mem_ofPred`：mem_ofPred {a : α} {p : α -> Prop} : a in { x | p x } ↔ 
p a
-/
lemma span_range_eq_span_range_support (x : ι → α) :
    span (range x) = span (range fun i : x.support ↦ x i.val) := by
  rw [← span_sdiff_singleton_zero (s := range x), Function.support]
  congr
  ext1 a
  simp only [mem_sdiff, mem_range, mem_singleton_iff]
  exact ⟨fun ⟨⟨i, hi⟩, ha⟩ ↦ ⟨⟨i, mem_ofPred.mpr (hi ▸ ha)⟩, hi⟩,
    fun ⟨j, hj⟩ ↦ ⟨⟨j.val, hj⟩, by grind⟩⟩

end Semiring

section CommSemiring

variable [CommSemiring α] (I : Ideal α)

/-
**Ideal.mem_span_singleton** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：mem_span_singleton {x y : α} : x in span ({y} : Set α) ↔ y ∣ x
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `Ideal.mem_span_singleton'`：mem_span_singleton' {x y : α} : x in span ({y
} : Set α) ↔ exists a, a * y = x
· 使用定理 `exists_congr`：∀ {α : Sort u_1} {p q : α → Prop}, (∀ (a : α), p a ↔ q a) 
→ ((∃ a, p a) ↔ ∃ a, q a)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `eq_comm`：∀ {α : Sort u_1} {a b : α}, a = b ↔ b = a
· 使用定理 `mul_comm`：mul_comm : forall a b : G, a * b = b * a
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem mem_span_singleton {x y : α} : x ∈ span ({y} : Set α) ↔ y ∣ x :=
  mem_span_singleton'.trans <| exists_congr fun _ => by rw [eq_comm, mul_comm]
/-
**Ideal.span_singleton_le_span_singleton** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_singleton_le_span_singleton {x y : α} : span ({x} : Set α) <= span ({
y} : Set α) ↔ y ∣ x
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.trans`：∀ {a b c : Prop}, (a ↔ b) → (b ↔ c) → (a ↔ c)
· 使用定理 `Ideal.span_le`：span_le {s : Set α} {I} : span s <= I ↔ s subseteq I
· 使用定理 `Set.singleton_subset_iff`：singleton_subset_iff {a : α} {s : Set α} : {a}
 subseteq s ↔ a in s
· 使用定理 `Ideal.mem_span_singleton`：mem_span_singleton {x y : α} : x in span ({y} 
: Set α) ↔ y ∣ x
-/
theorem span_singleton_le_span_singleton {x y : α} :
    span ({x} : Set α) ≤ span ({y} : Set α) ↔ y ∣ x :=
  span_le.trans <| singleton_subset_iff.trans mem_span_singleton
/-
**Ideal.span_singleton_eq_span_singleton** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_singleton_eq_span_singleton {α : Type u} [CommSemiring α] [IsDomain α
] {x y : α} : span ({x} : Set α) = span ({y} : Set α) ↔ Associated x y
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `dvd_dvd_iff_associated`：dvd_dvd_iff_associated [MonoidWithZero M] [IsLef
tCancelMulZero M] {a b : M} : a ∣ b ∧ b ∣ a ↔ a ~ᵤ b
· 使用定理 `IsCancelMulZero.toIsLeftCancelMulZero`：∀ {M₀ : Type u} {inst : Mul M₀} {
inst_1 : Zero M₀} [self : IsCancelMulZero M₀], IsLeftCancelMulZero M₀
· 使用定理 `IsDomain.toIsCancelMulZero`：∀ {α : Type u} {inst : Semiring α} [self : I
sDomain α], IsCancelMulZero α
· 使用引理 `le_antisymm_iff`：le_antisymm_iff : a = b ↔ a <= b ∧ b <= a
· 使用定理 `and_comm`：∀ {a b : Prop}, a ∧ b ↔ b ∧ a
· 使用定理 `and_congr`：∀ {a c b d : Prop}, (a ↔ c) → (b ↔ d) → (a ∧ b ↔ c ∧ d)
· 使用定理 `Ideal.span_singleton_le_span_singleton`：span_singleton_le_span_singleton
 {x y : α} : span ({x} : Set α) <= span ({y} : Set α) ↔ y ∣ x
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem span_singleton_eq_span_singleton {α : Type u} [CommSemiring α] [IsDomain α] {x y : α} :
    span ({x} : Set α) = span ({y} : Set α) ↔ Associated x y := by
  rw [← dvd_dvd_iff_associated, le_antisymm_iff, and_comm]
  apply and_congr <;> rw [span_singleton_le_span_singleton]

@[simp]
/-
**Ideal.span_pair_eq_span_left_iff_dvd** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_eq_span_left_iff_dvd {x y : α} : span {x, y} = span {x} ↔ x ∣ y
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.span_insert`：span_insert (x) (s : Set α) : span (insert x s) = spa
n ({x} : Set α) ⊔ span s
· 使用定理 `sup_eq_left`：sup_eq_left : a ⊔ b = a ↔ b <= a
· 使用定理 `Ideal.span_singleton_le_span_singleton`：span_singleton_le_span_singleton
 {x y : α} : span ({x} : Set α) <= span ({y} : Set α) ↔ y ∣ x
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem span_pair_eq_span_left_iff_dvd {x y : α} : span {x, y} = span {x} ↔ x ∣ y := by
  rw [Ideal.span_insert, sup_eq_left, span_singleton_le_span_singleton]

@[simp]
/-
**Ideal.span_pair_eq_span_right_iff_dvd** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_eq_span_right_iff_dvd {x y : α} : span {x, y} = span {y} ↔ y ∣ x
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.span_insert`：span_insert (x) (s : Set α) : span (insert x s) = spa
n ({x} : Set α) ⊔ span s
· 使用定理 `sup_eq_right`：sup_eq_right : a ⊔ b = b ↔ a <= b
· 使用定理 `Ideal.span_singleton_le_span_singleton`：span_singleton_le_span_singleton
 {x y : α} : span ({x} : Set α) <= span ({y} : Set α) ↔ y ∣ x
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem span_pair_eq_span_right_iff_dvd {x y : α} : span {x, y} = span {y} ↔ y ∣ x := by
  rw [Ideal.span_insert, sup_eq_right, span_singleton_le_span_singleton]
/-
**Ideal.span_singleton_mul_right_unit** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_singleton_mul_right_unit {a : α} (h2 : IsUnit a) (x : α) : span ({x *
 a} : Set α) = span {x}
参数：h2 : IsUnit a；x : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mul_comm`：mul_comm : forall a b : G, a * b = b * a
· 使用定理 `Ideal.span_singleton_mul_left_unit`：span_singleton_mul_left_unit {a : α}
 (h2 : IsUnit a) (x : α) : span ({a * x} : Set α) = span {x}
-/
theorem span_singleton_mul_right_unit {a : α} (h2 : IsUnit a) (x : α) :
    span ({x * a} : Set α) = span {x} := by rw [mul_comm, span_singleton_mul_left_unit h2]

@[simp]
/-
**Ideal.span_singleton_eq_top** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_singleton_eq_top {x} : span ({x} : Set α) = ⊤ ↔ IsUnit x
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `isUnit_iff_dvd_one`：isUnit_iff_dvd_one {x : α} : IsUnit x ↔ x ∣ 1
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.span_singleton_le_span_singleton`：span_singleton_le_span_singleton
 {x y : α} : span ({x} : Set α) <= span ({y} : Set α) ↔ y ∣ x
· 使用定理 `Ideal.span_singleton_one`：span_singleton_one : span ({1} : Set α) = ⊤
· 使用定理 `eq_top_iff`：eq_top_iff : a = ⊤ ↔ ⊤ <= a
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem span_singleton_eq_top {x} : span ({x} : Set α) = ⊤ ↔ IsUnit x := by
  rw [isUnit_iff_dvd_one, ← span_singleton_le_span_singleton, span_singleton_one, eq_top_iff]
/-
**Ideal.factors_decreasing** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：factors_decreasing [IsDomain α] (b₁ b₂ : α) (h₁ : b₁ != 0) (h₂ : ¬IsUnit b
₂) : span ({b₁ * b₂} : Set α) < span {b₁}
参数：b₁ b₂ : α；h₁ : b₁ != 0；h₂ : ¬IsUnit b₂。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `lt_of_le_not_ge`：lt_of_le_not_ge (hab : a <= b) (hba : ¬ b <= a) : a < b
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Ideal.span_le`：span_le {s : Set α} {I} : span s <= I ↔ s subseteq I
· 使用定理 `Set.singleton_subset_iff`：singleton_subset_iff {a : α} {s : Set α} : {a}
 subseteq s ↔ a in s
· 使用定理 `Ideal.mem_span_singleton`：mem_span_singleton {x y : α} : x in span ({y} 
: Set α) ↔ y ∣ x
· 使用定理 `isUnit_of_dvd_one`：isUnit_of_dvd_one {a : α} (h : a ∣ 1) : IsUnit (a : α
)
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `mul_dvd_mul_iff_left`：mul_dvd_mul_iff_left [MonoidWithZero α] [IsLeftCan
celMulZero α] {a b c : α} (ha : a != 0) : a * b ∣ a * c ↔ b ∣ c
· 使用定理 `IsCancelMulZero.toIsLeftCancelMulZero`：∀ {M₀ : Type u} {inst : Mul M₀} {
inst_1 : Zero M₀} [self : IsCancelMulZero M₀], IsLeftCancelMulZero M₀
· 使用定理 `IsDomain.toIsCancelMulZero`：∀ {α : Type u} {inst : Semiring α} [self : I
sDomain α], IsCancelMulZero α
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mul_one`：mul_one : forall a : M, a * 1 = a
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.span_singleton_le_span_singleton`：span_singleton_le_span_singleton
 {x y : α} : span ({x} : Set α) <= span ({y} : Set α) ↔ y ∣ x
-/
theorem factors_decreasing [IsDomain α] (b₁ b₂ : α) (h₁ : b₁ ≠ 0) (h₂ : ¬IsUnit b₂) :
    span ({b₁ * b₂} : Set α) < span {b₁} :=
  lt_of_le_not_ge
    (Ideal.span_le.2 <| singleton_subset_iff.2 <| Ideal.mem_span_singleton.2 ⟨b₂, rfl⟩) fun h =>
    h₂ <| isUnit_of_dvd_one <|
        (mul_dvd_mul_iff_left h₁).1 <| by rwa [mul_one, ← Ideal.span_singleton_le_span_singleton]

variable {I} in
/-
**Ideal.mem_iff_of_associated** 是 Mathlib 中的一个引理，位于命名空间 `Ideal`。
形式化陈述：mem_iff_of_associated {x y : α} (h : Associated x y) : x in I ↔ y in I
参数：h : Associated x y。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Associated.symm`：∀ {M : Type u_1} [inst : Monoid M] {x y : M}, Associate
d x y → Associated y x
· 使用定理 `Ideal.mul_unit_mem_iff_mem`：mul_unit_mem_iff_mem {x y : α} (hy : IsUnit 
y) : x * y in I ↔ x in I
· 使用定理 `Units.isUnit`：∀ {M : Type u_1} [inst : Monoid M] (u : Mˣ), IsUnit ↑u
-/
lemma mem_iff_of_associated {x y : α} (h : Associated x y) : x ∈ I ↔ y ∈ I := by
  obtain ⟨u, rfl⟩ := h.symm
  exact I.mul_unit_mem_iff_mem u.isUnit

end CommSemiring

section Ring

variable [Ring α] (x y : α) (s : Set α)

/-
**Ideal.mem_span_insert'** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：mem_span_insert' : x in span (insert y s) ↔ exists a, x + a * y in span s
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Submodule.mem_span_insert'`：mem_span_insert' {x y} {s : Set M} : x in sp
an R (insert y s) ↔ exists a : R, x + a • y in span R s
-/
theorem mem_span_insert' : x ∈ span (insert y s) ↔ ∃ a, x + a * y ∈ span s :=
  Submodule.mem_span_insert'

@[simp]
/-
**Ideal.span_singleton_neg** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_singleton_neg : span {-x} = span {x}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.ext`：ext {I J : Ideal α} (h : forall x, x in I ↔ x in J) : I = J
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `neg_mul_comm`：neg_mul_comm (a b : α) : -a * b = a * -b
· 使用定理 `neg_mul_neg`：neg_mul_neg (a b : α) : -a * -b = a * b
-/
theorem span_singleton_neg : span {-x} = span {x} := by
  ext
  simp only [mem_span_singleton']
  exact ⟨fun ⟨y, h⟩ => ⟨-y, h ▸ neg_mul_comm y x⟩, fun ⟨y, h⟩ => ⟨-y, h ▸ neg_mul_neg y x⟩⟩

@[simp]
/-
**Ideal.span_insert_neg** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_insert_neg : span (insert (-x) s) = span (insert x s)
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.span_insert`：span_insert (x) (s : Set α) : span (insert x s) = spa
n ({x} : Set α) ⊔ span s
· 使用定理 `Ideal.span_singleton_neg`：span_singleton_neg : span {-x} = span {x}
-/
theorem span_insert_neg : span (insert (-x) s) = span (insert x s) := by
  rw [span_insert, span_singleton_neg, span_insert]

@[simp]
/-
**Ideal.span_pair_neg** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_neg : span {x, -y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.span_pair_comm`：span_pair_comm {x y : α} : (span {x, y} : Ideal α)
 = span {y, x}
· 使用定理 `Ideal.span_insert_neg`：span_insert_neg : span (insert (-x) s) = span (in
sert x s)
-/
theorem span_pair_neg : span {x, -y} = span {x, y} := by
  rw [span_pair_comm, span_insert_neg, span_pair_comm]

@[simp]
/-
**Ideal.span_pair_add_right** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_add_right : span {x + y, y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.ext`：ext {I J : Ideal α} (h : forall x, x in I ↔ x in J) : I = J
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `add_comm`：∀ {G : Type u_1} [inst : AddCommMagma G] (a b : G), a + b = b 
+ a
· 使用定理 `add_mul`：add_mul {d : R} (_ : (a₁ : R) * b = c₁) (_ : a₂ * b = c₂) (_ : 
c₁ + c₂ = d) : (a₁ + a₂) * b = d
· 使用定理 `Distrib.rightDistribClass`：∀ (R : Type u_1) [inst : Distrib R], RightDis
tribClass R
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `add_assoc`：∀ {G : Type u_1} [inst : AddSemigroup G] (a b c : G), a + b +
 c = a + (b + c)
· 使用定理 `mul_add`：mul_add {d : R} (_ : (a : R) * b₁ = c₁) (_ : a * b₂ = c₂) (_ : 
c₁ + 0 + c₂ = d) : a * (b₁ + b₂) = d
· 使用定理 `Distrib.leftDistribClass`：∀ (R : Type u_1) [inst : Distrib R], LeftDistr
ibClass R
· 使用定理 `sub_mul`：∀ {α : Type u} [inst : NonUnitalNonAssocRing α] (a b c : α), (a
 - b) * c = a * c - b * c
· 使用定理 `add_add_sub_cancel`：∀ {G : Type u_3} [inst : AddCommGroup G] (a b c : G)
, a + c + (b - c) = a + b
-/
theorem span_pair_add_right : span {x + y, y} = span {x, y} := by
  ext
  simp_rw [mem_span_pair]
  exact ⟨fun ⟨a, b, h⟩ => ⟨a, b + a, by rw [add_comm b, add_mul, ← add_assoc, ← mul_add, h]⟩,
    fun ⟨a, b, h⟩ => ⟨a, b - a, by rw [mul_add, sub_mul, add_add_sub_cancel, h]⟩⟩

@[simp]
/-
**Ideal.span_pair_add_left** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_add_left : span {x, y + x} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.span_pair_comm`：span_pair_comm {x y : α} : (span {x, y} : Ideal α)
 = span {y, x}
· 使用定理 `Ideal.span_pair_add_right`：span_pair_add_right : span {x + y, y} = span 
{x, y}
-/
theorem span_pair_add_left : span {x, y + x} = span {x, y} := by
  rw [span_pair_comm, span_pair_add_right, span_pair_comm]

@[simp]
/-
**Ideal.span_pair_right_add** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_right_add : span {y + x, y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `add_comm`：∀ {G : Type u_1} [inst : AddCommMagma G] (a b : G), a + b = b 
+ a
· 使用定理 `Ideal.span_pair_add_right`：span_pair_add_right : span {x + y, y} = span 
{x, y}
-/
theorem span_pair_right_add : span {y + x, y} = span {x, y} := by
  rw [add_comm, span_pair_add_right]

@[simp]
/-
**Ideal.span_pair_left_add** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_left_add : span {x, x + y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `add_comm`：∀ {G : Type u_1} [inst : AddCommMagma G] (a b : G), a + b = b 
+ a
· 使用定理 `Ideal.span_pair_add_left`：span_pair_add_left : span {x, y + x} = span {x
, y}
-/
theorem span_pair_left_add : span {x, x + y} = span {x, y} := by
  rw [add_comm, span_pair_add_left]

@[simp]
/-
**Ideal.span_pair_sub_right** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_sub_right : span {x - y, y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.span_pair_add_right`：span_pair_add_right : span {x + y, y} = span 
{x, y}
· 使用定理 `sub_add_cancel`：∀ {G : Type u_1} [inst : AddGroup G] (a b : G), a - b + 
b = a
-/
theorem span_pair_sub_right : span {x - y, y} = span {x, y} := by
  rw [← span_pair_add_right, sub_add_cancel]

@[simp]
/-
**Ideal.span_pair_sub_left** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_sub_left : span {x, y - x} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.span_pair_add_left`：span_pair_add_left : span {x, y + x} = span {x
, y}
· 使用定理 `sub_add_cancel`：∀ {G : Type u_1} [inst : AddGroup G] (a b : G), a - b + 
b = a
-/
theorem span_pair_sub_left : span {x, y - x} = span {x, y} := by
  rw [← span_pair_add_left, sub_add_cancel]

@[simp]
/-
**Ideal.span_pair_sub_right'** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_sub_right' : span {x, x - y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.span_pair_sub_left`：span_pair_sub_left : span {x, y - x} = span {x
, y}
· 使用定理 `sub_sub_cancel_left`：∀ {G : Type u_3} [inst : AddCommGroup G] (a b : G),
 a - b - a = -b
· 使用定理 `Ideal.span_pair_neg`：span_pair_neg : span {x, -y} = span {x, y}
-/
theorem span_pair_sub_right' : span {x, x - y} = span {x, y} := by
  rw [← span_pair_sub_left, sub_sub_cancel_left, span_pair_neg]

@[simp]
/-
**Ideal.span_pair_sub_left'** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_sub_left' : span {y - x, y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.span_pair_sub_right`：span_pair_sub_right : span {x - y, y} = span 
{x, y}
· 使用定理 `sub_sub_cancel_left`：∀ {G : Type u_3} [inst : AddCommGroup G] (a b : G),
 a - b - a = -b
· 使用定理 `Ideal.span_insert_neg`：span_insert_neg : span (insert (-x) s) = span (in
sert x s)
-/
theorem span_pair_sub_left' : span {y - x, y} = span {x, y} := by
  rw [← span_pair_sub_right, sub_sub_cancel_left, span_insert_neg]

@[simp]
/-
**Ideal.span_singleton_abs** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_singleton_abs [LinearOrder α] : span {|x|} = span {x}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `abs_choice`：∀ {α : Type u_1} [inst : AddGroup α] [inst_1 : LinearOrder α
] (x : α), |x| = x ∨ |x| = -x
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Ideal.span_singleton_neg`：span_singleton_neg : span {-x} = span {x}
-/
theorem span_singleton_abs [LinearOrder α] : span {|x|} = span {x} := by
  obtain h | h := abs_choice x <;>
  simp [h]

@[simp]
/-
**Ideal.span_insert_abs** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_insert_abs [LinearOrder α] : span (insert |x| s) = span (insert x s)
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.span_insert`：span_insert (x) (s : Set α) : span (insert x s) = spa
n ({x} : Set α) ⊔ span s
· 使用定理 `Ideal.span_singleton_abs`：span_singleton_abs [LinearOrder α] : span {|x|
} = span {x}
-/
theorem span_insert_abs [LinearOrder α] : span (insert |x| s) = span (insert x s) := by
  rw [span_insert, span_singleton_abs, span_insert]

@[simp]
/-
**Ideal.span_pair_abs** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_abs [LinearOrder α] : span {x, |y|} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.span_pair_comm`：span_pair_comm {x y : α} : (span {x, y} : Ideal α)
 = span {y, x}
· 使用定理 `Ideal.span_insert_abs`：span_insert_abs [LinearOrder α] : span (insert |x
| s) = span (insert x s)
-/
theorem span_pair_abs [LinearOrder α] : span {x, |y|} = span {x, y} := by
  rw [span_pair_comm, span_insert_abs, span_pair_comm]

end Ring

section CommRing

variable [CommRing α] (x y z : α)

@[simp]
/-
**Ideal.span_pair_add_right_mul** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_add_right_mul : span {x + y * z, y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.ext`：ext {I J : Ideal α} (h : forall x, x in I ↔ x in J) : I = J
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Mathlib.Tactic.Ring.of_eq`：∀ {α : Sort u_2} {a b c : α}, a = c → b = c →
 a = b
· 使用定理 `Mathlib.Tactic.Ring.Common.add_congr`：∀ {R : Type u_1} [inst : CommSemir
ing R] {a a' b b' c : R}, a = a' → b = b' → a' + b' = c → a + b = c
· 使用定理 `Mathlib.Tactic.Ring.Common.mul_congr`：∀ {R : Type u_1} [inst : CommSemir
ing R] {a a' b b' c : R}, a = a' → b = b' → a' * b' = c → a * b = c
· 使用定理 `Mathlib.Tactic.Ring.Common.atom_pf`：∀ {R : Type u_1} [inst : CommSemirin
g R] {b : R} (a : R) {e : ℕ},   Nat.rawCast 1 = e → a ^ e * Nat.rawCast 1 = b → 
a = b + 0
· 使用定理 `Mathlib.Tactic.Ring.Common.add_mul`：∀ {R : Type u_1} [inst : CommSemirin
g R] {a₁ a₂ b c₁ c₂ d : R},   a₁ * b = c₁ → a₂ * b = c₂ → c₁ + c₂ = d → (a₁ + a₂
) * b = d
· 使用定理 `Mathlib.Tactic.Ring.Common.mul_add`：∀ {R : Type u_1} [inst : CommSemirin
g R] {a b₁ b₂ c₁ c₂ d : R},   a * b₁ = c₁ → a * b₂ = c₂ → c₁ + 0 + c₂ = d → a * 
(b₁ + b₂) = d
· 使用定理 `Mathlib.Tactic.Ring.Common.mul_pf_left`：∀ {R : Type u_1} [inst : CommSem
iring R] {a₃ b c : R} (a₁ : R) (a₂ : ℕ), a₃ * b = c → a₁ ^ a₂ * a₃ * b = a₁ ^ a₂
 * c
· 使用定理 `Mathlib.Tactic.Ring.Common.mul_pf_right`：∀ {R : Type u_1} [inst : CommSe
miring R] {a b₃ c : R} (b₁ : R) (b₂ : ℕ), a * b₃ = c → a * (b₁ ^ b₂ * b₃) = b₁ ^
 b₂ * c
· 使用定理 `Mathlib.Meta.NormNum.IsNat.to_raw_eq`：∀ {α : Type u} {a : α} {n : ℕ} [in
st : AddMonoidWithOne α], Mathlib.Meta.NormNum.IsNat a n → a = n.rawCast
· 使用定理 `Mathlib.Meta.NormNum.isNat_mul`：∀ {α : Type u_1} [inst : Semiring α] {f 
: α → α → α} {a b : α} {a' b' c : ℕ},   f = HMul.hMul →     Mathlib.Meta.NormNum
.IsNat a a' →       …
· 使用定理 `Mathlib.Meta.NormNum.IsNat.of_raw`：∀ (α : Type u_1) [inst : AddMonoidWit
hOne α] (n : ℕ), Mathlib.Meta.NormNum.IsNat n.rawCast n
· 使用定理 `Mathlib.Tactic.Ring.Common.mul_zero`：∀ {R : Type u_1} [inst : CommSemiri
ng R] (a : R), a * 0 = 0
· 使用定理 `Mathlib.Tactic.Ring.Common.add_pf_add_zero`：∀ {R : Type u_1} [inst : Com
mSemiring R] (a : R), a + 0 = a
· 使用定理 `Mathlib.Tactic.Ring.Common.zero_mul`：∀ {R : Type u_1} [inst : CommSemiri
ng R] (b : R), 0 * b = 0
· 使用定理 `Mathlib.Tactic.Ring.Common.add_pf_add_gt`：∀ {R : Type u_1} [inst : CommS
emiring R] {a b₂ c : R} (b₁ : R), a + b₂ = c → a + (b₁ + b₂) = b₁ + c
· 使用定理 `Mathlib.Tactic.Ring.Common.add_pf_add_lt`：∀ {R : Type u_1} [inst : CommS
emiring R] {a₂ b c : R} (a₁ : R), a₂ + b = c → a₁ + a₂ + b = a₁ + c
· 使用定理 `Mathlib.Tactic.Ring.Common.add_pf_zero_add`：∀ {R : Type u_1} [inst : Com
mSemiring R] (b : R), 0 + b = b
· 使用定理 `Mathlib.Tactic.Ring.Common.sub_congr`：∀ {R : Type u_2} [inst : CommRing 
R] {a a' b b' c : R}, a = a' → b = b' → a' - b' = c → a - b = c
· 使用定理 `Mathlib.Tactic.Ring.Common.sub_pf`：∀ {R : Type u_2} [inst : CommRing R] 
{a b c d : R}, -b = c → a + c = d → a - b = d
· 使用定理 `Mathlib.Tactic.Ring.Common.neg_add`：∀ {R : Type u_2} [inst : CommRing R]
 {a₁ a₂ b₁ b₂ : R}, -a₁ = b₁ → -a₂ = b₂ → -(a₁ + a₂) = b₁ + b₂
· 使用定理 `Mathlib.Tactic.Ring.Common.neg_mul`：∀ {R : Type u_2} [inst : CommRing R]
 (a₁ : R) (a₂ : ℕ) {a₃ b : R}, -a₃ = b → -(a₁ ^ a₂ * a₃) = a₁ ^ a₂ * b
· 使用定理 `Mathlib.Meta.NormNum.IsInt.to_raw_eq`：∀ {α : Type u} {a : α} {n : ℤ} [in
st : Ring α], Mathlib.Meta.NormNum.IsInt a n → a = n.rawCast
· 使用定理 `Mathlib.Meta.NormNum.isInt_neg`：∀ {α : Type u_1} [inst : Ring α] {f : α 
→ α} {a : α} {a' b : ℤ},   f = Neg.neg → Mathlib.Meta.NormNum.IsInt a a' → a'.ne
g = b → Mathlib.Meta…
· 使用定理 `Mathlib.Meta.NormNum.IsNat.to_isInt`：∀ {α : Type u_1} [inst : Ring α] {a
 : α} {n : ℕ},   Mathlib.Meta.NormNum.IsNat a n → Mathlib.Meta.NormNum.IsInt a (
Int.ofNat n)
· 使用定理 `Mathlib.Tactic.Ring.Common.neg_zero`：∀ {R : Type u_2} [inst : CommRing R
], -0 = 0
· 使用定理 `Mathlib.Meta.NormNum.isInt_mul`：∀ {α : Type u_1} [inst : Ring α] {f : α 
→ α → α} {a b : α} {a' b' c : ℤ},   f = HMul.hMul →     Mathlib.Meta.NormNum.IsI
nt a a' →       Math…
（共 35 条，此处仅展示前 30 条）
-/
theorem span_pair_add_right_mul : span {x + y * z, y} = span {x, y} := by
  ext
  simp_rw [mem_span_pair]
  exact ⟨fun ⟨a, b, h⟩ => ⟨a, b + a * z, by rw [← h]; ring1⟩,
    fun ⟨a, b, h⟩ => ⟨a, b - a * z, by rw [← h]; ring1⟩⟩

@[simp]
/-
**Ideal.span_pair_add_left_mul** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_add_left_mul : span {x, y + x * z} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.span_pair_comm`：span_pair_comm {x y : α} : (span {x, y} : Ideal α)
 = span {y, x}
· 使用定理 `Ideal.span_pair_add_right_mul`：span_pair_add_right_mul : span {x + y * z
, y} = span {x, y}
-/
theorem span_pair_add_left_mul : span {x, y + x * z} = span {x, y} := by
  rw [span_pair_comm, span_pair_add_right_mul, span_pair_comm]

@[simp]
/-
**Ideal.span_pair_right_mul_add** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_right_mul_add : span {y * z + x, y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `add_comm`：∀ {G : Type u_1} [inst : AddCommMagma G] (a b : G), a + b = b 
+ a
· 使用定理 `Ideal.span_pair_add_right_mul`：span_pair_add_right_mul : span {x + y * z
, y} = span {x, y}
-/
theorem span_pair_right_mul_add : span {y * z + x, y} = span {x, y} := by
  rw [add_comm, span_pair_add_right_mul]

@[simp]
/-
**Ideal.span_pair_left_mul_add** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_left_mul_add : span {x, x * z + y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `add_comm`：∀ {G : Type u_1} [inst : AddCommMagma G] (a b : G), a + b = b 
+ a
· 使用定理 `Ideal.span_pair_add_left_mul`：span_pair_add_left_mul : span {x, y + x * 
z} = span {x, y}
-/
theorem span_pair_left_mul_add : span {x, x * z + y} = span {x, y} := by
  rw [add_comm, span_pair_add_left_mul]

@[simp]
/-
**Ideal.span_pair_add_mul_right** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_add_mul_right : span {x + z * y, y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mul_comm`：mul_comm : forall a b : G, a * b = b * a
· 使用定理 `Ideal.span_pair_add_right_mul`：span_pair_add_right_mul : span {x + y * z
, y} = span {x, y}
-/
theorem span_pair_add_mul_right : span {x + z * y, y} = span {x, y} := by
  rw [mul_comm, span_pair_add_right_mul]

@[simp]
/-
**Ideal.span_pair_add_mul_left** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_add_mul_left : span {x, y + z * x} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mul_comm`：mul_comm : forall a b : G, a * b = b * a
· 使用定理 `Ideal.span_pair_add_left_mul`：span_pair_add_left_mul : span {x, y + x * 
z} = span {x, y}
-/
theorem span_pair_add_mul_left : span {x, y + z * x} = span {x, y} := by
  rw [mul_comm, span_pair_add_left_mul]

@[simp]
/-
**Ideal.span_pair_mul_right_add** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_mul_right_add : span {z * y + x, y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mul_comm`：mul_comm : forall a b : G, a * b = b * a
· 使用定理 `Ideal.span_pair_right_mul_add`：span_pair_right_mul_add : span {y * z + x
, y} = span {x, y}
-/
theorem span_pair_mul_right_add : span {z * y + x, y} = span {x, y} := by
  rw [mul_comm, span_pair_right_mul_add]

@[simp]
/-
**Ideal.span_pair_mul_left_add** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_mul_left_add : span {x, z * x + y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mul_comm`：mul_comm : forall a b : G, a * b = b * a
· 使用定理 `Ideal.span_pair_left_mul_add`：span_pair_left_mul_add : span {x, x * z + 
y} = span {x, y}
-/
theorem span_pair_mul_left_add : span {x, z * x + y} = span {x, y} := by
  rw [mul_comm, span_pair_left_mul_add]

@[simp]
/-
**Ideal.span_pair_sub_right_mul** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_sub_right_mul : span {x - y * z, y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.span_pair_add_right_mul`：span_pair_add_right_mul : span {x + y * z
, y} = span {x, y}
· 使用定理 `sub_add_cancel`：∀ {G : Type u_1} [inst : AddGroup G] (a b : G), a - b + 
b = a
-/
theorem span_pair_sub_right_mul : span {x - y * z, y} = span {x, y} := by
  rw [← span_pair_add_right_mul, sub_add_cancel]

@[simp]
/-
**Ideal.span_pair_sub_left_mul** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_sub_left_mul : span {x, y - x * z} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.span_pair_add_left_mul`：span_pair_add_left_mul : span {x, y + x * 
z} = span {x, y}
· 使用定理 `sub_add_cancel`：∀ {G : Type u_1} [inst : AddGroup G] (a b : G), a - b + 
b = a
-/
theorem span_pair_sub_left_mul : span {x, y - x * z} = span {x, y} := by
  rw [← span_pair_add_left_mul, sub_add_cancel]

@[simp]
/-
**Ideal.span_pair_sub_mul_right** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_sub_mul_right : span {x - z * y, y} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mul_comm`：mul_comm : forall a b : G, a * b = b * a
· 使用定理 `Ideal.span_pair_sub_right_mul`：span_pair_sub_right_mul : span {x - y * z
, y} = span {x, y}
-/
theorem span_pair_sub_mul_right : span {x - z * y, y} = span {x, y} := by
  rw [mul_comm, span_pair_sub_right_mul]

@[simp]
/-
**Ideal.span_pair_sub_mul_left** 是 Mathlib 中的一个定理，位于命名空间 `Ideal`。
形式化陈述：span_pair_sub_mul_left : span {x, y - z * x} = span {x, y}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `mul_comm`：mul_comm : forall a b : G, a * b = b * a
· 使用定理 `Ideal.span_pair_sub_left_mul`：span_pair_sub_left_mul : span {x, y - x * 
z} = span {x, y}
-/
theorem span_pair_sub_mul_left : span {x, y - z * x} = span {x, y} := by
  rw [mul_comm, span_pair_sub_left_mul]

end CommRing

end Ideal

namespace IsIdempotentElem

variable {R} [CommRing R] {e : R} (he : IsIdempotentElem e)
include he

/-
**IsIdempotentElem.ker_toSpanSingleton_eq_span** 是 Mathlib 中的一个定理，位于命名空间 `IsIdem
potentElem`。
形式化陈述：ker_toSpanSingleton_eq_span : LinearMap.ker (LinearMap.toSpanSingleton R R
 e) = Ideal.span {1 - e}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `SetLike.ext`：ext (h : forall x, x in p ↔ x in q) : p = q
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.mem_span_singleton'`：mem_span_singleton' {x y : α} : x in span ({y
} : Set α) ↔ exists a, a * y = x
· 使用定理 `mul_sub`：∀ {α : Type u} [inst : NonUnitalNonAssocRing α] (a b c : α), a 
* (b - c) = a * b - a * c
· 使用定理 `mul_one`：mul_one : forall a : M, a * 1 = a
· 使用定理 `sub_zero`：∀ {G : Type u_3} [inst : SubNegZeroMonoid G] (a : G), a - 0 = 
a
· 使用定理 `mul_assoc`：mul_assoc : forall a b c : G, a * b * c = a * (b * c)
· 使用定理 `sub_mul`：∀ {α : Type u} [inst : NonUnitalNonAssocRing α] (a b c : α), (a
 - b) * c = a * c - b * c
· 使用定理 `one_mul`：one_mul : forall a : M, 1 * a = a
· 使用定理 `sub_self`：∀ {G : Type u_1} [inst : AddGroup G] (a : G), a - a = 0
· 使用定理 `MulZeroClass.mul_zero`：∀ {M₀ : Type u} [self : MulZeroClass M₀] (a : M₀)
, a * 0 = 0
-/
theorem ker_toSpanSingleton_eq_span :
    LinearMap.ker (LinearMap.toSpanSingleton R R e) = Ideal.span {1 - e} := SetLike.ext fun x ↦ by
  rw [Ideal.mem_span_singleton']
  refine ⟨fun h ↦ ⟨x, by rw [mul_sub, show x * e = 0 from h, mul_one, sub_zero]⟩, fun h ↦ ?_⟩
  obtain ⟨x, rfl⟩ := h
  change x * (1 - e) * e = 0
  rw [mul_assoc, sub_mul, one_mul, he, sub_self, mul_zero]
/-
**IsIdempotentElem.ker_toSpanSingleton_one_sub_eq_span** 是 Mathlib 中的一个定理，位于命名空间
 `IsIdempotentElem`。
形式化陈述：ker_toSpanSingleton_one_sub_eq_span : LinearMap.ker (LinearMap.toSpanSingl
eton R R (1 - e)) = Ideal.span {e}
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `IsIdempotentElem.ker_toSpanSingleton_eq_span`：ker_toSpanSingleton_eq_spa
n : LinearMap.ker (LinearMap.toSpanSingleton R R e) = Ideal.span {1 - e}
· 使用引理 `IsIdempotentElem.one_sub`：one_sub (h : IsIdempotentElem a) : IsIdempoten
tElem (1 - a)
· 使用定理 `sub_sub_cancel`：∀ {G : Type u_3} [inst : AddCommGroup G] (a b : G), a - 
(a - b) = b
-/
theorem ker_toSpanSingleton_one_sub_eq_span :
    LinearMap.ker (LinearMap.toSpanSingleton R R (1 - e)) = Ideal.span {e} := by
  rw [ker_toSpanSingleton_eq_span he.one_sub, sub_sub_cancel]

end IsIdempotentElem

section PrincipalIdeal

/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance {R : Type*} [Semiring R] {x : R} : (Ideal.span {x}).IsPrincipal :=
  ⟨x, rfl⟩

end PrincipalIdeal

