/-
Copyright (c) 2025 Joël Riou. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Joël Riou
-/
module

public import Mathlib.CategoryTheory.Subfunctor.OfSection

/-!
# Subpresheaves that are generated by finitely many sections

Given `F : Cᵒᵖ ⥤ Type w`, `G : Subfunctor F`, objects `X : ι → Cᵒᵖ` and sections
`x : (i : ι) → F.obj (X i)`, we define a predicate `G.IsGeneratedBy x` saying
that `G` is the subpresheaf generated by the sections `x i`. If this holds for
a finite index type `ι`, we say that `G` is "finite", and this gives a type
class `G.IsFinite`.

-/

@[expose] public section

universe w'' w' w v u

namespace CategoryTheory

open Opposite

variable {C : Type u} [Category.{v} C] {F : Cᵒᵖ ⥤ Type w}

namespace Subfunctor

variable (G : Subfunctor F)

section

variable {ι : Type w'} {X : ι → Cᵒᵖ} (x : (i : ι) → F.obj (X i))

/-- A subpresheaf `G : Subfunctor F` is generated by sections `x i : F.obj (X i)`
if `⨆ (i : ι), ofSection (x i) = G`. -/
/-
**CategoryTheory.Subfunctor.IsGeneratedBy** 是 Mathlib 中的一个定义，位于命名空间 `CategoryThe
ory.Subfunctor`。
形式化陈述：IsGeneratedBy : Prop
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A subpresheaf `G : Subfunctor F` is generated by sections `x i : F.obj (X i)`
if `⨆ (i : ι), ofSection (x i) = G`.
-/
def IsGeneratedBy : Prop := ⨆ (i : ι), ofSection (x i) = G
/-
**CategoryTheory.Subfunctor.isGeneratedBy_iff** 是 Mathlib 中的一个引理，位于命名空间 `Categor
yTheory.Subfunctor`。
形式化陈述：isGeneratedBy_iff : G.IsGeneratedBy x ↔ ⨆ (i : ι), ofSection (x i) = G
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma isGeneratedBy_iff : G.IsGeneratedBy x ↔ ⨆ (i : ι), ofSection (x i) = G := Iff.rfl

namespace IsGeneratedBy

variable {G x} (h : G.IsGeneratedBy x)
include h

/-
**CategoryTheory.Subfunctor.IsGeneratedBy.iSup_eq** 是 Mathlib 中的一个引理，位于命名空间 `Cat
egoryTheory.Subfunctor.IsGeneratedBy`。
形式化陈述：iSup_eq : ⨆ (i : ι), ofSection (x i) = G
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
lemma iSup_eq : ⨆ (i : ι), ofSection (x i) = G := h
/-
**CategoryTheory.Subfunctor.IsGeneratedBy.ofSection_le** 是 Mathlib 中的一个引理，位于命名空间
 `CategoryTheory.Subfunctor.IsGeneratedBy`。
形式化陈述：ofSection_le (i : ι) : ofSection (x i) <= G
参数：i : ι。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `CategoryTheory.Subfunctor.IsGeneratedBy.iSup_eq`：iSup_eq : ⨆ (i : ι), of
Section (x i) = G
· 使用定理 `le_iSup`：le_iSup (f : ι -> α) (i : ι) : f i <= iSup f
-/
lemma ofSection_le (i : ι) : ofSection (x i) ≤ G := by
  rw [← h.iSup_eq]
  exact le_iSup (fun i ↦ ofSection (x i)) i
/-
**CategoryTheory.Subfunctor.IsGeneratedBy.mem** 是 Mathlib 中的一个引理，位于命名空间 `Categor
yTheory.Subfunctor.IsGeneratedBy`。
形式化陈述：mem (i : ι) : x i in G.obj (X i)
参数：i : ι。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `CategoryTheory.Subfunctor.ofSection_le_iff`：ofSection_le_iff (G : Subfun
ctor F) : ofSection x <= G ↔ x in G.obj X
· 使用引理 `CategoryTheory.Subfunctor.IsGeneratedBy.ofSection_le`：ofSection_le (i : 
ι) : ofSection (x i) <= G
-/
lemma mem (i : ι) : x i ∈ G.obj (X i) := by
  rw [← ofSection_le_iff]
  exact h.ofSection_le i
/-
**CategoryTheory.Subfunctor.IsGeneratedBy.of_equiv** 是 Mathlib 中的一个引理，位于命名空间 `Ca
tegoryTheory.Subfunctor.IsGeneratedBy`。
形式化陈述：of_equiv {ι' : Type w''} (e : ι' ≃ ι) : G.IsGeneratedBy (fun i' => x (e i'
))
参数：e : ι' ≃ ι。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `CategoryTheory.Subfunctor.isGeneratedBy_iff`：isGeneratedBy_iff : G.IsGen
eratedBy x ↔ ⨆ (i : ι), ofSection (x i) = G
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `CategoryTheory.Subfunctor.IsGeneratedBy.iSup_eq`：iSup_eq : ⨆ (i : ι), of
Section (x i) = G
· 使用定理 `Equiv.iSup_congr`：∀ {α : Type u_1} {ι : Sort u_4} {ι' : Sort u_5} [inst 
: SupSet α] {f : ι → α} {g : ι' → α} (e : ι ≃ ι'),   (∀ (x : ι), g (e x) = f x) 
→ ⨆ x,…
· 使用定理 `congrFun`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, f = g →
 ∀ (a : α), f a = g a
-/
lemma of_equiv {ι' : Type w''} (e : ι' ≃ ι) :
    G.IsGeneratedBy (fun i' ↦ x (e i')) := by
  rw [isGeneratedBy_iff, ← h.iSup_eq]
  exact Equiv.iSup_congr e (congrFun rfl)
/-
**CategoryTheory.Subfunctor.IsGeneratedBy.image** 是 Mathlib 中的一个引理，位于命名空间 `Categ
oryTheory.Subfunctor.IsGeneratedBy`。
形式化陈述：image {F' : Cᵒᵖ ⥤ Type w} (f : F ⟶ F') : (G.image f).IsGeneratedBy (fun i 
=> f.app _ (x i))
参数：f : F ⟶ F'。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrFun`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, f = g →
 ∀ (a : α), f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `CategoryTheory.Subfunctor.IsGeneratedBy.iSup_eq`：iSup_eq : ⨆ (i : ι), of
Section (x i) = G
· 使用引理 `CategoryTheory.Subfunctor.image_iSup`：image_iSup {ι : Type*} (G : ι -> S
ubfunctor F) (f : F ⟶ F') : (⨆ i, G i).image f = ⨆ i, (G i).image f
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用引理 `CategoryTheory.Subfunctor.ofSection_image`：ofSection_image {F' : Cᵒᵖ ⥤ T
ype w} (f : F ⟶ F') : (ofSection x).image f = ofSection (f.app _ x)
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma image {F' : Cᵒᵖ ⥤ Type w} (f : F ⟶ F') :
    (G.image f).IsGeneratedBy (fun i ↦ f.app _ (x i)) := by
  simp only [isGeneratedBy_iff, ← h.iSup_eq, image_iSup, ofSection_image]

end IsGeneratedBy

end

/-- A subpresheaf of types is "finite" if it is generated by finitely many sections. -/
/-
**CategoryTheory.Subfunctor.IsFinite** 是 Mathlib 中的一个归纳类型，位于命名空间 `CategoryTheory
.Subfunctor`。
形式化陈述：{C : Type u} →   [inst : CategoryTheory.Category.{v, u} C] →     {F : Cate
goryTheory.Functor Cᵒᵖ (Type w)} → CategoryTheory.Subfunctor F → Prop
参数：Type w。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A subpresheaf of types is "finite" if it is generated by finitely many sections.
-/
class IsFinite : Prop where
  exists_isGeneratedBy :
    ∃ (ι : Type) (_ : Finite ι) (X : ι → Cᵒᵖ) (x : (i : ι) → F.obj (X i)),
      Nonempty (G.IsGeneratedBy x)

namespace IsFinite

variable [hG : G.IsFinite]

/-- A choice of index type for the generating sections of a finitely generated subpresheaf. -/
/-
**CategoryTheory.Subfunctor.IsFinite.Index** 是 Mathlib 中的一个定义，位于命名空间 `CategoryTh
eory.Subfunctor.IsFinite`。
形式化陈述：Index : Type
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Subfunctor.IsFinite.exists_isGeneratedBy`：∀ {C : Type u} 
{inst : CategoryTheory.Category.{v, u} C} {F : CategoryTheory.Functor Cᵒᵖ (Type 
w)}   {G : CategoryTheory.Subfunctor F} [self…

--- 原说明 ---
A choice of index type for the generating sections of a finitely generated subpr
esheaf.
-/
def Index : Type := hG.exists_isGeneratedBy.choose
/-
**CategoryTheory.Subfunctor.IsFinite.** 是 Mathlib 中的一个实例，位于命名空间 `CategoryTheory.
Subfunctor.IsFinite`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : Finite (Index G) := hG.exists_isGeneratedBy.choose_spec.choose

variable {G}

/-- Objects on which a choice of generating sections of a finitely generated subpresheaf
are defined. -/
/-
**CategoryTheory.Subfunctor.IsFinite.X** 是 Mathlib 中的一个定义，位于命名空间 `CategoryTheory
.Subfunctor.IsFinite`。
形式化陈述：X : Index G -> Cᵒᵖ
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Subfunctor.IsFinite.exists_isGeneratedBy`：∀ {C : Type u} 
{inst : CategoryTheory.Category.{v, u} C} {F : CategoryTheory.Functor Cᵒᵖ (Type 
w)}   {G : CategoryTheory.Subfunctor F} [self…

--- 原说明 ---
Objects on which a choice of generating sections of a finitely generated subpres
heaf
are defined.
-/
noncomputable def X : Index G → Cᵒᵖ :=
  hG.exists_isGeneratedBy.choose_spec.choose_spec.choose

/-- A choice of generating sections of a finitely generated subpresheaf. -/
/-
**CategoryTheory.Subfunctor.IsFinite.x** 是 Mathlib 中的一个定义，位于命名空间 `CategoryTheory
.Subfunctor.IsFinite`。
形式化陈述：x : (i : Index G) -> F.obj (X i)
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Subfunctor.IsFinite.exists_isGeneratedBy`：∀ {C : Type u} 
{inst : CategoryTheory.Category.{v, u} C} {F : CategoryTheory.Functor Cᵒᵖ (Type 
w)}   {G : CategoryTheory.Subfunctor F} [self…

--- 原说明 ---
A choice of generating sections of a finitely generated subpresheaf.
-/
noncomputable def x : (i : Index G) → F.obj (X i) :=
  hG.exists_isGeneratedBy.choose_spec.choose_spec.choose_spec.choose

end IsFinite

/-
**CategoryTheory.Subfunctor.isGeneratedBy_of_isFinite** 是 Mathlib 中的一个引理，位于命名空间 
`CategoryTheory.Subfunctor`。
形式化陈述：isGeneratedBy_of_isFinite [hG : G.IsFinite] : G.IsGeneratedBy (IsFinite.x 
(G
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Subfunctor.IsFinite.exists_isGeneratedBy`：∀ {C : Type u} 
{inst : CategoryTheory.Category.{v, u} C} {F : CategoryTheory.Functor Cᵒᵖ (Type 
w)}   {G : CategoryTheory.Subfunctor F} [self…
· 使用定理 `Exists.choose_spec`：∀ {α : Sort u_1} {p : α → Prop} (P : ∃ a, p a), p P.
choose
-/
lemma isGeneratedBy_of_isFinite [hG : G.IsFinite] :
    G.IsGeneratedBy (IsFinite.x (G := G)) :=
  hG.exists_isGeneratedBy.choose_spec.choose_spec.choose_spec.choose_spec.some
/-
**CategoryTheory.Subfunctor.IsGeneratedBy.isFinite** 是 Mathlib 中的一个定理，位于命名空间 `Ca
tegoryTheory.Subfunctor.IsGeneratedBy`。
形式化陈述：∀ {C : Type u} [inst : CategoryTheory.Category.{v, u} C] {F : CategoryTheo
ry.Functor Cᵒᵖ (Type w)}   (G : CategoryTheory.Subfunctor F) {ι : Type w'} [Fini
te ι] {X : ι → Cᵒᵖ} {x : (i : ι) → F.obj (X i)},   G.IsGeneratedBy x → G.IsFinit
e
参数：Type w；G : CategoryTheory.Subfunctor F；i : ι；X i。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Finite.exists_equiv_fin`：Finite.exists_equiv_fin (α : Sort*) [h : Finite
 α] : exists n : Nat, Nonempty (α ≃ Fin n)
· 使用定理 `Finite.of_fintype`：∀ (α : Type u_4) [Fintype α], Finite α
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用引理 `CategoryTheory.Subfunctor.IsGeneratedBy.of_equiv`：of_equiv {ι' : Type w'
'} (e : ι' ≃ ι) : G.IsGeneratedBy (fun i' => x (e i'))
-/
lemma IsGeneratedBy.isFinite
    {ι : Type w'} [Finite ι] {X : ι → Cᵒᵖ} {x : (i : ι) → F.obj (X i)}
    (h : G.IsGeneratedBy x) : G.IsFinite := by
  obtain ⟨n, ⟨e⟩⟩ := Finite.exists_equiv_fin ι
  exact ⟨Fin n, inferInstance, _, _, ⟨h.of_equiv e.symm⟩⟩
/-
**CategoryTheory.Subfunctor.image_isFinite** 是 Mathlib 中的一个引理，位于命名空间 `CategoryTh
eory.Subfunctor`。
形式化陈述：image_isFinite [G.IsFinite] {F' : Cᵒᵖ ⥤ Type w} (f : F ⟶ F') : (G.image f)
.IsFinite
参数：f : F ⟶ F'。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Subfunctor.IsGeneratedBy.isFinite`：∀ {C : Type u} [inst :
 CategoryTheory.Category.{v, u} C] {F : CategoryTheory.Functor Cᵒᵖ (Type w)}   (
G : CategoryTheory.Subfunctor F) {ι : …
· 使用定理 `CategoryTheory.Subfunctor.IsFinite.instFiniteIndex`：∀ {C : Type u} [inst
 : CategoryTheory.Category.{v, u} C] {F : CategoryTheory.Functor Cᵒᵖ (Type w)}  
 (G : CategoryTheory.Subfunctor F) [hG :…
· 使用引理 `CategoryTheory.Subfunctor.IsGeneratedBy.image`：image {F' : Cᵒᵖ ⥤ Type w}
 (f : F ⟶ F') : (G.image f).IsGeneratedBy (fun i => f.app _ (x i))
· 使用引理 `CategoryTheory.Subfunctor.isGeneratedBy_of_isFinite`：isGeneratedBy_of_is
Finite [hG : G.IsFinite] : G.IsGeneratedBy (IsFinite.x (G
-/
lemma image_isFinite [G.IsFinite] {F' : Cᵒᵖ ⥤ Type w} (f : F ⟶ F') :
    (G.image f).IsFinite :=
  ((isGeneratedBy_of_isFinite G).image f).isFinite

end Subfunctor

variable (F)

section

variable {ι : Type w'} {X : ι → Cᵒᵖ} (x : (i : ι) → F.obj (X i))

/-- The condition that a presheaf of types `F : Cᵒᵖ ⥤ Type w` is generated by
a family of sections. -/
/-
**CategoryTheory.PresheafIsGeneratedBy** 是 Mathlib 中的一个缩写定义，位于命名空间 `CategoryTheo
ry`。
形式化陈述：PresheafIsGeneratedBy : Prop
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The condition that a presheaf of types `F : Cᵒᵖ ⥤ Type w` is generated by
a family of sections.
-/
abbrev PresheafIsGeneratedBy : Prop := (⊤ : Subfunctor F).IsGeneratedBy x

namespace PresheafIsGeneratedBy

variable {F x} (h : PresheafIsGeneratedBy F x) {F' : Cᵒᵖ ⥤ Type w} (f : F ⟶ F')

include h

/-
**CategoryTheory.PresheafIsGeneratedBy.range** 是 Mathlib 中的一个引理，位于命名空间 `Category
Theory.PresheafIsGeneratedBy`。
形式化陈述：range : (Subfunctor.range f).IsGeneratedBy (fun i => f.app _ (x i))
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrFun`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, f = g →
 ∀ (a : α), f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `CategoryTheory.Subfunctor.IsGeneratedBy.image`：image {F' : Cᵒᵖ ⥤ Type w}
 (f : F ⟶ F') : (G.image f).IsGeneratedBy (fun i => f.app _ (x i))
-/
lemma range : (Subfunctor.range f).IsGeneratedBy (fun i ↦ f.app _ (x i)) := by
  simpa only [← Subfunctor.image_top] using h.image f
/-
**CategoryTheory.PresheafIsGeneratedBy.of_epi** 是 Mathlib 中的一个引理，位于命名空间 `Categor
yTheory.PresheafIsGeneratedBy`。
形式化陈述：of_epi [Epi f] : PresheafIsGeneratedBy F' (fun i => f.app _ (x i))
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrFun`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, f = g →
 ∀ (a : α), f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `CategoryTheory.Subfunctor.range_eq_top`：range_eq_top [Epi p] : range p =
 ⊤
· 使用引理 `CategoryTheory.PresheafIsGeneratedBy.range`：range : (Subfunctor.range f)
.IsGeneratedBy (fun i => f.app _ (x i))
-/
lemma of_epi [Epi f] : PresheafIsGeneratedBy F' (fun i ↦ f.app _ (x i)) := by
  simpa only [Subfunctor.range_eq_top f] using h.range f

end PresheafIsGeneratedBy

end

/-- A presheaf is "finite" if it is generated by finitely many sections. -/
/-
**CategoryTheory.PresheafIsFinite** 是 Mathlib 中的一个缩写定义，位于命名空间 `CategoryTheory`。
形式化陈述：PresheafIsFinite : Prop
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A presheaf is "finite" if it is generated by finitely many sections.
-/
abbrev PresheafIsFinite : Prop := (⊤ : Subfunctor F).IsFinite
/-
**CategoryTheory.presheafIsGeneratedBy_of_isFinite** 是 Mathlib 中的一个引理，位于命名空间 `Ca
tegoryTheory`。
形式化陈述：presheafIsGeneratedBy_of_isFinite [PresheafIsFinite F] : PresheafIsGenerat
edBy F (Subfunctor.IsFinite.x (G
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `CategoryTheory.Subfunctor.isGeneratedBy_of_isFinite`：isGeneratedBy_of_is
Finite [hG : G.IsFinite] : G.IsGeneratedBy (IsFinite.x (G
-/
lemma presheafIsGeneratedBy_of_isFinite [PresheafIsFinite F] :
    PresheafIsGeneratedBy F (Subfunctor.IsFinite.x (G := ⊤)) :=
  (Subfunctor.isGeneratedBy_of_isFinite (⊤ : Subfunctor F))
/-
**CategoryTheory.Subfunctor.range_isFinite** 是 Mathlib 中的一个定理，位于命名空间 `CategoryTh
eory.Subfunctor`。
形式化陈述：∀ {C : Type u} [inst : CategoryTheory.Category.{v, u} C] (F : CategoryTheo
ry.Functor Cᵒᵖ (Type w))   [CategoryTheory.PresheafIsFinite F] {F' : CategoryThe
ory.Functor Cᵒᵖ (Type w)} (f : F ⟶ F'),   (CategoryTheory.Subfunctor.range f).Is
Finite
参数：F : CategoryTheory.Functor Cᵒᵖ (Type w)；Type w；f : F ⟶ F'；CategoryTheory.Subf
unctor.range f。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Subfunctor.IsGeneratedBy.isFinite`：∀ {C : Type u} [inst :
 CategoryTheory.Category.{v, u} C] {F : CategoryTheory.Functor Cᵒᵖ (Type w)}   (
G : CategoryTheory.Subfunctor F) {ι : …
· 使用定理 `CategoryTheory.Subfunctor.IsFinite.instFiniteIndex`：∀ {C : Type u} [inst
 : CategoryTheory.Category.{v, u} C] {F : CategoryTheory.Functor Cᵒᵖ (Type w)}  
 (G : CategoryTheory.Subfunctor F) [hG :…
· 使用引理 `CategoryTheory.PresheafIsGeneratedBy.range`：range : (Subfunctor.range f)
.IsGeneratedBy (fun i => f.app _ (x i))
· 使用引理 `CategoryTheory.presheafIsGeneratedBy_of_isFinite`：presheafIsGeneratedBy_
of_isFinite [PresheafIsFinite F] : PresheafIsGeneratedBy F (Subfunctor.IsFinite.
x (G
-/
lemma Subfunctor.range_isFinite [PresheafIsFinite F] {F' : Cᵒᵖ ⥤ Type w} (f : F ⟶ F') :
    (Subfunctor.range f).IsFinite :=
  ((presheafIsGeneratedBy_of_isFinite F).range f).isFinite
/-
**CategoryTheory.presheafIsFinite_of_epi** 是 Mathlib 中的一个引理，位于命名空间 `CategoryTheo
ry`。
形式化陈述：presheafIsFinite_of_epi [PresheafIsFinite F] {F' : Cᵒᵖ ⥤ Type w} (f : F ⟶ 
F') [Epi f] : PresheafIsFinite F'
参数：f : F ⟶ F'。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Subfunctor.IsGeneratedBy.isFinite`：∀ {C : Type u} [inst :
 CategoryTheory.Category.{v, u} C] {F : CategoryTheory.Functor Cᵒᵖ (Type w)}   (
G : CategoryTheory.Subfunctor F) {ι : …
· 使用定理 `CategoryTheory.Subfunctor.IsFinite.instFiniteIndex`：∀ {C : Type u} [inst
 : CategoryTheory.Category.{v, u} C] {F : CategoryTheory.Functor Cᵒᵖ (Type w)}  
 (G : CategoryTheory.Subfunctor F) [hG :…
· 使用引理 `CategoryTheory.PresheafIsGeneratedBy.of_epi`：of_epi [Epi f] : PresheafIs
GeneratedBy F' (fun i => f.app _ (x i))
· 使用引理 `CategoryTheory.presheafIsGeneratedBy_of_isFinite`：presheafIsGeneratedBy_
of_isFinite [PresheafIsFinite F] : PresheafIsGeneratedBy F (Subfunctor.IsFinite.
x (G
-/
lemma presheafIsFinite_of_epi [PresheafIsFinite F] {F' : Cᵒᵖ ⥤ Type w} (f : F ⟶ F') [Epi f] :
    PresheafIsFinite F' :=
  ((presheafIsGeneratedBy_of_isFinite F).of_epi f).isFinite
/-
**CategoryTheory.yoneda_obj_isGeneratedBy** 是 Mathlib 中的一个引理，位于命名空间 `CategoryThe
ory`。
形式化陈述：yoneda_obj_isGeneratedBy (X : C) : PresheafIsGeneratedBy (yoneda.obj X) (f
un (_ : Unit) => 𝟙 X)
参数：X : C。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `CategoryTheory.Subfunctor.ext`：∀ {C : Type u} {inst : CategoryTheory.Cat
egory.{v, u} C} {F : CategoryTheory.Functor C (Type w)}   {x y : CategoryTheory.
Subfunctor F}, x.ob…
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用引理 `CategoryTheory.Subfunctor.iSup_obj`：iSup_obj {ι : Sort*} (S : ι -> Subfu
nctor F) (U : C) : (⨆ i, S i).obj U = ⋃ i, (S i).obj U
· 使用定理 `instNonemptyOfInhabited`：∀ {α : Sort u} [Inhabited α], Nonempty α
· 使用定理 `iff_true`：∀ (p : Prop), (p ↔ True) = p
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `CategoryTheory.Category.comp_id`：∀ {obj : Type u} [self : CategoryTheory
.Category.{v, u} obj] {X Y : obj} (f : X ⟶ Y),   CategoryTheory.CategoryStruct.c
omp f (CategoryTheory…
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
lemma yoneda_obj_isGeneratedBy (X : C) :
    PresheafIsGeneratedBy (yoneda.obj X) (fun (_ : Unit) ↦ 𝟙 X) := by
  simp only [Subfunctor.isGeneratedBy_iff]
  ext U u
  simp only [Subfunctor.iSup_obj, Set.mem_iUnion,
    exists_const, Subfunctor.top_obj, Set.top_eq_univ, Set.mem_univ, iff_true]
  exact ⟨u.op, by simp⟩
/-
**CategoryTheory.** 是 Mathlib 中的一个实例，位于命名空间 `CategoryTheory`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (X : C) : PresheafIsFinite (yoneda.obj X) :=
  (yoneda_obj_isGeneratedBy X).isFinite

end CategoryTheory

