/-
Copyright (c) 2022 Sébastien Gouëzel. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Sébastien Gouëzel, Violeta Hernández Palacios
-/
module

public import Mathlib.MeasureTheory.MeasurableSpace.Defs
public import Mathlib.SetTheory.Cardinal.Regular
public import Mathlib.SetTheory.Cardinal.Continuum
public import Mathlib.SetTheory.Cardinal.Ordinal

/-!
# Cardinal of sigma-algebras

If a sigma-algebra is generated by a set of sets `s`, then the cardinality of the sigma-algebra is
bounded by `(max #s 2) ^ ℵ₀`. This is stated in `MeasurableSpace.cardinal_generate_measurable_le`
and `MeasurableSpace.cardinalMeasurableSet_le`.

In particular, if `#s ≤ 𝔠`, then the generated sigma-algebra has cardinality at most `𝔠`, see
`MeasurableSpace.cardinal_measurableSet_le_continuum`.

For the proof, we rely on an explicit inductive construction of the sigma-algebra generated by
`s` (instead of the inductive predicate `GenerateMeasurable`). This transfinite inductive
construction is parameterized by an ordinal `< ω₁`, and the cardinality bound is preserved along
each step of the construction. We show in `MeasurableSpace.generateMeasurable_eq_rec` that this
indeed generates this sigma-algebra.
-/

@[expose] public section


universe u v

variable {α : Type u}

open Cardinal Ordinal Set MeasureTheory

namespace MeasurableSpace

/-- Transfinite induction construction of the sigma-algebra generated by a set of sets `s`. At each
step, we add all elements of `s`, the empty set, the complements of already constructed sets, and
countable unions of already constructed sets.

We index this construction by an arbitrary ordinal for simplicity, but by `ω₁` we will have
generated all the sets in the sigma-algebra.

This construction is very similar to that of the Borel hierarchy. -/
/-
**MeasurableSpace.generateMeasurableRec** 是 Mathlib 中的一个定义，位于命名空间 `MeasurableSpa
ce`。
形式化陈述：generateMeasurableRec (s : Set (Set α)) (i : Ordinal) : Set (Set α)
参数：s : Set (Set α)；i : Ordinal。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Transfinite induction construction of the sigma-algebra generated by a set of se
ts `s`. At each
step, we add all elements of `s`, the empty set, the complements of already cons
tructed sets, and
countable unions of already constructed sets.

We index this construction by an arbitrary ordinal for simplicity, but by `ω₁` w
e will have
generated all the sets in the sigma-algebra.

This construction is very similar to that of the Borel hierarchy.
-/
def generateMeasurableRec (s : Set (Set α)) (i : Ordinal) : Set (Set α) :=
  let S := ⋃ j < i, generateMeasurableRec s j
  s ∪ {∅} ∪ compl '' S ∪ Set.range fun f : ℕ → S => ⋃ n, (f n).1
termination_by i
/-
**MeasurableSpace.self_subset_generateMeasurableRec** 是 Mathlib 中的一个定理，位于命名空间 `M
easurableSpace`。
形式化陈述：self_subset_generateMeasurableRec (s : Set (Set α)) (i : Ordinal) : s subs
eteq generateMeasurableRec s i
参数：s : Set (Set α)；i : Ordinal。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSpace.generateMeasurableRec.eq_def`：∀ {α : Type u} (s : Set (S
et α)) (i : Ordinal.{u_1}),   MeasurableSpace.generateMeasurableRec s i =     ha
ve S := ⋃ j, ⋃ (_ : j < i), Measur…
· 使用定理 `Set.subset_union_of_subset_left`：subset_union_of_subset_left {s t : Set 
α} (h : s subseteq t) (u : Set α) : s subseteq t union u
· 使用定理 `subset_rfl`：∀ {α : Type u_1} [UsesSetNotationForOrder α] [inst : Preorde
r α] {a : α}, a ⊆ a
-/
theorem self_subset_generateMeasurableRec (s : Set (Set α)) (i : Ordinal) :
    s ⊆ generateMeasurableRec s i := by
  unfold generateMeasurableRec
  apply_rules [subset_union_of_subset_left]
  exact subset_rfl
/-
**MeasurableSpace.empty_mem_generateMeasurableRec** 是 Mathlib 中的一个定理，位于命名空间 `Mea
surableSpace`。
形式化陈述：empty_mem_generateMeasurableRec (s : Set (Set α)) (i : Ordinal) : ∅ in gen
erateMeasurableRec s i
参数：s : Set (Set α)；i : Ordinal。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSpace.generateMeasurableRec.eq_def`：∀ {α : Type u} (s : Set (S
et α)) (i : Ordinal.{u_1}),   MeasurableSpace.generateMeasurableRec s i =     ha
ve S := ⋃ j, ⋃ (_ : j < i), Measur…
· 使用定理 `Set.mem_union_left`：mem_union_left {x : α} {a : Set α} (b : Set α) : x i
n a -> x in a union b
· 使用定理 `Set.mem_union_right`：mem_union_right {x : α} {b : Set α} (a : Set α) : x
 in b -> x in a union b
· 使用定理 `Set.mem_singleton`：mem_singleton (a : α) : a in ({a} : Set α)
-/
theorem empty_mem_generateMeasurableRec (s : Set (Set α)) (i : Ordinal) :
    ∅ ∈ generateMeasurableRec s i := by
  unfold generateMeasurableRec
  exact mem_union_left _ (mem_union_left _ (mem_union_right _ (mem_singleton ∅)))
/-
**MeasurableSpace.compl_mem_generateMeasurableRec** 是 Mathlib 中的一个定理，位于命名空间 `Mea
surableSpace`。
形式化陈述：compl_mem_generateMeasurableRec {s : Set (Set α)} {i j : Ordinal} (h : j <
 i) {t : Set α} (ht : t in generateMeasurableRec s j) : tᶜ in generateMeasurable
Rec s i
参数：Set α；h : j < i；ht : t in generateMeasurableRec s j。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSpace.generateMeasurableRec.eq_def`：∀ {α : Type u} (s : Set (S
et α)) (i : Ordinal.{u_1}),   MeasurableSpace.generateMeasurableRec s i =     ha
ve S := ⋃ j, ⋃ (_ : j < i), Measur…
· 使用定理 `Set.mem_union_left`：mem_union_left {x : α} {a : Set α} (b : Set α) : x i
n a -> x in a union b
· 使用定理 `Set.mem_union_right`：mem_union_right {x : α} {b : Set α} (a : Set α) : x
 in b -> x in a union b
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Set.mem_iUnion₂`：mem_iUnion₂ {x : γ} {s : forall i, κ i -> Set γ} : (x i
n ⋃ (i) (j), s i j) ↔ exists i j, x in s i j
-/
theorem compl_mem_generateMeasurableRec {s : Set (Set α)} {i j : Ordinal} (h : j < i) {t : Set α}
    (ht : t ∈ generateMeasurableRec s j) : tᶜ ∈ generateMeasurableRec s i := by
  unfold generateMeasurableRec
  exact mem_union_left _ (mem_union_right _ ⟨t, mem_iUnion₂.2 ⟨j, h, ht⟩, rfl⟩)
/-
**MeasurableSpace.iUnion_mem_generateMeasurableRec** 是 Mathlib 中的一个定理，位于命名空间 `Me
asurableSpace`。
形式化陈述：iUnion_mem_generateMeasurableRec {s : Set (Set α)} {i : Ordinal} {f : Nat 
-> Set α} (hf : forall n, exists j < i, f n in generateMeasurableRec s j) : ⋃ n,
 f n in generateMeasurableRec s i
参数：Set α；hf : forall n, exists j < i, f n in generateMeasurableRec s j。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSpace.generateMeasurableRec.eq_def`：∀ {α : Type u} (s : Set (S
et α)) (i : Ordinal.{u_1}),   MeasurableSpace.generateMeasurableRec s i =     ha
ve S := ⋃ j, ⋃ (_ : j < i), Measur…
· 使用定理 `Set.mem_union_right`：mem_union_right {x : α} {b : Set α} (a : Set α) : x
 in b -> x in a union b
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Set.mem_iUnion₂`：mem_iUnion₂ {x : γ} {s : forall i, κ i -> Set γ} : (x i
n ⋃ (i) (j), s i j) ↔ exists i j, x in s i j
-/
theorem iUnion_mem_generateMeasurableRec {s : Set (Set α)} {i : Ordinal} {f : ℕ → Set α}
    (hf : ∀ n, ∃ j < i, f n ∈ generateMeasurableRec s j) :
    ⋃ n, f n ∈ generateMeasurableRec s i := by
  unfold generateMeasurableRec
  exact mem_union_right _ ⟨fun n => ⟨f n, let ⟨j, hj, hf⟩ := hf n; mem_iUnion₂.2 ⟨j, hj, hf⟩⟩, rfl⟩
/-
**MeasurableSpace.generateMeasurableRec_mono** 是 Mathlib 中的一个定理，位于命名空间 `Measurab
leSpace`。
形式化陈述：generateMeasurableRec_mono (s : Set (Set α)) : Monotone (generateMeasurabl
eRec s)
参数：s : Set (Set α)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `LE.le.eq_or_lt`：∀ {α : Type u_2} [inst : PartialOrder α] {a b : α}, a ≤ 
b → a = b ∨ a < b
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `Set.iUnion_const`：iUnion_const (s : Set β) : ⋃ _ : ι, s = s
· 使用定理 `instNonemptyOfInhabited`：∀ {α : Sort u} [Inhabited α], Nonempty α
· 使用定理 `MeasurableSpace.iUnion_mem_generateMeasurableRec`：iUnion_mem_generateMea
surableRec {s : Set (Set α)} {i : Ordinal} {f : Nat -> Set α} (hf : forall n, ex
ists j < i, f n in generateMeasurableR…
-/
theorem generateMeasurableRec_mono (s : Set (Set α)) : Monotone (generateMeasurableRec s) := by
  intro i j h x hx
  rcases h.eq_or_lt with (rfl | h)
  · exact hx
  · convert! iUnion_mem_generateMeasurableRec fun _ => ⟨i, h, hx⟩
    exact (iUnion_const x).symm

/-- An inductive principle for the elements of `generateMeasurableRec`. -/
@[elab_as_elim]
/-
**MeasurableSpace.generateMeasurableRec_induction** 是 Mathlib 中的一个定理，位于命名空间 `Mea
surableSpace`。
形式化陈述：generateMeasurableRec_induction {s : Set (Set α)} {i : Ordinal} {t : Set α
} {p : Set α -> Prop} (hs : forall t in s, p t) (h0 : p ∅) (hc : forall u, p u -
> (exists j < i, u in generateMeasurableRec s j) -> p uᶜ) (hn : forall f : Nat -
> Set α, (forall n, p (f n) ∧ exists j < i, f n in generateMeasurableRec s j) ->
 p (⋃ n, f n)) : t in generateMeasurableRec s i -> p t
参数：Set α；hs : forall t in s, p t；h0 : p ∅；hc : forall u, p u -> (exists j < i, u
 in generateMeasurableRec s j) -> p uᶜ；hn : forall f : Nat -> Set α, (forall n, 
p (f n) ∧ exists j < i, f n in generateMeasurableRec s j) -> p (⋃ n, f n)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `WellFoundedLT.induction`：induction {motive : α -> Prop} (a : α) (ind : f
orall x, (forall y, y < x -> motive y) -> motive x) : motive a
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用定理 `LT.lt.le`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a < b → a ≤ b
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSpace.generateMeasurableRec.eq_def`：∀ {α : Type u} (s : Set (S
et α)) (i : Ordinal.{u_1}),   MeasurableSpace.generateMeasurableRec s i =     ha
ve S := ⋃ j, ⋃ (_ : j < i), Measur…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `LT.lt.trans_le`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a < b 
→ b ≤ c → a < c
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Set.mem_iUnion₂`：mem_iUnion₂ {x : γ} {s : forall i, κ i -> Set γ} : (x i
n ⋃ (i) (j), s i j) ↔ exists i j, x in s i j
· 使用定理 `Subtype.property`：∀ {α : Sort u} {p : α → Prop} (self : Subtype p), p ↑s
elf
· 使用引理 `le_rfl`：le_rfl : a <= a

--- 原说明 ---
An inductive principle for the elements of `generateMeasurableRec`.
-/
theorem generateMeasurableRec_induction {s : Set (Set α)} {i : Ordinal} {t : Set α}
    {p : Set α → Prop} (hs : ∀ t ∈ s, p t) (h0 : p ∅)
    (hc : ∀ u, p u → (∃ j < i, u ∈ generateMeasurableRec s j) → p uᶜ)
    (hn : ∀ f : ℕ → Set α,
      (∀ n, p (f n) ∧ ∃ j < i, f n ∈ generateMeasurableRec s j) → p (⋃ n, f n)) :
    t ∈ generateMeasurableRec s i → p t := by
  suffices H : ∀ k ≤ i, ∀ t ∈ generateMeasurableRec s k, p t from H i le_rfl t
  intro k
  apply WellFoundedLT.induction k
  intro k IH hk t
  replace IH := fun j hj => IH j hj (hj.le.trans hk)
  unfold generateMeasurableRec
  rintro (((ht | rfl) | ht) | ⟨f, rfl⟩)
  · exact hs t ht
  · exact h0
  · simp_rw [mem_image, mem_iUnion₂] at ht
    obtain ⟨u, ⟨⟨j, hj, hj'⟩, rfl⟩⟩ := ht
    exact hc u (IH j hj u hj') ⟨j, hj.trans_le hk, hj'⟩
  · apply hn
    intro n
    obtain ⟨j, hj, hj'⟩ := mem_iUnion₂.1 (f n).2
    use IH j hj _ hj', j, hj.trans_le hk
/-
**MeasurableSpace.generateMeasurableRec_omega_one** 是 Mathlib 中的一个定理，位于命名空间 `Mea
surableSpace`。
形式化陈述：generateMeasurableRec_omega_one (s : Set (Set α)) : generateMeasurableRec 
s (ω₁ : Ordinal.{v}) = ⋃ i < (ω₁ : Ordinal.{v}), generateMeasurableRec s i
参数：s : Set (Set α)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `LE.le.antisymm'`：∀ {α : Type u_1} [inst : PartialOrder α] {a b : α}, b ≤
 a → a ≤ b → a = b
· 使用定理 `Set.iUnion₂_subset`：iUnion₂_subset {s : forall i, κ i -> Set α} {t : Set
 α} (h : forall i j, s i j subseteq t) : ⋃ (i) (j), s i j subseteq t
· 使用定理 `MeasurableSpace.generateMeasurableRec_mono`：generateMeasurableRec_mono (
s : Set (Set α)) : Monotone (generateMeasurableRec s)
· 使用定理 `LT.lt.le`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a < b → a ≤ b
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Set.mem_iUnion₂`：mem_iUnion₂ {x : γ} {s : forall i, κ i -> Set γ} : (x i
n ⋃ (i) (j), s i j) ↔ exists i j, x in s i j
· 使用定理 `MeasurableSpace.generateMeasurableRec_induction`：generateMeasurableRec_i
nduction {s : Set (Set α)} {i : Ordinal} {t : Set α} {p : Set α -> Prop} (hs : f
orall t in s, p t) (h0 : p ∅) (hc : f…
· 使用定理 `Ordinal.omega_pos`：omega_pos (o : Ordinal) : 0 < ω_ o
· 使用定理 `MeasurableSpace.self_subset_generateMeasurableRec`：self_subset_generateM
easurableRec (s : Set (Set α)) (i : Ordinal) : s subseteq generateMeasurableRec 
s i
· 使用定理 `MeasurableSpace.empty_mem_generateMeasurableRec`：empty_mem_generateMeasu
rableRec (s : Set (Set α)) (i : Ordinal) : ∅ in generateMeasurableRec s i
· 使用定理 `Order.IsSuccLimit.succ_lt`：∀ {α : Type u_1} {a b : α} [inst : PartialOrd
er α] [inst_1 : SuccOrder α],   Order.IsSuccLimit b → a < b → Order.succ a < b
· 使用定理 `Cardinal.isSuccLimit_omega`：isSuccLimit_omega (o : Ordinal) : IsSuccLimi
t (ω_ o)
· 使用定理 `MeasurableSpace.compl_mem_generateMeasurableRec`：compl_mem_generateMeasu
rableRec {s : Set (Set α)} {i j : Ordinal} (h : j < i) {t : Set α} (ht : t in ge
nerateMeasurableRec s j) : tᶜ in gene…
· 使用定理 `Order.lt_succ`：lt_succ (a : α) : a < succ a
· 使用定理 `Ordinal.instNoMaxOrder`：NoMaxOrder Ordinal.{u_1}
· 使用定理 `Ordinal.lift_iSup_add_one_lt_of_lt_cof`：lift_iSup_add_one_lt_of_lt_cof {
f : β -> Ordinal.{u}} {a : Ordinal.{u}} (ha : Cardinal.lift.{u} #β < (lift.{v} a
).cof) (hf : forall i, f i <…
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `Cardinal.mk_eq_aleph0`：mk_eq_aleph0 (α : Type*) [Countable α] [Infinite 
α] : #α = ℵ₀
· 使用定理 `instCountableNat`：Countable ℕ
· 使用定理 `instInfiniteNat`：Infinite ℕ
· 使用定理 `Cardinal.lift_aleph0`：lift_aleph0 : lift ℵ₀ = ℵ₀
· 使用定理 `Ordinal.lift_uzero`：lift_uzero (a : Ordinal.{u}) : lift.{0} a = a
· 使用定理 `Cardinal.cof_omega_one`：cof_omega_one : cof ω₁ = ℵ₁
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `MeasurableSpace.iUnion_mem_generateMeasurableRec`：iUnion_mem_generateMea
surableRec {s : Set (Set α)} {i : Ordinal} {f : Nat -> Set α} (hf : forall n, ex
ists j < i, f n in generateMeasurableR…
· 使用定理 `Ordinal.lt_iSup_add_one`：lt_iSup_add_one {ι} (f : ι -> Ordinal.{u}) [Sma
ll.{u} ι] (i) : f i < ⨆ i, f i + 1
· 使用定理 `UnivLE.small`：∀ [self : UnivLE.{u, v}] (α : Type u), Small.{v, u} α
（共 32 条，此处仅展示前 30 条）
-/
theorem generateMeasurableRec_omega_one (s : Set (Set α)) :
    generateMeasurableRec s (ω₁ : Ordinal.{v}) =
      ⋃ i < (ω₁ : Ordinal.{v}), generateMeasurableRec s i := by
  apply (iUnion₂_subset fun i h => generateMeasurableRec_mono s h.le).antisymm'
  intro t ht
  rw [mem_iUnion₂]
  refine generateMeasurableRec_induction ?_ ?_ ?_ ?_ ht
  · intro t ht
    exact ⟨0, omega_pos 1, self_subset_generateMeasurableRec s 0 ht⟩
  · exact ⟨0, omega_pos 1, empty_mem_generateMeasurableRec s 0⟩
  · rintro u - ⟨j, hj, hj'⟩
    exact ⟨_, (isSuccLimit_omega 1).succ_lt hj,
      compl_mem_generateMeasurableRec (Order.lt_succ j) hj'⟩
  · intro f H
    choose I hI using fun n => (H n).1
    simp_rw [exists_prop] at hI
    refine ⟨_, Ordinal.lift_iSup_add_one_lt_of_lt_cof ?_ fun n => (hI n).1,
      iUnion_mem_generateMeasurableRec fun n => ⟨_, Ordinal.lt_iSup_add_one I n, (hI n).2⟩⟩
    simp
/-
**MeasurableSpace.generateMeasurableRec_subset** 是 Mathlib 中的一个定理，位于命名空间 `Measur
ableSpace`。
形式化陈述：generateMeasurableRec_subset (s : Set (Set α)) (i : Ordinal) : generateMea
surableRec s i subseteq { t | GenerateMeasurable s t }
参数：s : Set (Set α)；i : Ordinal。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `WellFoundedLT.induction`：induction {motive : α -> Prop} (a : α) (ind : f
orall x, (forall y, y < x -> motive y) -> motive x) : motive a
· 使用定理 `MeasurableSpace.generateMeasurableRec_induction`：generateMeasurableRec_i
nduction {s : Set (Set α)} {i : Ordinal} {t : Set α} {p : Set α -> Prop} (hs : f
orall t in s, p t) (h0 : p ∅) (hc : f…
· 使用定理 `And.left`：∀ {a b : Prop}, a ∧ b → a
-/
theorem generateMeasurableRec_subset (s : Set (Set α)) (i : Ordinal) :
    generateMeasurableRec s i ⊆ { t | GenerateMeasurable s t } := by
  apply WellFoundedLT.induction i
  exact fun i IH t ht => generateMeasurableRec_induction .basic .empty
    (fun u _ ⟨j, hj, hj'⟩ => .compl _ (IH j hj hj')) (fun f H => .iUnion _ fun n => (H n).1) ht

/-- `generateMeasurableRec s ω₁` generates precisely the smallest sigma-algebra containing `s`. -/
/-
**MeasurableSpace.generateMeasurable_eq_rec** 是 Mathlib 中的一个定理，位于命名空间 `Measurabl
eSpace`。
形式化陈述：generateMeasurable_eq_rec (s : Set (Set α)) : { t | GenerateMeasurable s t
 } = generateMeasurableRec s ω₁
参数：s : Set (Set α)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `LE.le.antisymm'`：∀ {α : Type u_1} [inst : PartialOrder α] {a b : α}, b ≤
 a → a ≤ b → a = b
· 使用定理 `MeasurableSpace.generateMeasurableRec_subset`：generateMeasurableRec_subs
et (s : Set (Set α)) (i : Ordinal) : generateMeasurableRec s i subseteq { t | Ge
nerateMeasurable s t }
· 使用定理 `MeasurableSpace.self_subset_generateMeasurableRec`：self_subset_generateM
easurableRec (s : Set (Set α)) (i : Ordinal) : s subseteq generateMeasurableRec 
s i
· 使用定理 `MeasurableSpace.empty_mem_generateMeasurableRec`：empty_mem_generateMeasu
rableRec (s : Set (Set α)) (i : Ordinal) : ∅ in generateMeasurableRec s i
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Set.mem_iUnion₂`：mem_iUnion₂ {x : γ} {s : forall i, κ i -> Set γ} : (x i
n ⋃ (i) (j), s i j) ↔ exists i j, x in s i j
· 使用定理 `MeasurableSpace.generateMeasurableRec_omega_one`：generateMeasurableRec_o
mega_one (s : Set (Set α)) : generateMeasurableRec s (ω₁ : Ordinal.{v}) = ⋃ i < 
(ω₁ : Ordinal.{v}), generateMeasurabl…
· 使用定理 `MeasurableSpace.generateMeasurableRec_mono`：generateMeasurableRec_mono (
s : Set (Set α)) : Monotone (generateMeasurableRec s)
· 使用定理 `LT.lt.le`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a < b → a ≤ b
· 使用定理 `Order.IsSuccLimit.succ_lt`：∀ {α : Type u_1} {a b : α} [inst : PartialOrd
er α] [inst_1 : SuccOrder α],   Order.IsSuccLimit b → a < b → Order.succ a < b
· 使用定理 `Cardinal.isSuccLimit_omega`：isSuccLimit_omega (o : Ordinal) : IsSuccLimi
t (ω_ o)
· 使用定理 `MeasurableSpace.compl_mem_generateMeasurableRec`：compl_mem_generateMeasu
rableRec {s : Set (Set α)} {i j : Ordinal} (h : j < i) {t : Set α} (ht : t in ge
nerateMeasurableRec s j) : tᶜ in gene…
· 使用定理 `Order.lt_succ`：lt_succ (a : α) : a < succ a
· 使用定理 `Ordinal.instNoMaxOrder`：NoMaxOrder Ordinal.{u_1}
· 使用定理 `MeasurableSpace.iUnion_mem_generateMeasurableRec`：iUnion_mem_generateMea
surableRec {s : Set (Set α)} {i : Ordinal} {f : Nat -> Set α} (hf : forall n, ex
ists j < i, f n in generateMeasurableR…
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a

--- 原说明 ---
`generateMeasurableRec s ω₁` generates precisely the smallest sigma-algebra cont
aining `s`.
-/
theorem generateMeasurable_eq_rec (s : Set (Set α)) :
    { t | GenerateMeasurable s t } = generateMeasurableRec s ω₁ := by
  apply (generateMeasurableRec_subset s _).antisymm'
  intro t ht
  induction ht with
  | basic u hu => exact self_subset_generateMeasurableRec s _ hu
  | empty => exact empty_mem_generateMeasurableRec s _
  | compl u _ IH =>
    rw [generateMeasurableRec_omega_one, mem_iUnion₂] at IH
    obtain ⟨i, hi, hi'⟩ := IH
    exact generateMeasurableRec_mono _ ((isSuccLimit_omega 1).succ_lt hi).le
      (compl_mem_generateMeasurableRec (Order.lt_succ i) hi')
  | iUnion f _ IH =>
    simp_rw [generateMeasurableRec_omega_one, mem_iUnion₂, exists_prop] at IH
    exact iUnion_mem_generateMeasurableRec IH

/-- `generateMeasurableRec` is constant for ordinals `≥ ω₁`. -/
/-
**MeasurableSpace.generateMeasurableRec_of_omega_one_le** 是 Mathlib 中的一个定理，位于命名空
间 `MeasurableSpace`。
形式化陈述：generateMeasurableRec_of_omega_one_le (s : Set (Set α)) {i : Ordinal.{v}} 
(hi : ω₁ <= i) : generateMeasurableRec s i = generateMeasurableRec s (ω₁ : Ordin
al.{v})
参数：s : Set (Set α)；hi : ω₁ <= i。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `LE.le.antisymm'`：∀ {α : Type u_1} [inst : PartialOrder α] {a b : α}, b ≤
 a → a ≤ b → a = b
· 使用定理 `MeasurableSpace.generateMeasurableRec_mono`：generateMeasurableRec_mono (
s : Set (Set α)) : Monotone (generateMeasurableRec s)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MeasurableSpace.generateMeasurable_eq_rec`：generateMeasurable_eq_rec (s 
: Set (Set α)) : { t | GenerateMeasurable s t } = generateMeasurableRec s ω₁
· 使用定理 `MeasurableSpace.generateMeasurableRec_subset`：generateMeasurableRec_subs
et (s : Set (Set α)) (i : Ordinal) : generateMeasurableRec s i subseteq { t | Ge
nerateMeasurable s t }

--- 原说明 ---
`generateMeasurableRec` is constant for ordinals `≥ ω₁`.
-/
theorem generateMeasurableRec_of_omega_one_le (s : Set (Set α)) {i : Ordinal.{v}} (hi : ω₁ ≤ i) :
    generateMeasurableRec s i = generateMeasurableRec s (ω₁ : Ordinal.{v}) := by
  apply (generateMeasurableRec_mono s hi).antisymm'
  rw [← generateMeasurable_eq_rec]
  exact generateMeasurableRec_subset s i

/-- At each step of the inductive construction, the cardinality bound `≤ #s ^ ℵ₀` holds. -/
/-
**MeasurableSpace.cardinal_generateMeasurableRec_le** 是 Mathlib 中的一个定理，位于命名空间 `M
easurableSpace`。
形式化陈述：cardinal_generateMeasurableRec_le (s : Set (Set α)) (i : Ordinal.{v}) : #(
generateMeasurableRec s i) <= max #s 2 ^ ℵ₀
参数：s : Set (Set α)；i : Ordinal.{v}。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Nat.instAtLeastTwoHAddOfNat`：∀ (n : ℕ) [NeZero n], (n + 1).AtLeastTwo
· 使用定理 `Nat.instNeZeroSucc`：∀ {n : ℕ}, NeZero (n + 1)
· 使用定理 `WellFoundedLT.induction`：induction {motive : α -> Prop} (a : α) (ind : f
orall x, (forall y, y < x -> motive y) -> motive x) : motive a
· 使用定理 `Cardinal.power_le_power_right`：power_le_power_right {a b c : Cardinal} :
 a <= b -> a ^ c <= b ^ c
· 使用定理 `le_max_right`：∀ {α : Type u_1} [inst : LinearOrder α] (a b : α), b ≤ max
 a b
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用定理 `Cardinal.aleph0_le_continuum`：aleph0_le_continuum : ℵ₀ <= 𝔠
· 使用引理 `Cardinal.mk_biUnion_le_of_le_lift`：mk_biUnion_le_of_le_lift {β : Type v}
 {o : Ordinal.{u}} {c : Cardinal.{v}} (ho : lift.{v} o.card <= lift.{u} c) (hc :
 ℵ₀ <= c) (A : Ordinal …
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Cardinal.lift_power`：lift_power (a b : Cardinal.{u}) : lift.{v} (a ^ b) 
= lift.{v} a ^ lift.{v} b
· 使用定理 `Cardinal.lift_aleph0`：lift_aleph0 : lift ℵ₀ = ℵ₀
· 使用引理 `Cardinal.card_le_of_le_ord`：card_le_of_le_ord {o : Ordinal} {c : Cardina
l} (ho : o <= c.ord) : o.card <= c
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Cardinal.ord_aleph`：ord_aleph (o : Ordinal) : (ℵ_ o).ord = ω_ o
· 使用定理 `Ordinal.lift_one`：lift_one : lift 1 = 1
· 使用定理 `Ordinal.lift_omega`：∀ (o : Ordinal.{u}), Ordinal.lift.{v, u} (Ordinal.om
ega o) = Ordinal.omega (Ordinal.lift.{v, u} o)
· 使用定理 `Ordinal.lift_le`：lift_le {a b : Ordinal} : lift.{u, v} a <= lift.{u, v} 
b ↔ a <= b
· 使用定理 `Ordinal.lift_card`：lift_card (a) : Cardinal.lift.{u, v} (card a) = card 
(lift.{u} a)
· 使用定理 `Cardinal.aleph_one_le_continuum`：aleph_one_le_continuum : ℵ₁ <= 𝔠
· 使用定理 `Cardinal.lift_max`：lift_max {a b : Cardinal} : lift.{u, v} (max a b) = m
ax (lift.{u, v} a) (lift.{u, v} b)
· 使用定理 `Cardinal.lift_ofNat`：lift_ofNat (n : Nat) [n.AtLeastTwo] : lift.{u} (ofN
at(n) : Cardinal.{v}) = OfNat.ofNat n
· 使用定理 `LT.lt.le`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a < b → a ≤ b
· 使用定理 `LT.lt.trans_le`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a < b 
→ b ≤ c → a < c
· 使用定理 `MeasurableSpace.generateMeasurableRec.eq_1`：∀ {α : Type u} (s : Set (Set
 α)) (i : Ordinal.{u_1}),   MeasurableSpace.generateMeasurableRec s i =     (s ∪
 {∅} ∪ compl '' ⋃ j, ⋃ (_ : j < …
· 使用定理 `Cardinal.mk_union_le`：mk_union_le {α : Type u} (S T : Set α) : #(S union
 T : Set α) <= #S + #T
· 使用定理 `Cardinal.add_le_of_le`：add_le_of_le {a b c : Cardinal} (hc : ℵ₀ <= c) (h
1 : a <= c) (h2 : b <= c) : a + b <= c
· 使用定理 `Cardinal.self_le_power`：self_le_power (a : Cardinal) {b : Cardinal} (hb 
: 1 <= b) : a <= a ^ b
· 使用定理 `Cardinal.one_le_aleph0`：one_le_aleph0 : 1 <= ℵ₀
· 使用定理 `le_max_left`：∀ {α : Type u_1} [inst : LinearOrder α] (a b : α), a ≤ max 
a b
· 使用定理 `Cardinal.mk_singleton`：mk_singleton {α : Type u} (x : α) : #({x} : Set α
) = 1
（共 45 条，此处仅展示前 30 条）

--- 原说明 ---
At each step of the inductive construction, the cardinality bound `≤ #s ^ ℵ₀` ho
lds.
-/
theorem cardinal_generateMeasurableRec_le (s : Set (Set α)) (i : Ordinal.{v}) :
    #(generateMeasurableRec s i) ≤ max #s 2 ^ ℵ₀ := by
  suffices ∀ i ≤ ω₁, #(generateMeasurableRec s i) ≤ max #s 2 ^ ℵ₀ by
    obtain hi | hi := le_or_gt i ω₁
    · exact this i hi
    · rw [generateMeasurableRec_of_omega_one_le s hi.le]
      exact this _ le_rfl
  intro i
  apply WellFoundedLT.induction i
  intro i IH hi
  have A : 𝔠 ≤ max #s 2 ^ ℵ₀ := power_le_power_right (le_max_right _ _)
  have B := aleph0_le_continuum.trans A
  have C : #(⋃ j < i, generateMeasurableRec s j) ≤ max #s 2 ^ ℵ₀ := by
    apply mk_biUnion_le_of_le_lift _ B _
    · intro j hj
      exact IH j hj (hj.trans_le hi).le
    · rw [lift_power, lift_aleph0]
      rw [← Ordinal.lift_le.{u}, lift_omega, Ordinal.lift_one, ← ord_aleph] at hi
      have H := card_le_of_le_ord hi
      rw [← Ordinal.lift_card] at H
      apply H.trans <| aleph_one_le_continuum.trans <| power_le_power_right _
      rw [lift_max, Cardinal.lift_ofNat]
      exact le_max_right _ _
  rw [generateMeasurableRec]
  apply_rules [(mk_union_le _ _).trans, add_le_of_le (aleph_one_le_continuum.trans A),
    mk_image_le.trans]
  · exact (self_le_power _ one_le_aleph0).trans (power_le_power_right (le_max_left _ _))
  · rw [mk_singleton]
    exact one_lt_aleph0.le.trans B
  · apply mk_range_le.trans
    simp only [mk_pi, prod_const, Cardinal.lift_uzero, mk_denumerable, lift_aleph0]
    have := @power_le_power_right _ _ ℵ₀ C
    rwa [← power_mul, aleph0_mul_aleph0] at this

/-- If a sigma-algebra is generated by a set of sets `s`, then the sigma-algebra has cardinality at
most `max #s 2 ^ ℵ₀`. -/
/-
**MeasurableSpace.cardinal_generateMeasurable_le** 是 Mathlib 中的一个定理，位于命名空间 `Meas
urableSpace`。
形式化陈述：cardinal_generateMeasurable_le (s : Set (Set α)) : #{ t | GenerateMeasurab
le s t } <= max #s 2 ^ ℵ₀
参数：s : Set (Set α)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Nat.instAtLeastTwoHAddOfNat`：∀ (n : ℕ) [NeZero n], (n + 1).AtLeastTwo
· 使用定理 `Nat.instNeZeroSucc`：∀ {n : ℕ}, NeZero (n + 1)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `MeasurableSpace.generateMeasurable_eq_rec`：generateMeasurable_eq_rec (s 
: Set (Set α)) : { t | GenerateMeasurable s t } = generateMeasurableRec s ω₁
· 使用定理 `MeasurableSpace.cardinal_generateMeasurableRec_le`：cardinal_generateMeas
urableRec_le (s : Set (Set α)) (i : Ordinal.{v}) : #(generateMeasurableRec s i) 
<= max #s 2 ^ ℵ₀

--- 原说明 ---
If a sigma-algebra is generated by a set of sets `s`, then the sigma-algebra has
 cardinality at
most `max #s 2 ^ ℵ₀`.
-/
theorem cardinal_generateMeasurable_le (s : Set (Set α)) :
    #{ t | GenerateMeasurable s t } ≤ max #s 2 ^ ℵ₀ := by
  rw [generateMeasurable_eq_rec.{u, 0}]
  exact cardinal_generateMeasurableRec_le s _

/-- If a sigma-algebra is generated by a set of sets `s`, then the sigma
algebra has cardinality at most `max #s 2 ^ ℵ₀`. -/
/-
**MeasurableSpace.cardinal_measurableSet_le** 是 Mathlib 中的一个定理，位于命名空间 `Measurabl
eSpace`。
形式化陈述：cardinal_measurableSet_le (s : Set (Set α)) : #{ t | @MeasurableSet α (gen
erateFrom s) t } <= max #s 2 ^ ℵ₀
参数：s : Set (Set α)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.cardinal_generateMeasurable_le`：cardinal_generateMeasura
ble_le (s : Set (Set α)) : #{ t | GenerateMeasurable s t } <= max #s 2 ^ ℵ₀

--- 原说明 ---
If a sigma-algebra is generated by a set of sets `s`, then the sigma
algebra has cardinality at most `max #s 2 ^ ℵ₀`.
-/
theorem cardinal_measurableSet_le (s : Set (Set α)) :
    #{ t | @MeasurableSet α (generateFrom s) t } ≤ max #s 2 ^ ℵ₀ :=
  cardinal_generateMeasurable_le s

/-- If a sigma-algebra is generated by a set of sets `s` with cardinality at most the continuum,
then the sigma algebra has the same cardinality bound. -/
/-
**MeasurableSpace.cardinal_generateMeasurable_le_continuum** 是 Mathlib 中的一个定理，位于
命名空间 `MeasurableSpace`。
形式化陈述：cardinal_generateMeasurable_le_continuum {s : Set (Set α)} (hs : #s <= 𝔠) 
: #{ t | GenerateMeasurable s t } <= 𝔠
参数：Set α；hs : #s <= 𝔠。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用定理 `Nat.instAtLeastTwoHAddOfNat`：∀ (n : ℕ) [NeZero n], (n + 1).AtLeastTwo
· 使用定理 `Nat.instNeZeroSucc`：∀ {n : ℕ}, NeZero (n + 1)
· 使用定理 `MeasurableSpace.cardinal_generateMeasurable_le`：cardinal_generateMeasura
ble_le (s : Set (Set α)) : #{ t | GenerateMeasurable s t } <= max #s 2 ^ ℵ₀
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Cardinal.continuum_power_aleph0`：continuum_power_aleph0 : 𝔠 ^ ℵ₀ = 𝔠
· 使用定理 `Cardinal.power_le_power_right`：power_le_power_right {a b c : Cardinal} :
 a <= b -> a ^ c <= b ^ c
· 使用定理 `max_le`：∀ {α : Type u_1} [inst : LinearOrder α] {a b c : α}, a ≤ c → b ≤
 c → max a b ≤ c
· 使用定理 `LT.lt.le`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a < b → a ≤ b
· 使用定理 `Cardinal.nat_lt_continuum`：nat_lt_continuum (n : Nat) : ↑n < 𝔠

--- 原说明 ---
If a sigma-algebra is generated by a set of sets `s` with cardinality at most th
e continuum,
then the sigma algebra has the same cardinality bound.
-/
theorem cardinal_generateMeasurable_le_continuum {s : Set (Set α)} (hs : #s ≤ 𝔠) :
    #{ t | GenerateMeasurable s t } ≤ 𝔠 := by
  apply (cardinal_generateMeasurable_le s).trans
  rw [← continuum_power_aleph0]
  exact_mod_cast power_le_power_right (max_le hs (nat_lt_continuum 2).le)

/-- If a sigma-algebra is generated by a set of sets `s` with cardinality at most the continuum,
then the sigma algebra has the same cardinality bound. -/
/-
**MeasurableSpace.cardinal_measurableSet_le_continuum** 是 Mathlib 中的一个定理，位于命名空间 
`MeasurableSpace`。
形式化陈述：cardinal_measurableSet_le_continuum {s : Set (Set α)} : #s <= 𝔠 -> #{ t | 
@MeasurableSet α (generateFrom s) t } <= 𝔠
参数：Set α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.cardinal_generateMeasurable_le_continuum`：cardinal_gener
ateMeasurable_le_continuum {s : Set (Set α)} (hs : #s <= 𝔠) : #{ t | GenerateMea
surable s t } <= 𝔠

--- 原说明 ---
If a sigma-algebra is generated by a set of sets `s` with cardinality at most th
e continuum,
then the sigma algebra has the same cardinality bound.
-/
theorem cardinal_measurableSet_le_continuum {s : Set (Set α)} :
    #s ≤ 𝔠 → #{ t | @MeasurableSet α (generateFrom s) t } ≤ 𝔠 :=
  cardinal_generateMeasurable_le_continuum

end MeasurableSpace

