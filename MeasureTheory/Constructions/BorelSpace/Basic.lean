/-
Copyright (c) 2017 Johannes Hölzl. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Johannes Hölzl, Yury Kudryashov
-/
module

public import Mathlib.MeasureTheory.Group.Arithmetic
public import Mathlib.Topology.GDelta.MetrizableSpace

/-!
# Borel (measurable) space

## Main definitions

* `borel α` : the least `σ`-algebra that contains all open sets;
* `class BorelSpace` : a space with `TopologicalSpace` and `MeasurableSpace` structures
  such that `‹MeasurableSpace α› = borel α`;
* `class OpensMeasurableSpace` : a space with `TopologicalSpace` and `MeasurableSpace`
  structures such that all open sets are measurable; equivalently, `borel α ≤ ‹MeasurableSpace α›`.
* `BorelSpace` instances on `Empty`, `Unit`, `Bool`, `Nat`, `Int`, `Rat`;
* `MeasurableSpace` and `BorelSpace` instances on `ℝ`, `ℝ≥0`, `ℝ≥0∞`.

## Main statements

* `IsOpen.measurableSet`, `IsClosed.measurableSet`: open and closed sets are measurable;
* `Continuous.measurable` : a continuous function is measurable;
* `Continuous.measurable2` : if `f : α → β` and `g : α → γ` are measurable and `op : β × γ → δ`
  is continuous, then `fun x => op (f x, g x)` is measurable;
* `Measurable.add` etc. : dot notation for arithmetic operations on `Measurable` predicates,
  and similarly for `dist` and `edist`;
* `AEMeasurable.add` : similar dot notation for almost everywhere measurable functions;
-/

@[expose] public section


noncomputable section

open Filter MeasureTheory Set Topology
open scoped NNReal ENNReal MeasureTheory

universe u v w x y

variable {α β γ γ₂ δ : Type*} {ι : Sort y} {s t u : Set α}

open MeasurableSpace TopologicalSpace

/-- `MeasurableSpace` structure generated by `TopologicalSpace`. -/
@[instance_reducible]
/-
**borel** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：borel (α : Type u) [TopologicalSpace α] : MeasurableSpace α
参数：α : Type u。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`MeasurableSpace` structure generated by `TopologicalSpace`.
-/
def borel (α : Type u) [TopologicalSpace α] : MeasurableSpace α :=
  generateFrom { s : Set α | IsOpen s }
/-
**borel_anti** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：borel_anti : Antitone (@borel α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
-/
theorem borel_anti : Antitone (@borel α) := fun _ _ h =>
  MeasurableSpace.generateFrom_le fun _ hs => .basic _ (h _ hs)
/-
**borel_eq_top_of_discrete** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：borel_eq_top_of_discrete [TopologicalSpace α] [DiscreteTopology α] : borel
 α = ⊤
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `top_le_iff`：top_le_iff : ⊤ <= a ↔ a = ⊤
· 使用定理 `isOpen_discrete`：isOpen_discrete (s : Set α) : IsOpen s
-/
theorem borel_eq_top_of_discrete [TopologicalSpace α] [DiscreteTopology α] : borel α = ⊤ :=
  top_le_iff.1 fun s _ => GenerateMeasurable.basic s (isOpen_discrete s)
/-
**borel_eq_generateFrom_of_subbasis** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：borel_eq_generateFrom_of_subbasis {s : Set (Set α)} [t : TopologicalSpace 
α] [SecondCountableTopology α] (hs : t = .generateFrom s) : borel α = .generateF
rom s
参数：Set α；hs : t = .generateFrom s。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
· 使用定理 `MeasurableSet.univ`：∀ {α : Type u_1} {m : MeasurableSpace α}, Measurable
Set Set.univ
· 使用定理 `MeasurableSet.inter`：∀ {α : Type u_1} {m : MeasurableSpace α} {s₁ s₂ : S
et α}, MeasurableSet s₁ → MeasurableSet s₂ → MeasurableSet (s₁ ∩ s₂)
· 使用定理 `TopologicalSpace.isOpen_sUnion_countable`：isOpen_sUnion_countable [Secon
dCountableTopology α] (S : Set (Set α)) (H : forall s in S, IsOpen s) : exists T
 : Set (Set α), T.Countable ∧ …
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MeasurableSet.sUnion`：∀ {α : Type u_1} {m : MeasurableSpace α} {s : Set 
(Set α)},   s.Countable → (∀ t ∈ s, MeasurableSet t) → MeasurableSet (⋃₀ s)
-/
theorem borel_eq_generateFrom_of_subbasis {s : Set (Set α)} [t : TopologicalSpace α]
    [SecondCountableTopology α] (hs : t = .generateFrom s) : borel α = .generateFrom s :=
  le_antisymm
    (generateFrom_le fun u (hu : t.IsOpen u) => by
      rw [hs] at hu
      induction hu with
      | basic u hu => exact GenerateMeasurable.basic u hu
      | univ => exact @MeasurableSet.univ α (generateFrom s)
      | inter s₁ s₂ _ _ hs₁ hs₂ => exact @MeasurableSet.inter α (generateFrom s) _ _ hs₁ hs₂
      | sUnion f hf ih =>
        rcases isOpen_sUnion_countable f (by rwa [hs]) with ⟨v, hv, vf, vu⟩
        rw [← vu]
        exact @MeasurableSet.sUnion α (generateFrom s) _ hv fun x xv => ih _ (vf xv))
    (generateFrom_le fun u hu =>
      GenerateMeasurable.basic _ <| show t.IsOpen u by rw [hs]; exact GenerateOpen.basic _ hu)
/-
**TopologicalSpace.IsTopologicalBasis.borel_eq_generateFrom** 是 Mathlib 中的一个定理，位
于命名空间 ``。
形式化陈述：TopologicalSpace.IsTopologicalBasis.borel_eq_generateFrom [TopologicalSpac
e α] [SecondCountableTopology α] {s : Set (Set α)} (hs : IsTopologicalBasis s) :
 borel α = .generateFrom s
参数：Set α；hs : IsTopologicalBasis s。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `borel_eq_generateFrom_of_subbasis`：borel_eq_generateFrom_of_subbasis {s 
: Set (Set α)} [t : TopologicalSpace α] [SecondCountableTopology α] (hs : t = .g
enerateFrom s) : borel …
· 使用定理 `TopologicalSpace.IsTopologicalBasis.eq_generateFrom`：∀ {α : Type u} [t :
 TopologicalSpace α] {s : Set (Set α)},   TopologicalSpace.IsTopologicalBasis s 
→ t = TopologicalSpace.generateFrom s
-/
theorem TopologicalSpace.IsTopologicalBasis.borel_eq_generateFrom [TopologicalSpace α]
    [SecondCountableTopology α] {s : Set (Set α)} (hs : IsTopologicalBasis s) :
    borel α = .generateFrom s :=
  borel_eq_generateFrom_of_subbasis hs.eq_generateFrom
/-
**isPiSystem_isOpen** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：isPiSystem_isOpen [TopologicalSpace α] : IsPiSystem ({s : Set α | IsOpen s
})
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsOpen.inter`：IsOpen.inter (s t : Set α) : IsOpen α s -> IsOpen α t -> I
sOpen α (s inter t)
-/
theorem isPiSystem_isOpen [TopologicalSpace α] : IsPiSystem ({s : Set α | IsOpen s}) :=
  fun _s hs _t ht _ => IsOpen.inter hs ht
/-
**isPiSystem_isClosed** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：isPiSystem_isClosed [TopologicalSpace α] : IsPiSystem ({s : Set α | IsClos
ed s})
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsClosed.inter`：IsClosed.inter (h₁ : IsClosed s₁) (h₂ : IsClosed s₂) : I
sClosed (s₁ inter s₂)
-/
lemma isPiSystem_isClosed [TopologicalSpace α] : IsPiSystem ({s : Set α | IsClosed s}) :=
  fun _s hs _t ht _ ↦ IsClosed.inter hs ht
/-
**borel_eq_generateFrom_isClosed** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：borel_eq_generateFrom_isClosed [TopologicalSpace α] : borel α = .generateF
rom { s | IsClosed s }
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
· 使用定理 `MeasurableSet.of_compl`：∀ {α : Type u_1} {s : Set α} {m : MeasurableSpac
e α}, MeasurableSet sᶜ → MeasurableSet s
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `isClosed_compl_iff`：isClosed_compl_iff {s : Set X} : IsClosed sᶜ ↔ IsOpe
n s
· 使用定理 `isOpen_compl_iff`：∀ {X : Type u} {s : Set X} [inst : TopologicalSpace X]
, IsOpen sᶜ ↔ IsClosed s
-/
theorem borel_eq_generateFrom_isClosed [TopologicalSpace α] :
    borel α = .generateFrom { s | IsClosed s } :=
  le_antisymm
    (generateFrom_le fun _t ht =>
      @MeasurableSet.of_compl α _ (generateFrom { s | IsClosed s })
        (GenerateMeasurable.basic _ <| isClosed_compl_iff.2 ht))
    (generateFrom_le fun _t ht =>
      @MeasurableSet.of_compl α _ (borel α) (GenerateMeasurable.basic _ <| isOpen_compl_iff.2 ht))
/-
**borel_comap** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：borel_comap {f : α -> β} {t : TopologicalSpace β} : @borel α (t.induced f)
 = (@borel β t).comap f
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MeasurableSpace.comap_generateFrom`：comap_generateFrom {f : α -> β} {s :
 Set (Set β)} : (generateFrom s).comap f = generateFrom (preimage f '' s)
-/
theorem borel_comap {f : α → β} {t : TopologicalSpace β} :
    @borel α (t.induced f) = (@borel β t).comap f :=
  comap_generateFrom.symm
/-
**Continuous.borel_measurable** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：Continuous.borel_measurable [TopologicalSpace α] [TopologicalSpace β] {f :
 α -> β} (hf : Continuous f) : @Measurable α β (borel α) (borel β) f
参数：hf : Continuous f。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Measurable.of_le_map`：∀ {α : Type u_1} {β : Type u_2} {m₁ : MeasurableSp
ace α} {m₂ : MeasurableSpace β} {f : α → β},   m₂ ≤ MeasurableSpace.map f m₁ → M
easurable …
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
· 使用定理 `IsOpen.preimage`：IsOpen.preimage (hf : Continuous f) {t : Set Y} (h : Is
Open t) : IsOpen (f ⁻¹' t)
-/
theorem Continuous.borel_measurable [TopologicalSpace α] [TopologicalSpace β] {f : α → β}
    (hf : Continuous f) : @Measurable α β (borel α) (borel β) f :=
  Measurable.of_le_map <|
    generateFrom_le fun s hs => GenerateMeasurable.basic (f ⁻¹' s) (hs.preimage hf)

/-- A space with `MeasurableSpace` and `TopologicalSpace` structures such that
all open sets are measurable. -/
/-
**OpensMeasurableSpace** 是 Mathlib 中的一个归纳类型，位于命名空间 ``。
形式化陈述：(α : Type u_6) → [TopologicalSpace α] → [h : MeasurableSpace α] → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A space with `MeasurableSpace` and `TopologicalSpace` structures such that
all open sets are measurable.
-/
class OpensMeasurableSpace (α : Type*) [TopologicalSpace α] [h : MeasurableSpace α] : Prop where
  /-- Borel-measurable sets are measurable. -/
  borel_le : borel α ≤ h

/-- A space with `MeasurableSpace` and `TopologicalSpace` structures such that
the `σ`-algebra of measurable sets is exactly the `σ`-algebra generated by open sets. -/
/-
**BorelSpace** 是 Mathlib 中的一个归纳类型，位于命名空间 ``。
形式化陈述：(α : Type u_6) → [TopologicalSpace α] → [MeasurableSpace α] → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A space with `MeasurableSpace` and `TopologicalSpace` structures such that
the `σ`-algebra of measurable sets is exactly the `σ`-algebra generated by open 
sets.
-/
class BorelSpace (α : Type*) [TopologicalSpace α] [MeasurableSpace α] : Prop where
  /-- The measurable sets are exactly the Borel-measurable sets. -/
  measurable_eq : ‹MeasurableSpace α› = borel α

namespace Mathlib.Tactic.Borelize

open Lean Elab Term Tactic Meta

/-- The behaviour of `borelize α` depends on the existing assumptions on `α`.

- if `α` is a topological space with instances `[MeasurableSpace α] [BorelSpace α]`, then
  `borelize α` replaces the former instance by `borel α`;
- otherwise, `borelize α` adds instances `borel α : MeasurableSpace α` and `⟨rfl⟩ : BorelSpace α`.

Finally, `borelize α β γ` runs `borelize α; borelize β; borelize γ`.
-/
syntax "borelize" (ppSpace colGt term:max)* : tactic

/-- Add instances `borel e : MeasurableSpace e` and `⟨rfl⟩ : BorelSpace e`. -/
meta def addBorelInstance (e : Expr) : TacticM Unit := do
  let t ← Lean.Elab.Term.exprToSyntax e
  evalTactic <| ← `(tactic|
    refine_lift
      letI : MeasurableSpace $t := borel $t
      haveI : BorelSpace $t := ⟨rfl⟩
      ?_)

/-- Given a type `e`, an assumption `i : MeasurableSpace e`, and an instance `[BorelSpace e]`,
replace `i` with `borel e`. -/
meta def borelToRefl (e : Expr) (i : FVarId) : TacticM Unit := do
  let te ← Lean.Elab.Term.exprToSyntax e
  evalTactic <| ← `(tactic|
    have := @BorelSpace.measurable_eq $te _ _ _)
  try
    liftMetaTactic fun m => return [← subst m i]
  catch _ =>
    let et ← synthInstance (← mkAppOptM ``TopologicalSpace #[e])
    throwError m!"\
      `‹TopologicalSpace {e}› := {et}\n\
      depends on\n\
      {Expr.fvar i} : MeasurableSpace {e}`\n\
      so `borelize` isn't available"
  evalTactic <| ← `(tactic|
    refine_lift
      letI : MeasurableSpace $te := borel $te
      ?_)

/-- Given a type `$t`, if there is an assumption `[i : MeasurableSpace $t]`, then try to prove
`[BorelSpace $t]` and replace `i` with `borel $t`. Otherwise, add instances
`borel $t : MeasurableSpace $t` and `⟨rfl⟩ : BorelSpace $t`. -/
meta def borelize (t : Term) : TacticM Unit := withMainContext do
  let u ← mkFreshLevelMVar
  let e ← withoutRecover <| Tactic.elabTermEnsuringType t (mkSort (mkLevelSucc u))
  let i? ← findLocalDeclWithType? (← mkAppOptM ``MeasurableSpace #[e])
  i?.elim (addBorelInstance e) (borelToRefl e)

elab_rules : tactic
  | `(tactic| borelize $[$t:term]*) => t.forM borelize

end Mathlib.Tactic.Borelize

/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) OrderDual.opensMeasurableSpace {α : Type*} [TopologicalSpace α]
    [MeasurableSpace α] [h : OpensMeasurableSpace α] : OpensMeasurableSpace αᵒᵈ where
  borel_le := h.borel_le
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) OrderDual.borelSpace {α : Type*} [TopologicalSpace α]
    [MeasurableSpace α] [h : BorelSpace α] : BorelSpace αᵒᵈ where
  measurable_eq := h.measurable_eq

/-- In a `BorelSpace` all open sets are measurable. -/
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
In a `BorelSpace` all open sets are measurable.
-/
instance (priority := 100) BorelSpace.opensMeasurable {α : Type*} [TopologicalSpace α]
    [MeasurableSpace α] [BorelSpace α] : OpensMeasurableSpace α :=
  ⟨ge_of_eq <| BorelSpace.measurable_eq⟩
/-
**Subtype.borelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Subtype.borelSpace {α : Type*} [TopologicalSpace α] [MeasurableSpace α] [h
α : BorelSpace α] (p : α -> Prop) : BorelSpace (Subtype p)
参数：p : α -> Prop。
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `BorelSpace.measurable_eq`：∀ {α : Type u_6} {inst : TopologicalSpace α} {
inst_1 : MeasurableSpace α} [self : BorelSpace α], inst_1 = borel α
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `borel_comap`：borel_comap {f : α -> β} {t : TopologicalSpace β} : @borel 
α (t.induced f) = (@borel β t).comap f
-/
instance Subtype.borelSpace {α : Type*} [TopologicalSpace α] [MeasurableSpace α]
    [hα : BorelSpace α] (p : α → Prop) : BorelSpace (Subtype p) :=
  ⟨by borelize α; symm; apply borel_comap⟩
/-
**Countable.instBorelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Countable.instBorelSpace [Countable α] [MeasurableSpace α] [MeasurableSing
letonClass α] [TopologicalSpace α] [DiscreteTopology α] : BorelSpace α
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.Countable.measurableSet`：Set.Countable.measurableSet {s : Set α} (hs
 : s.Countable) : MeasurableSet s
· 使用定理 `Set.to_countable`：to_countable (s : Set α) [Countable s] : s.Countable
· 使用定理 `SetCoe.countable`：∀ {α : Type u} [Countable α] (s : Set α), Countable ↑s
· 使用定理 `MeasurableSpace.measurableSet_generateFrom`：measurableSet_generateFrom {
s : Set (Set α)} {t : Set α} (ht : t in s) : MeasurableSet[generateFrom s] t
· 使用定理 `isOpen_discrete`：isOpen_discrete (s : Set α) : IsOpen s
· 使用定理 `MeasurableSpace.ext`：MeasurableSpace.ext {m₁ m₂ : MeasurableSpace α} (h 
: forall s : Set α, MeasurableSet[m₁] s ↔ MeasurableSet[m₂] s) : m₁ = m₂
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
instance Countable.instBorelSpace [Countable α] [MeasurableSpace α] [MeasurableSingletonClass α]
    [TopologicalSpace α] [DiscreteTopology α] : BorelSpace α := by
  have : ∀ s, @MeasurableSet α inferInstance s := fun s ↦ s.to_countable.measurableSet
  have : ∀ s, @MeasurableSet α (borel α) s := fun s ↦ measurableSet_generateFrom (isOpen_discrete s)
  exact ⟨by aesop⟩
/-
**Subtype.opensMeasurableSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Subtype.opensMeasurableSpace {α : Type*} [TopologicalSpace α] [MeasurableS
pace α] [h : OpensMeasurableSpace α] (p : α -> Prop) : OpensMeasurableSpace (Sub
type p)
参数：p : α -> Prop。
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `borel_comap`：borel_comap {f : α -> β} {t : TopologicalSpace β} : @borel 
α (t.induced f) = (@borel β t).comap f
· 使用定理 `MeasurableSpace.comap_mono`：comap_mono (h : m₁ <= m₂) : m₁.comap g <= m₂
.comap g
· 使用定理 `OpensMeasurableSpace.borel_le`：∀ {α : Type u_6} {inst : TopologicalSpace
 α} {h : MeasurableSpace α} [self : OpensMeasurableSpace α], borel α ≤ h
-/
instance Subtype.opensMeasurableSpace {α : Type*} [TopologicalSpace α] [MeasurableSpace α]
    [h : OpensMeasurableSpace α] (p : α → Prop) : OpensMeasurableSpace (Subtype p) :=
  ⟨by
    rw [borel_comap]
    exact comap_mono h.1⟩
/-
**opensMeasurableSpace_iff_forall_measurableSet** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：opensMeasurableSpace_iff_forall_measurableSet [TopologicalSpace α] [Measur
ableSpace α] : OpensMeasurableSpace α ↔ (forall (s : Set α), IsOpen s -> Measura
bleSet s)
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `OpensMeasurableSpace.borel_le`：∀ {α : Type u_6} {inst : TopologicalSpace
 α} {h : MeasurableSpace α} [self : OpensMeasurableSpace α], borel α ≤ h
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
-/
lemma opensMeasurableSpace_iff_forall_measurableSet
    [TopologicalSpace α] [MeasurableSpace α] :
    OpensMeasurableSpace α ↔ (∀ (s : Set α), IsOpen s → MeasurableSet s) := by
  refine ⟨fun h s hs ↦ ?_, fun h ↦ ⟨generateFrom_le h⟩⟩
  exact OpensMeasurableSpace.borel_le _ <| GenerateMeasurable.basic _ hs
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) BorelSpace.countablyGenerated {α : Type*} [TopologicalSpace α]
    [MeasurableSpace α] [BorelSpace α] [SecondCountableTopology α] : CountablyGenerated α := by
  obtain ⟨b, bct, -, hb⟩ := exists_countable_basis α
  refine ⟨⟨b, bct, ?_⟩⟩
  borelize α
  exact hb.borel_eq_generateFrom

section

variable [TopologicalSpace α] [MeasurableSpace α] [OpensMeasurableSpace α] [TopologicalSpace β]
  [MeasurableSpace β] [OpensMeasurableSpace β] [TopologicalSpace γ] [MeasurableSpace γ]
  [BorelSpace γ] [TopologicalSpace γ₂] [MeasurableSpace γ₂] [BorelSpace γ₂] [MeasurableSpace δ]

/-
**IsOpen.measurableSet** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：IsOpen.measurableSet (h : IsOpen s) : MeasurableSet s
参数：h : IsOpen s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `OpensMeasurableSpace.borel_le`：∀ {α : Type u_6} {inst : TopologicalSpace
 α} {h : MeasurableSpace α} [self : OpensMeasurableSpace α], borel α ≤ h
-/
theorem IsOpen.measurableSet (h : IsOpen s) : MeasurableSet s :=
  OpensMeasurableSpace.borel_le _ <| GenerateMeasurable.basic _ h
/-
**IsOpen.nullMeasurableSet** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：IsOpen.nullMeasurableSet {μ} (h : IsOpen s) : NullMeasurableSet s μ
参数：h : IsOpen s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSet.nullMeasurableSet`：∀ {α : Type u_2} {m0 : MeasurableSpace 
α} {μ : MeasureTheory.Measure α} {s : Set α},   MeasurableSet s → MeasureTheory.
NullMeasurableSet s μ
· 使用定理 `IsOpen.measurableSet`：IsOpen.measurableSet (h : IsOpen s) : MeasurableSe
t s
-/
theorem IsOpen.nullMeasurableSet {μ} (h : IsOpen s) : NullMeasurableSet s μ :=
  h.measurableSet.nullMeasurableSet

open scoped Function in -- required for scoped `on` notation
@[elab_as_elim]
/-
**MeasurableSet.induction_on_open** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：MeasurableSet.induction_on_open {C : forall s : Set γ, MeasurableSet s -> 
Prop} (isOpen : forall U (hU : IsOpen U), C U hU.measurableSet) (compl : forall 
t (ht : MeasurableSet t), C t ht -> C tᶜ ht.compl) (iUnion : forall f : Nat -> S
et γ, Pairwise (Disjoint on f) -> forall (hf : forall i, MeasurableSet (f i)), (
forall i, C (f i) (hf i)) -> C (⋃ i, f i) (.iUnion hf)) : forall t (ht : Measura
bleSet t), C t ht
参数：isOpen : forall U (hU : IsOpen U), C U hU.measurableSet；compl : forall t (ht 
: MeasurableSet t), C t ht -> C tᶜ ht.compl；iUnion : forall f : Nat -> Set γ, Pa
irwise (Disjoint on f) -> forall (hf : forall i, MeasurableSet (f i)), (forall i
, C (f i) (hf i)) -> C (⋃ i, f i) (.iUnion hf)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsOpen.measurableSet`：IsOpen.measurableSet (h : IsOpen s) : MeasurableSe
t s
· 使用定理 `BorelSpace.opensMeasurable`：∀ {α : Type u_6} [inst : TopologicalSpace α]
 [inst_1 : MeasurableSpace α] [BorelSpace α], OpensMeasurableSpace α
· 使用定理 `MeasurableSet.compl`：∀ {α : Type u_1} {s : Set α} {m : MeasurableSpace α
}, MeasurableSet s → MeasurableSet sᶜ
· 使用定理 `MeasurableSet.iUnion`：∀ {α : Type u_1} {ι : Sort u_6} {m : MeasurableSpa
ce α} [Countable ι] ⦃f : ι → Set α⦄,   (∀ (b : ι), MeasurableSet (f b)) → Measur
ableSet (⋃…
· 使用定理 `instCountableNat`：Countable ℕ
· 使用定理 `MeasurableSpace.induction_on_inter`：induction_on_inter {m : MeasurableSp
ace α} {C : forall s : Set α, MeasurableSet s -> Prop} {s : Set (Set α)} (h_eq :
 m = generateFrom s) (h_…
· 使用定理 `BorelSpace.measurable_eq`：∀ {α : Type u_6} {inst : TopologicalSpace α} {
inst_1 : MeasurableSpace α} [self : BorelSpace α], inst_1 = borel α
· 使用定理 `isPiSystem_isOpen`：isPiSystem_isOpen [TopologicalSpace α] : IsPiSystem (
{s : Set α | IsOpen s})
· 使用定理 `isOpen_empty`：∀ {X : Type u} [inst : TopologicalSpace X], IsOpen ∅
-/
theorem MeasurableSet.induction_on_open {C : ∀ s : Set γ, MeasurableSet s → Prop}
    (isOpen : ∀ U (hU : IsOpen U), C U hU.measurableSet)
    (compl : ∀ t (ht : MeasurableSet t), C t ht → C tᶜ ht.compl)
    (iUnion : ∀ f : ℕ → Set γ, Pairwise (Disjoint on f) → ∀ (hf : ∀ i, MeasurableSet (f i)),
      (∀ i, C (f i) (hf i)) → C (⋃ i, f i) (.iUnion hf)) :
    ∀ t (ht : MeasurableSet t), C t ht := fun t ht ↦
  MeasurableSpace.induction_on_inter BorelSpace.measurable_eq isPiSystem_isOpen
    (isOpen _ isOpen_empty) isOpen compl iUnion t ht
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 1000) {s : Set α} [h : HasCountableSeparatingOn α IsOpen s] :
    CountablySeparated s := by
  rw [CountablySeparated.subtype_iff]
  exact .mono (fun _ ↦ IsOpen.measurableSet) Subset.rfl

@[measurability]
/-
**measurableSet_interior** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：measurableSet_interior : MeasurableSet (interior s)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsOpen.measurableSet`：IsOpen.measurableSet (h : IsOpen s) : MeasurableSe
t s
· 使用定理 `isOpen_interior`：isOpen_interior : IsOpen (interior s)
-/
theorem measurableSet_interior : MeasurableSet (interior s) :=
  isOpen_interior.measurableSet
/-
**IsG** 是 Mathlib 中的一个定理，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem IsGδ.measurableSet (h : IsGδ s) : MeasurableSet s := by
  rcases h with ⟨S, hSo, hSc, rfl⟩
  exact MeasurableSet.sInter hSc fun t ht => (hSo t ht).measurableSet
/-
**measurableSet_of_continuousAt** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：measurableSet_of_continuousAt {β} [PseudoEMetricSpace β] (f : α -> β) : Me
asurableSet { x | ContinuousAt f x }
参数：f : α -> β。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsGδ.measurableSet`：IsGδ.measurableSet (h : IsGδ s) : MeasurableSet s
· 使用定理 `IsGδ.setOfPred_continuousAt`：IsGδ.setOfPred_continuousAt [PseudoMetrizab
leSpace Y] (f : X -> Y) : IsGδ { x | ContinuousAt f x }
· 使用定理 `PseudoEMetricSpace.pseudoMetrizableSpace`：∀ {α : Type u_2} [inst : Pseud
oEMetricSpace α], TopologicalSpace.PseudoMetrizableSpace α
-/
theorem measurableSet_of_continuousAt {β} [PseudoEMetricSpace β] (f : α → β) :
    MeasurableSet { x | ContinuousAt f x } :=
  (IsGδ.setOfPred_continuousAt f).measurableSet
/-
**IsClosed.measurableSet** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：IsClosed.measurableSet (h : IsClosed s) : MeasurableSet s
参数：h : IsClosed s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSet.of_compl`：∀ {α : Type u_1} {s : Set α} {m : MeasurableSpac
e α}, MeasurableSet sᶜ → MeasurableSet s
· 使用定理 `IsOpen.measurableSet`：IsOpen.measurableSet (h : IsOpen s) : MeasurableSe
t s
· 使用定理 `IsClosed.isOpen_compl`：∀ {X : Type u} {inst : TopologicalSpace X} {s : S
et X} [self : IsClosed s], IsOpen sᶜ
-/
theorem IsClosed.measurableSet (h : IsClosed s) : MeasurableSet s :=
  h.isOpen_compl.measurableSet.of_compl
/-
**IsClosed.nullMeasurableSet** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：IsClosed.nullMeasurableSet {μ} (h : IsClosed s) : NullMeasurableSet s μ
参数：h : IsClosed s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSet.nullMeasurableSet`：∀ {α : Type u_2} {m0 : MeasurableSpace 
α} {μ : MeasureTheory.Measure α} {s : Set α},   MeasurableSet s → MeasureTheory.
NullMeasurableSet s μ
· 使用定理 `IsClosed.measurableSet`：IsClosed.measurableSet (h : IsClosed s) : Measur
ableSet s
-/
theorem IsClosed.nullMeasurableSet {μ} (h : IsClosed s) : NullMeasurableSet s μ :=
  h.measurableSet.nullMeasurableSet
/-
**IsCompact.measurableSet** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：IsCompact.measurableSet [T2Space α] (h : IsCompact s) : MeasurableSet s
参数：h : IsCompact s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsClosed.measurableSet`：IsClosed.measurableSet (h : IsClosed s) : Measur
ableSet s
· 使用定理 `IsCompact.isClosed`：IsCompact.isClosed [T2Space X] {s : Set X} (hs : IsC
ompact s) : IsClosed s
-/
theorem IsCompact.measurableSet [T2Space α] (h : IsCompact s) : MeasurableSet s :=
  h.isClosed.measurableSet
/-
**IsCompact.nullMeasurableSet** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：IsCompact.nullMeasurableSet [T2Space α] {μ} (h : IsCompact s) : NullMeasur
ableSet s μ
参数：h : IsCompact s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsClosed.nullMeasurableSet`：IsClosed.nullMeasurableSet {μ} (h : IsClosed
 s) : NullMeasurableSet s μ
· 使用定理 `IsCompact.isClosed`：IsCompact.isClosed [T2Space X] {s : Set X} (hs : IsC
ompact s) : IsClosed s
-/
theorem IsCompact.nullMeasurableSet [T2Space α] {μ} (h : IsCompact s) : NullMeasurableSet s μ :=
  h.isClosed.nullMeasurableSet

/-- If two points are topologically inseparable,
then they can't be separated by a Borel measurable set. -/
/-
**Inseparable.mem_measurableSet_iff** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：Inseparable.mem_measurableSet_iff {x y : γ} (h : Inseparable x y) {s : Set
 γ} (hs : MeasurableSet s) : x in s ↔ y in s
参数：h : Inseparable x y；hs : MeasurableSet s。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSet.induction_on_open`：MeasurableSet.induction_on_open {C : fo
rall s : Set γ, MeasurableSet s -> Prop} (isOpen : forall U (hU : IsOpen U), C U
 hU.measurableSet) (c…
· 使用定理 `Inseparable.mem_open_iff`：mem_open_iff (h : x ~ᵢ y) (hs : IsOpen s) : x 
in s ↔ y in s
· 使用定理 `Iff.not`：∀ {a b : Prop}, (a ↔ b) → (¬a ↔ ¬b)
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True

--- 原说明 ---
If two points are topologically inseparable,
then they can't be separated by a Borel measurable set.
-/
theorem Inseparable.mem_measurableSet_iff {x y : γ} (h : Inseparable x y) {s : Set γ}
    (hs : MeasurableSet s) : x ∈ s ↔ y ∈ s :=
  MeasurableSet.induction_on_open (fun _ ↦ h.mem_open_iff) (fun _ _ ↦ Iff.not)
    (fun _ _ _ h ↦ by simp [h]) s hs

/-- If `K` is a compact set in an R₁ space and `s ⊇ K` is a Borel measurable superset,
then `s` includes the closure of `K` as well. -/
/-
**IsCompact.closure_subset_measurableSet** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：IsCompact.closure_subset_measurableSet [R1Space γ] {K s : Set γ} (hK : IsC
ompact K) (hs : MeasurableSet s) (hKs : K subseteq s) : closure K subseteq s
参数：hK : IsCompact K；hs : MeasurableSet s；hKs : K subseteq s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `IsCompact.closure_eq_biUnion_inseparable`：IsCompact.closure_eq_biUnion_i
nseparable {K : Set X} (hK : IsCompact K) : closure K = ⋃ x in K, {y | Inseparab
le x y}
· 使用定理 `Set.iUnion₂_subset_iff`：iUnion₂_subset_iff {s : forall i, κ i -> Set α} 
{t : Set α} : ⋃ (i) (j), s i j subseteq t ↔ forall i j, s i j subseteq t
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Inseparable.mem_measurableSet_iff`：Inseparable.mem_measurableSet_iff {x 
y : γ} (h : Inseparable x y) {s : Set γ} (hs : MeasurableSet s) : x in s ↔ y in 
s

--- 原说明 ---
If `K` is a compact set in an R₁ space and `s ⊇ K` is a Borel measurable superse
t,
then `s` includes the closure of `K` as well.
-/
theorem IsCompact.closure_subset_measurableSet [R1Space γ] {K s : Set γ} (hK : IsCompact K)
    (hs : MeasurableSet s) (hKs : K ⊆ s) : closure K ⊆ s := by
  rw [hK.closure_eq_biUnion_inseparable, iUnion₂_subset_iff]
  exact fun x hx y hy ↦ (hy.mem_measurableSet_iff hs).1 (hKs hx)

/-- In an R₁ topological space with Borel measure `μ`,
the measure of the closure of a compact set `K` is equal to the measure of `K`.

See also `MeasureTheory.Measure.OuterRegular.measure_closure_eq_of_isCompact`
for a version that assumes `μ` to be outer regular
but does not assume the `σ`-algebra to be Borel. -/
/-
**IsCompact.measure_closure** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：IsCompact.measure_closure [R1Space γ] {K : Set γ} (hK : IsCompact K) (μ : 
Measure γ) : μ (closure K) = μ K
参数：hK : IsCompact K；μ : Measure γ。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `MeasureTheory.measure_mono`：measure_mono (h : s subseteq t) : μ s <= μ t
· 使用定理 `MeasureTheory.Measure.instOuterMeasureClass`：∀ {α : Type u_1} [inst : Me
asurableSpace α], MeasureTheory.OuterMeasureClass (MeasureTheory.Measure α) α
· 使用定理 `IsCompact.closure_subset_measurableSet`：IsCompact.closure_subset_measura
bleSet [R1Space γ] {K s : Set γ} (hK : IsCompact K) (hs : MeasurableSet s) (hKs 
: K subseteq s) : closure K …
· 使用定理 `MeasureTheory.measurableSet_toMeasurable`：measurableSet_toMeasurable (μ 
: Measure α) (s : Set α) : MeasurableSet (toMeasurable μ s)
· 使用定理 `MeasureTheory.subset_toMeasurable`：subset_toMeasurable (μ : Measure α) (
s : Set α) : s subseteq toMeasurable μ s
· 使用定理 `MeasureTheory.measure_toMeasurable`：measure_toMeasurable (s : Set α) : μ
 (toMeasurable μ s) = μ s
· 使用定理 `subset_closure`：subset_closure : s subseteq closure s

--- 原说明 ---
In an R₁ topological space with Borel measure `μ`,
the measure of the closure of a compact set `K` is equal to the measure of `K`.

See also `MeasureTheory.Measure.OuterRegular.measure_closure_eq_of_isCompact`
for a version that assumes `μ` to be outer regular
but does not assume the `σ`-algebra to be Borel.
-/
theorem IsCompact.measure_closure [R1Space γ] {K : Set γ} (hK : IsCompact K) (μ : Measure γ) :
    μ (closure K) = μ K := by
  refine le_antisymm ?_ (measure_mono subset_closure)
  calc
    μ (closure K) ≤ μ (toMeasurable μ K) := measure_mono <|
      hK.closure_subset_measurableSet (measurableSet_toMeasurable ..) (subset_toMeasurable ..)
    _ = μ K := measure_toMeasurable ..

@[measurability]
/-
**measurableSet_closure** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：measurableSet_closure : MeasurableSet (closure s)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `IsClosed.measurableSet`：IsClosed.measurableSet (h : IsClosed s) : Measur
ableSet s
· 使用定理 `isClosed_closure`：isClosed_closure : IsClosed (closure s)
-/
theorem measurableSet_closure : MeasurableSet (closure s) :=
  isClosed_closure.measurableSet

@[measurability]
/-
**measurableSet_frontier** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：measurableSet_frontier : MeasurableSet (frontier s)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSet.diff`：∀ {α : Type u_1} {m : MeasurableSpace α} {s₁ s₂ : Se
t α}, MeasurableSet s₁ → MeasurableSet s₂ → MeasurableSet (s₁ \ s₂)
· 使用定理 `measurableSet_closure`：measurableSet_closure : MeasurableSet (closure s)
· 使用定理 `measurableSet_interior`：measurableSet_interior : MeasurableSet (interior
 s)
-/
theorem measurableSet_frontier : MeasurableSet (frontier s) :=
  measurableSet_closure.diff measurableSet_interior
/-
**measurable_of_isOpen** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：measurable_of_isOpen {f : δ -> γ} (hf : forall s, IsOpen s -> MeasurableSe
t (f ⁻¹' s)) : Measurable f
参数：hf : forall s, IsOpen s -> MeasurableSet (f ⁻¹' s)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `BorelSpace.measurable_eq`：∀ {α : Type u_6} {inst : TopologicalSpace α} {
inst_1 : MeasurableSpace α} [self : BorelSpace α], inst_1 = borel α
· 使用定理 `measurable_generateFrom`：measurable_generateFrom [MeasurableSpace α] {s 
: Set (Set β)} {f : α -> β} (h : forall t in s, MeasurableSet (f ⁻¹' t)) : @Meas
urable _ _ _ …
-/
theorem measurable_of_isOpen {f : δ → γ} (hf : ∀ s, IsOpen s → MeasurableSet (f ⁻¹' s)) :
    Measurable f := by
  rw [‹BorelSpace γ›.measurable_eq]
  exact measurable_generateFrom hf
/-
**measurable_of_isClosed** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：measurable_of_isClosed {f : δ -> γ} (hf : forall s, IsClosed s -> Measurab
leSet (f ⁻¹' s)) : Measurable f
参数：hf : forall s, IsClosed s -> MeasurableSet (f ⁻¹' s)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `measurable_of_isOpen`：measurable_of_isOpen {f : δ -> γ} (hf : forall s, 
IsOpen s -> MeasurableSet (f ⁻¹' s)) : Measurable f
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `MeasurableSet.compl_iff`：MeasurableSet.compl_iff : MeasurableSet sᶜ ↔ Me
asurableSet s
· 使用定理 `Set.preimage_compl`：preimage_compl {s : Set β} : f ⁻¹' sᶜ = (f ⁻¹' s)ᶜ
· 使用定理 `isClosed_compl_iff`：isClosed_compl_iff {s : Set X} : IsClosed sᶜ ↔ IsOpe
n s
-/
theorem measurable_of_isClosed {f : δ → γ} (hf : ∀ s, IsClosed s → MeasurableSet (f ⁻¹' s)) :
    Measurable f := by
  apply measurable_of_isOpen; intro s hs
  rw [← MeasurableSet.compl_iff, ← preimage_compl]; apply hf; rw [isClosed_compl_iff]; exact hs
/-
**measurable_of_isClosed'** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：measurable_of_isClosed' {f : δ -> γ} (hf : forall s, IsClosed s -> s.Nonem
pty -> s != univ -> MeasurableSet (f ⁻¹' s)) : Measurable f
参数：hf : forall s, IsClosed s -> s.Nonempty -> s != univ -> MeasurableSet (f ⁻¹' 
s)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `measurable_of_isClosed`：measurable_of_isClosed {f : δ -> γ} (hf : forall
 s, IsClosed s -> MeasurableSet (f ⁻¹' s)) : Measurable f
· 使用定理 `Set.eq_empty_or_nonempty`：eq_empty_or_nonempty (s : Set α) : s = ∅ ∨ s.N
onempty
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
-/
theorem measurable_of_isClosed' {f : δ → γ}
    (hf : ∀ s, IsClosed s → s.Nonempty → s ≠ univ → MeasurableSet (f ⁻¹' s)) : Measurable f := by
  apply measurable_of_isClosed; intro s hs
  rcases eq_empty_or_nonempty s with h1 | h1
  · simp [h1]
  by_cases h2 : s = univ
  · simp [h2]
  exact hf s hs h1 h2
/-
**nhds_isMeasurablyGenerated** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：nhds_isMeasurablyGenerated (a : α) : (𝓝 a).IsMeasurablyGenerated
参数：a : α。
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `nhds_def`：∀ {X : Type u_3} [inst : TopologicalSpace X] (x : X), nhds x =
 ⨅ s ∈ {s | x ∈ s ∧ IsOpen s}, Filter.principal s
· 使用定理 `iInf_subtype'`：∀ {α : Type u_1} {ι : Sort u_4} [inst : CompleteLattice α
] {p : ι → Prop} {f : (i : ι) → p i → α},   ⨅ i, ⨅ (h : p i), f i h = ⨅ x, f ↑x 
⋯
· 使用定理 `MeasurableSet.principal_isMeasurablyGenerated`：∀ {α : Type u_1} [inst : 
MeasurableSpace α] {s : Set α}, MeasurableSet s → (Filter.principal s).IsMeasura
blyGenerated
· 使用定理 `IsOpen.measurableSet`：IsOpen.measurableSet (h : IsOpen s) : MeasurableSe
t s
· 使用定理 `And.right`：∀ {a b : Prop}, a ∧ b → b
· 使用定理 `Subtype.property`：∀ {α : Sort u} {p : α → Prop} (self : Subtype p), p ↑s
elf
-/
instance nhds_isMeasurablyGenerated (a : α) : (𝓝 a).IsMeasurablyGenerated := by
  rw [nhds, iInf_subtype']
  refine @Filter.iInf_isMeasurablyGenerated α _ _ _ fun i => ?_
  exact i.2.2.measurableSet.principal_isMeasurablyGenerated

/-- If `s` is a measurable set, then `𝓝[s] a` is a measurably generated filter for
each `a`. This cannot be an `instance` because it depends on a non-instance `hs : MeasurableSet s`.
-/
/-
**MeasurableSet.nhdsWithin_isMeasurablyGenerated** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：MeasurableSet.nhdsWithin_isMeasurablyGenerated {s : Set α} (hs : Measurabl
eSet s) (a : α) : (𝓝[s] a).IsMeasurablyGenerated
参数：hs : MeasurableSet s；a : α。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSet.principal_isMeasurablyGenerated`：∀ {α : Type u_1} [inst : 
MeasurableSpace α] {s : Set α}, MeasurableSet s → (Filter.principal s).IsMeasura
blyGenerated

--- 原说明 ---
If `s` is a measurable set, then `𝓝[s] a` is a measurably generated filter for
each `a`. This cannot be an `instance` because it depends on a non-instance `hs 
: MeasurableSet s`.
-/
theorem MeasurableSet.nhdsWithin_isMeasurablyGenerated {s : Set α} (hs : MeasurableSet s) (a : α) :
    (𝓝[s] a).IsMeasurablyGenerated :=
  haveI := hs.principal_isMeasurablyGenerated
  Filter.inf_isMeasurablyGenerated _ _
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) OpensMeasurableSpace.separatesPoints [T0Space α] :
    SeparatesPoints α := by
  rw [separatesPoints_iff]
  intro x y hxy
  apply Inseparable.eq
  rw [inseparable_iff_forall_isOpen]
  exact fun s hs => hxy _ hs.measurableSet
/-
**borel_eq_top_of_countable** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：borel_eq_top_of_countable {α : Type*} [TopologicalSpace α] [T0Space α] [Co
untable α] : borel α = ⊤
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `top_unique`：top_unique (h : ⊤ <= a) : a = ⊤
· 使用定理 `MeasurableSet.of_discrete`：∀ {α : Type u_1} [inst : MeasurableSpace α] [
DiscreteMeasurableSpace α] {s : Set α}, MeasurableSet s
· 使用定理 `MeasurableSingletonClass.toDiscreteMeasurableSpace`：∀ {α : Type u_1} [in
st : MeasurableSpace α] [MeasurableSingletonClass α] [Countable α], DiscreteMeas
urableSpace α
· 使用定理 `MeasurableSpace.MeasurableSingletonClass.of_separatesPoints`：∀ {α : Type
 u_1} [inst : MeasurableSpace α] [Countable α] [MeasurableSpace.SeparatesPoints 
α],   MeasurableSingletonClass α
· 使用定理 `OpensMeasurableSpace.separatesPoints`：∀ {α : Type u_1} [inst : Topologic
alSpace α] [inst_1 : MeasurableSpace α] [OpensMeasurableSpace α] [T0Space α],   
MeasurableSpace.SeparatesP…
· 使用定理 `BorelSpace.opensMeasurable`：∀ {α : Type u_6} [inst : TopologicalSpace α]
 [inst_1 : MeasurableSpace α] [BorelSpace α], OpensMeasurableSpace α
-/
theorem borel_eq_top_of_countable {α : Type*} [TopologicalSpace α] [T0Space α] [Countable α] :
    borel α = ⊤ := by
  refine top_unique fun s _ ↦ ?_
  borelize α
  exact .of_discrete

-- see Note [lower instance priority]
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) OpensMeasurableSpace.toMeasurableSingletonClass [T1Space α] :
    MeasurableSingletonClass α :=
  ⟨fun _ => isClosed_singleton.measurableSet⟩
/-
**Pi.opensMeasurableSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Pi.opensMeasurableSpace {ι : Type*} {X : ι -> Type*} [Countable ι] [t' : f
orall i, TopologicalSpace (X i)] [forall i, MeasurableSpace (X i)] [forall i, Se
condCountableTopology (X i)] [forall i, OpensMeasurableSpace (X i)] : OpensMeasu
rableSpace (forall i, X i)
参数：X i；X i；X i；X i。
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `TopologicalSpace.eq_generateFrom_countableBasis`：eq_generateFrom_countab
leBasis [SecondCountableTopology α] : ‹TopologicalSpace α› = generateFrom (count
ableBasis α)
· 使用定理 `pi_generateFrom_eq`：pi_generateFrom_eq {A : ι -> Type*} {g : forall a, S
et (Set (A a))} : (@Pi.topologicalSpace ι A fun a => generateFrom (g a)) = gener
ateFrom …
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `borel_eq_generateFrom_of_subbasis`：borel_eq_generateFrom_of_subbasis {s 
: Set (Set α)} [t : TopologicalSpace α] [SecondCountableTopology α] (hs : t = .g
enerateFrom s) : borel …
· 使用定理 `TopologicalSpace.instSecondCountableTopologyForallOfCountable`：∀ {ι : Ty
pe u_1} {X : ι → Type u_2} [Countable ι] [inst : (a : ι) → TopologicalSpace (X a
)]   [∀ (a : ι), SecondCountableTopology (X a)], Se…
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
· 使用定理 `MeasurableSet.pi`：∀ {δ : Type u_4} {X : δ → Type u_6} [inst : (a : δ) → 
MeasurableSpace (X a)] {s : Set δ} {t : (i : δ) → Set (X i)},   s.Countable → (∀
 i ∈ s…
· 使用定理 `Finset.countable_toSet`：Finset.countable_toSet (s : Finset α) : Set.Coun
table (↑s : Set α)
· 使用定理 `IsOpen.measurableSet`：IsOpen.measurableSet (h : IsOpen s) : MeasurableSe
t s
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
-/
instance Pi.opensMeasurableSpace {ι : Type*} {X : ι → Type*} [Countable ι]
    [t' : ∀ i, TopologicalSpace (X i)] [∀ i, MeasurableSpace (X i)]
    [∀ i, SecondCountableTopology (X i)] [∀ i, OpensMeasurableSpace (X i)] :
    OpensMeasurableSpace (∀ i, X i) := by
  constructor
  have : Pi.topologicalSpace = .generateFrom { t | ∃ (s : ∀ a, Set (X a)) (i : Finset ι),
      (∀ a ∈ i, s a ∈ countableBasis (X a)) ∧ t = pi (↑i) s } := by
    simp +instances only [funext fun a => @eq_generateFrom_countableBasis (X a) _ _,
      pi_generateFrom_eq]
  rw [borel_eq_generateFrom_of_subbasis this]
  apply generateFrom_le
  rintro _ ⟨s, i, hi, rfl⟩
  refine MeasurableSet.pi i.countable_toSet fun a ha => IsOpen.measurableSet ?_
  rw [eq_generateFrom_countableBasis (X a)]
  exact .basic _ (hi a ha)

/-- This is not covered by `Pi.opensMeasurableSpace` as it does not require second countability. -/
/-
**Pi.opensMeasurableSpace_of_subsingleton** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Pi.opensMeasurableSpace_of_subsingleton {ι : Type*} {X : ι -> Type*} [Subs
ingleton ι] [forall i, TopologicalSpace (X i)] [forall i, MeasurableSpace (X i)]
 [forall i, OpensMeasurableSpace (X i)] : OpensMeasurableSpace (forall i, X i) w
here borel_le
参数：X i；X i；X i。
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `isEmpty_or_nonempty`：isEmpty_or_nonempty : IsEmpty α ∨ Nonempty α
· 使用定理 `Subsingleton.set_cases`：set_cases {p : Set α -> Prop} (h0 : p ∅) (h1 : p
 univ) (s) : p s
· 使用定理 `Unique.instSubsingleton`：∀ {α : Sort u_1} [Unique α], Subsingleton α
· 使用定理 `MeasurableSet.empty`：MeasurableSet.empty [MeasurableSpace α] : Measurabl
eSet (∅ : Set α)
· 使用定理 `MeasurableSet.univ`：∀ {α : Type u_1} {m : MeasurableSpace α}, Measurable
Set Set.univ
· 使用定理 `nonempty_unique`：nonempty_unique (α : Sort u) [Subsingleton α] [Nonempty
 α] : Nonempty (Unique α)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `borel.eq_1`：∀ (α : Type u) [inst : TopologicalSpace α], borel α = Measur
ableSpace.generateFrom {s | IsOpen s}
· 使用定理 `MeasurableSpace.pi.eq_1`：∀ {δ : Type u_4} {X : δ → Type u_6} [m : (a : δ
) → MeasurableSpace (X a)],   MeasurableSpace.pi = ⨆ a, MeasurableSpace.comap (f
un b => b a) …
· 使用定理 `ciSup_unique`：ciSup_unique [Unique ι] {s : ι -> α} : ⨆ i, s i = s defaul
t
· 使用定理 `MeasurableSpace.generateFrom_le`：generateFrom_le {s : Set (Set α)} {m : 
MeasurableSpace α} (h : forall t in s, MeasurableSet[m] t) : generateFrom s <= m
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用引理 `MeasurableSpace.measurableSet_comap`：measurableSet_comap {m : Measurable
Space β} : MeasurableSet[m.comap f] s ↔ exists s', MeasurableSet[m] s' ∧ f ⁻¹' s
' = s
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `ciInf_unique`：∀ {α : Type u_1} {ι : Sort u_4} [inst : ConditionallyCompl
etePartialOrderInf α] [inst_1 : Unique ι] {s : ι → α},   ⨅ i, s i = s default
· 使用定理 `IsOpen.measurableSet`：IsOpen.measurableSet (h : IsOpen s) : MeasurableSe
t s

--- 原说明 ---
This is not covered by `Pi.opensMeasurableSpace` as it does not require second c
ountability.
-/
instance Pi.opensMeasurableSpace_of_subsingleton {ι : Type*} {X : ι → Type*} [Subsingleton ι]
    [∀ i, TopologicalSpace (X i)] [∀ i, MeasurableSpace (X i)] [∀ i, OpensMeasurableSpace (X i)] :
    OpensMeasurableSpace (∀ i, X i) where
  borel_le := by
    obtain h | h := isEmpty_or_nonempty ι
    · exact fun s _ ↦ Subsingleton.set_cases .empty .univ s
    have := Classical.choice (nonempty_unique ι)
    rw [borel, MeasurableSpace.pi, ciSup_unique]
    refine MeasurableSpace.generateFrom_le fun s hs ↦ MeasurableSpace.measurableSet_comap.2 ?_
    simp +instances only [Pi.topologicalSpace, ciInf_unique, isOpen_induced_eq, Set.mem_image,
      Set.mem_ofPred_eq] at hs
    obtain ⟨t, ht, rfl⟩ := hs
    exact ⟨t, ht.measurableSet, rfl⟩

/-- The typeclass `SecondCountableTopologyEither α β` registers the fact that at least one of
the two spaces has second countable topology. This is the right assumption to ensure that continuous
maps from `α` to `β` are strongly measurable. -/
/-
**SecondCountableTopologyEither** 是 Mathlib 中的一个归纳类型，位于命名空间 ``。
形式化陈述：(α : Type u_6) → (β : Type u_7) → [TopologicalSpace α] → [TopologicalSpace
 β] → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The typeclass `SecondCountableTopologyEither α β` registers the fact that at lea
st one of
the two spaces has second countable topology. This is the right assumption to en
sure that continuous
maps from `α` to `β` are strongly measurable.
-/
class SecondCountableTopologyEither (α β : Type*) [TopologicalSpace α] [TopologicalSpace β] :
  Prop where
  /-- The projection out of `SecondCountableTopologyEither` -/
  out : SecondCountableTopology α ∨ SecondCountableTopology β
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) secondCountableTopologyEither_of_left (α β : Type*) [TopologicalSpace α]
    [TopologicalSpace β] [SecondCountableTopology α] : SecondCountableTopologyEither α β where
  out := Or.inl (by infer_instance)
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) secondCountableTopologyEither_of_right (α β : Type*)
    [TopologicalSpace α] [TopologicalSpace β] [SecondCountableTopology β] :
    SecondCountableTopologyEither α β where
  out := Or.inr (by infer_instance)

/-- If either `α` or `β` has second-countable topology, then the open sets in `α × β` belong to the
product sigma-algebra. -/
/-
**Prod.opensMeasurableSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Prod.opensMeasurableSpace [h : SecondCountableTopologyEither α β] : OpensM
easurableSpace (α × β)
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用引理 `opensMeasurableSpace_iff_forall_measurableSet`：opensMeasurableSpace_iff_
forall_measurableSet [TopologicalSpace α] [MeasurableSpace α] : OpensMeasurableS
pace α ↔ (forall (s : Set α), IsOpe…
· 使用定理 `SecondCountableTopologyEither.out`：∀ {α : Type u_6} {β : Type u_7} {inst
 : TopologicalSpace α} {inst_1 : TopologicalSpace β}   [self : SecondCountableTo
pologyEither α β], Seco…
· 使用定理 `isOpen_iff_forall_mem_open`：isOpen_iff_forall_mem_open : IsOpen s ↔ fora
ll x in s, exists t, t subseteq s ∧ IsOpen t ∧ x in t
· 使用定理 `Set.Subset.antisymm`：∀ {α : Type u} {a b : Set α}, a ⊆ b → b ⊆ a → a = b
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `isOpen_prod_iff`：isOpen_prod_iff {s : Set (X × Y)} : IsOpen s ↔ forall a
 b, (a, b) in s -> exists u v, IsOpen u ∧ IsOpen v ∧ a in u ∧ b in v ∧ u ×ˢ v su
bsete…
· 使用定理 `TopologicalSpace.IsTopologicalBasis.exists_subset_of_mem_open`：∀ {α : Ty
pe u} [t : TopologicalSpace α] {b : Set (Set α)},   TopologicalSpace.IsTopologic
alBasis b → ∀ {a : α} {u : Set α}, a ∈ u → IsOpen u…
· 使用定理 `TopologicalSpace.isBasis_countableBasis`：isBasis_countableBasis [SecondC
ountableTopology α] : IsTopologicalBasis (countableBasis α)
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `exists_prop_congr`：∀ {p p' : Prop} {q q' : p → Prop}, (∀ (h : p), q h ↔ 
q' h) → ∀ (hp : p ↔ p'), Exists q ↔ ∃ (h : p'), q' ⋯
· 使用定理 `Iff.of_eq`：∀ {a b : Prop}, a = b → (a ↔ b)
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用定理 `Set.prod_mono_left`：prod_mono_left (hs : s₁ subseteq s₂) : s₁ ×ˢ t subse
teq s₂ ×ˢ t
· 使用定理 `Set.mem_prod`：mem_prod : p in s ×ˢ t ↔ p.1 in s ∧ p.2 in t
· 使用定理 `MeasurableSet.biUnion`：∀ {α : Type u_1} {β : Type u_2} {m : MeasurableSp
ace α} {f : β → Set α} {s : Set β},   s.Countable → (∀ b ∈ s, MeasurableSet (f b
)) → Measur…
· 使用定理 `TopologicalSpace.countable_countableBasis`：countable_countableBasis [Sec
ondCountableTopology α] : (countableBasis α).Countable
· 使用定理 `MeasurableSet.prod`：∀ {α : Type u_1} {β : Type u_2} {m : MeasurableSpace
 α} {mβ : MeasurableSpace β} {s : Set α} {t : Set β},   MeasurableSet s → Measur
ableSet …
· 使用定理 `IsOpen.measurableSet`：IsOpen.measurableSet (h : IsOpen s) : MeasurableSe
t s
· 使用定理 `TopologicalSpace.isOpen_of_mem_countableBasis`：isOpen_of_mem_countableBa
sis [SecondCountableTopology α] {s : Set α} (hs : s in countableBasis α) : IsOpe
n s
· 使用定理 `Set.prod_mono_right`：prod_mono_right (ht : t₁ subseteq t₂) : s ×ˢ t₁ sub
seteq s ×ˢ t₂

--- 原说明 ---
If either `α` or `β` has second-countable topology, then the open sets in `α × β
` belong to the
product sigma-algebra.
-/
instance Prod.opensMeasurableSpace [h : SecondCountableTopologyEither α β] :
    OpensMeasurableSpace (α × β) := by
  apply opensMeasurableSpace_iff_forall_measurableSet.2 (fun s hs ↦ ?_)
  rcases h.out with hα | hβ
  · let F : Set α → Set β := fun a ↦ {y | ∃ b, IsOpen b ∧ y ∈ b ∧ a ×ˢ b ⊆ s}
    have A : ∀ a, IsOpen (F a) := by
      intro a
      apply isOpen_iff_forall_mem_open.2
      rintro y ⟨b, b_open, yb, hb⟩
      exact ⟨b, fun z zb ↦ ⟨b, b_open, zb, hb⟩, b_open, yb⟩
    have : s = ⋃ a ∈ countableBasis α, a ×ˢ F a := by
      apply Subset.antisymm
      · rintro ⟨y1, y2⟩ hy
        rcases isOpen_prod_iff.1 hs y1 y2 hy with ⟨u, v, u_open, v_open, yu, yv, huv⟩
        obtain ⟨a, ha, ya, au⟩ : ∃ a ∈ countableBasis α, y1 ∈ a ∧ a ⊆ u :=
          IsTopologicalBasis.exists_subset_of_mem_open (isBasis_countableBasis α) yu u_open
        simp only [mem_iUnion, mem_prod, exists_and_left, exists_prop]
        exact ⟨a, ya, ha, v, v_open, yv, (Set.prod_mono_left au).trans huv⟩
      · rintro ⟨y1, y2⟩ hy
        simp only [mem_iUnion, mem_prod, exists_and_left, exists_prop] at hy
        rcases hy with ⟨a, ya, -, b, -, yb, hb⟩
        exact hb (mem_prod.2 ⟨ya, yb⟩)
    rw [this]
    apply MeasurableSet.biUnion (countable_countableBasis α) (fun a ha ↦ ?_)
    exact (isOpen_of_mem_countableBasis ha).measurableSet.prod (A a).measurableSet
  · let F : Set β → Set α := fun a ↦ {y | ∃ b, IsOpen b ∧ y ∈ b ∧ b ×ˢ a ⊆ s}
    have A : ∀ a, IsOpen (F a) := by
      intro a
      apply isOpen_iff_forall_mem_open.2
      rintro y ⟨b, b_open, yb, hb⟩
      exact ⟨b, fun z zb ↦ ⟨b, b_open, zb, hb⟩, b_open, yb⟩
    have : s = ⋃ a ∈ countableBasis β, F a ×ˢ a := by
      apply Subset.antisymm
      · rintro ⟨y1, y2⟩ hy
        rcases isOpen_prod_iff.1 hs y1 y2 hy with ⟨u, v, u_open, v_open, yu, yv, huv⟩
        obtain ⟨a, ha, ya, au⟩ : ∃ a ∈ countableBasis β, y2 ∈ a ∧ a ⊆ v :=
          IsTopologicalBasis.exists_subset_of_mem_open (isBasis_countableBasis β) yv v_open
        simp only [mem_iUnion, mem_prod, exists_and_left, exists_prop]
        exact ⟨a, ⟨u, u_open, yu, (Set.prod_mono_right au).trans huv⟩, ha, ya⟩
      · rintro ⟨y1, y2⟩ hy
        simp only [mem_iUnion, mem_prod, exists_and_left, exists_prop] at hy
        rcases hy with ⟨a, ⟨b, -, yb, hb⟩, -, ya⟩
        exact hb (mem_prod.2 ⟨yb, ya⟩)
    rw [this]
    apply MeasurableSet.biUnion (countable_countableBasis β) (fun a ha ↦ ?_)
    exact (A a).measurableSet.prod (isOpen_of_mem_countableBasis ha).measurableSet

variable {α' : Type*} [TopologicalSpace α'] [MeasurableSpace α']
/-
**interior_ae_eq_of_null_frontier** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：interior_ae_eq_of_null_frontier {μ : Measure α'} {s : Set α'} (h : μ (fron
tier s) = 0) : interior s =ᵐ[μ] s
参数：h : μ (frontier s) = 0。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Filter.EventuallyLE.antisymm`：∀ {α : Type u} {β : Type v} [inst : Partia
lOrder β] {l : Filter α} {f g : α → β}, f ≤ᶠ[l] g → g ≤ᶠ[l] f → f =ᶠ[l] g
· 使用定理 `MeasureTheory.Measure.instOuterMeasureClass`：∀ {α : Type u_1} [inst : Me
asurableSpace α], MeasureTheory.OuterMeasureClass (MeasureTheory.Measure α) α
· 使用定理 `LE.le.eventuallyLE`：LE.le.eventuallyLE {α} {l : Filter α} {s t : Set α} 
(h : s subseteq t) : s <=ᶠ[l] t
· 使用定理 `interior_subset`：interior_subset : interior s subseteq s
· 使用定理 `Filter.EventuallyLE.trans`：∀ {α : Type u} {β : Type v} [inst : Preorder 
β] {l : Filter α} {f g h : α → β}, f ≤ᶠ[l] g → g ≤ᶠ[l] h → f ≤ᶠ[l] h
· 使用定理 `subset_closure`：subset_closure : s subseteq closure s
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `MeasureTheory.ae_le_set`：ae_le_set : s <=ᵐ[μ] t ↔ μ (s \ t) = 0
-/
theorem interior_ae_eq_of_null_frontier {μ : Measure α'} {s : Set α'} (h : μ (frontier s) = 0) :
    interior s =ᵐ[μ] s :=
  interior_subset.eventuallyLE.antisymm <| subset_closure.eventuallyLE.trans (ae_le_set.2 h)
/-
**measure_interior_of_null_frontier** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：measure_interior_of_null_frontier {μ : Measure α'} {s : Set α'} (h : μ (fr
ontier s) = 0) : μ (interior s) = μ s
参数：h : μ (frontier s) = 0。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasureTheory.measure_congr`：measure_congr (H : s =ᵐ[μ] t) : μ s = μ t
· 使用定理 `MeasureTheory.Measure.instOuterMeasureClass`：∀ {α : Type u_1} [inst : Me
asurableSpace α], MeasureTheory.OuterMeasureClass (MeasureTheory.Measure α) α
· 使用定理 `interior_ae_eq_of_null_frontier`：interior_ae_eq_of_null_frontier {μ : Me
asure α'} {s : Set α'} (h : μ (frontier s) = 0) : interior s =ᵐ[μ] s
-/
theorem measure_interior_of_null_frontier {μ : Measure α'} {s : Set α'} (h : μ (frontier s) = 0) :
    μ (interior s) = μ s :=
  measure_congr (interior_ae_eq_of_null_frontier h)
/-
**nullMeasurableSet_of_null_frontier** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：nullMeasurableSet_of_null_frontier {s : Set α} {μ : Measure α} (h : μ (fro
ntier s) = 0) : NullMeasurableSet s μ
参数：h : μ (frontier s) = 0。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasureTheory.Measure.instOuterMeasureClass`：∀ {α : Type u_1} [inst : Me
asurableSpace α], MeasureTheory.OuterMeasureClass (MeasureTheory.Measure α) α
· 使用定理 `IsOpen.measurableSet`：IsOpen.measurableSet (h : IsOpen s) : MeasurableSe
t s
· 使用定理 `isOpen_interior`：isOpen_interior : IsOpen (interior s)
· 使用定理 `Filter.EventuallyEq.symm`：∀ {α : Type u} {β : Type v} {f g : α → β} {l :
 Filter α}, f =ᶠ[l] g → g =ᶠ[l] f
· 使用定理 `interior_ae_eq_of_null_frontier`：interior_ae_eq_of_null_frontier {μ : Me
asure α'} {s : Set α'} (h : μ (frontier s) = 0) : interior s =ᵐ[μ] s
-/
theorem nullMeasurableSet_of_null_frontier {s : Set α} {μ : Measure α} (h : μ (frontier s) = 0) :
    NullMeasurableSet s μ :=
  ⟨interior s, isOpen_interior.measurableSet, (interior_ae_eq_of_null_frontier h).symm⟩
/-
**closure_ae_eq_of_null_frontier** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：closure_ae_eq_of_null_frontier {μ : Measure α'} {s : Set α'} (h : μ (front
ier s) = 0) : closure s =ᵐ[μ] s
参数：h : μ (frontier s) = 0。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Filter.EventuallyLE.antisymm`：∀ {α : Type u} {β : Type v} [inst : Partia
lOrder β] {l : Filter α} {f g : α → β}, f ≤ᶠ[l] g → g ≤ᶠ[l] f → f =ᶠ[l] g
· 使用定理 `MeasureTheory.Measure.instOuterMeasureClass`：∀ {α : Type u_1} [inst : Me
asurableSpace α], MeasureTheory.OuterMeasureClass (MeasureTheory.Measure α) α
· 使用定理 `Filter.EventuallyLE.trans`：∀ {α : Type u} {β : Type v} [inst : Preorder 
β] {l : Filter α} {f g h : α → β}, f ≤ᶠ[l] g → g ≤ᶠ[l] h → f ≤ᶠ[l] h
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `MeasureTheory.ae_le_set`：ae_le_set : s <=ᵐ[μ] t ↔ μ (s \ t) = 0
· 使用定理 `LE.le.eventuallyLE`：LE.le.eventuallyLE {α} {l : Filter α} {s t : Set α} 
(h : s subseteq t) : s <=ᶠ[l] t
· 使用定理 `interior_subset`：interior_subset : interior s subseteq s
· 使用定理 `subset_closure`：subset_closure : s subseteq closure s
-/
theorem closure_ae_eq_of_null_frontier {μ : Measure α'} {s : Set α'} (h : μ (frontier s) = 0) :
    closure s =ᵐ[μ] s :=
  ((ae_le_set.2 h).trans interior_subset.eventuallyLE).antisymm <| subset_closure.eventuallyLE
/-
**measure_closure_of_null_frontier** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：measure_closure_of_null_frontier {μ : Measure α'} {s : Set α'} (h : μ (fro
ntier s) = 0) : μ (closure s) = μ s
参数：h : μ (frontier s) = 0。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasureTheory.measure_congr`：measure_congr (H : s =ᵐ[μ] t) : μ s = μ t
· 使用定理 `MeasureTheory.Measure.instOuterMeasureClass`：∀ {α : Type u_1} [inst : Me
asurableSpace α], MeasureTheory.OuterMeasureClass (MeasureTheory.Measure α) α
· 使用定理 `closure_ae_eq_of_null_frontier`：closure_ae_eq_of_null_frontier {μ : Meas
ure α'} {s : Set α'} (h : μ (frontier s) = 0) : closure s =ᵐ[μ] s
-/
theorem measure_closure_of_null_frontier {μ : Measure α'} {s : Set α'} (h : μ (frontier s) = 0) :
    μ (closure s) = μ s :=
  measure_congr (closure_ae_eq_of_null_frontier h)
/-
**null_frontier_inter** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：null_frontier_inter {μ : Measure α'} {s s' : Set α'} (h : μ (frontier s) =
 0) (h' : μ (frontier s') = 0) : μ (frontier (s inter s')) = 0
参数：h : μ (frontier s) = 0；h' : μ (frontier s') = 0。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `bot_unique`：∀ {α : Type u} [inst : PartialOrder α] [inst_1 : OrderBot α]
 {a : α}, a ≤ ⊥ → a = ⊥
· 使用定理 `MeasureTheory.measure_mono`：measure_mono (h : s subseteq t) : μ s <= μ t
· 使用定理 `MeasureTheory.Measure.instOuterMeasureClass`：∀ {α : Type u_1} [inst : Me
asurableSpace α], MeasureTheory.OuterMeasureClass (MeasureTheory.Measure α) α
· 使用定理 `LE.le.trans`：∀ {α : Type u_1} [inst : Preorder α] {a b c : α}, a ≤ b → b
 ≤ c → a ≤ c
· 使用定理 `frontier_inter_subset`：frontier_inter_subset (s t : Set X) : frontier (s
 inter t) subseteq frontier s inter closure t union closure s inter frontier t
· 使用定理 `MeasureTheory.measure_union_le`：measure_union_le (s t : Set α) : μ (s un
ion t) <= μ s + μ t
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `add_zero`：∀ {M : Type u} [inst : AddZeroClass M] (a : M), a + 0 = a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
-/
theorem null_frontier_inter {μ : Measure α'} {s s' : Set α'}
    (h : μ (frontier s) = 0) (h' : μ (frontier s') = 0) :
    μ (frontier (s ∩ s')) = 0 := by
  apply bot_unique
  calc μ (frontier (s ∩ s'))
  _ ≤ μ (frontier s ∪ frontier s') := measure_mono <| (frontier_inter_subset _ _).trans (by grind)
  _ ≤ μ (frontier s) + μ (frontier s') := measure_union_le _ _
  _ = 0 := by simp [h, h']
/-
**separatesPointsOfOpensMeasurableSpaceOfT0Space** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：separatesPointsOfOpensMeasurableSpaceOfT0Space [T0Space α] : MeasurableSpa
ce.SeparatesPoints α where separates x y
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用引理 `Mathlib.Tactic.Contrapose.contrapose₁`：contrapose₁ {p q : Prop} : (¬ q -
> ¬ p) -> (p -> q)
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Mathlib.Tactic.Push.not_forall_eq`：not_forall_eq : (¬ forall x, s x) = (
exists x, ¬ s x)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `MeasurableSpace.exists_measurableSet_of_ne`：exists_measurableSet_of_ne [
MeasurableSpace α] [SeparatesPoints α] {x y : α} (h : x != y) : exists s, Measur
ableSet s ∧ x in s ∧ y ∉ s
· 使用定理 `OpensMeasurableSpace.separatesPoints`：∀ {α : Type u_1} [inst : Topologic
alSpace α] [inst_1 : MeasurableSpace α] [OpensMeasurableSpace α] [T0Space α],   
MeasurableSpace.SeparatesP…
-/
instance separatesPointsOfOpensMeasurableSpaceOfT0Space [T0Space α] :
    MeasurableSpace.SeparatesPoints α where
  separates x y := by
    contrapose!
    exact fun a => exists_measurableSet_of_ne a

/-- A continuous function from an `OpensMeasurableSpace` to a `BorelSpace`
is measurable. -/
@[fun_prop]
/-
**Continuous.measurable** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：Continuous.measurable {f : α -> γ} (hf : Continuous f) : Measurable f
参数：hf : Continuous f。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Measurable.mono`：Measurable.mono {ma ma' : MeasurableSpace α} {mb mb' : 
MeasurableSpace β} {f : α -> β} (hf : @Measurable α β ma mb f) (ha : ma <= ma') 
(hb :…
· 使用定理 `Continuous.borel_measurable`：Continuous.borel_measurable [TopologicalSpa
ce α] [TopologicalSpace β] {f : α -> β} (hf : Continuous f) : @Measurable α β (b
orel α) (borel β)…
· 使用定理 `OpensMeasurableSpace.borel_le`：∀ {α : Type u_6} {inst : TopologicalSpace
 α} {h : MeasurableSpace α} [self : OpensMeasurableSpace α], borel α ≤ h
· 使用定理 `le_of_eq`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a = b → a ≤ b
· 使用定理 `BorelSpace.measurable_eq`：∀ {α : Type u_6} {inst : TopologicalSpace α} {
inst_1 : MeasurableSpace α} [self : BorelSpace α], inst_1 = borel α

--- 原说明 ---
A continuous function from an `OpensMeasurableSpace` to a `BorelSpace`
is measurable.
-/
theorem Continuous.measurable {f : α → γ} (hf : Continuous f) : Measurable f :=
  hf.borel_measurable.mono OpensMeasurableSpace.borel_le (le_of_eq <| BorelSpace.measurable_eq)

/-- A continuous function from an `OpensMeasurableSpace` to a `BorelSpace`
is ae-measurable. -/
@[fun_prop]
/-
**Continuous.aemeasurable** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：Continuous.aemeasurable {f : α -> γ} (h : Continuous f) {μ : Measure α} : 
AEMeasurable f μ
参数：h : Continuous f。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Measurable.aemeasurable`：Measurable.aemeasurable (h : Measurable f) : AE
Measurable f μ
· 使用定理 `Continuous.measurable`：Continuous.measurable {f : α -> γ} (hf : Continuo
us f) : Measurable f

--- 原说明 ---
A continuous function from an `OpensMeasurableSpace` to a `BorelSpace`
is ae-measurable.
-/
theorem Continuous.aemeasurable {f : α → γ} (h : Continuous f) {μ : Measure α} : AEMeasurable f μ :=
  h.measurable.aemeasurable

/-- If a function is continuous outside of a countable set, it is measurable. -/
/-
**ContinuousOn.measurable_of_countable_compl** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ContinuousOn.measurable_of_countable_compl [MeasurableSingletonClass α] {f
 : α -> γ} {s : Set α} (hf : ContinuousOn f s) (hs : (sᶜ).Countable) : Measurabl
e f
参数：hf : ContinuousOn f s；hs : (sᶜ).Countable。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `measurable_of_measurable_on_compl_countable`：measurable_of_measurable_on
_compl_countable [MeasurableSingletonClass α] {f : α -> β} (s : Set α) (hs : s.C
ountable) (hf : Measurable (sᶜ.do…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `compl_compl`：compl_compl (x : α) : xᶜᶜ = x
· 使用定理 `Continuous.measurable`：Continuous.measurable {f : α -> γ} (hf : Continuo
us f) : Measurable f
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `continuousOn_iff_continuous_domRestrict`：continuousOn_iff_continuous_dom
Restrict : ContinuousOn f s ↔ Continuous (s.domRestrict f)

--- 原说明 ---
If a function is continuous outside of a countable set, it is measurable.
-/
theorem ContinuousOn.measurable_of_countable_compl [MeasurableSingletonClass α]
    {f : α → γ} {s : Set α} (hf : ContinuousOn f s) (hs : (sᶜ).Countable) : Measurable f := by
  apply measurable_of_measurable_on_compl_countable _ hs
  rw [compl_compl]
  exact (continuousOn_iff_continuous_domRestrict.1 hf).measurable

/-- If a function is continuous outside of a countable set, then it is measurable. -/
/-
**measurable_of_countable_not_continuousAt** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：measurable_of_countable_not_continuousAt [MeasurableSingletonClass α] {f :
 α -> γ} (hf : Set.Countable {x | ¬ ContinuousAt f x}) : Measurable f
参数：hf : Set.Countable {x | ¬ ContinuousAt f x}。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `ContinuousAt.continuousWithinAt`：ContinuousAt.continuousWithinAt (h : Co
ntinuousAt f x) : ContinuousWithinAt f s x
· 使用定理 `ContinuousOn.measurable_of_countable_compl`：ContinuousOn.measurable_of_c
ountable_compl [MeasurableSingletonClass α] {f : α -> γ} {s : Set α} (hf : Conti
nuousOn f s) (hs : (sᶜ).Countabl…
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a

--- 原说明 ---
If a function is continuous outside of a countable set, then it is measurable.
-/
theorem measurable_of_countable_not_continuousAt [MeasurableSingletonClass α]
    {f : α → γ} (hf : Set.Countable {x | ¬ ContinuousAt f x}) : Measurable f := by
  have : ContinuousOn f {x | ContinuousAt f x} := fun x hx ↦ hx.continuousWithinAt
  apply this.measurable_of_countable_compl
  convert hf
  grind
/-
**Topology.IsClosedEmbedding.measurable** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：Topology.IsClosedEmbedding.measurable {f : α -> γ} (hf : IsClosedEmbedding
 f) : Measurable f
参数：hf : IsClosedEmbedding f。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Continuous.measurable`：Continuous.measurable {f : α -> γ} (hf : Continuo
us f) : Measurable f
· 使用定理 `Topology.IsClosedEmbedding.continuous`：∀ {X : Type u_1} {Y : Type u_2} {
f : X → Y} [inst : TopologicalSpace X] [inst_1 : TopologicalSpace Y],   Topology
.IsClosedEmbedding f → Cont…
-/
theorem Topology.IsClosedEmbedding.measurable {f : α → γ} (hf : IsClosedEmbedding f) :
    Measurable f :=
  hf.continuous.measurable

/-- If a function is defined piecewise in terms of functions which are continuous on their
respective pieces, then it is measurable. -/
/-
**ContinuousOn.measurable_piecewise** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ContinuousOn.measurable_piecewise {f g : α -> γ} {s : Set α} [forall j : α
, Decidable (j in s)] (hf : ContinuousOn f s) (hg : ContinuousOn g sᶜ) (hs : Mea
surableSet s) : Measurable (s.piecewise f g)
参数：j in s；hf : ContinuousOn f s；hg : ContinuousOn g sᶜ；hs : MeasurableSet s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `measurable_of_isOpen`：measurable_of_isOpen {f : δ -> γ} (hf : forall s, 
IsOpen s -> MeasurableSet (f ⁻¹' s)) : Measurable f
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Set.piecewise_preimage`：piecewise_preimage (f g : α -> β) (t) : s.piecew
ise f g ⁻¹' t = s.ite (f ⁻¹' t) (g ⁻¹' t)
· 使用定理 `Set.ite.eq_1`：∀ {α : Type u_1} (t s s' : Set α), t.ite s s' = s ∩ t ∪ s'
 \ t
· 使用定理 `MeasurableSet.union`：∀ {α : Type u_1} {m : MeasurableSpace α} {s₁ s₂ : S
et α}, MeasurableSet s₁ → MeasurableSet s₂ → MeasurableSet (s₁ ∪ s₂)
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `continuousOn_iff'`：continuousOn_iff' : ContinuousOn f s ↔ forall t : Set
 β, IsOpen t -> exists u, IsOpen u ∧ f ⁻¹' t inter s = u inter s
· 使用定理 `MeasurableSet.inter`：∀ {α : Type u_1} {m : MeasurableSpace α} {s₁ s₂ : S
et α}, MeasurableSet s₁ → MeasurableSet s₂ → MeasurableSet (s₁ ∩ s₂)
· 使用定理 `IsOpen.measurableSet`：IsOpen.measurableSet (h : IsOpen s) : MeasurableSe
t s
· 使用定理 `Set.sdiff_eq_compl_inter`：sdiff_eq_compl_inter {s t : Set α} : s \ t = t
ᶜ inter s
· 使用定理 `Set.inter_comm`：inter_comm (a b : Set α) : a inter b = b inter a
· 使用定理 `MeasurableSet.compl`：∀ {α : Type u_1} {s : Set α} {m : MeasurableSpace α
}, MeasurableSet s → MeasurableSet sᶜ

--- 原说明 ---
If a function is defined piecewise in terms of functions which are continuous on
 their
respective pieces, then it is measurable.
-/
theorem ContinuousOn.measurable_piecewise {f g : α → γ} {s : Set α} [∀ j : α, Decidable (j ∈ s)]
    (hf : ContinuousOn f s) (hg : ContinuousOn g sᶜ) (hs : MeasurableSet s) :
    Measurable (s.piecewise f g) := by
  refine measurable_of_isOpen fun t ht => ?_
  rw [piecewise_preimage, Set.ite]
  apply MeasurableSet.union
  · rcases _root_.continuousOn_iff'.1 hf t ht with ⟨u, u_open, hu⟩
    rw [hu]
    exact u_open.measurableSet.inter hs
  · rcases _root_.continuousOn_iff'.1 hg t ht with ⟨u, u_open, hu⟩
    rw [sdiff_eq_compl_inter, inter_comm, hu]
    exact u_open.measurableSet.inter hs.compl

@[to_additive]
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) ContinuousMul.measurableMul [Mul γ] [SeparatelyContinuousMul γ] :
    MeasurableMul γ where
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) ContinuousSub.measurableSub [Sub γ] [ContinuousSub γ] :
    MeasurableSub γ where

@[to_additive]
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) ContinuousInv.measurableInv [Inv γ] [ContinuousInv γ] :
    MeasurableInv γ := ⟨continuous_inv.measurable⟩

@[to_additive]
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) ContinuousConstSMul.toMeasurableConstSMul {M α} [TopologicalSpace α]
    [MeasurableSpace α] [BorelSpace α] [SMul M α] [ContinuousConstSMul M α] :
    MeasurableConstSMul M α where

@[to_additive]
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) ContinuousSMul.toMeasurableSMul {M α} [TopologicalSpace M]
    [TopologicalSpace α] [MeasurableSpace M] [MeasurableSpace α] [OpensMeasurableSpace M]
    [BorelSpace α] [SMul M α] [ContinuousSMul M α] : MeasurableSMul M α where

section Homeomorph

@[fun_prop]
/-
**Homeomorph.measurable** 是 Mathlib 中的一个定理，位于命名空间 `Homeomorph`。
形式化陈述：∀ {α : Type u_1} {γ : Type u_3} [inst : TopologicalSpace α] [inst_1 : Meas
urableSpace α] [OpensMeasurableSpace α]   [inst_3 : TopologicalSpace γ] [inst_4 
: MeasurableSpace γ] [BorelSpace γ] (h : α ≃ₜ γ), Measurable ⇑h
参数：h : α ≃ₜ γ。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Continuous.measurable`：Continuous.measurable {f : α -> γ} (hf : Continuo
us f) : Measurable f
· 使用定理 `Homeomorph.continuous`：∀ {X : Type u_1} {Y : Type u_2} [inst : Topologic
alSpace X] [inst_1 : TopologicalSpace Y] (h : X ≃ₜ Y), Continuous ⇑h
-/
protected theorem Homeomorph.measurable (h : α ≃ₜ γ) : Measurable h :=
  h.continuous.measurable

/-- A homeomorphism between two Borel spaces is a measurable equivalence. -/
/-
**Homeomorph.toMeasurableEquiv** 是 Mathlib 中的一个定义，位于命名空间 ``。
形式化陈述：Homeomorph.toMeasurableEquiv (h : γ ≃ₜ γ₂) : γ ≃ᵐ γ₂ where toEquiv
参数：h : γ ≃ₜ γ₂。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A homeomorphism between two Borel spaces is a measurable equivalence.
-/
def Homeomorph.toMeasurableEquiv (h : γ ≃ₜ γ₂) : γ ≃ᵐ γ₂ where
  toEquiv := h.toEquiv
/-
**Homeomorph.measurableEmbedding** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：Homeomorph.measurableEmbedding (h : γ ≃ₜ γ₂) : MeasurableEmbedding h
参数：h : γ ≃ₜ γ₂。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableEquiv.measurableEmbedding`：∀ {α : Type u_1} {β : Type u_2} [in
st : MeasurableSpace α] [inst_1 : MeasurableSpace β] (e : α ≃ᵐ β),   MeasurableE
mbedding ⇑e
-/
lemma Homeomorph.measurableEmbedding (h : γ ≃ₜ γ₂) : MeasurableEmbedding h :=
  h.toMeasurableEquiv.measurableEmbedding

@[simp]
/-
**Homeomorph.toMeasurableEquiv_coe** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：Homeomorph.toMeasurableEquiv_coe (h : γ ≃ₜ γ₂) : (h.toMeasurableEquiv : γ 
-> γ₂) = h
参数：h : γ ≃ₜ γ₂。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem Homeomorph.toMeasurableEquiv_coe (h : γ ≃ₜ γ₂) : (h.toMeasurableEquiv : γ → γ₂) = h :=
  rfl

@[simp]
/-
**Homeomorph.toMeasurableEquiv_symm_coe** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：Homeomorph.toMeasurableEquiv_symm_coe (h : γ ≃ₜ γ₂) : (h.toMeasurableEquiv
.symm : γ₂ -> γ) = h.symm
参数：h : γ ≃ₜ γ₂。
该定理/引理给出了一组等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
theorem Homeomorph.toMeasurableEquiv_symm_coe (h : γ ≃ₜ γ₂) :
    (h.toMeasurableEquiv.symm : γ₂ → γ) = h.symm :=
  rfl

end Homeomorph

@[fun_prop]
/-
**ContinuousMap.measurable** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：ContinuousMap.measurable (f : C(α, γ)) : Measurable f
参数：f : C(α, γ)。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Continuous.measurable`：Continuous.measurable {f : α -> γ} (hf : Continuo
us f) : Measurable f
· 使用定理 `ContinuousMap.continuous`：∀ {X : Type u_1} {Y : Type u_2} [inst : Topolo
gicalSpace X] [inst_1 : TopologicalSpace Y] (f : C(X, Y)), Continuous ⇑f
-/
theorem ContinuousMap.measurable (f : C(α, γ)) : Measurable f :=
  f.continuous.measurable

@[fun_prop]
/-
**measurable_of_continuousOn_compl_singleton** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：measurable_of_continuousOn_compl_singleton [T1Space α] {f : α -> γ} (a : α
) (hf : ContinuousOn f {a}ᶜ) : Measurable f
参数：a : α；hf : ContinuousOn f {a}ᶜ。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `measurable_of_measurable_on_compl_singleton`：measurable_of_measurable_on
_compl_singleton [MeasurableSingletonClass α] {f : α -> β} (a : α) (hf : Measura
ble ({ x | x != a }.domRestrict f…
· 使用定理 `OpensMeasurableSpace.toMeasurableSingletonClass`：∀ {α : Type u_1} [inst 
: TopologicalSpace α] [inst_1 : MeasurableSpace α] [OpensMeasurableSpace α] [T1S
pace α],   MeasurableSingletonClass α
· 使用定理 `Continuous.measurable`：Continuous.measurable {f : α -> γ} (hf : Continuo
us f) : Measurable f
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `continuousOn_iff_continuous_domRestrict`：continuousOn_iff_continuous_dom
Restrict : ContinuousOn f s ↔ Continuous (s.domRestrict f)
-/
theorem measurable_of_continuousOn_compl_singleton [T1Space α] {f : α → γ} (a : α)
    (hf : ContinuousOn f {a}ᶜ) : Measurable f :=
  measurable_of_measurable_on_compl_singleton a
    (continuousOn_iff_continuous_domRestrict.1 hf).measurable
/-
**Continuous.measurable2** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：Continuous.measurable2 [SecondCountableTopologyEither α β] {f : δ -> α} {g
 : δ -> β} {c : α -> β -> γ} (h : Continuous fun p : α × β => c p.1 p.2) (hf : M
easurable f) (hg : Measurable g) : Measurable fun a => c (f a) (g a)
参数：h : Continuous fun p : α × β => c p.1 p.2；hf : Measurable f；hg : Measurable g
。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Measurable.comp`：∀ {α : Type u_1} {β : Type u_2} {γ : Type u_3} {x : Mea
surableSpace α} {x_1 : MeasurableSpace β}   {x_2 : MeasurableSpace γ} {g : β → γ
} {f …
· 使用定理 `Continuous.measurable`：Continuous.measurable {f : α -> γ} (hf : Continuo
us f) : Measurable f
· 使用定理 `Measurable.prodMk`：Measurable.prodMk {β γ} {_ : MeasurableSpace β} {_ : 
MeasurableSpace γ} {f : α -> β} {g : α -> γ} (hf : Measurable f) (hg : Measurabl
e g) : …
-/
theorem Continuous.measurable2 [SecondCountableTopologyEither α β] {f : δ → α}
    {g : δ → β} {c : α → β → γ} (h : Continuous fun p : α × β => c p.1 p.2) (hf : Measurable f)
    (hg : Measurable g) : Measurable fun a => c (f a) (g a) :=
  h.measurable.comp (hf.prodMk hg)
/-
**Continuous.aemeasurable2** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：Continuous.aemeasurable2 [SecondCountableTopologyEither α β] {f : δ -> α} 
{g : δ -> β} {c : α -> β -> γ} {μ : Measure δ} (h : Continuous fun p : α × β => 
c p.1 p.2) (hf : AEMeasurable f μ) (hg : AEMeasurable g μ) : AEMeasurable (fun a
 => c (f a) (g a)) μ
参数：h : Continuous fun p : α × β => c p.1 p.2；hf : AEMeasurable f μ；hg : AEMeasur
able g μ。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Measurable.comp_aemeasurable`：Measurable.comp_aemeasurable [MeasurableSp
ace δ] {f : α -> δ} {g : δ -> β} (hg : Measurable g) (hf : AEMeasurable f μ) : A
EMeasurable (g ∘ f…
· 使用定理 `Continuous.measurable`：Continuous.measurable {f : α -> γ} (hf : Continuo
us f) : Measurable f
· 使用定理 `AEMeasurable.prodMk`：prodMk {f : α -> β} {g : α -> γ} (hf : AEMeasurable
 f μ) (hg : AEMeasurable g μ) : AEMeasurable (fun x => (f x, g x)) μ
-/
theorem Continuous.aemeasurable2 [SecondCountableTopologyEither α β]
    {f : δ → α} {g : δ → β} {c : α → β → γ} {μ : Measure δ}
    (h : Continuous fun p : α × β => c p.1 p.2) (hf : AEMeasurable f μ) (hg : AEMeasurable g μ) :
    AEMeasurable (fun a => c (f a) (g a)) μ :=
  h.measurable.comp_aemeasurable (hf.prodMk hg)
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) ContinuousInv₀.measurableInv [GroupWithZero γ] [T1Space γ]
    [ContinuousInv₀ γ] : MeasurableInv γ :=
  ⟨measurable_of_continuousOn_compl_singleton 0 continuousOn_inv₀⟩

@[to_additive]
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) ContinuousMul.measurableMul₂ [SecondCountableTopology γ] [Mul γ]
    [ContinuousMul γ] : MeasurableMul₂ γ :=
  ⟨continuous_mul.measurable⟩
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) ContinuousSub.measurableSub₂ [SecondCountableTopology γ] [Sub γ]
    [ContinuousSub γ] : MeasurableSub₂ γ :=
  ⟨continuous_sub.measurable⟩
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 100) ContinuousSMul.measurableSMul₂ {M α} [TopologicalSpace M]
    [MeasurableSpace M] [OpensMeasurableSpace M] [TopologicalSpace α]
    [SecondCountableTopologyEither M α] [MeasurableSpace α] [BorelSpace α] [SMul M α]
    [ContinuousSMul M α] : MeasurableSMul₂ M α :=
  ⟨continuous_smul.measurable⟩
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [SecondCountableTopology α] [T2Space α] : MeasurableEq α :=
  ⟨isClosed_diagonal.measurableSet⟩

end

section BorelSpace

variable [TopologicalSpace α] [mα : MeasurableSpace α] [BorelSpace α] [mβ : TopologicalSpace β]
  [MeasurableSpace β] [BorelSpace β] [TopologicalSpace γ] [MeasurableSpace γ] [BorelSpace γ]
  [MeasurableSpace δ]

/-
**pi_le_borel_pi** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：pi_le_borel_pi {ι : Type*} {X : ι -> Type*} [forall i, TopologicalSpace (X
 i)] [forall i, MeasurableSpace (X i)] [forall i, BorelSpace (X i)] : Measurable
Space.pi <= borel (forall i, X i)
参数：X i；X i；X i。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `BorelSpace.measurable_eq`：∀ {α : Type u_6} {inst : TopologicalSpace α} {
inst_1 : MeasurableSpace α} [self : BorelSpace α], inst_1 = borel α
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `iSup_le`：iSup_le (h : forall i, f i <= a) : iSup f <= a
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `MeasurableSpace.comap_le_iff_le_map`：comap_le_iff_le_map {f : α -> β} : 
m'.comap f <= m ↔ m' <= m.map f
· 使用定理 `Continuous.borel_measurable`：Continuous.borel_measurable [TopologicalSpa
ce α] [TopologicalSpace β] {f : α -> β} (hf : Continuous f) : @Measurable α β (b
orel α) (borel β)…
· 使用定理 `continuous_apply`：continuous_apply (a : α) : Continuous (fun f : (α → X)
 ↦ f a)
-/
theorem pi_le_borel_pi {ι : Type*} {X : ι → Type*} [∀ i, TopologicalSpace (X i)]
    [∀ i, MeasurableSpace (X i)] [∀ i, BorelSpace (X i)] :
      MeasurableSpace.pi ≤ borel (∀ i, X i) := by
  have : ‹∀ i, MeasurableSpace (X i)› = fun i => borel (X i) :=
    funext fun i => BorelSpace.measurable_eq
  rw [this]
  exact iSup_le fun i => comap_le_iff_le_map.2 <| (continuous_apply i).borel_measurable
/-
**prod_le_borel_prod** 是 Mathlib 中的一个定理，位于命名空间 ``。
形式化陈述：prod_le_borel_prod : Prod.instMeasurableSpace <= borel (α × β)
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `BorelSpace.measurable_eq`：∀ {α : Type u_6} {inst : TopologicalSpace α} {
inst_1 : MeasurableSpace α} [self : BorelSpace α], inst_1 = borel α
· 使用定理 `sup_le`：sup_le : a <= c -> b <= c -> a ⊔ b <= c
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `MeasurableSpace.comap_le_iff_le_map`：comap_le_iff_le_map {f : α -> β} : 
m'.comap f <= m ↔ m' <= m.map f
· 使用定理 `Continuous.borel_measurable`：Continuous.borel_measurable [TopologicalSpa
ce α] [TopologicalSpace β] {f : α -> β} (hf : Continuous f) : @Measurable α β (b
orel α) (borel β)…
· 使用定理 `continuous_fst`：continuous_fst (f : X → Y × Z) (hf : Continuous f) : Con
tinuous (fun x ↦ (f x).fst)
· 使用定理 `continuous_snd`：continuous_snd (f : X → Y × Z) (hf : Continuous f) : Con
tinuous (fun x ↦ (f x).snd)
-/
theorem prod_le_borel_prod : Prod.instMeasurableSpace ≤ borel (α × β) := by
  rw [‹BorelSpace α›.measurable_eq, ‹BorelSpace β›.measurable_eq]
  refine sup_le ?_ ?_
  · exact comap_le_iff_le_map.mpr continuous_fst.borel_measurable
  · exact comap_le_iff_le_map.mpr continuous_snd.borel_measurable
/-
**Pi.borelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Pi.borelSpace {ι : Type*} {X : ι -> Type*} [Countable ι] [forall i, Topolo
gicalSpace (X i)] [forall i, MeasurableSpace (X i)] [forall i, SecondCountableTo
pology (X i)] [forall i, BorelSpace (X i)] : BorelSpace (forall i, X i)
参数：X i；X i；X i；X i。
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `pi_le_borel_pi`：pi_le_borel_pi {ι : Type*} {X : ι -> Type*} [forall i, T
opologicalSpace (X i)] [forall i, MeasurableSpace (X i)] [forall i, BorelSpace (
X i)…
· 使用定理 `OpensMeasurableSpace.borel_le`：∀ {α : Type u_6} {inst : TopologicalSpace
 α} {h : MeasurableSpace α} [self : OpensMeasurableSpace α], borel α ≤ h
· 使用定理 `BorelSpace.opensMeasurable`：∀ {α : Type u_6} [inst : TopologicalSpace α]
 [inst_1 : MeasurableSpace α] [BorelSpace α], OpensMeasurableSpace α
-/
instance Pi.borelSpace {ι : Type*} {X : ι → Type*} [Countable ι] [∀ i, TopologicalSpace (X i)]
    [∀ i, MeasurableSpace (X i)] [∀ i, SecondCountableTopology (X i)] [∀ i, BorelSpace (X i)] :
    BorelSpace (∀ i, X i) :=
  ⟨le_antisymm pi_le_borel_pi OpensMeasurableSpace.borel_le⟩

/-- This is not covered by `Pi.borelSpace` as it does not require second countability. -/
/-
**Pi.borelSpace_of_subsingleton** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Pi.borelSpace_of_subsingleton {ι : Type*} {X : ι -> Type*} [Subsingleton ι
] [forall i, TopologicalSpace (X i)] [forall i, MeasurableSpace (X i)] [forall i
, BorelSpace (X i)] : BorelSpace (forall i, X i)
参数：X i；X i；X i。
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `pi_le_borel_pi`：pi_le_borel_pi {ι : Type*} {X : ι -> Type*} [forall i, T
opologicalSpace (X i)] [forall i, MeasurableSpace (X i)] [forall i, BorelSpace (
X i)…
· 使用定理 `OpensMeasurableSpace.borel_le`：∀ {α : Type u_6} {inst : TopologicalSpace
 α} {h : MeasurableSpace α} [self : OpensMeasurableSpace α], borel α ≤ h
· 使用定理 `BorelSpace.opensMeasurable`：∀ {α : Type u_6} [inst : TopologicalSpace α]
 [inst_1 : MeasurableSpace α] [BorelSpace α], OpensMeasurableSpace α

--- 原说明 ---
This is not covered by `Pi.borelSpace` as it does not require second countabilit
y.
-/
instance Pi.borelSpace_of_subsingleton {ι : Type*} {X : ι → Type*} [Subsingleton ι]
    [∀ i, TopologicalSpace (X i)] [∀ i, MeasurableSpace (X i)] [∀ i, BorelSpace (X i)] :
    BorelSpace (∀ i, X i) :=
  ⟨le_antisymm pi_le_borel_pi OpensMeasurableSpace.borel_le⟩
/-
**Prod.borelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Prod.borelSpace [SecondCountableTopologyEither α β] : BorelSpace (α × β)
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `prod_le_borel_prod`：prod_le_borel_prod : Prod.instMeasurableSpace <= bor
el (α × β)
· 使用定理 `OpensMeasurableSpace.borel_le`：∀ {α : Type u_6} {inst : TopologicalSpace
 α} {h : MeasurableSpace α} [self : OpensMeasurableSpace α], borel α ≤ h
· 使用定理 `BorelSpace.opensMeasurable`：∀ {α : Type u_6} [inst : TopologicalSpace α]
 [inst_1 : MeasurableSpace α] [BorelSpace α], OpensMeasurableSpace α
-/
instance Prod.borelSpace [SecondCountableTopologyEither α β] :
    BorelSpace (α × β) :=
  ⟨le_antisymm prod_le_borel_prod OpensMeasurableSpace.borel_le⟩

/-- Given a measurable embedding to a Borel space which is also a topological embedding, then the
source space is also a Borel space. -/
/-
**MeasurableEmbedding.borelSpace** 是 Mathlib 中的一个引理，位于命名空间 ``。
形式化陈述：MeasurableEmbedding.borelSpace {α β : Type*} [MeasurableSpace α] [Topologi
calSpace α] [MeasurableSpace β] [TopologicalSpace β] [hβ : BorelSpace β] {e : α 
-> β} (h'e : MeasurableEmbedding e) (h''e : IsInducing e) : BorelSpace α
参数：h'e : MeasurableEmbedding e；h''e : IsInducing e。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `BorelSpace.measurable_eq`：∀ {α : Type u_6} {inst : TopologicalSpace α} {
inst_1 : MeasurableSpace α} [self : BorelSpace α], inst_1 = borel α
· 使用定理 `MeasurableEmbedding.comap_eq`：∀ {α : Type u_1} {β : Type u_2} [inst : Me
asurableSpace α] [inst_1 : MeasurableSpace β] {f : α → β},   MeasurableEmbedding
 f → MeasurableSpa…
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `borel_comap`：borel_comap {f : α -> β} {t : TopologicalSpace β} : @borel 
α (t.induced f) = (@borel β t).comap f
· 使用定理 `Topology.IsInducing.eq_induced`：∀ {X : Type u_1} {Y : Type u_2} [tX : To
pologicalSpace X] [tY : TopologicalSpace Y] {f : X → Y},   Topology.IsInducing f
 → tX = TopologicalS…

--- 原说明 ---
Given a measurable embedding to a Borel space which is also a topological embedd
ing, then the
source space is also a Borel space.
-/
lemma MeasurableEmbedding.borelSpace {α β : Type*} [MeasurableSpace α] [TopologicalSpace α]
    [MeasurableSpace β] [TopologicalSpace β] [hβ : BorelSpace β] {e : α → β}
    (h'e : MeasurableEmbedding e) (h''e : IsInducing e) :
    BorelSpace α := by
  constructor
  have : MeasurableSpace.comap e (borel β) = ‹_› := by simpa [hβ.measurable_eq] using h'e.comap_eq
  rw [← this, ← borel_comap, h''e.eq_induced]
/-
**_root_.ULift.instBorelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：_root_.ULift.instBorelSpace : BorelSpace (ULift α)
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance _root_.ULift.instBorelSpace : BorelSpace (ULift α) :=
  MeasurableEquiv.ulift.measurableEmbedding.borelSpace Homeomorph.ulift.isInducing
/-
**DiscreteMeasurableSpace.toBorelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：DiscreteMeasurableSpace.toBorelSpace {α : Type*} [TopologicalSpace α] [Dis
creteTopology α] [MeasurableSpace α] [DiscreteMeasurableSpace α] : BorelSpace α
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableSpace.ext`：MeasurableSpace.ext {m₁ m₂ : MeasurableSpace α} (h 
: forall s : Set α, MeasurableSet[m₁] s ↔ MeasurableSet[m₂] s) : m₁ = m₂
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
instance DiscreteMeasurableSpace.toBorelSpace {α : Type*} [TopologicalSpace α] [DiscreteTopology α]
    [MeasurableSpace α] [DiscreteMeasurableSpace α] : BorelSpace α := by
  constructor; ext; simp [MeasurableSpace.measurableSet_generateFrom, MeasurableSet.of_discrete]
/-
**Topology.IsEmbedding.measurableEmbedding** 是 Mathlib 中的一个定理，位于命名空间 `Topology.I
sEmbedding`。
形式化陈述：∀ {α : Type u_1} {β : Type u_2} [inst : TopologicalSpace α] [mα : Measurab
leSpace α] [BorelSpace α]   [mβ : TopologicalSpace β] [inst_2 : MeasurableSpace 
β] [BorelSpace β] {f : α → β},   Topology.IsEmbedding f → MeasurableSet (Set.ran
ge f) → MeasurableEmbedding f
参数：Set.range f。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasurableEmbedding.comp`：comp (hg : MeasurableEmbedding g) (hf : Measur
ableEmbedding f) : MeasurableEmbedding (g ∘ f)
· 使用定理 `MeasurableEmbedding.subtype_coe`：subtype_coe (hs : MeasurableSet s) : Me
asurableEmbedding ((↑) : s -> α) where injective
· 使用定理 `MeasurableEquiv.measurableEmbedding`：∀ {α : Type u_1} {β : Type u_2} [in
st : MeasurableSpace α] [inst_1 : MeasurableSpace β] (e : α ≃ᵐ β),   MeasurableE
mbedding ⇑e
-/
protected theorem Topology.IsEmbedding.measurableEmbedding {f : α → β} (h₁ : IsEmbedding f)
    (h₂ : MeasurableSet (range f)) : MeasurableEmbedding f :=
  show MeasurableEmbedding
      (((↑) : range f → β) ∘ h₁.toHomeomorph.toMeasurableEquiv) from
    (MeasurableEmbedding.subtype_coe h₂).comp (MeasurableEquiv.measurableEmbedding _)
/-
**Topology.IsClosedEmbedding.measurableEmbedding** 是 Mathlib 中的一个定理，位于命名空间 `Topo
logy.IsClosedEmbedding`。
形式化陈述：∀ {α : Type u_1} {β : Type u_2} [inst : TopologicalSpace α] [mα : Measurab
leSpace α] [BorelSpace α]   [mβ : TopologicalSpace β] [inst_2 : MeasurableSpace 
β] [BorelSpace β] {f : α → β},   Topology.IsClosedEmbedding f → MeasurableEmbedd
ing f
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Topology.IsEmbedding.measurableEmbedding`：∀ {α : Type u_1} {β : Type u_2
} [inst : TopologicalSpace α] [mα : MeasurableSpace α] [BorelSpace α]   [mβ : To
pologicalSpace β] [inst_2 : Me…
· 使用定理 `Topology.IsClosedEmbedding.isEmbedding`：∀ {X : Type u_1} {Y : Type u_2} 
{f : X → Y} [inst : TopologicalSpace X] [inst_1 : TopologicalSpace Y],   Topolog
y.IsClosedEmbedding f → Topo…
· 使用定理 `IsClosed.measurableSet`：IsClosed.measurableSet (h : IsClosed s) : Measur
ableSet s
· 使用定理 `BorelSpace.opensMeasurable`：∀ {α : Type u_6} [inst : TopologicalSpace α]
 [inst_1 : MeasurableSpace α] [BorelSpace α], OpensMeasurableSpace α
· 使用定理 `Topology.IsClosedEmbedding.isClosed_range`：∀ {X : Type u_1} {Y : Type u_
2} [tX : TopologicalSpace X] [tY : TopologicalSpace Y] {f : X → Y},   Topology.I
sClosedEmbedding f → IsClosed (…
-/
protected theorem Topology.IsClosedEmbedding.measurableEmbedding {f : α → β}
    (h : IsClosedEmbedding f) : MeasurableEmbedding f :=
  h.isEmbedding.measurableEmbedding h.isClosed_range.measurableSet
/-
**Topology.IsOpenEmbedding.measurableEmbedding** 是 Mathlib 中的一个定理，位于命名空间 `Topolo
gy.IsOpenEmbedding`。
形式化陈述：∀ {α : Type u_1} {β : Type u_2} [inst : TopologicalSpace α] [mα : Measurab
leSpace α] [BorelSpace α]   [mβ : TopologicalSpace β] [inst_2 : MeasurableSpace 
β] [BorelSpace β] {f : α → β},   Topology.IsOpenEmbedding f → MeasurableEmbeddin
g f
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Topology.IsEmbedding.measurableEmbedding`：∀ {α : Type u_1} {β : Type u_2
} [inst : TopologicalSpace α] [mα : MeasurableSpace α] [BorelSpace α]   [mβ : To
pologicalSpace β] [inst_2 : Me…
· 使用定理 `Topology.IsOpenEmbedding.isEmbedding`：∀ {X : Type u_1} {Y : Type u_2} {f
 : X → Y} [inst : TopologicalSpace X] [inst_1 : TopologicalSpace Y],   Topology.
IsOpenEmbedding f → Topolo…
· 使用定理 `IsOpen.measurableSet`：IsOpen.measurableSet (h : IsOpen s) : MeasurableSe
t s
· 使用定理 `BorelSpace.opensMeasurable`：∀ {α : Type u_6} [inst : TopologicalSpace α]
 [inst_1 : MeasurableSpace α] [BorelSpace α], OpensMeasurableSpace α
· 使用定理 `Topology.IsOpenEmbedding.isOpen_range`：∀ {X : Type u_1} {Y : Type u_2} [
tX : TopologicalSpace X] [tY : TopologicalSpace Y] {f : X → Y},   Topology.IsOpe
nEmbedding f → IsOpen (Set.…
-/
protected theorem Topology.IsOpenEmbedding.measurableEmbedding {f : α → β} (h : IsOpenEmbedding f) :
    MeasurableEmbedding f :=
  h.isEmbedding.measurableEmbedding h.isOpen_range.measurableSet
/-
**Empty.borelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Empty.borelSpace : BorelSpace Empty
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `borel_eq_top_of_discrete`：borel_eq_top_of_discrete [TopologicalSpace α] 
[DiscreteTopology α] : borel α = ⊤
· 使用定理 `instDiscreteTopologyEmpty`：DiscreteTopology Empty
-/
instance Empty.borelSpace : BorelSpace Empty :=
  ⟨borel_eq_top_of_discrete.symm⟩
/-
**Unit.borelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Unit.borelSpace : BorelSpace Unit
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `borel_eq_top_of_discrete`：borel_eq_top_of_discrete [TopologicalSpace α] 
[DiscreteTopology α] : borel α = ⊤
· 使用定理 `instDiscreteTopologyPUnit`：DiscreteTopology PUnit.{u_1 + 1}
-/
instance Unit.borelSpace : BorelSpace Unit :=
  ⟨borel_eq_top_of_discrete.symm⟩
/-
**Bool.borelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Bool.borelSpace : BorelSpace Bool
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `borel_eq_top_of_discrete`：borel_eq_top_of_discrete [TopologicalSpace α] 
[DiscreteTopology α] : borel α = ⊤
· 使用定理 `instDiscreteTopologyBool`：DiscreteTopology Bool
-/
instance Bool.borelSpace : BorelSpace Bool :=
  ⟨borel_eq_top_of_discrete.symm⟩
/-
**Nat.borelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Nat.borelSpace : BorelSpace Nat
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `borel_eq_top_of_discrete`：borel_eq_top_of_discrete [TopologicalSpace α] 
[DiscreteTopology α] : borel α = ⊤
· 使用定理 `instDiscreteTopologyNat`：DiscreteTopology ℕ
-/
instance Nat.borelSpace : BorelSpace ℕ :=
  ⟨borel_eq_top_of_discrete.symm⟩
/-
**Int.borelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Int.borelSpace : BorelSpace Int
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `borel_eq_top_of_discrete`：borel_eq_top_of_discrete [TopologicalSpace α] 
[DiscreteTopology α] : borel α = ⊤
· 使用定理 `instDiscreteTopologyInt`：DiscreteTopology ℤ
-/
instance Int.borelSpace : BorelSpace ℤ :=
  ⟨borel_eq_top_of_discrete.symm⟩
/-
**Rat.borelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Rat.borelSpace : BorelSpace Rat
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `borel_eq_top_of_countable`：borel_eq_top_of_countable {α : Type*} [Topolo
gicalSpace α] [T0Space α] [Countable α] : borel α = ⊤
· 使用定理 `T6Space.toT0Space`：∀ {X : Type u} {inst : TopologicalSpace X} [self : T6
Space X], T0Space X
· 使用定理 `instT6SpaceOfMetrizableSpace`：∀ {X : Type u_1} [inst : TopologicalSpace 
X] [TopologicalSpace.MetrizableSpace X], T6Space X
· 使用定理 `EMetricSpace.metrizableSpace`：∀ {α : Type u_2} [inst : EMetricSpace α], 
TopologicalSpace.MetrizableSpace α
· 使用定理 `Encodable.countable`：∀ {α : Type u_1} [Encodable α], Countable α
-/
instance Rat.borelSpace : BorelSpace ℚ :=
  ⟨borel_eq_top_of_countable.symm⟩

/- Instances on `Real` and `Complex` are special cases of `RCLike` but without these instances,
Lean fails to prove `BorelSpace (ι → ℝ)`, so we leave them here. -/
/-
**Real.measurableSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Real.measurableSpace : MeasurableSpace Real
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Instances on `Real` and `Complex` are special cases of `RCLike` but without thes
e instances,
Lean fails to prove `BorelSpace (ι → ℝ)`, so we leave them here.
-/
instance Real.measurableSpace : MeasurableSpace ℝ :=
  borel ℝ
/-
**Real.borelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：Real.borelSpace : BorelSpace Real
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance Real.borelSpace : BorelSpace ℝ :=
  ⟨rfl⟩
/-
**NNReal.measurableSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：NNReal.measurableSpace : MeasurableSpace Real>=0
该定义给出了一等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance NNReal.measurableSpace : MeasurableSpace ℝ≥0 :=
  inferInstanceAs <| MeasurableSpace (Subtype _)
/-
**NNReal.borelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：NNReal.borelSpace : BorelSpace Real>=0
该定义给出了一等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance NNReal.borelSpace : BorelSpace ℝ≥0 :=
  inferInstanceAs <| BorelSpace (Subtype _)
/-
**ENNReal.measurableSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：ENNReal.measurableSpace : MeasurableSpace Real>=0∞
该定义给出了一等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance ENNReal.measurableSpace : MeasurableSpace ℝ≥0∞ :=
  borel ℝ≥0∞
/-
**ENNReal.borelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：ENNReal.borelSpace : BorelSpace Real>=0∞
该定义给出了一等式。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance ENNReal.borelSpace : BorelSpace ℝ≥0∞ :=
  ⟨rfl⟩
/-
**EReal.measurableSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：EReal.measurableSpace : MeasurableSpace EReal
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance EReal.measurableSpace : MeasurableSpace EReal :=
  borel EReal
/-
**EReal.borelSpace** 是 Mathlib 中的一个实例，位于命名空间 ``。
形式化陈述：EReal.borelSpace : BorelSpace EReal
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance EReal.borelSpace : BorelSpace EReal :=
  ⟨rfl⟩

namespace MeasureTheory.Measure.IsFiniteMeasureOnCompacts

variable {mα} in
/-
**MeasureTheory.Measure.IsFiniteMeasureOnCompacts.map** 是 Mathlib 中的一个定理，位于命名空间 
`MeasureTheory.Measure.IsFiniteMeasureOnCompacts`。
形式化陈述：∀ {α : Type u_1} {β : Type u_2} [inst : TopologicalSpace α] {mα : Measurab
leSpace α} [BorelSpace α]   [mβ : TopologicalSpace β] [inst_2 : MeasurableSpace 
β] [BorelSpace β] (μ : MeasureTheory.Measure α)   [MeasureTheory.IsFiniteMeasure
OnCompacts μ] (f : α ≃ₜ β),   MeasureTheory.IsFiniteMeasureOnCompacts (MeasureTh
eory.Measure.map (⇑f) μ)
参数：μ : MeasureTheory.Measure α；f : α ≃ₜ β；MeasureTheory.Measure.map (⇑f) μ。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Homeomorph.toMeasurableEquiv_coe`：Homeomorph.toMeasurableEquiv_coe (h : 
γ ≃ₜ γ₂) : (h.toMeasurableEquiv : γ -> γ₂) = h
· 使用定理 `MeasurableEquiv.map_apply`：∀ {α : Type u_1} {β : Type u_2} {x : Measurab
leSpace α} [inst : MeasurableSpace β] {μ : MeasureTheory.Measure α}   (f : α ≃ᵐ 
β) (s : Set β),…
· 使用定理 `IsCompact.measure_lt_top`：∀ {α : Type u_1} {m0 : MeasurableSpace α} [ins
t : TopologicalSpace α] {μ : MeasureTheory.Measure α}   [MeasureTheory.IsFiniteM
easureOnCompac…
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Homeomorph.isCompact_preimage`：isCompact_preimage {s : Set Y} (h : X ≃ₜ 
Y) : IsCompact (h ⁻¹' s) ↔ IsCompact s
-/
protected theorem map (μ : Measure α) [IsFiniteMeasureOnCompacts μ] (f : α ≃ₜ β) :
    IsFiniteMeasureOnCompacts (μ.map f) := by
  refine ⟨fun K hK ↦ ?_⟩
  rw [← f.toMeasurableEquiv_coe, MeasurableEquiv.map_apply]
  exact IsCompact.measure_lt_top (f.isCompact_preimage.2 hK)

variable {mβ} in
/-
**MeasureTheory.Measure.IsFiniteMeasureOnCompacts.comap** 是 Mathlib 中的一个定理，位于命名空
间 `MeasureTheory.Measure.IsFiniteMeasureOnCompacts`。
形式化陈述：∀ {α : Type u_1} {β : Type u_2} [inst : TopologicalSpace α] [mα : Measurab
leSpace α] [BorelSpace α]   {mβ : TopologicalSpace β} [inst_2 : MeasurableSpace 
β] [BorelSpace β] (μ : MeasureTheory.Measure β)   [MeasureTheory.IsFiniteMeasure
OnCompacts μ] (f : α ≃ₜ β),   MeasureTheory.IsFiniteMeasureOnCompacts (MeasureTh
eory.Measure.comap (⇑f) μ)
参数：μ : MeasureTheory.Measure β；f : α ≃ₜ β；MeasureTheory.Measure.comap (⇑f) μ。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `MeasureTheory.IsFiniteMeasureOnCompacts.comap'`：∀ {α : Type u_1} {β : Ty
pe u_2} {m0 : MeasurableSpace α} {mβ : MeasurableSpace β} [inst : TopologicalSpa
ce α]   [inst_1 : TopologicalSpace β…
· 使用定理 `Homeomorph.continuous`：∀ {X : Type u_1} {Y : Type u_2} [inst : Topologic
alSpace X] [inst_1 : TopologicalSpace Y] (h : X ≃ₜ Y), Continuous ⇑h
· 使用引理 `Homeomorph.measurableEmbedding`：Homeomorph.measurableEmbedding (h : γ ≃ₜ
 γ₂) : MeasurableEmbedding h
-/
protected theorem comap (μ : Measure β) [IsFiniteMeasureOnCompacts μ] (f : α ≃ₜ β) :
    IsFiniteMeasureOnCompacts (μ.comap f) :=
  IsFiniteMeasureOnCompacts.comap' μ f.continuous f.measurableEmbedding

end MeasureTheory.Measure.IsFiniteMeasureOnCompacts

end BorelSpace

