/-
Copyright (c) 2020 Johan Commelin. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Johan Commelin
-/
module

public import Mathlib.Data.Fintype.Lattice
public import Mathlib.RingTheory.Finiteness.Basic

/-!
# Finite suprema of finite modules

-/

public section

namespace Submodule

open Module

variable {R V} [Ring R] [AddCommGroup V] [Module R V]

/-- The submodule generated by a supremum of finite-dimensional submodules, indexed by a finite
sort is finite-dimensional. -/
/-
**Submodule.finite_iSup** 是 Mathlib 中的一个实例，位于命名空间 `Submodule`。
形式化陈述：finite_iSup {ι : Sort*} [Finite ι] (S : ι -> Submodule R V) [forall i, Mod
ule.Finite R (S i)] : Module.Finite R ↑(⨆ i, S i)
参数：S : ι -> Submodule R V；S i。
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `nonempty_fintype`：nonempty_fintype (α : Type*) [Finite α] : Nonempty (Fi
ntype α)
· 使用定理 `instFinitePLift`：∀ {α : Sort u_1} [Finite α], Finite (PLift α)
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `iSup_plift_down`：iSup_plift_down (f : ι -> α) : ⨆ i, f (PLift.down i) = 
⨆ i, f i
· 使用定理 `Finset.sup_univ_eq_iSup`：sup_univ_eq_iSup [CompleteLattice β] (f : α -> 
β) : Finset.univ.sup f = iSup f

--- 原说明 ---
The submodule generated by a supremum of finite-dimensional submodules, indexed 
by a finite
sort is finite-dimensional.
-/
instance finite_iSup {ι : Sort*} [Finite ι] (S : ι → Submodule R V)
    [∀ i, Module.Finite R (S i)] : Module.Finite R ↑(⨆ i, S i) := by
  cases nonempty_fintype (PLift ι)
  rw [← iSup_plift_down, ← Finset.sup_univ_eq_iSup]
  exact Submodule.finite_finset_sup _ _

end Submodule

