/-
Copyright (c) 2025 Riccardo Brasca. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Riccardo Brasca, Anthony Fernandes, Marc Robin
-/
module

public import Mathlib.Algebra.Module.SpanRank
public import Mathlib.RingTheory.Noetherian.OfPrime
public import Mathlib.RingTheory.PowerSeries.Inverse
public import Mathlib.RingTheory.PowerSeries.Trunc
public import Mathlib.RingTheory.UniqueFactorizationDomain.Kaplansky

/-!
# Ideals in power series.

We gather miscellaneous results about prime ideals in `R⟦X⟧`. More precisely, we prove that, given
a prime ideal `I` of `R⟦X⟧`, if the ideal generated by the constant coefficients of the `f ∈ I` is
generated by `n` elements, then `I` is generated by at most `n + 1` elements (actually it is
generated by `n` elements if `X ∉ I` and `n + 1` elements otherwise). This implies immediately that
`R⟦X⟧` is noetherian if `R` is. The proof of `eq_of_le_of_X_notMem_of_fg_of_isPrime` is inspired by
[zbMATH05171613].


## Main results

* `PowerSeries.eq_span_insert_X_of_X_mem_of_span_eq` : given an ideal `I` of `R⟦X⟧` such that
  `X ∈ I`, if `I.map constantCoeff` is generated by `S : Set T`, then `I` is generated by
  `insert X (C '' S)`.
* `PowerSeries.exist_eq_span_eq_ncard_of_X_notMem` : given a prime ideal `I` of `R⟦X⟧` such
  that `X ∉ I`, if `I.map constantCoeff` is generated by a finite set `S : Set T`, then there exists
  `T : Set R`, of the same cardinality as `S`, that generates `I`.
* `PowerSeries.fg_iff_of_isPrime` : a prime ideal `P` of `R⟦X⟧` is finitely generated if and only
  if `P.map constantCoeff` is finitely generated.


## Implementation details

There is a simpler proof of `IsNoetherianRing R → IsNoetherianRing R⟦X⟧`, similar to that for
polynomials. On the other hand our proof has the advantage of giving explicitly the number of
generators, and it is used to prove that, for a domain `R`, we have
`IsPrincipalIdealRing R → UniqueFactorizationMonoid R⟦X⟧`.

## TODO
Prove noetherianity of `MvPowerSeries` in finitely many variables.

-/

public section

variable {R : Type*}

open Ideal Set Finset

namespace PowerSeries
variable [CommRing R] {I : Ideal R⟦X⟧} {S : Set R}

section X_mem

/-
**PowerSeries.map_constantCoeff_le_self_of_X_mem** 是 Mathlib 中的一个定理，位于命名空间 `Powe
rSeries`。
形式化陈述：map_constantCoeff_le_self_of_X_mem (hXI : X in I) : I.map (C.comp constant
Coeff) <= I
参数：hXI : X in I。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `sub_eq_iff_eq_add'`：∀ {G : Type u_3} [inst : AddCommGroup G] {a b c : G}
, a - b = c ↔ a = b + c
· 使用定理 `PowerSeries.eq_X_mul_shift_add_const`：eq_X_mul_shift_add_const (φ : R⟦X⟧
) : φ = (X * mk fun p => coeff (p + 1) φ) + C (constantCoeff φ)
· 使用定理 `sub_mem`：∀ {M : Type u_3} {S : Type u_4} [inst : SubNegMonoid M] [inst_1
 : SetLike S M] [hSM : AddSubgroupClass S M] {H : S}   {x y : M}, x ∈ H → y ∈…
· 使用定理 `Ideal.mul_mem_right`：mul_mem_right {α} {a : α} (b : α) [Semiring α] (I :
 Ideal α) [I.IsTwoSided] (h : a in I) : a * b in I
· 使用定理 `Ideal.instIsTwoSided_1`：∀ {α : Type u_1} [inst : CommRing α] (I : Ideal 
α), I.IsTwoSided
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
-/
theorem map_constantCoeff_le_self_of_X_mem (hXI : X ∈ I) :
    I.map (C.comp constantCoeff) ≤ I := by
  suffices ∀ x ∈ I, C (constantCoeff x) ∈ I by simpa [Ideal.map_le_iff_le_comap]
  intro f hf
  rw [← sub_eq_iff_eq_add'.mpr f.eq_X_mul_shift_add_const]
  exact sub_mem hf (I.mul_mem_right _ hXI)

/-- Given an ideal `I` of `R⟦X⟧` such that `X ∈ I`, if `I.map constantCoeff` is generated by
`S : Set T`, then `I` is generated by `insert X (C '' S)`. -/
/-
**PowerSeries.eq_span_insert_X_of_X_mem_of_span_eq** 是 Mathlib 中的一个定理，位于命名空间 `Po
werSeries`。
形式化陈述：eq_span_insert_X_of_X_mem_of_span_eq (hXI : X in I) (hSI : span S = I.map 
constantCoeff) : I = span (insert X (C '' S))
参数：hXI : X in I；hSI : span S = I.map constantCoeff。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Ideal.span_insert`：span_insert (x) (s : Set α) : span (insert x s) = spa
n ({x} : Set α) ⊔ span s
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.map_span`：map_span (s : Set R) : map f (span s) = span (f '' s)
· 使用定理 `Ideal.map_map`：map_map {T : Type*} [Semiring T] {I : Ideal R} (f : R ->+
* S) (g : S ->+* T) : (I.map f).map g = I.map (g.comp f)
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Ideal.mem_span_singleton_sup`：mem_span_singleton_sup {x y : α} {I : Idea
l α} : x in Ideal.span {y} ⊔ I ↔ exists a : α, exists b in I, a * y + b = x
· 使用定理 `Ideal.mem_map_of_mem`：mem_map_of_mem (f : F) {I : Ideal R} {x : R} (h : 
x in I) : f x in map f I
· 使用定理 `PowerSeries.eq_shift_mul_X_add_const`：eq_shift_mul_X_add_const (φ : R⟦X⟧
) : φ = (mk fun p => coeff (p + 1) φ) * X + C (constantCoeff φ)
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `and_self`：∀ (p : Prop), (p ∧ p) = p

--- 原说明 ---
Given an ideal `I` of `R⟦X⟧` such that `X ∈ I`, if `I.map constantCoeff` is gene
rated by
`S : Set T`, then `I` is generated by `insert X (C '' S)`.
-/
theorem eq_span_insert_X_of_X_mem_of_span_eq (hXI : X ∈ I) (hSI : span S = I.map constantCoeff) :
    I = span (insert X (C '' S)) := by
  rw [Ideal.span_insert, ← Ideal.map_span, hSI, Ideal.map_map]
  refine le_antisymm ?_ (by simp [Ideal.span_le, hXI, map_constantCoeff_le_self_of_X_mem])
  exact fun f hf ↦ mem_span_singleton_sup.mpr
    ⟨_, _, mem_map_of_mem _ hf, f.eq_shift_mul_X_add_const.symm⟩

open Submodule in
/-
**PowerSeries.spanFinrank_le_spanFinrank_map_constantCoeff_add_one_of_X_mem** 是 
Mathlib 中的一个定理，位于命名空间 `PowerSeries`。
形式化陈述：spanFinrank_le_spanFinrank_map_constantCoeff_add_one_of_X_mem (hI : X in I
) : spanFinrank I <= spanFinrank (I.map constantCoeff) + 1
参数：hI : X in I。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `PowerSeries.constantCoeff_surj`：constantCoeff_surj : Function.Surjective
 (constantCoeff (R
· 使用定理 `Submodule.FG.map`：∀ {R : Type u_1} {M : Type u_2} [inst : Semiring R] [i
nst_1 : AddCommMonoid M] [inst_2 : _root_.Module R M]   {S : Type u_3} {P : Type
 u_4} …
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.map_eq_submodule_map`：map_eq_submodule_map (f : R ->+* S) [h : Rin
gHomSurjective f] (I : Ideal R) : I.map f = Submodule.map f.toSemilinearMap I
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `PowerSeries.eq_span_insert_X_of_X_mem_of_span_eq`：eq_span_insert_X_of_X_
mem_of_span_eq (hXI : X in I) (hSI : span S = I.map constantCoeff) : I = span (i
nsert X (C '' S))
· 使用引理 `Submodule.span_generators`：span_generators (p : Submodule R M) : span R 
(generators p) = p
· 使用引理 `le_trans`：le_trans : a <= b -> b <= c -> a <= c
· 使用引理 `Submodule.spanFinrank_span_le_ncard_of_finite`：spanFinrank_span_le_ncard
_of_finite {s : Set M} (hs : s.Finite) : (span R s).spanFinrank <= s.ncard
· 使用定理 `Set.Finite.map`：∀ {α β : Type u_1} {s : Set α} (f : α → β), s.Finite → (
f <$> s).Finite
· 使用定理 `Submodule.FG.finite_generators`：∀ {R : Type u_1} {M : Type u} [inst : Se
miring R] [inst_1 : AddCommMonoid M] [inst_2 : _root_.Module R M]   {p : Submodu
le R M}, p.FG → p.ge…
· 使用定理 `Set.ncard_insert_le`：ncard_insert_le (a : α) (s : Set α) : (insert a s).
ncard <= s.ncard + 1
· 使用定理 `covariant_swap_add_of_covariant_add`：∀ (N : Type u_2) (r : N → N → Prop)
 [inst : AddCommSemigroup N] [CovariantClass N N (fun x1 x2 => x1 + x2) r],   Co
variantClass N N (Functio…
· 使用定理 `IsOrderedAddMonoid.toAddLeftMono`：∀ {α : Type u_1} [inst : AddCommMonoid
 α] [inst_1 : Preorder α] [IsOrderedAddMonoid α], AddLeftMono α
· 使用定理 `IsRightCancelAdd.addRightReflectLE_of_addRightReflectLT`：∀ (N : Type u_2
) [inst : Add N] [IsRightCancelAdd N] [inst_2 : PartialOrder N] [AddRightReflect
LT N],   AddRightReflectLE N
· 使用定理 `instIsRightCancelAddOfAddRightReflectLE`：∀ {α : Type u_1} [inst : Add α]
 [inst_1 : PartialOrder α] [AddRightReflectLE α], IsRightCancelAdd α
· 使用定理 `addRightReflectLE_of_addLeftReflectLE`：∀ (N : Type u_2) [inst : AddCommS
emigroup N] [inst_1 : LE N] [AddLeftReflectLE N], AddRightReflectLE N
· 使用定理 `IsLeftCancelAdd.addLeftReflectLE_of_addLeftReflectLT`：∀ (N : Type u_2) [
inst : Add N] [IsLeftCancelAdd N] [inst_2 : PartialOrder N] [AddLeftReflectLT N]
, AddLeftReflectLE N
· 使用定理 `AddLeftCancelSemigroup.toIsLeftCancelAdd`：∀ {G : Type u} [self : AddLeft
CancelSemigroup G], IsLeftCancelAdd G
· 使用定理 `contravariant_swap_add_of_contravariant_add`：∀ (N : Type u_2) (r : N → N
 → Prop) [inst : AddCommSemigroup N] [ContravariantClass N N (fun x1 x2 => x1 + 
x2) r],   ContravariantClass N N …
· 使用定理 `Set.ncard_image_le`：ncard_image_le (hs : s.Finite
· 使用定理 `Submodule.FG.generators_ncard`：∀ {R : Type u_1} {M : Type u} [inst : Sem
iring R] [inst_1 : AddCommMonoid M] [inst_2 : _root_.Module R M]   {p : Submodul
e R M}, p.FG → p.ge…
· 使用定理 `le_refl`：∀ {α : Type u_1} [inst : Preorder α] (a : α), a ≤ a
· 使用定理 `Nat.zero_le`：∀ (n : ℕ), 0 ≤ n
· 使用引理 `Submodule.spanFinrank_of_not_fg`：spanFinrank_of_not_fg {p : Submodule R 
M} (hp : ¬p.FG) : p.spanFinrank = 0
-/
theorem spanFinrank_le_spanFinrank_map_constantCoeff_add_one_of_X_mem (hI : X ∈ I) :
    spanFinrank I ≤ spanFinrank (I.map constantCoeff) + 1 := by
  by_cases hfg : I.FG
  swap; · exact spanFinrank_of_not_fg hfg ▸ Nat.zero_le _
  replace hfg : (Ideal.map constantCoeff I).FG := by
    have : RingHomSurjective (constantCoeff (R := R)) := ⟨constantCoeff_surj⟩
    exact map_eq_submodule_map constantCoeff I ▸ Submodule.FG.map _ hfg
  nth_rw 1 [eq_span_insert_X_of_X_mem_of_span_eq hI (I.map constantCoeff).span_generators]
  refine le_trans (spanFinrank_span_le_ncard_of_finite ?_) (le_trans (Set.ncard_insert_le _ _) ?_)
  · simpa using! Set.Finite.map _ (FG.finite_generators hfg)
  · simp only [add_le_add_iff_right]
    refine le_trans (Set.ncard_image_le (FG.finite_generators hfg)) ?_
    rw [FG.generators_ncard hfg]

end X_mem

section X_notMem

variable (hXI : X ∉ I) [I.IsPrime]

/-
**PowerSeries.eq_of_le_of_X_notMem_of_fg_of_isPrime** 是 Mathlib 中的一个定理，位于命名空间 `P
owerSeries`。
形式化陈述：eq_of_le_of_X_notMem_of_fg_of_isPrime {J : Ideal R⟦X⟧} (hJI : J <= I) (hXI
 : X ∉ I) (hJ : J.FG) (h' : I.map constantCoeff <= J.map constantCoeff) : I = J
参数：hJI : J <= I；hXI : X ∉ I；hJ : J.FG；h' : I.map constantCoeff <= J.map constant
Coeff。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `LE.le.antisymm'`：∀ {α : Type u_1} [inst : PartialOrder α] {a b : α}, b ≤
 a → a ≤ b → a = b
· 使用定理 `LE.le.antisymm`：∀ {α : Type u_1} [inst : PartialOrder α] {a b : α}, a ≤ 
b → b ≤ a → a = b
· 使用定理 `Ideal.map_mono`：map_mono (h : I <= J) : map f I <= map f J
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Submodule.fg_iff_exists_fin_generating_family`：fg_iff_exists_fin_generat
ing_family {N : Submodule R M} : N.FG ↔ exists (n : Nat) (s : Fin n -> M), span 
R (range s) = N
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `map_sub`：∀ {G : Type u_7} {H : Type u_8} {F : Type u_9} [inst : FunLike 
F G H] [inst_1 : AddGroup G]   [inst_2 : SubtractionMonoid H] [AddMonoidHomCl…
· 使用定理 `RingHomClass.toAddMonoidHomClass`：∀ {F : Type u_5} {α : outParam (Type u
_6)} {β : outParam (Type u_7)} {inst : NonAssocSemiring α}   {inst_1 : NonAssocS
emiring β} {inst_2 : F…
· 使用定理 `map_sum`：∀ {ι : Type u_1} {M : Type u_3} {N : Type u_4} [inst : AddCommM
onoid M] [inst_1 : AddCommMonoid N] {G : Type u_7}   [inst_2 : FunLike G M N]…
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `map_mul`：map_mul [MulHomClass F M N] (f : F) (x y : M) : f (x * y) = f x
 * f y
· 使用定理 `NonUnitalRingHomClass.toMulHomClass`：∀ {F : Type u_5} {α : outParam (Typ
e u_6)} {β : outParam (Type u_7)} {inst : NonUnitalNonAssocSemiring α}   {inst_1
 : NonUnitalNonAssocSemir…
· 使用定理 `RingHomClass.toNonUnitalRingHomClass`：∀ {F : Type u_1} {α : Type u_2} {β
 : Type u_3} [inst : FunLike F α β] {x : NonAssocSemiring α}   {x_1 : NonAssocSe
miring β} [RingHomClass F …
· 使用定理 `sub_self`：∀ {G : Type u_1} [inst : AddGroup G] (a : G), a - a = 0
· 使用定理 `map_zero`：∀ {M : Type u_4} {N : Type u_5} {F : Type u_9} [inst : Zero M]
 [inst_1 : Zero N] [inst_2 : FunLike F M N]   [ZeroHomClass F M N] (f : F), f …
· 使用定理 `MonoidWithZeroHomClass.toZeroHomClass`：∀ {F : Type u_7} {α : outParam (T
ype u_8)} {β : outParam (Type u_9)} {inst : MulZeroOneClass α}   {inst_1 : MulZe
roOneClass β} {inst_2 : Fun…
· 使用定理 `RingHomClass.toMonoidWithZeroHomClass`：∀ {F : Type u_5} {α : outParam (T
ype u_6)} {β : outParam (Type u_7)} [inst : NonAssocSemiring α]   [inst_1 : NonA
ssocSemiring β] [inst_2 : F…
· 使用定理 `sub_zero`：∀ {G : Type u_3} [inst : SubNegZeroMonoid G] (a : G), a - 0 = 
a
· 使用定理 `DistribMulActionSemiHomClass.toAddMonoidHomClass`：∀ {F : Type u_10} {M :
 outParam (Type u_11)} {N : outParam (Type u_12)} {φ : outParam (M → N)}   {A : 
outParam (Type u_13)} {B : outParam (T…
· 使用定理 `SemilinearMapClass.distribMulActionSemiHomClass`：∀ {R : Type u_1} {S : T
ype u_5} {M : Type u_8} {M₃ : Type u_11} (F : Type u_14) [inst : Semiring R]   [
inst_1 : Semiring S] [inst_2 : AddCom…
· 使用定理 `Finset.sum_congr`：∀ {ι : Type u_1} {M : Type u_4} {s₁ s₂ : Finset ι} [in
st : AddCommMonoid M] {f g : ι → M},   s₁ = s₂ → (∀ x ∈ s₂, f x = g x) → s₁.sum 
f = s₂…
· 使用定理 `PowerSeries.coeff_C_mul`：coeff_C_mul (n : Nat) (φ : R⟦X⟧) (a : R) : coef
f n (C a * φ) = a * coeff n φ
· 使用定理 `PowerSeries.sub_const_eq_X_mul_shift`：sub_const_eq_X_mul_shift (φ : R⟦X⟧
) : φ - C (constantCoeff φ) = X * mk fun p => coeff (p + 1) φ
· 使用定理 `Or.resolve_left`：∀ {a b : Prop}, a ∨ b → ¬a → b
· 使用定理 `Ideal.IsPrime.mul_mem_iff_mem_or_mem`：∀ {α : Type u} [inst : Semiring α]
 {I : Ideal α} [I.IsTwoSided], I.IsPrime → ∀ {x y : α}, x * y ∈ I ↔ x ∈ I ∨ y ∈ 
I
· 使用定理 `Ideal.instIsTwoSided_1`：∀ {α : Type u_1} [inst : CommRing α] (I : Ideal 
α), I.IsTwoSided
· 使用定理 `Ideal.sub_mem`：∀ {α : Type u} [inst : Ring α] (I : Ideal α) {a b : α}, a
 ∈ I → b ∈ I → a - b ∈ I
· 使用定理 `Ideal.sum_mem`：sum_mem (I : Ideal α) {ι : Type*} {t : Finset ι} {f : ι -
> α} : (forall c in t, f c in I) -> (∑ i in t, f i) in I
（共 78 条，此处仅展示前 30 条）
-/
theorem eq_of_le_of_X_notMem_of_fg_of_isPrime {J : Ideal R⟦X⟧} (hJI : J ≤ I) (hXI : X ∉ I)
    (hJ : J.FG) (h' : I.map constantCoeff ≤ J.map constantCoeff) : I = J := by
  refine hJI.antisymm' ?_
  replace h' := h'.antisymm (Ideal.map_mono hJI)
  obtain ⟨n, F, rfl⟩ := Submodule.fg_iff_exists_fin_generating_family.mp hJ
  rw [submodule_span_eq, map_span, ← Set.range_comp] at h'
  choose! T hT using fun g (hg : g ∈ I) ↦
    mem_span_range_iff_exists_fun.mp (h'.le (Ideal.mem_map_of_mem _ hg))
  dsimp at hT
  intro g hg
  let G : ℕ → R⟦X⟧ :=
    Nat.rec g fun _ G' ↦ mk fun p ↦ coeff (p + 1) G' - ∑ i, T G' i * coeff (p + 1) (F i)
  have hG' (n : ℕ) (H : G n ∈ I) : G n - ∑ i, C (T (G n) i) * F i = X * G (n + 1) := by
    simpa [hT _ H] using sub_const_eq_X_mul_shift (G n - ∑ i, C (T (G n) i) * F i)
  have hG : ∀ n, G n ∈ I := Nat.rec hg fun n IH ↦
    (‹I.IsPrime›.mul_mem_iff_mem_or_mem.mp (hG' n IH ▸ I.sub_mem IH (I.sum_mem fun i _ ↦
      I.mul_mem_left _ (hJI (subset_span (Set.mem_range_self _)))))).resolve_left hXI
  replace hG' := fun n ↦ hG' n (hG n)
  let H (i : Fin n) : R⟦X⟧ := mk fun n ↦ T (G n) i
  have h₁ (n : ℕ) : g - ∑ i, trunc n (H i) * F i = X ^ n * G n := by
    induction n with
    | zero => simp [G]
    | succ n IH =>
      simp [trunc_succ, add_mul, sum_add_distrib, ← sub_sub, IH, pow_succ, mul_assoc, ← hG',
        mul_sub, H, mul_sum, monomial_eq_C_mul_X_pow, mul_left_comm (C _)]
  have h₂ : g = ∑ i, H i * F i := by
    ext n; simpa [coeff_trunc, sub_eq_zero] using congr(($(h₁ (n + 1)).trunc (n + 1)).coeff n)
  rw [h₂]
  exact Ideal.sum_mem _ fun i _ ↦ Ideal.mul_mem_left _ _ (subset_span (Set.mem_range_self _))

/-- Given a prime ideal `I` of `R⟦X⟧` such that `X ∉ I`, if `I.map constantCoeff` is generated by a
finite set `S : Set T`, then there exists `T : Set R`, of the same cardinality as `S`, that
generates `I`. -/
/-
**PowerSeries.exist_eq_span_eq_ncard_of_X_notMem** 是 Mathlib 中的一个定理，位于命名空间 `Powe
rSeries`。
形式化陈述：exist_eq_span_eq_ncard_of_X_notMem (hI : X ∉ I) {S : Set R} (hSI : span S 
= I.map constantCoeff) (hS : S.Finite) : exists T, I = span T ∧ T.Finite ∧ T.nca
rd = S.ncard
参数：hI : X ∉ I；hSI : span S = I.map constantCoeff；hS : S.Finite。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `Ideal.mem_map_iff_of_surjective`：mem_map_iff_of_surjective {I : Ideal R}
 {y} : y in map f I ↔ exists x, x in I ∧ f x = y
· 使用定理 `PowerSeries.constantCoeff_surj`：constantCoeff_surj : Function.Surjective
 (constantCoeff (R
· 使用定理 `Ideal.subset_span`：subset_span {s : Set α} : s subseteq span s
· 使用定理 `Set.SurjOn.exists_subset_injOn_image_eq`：∀ {α : Type u_1} {β : Type u_2}
 {s : Set α} {t : Set β} {f : α → β},   Set.SurjOn f s t → ∃ u ⊆ s, Set.InjOn f 
u ∧ f '' u = t
· 使用定理 `PowerSeries.eq_of_le_of_X_notMem_of_fg_of_isPrime`：eq_of_le_of_X_notMem_
of_fg_of_isPrime {J : Ideal R⟦X⟧} (hJI : J <= I) (hXI : X ∉ I) (hJ : J.FG) (h' :
 I.map constantCoeff <= J.map constantC…
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Ideal.span_le`：span_le {s : Set α} {I} : span s <= I ↔ s subseteq I
· 使用定理 `Submodule.fg_def`：fg_def {N : Submodule R M} : N.FG ↔ exists S : Set M, 
S.Finite ∧ span R S = N
· 使用定理 `Set.Finite.of_finite_image`：∀ {α : Type u} {β : Type v} {s : Set α} {f :
 α → β}, (f '' s).Finite → Set.InjOn f s → s.Finite
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.map_le_iff_le_comap`：map_le_iff_le_comap [RingHomClass F R S] : ma
p f I <= K ↔ I <= comap f K
· 使用定理 `Ideal.mem_comap`：mem_comap [RingHomClass F R S] {x} : x in comap f K ↔ f
 x in K
· 使用定理 `Ideal.mem_map_of_mem`：mem_map_of_mem (f : F) {I : Ideal R} {x : R} (h : 
x in I) : f x in map f I
· 使用定理 `Submodule.span_induction`：span_induction {p : (x : M) -> x in span R s -
> Prop} (mem : forall (x) (h : x in s), p x (subset_span h)) (zero : p 0 (Submod
ule.zero_mem _…
· 使用定理 `ZeroMemClass.zero_mem`：∀ {S : Type u_3} {M : outParam (Type u_4)} {inst 
: Zero M} {inst_1 : SetLike S M} [self : ZeroMemClass S M] (s : S),   0 ∈ s
· 使用定理 `AddSubmonoidClass.toZeroMemClass`：∀ {S : Type u_3} {M : outParam (Type u
_4)} {inst : AddZeroClass M} {inst_1 : SetLike S M}   [self : AddSubmonoidClass 
S M], ZeroMemClass S M
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Ideal.add_mem`：∀ {α : Type u} [inst : Semiring α] (I : Ideal α) {a b : α
}, a ∈ I → b ∈ I → a + b ∈ I
· 使用定理 `map_add`：∀ {M : Type u_4} {N : Type u_5} {F : Type u_9} [inst : Add M] [
inst_1 : Add N] [inst_2 : FunLike F M N]   [AddHomClass F M N] (f : F) (x y :…
· 使用定理 `AddMonoidHomClass.toAddHomClass`：∀ {F : Type u_10} {M : outParam (Type u
_11)} {N : outParam (Type u_12)} {inst : AddZero M} {inst_1 : AddZero N}   {inst
_2 : FunLike F M N} […
· 使用定理 `RingHomClass.toAddMonoidHomClass`：∀ {F : Type u_5} {α : outParam (Type u
_6)} {β : outParam (Type u_7)} {inst : NonAssocSemiring α}   {inst_1 : NonAssocS
emiring β} {inst_2 : F…
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `Submodule.smul_of_tower_mem`：smul_of_tower_mem [SMul S R] [SMul S M] [Is
ScalarTower S R M] (r : S) (h : x in p) : r • x in p
· 使用定理 `IsScalarTower.right`：∀ {R : Type u} {A : Type w} [inst : CommSemiring R]
 [inst_1 : Semiring A] [inst_2 : Algebra R A], IsScalarTower R A A
· 使用定理 `Set.Finite.of_injOn`：∀ {α : Type u} {β : Type v} {f : α → β} {s : Set α}
 {t : Set β}, Set.MapsTo f s t → Set.InjOn f s → t.Finite → s.Finite
（共 32 条，此处仅展示前 30 条）

--- 原说明 ---
Given a prime ideal `I` of `R⟦X⟧` such that `X ∉ I`, if `I.map constantCoeff` is
 generated by a
finite set `S : Set T`, then there exists `T : Set R`, of the same cardinality a
s `S`, that
generates `I`.
-/
theorem exist_eq_span_eq_ncard_of_X_notMem (hI : X ∉ I) {S : Set R}
    (hSI : span S = I.map constantCoeff) (hS : S.Finite) :
      ∃ T, I = span T ∧ T.Finite ∧ T.ncard = S.ncard := by
  have : SurjOn constantCoeff I S := by
    intro r hr
    simpa [mem_map_iff_of_surjective _ constantCoeff_surj, hSI] using subset_span hr
  obtain ⟨T, hTI, hinj, hT⟩ := this.exists_subset_injOn_image_eq
  refine ⟨T, eq_of_le_of_X_notMem_of_fg_of_isPrime (span_le.2  hTI) hI (Submodule.fg_def.2
    ⟨T, (hT ▸ hS).of_finite_image hinj, rfl⟩) ?_, Finite.of_injOn
    (fun f hf ↦ hT ▸ Set.mem_image_of_mem _ hf) hinj hS, hT ▸ hinj.ncard_image.symm⟩
  rw [map_le_iff_le_comap]
  intro f hf
  rw [mem_comap, mem_map_iff_of_surjective _ constantCoeff_surj]
  replace hf : constantCoeff f ∈ span S := hSI ▸ Ideal.mem_map_of_mem constantCoeff hf
  refine Submodule.span_induction (fun s hs ↦ ?_) ⟨0, zero_mem _, by simp⟩ ?_ ?_ hf
  · obtain ⟨t, htmem, ht⟩ := hT ▸ hs
    exact ⟨t, subset_span htmem, ht⟩
  · intro r₁ r₂ hr₁ hr₂ ⟨s₁, hs₁mem, hs₁⟩ ⟨s₂, hs₂mem, hs₂⟩
    exact ⟨s₁ + s₂, Ideal.add_mem _ hs₁mem hs₂mem, by simp [hs₁, hs₂]⟩
  · intro r₁ r₂ hr₁ ⟨f, hfmem, hf⟩
    exact ⟨r₁ • f, Submodule.smul_of_tower_mem _ _ hfmem, by simp [hf]⟩

open Submodule in
/-
**PowerSeries.spanFinrank_eq_spanFinrank_map_constantCoeff_of_X_notMem_of_fg_of_
isPrime** 是 Mathlib 中的一个定理，位于命名空间 `PowerSeries`。
形式化陈述：spanFinrank_eq_spanFinrank_map_constantCoeff_of_X_notMem_of_fg_of_isPrime 
(hI : X ∉ I) (hfg : I.FG) : spanFinrank I = spanFinrank (I.map constantCoeff)
参数：hI : X ∉ I；hfg : I.FG。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `PowerSeries.constantCoeff_surj`：constantCoeff_surj : Function.Surjective
 (constantCoeff (R
· 使用定理 `PowerSeries.exist_eq_span_eq_ncard_of_X_notMem`：exist_eq_span_eq_ncard_o
f_X_notMem (hI : X ∉ I) {S : Set R} (hSI : span S = I.map constantCoeff) (hS : S
.Finite) : exists T, I = span T ∧ T.…
· 使用引理 `Submodule.span_generators`：span_generators (p : Submodule R M) : span R 
(generators p) = p
· 使用定理 `Submodule.FG.finite_generators`：∀ {R : Type u_1} {M : Type u} [inst : Se
miring R] [inst_1 : AddCommMonoid M] [inst_2 : _root_.Module R M]   {p : Submodu
le R M}, p.FG → p.ge…
· 使用定理 `Submodule.FG.map`：∀ {R : Type u_1} {M : Type u_2} [inst : Semiring R] [i
nst_1 : AddCommMonoid M] [inst_2 : _root_.Module R M]   {S : Type u_3} {P : Type
 u_4} …
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Ideal.map_eq_submodule_map`：map_eq_submodule_map (f : R ->+* S) [h : Rin
gHomSurjective f] (I : Ideal R) : I.map f = Submodule.map f.toSemilinearMap I
· 使用引理 `le_trans`：le_trans : a <= b -> b <= c -> a <= c
· 使用引理 `Submodule.spanFinrank_span_le_ncard_of_finite`：spanFinrank_span_le_ncard
_of_finite {s : Set M} (hs : s.Finite) : (span R s).spanFinrank <= s.ncard
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Submodule.FG.generators_ncard`：∀ {R : Type u_1} {M : Type u} [inst : Sem
iring R] [inst_1 : AddCommMonoid M] [inst_2 : _root_.Module R M]   {p : Submodul
e R M}, p.FG → p.ge…
· 使用定理 `le_refl`：∀ {α : Type u_1} [inst : Preorder α] (a : α), a ≤ a
· 使用引理 `Ideal.spanFinrank_map_le_of_fg`：Ideal.spanFinrank_map_le_of_fg (f : R ->
+* T) {I : Ideal R} (hI : I.FG) : (I.map f).spanFinrank <= I.spanFinrank
-/
theorem spanFinrank_eq_spanFinrank_map_constantCoeff_of_X_notMem_of_fg_of_isPrime (hI : X ∉ I)
    (hfg : I.FG) : spanFinrank I = spanFinrank (I.map constantCoeff) := by
  refine le_antisymm ?_ ?_
  swap; · exact Ideal.spanFinrank_map_le_of_fg constantCoeff hfg
  have : RingHomSurjective (constantCoeff (R := R)) := ⟨constantCoeff_surj⟩
  obtain ⟨S, rfl, hS, hScard⟩ := exist_eq_span_eq_ncard_of_X_notMem hI
    (I.map constantCoeff).span_generators
    (FG.finite_generators <| map_eq_submodule_map constantCoeff I ▸ Submodule.FG.map _ hfg)
  refine le_trans (spanFinrank_span_le_ncard_of_finite hS) ?_
  rw [hScard, FG.generators_ncard]
  exact map_eq_submodule_map constantCoeff (Ideal.span S) ▸ Submodule.FG.map _ hfg

end X_notMem

variable {P : Ideal R⟦X⟧} [P.IsPrime]

open Submodule in
/-
**PowerSeries.spanFinrank_le_spanFinrank_map_constantCoeff_add_one_of_isPrime** 
是 Mathlib 中的一个定理，位于命名空间 `PowerSeries`。
形式化陈述：spanFinrank_le_spanFinrank_map_constantCoeff_add_one_of_isPrime : spanFinr
ank P <= spanFinrank (P.map constantCoeff) + 1
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `PowerSeries.spanFinrank_le_spanFinrank_map_constantCoeff_add_one_of_X_me
m`：spanFinrank_le_spanFinrank_map_constantCoeff_add_one_of_X_mem (hI : X in I) :
 spanFinrank I <= spanFinrank (I.map constantCoeff) + 1
· 使用引理 `le_trans`：le_trans : a <= b -> b <= c -> a <= c
· 使用定理 `Eq.le`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a = b → a ≤ b
· 使用定理 `PowerSeries.spanFinrank_eq_spanFinrank_map_constantCoeff_of_X_notMem_of_
fg_of_isPrime`：spanFinrank_eq_spanFinrank_map_constantCoeff_of_X_notMem_of_fg_of
_isPrime (hI : X ∉ I) (hfg : I.FG) : spanFinrank I = spanFinrank (I.map con…
· 使用定理 `Nat.le_succ`：∀ (n : ℕ), n ≤ n.succ
· 使用定理 `Nat.zero_le`：∀ (n : ℕ), 0 ≤ n
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `Submodule.spanFinrank_of_not_fg`：spanFinrank_of_not_fg {p : Submodule R 
M} (hp : ¬p.FG) : p.spanFinrank = 0
-/
theorem spanFinrank_le_spanFinrank_map_constantCoeff_add_one_of_isPrime :
    spanFinrank P ≤ spanFinrank (P.map constantCoeff) + 1 := by
  by_cases hfg : P.FG
  swap; · exact spanFinrank_of_not_fg hfg ▸ Nat.zero_le _
  by_cases hP : X ∈ P
  · exact spanFinrank_le_spanFinrank_map_constantCoeff_add_one_of_X_mem hP
  · exact le_trans (spanFinrank_eq_spanFinrank_map_constantCoeff_of_X_notMem_of_fg_of_isPrime
      hP hfg).le (Nat.le_succ _)

/-- A prime ideal `P` of `R⟦X⟧` is finitely generated if and only if `P.map constantCoeff` is
finitely generated. -/
/-
**PowerSeries.fg_iff_of_isPrime** 是 Mathlib 中的一个引理，位于命名空间 `PowerSeries`。
形式化陈述：fg_iff_of_isPrime : P.FG ↔ (P.map constantCoeff).FG
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Ideal.FG.map`：∀ {R : Type u_3} {S : Type u_4} [inst : Semiring R] [inst_
1 : Semiring S] {I : Ideal R},   I.FG → ∀ (f : R →+* S), (Ideal.map f I).FG
· 使用定理 `PowerSeries.eq_span_insert_X_of_X_mem_of_span_eq`：eq_span_insert_X_of_X_
mem_of_span_eq (hXI : X in I) (hSI : span S = I.map constantCoeff) : I = span (i
nsert X (C '' S))
· 使用定理 `Set.Finite.insert`：∀ {α : Type u} (a : α) {s : Set α}, s.Finite → (inser
t a s).Finite
· 使用定理 `Set.Finite.image`：∀ {α : Type u} {β : Type v} {s : Set α} (f : α → β), s
.Finite → (f '' s).Finite
· 使用定理 `Finset.finite_toSet`：finite_toSet (s : Finset α) : (s : Set α).Finite
· 使用定理 `CanLift.prf`：∀ {α : Sort u_1} {β : Sort u_2} {coe : outParam (β → α)} {c
ond : outParam (α → Prop)} [self : CanLift α β coe cond]   (x : α), cond x → ∃ y
,…
· 使用定理 `Set.instCanLiftFinsetCoeFinite`：∀ {α : Type u}, CanLift (Set α) (Finset 
α) SetLike.coe Set.Finite
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `PowerSeries.exist_eq_span_eq_ncard_of_X_notMem`：exist_eq_span_eq_ncard_o
f_X_notMem (hI : X ∉ I) {S : Set R} (hSI : span S = I.map constantCoeff) (hS : S
.Finite) : exists T, I = span T ∧ T.…

--- 原说明 ---
A prime ideal `P` of `R⟦X⟧` is finitely generated if and only if `P.map constant
Coeff` is
finitely generated.
-/
lemma fg_iff_of_isPrime : P.FG ↔ (P.map constantCoeff).FG := by
  constructor
  · exact (FG.map · _)
  · intro ⟨S, hS⟩
    by_cases hX : X ∈ P
    · have H := eq_span_insert_X_of_X_mem_of_span_eq hX hS
      have : (insert X <| (C (R := R)) '' S).Finite :=
        Finite.insert X <| Finite.image _ S.finite_toSet
      lift insert X <| (C (R := R)) '' S to Finset R⟦X⟧ using this with T hT
      exact ⟨T, hT ▸ H.symm⟩
    · obtain ⟨T, hT, hT₂, _⟩ := exist_eq_span_eq_ncard_of_X_notMem hX hS S.finite_toSet
      lift T to Finset R⟦X⟧ using hT₂
      exact ⟨T, hT.symm⟩

/-- If `R` is noetherian then so is `R⟦X⟧`. -/
/-
**PowerSeries.** 是 Mathlib 中的一个实例，位于命名空间 `PowerSeries`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If `R` is noetherian then so is `R⟦X⟧`.
-/
instance [IsNoetherianRing R] : IsNoetherianRing R⟦X⟧ :=
  IsNoetherianRing.of_prime fun P _ ↦
    fg_iff_of_isPrime.2 <| (isNoetherianRing_iff_ideal_fg R).1 inferInstance (P.map constantCoeff)

/-- If `R` is principal then `R⟦X⟧` is a `UniqueFactorizationMonoid`. Note that `R` being a
`UniqueFactorizationMonoid` is not enough. -/
/-
**PowerSeries.** 是 Mathlib 中的一个实例，位于命名空间 `PowerSeries`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If `R` is principal then `R⟦X⟧` is a `UniqueFactorizationMonoid`. Note that `R` 
being a
`UniqueFactorizationMonoid` is not enough.
-/
instance [IsPrincipalIdealRing R] [IsDomain R] : UniqueFactorizationMonoid R⟦X⟧ := by
  refine UniqueFactorizationMonoid.iff_exists_prime_mem_of_isPrime.mpr (fun P h₁ h₂ ↦ ?_)
  by_cases hXP : X ∈ P
  · exact ⟨X, hXP, X_prime⟩
  · obtain ⟨_, h⟩ := (IsPrincipalIdealRing.principal (P.map constantCoeff)).principal
    obtain ⟨_, rfl, _, h⟩ := exist_eq_span_eq_ncard_of_X_notMem hXP h.symm (finite_singleton _)
    simp only [ncard_singleton, ncard_eq_one] at h
    obtain ⟨_, rfl⟩ := h
    exact ⟨_, mem_span_singleton_self _,
      (span_singleton_prime (span_singleton_eq_bot.not.1 h₁)).1 h₂⟩

end PowerSeries

