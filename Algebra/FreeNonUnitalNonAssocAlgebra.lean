/-
Copyright (c) 2021 Oliver Nash. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Oliver Nash
-/
module

public import Mathlib.Algebra.Free
public import Mathlib.Algebra.MonoidAlgebra.Basic

/-!
# Free algebras

Given a semiring `R` and a type `X`, we construct the free non-unital, non-associative algebra on
`X` with coefficients in `R`, together with its universal property. The construction is valuable
because it can be used to build free algebras with more structure, e.g., free Lie algebras.

Note that elsewhere we have a construction of the free unital, associative algebra. This is called
`FreeAlgebra`.

## Main definitions

  * `FreeNonUnitalNonAssocAlgebra`
  * `FreeNonUnitalNonAssocAlgebra.lift`
  * `FreeNonUnitalNonAssocAlgebra.of`

## Implementation details

We construct the free algebra as the magma algebra, with coefficients in `R`, of the free magma on
`X`. However we regard this as an implementation detail and thus deliberately omit the lemmas
`of_apply` and `lift_apply`, and we mark `FreeNonUnitalNonAssocAlgebra` and `lift` as
irreducible once we have established the universal property.

## Tags

free algebra, non-unital, non-associative, free magma, magma algebra, universal property,
forgetful functor, adjoint functor
-/

@[expose] public noncomputable section

open scoped MonoidAlgebra

variable (R X A : Type*) [Semiring R]

/--
If `α` is a type, and `R` is a semiring, then `FreeNonUnitalNonAssocAlgebra R α` is the free
non-unital non-associative `R`-algebra generated by `α`.
This is an `R`-algebra equipped with a function
`FreeNonUnitalNonAssocAlgebra.of R : α → FreeNonUnitalNonAssocAlgebra R α` which has
the following universal property: if `A` is any `R`-algebra, and `f : α → A` is any function,
then this function is the composite of `FreeNonUnitalNonAssocAlgebra.of R`
and a unique non-unital `R`-algebra homomorphism
`FreeNonUnitalNonAssocAlgebra.lift R f : FreeNonUnitalNonAssocAlgebra R α →ₙₐ[R] A`.

A typical element of `FreeNonUnitalNonAssocAlgebra R α` is an `R`-linear combination of
nonempty formal (non-commutative, non-associative) products of elements of `α`.
For example if `x` and `y` are terms of type `α` and
`a`, `b` are terms of type `R` then `(3 * a * a) • (x * (y * x)) + (2 * b + 1) • (y * x)` is a
"typical" element of `FreeNonUnitalNonAssocAlgebra R α`.
-/
/-
**FreeNonUnitalNonAssocAlgebra** 是 Mathlib 中的一个缩写定义，位于命名空间 ``。
形式化陈述：FreeNonUnitalNonAssocAlgebra
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If `α` is a type, and `R` is a semiring, then `FreeNonUnitalNonAssocAlgebra R α`
 is the free
non-unital non-associative `R`-algebra generated by `α`.
This is an `R`-algebra equipped with a function
`FreeNonUnitalNonAssocAlgebra.of R : α → FreeNonUnitalNonAssocAlgebra R α` which
 has
the following universal property: if `A` is any `R`-algebra, and `f : α → A` is 
any function,
then this function is the composite of `FreeNonUnitalNonAssocAlgebra.of R`
and a unique non-unital `R`-algebra homomorphism
`FreeNonUnitalNonAssocAlgebra.lift R f : FreeNonUnitalNonAssocAlgebra R α →ₙₐ[R]
 A`.

A typical element of `FreeNonUnitalNonAssocAlgebra R α` is an `R`-linear combina
tion of
nonempty formal (non-commutative, non-associative) products of elements of `α`.
For example if `x` and `y` are terms of type `α` and
`a`, `b` are terms of type `R` then `(3 * a * a) • (x * (y * x)) + (2 * b + 1) •
 (y * x)` is a
"typical" element of `FreeNonUnitalNonAssocAlgebra R α`.
-/
abbrev FreeNonUnitalNonAssocAlgebra := R[FreeMagma X]

namespace FreeNonUnitalNonAssocAlgebra

variable {X A}

/-- The embedding of `X` into the free algebra with coefficients in `R`. -/
/-
**FreeNonUnitalNonAssocAlgebra.of** 是 Mathlib 中的一个定义，位于命名空间 `FreeNonUnitalNonAss
ocAlgebra`。
形式化陈述：of : X -> FreeNonUnitalNonAssocAlgebra R X
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The embedding of `X` into the free algebra with coefficients in `R`.
-/
def of : X → FreeNonUnitalNonAssocAlgebra R X :=
  MonoidAlgebra.ofMagma R _ ∘ FreeMagma.of

variable [NonUnitalNonAssocSemiring A] [Module R A] [IsScalarTower R A A] [SMulCommClass R A A]

/-- The functor `X ↦ FreeNonUnitalNonAssocAlgebra R X` from the category of types to the
category of non-unital, non-associative algebras over `R` is adjoint to the forgetful functor in the
other direction. -/
/-
**FreeNonUnitalNonAssocAlgebra.lift** 是 Mathlib 中的一个定义，位于命名空间 `FreeNonUnitalNonA
ssocAlgebra`。
形式化陈述：lift : (X -> A) ≃ (FreeNonUnitalNonAssocAlgebra R X ->ₙₐ[R] A)
该定义给出了上述对象。
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.trans`：Equiv.trans {s t u : Computation α} : s ~ t -> t ~ u -> s ~
 u

--- 原说明 ---
The functor `X ↦ FreeNonUnitalNonAssocAlgebra R X` from the category of types to
 the
category of non-unital, non-associative algebras over `R` is adjoint to the forg
etful functor in the
other direction.
-/
def lift : (X → A) ≃ (FreeNonUnitalNonAssocAlgebra R X →ₙₐ[R] A) :=
  FreeMagma.lift.trans (MonoidAlgebra.liftMagma R)

@[simp]
/-
**FreeNonUnitalNonAssocAlgebra.lift_symm_apply** 是 Mathlib 中的一个定理，位于命名空间 `FreeNo
nUnitalNonAssocAlgebra`。
形式化陈述：lift_symm_apply (F : FreeNonUnitalNonAssocAlgebra R X ->ₙₐ[R] A) : (lift R
).symm F = F ∘ of R
参数：F : FreeNonUnitalNonAssocAlgebra R X ->ₙₐ[R] A。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
-/
theorem lift_symm_apply (F : FreeNonUnitalNonAssocAlgebra R X →ₙₐ[R] A) :
    (lift R).symm F = F ∘ of R := rfl

@[simp]
/-
**FreeNonUnitalNonAssocAlgebra.of_comp_lift** 是 Mathlib 中的一个定理，位于命名空间 `FreeNonUn
italNonAssocAlgebra`。
形式化陈述：of_comp_lift (f : X -> A) : lift R f ∘ of R = f
参数：f : X -> A。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.left_inv`：∀ {α : Sort u_1} {β : Sort u_2} (self : α ≃ β), Function
.LeftInverse self.invFun self.toFun
-/
theorem of_comp_lift (f : X → A) : lift R f ∘ of R = f :=
  (lift R).left_inv f

@[simp]
/-
**FreeNonUnitalNonAssocAlgebra.lift_unique** 是 Mathlib 中的一个定理，位于命名空间 `FreeNonUni
talNonAssocAlgebra`。
形式化陈述：lift_unique (f : X -> A) (F : FreeNonUnitalNonAssocAlgebra R X ->ₙₐ[R] A) 
: F ∘ of R = f ↔ F = lift R f
参数：f : X -> A；F : FreeNonUnitalNonAssocAlgebra R X ->ₙₐ[R] A。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.symm_apply_eq`：symm_apply_eq {α β} (e : α ≃ β) {x y} : e.symm x = 
y ↔ x = e y
-/
theorem lift_unique (f : X → A) (F : FreeNonUnitalNonAssocAlgebra R X →ₙₐ[R] A) :
    F ∘ of R = f ↔ F = lift R f :=
  (lift R).symm_apply_eq

@[simp]
/-
**FreeNonUnitalNonAssocAlgebra.lift_of_apply** 是 Mathlib 中的一个定理，位于命名空间 `FreeNonU
nitalNonAssocAlgebra`。
形式化陈述：lift_of_apply (f : X -> A) (x) : lift R f (of R x) = f x
参数：f : X -> A；x。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congr_fun`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, f = g 
→ ∀ (a : α), f a = g a
· 使用定理 `FreeNonUnitalNonAssocAlgebra.of_comp_lift`：of_comp_lift (f : X -> A) : l
ift R f ∘ of R = f
-/
theorem lift_of_apply (f : X → A) (x) : lift R f (of R x) = f x :=
  congr_fun (of_comp_lift _ f) x

@[simp]
/-
**FreeNonUnitalNonAssocAlgebra.lift_comp_of** 是 Mathlib 中的一个定理，位于命名空间 `FreeNonUn
italNonAssocAlgebra`。
形式化陈述：lift_comp_of (F : FreeNonUnitalNonAssocAlgebra R X ->ₙₐ[R] A) : lift R (F 
∘ of R) = F
参数：F : FreeNonUnitalNonAssocAlgebra R X ->ₙₐ[R] A。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.apply_symm_apply`：∀ {α : Sort u} {β : Sort v} (e : α ≃ β) (x : β),
 e (e.symm x) = x
-/
theorem lift_comp_of (F : FreeNonUnitalNonAssocAlgebra R X →ₙₐ[R] A) : lift R (F ∘ of R) = F :=
  (lift R).apply_symm_apply F

@[ext]
/-
**FreeNonUnitalNonAssocAlgebra.hom_ext** 是 Mathlib 中的一个定理，位于命名空间 `FreeNonUnitalN
onAssocAlgebra`。
形式化陈述：hom_ext {F₁ F₂ : FreeNonUnitalNonAssocAlgebra R X ->ₙₐ[R] A} (h : forall x
, F₁ (of R x) = F₂ (of R x)) : F₁ = F₂
参数：h : forall x, F₁ (of R x) = F₂ (of R x)。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.injective`：∀ {α : Sort u} {β : Sort v} (e : α ≃ β), Function.Injec
tive ⇑e
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
-/
theorem hom_ext {F₁ F₂ : FreeNonUnitalNonAssocAlgebra R X →ₙₐ[R] A}
    (h : ∀ x, F₁ (of R x) = F₂ (of R x)) : F₁ = F₂ :=
  (lift R).symm.injective <| funext h

end FreeNonUnitalNonAssocAlgebra

