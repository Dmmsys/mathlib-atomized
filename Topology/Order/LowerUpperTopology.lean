/-
Copyright (c) 2023 Christopher Hoskin. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Christopher Hoskin
-/
module

public import Mathlib.Order.Hom.CompleteLattice
public import Mathlib.Topology.Homeomorph.Defs
public import Mathlib.Topology.Order.Lattice

/-!
# Lower and Upper topology

This file introduces the lower topology on a preorder as the topology generated by the complements
of the left-closed right-infinite intervals.

For completeness we also introduce the dual upper topology, generated by the complements of the
right-closed left-infinite intervals.

## Main statements

- `IsLower.t0Space` - the lower topology on a partial order is T₀
- `IsLower.isTopologicalBasis` - the complements of the upper closures of finite
  subsets form a basis for the lower topology
- `IsLower.continuousInf` - the inf map is continuous with respect to the lower topology

## Implementation notes

A type synonym `WithLower` is introduced and for a preorder `α`, `WithLower α`
is made an instance of `TopologicalSpace` by the topology generated by the complements of the
closed intervals to infinity.

We define a mixin class `IsLower` for the class of types which are both a preorder and a
topology and where the topology is generated by the complements of the closed intervals to infinity.
It is shown that `WithLower α` is an instance of `IsLower`.

Similarly for the upper topology.

## Motivation

The lower topology is used with the `Scott` topology to define the Lawson topology. The restriction
of the lower topology to the spectrum of a complete lattice coincides with the hull-kernel topology.

## References

* [Gierz et al, *A Compendium of Continuous Lattices*][GierzEtAl1980]

## Tags

lower topology, upper topology, preorder
-/

@[expose] public section

open Set TopologicalSpace Topology

namespace Topology

/--
The lower topology is the topology generated by the complements of the left-closed right-infinite
intervals.
-/
@[instance_reducible]
/-
**Topology.lower** 是 Mathlib 中的一个定义，位于命名空间 `Topology`。
形式化陈述：lower (α : Type*) [Preorder α] : TopologicalSpace α
参数：α : Type*。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The lower topology is the topology generated by the complements of the left-clos
ed right-infinite
intervals.
-/
def lower (α : Type*) [Preorder α] : TopologicalSpace α := generateFrom {s | ∃ a, (Ici a)ᶜ = s}

/--
The upper topology is the topology generated by the complements of the right-closed left-infinite
intervals.
-/
@[instance_reducible]
/-
**Topology.upper** 是 Mathlib 中的一个定义，位于命名空间 `Topology`。
形式化陈述：upper (α : Type*) [Preorder α] : TopologicalSpace α
参数：α : Type*。
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The upper topology is the topology generated by the complements of the right-clo
sed left-infinite
intervals.
-/
def upper (α : Type*) [Preorder α] : TopologicalSpace α := generateFrom {s | ∃ a, (Iic a)ᶜ = s}

/-- Type synonym for a preorder equipped with the lower set topology. -/
/-
**Topology.WithLower** 是 Mathlib 中的一个定义，位于命名空间 `Topology`。
形式化陈述：WithLower (α : Type*)
参数：α : Type*。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Type synonym for a preorder equipped with the lower set topology.
-/
def WithLower (α : Type*) := α

variable {α β : Type*}

namespace WithLower

/-- `toLower` is the identity function to the `WithLower` of a type. -/
/-
**Topology.WithLower.toLower** 是 Mathlib 中的一个定义，位于命名空间 `Topology.WithLower`。
形式化陈述：{α : Type u_1} → α ≃ Topology.WithLower α
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.refl`：Equiv.refl (s : Computation α) : s ~ s

--- 原说明 ---
`toLower` is the identity function to the `WithLower` of a type.
-/
@[match_pattern] def toLower : α ≃ WithLower α := Equiv.refl _

/-- `ofLower` is the identity function from the `WithLower` of a type. -/
/-
**Topology.WithLower.ofLower** 是 Mathlib 中的一个定义，位于命名空间 `Topology.WithLower`。
形式化陈述：{α : Type u_1} → Topology.WithLower α ≃ α
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.refl`：Equiv.refl (s : Computation α) : s ~ s

--- 原说明 ---
`ofLower` is the identity function from the `WithLower` of a type.
-/
@[match_pattern] def ofLower : WithLower α ≃ α := Equiv.refl _
/-
**Topology.WithLower.toLower_symm** 是 Mathlib 中的一个定理，位于命名空间 `Topology.WithLower`
。
形式化陈述：∀ {α : Type u_1}, Topology.WithLower.toLower.symm = Topology.WithLower.ofL
ower
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s

--- 原说明 ---
`ofLower` is the identity function from the `WithLower` of a type.
-/
@[simp] lemma toLower_symm : (@toLower α).symm = ofLower := rfl
/-
**Topology.WithLower.ofLower_symm** 是 Mathlib 中的一个定理，位于命名空间 `Topology.WithLower`
。
形式化陈述：∀ {α : Type u_1}, Topology.WithLower.ofLower.symm = Topology.WithLower.toL
ower
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s

--- 原说明 ---
`ofLower` is the identity function from the `WithLower` of a type.
-/
@[simp] lemma ofLower_symm : (@ofLower α).symm = toLower := rfl
/-
**Topology.WithLower.toLower_ofLower** 是 Mathlib 中的一个定理，位于命名空间 `Topology.WithLow
er`。
形式化陈述：∀ {α : Type u_1} (a : Topology.WithLower α), Topology.WithLower.toLower (T
opology.WithLower.ofLower a) = a
参数：a : Topology.WithLower α；Topology.WithLower.ofLower a。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`ofLower` is the identity function from the `WithLower` of a type.
-/
@[simp] lemma toLower_ofLower (a : WithLower α) : toLower (ofLower a) = a := rfl
/-
**Topology.WithLower.ofLower_toLower** 是 Mathlib 中的一个定理，位于命名空间 `Topology.WithLow
er`。
形式化陈述：∀ {α : Type u_1} (a : α), Topology.WithLower.ofLower (Topology.WithLower.t
oLower a) = a
参数：a : α；Topology.WithLower.toLower a。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`ofLower` is the identity function from the `WithLower` of a type.
-/
@[simp] lemma ofLower_toLower (a : α) : ofLower (toLower a) = a := rfl
/-
**Topology.WithLower.toLower_inj** 是 Mathlib 中的一个引理，位于命名空间 `Topology.WithLower`。
形式化陈述：toLower_inj {a b : α} : toLower a = toLower b ↔ a = b
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a

--- 原说明 ---
`ofLower` is the identity function from the `WithLower` of a type.
-/
lemma toLower_inj {a b : α} : toLower a = toLower b ↔ a = b := Iff.rfl
/-
**Topology.WithLower.ofLower_inj** 是 Mathlib 中的一个定理，位于命名空间 `Topology.WithLower`。
形式化陈述：ofLower_inj {a b : WithLower α} : ofLower a = ofLower b ↔ a = b
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem ofLower_inj {a b : WithLower α} : ofLower a = ofLower b ↔ a = b :=
  Iff.rfl

/-- A recursor for `WithLower`. Use as `induction x`. -/
@[elab_as_elim, cases_eliminator, induction_eliminator]
/-
**Topology.WithLower.rec** 是 Mathlib 中的一个定义，位于命名空间 `Topology.WithLower`。
形式化陈述：{α : Type u_1} →   {motive : Topology.WithLower α → Sort u_3} →     ((a : 
α) → motive (Topology.WithLower.toLower a)) → (a : Topology.WithLower α) → motiv
e a
参数：(a : α) → motive (Topology.WithLower.toLower a)；a : Topology.WithLower α。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A recursor for `WithLower`. Use as `induction x`.
-/
protected def rec {motive : WithLower α → Sort*} (toLower : ∀ a, motive (toLower a)) :
    ∀ a, motive a := fun a =>
  toLower (ofLower a)
/-
**Topology.WithLower.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.WithLower`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [Nonempty α] : Nonempty (WithLower α) := ‹Nonempty α›
/-
**Topology.WithLower.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.WithLower`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [Inhabited α] : Inhabited (WithLower α) := ‹Inhabited α›

section Preorder

variable [Preorder α] {s : Set α}

/-
**Topology.WithLower.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.WithLower`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : Preorder (WithLower α) := ‹Preorder α›
/-
**Topology.WithLower.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.WithLower`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : TopologicalSpace (WithLower α) := lower (WithLower α)
/-
**Topology.WithLower.toLower_le_toLower** 是 Mathlib 中的一个定理，位于命名空间 `Topology.With
Lower`。
形式化陈述：∀ {α : Type u_1} [inst : Preorder α] {x y : α}, Topology.WithLower.toLower
 x ≤ Topology.WithLower.toLower y ↔ x ≤ y
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
@[simp] lemma toLower_le_toLower {x y : α} : toLower x ≤ toLower y ↔ x ≤ y := .rfl
/-
**Topology.WithLower.toLower_lt_toLower** 是 Mathlib 中的一个定理，位于命名空间 `Topology.With
Lower`。
形式化陈述：∀ {α : Type u_1} [inst : Preorder α] {x y : α}, Topology.WithLower.toLower
 x < Topology.WithLower.toLower y ↔ x < y
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
@[simp] lemma toLower_lt_toLower {x y : α} : toLower x < toLower y ↔ x < y := .rfl
/-
**Topology.WithLower.ofLower_le_ofLower** 是 Mathlib 中的一个定理，位于命名空间 `Topology.With
Lower`。
形式化陈述：∀ {α : Type u_1} [inst : Preorder α] {x y : Topology.WithLower α},   Topol
ogy.WithLower.ofLower x ≤ Topology.WithLower.ofLower y ↔ x ≤ y
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
@[simp] lemma ofLower_le_ofLower {x y : WithLower α} : ofLower x ≤ ofLower y ↔ x ≤ y := .rfl
/-
**Topology.WithLower.ofLower_lt_ofLower** 是 Mathlib 中的一个定理，位于命名空间 `Topology.With
Lower`。
形式化陈述：∀ {α : Type u_1} [inst : Preorder α] {x y : Topology.WithLower α},   Topol
ogy.WithLower.ofLower x < Topology.WithLower.ofLower y ↔ x < y
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
@[simp] lemma ofLower_lt_ofLower {x y : WithLower α} : ofLower x < ofLower y ↔ x < y := .rfl
/-
**Topology.WithLower.isOpen_preimage_ofLower** 是 Mathlib 中的一个引理，位于命名空间 `Topology
.WithLower`。
形式化陈述：isOpen_preimage_ofLower : IsOpen (ofLower ⁻¹' s) ↔ IsOpen[lower α] s
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma isOpen_preimage_ofLower : IsOpen (ofLower ⁻¹' s) ↔ IsOpen[lower α] s := Iff.rfl
/-
**Topology.WithLower.isOpen_def** 是 Mathlib 中的一个引理，位于命名空间 `Topology.WithLower`。
形式化陈述：isOpen_def (T : Set (WithLower α)) : IsOpen T ↔ IsOpen[lower α] (WithLower
.toLower ⁻¹' T)
参数：T : Set (WithLower α)。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma isOpen_def (T : Set (WithLower α)) : IsOpen T ↔ IsOpen[lower α] (WithLower.toLower ⁻¹' T) :=
  Iff.rfl
/-
**Topology.WithLower.continuous_toLower** 是 Mathlib 中的一个定理，位于命名空间 `Topology.With
Lower`。
形式化陈述：continuous_toLower [TopologicalSpace α] [ClosedIciTopology α] : Continuous
 (toLower : α -> WithLower α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用引理 `continuous_generateFrom_iff`：continuous_generateFrom_iff {t : Topologica
lSpace α} {b : Set (Set β)} : Continuous[t, generateFrom b] f ↔ forall s in b, I
sOpen (f ⁻¹' s)
· 使用定理 `IsClosed.isOpen_compl`：∀ {X : Type u} {inst : TopologicalSpace X} {s : S
et X} [self : IsClosed s], IsOpen sᶜ
· 使用定理 `isClosed_Ici`：∀ {α : Type u} [inst : TopologicalSpace α] [inst_1 : Preor
der α] [ClosedIciTopology α] {a : α}, IsClosed (Set.Ici a)
-/
theorem continuous_toLower [TopologicalSpace α] [ClosedIciTopology α] :
    Continuous (toLower : α → WithLower α) :=
  continuous_generateFrom_iff.mpr <| by rintro _ ⟨a, rfl⟩; exact isClosed_Ici.isOpen_compl

end Preorder

/-
**Topology.WithLower.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.WithLower`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [PartialOrder α] : PartialOrder (WithLower α) := ‹PartialOrder α›
/-
**Topology.WithLower.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.WithLower`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [LinearOrder α] : LinearOrder (WithLower α) := ‹LinearOrder α›

end WithLower

/-- Type synonym for a preorder equipped with the upper topology. -/
/-
**Topology.WithUpper** 是 Mathlib 中的一个定义，位于命名空间 `Topology`。
形式化陈述：WithUpper (α : Type*)
参数：α : Type*。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Type synonym for a preorder equipped with the upper topology.
-/
def WithUpper (α : Type*) := α
namespace WithUpper

/-- `toUpper` is the identity function to the `WithUpper` of a type. -/
/-
**Topology.WithUpper.toUpper** 是 Mathlib 中的一个定义，位于命名空间 `Topology.WithUpper`。
形式化陈述：{α : Type u_1} → α ≃ Topology.WithUpper α
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.refl`：Equiv.refl (s : Computation α) : s ~ s

--- 原说明 ---
`toUpper` is the identity function to the `WithUpper` of a type.
-/
@[match_pattern] def toUpper : α ≃ WithUpper α := Equiv.refl _

/-- `ofUpper` is the identity function from the `WithUpper` of a type. -/
/-
**Topology.WithUpper.ofUpper** 是 Mathlib 中的一个定义，位于命名空间 `Topology.WithUpper`。
形式化陈述：{α : Type u_1} → Topology.WithUpper α ≃ α
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.refl`：Equiv.refl (s : Computation α) : s ~ s

--- 原说明 ---
`ofUpper` is the identity function from the `WithUpper` of a type.
-/
@[match_pattern] def ofUpper : WithUpper α ≃ α := Equiv.refl _
/-
**Topology.WithUpper.toUpper_symm** 是 Mathlib 中的一个定理，位于命名空间 `Topology.WithUpper`
。
形式化陈述：∀ {α : Type u_3}, Topology.WithUpper.toUpper.symm = Topology.WithUpper.ofU
pper
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s

--- 原说明 ---
`ofUpper` is the identity function from the `WithUpper` of a type.
-/
@[simp] lemma toUpper_symm {α} : (@toUpper α).symm = ofUpper := rfl
/-
**Topology.WithUpper.ofUpper_symm** 是 Mathlib 中的一个定理，位于命名空间 `Topology.WithUpper`
。
形式化陈述：∀ {α : Type u_1}, Topology.WithUpper.ofUpper.symm = Topology.WithUpper.toU
pper
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Equiv.symm`：Equiv.symm {s t : Computation α} : s ~ t -> t ~ s

--- 原说明 ---
`ofUpper` is the identity function from the `WithUpper` of a type.
-/
@[simp] lemma ofUpper_symm : (@ofUpper α).symm = toUpper := rfl
/-
**Topology.WithUpper.toUpper_ofUpper** 是 Mathlib 中的一个定理，位于命名空间 `Topology.WithUpp
er`。
形式化陈述：∀ {α : Type u_1} (a : Topology.WithUpper α), Topology.WithUpper.toUpper (T
opology.WithUpper.ofUpper a) = a
参数：a : Topology.WithUpper α；Topology.WithUpper.ofUpper a。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`ofUpper` is the identity function from the `WithUpper` of a type.
-/
@[simp] lemma toUpper_ofUpper (a : WithUpper α) : toUpper (ofUpper a) = a := rfl
/-
**Topology.WithUpper.ofUpper_toUpper** 是 Mathlib 中的一个定理，位于命名空间 `Topology.WithUpp
er`。
形式化陈述：∀ {α : Type u_1} (a : α), Topology.WithUpper.ofUpper (Topology.WithUpper.t
oUpper a) = a
参数：a : α；Topology.WithUpper.toUpper a。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
`ofUpper` is the identity function from the `WithUpper` of a type.
-/
@[simp] lemma ofUpper_toUpper (a : α) : ofUpper (toUpper a) = a := rfl
/-
**Topology.WithUpper.toUpper_inj** 是 Mathlib 中的一个引理，位于命名空间 `Topology.WithUpper`。
形式化陈述：toUpper_inj {a b : α} : toUpper a = toUpper b ↔ a = b
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a

--- 原说明 ---
`ofUpper` is the identity function from the `WithUpper` of a type.
-/
lemma toUpper_inj {a b : α} : toUpper a = toUpper b ↔ a = b := Iff.rfl
/-
**Topology.WithUpper.ofUpper_inj** 是 Mathlib 中的一个引理，位于命名空间 `Topology.WithUpper`。
形式化陈述：ofUpper_inj {a b : WithUpper α} : ofUpper a = ofUpper b ↔ a = b
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma ofUpper_inj {a b : WithUpper α} : ofUpper a = ofUpper b ↔ a = b := Iff.rfl

/-- A recursor for `WithUpper`. Use as `induction x`. -/
@[elab_as_elim, cases_eliminator, induction_eliminator]
/-
**Topology.WithUpper.rec** 是 Mathlib 中的一个定义，位于命名空间 `Topology.WithUpper`。
形式化陈述：{α : Type u_1} →   {motive : Topology.WithUpper α → Sort u_3} →     ((a : 
α) → motive (Topology.WithUpper.toUpper a)) → (a : Topology.WithUpper α) → motiv
e a
参数：(a : α) → motive (Topology.WithUpper.toUpper a)；a : Topology.WithUpper α。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
A recursor for `WithUpper`. Use as `induction x`.
-/
protected def rec {motive : WithUpper α → Sort*} (toUpper : ∀ a, motive (toUpper a)) :
    ∀ a, motive a := fun a =>
  toUpper (ofUpper a)
/-
**Topology.WithUpper.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.WithUpper`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [Nonempty α] : Nonempty (WithUpper α) := ‹Nonempty α›
/-
**Topology.WithUpper.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.WithUpper`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [Inhabited α] : Inhabited (WithUpper α) := ‹Inhabited α›

section Preorder

variable [Preorder α] {s : Set α}

/-
**Topology.WithUpper.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.WithUpper`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : Preorder (WithUpper α) := ‹Preorder α›
/-
**Topology.WithUpper.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.WithUpper`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance : TopologicalSpace (WithUpper α) := upper (WithUpper α)
/-
**Topology.WithUpper.toUpper_le_toUpper** 是 Mathlib 中的一个定理，位于命名空间 `Topology.With
Upper`。
形式化陈述：∀ {α : Type u_1} [inst : Preorder α] {x y : α}, Topology.WithUpper.toUpper
 x ≤ Topology.WithUpper.toUpper y ↔ x ≤ y
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
@[simp] lemma toUpper_le_toUpper {x y : α} : toUpper x ≤ toUpper y ↔ x ≤ y := .rfl
/-
**Topology.WithUpper.toUpper_lt_toUpper** 是 Mathlib 中的一个定理，位于命名空间 `Topology.With
Upper`。
形式化陈述：∀ {α : Type u_1} [inst : Preorder α] {x y : α}, Topology.WithUpper.toUpper
 x < Topology.WithUpper.toUpper y ↔ x < y
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
@[simp] lemma toUpper_lt_toUpper {x y : α} : toUpper x < toUpper y ↔ x < y := .rfl
/-
**Topology.WithUpper.ofUpper_le_ofUpper** 是 Mathlib 中的一个定理，位于命名空间 `Topology.With
Upper`。
形式化陈述：∀ {α : Type u_1} [inst : Preorder α] {x y : Topology.WithUpper α},   Topol
ogy.WithUpper.ofUpper x ≤ Topology.WithUpper.ofUpper y ↔ x ≤ y
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
@[simp] lemma ofUpper_le_ofUpper {x y : WithUpper α} : ofUpper x ≤ ofUpper y ↔ x ≤ y := .rfl
/-
**Topology.WithUpper.ofUpper_lt_ofUpper** 是 Mathlib 中的一个定理，位于命名空间 `Topology.With
Upper`。
形式化陈述：∀ {α : Type u_1} [inst : Preorder α] {x y : Topology.WithUpper α},   Topol
ogy.WithUpper.ofUpper x < Topology.WithUpper.ofUpper y ↔ x < y
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
@[simp] lemma ofUpper_lt_ofUpper {x y : WithUpper α} : ofUpper x < ofUpper y ↔ x < y := .rfl
/-
**Topology.WithUpper.isOpen_preimage_ofUpper** 是 Mathlib 中的一个引理，位于命名空间 `Topology
.WithUpper`。
形式化陈述：isOpen_preimage_ofUpper : IsOpen (ofUpper ⁻¹' s) ↔ (upper α).IsOpen s
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma isOpen_preimage_ofUpper : IsOpen (ofUpper ⁻¹' s) ↔ (upper α).IsOpen s := Iff.rfl
/-
**Topology.WithUpper.isOpen_def** 是 Mathlib 中的一个引理，位于命名空间 `Topology.WithUpper`。
形式化陈述：isOpen_def {s : Set (WithUpper α)} : IsOpen s ↔ (upper α).IsOpen (toUpper 
⁻¹' s)
参数：WithUpper α。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
lemma isOpen_def {s : Set (WithUpper α)} : IsOpen s ↔ (upper α).IsOpen (toUpper ⁻¹' s) := Iff.rfl
/-
**Topology.WithUpper.continuous_toUpper** 是 Mathlib 中的一个定理，位于命名空间 `Topology.With
Upper`。
形式化陈述：continuous_toUpper [TopologicalSpace α] [ClosedIicTopology α] : Continuous
 (toUpper : α -> WithUpper α)
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用引理 `continuous_generateFrom_iff`：continuous_generateFrom_iff {t : Topologica
lSpace α} {b : Set (Set β)} : Continuous[t, generateFrom b] f ↔ forall s in b, I
sOpen (f ⁻¹' s)
· 使用定理 `IsClosed.isOpen_compl`：∀ {X : Type u} {inst : TopologicalSpace X} {s : S
et X} [self : IsClosed s], IsOpen sᶜ
· 使用定理 `isClosed_Iic`：isClosed_Iic : IsClosed (Iic a)
-/
theorem continuous_toUpper [TopologicalSpace α] [ClosedIicTopology α] :
    Continuous (toUpper : α → WithUpper α) :=
  continuous_generateFrom_iff.mpr <| by rintro _ ⟨a, rfl⟩; exact isClosed_Iic.isOpen_compl

end Preorder

/-
**Topology.WithUpper.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.WithUpper`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [PartialOrder α] : PartialOrder (WithUpper α) := ‹PartialOrder α›
/-
**Topology.WithUpper.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.WithUpper`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [LinearOrder α] : LinearOrder (WithUpper α) := ‹LinearOrder α›

end WithUpper

/--
The lower topology is the topology generated by the complements of the left-closed right-infinite
intervals.
-/
/-
**Topology.IsLower** 是 Mathlib 中的一个归纳类型，位于命名空间 `Topology`。
形式化陈述：(α : Type u_3) → [t : TopologicalSpace α] → [Preorder α] → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The lower topology is the topology generated by the complements of the left-clos
ed right-infinite
intervals.
-/
class IsLower (α : Type*) [t : TopologicalSpace α] [Preorder α] : Prop where
  topology_eq_lowerTopology : t = lower α

attribute [nolint docBlame] IsLower.topology_eq_lowerTopology

/--
The upper topology is the topology generated by the complements of the right-closed left-infinite
intervals.
-/
/-
**Topology.IsUpper** 是 Mathlib 中的一个归纳类型，位于命名空间 `Topology`。
形式化陈述：(α : Type u_3) → [t : TopologicalSpace α] → [Preorder α] → Prop
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The upper topology is the topology generated by the complements of the right-clo
sed left-infinite
intervals.
-/
class IsUpper (α : Type*) [t : TopologicalSpace α] [Preorder α] : Prop where
  topology_eq_upperTopology : t = upper α
attribute [nolint docBlame] IsUpper.topology_eq_upperTopology
/-
**Topology.** 是 Mathlib 中的一个实例，位于命名空间 `Topology`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [Preorder α] : IsLower (WithLower α) := ⟨rfl⟩
/-
**Topology.** 是 Mathlib 中的一个实例，位于命名空间 `Topology`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance [Preorder α] : IsUpper (WithUpper α) := ⟨rfl⟩

/--
The lower topology is homeomorphic to the upper topology on the dual order
-/
/-
**Topology.WithLower.toDualHomeomorph** 是 Mathlib 中的一个定义，位于命名空间 `Topology.WithLo
wer`。
形式化陈述：{α : Type u_1} → [inst : Preorder α] → Topology.WithLower α ≃ₜ Topology.Wi
thUpper αᵒᵈ
本定义的构造引用了以下数学事实（定理与引理）：
· 使用定理 `OrderDual.toDual_ofDual`：∀ {α : Type u_1} (a : αᵒᵈ), OrderDual.toDual (O
rderDual.ofDual a) = a

--- 原说明 ---
The lower topology is homeomorphic to the upper topology on the dual order
-/
def WithLower.toDualHomeomorph [Preorder α] : WithLower α ≃ₜ WithUpper αᵒᵈ where
  toFun := OrderDual.toDual
  invFun := OrderDual.ofDual
  left_inv := OrderDual.toDual_ofDual
  right_inv := OrderDual.ofDual_toDual
  continuous_toFun := continuous_coinduced_rng
  continuous_invFun := continuous_coinduced_rng

namespace IsLower

/-- The complements of the upper closures of finite sets are a collection of lower sets
which form a basis for the lower topology. -/
/-
**Topology.IsLower.lowerBasis** 是 Mathlib 中的一个定义，位于命名空间 `Topology.IsLower`。
形式化陈述：lowerBasis (α : Type*) [Preorder α]
参数：α : Type*。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The complements of the upper closures of finite sets are a collection of lower s
ets
which form a basis for the lower topology.
-/
def lowerBasis (α : Type*) [Preorder α] :=
  { s : Set α | ∃ t : Set α, t.Finite ∧ (upperClosure t : Set α)ᶜ = s }

section Preorder

variable (α)
variable [Preorder α] [TopologicalSpace α] [IsLower α] {s : Set α}

/-
**Topology.IsLower.topology_eq** 是 Mathlib 中的一个引理，位于命名空间 `Topology.IsLower`。
形式化陈述：topology_eq : ‹_› = lower α
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Topology.IsLower.topology_eq_lowerTopology`：∀ {α : Type u_3} {t : Topolo
gicalSpace α} {inst : Preorder α} [self : Topology.IsLower α], t = Topology.lowe
r α
-/
lemma topology_eq : ‹_› = lower α := topology_eq_lowerTopology

variable {α}

/-- If `α` is equipped with the lower topology, then it is homeomorphic to `WithLower α`.
-/
/-
**Topology.IsLower.withLowerHomeomorph** 是 Mathlib 中的一个定义，位于命名空间 `Topology.IsLow
er`。
形式化陈述：withLowerHomeomorph : WithLower α ≃ₜ α
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If `α` is equipped with the lower topology, then it is homeomorphic to `WithLowe
r α`.
-/
def withLowerHomeomorph : WithLower α ≃ₜ α :=
  WithLower.ofLower.toHomeomorphOfIsInducing ⟨topology_eq α ▸ induced_id.symm⟩
/-
**Topology.IsLower.isOpen_iff_generate_Ici_compl** 是 Mathlib 中的一个定理，位于命名空间 `Topo
logy.IsLower`。
形式化陈述：isOpen_iff_generate_Ici_compl : IsOpen s ↔ GenerateOpen { t | exists a, (I
ci a)ᶜ = t } s
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `Topology.IsLower.topology_eq`：topology_eq : ‹_› = lower α
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem isOpen_iff_generate_Ici_compl : IsOpen s ↔ GenerateOpen { t | ∃ a, (Ici a)ᶜ = t } s := by
  rw [topology_eq α]; rfl
/-
**Topology.IsLower._root_.OrderDual.instIsUpper** 是 Mathlib 中的一个实例，位于命名空间 `Topol
ogy.IsLower`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance _root_.OrderDual.instIsUpper : IsUpper αᵒᵈ where
  topology_eq_upperTopology := topology_eq_lowerTopology (α := α)

/-- Left-closed right-infinite intervals $[a, ∞)$ are closed in the lower topology. -/
/-
**Topology.IsLower.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.IsLower`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Left-closed right-infinite intervals $[a, ∞)$ are closed in the lower topology.
-/
instance : ClosedIciTopology α :=
  ⟨fun a ↦ isOpen_compl_iff.1 <| isOpen_iff_generate_Ici_compl.2 <| GenerateOpen.basic _ ⟨a, rfl⟩⟩

/-- The upper closure of a finite set is closed in the lower topology. -/
/-
**Topology.IsLower.isClosed_upperClosure** 是 Mathlib 中的一个定理，位于命名空间 `Topology.IsL
ower`。
形式化陈述：isClosed_upperClosure (h : s.Finite) : IsClosed (upperClosure s : Set α)
参数：h : s.Finite。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `UpperSet.coe_iInf`：coe_iInf (f : ι -> UpperSet α) : (↑(⨅ i, f i) : Set α
) = ⋃ i, f i
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Set.Finite.isClosed_biUnion`：Set.Finite.isClosed_biUnion {s : Set α} {f 
: α -> Set X} (hs : s.Finite) (h : forall i in s, IsClosed (f i)) : IsClosed (⋃ 
i in s, f i)
· 使用定理 `isClosed_Ici`：∀ {α : Type u} [inst : TopologicalSpace α] [inst_1 : Preor
der α] [ClosedIciTopology α] {a : α}, IsClosed (Set.Ici a)
· 使用定理 `Topology.IsLower.instClosedIciTopology`：∀ {α : Type u_1} [inst : Preorde
r α] [inst_1 : TopologicalSpace α] [Topology.IsLower α], ClosedIciTopology α

--- 原说明 ---
The upper closure of a finite set is closed in the lower topology.
-/
theorem isClosed_upperClosure (h : s.Finite) : IsClosed (upperClosure s : Set α) := by
  simp only [← UpperSet.iInf_Ici, UpperSet.coe_iInf]
  exact h.isClosed_biUnion fun _ _ => isClosed_Ici

/-- Every set open in the lower topology is a lower set. -/
/-
**Topology.IsLower.isLowerSet_of_isOpen** 是 Mathlib 中的一个定理，位于命名空间 `Topology.IsLo
wer`。
形式化陈述：isLowerSet_of_isOpen (h : IsOpen s) : IsLowerSet s
参数：h : IsOpen s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Topology.IsLower.isOpen_iff_generate_Ici_compl`：isOpen_iff_generate_Ici_
compl : IsOpen s ↔ GenerateOpen { t | exists a, (Ici a)ᶜ = t } s
· 使用定理 `IsUpperSet.compl`：IsUpperSet.compl (hs : IsUpperSet s) : IsLowerSet sᶜ
· 使用定理 `isUpperSet_Ici`：∀ {α : Type u_1} [inst : Preorder α] (a : α), IsUpperSet
 (Set.Ici a)
· 使用定理 `isLowerSet_univ`：∀ {α : Type u_1} [inst : LE α], IsLowerSet Set.univ
· 使用定理 `IsLowerSet.inter`：∀ {α : Type u_1} [inst : LE α] {s t : Set α}, IsLowerS
et s → IsLowerSet t → IsLowerSet (s ∩ t)
· 使用定理 `isLowerSet_sUnion`：∀ {α : Type u_1} [inst : LE α] {S : Set (Set α)}, (∀ 
s ∈ S, IsLowerSet s) → IsLowerSet (⋃₀ S)

--- 原说明 ---
Every set open in the lower topology is a lower set.
-/
theorem isLowerSet_of_isOpen (h : IsOpen s) : IsLowerSet s := by
  replace h := isOpen_iff_generate_Ici_compl.1 h
  induction h with
  | basic u h' => obtain ⟨a, rfl⟩ := h'; exact (isUpperSet_Ici a).compl
  | univ => exact isLowerSet_univ
  | inter u v _ _ hu2 hv2 => exact hu2.inter hv2
  | sUnion _ _ ih => exact isLowerSet_sUnion ih
/-
**Topology.IsLower.isUpperSet_of_isClosed** 是 Mathlib 中的一个定理，位于命名空间 `Topology.Is
Lower`。
形式化陈述：isUpperSet_of_isClosed (h : IsClosed s) : IsUpperSet s
参数：h : IsClosed s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `isLowerSet_compl`：∀ {α : Type u_1} [inst : LE α] {s : Set α}, IsLowerSet
 sᶜ ↔ IsUpperSet s
· 使用定理 `Topology.IsLower.isLowerSet_of_isOpen`：isLowerSet_of_isOpen (h : IsOpen 
s) : IsLowerSet s
· 使用定理 `IsClosed.isOpen_compl`：∀ {X : Type u} {inst : TopologicalSpace X} {s : S
et X} [self : IsClosed s], IsOpen sᶜ
-/
theorem isUpperSet_of_isClosed (h : IsClosed s) : IsUpperSet s :=
  isLowerSet_compl.1 <| isLowerSet_of_isOpen h.isOpen_compl
/-
**Topology.IsLower.tendsto_nhds_iff_not_le** 是 Mathlib 中的一个定理，位于命名空间 `Topology.I
sLower`。
形式化陈述：tendsto_nhds_iff_not_le {β : Type*} {f : β -> α} {l : Filter β} {x : α} : 
Filter.Tendsto f l (𝓝 x) ↔ forall y, ¬y <= x -> forallᶠ z in l, ¬y <= f z
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `Topology.IsLower.topology_eq_lowerTopology`：∀ {α : Type u_3} {t : Topolo
gicalSpace α} {inst : Preorder α} [self : Topology.IsLower α], t = Topology.lowe
r α
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
theorem tendsto_nhds_iff_not_le {β : Type*} {f : β → α} {l : Filter β} {x : α} :
    Filter.Tendsto f l (𝓝 x) ↔ ∀ y, ¬y ≤ x → ∀ᶠ z in l, ¬y ≤ f z := by
  simp +instances [topology_eq_lowerTopology, tendsto_nhds_generateFrom_iff, Filter.Eventually, Ici,
    compl_ofPred]

/--
The closure of a singleton `{a}` in the lower topology is the left-closed right-infinite interval
$[a, ∞)$.
-/
@[simp]
/-
**Topology.IsLower.closure_singleton** 是 Mathlib 中的一个定理，位于命名空间 `Topology.IsLower
`。
形式化陈述：closure_singleton (a : α) : closure {a} = Ici a
参数：a : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Set.Subset.antisymm`：∀ {α : Type u} {a b : Set α}, a ⊆ b → b ⊆ a → a = b
· 使用定理 `closure_minimal`：closure_minimal (h₁ : s subseteq t) (h₂ : IsClosed t) :
 closure s subseteq t
· 使用定理 `Eq.ge`：∀ {α : Type u_1} [inst : Preorder α] {a b : α}, a = b → b ≤ a
· 使用定理 `isClosed_Ici`：∀ {α : Type u} [inst : TopologicalSpace α] [inst_1 : Preor
der α] [ClosedIciTopology α] {a : α}, IsClosed (Set.Ici a)
· 使用定理 `Topology.IsLower.instClosedIciTopology`：∀ {α : Type u_1} [inst : Preorde
r α] [inst_1 : TopologicalSpace α] [Topology.IsLower α], ClosedIciTopology α
· 使用定理 `IsUpperSet.Ici_subset`：∀ {α : Type u_1} [inst : Preorder α] {s : Set α},
 IsUpperSet s → ∀ ⦃a : α⦄, a ∈ s → Set.Ici a ⊆ s
· 使用定理 `Topology.IsLower.isUpperSet_of_isClosed`：isUpperSet_of_isClosed (h : IsC
losed s) : IsUpperSet s
· 使用定理 `isClosed_closure`：isClosed_closure : IsClosed (closure s)
· 使用定理 `subset_closure`：subset_closure : s subseteq closure s

--- 原说明 ---
The closure of a singleton `{a}` in the lower topology is the left-closed right-
infinite interval
$[a, ∞)$.
-/
theorem closure_singleton (a : α) : closure {a} = Ici a :=
  Subset.antisymm ((closure_minimal fun _ h => h.ge) <| isClosed_Ici) <|
    (isUpperSet_of_isClosed isClosed_closure).Ici_subset <| subset_closure rfl
/-
**Topology.IsLower.isTopologicalBasis** 是 Mathlib 中的一个定理，位于命名空间 `Topology.IsLowe
r`。
形式化陈述：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : TopologicalSpace α] [Topolo
gy.IsLower α],   TopologicalSpace.IsTopologicalBasis (Topology.IsLower.lowerBasi
s α)
参数：Topology.IsLower.lowerBasis α。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `eq_of_heq`：∀ {α : Sort u} {a a' : α}, a ≍ a' → a = a'
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `coe_upperClosure`：coe_upperClosure (s : Set α) : ↑(upperClosure s) = ⋃ a
 in s, Ici a
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Set.compl_iUnion`：compl_iUnion (s : ι -> Set β) : (⋃ i, s i)ᶜ = ⋂ i, (s 
i)ᶜ
· 使用定理 `Set.ext`：ext {a b : Set α} (h : forall (x : α), x in a ↔ x in b) : a = b
· 使用定理 `Set.Finite.image`：∀ {α : Type u} {β : Type v} {s : Set α} (f : α → β), s
.Finite → (f '' s).Finite
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Set.image_subset_iff`：image_subset_iff {s : Set α} {t : Set β} {f : α ->
 β} : f '' s subseteq t ↔ s subseteq f ⁻¹' t
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Set.sInter_image`：sInter_image (f : α -> Set β) (s : Set α) : ⋂₀ (f '' s
) = ⋂ a in s, f a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Set.Finite.to_subtype`：∀ {α : Type u} {s : Set α}, s.Finite → Finite ↑s
· 使用定理 `Set.finite_range`：finite_range (f : ι -> α) [Finite ι] : (range f).Finit
e
· 使用定理 `Set.biInter_range`：biInter_range {f : ι -> α} {g : α -> Set β} : ⋂ x in 
range f, g x = ⋂ y, g (f y)
· 使用定理 `Set.sInter_eq_iInter`：sInter_eq_iInter {s : Set (Set α)} : ⋂₀ s = ⋂ i : 
s, i
· 使用定理 `Subtype.forall'`：∀ {α : Sort u_1} {p : α → Prop} {q : (x : α) → p x → Pr
op}, (∀ (x : α) (h : p x), q x h) ↔ ∀ (x : { a // p a }), q ↑x ⋯
· 使用定理 `Set.subset_def`：subset_def : (s subseteq t) = forall x, x in s -> x in t
· 使用定理 `Classical.choose_spec`：∀ {α : Sort u} {p : α → Prop} (h : ∃ x, p x), p (
Classical.choose h)
· 使用定理 `TopologicalSpace.isTopologicalBasis_of_subbasis`：isTopologicalBasis_of_s
ubbasis {s : Set (Set α)} (hs : t = generateFrom s) : IsTopologicalBasis ((fun f
 => ⋂₀ f) '' { f : Set (Set α) | f.Fi…
· 使用引理 `Topology.IsLower.topology_eq`：topology_eq : ‹_› = lower α
-/
protected theorem isTopologicalBasis : IsTopologicalBasis (lowerBasis α) := by
  convert! isTopologicalBasis_of_subbasis (topology_eq α)
  simp_rw [lowerBasis, coe_upperClosure, compl_iUnion]
  ext s
  constructor
  · rintro ⟨F, hF, rfl⟩
    refine ⟨(fun a => (Ici a)ᶜ) '' F, ⟨hF.image _, image_subset_iff.2 fun _ _ => ⟨_, rfl⟩⟩, ?_⟩
    simp only [sInter_image]
  · rintro ⟨F, ⟨hF, hs⟩, rfl⟩
    have := hF.to_subtype
    rw [subset_def, Subtype.forall'] at hs
    choose f hf using hs
    exact ⟨_, finite_range f, by simp_rw [biInter_range, hf, sInter_eq_iInter]⟩

/-- A function `f : β → α` with lower topology in the codomain is continuous
if and only if the preimage of every interval `Set.Ici a` is a closed set.
-/
/-
**Topology.IsLower.continuous_iff_Ici** 是 Mathlib 中的一个引理，位于命名空间 `Topology.IsLowe
r`。
形式化陈述：continuous_iff_Ici [TopologicalSpace β] {f : β -> α} : Continuous f ↔ fora
ll a, IsClosed (f ⁻¹' (Ici a))
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用引理 `Topology.IsLower.topology_eq`：topology_eq : ‹_› = lower α

--- 原说明 ---
A function `f : β → α` with lower topology in the codomain is continuous
if and only if the preimage of every interval `Set.Ici a` is a closed set.
-/
lemma continuous_iff_Ici [TopologicalSpace β] {f : β → α} :
    Continuous f ↔ ∀ a, IsClosed (f ⁻¹' (Ici a)) := by
  obtain rfl := IsLower.topology_eq α
  simp [continuous_generateFrom_iff]

end Preorder

section PartialOrder

variable [PartialOrder α] [TopologicalSpace α] [IsLower α]

-- see Note [lower instance priority]
/-- The lower topology on a partial order is T₀. -/
/-
**Topology.IsLower.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.IsLower`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The lower topology on a partial order is T₀.
-/
instance (priority := 90) t0Space : T0Space α :=
  (t0Space_iff_inseparable α).2 fun x y h =>
    Ici_injective <| by simpa only [inseparable_iff_closure_eq, closure_singleton] using h

end PartialOrder

section LinearOrder

variable [LinearOrder α] [TopologicalSpace α] [IsLower α]

/-
**Topology.IsLower.isTopologicalBasis_insert_univ_subbasis** 是 Mathlib 中的一个引理，位于
命名空间 `Topology.IsLower`。
形式化陈述：isTopologicalBasis_insert_univ_subbasis : IsTopologicalBasis (insert univ 
{s : Set α | exists a, (Ici a)ᶜ = s})
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `TopologicalSpace.isTopologicalBasis_of_subbasis_of_inter`：isTopologicalB
asis_of_subbasis_of_inter {r : Set (Set α)} (hsg : t = generateFrom r) (hsi : fo
rall ⦃s⦄, s in r -> forall ⦃t⦄, t in r -> s in…
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `Topology.IsLower.topology_eq`：topology_eq : ‹_› = lower α
· 使用定理 `Topology.lower.eq_1`：∀ (α : Type u_1) [inst : Preorder α], Topology.lowe
r α = TopologicalSpace.generateFrom {s | ∃ a, (Set.Ici a)ᶜ = s}
· 使用定理 `Set.compl_Ici`：∀ {α : Type u_1} [inst : LinearOrder α] {a : α}, (Set.Ici
 a)ᶜ = Set.Iio a
· 使用定理 `Set.Iio_inter_Iio`：∀ {α : Type u_1} [inst : LinearOrder α] {a b : α}, Se
t.Iio a ∩ Set.Iio b = Set.Iio (min a b)
-/
lemma isTopologicalBasis_insert_univ_subbasis :
    IsTopologicalBasis (insert univ {s : Set α | ∃ a, (Ici a)ᶜ = s}) :=
  isTopologicalBasis_of_subbasis_of_inter (by rw [topology_eq α, lower]) (by
    rintro _ ⟨b, rfl⟩ _ ⟨c, rfl⟩
    use b ⊓ c
    rw [compl_Ici, compl_Ici, compl_Ici, Iio_inter_Iio])
/-
**Topology.IsLower.tendsto_nhds_iff_lt** 是 Mathlib 中的一个定理，位于命名空间 `Topology.IsLow
er`。
形式化陈述：tendsto_nhds_iff_lt {β : Type*} {f : β -> α} {l : Filter β} {x : α} : Filt
er.Tendsto f l (𝓝 x) ↔ forall y, x < y -> forallᶠ z in l, f z < y
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
-/
theorem tendsto_nhds_iff_lt {β : Type*} {f : β → α} {l : Filter β} {x : α} :
    Filter.Tendsto f l (𝓝 x) ↔ ∀ y, x < y → ∀ᶠ z in l, f z < y := by
  simp only [tendsto_nhds_iff_not_le, not_le]

end LinearOrder

section CompleteLinearOrder

variable [CompleteLinearOrder α] [t : TopologicalSpace α] [IsLower α]

/-
**Topology.IsLower.isTopologicalSpace_basis** 是 Mathlib 中的一个引理，位于命名空间 `Topology.
IsLower`。
形式化陈述：isTopologicalSpace_basis (U : Set α) : IsOpen U ↔ U = univ ∨ exists a, (Ic
i a)ᶜ = U
参数：U : Set α。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Set.compl_Ici`：∀ {α : Type u_1} [inst : LinearOrder α] {a : α}, (Set.Ici
 a)ᶜ = Set.Iio a
· 使用定理 `true_or`：∀ (p : Prop), (True ∨ p) = True
· 使用定理 `iff_self`：∀ (p : Prop), (p ↔ p) = True
· 使用定理 `subset_trans`：∀ {α : Type u_1} [UsesSetNotationForOrder α] [inst : Preor
der α] {a b c : α}, a ⊆ b → b ⊆ c → a ⊆ c
· 使用定理 `Iff.mpr`：∀ {a b : Prop}, (a ↔ b) → b → a
· 使用定理 `Set.singleton_subset_iff`：singleton_subset_iff {a : α} {s : Set α} : {a}
 subseteq s ↔ a in s
· 使用定理 `Set.subset_insert`：subset_insert (x : α) (s : Set α) : s subseteq insert
 x s
· 使用定理 `Set.sUnion_singleton`：sUnion_singleton (s : Set α) : ⋃₀ {s} = s
· 使用定理 `Set.sUnion_eq_univ_iff`：sUnion_eq_univ_iff {c : Set (Set α)} : ⋃₀ c = un
iv ↔ forall a, exists b in c, a in b
· 使用定理 `trivial`：True
· 使用定理 `Set.sUnion_eq_compl_sInter_compl`：sUnion_eq_compl_sInter_compl (S : Set 
(Set α)) : ⋃₀ S = (⋂₀ (compl '' S))ᶜ
· 使用定理 `compl_inj_iff`：compl_inj_iff : xᶜ = yᶜ ↔ x = y
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `Set.sInter_image`：sInter_image (f : α -> Set β) (s : Set α) : ⋂₀ (f '' s
) = ⋂ a in s, f a
· 使用定理 `forall_congr`：∀ {α : Sort u} {p q : α → Prop}, (∀ (a : α), p a = q a) → 
(∀ (a : α), p a) = ∀ (a : α), q a
· 使用定理 `implies_congr`：∀ {p₁ p₂ : Sort u} {q₁ q₂ : Sort v}, p₁ = p₂ → q₁ = q₂ → 
(p₁ → q₁) = (p₂ → q₂)
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `Set.subset_insert_iff_of_notMem`：subset_insert_iff_of_notMem (ha : a ∉ s
) : s subseteq insert a t ↔ s subseteq t
· 使用定理 `eq_true`：∀ {p : Prop}, p → p = True
· 使用定理 `Eq.symm`：∀ {α : Sort u} {a b : α}, a = b → b = a
· 使用定理 `Set.mem_Ici`：∀ {α : Type u_1} [inst : Preorder α] {b x : α}, x ∈ Set.Ici
 b ↔ b ≤ x
· 使用定理 `sSup_le_iff`：sSup_le_iff : sSup s <= a ↔ forall b in s, b <= a
· 使用定理 `not_lt`：∀ {α : Type u_1} [inst : LinearOrder α] {a b : α}, ¬a < b ↔ b ≤ 
a
（共 34 条，此处仅展示前 30 条）
-/
lemma isTopologicalSpace_basis (U : Set α) : IsOpen U ↔ U = univ ∨ ∃ a, (Ici a)ᶜ = U := by
  by_cases hU : U = univ
  · simp only [hU, isOpen_univ, compl_Ici, true_or]
  refine ⟨?_, isTopologicalBasis_insert_univ_subbasis.isOpen⟩
  intro hO
  apply Or.inr
  convert! IsTopologicalBasis.open_eq_sUnion isTopologicalBasis_insert_univ_subbasis hO
  constructor
  · intro ⟨a, ha⟩
    use {U}
    constructor
    · apply subset_trans (singleton_subset_iff.mpr _) (subset_insert _ _)
      use a
    · rw [sUnion_singleton]
  · intro ⟨S, hS1, hS2⟩
    have hUS : univ ∉ S := by
      by_contra hUS'
      apply hU
      rw [hS2]
      exact sUnion_eq_univ_iff.mpr (fun a => ⟨univ, hUS', trivial⟩)
    use sSup {a | (Ici a)ᶜ ∈ S}
    rw [hS2, sUnion_eq_compl_sInter_compl, compl_inj_iff]
    apply le_antisymm
    · intro b hb
      simp only [sInter_image, mem_iInter, mem_compl_iff]
      intro s hs
      obtain ⟨a, ha⟩ := (subset_insert_iff_of_notMem hUS).mp hS1 hs
      subst hS2 ha
      simp_all only [compl_Ici, mem_Ici, sSup_le_iff, mem_ofPred_eq, mem_Iio, not_lt]
    · intro b hb
      rw [mem_Ici, sSup_le_iff]
      intro c hc
      simp only [sInter_image, mem_iInter] at hb
      rw [← not_lt, ← mem_Iio, ← compl_Ici]
      exact hb _ hc

end CompleteLinearOrder

end IsLower


namespace IsUpper

/-- The complements of the lower closures of finite sets are a collection of upper sets
which form a basis for the upper topology. -/
/-
**Topology.IsUpper.upperBasis** 是 Mathlib 中的一个定义，位于命名空间 `Topology.IsUpper`。
形式化陈述：upperBasis (α : Type*) [Preorder α]
参数：α : Type*。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The complements of the lower closures of finite sets are a collection of upper s
ets
which form a basis for the upper topology.
-/
def upperBasis (α : Type*) [Preorder α] :=
  { s : Set α | ∃ t : Set α, t.Finite ∧ (lowerClosure t : Set α)ᶜ = s }

section Preorder

variable (α)
variable [Preorder α] [TopologicalSpace α] [IsUpper α] {s : Set α}

/-
**Topology.IsUpper.topology_eq** 是 Mathlib 中的一个引理，位于命名空间 `Topology.IsUpper`。
形式化陈述：topology_eq : ‹_› = upper α
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Topology.IsUpper.topology_eq_upperTopology`：∀ {α : Type u_3} {t : Topolo
gicalSpace α} {inst : Preorder α} [self : Topology.IsUpper α], t = Topology.uppe
r α
-/
lemma topology_eq : ‹_› = upper α := topology_eq_upperTopology

variable {α}

/-- If `α` is equipped with the upper topology, then it is homeomorphic to `WithUpper α`.
-/
/-
**Topology.IsUpper.withUpperHomeomorph** 是 Mathlib 中的一个定义，位于命名空间 `Topology.IsUpp
er`。
形式化陈述：withUpperHomeomorph : WithUpper α ≃ₜ α
该定义给出了上述对象。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
If `α` is equipped with the upper topology, then it is homeomorphic to `WithUppe
r α`.
-/
def withUpperHomeomorph : WithUpper α ≃ₜ α :=
  WithUpper.ofUpper.toHomeomorphOfIsInducing ⟨topology_eq α ▸ induced_id.symm⟩
/-
**Topology.IsUpper.isOpen_iff_generate_Iic_compl** 是 Mathlib 中的一个定理，位于命名空间 `Topo
logy.IsUpper`。
形式化陈述：isOpen_iff_generate_Iic_compl : IsOpen s ↔ GenerateOpen { t | exists a, (I
ic a)ᶜ = t } s
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用引理 `Topology.IsUpper.topology_eq`：topology_eq : ‹_› = upper α
· 使用定理 `Iff.rfl`：∀ {a : Prop}, a ↔ a
-/
theorem isOpen_iff_generate_Iic_compl : IsOpen s ↔ GenerateOpen { t | ∃ a, (Iic a)ᶜ = t } s := by
  rw [topology_eq α]; rfl
/-
**Topology.IsUpper._root_.OrderDual.instIsLower** 是 Mathlib 中的一个实例，位于命名空间 `Topol
ogy.IsUpper`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance _root_.OrderDual.instIsLower : IsLower αᵒᵈ where
  topology_eq_lowerTopology := topology_eq_upperTopology (α := α)

/-- Left-infinite right-closed intervals $(-∞,a]$ are closed in the upper topology. -/
/-
**Topology.IsUpper.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.IsUpper`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
Left-infinite right-closed intervals $(-∞,a]$ are closed in the upper topology.
-/
instance : ClosedIicTopology α :=
  ⟨fun a ↦ isOpen_compl_iff.1 <| isOpen_iff_generate_Iic_compl.2 <| GenerateOpen.basic _ ⟨a, rfl⟩⟩

/-- The lower closure of a finite set is closed in the upper topology. -/
/-
**Topology.IsUpper.isClosed_lowerClosure** 是 Mathlib 中的一个定理，位于命名空间 `Topology.IsU
pper`。
形式化陈述：isClosed_lowerClosure (h : s.Finite) : IsClosed (lowerClosure s : Set α)
参数：h : s.Finite。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Topology.IsLower.isClosed_upperClosure`：isClosed_upperClosure (h : s.Fin
ite) : IsClosed (upperClosure s : Set α)
· 使用定理 `OrderDual.instIsLower`：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : To
pologicalSpace α] [Topology.IsUpper α], Topology.IsLower αᵒᵈ

--- 原说明 ---
The lower closure of a finite set is closed in the upper topology.
-/
theorem isClosed_lowerClosure (h : s.Finite) : IsClosed (lowerClosure s : Set α) :=
  IsLower.isClosed_upperClosure (α := αᵒᵈ) h

/-- Every set open in the upper topology is an upper set. -/
/-
**Topology.IsUpper.isUpperSet_of_isOpen** 是 Mathlib 中的一个定理，位于命名空间 `Topology.IsUp
per`。
形式化陈述：isUpperSet_of_isOpen (h : IsOpen s) : IsUpperSet s
参数：h : IsOpen s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Topology.IsLower.isLowerSet_of_isOpen`：isLowerSet_of_isOpen (h : IsOpen 
s) : IsLowerSet s
· 使用定理 `OrderDual.instIsLower`：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : To
pologicalSpace α] [Topology.IsUpper α], Topology.IsLower αᵒᵈ

--- 原说明 ---
Every set open in the upper topology is an upper set.
-/
theorem isUpperSet_of_isOpen (h : IsOpen s) : IsUpperSet s :=
  IsLower.isLowerSet_of_isOpen (α := αᵒᵈ) h
/-
**Topology.IsUpper.isLowerSet_of_isClosed** 是 Mathlib 中的一个定理，位于命名空间 `Topology.Is
Upper`。
形式化陈述：isLowerSet_of_isClosed (h : IsClosed s) : IsLowerSet s
参数：h : IsClosed s。
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.mp`：∀ {a b : Prop}, (a ↔ b) → a → b
· 使用定理 `isUpperSet_compl`：isUpperSet_compl : IsUpperSet sᶜ ↔ IsLowerSet s
· 使用定理 `Topology.IsUpper.isUpperSet_of_isOpen`：isUpperSet_of_isOpen (h : IsOpen 
s) : IsUpperSet s
· 使用定理 `IsClosed.isOpen_compl`：∀ {X : Type u} {inst : TopologicalSpace X} {s : S
et X} [self : IsClosed s], IsOpen sᶜ
-/
theorem isLowerSet_of_isClosed (h : IsClosed s) : IsLowerSet s :=
  isUpperSet_compl.1 <| isUpperSet_of_isOpen h.isOpen_compl
/-
**Topology.IsUpper.tendsto_nhds_iff_not_le** 是 Mathlib 中的一个定理，位于命名空间 `Topology.I
sUpper`。
形式化陈述：tendsto_nhds_iff_not_le {β : Type*} {f : β -> α} {l : Filter β} {x : α} : 
Filter.Tendsto f l (𝓝 x) ↔ forall y, ¬x <= y -> forallᶠ z in l, ¬f z <= y
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Topology.IsLower.tendsto_nhds_iff_not_le`：tendsto_nhds_iff_not_le {β : T
ype*} {f : β -> α} {l : Filter β} {x : α} : Filter.Tendsto f l (𝓝 x) ↔ forall y,
 ¬y <= x -> forallᶠ z in l, ¬y…
· 使用定理 `OrderDual.instIsLower`：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : To
pologicalSpace α] [Topology.IsUpper α], Topology.IsLower αᵒᵈ
-/
theorem tendsto_nhds_iff_not_le {β : Type*} {f : β → α} {l : Filter β} {x : α} :
    Filter.Tendsto f l (𝓝 x) ↔ ∀ y, ¬x ≤ y → ∀ᶠ z in l, ¬f z ≤ y :=
  IsLower.tendsto_nhds_iff_not_le (α := αᵒᵈ)

/--
The closure of a singleton `{a}` in the upper topology is the left-infinite right-closed interval
$(-∞,a]$.
-/
@[simp]
/-
**Topology.IsUpper.closure_singleton** 是 Mathlib 中的一个定理，位于命名空间 `Topology.IsUpper
`。
形式化陈述：closure_singleton (a : α) : closure {a} = Iic a
参数：a : α。
该定理/引理给出了一组等式。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Topology.IsLower.closure_singleton`：closure_singleton (a : α) : closure 
{a} = Ici a
· 使用定理 `OrderDual.instIsLower`：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : To
pologicalSpace α] [Topology.IsUpper α], Topology.IsLower αᵒᵈ

--- 原说明 ---
The closure of a singleton `{a}` in the upper topology is the left-infinite righ
t-closed interval
$(-∞,a]$.
-/
theorem closure_singleton (a : α) : closure {a} = Iic a :=
  IsLower.closure_singleton (α := αᵒᵈ) _
/-
**Topology.IsUpper.isTopologicalBasis** 是 Mathlib 中的一个定理，位于命名空间 `Topology.IsUppe
r`。
形式化陈述：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : TopologicalSpace α] [Topolo
gy.IsUpper α],   TopologicalSpace.IsTopologicalBasis (Topology.IsUpper.upperBasi
s α)
参数：Topology.IsUpper.upperBasis α。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Topology.IsLower.isTopologicalBasis`：∀ {α : Type u_1} [inst : Preorder α
] [inst_1 : TopologicalSpace α] [Topology.IsLower α],   TopologicalSpace.IsTopol
ogicalBasis (Topology.IsL…
· 使用定理 `OrderDual.instIsLower`：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : To
pologicalSpace α] [Topology.IsUpper α], Topology.IsLower αᵒᵈ
-/
protected theorem isTopologicalBasis : IsTopologicalBasis (upperBasis α) :=
  IsLower.isTopologicalBasis (α := αᵒᵈ)

/-- A function `f : β → α` with upper topology in the codomain is continuous
if and only if the preimage of every interval `Set.Iic a` is a closed set. -/
/-
**Topology.IsUpper.continuous_iff_Iic** 是 Mathlib 中的一个引理，位于命名空间 `Topology.IsUppe
r`。
形式化陈述：continuous_iff_Iic [TopologicalSpace β] {f : β -> α} : Continuous f ↔ fora
ll a, IsClosed (f ⁻¹' (Iic a))
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `Topology.IsLower.continuous_iff_Ici`：continuous_iff_Ici [TopologicalSpac
e β] {f : β -> α} : Continuous f ↔ forall a, IsClosed (f ⁻¹' (Ici a))
· 使用定理 `OrderDual.instIsLower`：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : To
pologicalSpace α] [Topology.IsUpper α], Topology.IsLower αᵒᵈ

--- 原说明 ---
A function `f : β → α` with upper topology in the codomain is continuous
if and only if the preimage of every interval `Set.Iic a` is a closed set.
-/
lemma continuous_iff_Iic [TopologicalSpace β] {f : β → α} :
    Continuous f ↔ ∀ a, IsClosed (f ⁻¹' (Iic a)) :=
  IsLower.continuous_iff_Ici (α := αᵒᵈ)

end Preorder


section PartialOrder

variable [PartialOrder α] [TopologicalSpace α] [IsUpper α]

-- see Note [lower instance priority]
/-- The upper topology on a partial order is T₀. -/
/-
**Topology.IsUpper.** 是 Mathlib 中的一个实例，位于命名空间 `Topology.IsUpper`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The upper topology on a partial order is T₀.
-/
instance (priority := 90) t0Space : T0Space α :=
  IsLower.t0Space (α := αᵒᵈ)

end PartialOrder

section LinearOrder

variable [LinearOrder α] [TopologicalSpace α] [IsUpper α]

/-
**Topology.IsUpper.isTopologicalBasis_insert_univ_subbasis** 是 Mathlib 中的一个引理，位于
命名空间 `Topology.IsUpper`。
形式化陈述：isTopologicalBasis_insert_univ_subbasis : IsTopologicalBasis (insert univ 
{s : Set α | exists a, (Iic a)ᶜ = s})
该定理/引理描述了相关对象所满足的性质。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `Topology.IsLower.isTopologicalBasis_insert_univ_subbasis`：isTopologicalB
asis_insert_univ_subbasis : IsTopologicalBasis (insert univ {s : Set α | exists 
a, (Ici a)ᶜ = s})
· 使用定理 `OrderDual.instIsLower`：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : To
pologicalSpace α] [Topology.IsUpper α], Topology.IsLower αᵒᵈ
-/
lemma isTopologicalBasis_insert_univ_subbasis :
    IsTopologicalBasis (insert univ {s : Set α | ∃ a, (Iic a)ᶜ = s}) :=
  IsLower.isTopologicalBasis_insert_univ_subbasis (α := αᵒᵈ)
/-
**Topology.IsUpper.tendsto_nhds_iff_lt** 是 Mathlib 中的一个定理，位于命名空间 `Topology.IsUpp
er`。
形式化陈述：tendsto_nhds_iff_lt {β : Type*} {f : β -> α} {l : Filter β} {x : α} : Filt
er.Tendsto f l (𝓝 x) ↔ forall y < x, forallᶠ z in l, y < f z
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Topology.IsLower.tendsto_nhds_iff_lt`：tendsto_nhds_iff_lt {β : Type*} {f
 : β -> α} {l : Filter β} {x : α} : Filter.Tendsto f l (𝓝 x) ↔ forall y, x < y -
> forallᶠ z in l, f z < y
· 使用定理 `OrderDual.instIsLower`：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : To
pologicalSpace α] [Topology.IsUpper α], Topology.IsLower αᵒᵈ
-/
theorem tendsto_nhds_iff_lt {β : Type*} {f : β → α} {l : Filter β} {x : α} :
    Filter.Tendsto f l (𝓝 x) ↔ ∀ y < x, ∀ᶠ z in l, y < f z :=
  IsLower.tendsto_nhds_iff_lt (α := αᵒᵈ)

end LinearOrder

section CompleteLinearOrder

variable [CompleteLinearOrder α] [t : TopologicalSpace α] [IsUpper α]

/-
**Topology.IsUpper.isTopologicalSpace_basis** 是 Mathlib 中的一个引理，位于命名空间 `Topology.
IsUpper`。
形式化陈述：isTopologicalSpace_basis (U : Set α) : IsOpen U ↔ U = univ ∨ exists a, (Ii
c a)ᶜ = U
参数：U : Set α。
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用引理 `Topology.IsLower.isTopologicalSpace_basis`：isTopologicalSpace_basis (U :
 Set α) : IsOpen U ↔ U = univ ∨ exists a, (Ici a)ᶜ = U
· 使用定理 `OrderDual.instIsLower`：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : To
pologicalSpace α] [Topology.IsUpper α], Topology.IsLower αᵒᵈ
-/
lemma isTopologicalSpace_basis (U : Set α) : IsOpen U ↔ U = univ ∨ ∃ a, (Iic a)ᶜ = U :=
  IsLower.isTopologicalSpace_basis (α := αᵒᵈ) U

end CompleteLinearOrder

end IsUpper

/-
**Topology.instIsLowerProd** 是 Mathlib 中的一个实例，位于命名空间 `Topology`。
形式化陈述：instIsLowerProd [Preorder α] [TopologicalSpace α] [IsLower α] [OrderBot α]
 [Preorder β] [TopologicalSpace β] [IsLower β] [OrderBot β] : IsLower (α × β) wh
ere topology_eq_lowerTopology
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用引理 `le_antisymm`：le_antisymm : a <= b -> b <= a -> a = b
· 使用定理 `le_generateFrom`：le_generateFrom {t : TopologicalSpace α} {g : Set (Set 
α)} (h : forall s in g, IsOpen s) : t <= generateFrom g
· 使用定理 `IsClosed.isOpen_compl`：∀ {X : Type u} {inst : TopologicalSpace X} {s : S
et X} [self : IsClosed s], IsOpen sᶜ
· 使用定理 `IsClosed.prod`：IsClosed.prod {s₁ : Set X} {s₂ : Set Y} (h₁ : IsClosed s₁
) (h₂ : IsClosed s₂) : IsClosed (s₁ ×ˢ s₂)
· 使用定理 `isClosed_Ici`：∀ {α : Type u} [inst : TopologicalSpace α] [inst_1 : Preor
der α] [ClosedIciTopology α] {a : α}, IsClosed (Set.Ici a)
· 使用定理 `Topology.IsLower.instClosedIciTopology`：∀ {α : Type u_1} [inst : Preorde
r α] [inst_1 : TopologicalSpace α] [Topology.IsLower α], ClosedIciTopology α
· 使用定理 `congrArg`：∀ {α : Sort u} {β : Sort v} {a₁ a₂ : α} (f : α → β), a₁ = a₂ →
 f a₁ = f a₂
· 使用定理 `TopologicalSpace.IsTopologicalBasis.eq_generateFrom`：∀ {α : Type u} [t :
 TopologicalSpace α] {s : Set (Set α)},   TopologicalSpace.IsTopologicalBasis s 
→ t = TopologicalSpace.generateFrom s
· 使用定理 `TopologicalSpace.IsTopologicalBasis.prod`：∀ {α : Type u} {β : Type u_1} 
[t : TopologicalSpace α] [inst : TopologicalSpace β] {B₁ : Set (Set α)}   {B₂ : 
Set (Set β)},   TopologicalSpa…
· 使用定理 `Topology.IsLower.isTopologicalBasis`：∀ {α : Type u_1} [inst : Preorder α
] [inst_1 : TopologicalSpace α] [Topology.IsLower α],   TopologicalSpace.IsTopol
ogicalBasis (Topology.IsL…
· 使用定理 `TopologicalSpace.le_generateFrom_iff_subset_isOpen`：le_generateFrom_iff_
subset_isOpen {g : Set (Set α)} {t : TopologicalSpace α} : t <= generateFrom g ↔
 g subseteq { s | IsOpen[t] s }
· 使用定理 `Set.image2_subset_iff`：image2_subset_iff {u : Set γ} : image2 f s t subs
eteq u ↔ forall x in s, forall y in t, f x y in u
· 使用定理 `congr`：∀ {α : Sort u} {β : Sort v} {f₁ f₂ : α → β} {a₁ a₂ : α}, f₁ = f₂ 
→ a₁ = a₂ → f₁ a₁ = f₂ a₂
· 使用定理 `coe_upperClosure`：coe_upperClosure (s : Set α) : ↑(upperClosure s) = ⋃ a
 in s, Ici a
· 使用定理 `Eq.trans`：∀ {α : Sort u} {a b c : α}, a = b → b = c → a = c
· 使用定理 `Set.compl_iUnion`：compl_iUnion (s : ι -> Set β) : (⋃ i, s i)ᶜ = ⋂ i, (s 
i)ᶜ
· 使用定理 `funext`：∀ {α : Sort u} {β : α → Sort v} {f g : (x : α) → β x}, (∀ (x : α
), f x = g x) → f = g
· 使用定理 `Set.preimage_iInter`：preimage_iInter {f : α -> β} {s : ι -> Set β} : (f 
⁻¹' ⋂ i, s i) = ⋂ i, f ⁻¹' s i
· 使用定理 `Set.iInter_congr_Prop`：iInter_congr_Prop {p q : Prop} {f₁ : p -> Set α} 
{f₂ : q -> Set α} (pq : p ↔ q) (f : forall x, f₁ (pq.mpr x) = f₂ x) : iInter f₁ 
= iInter f₂
· 使用定理 `Iff.of_eq`：∀ {a b : Prop}, a = b → (a ↔ b)
· 使用定理 `IsOpen.inter`：IsOpen.inter (s t : Set α) : IsOpen α s -> IsOpen α t -> I
sOpen α (s inter t)
· 使用定理 `Set.Finite.isOpen_biInter`：Set.Finite.isOpen_biInter {s : Set α} {f : α 
-> Set X} (hs : s.Finite) (h : forall i in s, IsOpen (f i)) : IsOpen (⋂ i in s, 
f i)
· 使用定理 `of_eq_true`：∀ {p : Prop}, p = True → p
· 使用定理 `congrFun'`：∀ {α : Sort u} {β : Sort v} {f g : α → β}, f = g → ∀ (a : α),
 f a = g a
· 使用定理 `Set.Ici_bot`：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : OrderBot α],
 Set.Ici ⊥ = Set.univ
· 使用定理 `Set.prod_univ`：prod_univ {s : Set α} : s ×ˢ (univ : Set β) = Prod.fst ⁻¹
' s
· 使用定理 `eq_self`：∀ {α : Sort u_1} (a : α), (a = a) = True
· 使用定理 `Set.univ_prod`：univ_prod {t : Set β} : (univ : Set α) ×ˢ t = Prod.snd ⁻¹
' t
-/
instance instIsLowerProd [Preorder α] [TopologicalSpace α] [IsLower α]
    [OrderBot α] [Preorder β] [TopologicalSpace β] [IsLower β] [OrderBot β] :
    IsLower (α × β) where
  topology_eq_lowerTopology := by
    refine le_antisymm (le_generateFrom ?_) ?_
    · rintro _ ⟨x, rfl⟩
      exact (isClosed_Ici.prod isClosed_Ici).isOpen_compl
    rw [(IsLower.isTopologicalBasis.prod
      IsLower.isTopologicalBasis).eq_generateFrom, le_generateFrom_iff_subset_isOpen,
      image2_subset_iff]
    rintro _ ⟨s, hs, rfl⟩ _ ⟨t, ht, rfl⟩
    dsimp
    simp_rw [coe_upperClosure, compl_iUnion, prod_eq, preimage_iInter, preimage_compl]
    -- without `let`, `refine` tries to use the product topology and fails
    let _ : TopologicalSpace (α × β) := lower (α × β)
    refine (hs.isOpen_biInter fun a _ => ?_).inter (ht.isOpen_biInter fun b _ => ?_)
    · exact GenerateOpen.basic _ ⟨(a, ⊥), by simp [Ici_prod_eq, prod_univ]⟩
    · exact GenerateOpen.basic _ ⟨(⊥, b), by simp [Ici_prod_eq, univ_prod]⟩
/-
**Topology.instIsUpperProd** 是 Mathlib 中的一个实例，位于命名空间 `Topology`。
形式化陈述：instIsUpperProd [Preorder α] [TopologicalSpace α] [IsUpper α] [OrderTop α]
 [Preorder β] [TopologicalSpace β] [IsUpper β] [OrderTop β] : IsUpper (α × β) wh
ere topology_eq_upperTopology
该定义给出了上述对象。
本声明引用了以下数学事实（定理与引理）：
· 使用定理 `OrderDual.instIsLower`：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : To
pologicalSpace α] [Topology.IsUpper α], Topology.IsLower αᵒᵈ
· 使用定理 `Topology.IsLower.topology_eq_lowerTopology`：∀ {α : Type u_3} {t : Topolo
gicalSpace α} {inst : Preorder α} [self : Topology.IsLower α], t = Topology.lowe
r α
-/
instance instIsUpperProd [Preorder α] [TopologicalSpace α] [IsUpper α]
    [OrderTop α] [Preorder β] [TopologicalSpace β] [IsUpper β] [OrderTop β] :
    IsUpper (α × β) where
  topology_eq_upperTopology := by
    suffices IsLower (α × β)ᵒᵈ from IsLower.topology_eq_lowerTopology (α := (α × β)ᵒᵈ)
    exact instIsLowerProd (α := αᵒᵈ) (β := βᵒᵈ)

section CompleteLattice_IsLower

variable [CompleteLattice α] [CompleteLattice β] [TopologicalSpace α] [IsLower α]
  [TopologicalSpace β] [IsLower β]

/-
**Topology._root_.sInfHom.continuous** 是 Mathlib 中的一个引理，位于命名空间 `Topology`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
protected lemma _root_.sInfHom.continuous (f : sInfHom α β) : Continuous f := by
  refine IsLower.continuous_iff_Ici.2 fun b => ?_
  convert! isClosed_Ici (a := sInf <| f ⁻¹' Ici b)
  refine Subset.antisymm (fun a => sInf_le) fun a ha => le_trans ?_ <|
    OrderHomClass.mono (f : α →o β) ha
  refine LE.le.trans ?_ (map_sInf f _).ge
  simp

-- see Note [lower instance priority]
/-
**Topology.** 是 Mathlib 中的一个实例，位于命名空间 `Topology`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 90) IsLower.toContinuousInf : ContinuousInf α :=
  ⟨(infsInfHom : sInfHom (α × α) α).continuous⟩

end CompleteLattice_IsLower

section CompleteLattice_IsUpper

variable [CompleteLattice α] [CompleteLattice β] [TopologicalSpace α] [IsUpper α]
  [TopologicalSpace β] [IsUpper β]

/-
**Topology._root_.sSupHom.continuous** 是 Mathlib 中的一个引理，位于命名空间 `Topology`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
protected lemma _root_.sSupHom.continuous (f : sSupHom α β) : Continuous f :=
  sInfHom.continuous (α := αᵒᵈ) (β := βᵒᵈ) (sSupHom.dual.toFun f)

-- see Note [lower instance priority]
/-
**Topology.** 是 Mathlib 中的一个实例，位于命名空间 `Topology`。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。
-/
instance (priority := 90) IsUpper.toContinuousInf : ContinuousSup α :=
  ⟨(supsSupHom : sSupHom (α × α) α).continuous⟩

end CompleteLattice_IsUpper

/-
**Topology.isUpper_orderDual** 是 Mathlib 中的一个引理，位于命名空间 `Topology`。
形式化陈述：isUpper_orderDual [Preorder α] [TopologicalSpace α] : IsUpper αᵒᵈ ↔ IsLowe
r α
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `OrderDual.instIsLower`：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : To
pologicalSpace α] [Topology.IsUpper α], Topology.IsLower αᵒᵈ
· 使用定理 `OrderDual.instIsUpper`：∀ {α : Type u_1} [inst : Preorder α] [inst_1 : To
pologicalSpace α] [Topology.IsLower α], Topology.IsUpper αᵒᵈ
-/
lemma isUpper_orderDual [Preorder α] [TopologicalSpace α] : IsUpper αᵒᵈ ↔ IsLower α := by
  constructor
  · apply OrderDual.instIsLower
  · apply OrderDual.instIsUpper
/-
**Topology.isLower_orderDual** 是 Mathlib 中的一个引理，位于命名空间 `Topology`。
形式化陈述：isLower_orderDual [Preorder α] [TopologicalSpace α] : IsLower αᵒᵈ ↔ IsUppe
r α
该定理/引理刻画了左右两侧的等价关系。
黑盒证明引用了以下数学事实（定理与引理）：
· 使用定理 `Iff.symm`：∀ {a b : Prop}, (a ↔ b) → (b ↔ a)
· 使用引理 `Topology.isUpper_orderDual`：isUpper_orderDual [Preorder α] [TopologicalS
pace α] : IsUpper αᵒᵈ ↔ IsLower α
-/
lemma isLower_orderDual [Preorder α] [TopologicalSpace α] : IsLower αᵒᵈ ↔ IsUpper α :=
  isUpper_orderDual.symm

end Topology

/-- The Sierpiński topology on `Prop` is the upper topology -/
/-
**** 是 Mathlib 中的一个实例，位于命名空间 ``。
黑盒内容：本声明未引用其他定理/引理；其成立仅依赖定义、结构与类型类实例。

--- 原说明 ---
The Sierpiński topology on `Prop` is the upper topology
-/
instance : IsUpper Prop where
  topology_eq_upperTopology := by
    rw [Topology.upper, sierpinskiSpace, ← generateFrom_insert_empty]
    congr
    exact le_antisymm
      (fun h hs => by
        simp only [compl_Iic, mem_ofPred_eq]
        rw [← Ioi_True, ← Ioi_False] at hs
        rcases hs with (rfl | rfl)
        · use True
        · use False)
      (by rintro _ ⟨a, rfl⟩; by_cases a <;> aesop (add simp [Ioi, lt_iff_le_not_ge]))
